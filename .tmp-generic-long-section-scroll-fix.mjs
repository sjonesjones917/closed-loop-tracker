import fs from 'node:fs';
import {createHash} from 'node:crypto';

const hashPath='hash.js';
let hash=fs.readFileSync(hashPath,'utf8');

const reviewBottomLine="  const reviewBottomJump=matchedScrollJump('review-bottom-jump','↓ Bottom of review','Scroll to bottom of proposal review',18,()=>scrollToMatchedTarget(document.getElementById('proposal-actions'),'end'));\n";
if(!hash.includes(reviewBottomLine))throw new Error('Expected review scroll-control declaration was not found.');
const genericControls=`  let activeLongSection=null;\n  function sectionSummary(section){\n    const first=section?.firstElementChild;\n    return first?.tagName==='SUMMARY'?first:section?.querySelector('summary');\n  }\n  function sectionDepth(section){\n    let depth=0,node=section;\n    while(node?.parentElement){depth++;node=node.parentElement;}\n    return depth;\n  }\n  function visibleLongSection(){\n    const candidates=[...document.querySelectorAll('details.record-card[open]')]\n      .filter(section=>!section.closest('#proposal-heading'))\n      .map(section=>({section,rect:section.getBoundingClientRect(),depth:sectionDepth(section)}))\n      .filter(({rect})=>rect.height>innerHeight&&rect.bottom>0&&rect.top<innerHeight)\n      .sort((a,b)=>b.depth-a.depth||Math.abs(a.rect.top)-Math.abs(b.rect.top));\n    return candidates[0]?.section||null;\n  }\n  const sectionTopJump=matchedScrollJump('section-top-jump','↑ Top of section','Scroll to top of long section',70,()=>scrollToMatchedTarget(sectionSummary(activeLongSection)||activeLongSection,'start'));\n  const sectionBottomJump=matchedScrollJump('section-bottom-jump','↓ Bottom of section','Scroll to bottom of long section',18,()=>scrollToMatchedTarget(activeLongSection,'end'));\n`;
hash=hash.replace(reviewBottomLine,reviewBottomLine+genericControls);

const syncStart="  function syncMatchedScrollJumps(){\n";
const syncEnd="  document.addEventListener('click',()=>queueMicrotask(syncMatchedScrollJumps));\n";
const startIndex=hash.indexOf(syncStart);
const endIndex=hash.indexOf(syncEnd,startIndex);
if(startIndex<0||endIndex<0)throw new Error('Expected matched-scroll synchronization block was not found.');
const newSync=`  function syncMatchedScrollJumps(){\n    const prompt=document.getElementById('generated-prompt');\n    const promptRect=prompt?.getBoundingClientRect();\n    const review=document.getElementById('proposal-heading');\n    const reviewRect=review?.getBoundingClientRect();\n    const longReview=Boolean(review&&[...review.querySelectorAll('details.record-card[open]')].some(details=>details.getBoundingClientRect().height>innerHeight));\n    const reviewActive=Boolean(longReview&&reviewRect&&reviewRect.bottom>0&&reviewRect.top<innerHeight);\n    const reviewPast=Boolean(longReview&&reviewRect&&reviewRect.bottom<=0);\n    const promptActive=Boolean(prompt?.classList.contains('expanded')&&prompt?.scrollHeight>innerHeight&&promptRect&&promptRect.bottom>0&&promptRect.top<innerHeight&&!reviewActive&&!reviewPast);\n    activeLongSection=!reviewActive&&!promptActive?visibleLongSection():null;\n    const sectionActive=Boolean(activeLongSection);\n    promptBottomJump().hidden=!promptActive;\n    promptTopJump.hidden=!promptActive;\n    reviewTopJump.hidden=!reviewActive;\n    reviewBottomJump.hidden=!reviewActive;\n    sectionTopJump.hidden=!sectionActive;\n    sectionBottomJump.hidden=!sectionActive;\n    if(sectionActive){\n      const name=String(sectionSummary(activeLongSection)?.textContent||'section').replace(/\\s+/g,' ').trim()||'section';\n      sectionTopJump.setAttribute('aria-label',\`Scroll to top of ${name}\`);\n      sectionBottomJump.setAttribute('aria-label',\`Scroll to bottom of ${name}\`);\n    }\n  }\n`;
hash=hash.slice(0,startIndex)+newSync+hash.slice(endIndex);
fs.writeFileSync(hashPath,hash);

const browserPath='verify-browser.mjs';
let browser=fs.readFileSync(browserPath,'utf8');
const acceptanceMarker="assert(receipt.acceptedCanonicalChangeId&&receipt.extractionManifestId,'Receipt does not link raw/proposal/change/manifest.');";
if(!browser.includes(acceptanceMarker))throw new Error('Expected accepted-response browser-test marker was not found.');
const genericTest=`\n // Every long open record section uses the same matched top/bottom controls, including provenance.\n const provenanceOpen=await evalValue(cdp,\`(()=>{const panel=[...document.querySelectorAll('.panel')].find(node=>node.querySelector(':scope > .section-title')?.textContent.trim()==='Provenance navigation'),section=panel?.querySelector(':scope > details.record-card');if(!section)return null;sectionSummary=section.querySelector(':scope > summary');sectionSummary?.click();return {records:section.querySelectorAll(':scope > .record-body > details.record-card').length};})()\`);assert(provenanceOpen&&provenanceOpen.records>8,\`Provenance section was not populated with a long traceable-value list: ${'${'}JSON.stringify(provenanceOpen)}\`);\n await waitExpr(cdp,\`(()=>{const panel=[...document.querySelectorAll('.panel')].find(node=>node.querySelector(':scope > .section-title')?.textContent.trim()==='Provenance navigation'),section=panel?.querySelector(':scope > details.record-card'),top=document.querySelector('#section-top-jump'),bottom=document.querySelector('#section-bottom-jump');return Boolean(section?.open&&section.getBoundingClientRect().height>innerHeight&&top&&!top.hidden&&bottom&&!bottom.hidden);})()\`,8000);\n const sectionPair=await evalValue(cdp,\`(()=>{const top=document.querySelector('#section-top-jump'),bottom=document.querySelector('#section-bottom-jump'),reference=document.querySelector('#prompt-bottom-jump'),a=top?.getBoundingClientRect(),b=bottom?.getBoundingClientRect(),sa=top&&getComputedStyle(top),sb=bottom&&getComputedStyle(bottom),sr=reference&&getComputedStyle(reference);return a&&b&&sr?{topText:top.textContent,bottomText:bottom.textContent,topBottom:a.bottom,bottomTop:b.top,rightA:a.right,rightB:b.right,backgroundA:sa.backgroundColor,backgroundB:sb.backgroundColor,backgroundR:sr.backgroundColor,radiusA:sa.borderRadius,radiusB:sb.borderRadius,radiusR:sr.borderRadius,colorA:sa.color,colorB:sb.color,colorR:sr.color,fontA:sa.font,fontB:sb.font,fontR:sr.font,shadowA:sa.boxShadow,shadowB:sb.boxShadow,shadowR:sr.boxShadow,minHeightA:sa.minHeight,minHeightB:sb.minHeight,minHeightR:sr.minHeight}:null;})()\`);assert(sectionPair&&sectionPair.topText==='↑ Top of section'&&sectionPair.bottomText==='↓ Bottom of section'&&sectionPair.topBottom<sectionPair.bottomTop&&Math.abs(sectionPair.rightA-sectionPair.rightB)<=1&&sectionPair.backgroundA===sectionPair.backgroundB&&sectionPair.backgroundA===sectionPair.backgroundR&&sectionPair.radiusA===sectionPair.radiusB&&sectionPair.radiusA===sectionPair.radiusR&&sectionPair.colorA===sectionPair.colorB&&sectionPair.colorA===sectionPair.colorR&&sectionPair.fontA===sectionPair.fontB&&sectionPair.fontA===sectionPair.fontR&&sectionPair.shadowA===sectionPair.shadowB&&sectionPair.shadowA===sectionPair.shadowR&&sectionPair.minHeightA===sectionPair.minHeightB&&sectionPair.minHeightA===sectionPair.minHeightR,\`Generic section scroll controls do not match the established controls: ${'${'}JSON.stringify(sectionPair)}\`);\n await click(cdp,'#section-bottom-jump');await waitExpr(cdp,\`(()=>{const panel=[...document.querySelectorAll('.panel')].find(node=>node.querySelector(':scope > .section-title')?.textContent.trim()==='Provenance navigation'),section=panel?.querySelector(':scope > details.record-card'),r=section?.getBoundingClientRect();return Boolean(r&&r.bottom>0&&r.bottom<=innerHeight+2);})()\`,8000);\n await click(cdp,'#section-top-jump');await waitExpr(cdp,\`(()=>{const panel=[...document.querySelectorAll('.panel')].find(node=>node.querySelector(':scope > .section-title')?.textContent.trim()==='Provenance navigation'),summary=panel?.querySelector(':scope > details.record-card > summary'),r=summary?.getBoundingClientRect();return Boolean(r&&r.bottom>0&&r.top<innerHeight);})()\`,8000);\n await evalValue(cdp,\`(()=>{const panel=[...document.querySelectorAll('.panel')].find(node=>node.querySelector(':scope > .section-title')?.textContent.trim()==='Provenance navigation'),summary=panel?.querySelector(':scope > details.record-card > summary');summary?.click();return true;})()\`);await waitExpr(cdp,\`(()=>{const panel=[...document.querySelectorAll('.panel')].find(node=>node.querySelector(':scope > .section-title')?.textContent.trim()==='Provenance navigation'),section=panel?.querySelector(':scope > details.record-card'),top=document.querySelector('#section-top-jump'),bottom=document.querySelector('#section-bottom-jump');return Boolean(section&&!section.open&&top?.hidden&&bottom?.hidden);})()\`);`;
browser=browser.replace(acceptanceMarker,acceptanceMarker+genericTest);
browser=browser.replace('extractionManifest:true,receiptLinkage:true,reloadPersistence:true','extractionManifest:true,receiptLinkage:true,genericLongSectionNavigation:true,reloadPersistence:true');
fs.writeFileSync(browserPath,browser);

const runtimeFiles=['workbook.js','hash.js','workflow-schema.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js'];
const gitBlobSha=file=>{const bytes=fs.readFileSync(file);return createHash('sha1').update(`blob ${bytes.length}\\0`).update(bytes).digest('hex');};
const runtimeManifest=runtimeFiles.map(file=>`${file}:${gitBlobSha(file)}\\n`).join('');
const runtimeBuildIdentity=`runtime-${createHash('sha256').update(runtimeManifest).digest('hex').slice(0,16)}`;
const indexPath='index.html';
let index=fs.readFileSync(indexPath,'utf8');
const tokens=[...index.matchAll(/<script\\s+defer\\s+src="[^"]+\\?v=([^"]+)"/g)].map(match=>match[1]);
if(tokens.length!==runtimeFiles.length||new Set(tokens).size!==1)throw new Error(`Expected one shared token across ${runtimeFiles.length} runtime scripts; found ${JSON.stringify(tokens)}.`);
index=index.replaceAll(tokens[0],runtimeBuildIdentity);
fs.writeFileSync(indexPath,index);
