import fs from 'node:fs';
const R=p=>fs.readFileSync(p,'utf8'), W=(p,s)=>fs.writeFileSync(p,s);
function one(t,a,b,label){const i=t.indexOf(a);if(i<0)throw new Error('Missing '+label);if(t.indexOf(a,i+1)>=0)throw new Error('Ambiguous '+label);return t.slice(0,i)+b+t.slice(i+a.length);}
function reOne(t,re,b,label){const m=[...t.matchAll(new RegExp(re.source,re.flags.includes('g')?re.flags:re.flags+'g'))];if(m.length!==1)throw new Error('Expected 1 '+label+', found '+m.length);return t.replace(re,b);}
function edit(p,fn){const a=R(p),b=fn(a);if(a===b)throw new Error('No change '+p);W(p,b);console.log('patched '+p);}

edit('workbook.js',t=>{
 t=one(t,"const PROJECT_SCHEMA='closed-loop-project/2';","const PROJECT_SCHEMA='closed-loop-project/3';",'project schema');
 t=one(t,"'REVISE THE RESPONSIBLE LAYER'","'CORRECT THE ROOT CAUSE'",'stage16 label');
 t=one(t,"1:['Exact user objective preserved verbatim','Every atomic statement from the original intent file is captured in the canonical intentStatements ledger','Explicit requirements and assumptions separated','Complete input set has a controlled identity and the original file is not needed later']","1:['Exact user objective preserved verbatim','Every controlled human-input unit is application-enumerated and accounted for in the canonical intent ledger','Every foreseeable human-only issue is supplied, asked and answered, or explicitly deferred','Explicit requirements, constraints, prohibitions, outputs, acceptance conditions, references, assumptions, and unknowns are preserved without loss','Complete input set has a controlled identity and the original file is not needed later']",'stage1 gate text');
 t=one(t,"3:['Every current accepted Stage 02 source has a research record','Every requirement-relevant canonical intent statement has a candidate requirement','Conflict, restriction, and exception pass complete','Latest complete pass found no new material requirement category']","3:['Every current accepted Stage 02 source has current research coverage','Every requirement-relevant canonical intent statement has a candidate requirement','Mandatory statements, recommendations, optional practices, examples, explanatory material, prohibitions, exceptions, dependencies, applicability facts, restrictions, and invalidating material are exhaustively covered','Second conflict and exception pass is complete','Latest complete pass found no new material category']",'stage3 gate text');
 t=one(t,"4:['Every requirement-relevant canonical intent statement maps to at least one atomic requirement','Every requirement has observable satisfaction and failure conditions','Every external requirement traces to exact evidence and every user-derived requirement traces to an exact STATEMENT_ID','Requirement registry has a controlled identity']","4:['The application-generated obligation manifest contains the complete current Stage 01 plus Stage 03 input union','Every obligation identity has one valid disposition','Every requirement-producing obligation maps to one or more atomic requirements','Every requirement has observable satisfaction and failure conditions','Every external requirement traces to exact evidence and every user-derived requirement traces to exact human provenance','No human is asked to resend information already captured','Requirement registry has a controlled identity']",'stage4 gate text');
 t=one(t,"1:['JOB_ID','JOB_TITLE','DATE_OPENED','JOB_OWNER','EXACT_USER_OBJECTIVE_VERBATIM','EXACT_DELIVERABLE_REQUESTED','SUPPLIED_MATERIALS_INVENTORY','REQUIRED_OUTPUT_FORMAT','DEADLINE_OR_TEMPORAL_SCOPE','DESIRED_SOURCE_COUNT','KNOWN_AUTHORITATIVE_SOURCES','AVAILABLE_TOOLS','PROHIBITED_ACTIONS','EXPLICIT_USER_REQUIREMENTS','ASSUMPTIONS','UNKNOWN_INFORMATION','INPUT_SET_VERSION','INPUT_SET_CONTENTS','INPUT_SET_HASH_OR_MANIFEST','JOB_RECORD_STATUS','STATUS_EVIDENCE']","1:['JOB_ID','JOB_TITLE','DATE_OPENED','JOB_OWNER','EXACT_USER_OBJECTIVE_VERBATIM','EXACT_DELIVERABLE_REQUESTED','SUPPLIED_MATERIALS_INVENTORY','REQUIRED_OUTPUT_FORMAT','DEADLINE_OR_TEMPORAL_SCOPE','DESIRED_SOURCE_COUNT','KNOWN_AUTHORITATIVE_SOURCES','AVAILABLE_TOOLS','PROHIBITED_ACTIONS','EXPLICIT_USER_REQUIREMENTS','ASSUMPTIONS','UNKNOWN_INFORMATION','INPUT_SET_VERSION','INPUT_SET_CONTENTS','INTAKE_ACCOUNTING','INPUT_SET_HASH_OR_MANIFEST','JOB_RECORD_STATUS','STATUS_EVIDENCE']",'stage1 fields');
 t=one(t,"4:['REQUIREMENTS_VERSION','REQUIREMENT_RECORDS','ATOMICITY_REVIEW_RESULTS','DEFINED_TERM_GAPS','TOTAL_REQUIREMENTS'","4:['REQUIREMENTS_VERSION','REQUIREMENT_RECORDS','ATOMICITY_REVIEW_RESULTS','DEFINED_TERM_GAPS','OBLIGATION_ACCOUNTING','TOTAL_REQUIREMENTS'",'stage4 fields');
 t=one(t,'      "INPUT_SET_CONTENTS"\n    ],\n    "application": [','      "INPUT_SET_CONTENTS",\n      "INTAKE_ACCOUNTING"\n    ],\n    "application": [','stage1 ownership');
 t=one(t,'      "DEFINED_TERM_GAPS",\n      "CONDITIONAL_REQUIREMENTS",','      "DEFINED_TERM_GAPS",\n      "OBLIGATION_ACCOUNTING",\n      "CONDITIONAL_REQUIREMENTS",','stage4 ownership');
 // v2 -> v3 migration must be explicit and preserve operational state.
 t=one(t,"  if(p.schema!=='human-project/30')throw new Error(`Unsupported project schema: ${p.schema||'MISSING'}`);","  if(p.schema==='closed-loop-project/2'){\n    const migrated=JSON.parse(JSON.stringify(p)),original=JSON.parse(JSON.stringify(p));migrated.schema=PROJECT_SCHEMA;migrated.workflow=WORKFLOW_ID;migrated.stageCount=STAGE_COUNT;migrated.revision=Number.isInteger(migrated.revision)?migrated.revision:0;migrated.projectData=migrated.projectData&&typeof migrated.projectData==='object'?migrated.projectData:{};migrated.projectData.migrationArchives=Array.isArray(migrated.projectData.migrationArchives)?migrated.projectData.migrationArchives:[];migrated.projectData.historicalImportRecords=Array.isArray(migrated.projectData.historicalImportRecords)?migrated.projectData.historicalImportRecords:[];migrated.projectData.migrationArchives.push({kind:'MIGRATION_SOURCE',schema:'closed-loop-project/2',preservedAt:new Date().toISOString(),payload:original});if(!migrated.stages||Object.keys(migrated.stages).length!==STAGE_COUNT)throw new Error('closed-loop-project/2 migration requires exactly 30 stages.');return migrated;\n  }\n  if(p.schema!=='human-project/30')throw new Error(`Unsupported project schema: ${p.schema||'MISSING'}`);",'v2 migration');
 return t;
});

edit('workflow-schema.js',t=>{
 t=one(t,"const RESPONSE_SCHEMA='closed-loop-stage-response/2';","const RESPONSE_SCHEMA='closed-loop-stage-response/3';",'response schema');
 t=one(t,"const HUMAN_JOB_FIELDS=Object.freeze([\n  'JOB_TITLE','JOB_OWNER','EXACT_USER_OBJECTIVE_VERBATIM'","const HUMAN_JOB_FIELDS=Object.freeze([\n  'EXACT_USER_OBJECTIVE_VERBATIM'",'human job fields');
 t=one(t,"]);\nconst APPLICATION_JOB_FIELDS=Object.freeze([","]);\nconst HUMAN_DECISION_JOB_FIELDS=Object.freeze(['JOB_TITLE','JOB_OWNER']);\nconst APPLICATION_JOB_FIELDS=Object.freeze([",'human decision job fields');
 t=one(t,"function jobFieldDefinition(name){\n  if(APPLICATION_JOB_FIELDS.includes(name))return field(name,PRODUCER.APPLICATION,{derivation:`Application derives ${name} from canonical project state.`});\n  if(HUMAN_JOB_FIELDS.includes(name))return field(name,PRODUCER.HUMAN","function jobFieldDefinition(name){\n  if(APPLICATION_JOB_FIELDS.includes(name))return field(name,PRODUCER.APPLICATION,{derivation:`Application derives ${name} from canonical project state.`});\n  if(HUMAN_DECISION_JOB_FIELDS.includes(name))return field(name,PRODUCER.HUMAN_DECISION,{provenanceRequired:false,nullable:true});\n  if(HUMAN_JOB_FIELDS.includes(name))return field(name,PRODUCER.HUMAN",'job ownership function');
 t=one(t,"[...new Set([...HUMAN_JOB_FIELDS,...APPLICATION_JOB_FIELDS,...AGENT_JOB_FIELDS])]","[...new Set([...HUMAN_JOB_FIELDS,...HUMAN_DECISION_JOB_FIELDS,...APPLICATION_JOB_FIELDS,...AGENT_JOB_FIELDS])]",'job field union');
 t=one(t,"const STAGE_FIELD_TYPE_OVERRIDES=Object.freeze({\n  '1':Object.freeze({DESIRED_SOURCE_COUNT:Object.freeze({valueType:'INTEGER',enumValues:Object.freeze([]),nullable:true,normalizerKey:null,closedProperties:null})}),","const STAGE_FIELD_TYPE_OVERRIDES=Object.freeze({\n  '1':Object.freeze({DESIRED_SOURCE_COUNT:Object.freeze({valueType:'INTEGER',enumValues:Object.freeze([]),nullable:true,normalizerKey:null,closedProperties:null}),INTAKE_ACCOUNTING:Object.freeze({valueType:'OBJECT_ARRAY',enumValues:Object.freeze([]),nullable:false,normalizerKey:null,closedProperties:null})}),\n  '4':Object.freeze({OBLIGATION_ACCOUNTING:Object.freeze({valueType:'OBJECT_ARRAY',enumValues:Object.freeze([]),nullable:false,normalizerKey:null,closedProperties:null})}),",'accounting field types');
 // exact v1 primitive surface
 t=reOne(t,/operations:Object\.freeze\(\[[\s\S]*?\]\),\n  limits:/,"operations:Object.freeze(['LOAD_ARTIFACT','READ_BYTES','DECODE_UTF8','PARSE_JSON','PARSE_CSV','PARSE_XML','SELECT_JSON_PATH','SELECT_XML','COUNT','SUM','MIN','MAX','SORT','UNIQUE','HASH_SHA256','REGEX','COMPARE','ASSERT_EQ','ASSERT_GT','ASSERT_GTE','ASSERT_LT','ASSERT_LTE','ASSERT_MATCH','ASSERT_CONTAINS','ASSERT_NOT_CONTAINS','ASSERT_SET_EQUAL','BYTE_COMPARE']),\n  limits:",'schema Test IR ops');
 t=t.replaceAll("executableKinds:Object.freeze(['NONE','CUSTOM_PIPELINE'])","executableKinds:Object.freeze(['NONE','TEST_IR'])").replaceAll("!=='CUSTOM_PIPELINE'","!=='TEST_IR'").replaceAll('EXECUTABLE_KIND must be CUSTOM_PIPELINE.','EXECUTABLE_KIND must be TEST_IR.');
 t=one(t,"version:'closed-loop-workflow-schema/2'","version:'closed-loop-workflow-schema/3'",'workflow schema version');
 t=one(t,"JOB_FIELDS,HUMAN_JOB_FIELDS,APPLICATION_JOB_FIELDS,AGENT_JOB_FIELDS,HUMAN_INTAKE_FIELDS,","JOB_FIELDS,HUMAN_JOB_FIELDS,HUMAN_DECISION_JOB_FIELDS,APPLICATION_JOB_FIELDS,AGENT_JOB_FIELDS,HUMAN_INTAKE_FIELDS,",'job ownership export');
 return t;
});

const helpers=String.raw`
const INTAKE_DISPOSITIONS=Object.freeze(['INCORPORATED_JOB_DEFINITION','RETAINED_CONTEXT','UNRESOLVED_HUMAN_ONLY','LATER_RESOLVABLE','INAPPLICABLE']);
const OBLIGATION_DISPOSITIONS=Object.freeze(['REQUIREMENT','RETAINED_NONNORMATIVE_CONTEXT','INAPPLICABLE','BLOCKED']);
function accountingUnits(value,path,out=[]){if(value===undefined||value===null||value==='')return out;if(Array.isArray(value)){value.forEach((x,i)=>accountingUnits(x,path+'['+i+']',out));return out;}if(value&&typeof value==='object'){Object.keys(value).sort().forEach(k=>accountingUnits(value[k],path+'.'+k,out));return out;}const raw=String(value),parts=raw.split(/\r?\n/).map(x=>x.trim()).filter(Boolean);(parts.length?parts:[raw.trim()]).forEach((text,i)=>{if(text)out.push({sourceLocation:(parts.length>1?path+'#'+(i+1):path),rawValue:text,rawValueSha256:hash.sha256Text(text)});});return out;}
function stage01IntakeManifest(project){ensureShape(project);const inputVersion=String(project.job.CURRENT_INPUT_VERSION||'UNKNOWN'),latest=safe(project.projectData.inputVersions).filter(x=>String(x.version||'')===inputVersion).at(-1),root=latest?.payload&&typeof latest.payload==='object'?latest.payload:(project.projectData.userEntered||{}),units=[];accountingUnits(root,'UserJobInput',units);for(const [name,d] of Object.entries(schema.JOB_FIELDS||{})){const v=project.job[name];if(['HUMAN','HUMAN_DECISION'].includes(d.producer)&&v!==undefined&&v!==null&&v!=='')accountingUnits(v,'job.'+name,units);}const dedup=[],seen=new Set();for(const u of units){const k=u.sourceLocation+'|'+u.rawValueSha256;if(!seen.has(k)){seen.add(k);dedup.push(u);}}const normalized=dedup.map((u,i)=>({...u,inputUnitId:'INTAKE-'+hash.sha256Value({inputVersion,sourceLocation:u.sourceLocation,index:i,rawValueSha256:u.rawValueSha256}).slice(0,20).toUpperCase()}));const statements=currentIntentStatements(project),covered=new Set();for(const s of statements){const exact=String(recordValue(s,'EXACT_STATEMENT')||'').trim(),material=String(recordValue(s,'SOURCE_MATERIAL')||'').trim(),location=String(recordValue(s,'SOURCE_LOCATION')||'').trim();for(const u of normalized)if(exact===u.rawValue||location===u.sourceLocation||material===u.sourceLocation||exact.includes(u.rawValue)||u.rawValue.includes(exact))covered.add(u.inputUnitId);}const accounting=safe(project.stages?.[1]?.agentData?.INTAKE_ACCOUNTING),disposed=new Set(accounting.map(x=>String(x?.inputUnitId||''))),accounted=normalized.filter(u=>covered.has(u.inputUnitId)||disposed.has(u.inputUnitId)).length;return {schema:'closed-loop-intake-manifest/1',inputVersion,projectRevision:Number(project.revision||0),units:normalized,expectedCount:normalized.length,accountedCount:accounted,coverage:normalized.length?accounted/normalized.length:1,complete:accounted===normalized.length};}
function stage03ResearchCoverage(project){const noSource=upper(project.stages?.[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE',sources=recordsForCurrentScope(project,'sources'),research=recordsForCurrentScope(project,'research'),covered=new Set(research.map(r=>String(recordValue(r,'SOURCE_ID')||r.relationships?.SOURCE_ID||''))),missingSourceIds=sources.map(r=>recordId(r,'sources')).filter(id=>!covered.has(id)),unsaturatedSourceIds=sources.map(r=>recordId(r,'sources')).filter(id=>!research.filter(rr=>String(recordValue(rr,'SOURCE_ID')||rr.relationships?.SOURCE_ID||'')===id).some(rr=>['SATURATED','COMPLETE','NO NEW MATERIAL'].includes(upper(recordValue(rr,'SATURATION_STATUS'))))),secondPass=truth(project.stages?.[3]?.agentData?.SECOND_CONFLICT_AND_EXCEPTION_PASS_COMPLETED),latestNew=upper(project.stages?.[3]?.agentData?.NEW_MATERIAL_CATEGORY_FOUND_IN_LATEST_PASS),noNew=['FALSE','NO','NONE','NOT APPLICABLE','0'].includes(latestNew),required=currentIntentStatements(project).filter(intentStatementRequiresRequirement),candidateLocations=new Set(recordsForCurrentScope(project,'candidateRequirements').map(r=>String(recordValue(r,'SOURCE_LOCATION')||''))),missingIntentStatementIds=required.map(r=>recordId(r,'intentStatements')).filter(id=>!candidateLocations.has(id));return {noApplicableExternalSource:noSource,sourceCount:sources.length,researchCount:research.length,missingSourceIds,unsaturatedSourceIds,secondConflictAndExceptionPassCompleted:secondPass,latestPassFoundNoNewMaterialCategory:noNew,missingIntentStatementIds,complete:missingIntentStatementIds.length===0&&(noSource||(sources.length>0&&missingSourceIds.length===0&&unsaturatedSourceIds.length===0&&secondPass&&noNew))};}
function stage04ObligationManifest(project){const inputVersion=String(project.job.CURRENT_INPUT_VERSION||'UNKNOWN'),sourceSetVersion=String(project.job.CURRENT_SOURCE_SET_VERSION||'NOT APPLICABLE'),entries=[];for(const s of currentIntentStatements(project)){const id=recordId(s,'intentStatements');entries.push({origin:'HUMAN_INTENT',sourceIdentity:id,sourceLocation:String(recordValue(s,'SOURCE_LOCATION')||id),text:String(recordValue(s,'EXACT_STATEMENT')||''),evidenceSha256:s.recordSha256||s.sha256||hash.sha256Value(recordFields(s)),requirementRelevant:intentStatementRequiresRequirement(s)});}for(const name of ['EXACT_DELIVERABLE_REQUESTED','ASSUMPTIONS','UNKNOWN_INFORMATION','INPUT_SET_CONTENTS']){const leaves=[];accountingUnits(project.stages?.[1]?.agentData?.[name], 'Stage01.'+name,leaves);for(const u of leaves)entries.push({origin:'STAGE01_ACCEPTED',sourceIdentity:'S1-'+name,sourceLocation:u.sourceLocation,text:u.rawValue,evidenceSha256:u.rawValueSha256,requirementRelevant:name!=='ASSUMPTIONS'});}for(const source of recordsForCurrentScope(project,'sources')){const id=recordId(source,'sources');entries.push({origin:'SOURCE_IDENTITY',sourceIdentity:id,sourceLocation:'sources.'+id,text:String(recordValue(source,'TITLE')||id),evidenceSha256:source.recordSha256||source.sha256||hash.sha256Value(recordFields(source)),requirementRelevant:false});}for(const rr of recordsForCurrentScope(project,'research')){const rid=recordId(rr,'research');for(const name of ['MANDATORY_STATEMENTS','RECOMMENDATIONS','OPTIONAL_PRACTICES','EXAMPLES','EXPLANATORY_MATERIAL','PROHIBITIONS','EXCEPTIONS','DEPENDENCIES','APPLICABILITY_FACTS','RESTRICTIONS','INVALIDATING_MATERIAL']){const leaves=[];accountingUnits(recordValue(rr,name),'research.'+rid+'.'+name,leaves);for(const u of leaves)entries.push({origin:'STAGE03_RESEARCH',sourceIdentity:rid,sourceLocation:u.sourceLocation,text:u.rawValue,evidenceSha256:u.rawValueSha256,requirementRelevant:['MANDATORY_STATEMENTS','PROHIBITIONS','EXCEPTIONS','DEPENDENCIES','APPLICABILITY_FACTS','RESTRICTIONS','INVALIDATING_MATERIAL'].includes(name)});}}for(const cr of recordsForCurrentScope(project,'candidateRequirements')){const id=recordId(cr,'candidateRequirements'),text=String(recordValue(cr,'CANDIDATE_OBLIGATION')||'').trim();if(text)entries.push({origin:'STAGE03_CANDIDATE',sourceIdentity:id,sourceLocation:String(recordValue(cr,'SOURCE_LOCATION')||id),text,evidenceSha256:hash.sha256Text(text),requirementRelevant:true});}const obligations=entries.map((e,i)=>({...e,obligationId:'OBL-'+hash.sha256Value({inputVersion,sourceSetVersion,origin:e.origin,sourceIdentity:e.sourceIdentity,sourceLocation:e.sourceLocation,index:i,evidenceSha256:e.evidenceSha256}).slice(0,20).toUpperCase()})),accounting=safe(project.stages?.[4]?.agentData?.OBLIGATION_ACCOUNTING),byId=new Map(accounting.map(x=>[String(x?.obligationId||''),x])),accounted=obligations.filter(o=>byId.has(o.obligationId)).length,duplicates=accounting.length-new Set(accounting.map(x=>String(x?.obligationId||''))).size,blocked=accounting.filter(x=>upper(x?.disposition)==='BLOCKED').length;return {schema:'closed-loop-obligation-manifest/1',inputVersion,sourceSetVersion,projectRevision:Number(project.revision||0),obligations,expectedCount:obligations.length,accountedCount:accounted,coverage:obligations.length?accounted/obligations.length:1,duplicateDispositionCount:duplicates,blockedDispositionCount:blocked,complete:accounted===obligations.length&&duplicates===0};}
`;
edit('workflow-engine.js',t=>{
 t=one(t,'function hasStageActivity(project,stage){',helpers+'\nfunction hasStageActivity(project,stage){','engine accounting helpers');
 t=one(t,"      const statements=currentIntentStatements(project);\n      if(!statements.length)reasons.push('Stage 01 requires a canonical intent-statement ledger; the original intent file may not be deferred to a later stage.');","      const statements=currentIntentStatements(project),intake=stage01IntakeManifest(project);\n      if(!statements.length)reasons.push('Stage 01 requires a canonical intent-statement ledger; the original intent file may not be deferred to a later stage.');\n      if(!intake.complete)reasons.push('Stage 01 controlled-input accounting is incomplete: '+intake.accountedCount+'/'+intake.expectedCount+' units accounted for.');",'stage1 manifest gate');
 t=one(t,"      if(sourceIds.length){requireCount('research',1);const researched=new Set(collection('research').map(record=>String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||''))),missing=sourceIds.filter(id=>!researched.has(id));if(missing.length)reasons.push(`Research is missing for source(s): ${missing.join(', ')}.`);}\n      const requiredStatements=currentIntentStatements(project).filter(intentStatementRequiresRequirement),candidateLocations=new Set(collection('candidateRequirements').map(record=>String(recordValue(record,'SOURCE_LOCATION')||'').trim())),missingStatements=requiredStatements.map(record=>recordId(record,'intentStatements')).filter(id=>!candidateLocations.has(id));\n      if(missingStatements.length)reasons.push(`Candidate requirement coverage is missing for canonical intent statement(s): ${missingStatements.join(', ')}.`);","      const coverage=stage03ResearchCoverage(project);\n      if(coverage.missingSourceIds.length)reasons.push('Research is missing for source(s): '+coverage.missingSourceIds.join(', ')+'.');\n      if(coverage.unsaturatedSourceIds.length)reasons.push('Research is not exhausted for source(s): '+coverage.unsaturatedSourceIds.join(', ')+'.');\n      if(!coverage.noApplicableExternalSource&&!coverage.secondConflictAndExceptionPassCompleted)reasons.push('Stage 03 second conflict and exception pass is incomplete.');\n      if(!coverage.noApplicableExternalSource&&!coverage.latestPassFoundNoNewMaterialCategory)reasons.push('Stage 03 latest pass still found new material; continue research to saturation.');\n      if(coverage.missingIntentStatementIds.length)reasons.push('Candidate requirement coverage is missing for canonical intent statement(s): '+coverage.missingIntentStatementIds.join(', ')+'.');",'stage3 exhaustive gate');
 t=one(t,"    case 4:{\n      requireAccepted();requireCount('requirements',1);","    case 4:{\n      requireAccepted();const obligationManifest=stage04ObligationManifest(project);if(!obligationManifest.complete)reasons.push('Stage 04 obligation accounting is incomplete: '+obligationManifest.accountedCount+'/'+obligationManifest.expectedCount+' obligations accounted for.');if(obligationManifest.blockedDispositionCount)reasons.push('Stage 04 has '+obligationManifest.blockedDispositionCount+' blocked obligation disposition(s).');requireCount('requirements',1);",'stage4 closure gate');
 t=one(t,'unresolvedHumanRequests,openBlockers,acceptedChanges,hasStageActivity,mandatoryRequirements,currentIntentStatements,intentStatementRequiresRequirement,','unresolvedHumanRequests,openBlockers,acceptedChanges,hasStageActivity,stage01IntakeManifest,stage03ResearchCoverage,stage04ObligationManifest,INTAKE_DISPOSITIONS,OBLIGATION_DISPOSITIONS,mandatoryRequirements,currentIntentStatements,intentStatementRequiresRequirement,','engine exports');
 return t;
});

edit('prompt-engine.js',t=>{
 t=one(t,"const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine/26';","const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine/27';",'prompt version');
 t=one(t,"filter(([,definition])=>definition?.producer==='HUMAN')","filter(([,definition])=>['HUMAN','HUMAN_DECISION'].includes(definition?.producer))",'human data block');
 t=one(t,"contractVersion:'closed-loop-response-contract/2.4'","contractVersion:'closed-loop-response-contract/3.0'",'response contract version');
 t=t.replaceAll('EXECUTABLE_KIND = CUSTOM_PIPELINE','EXECUTABLE_KIND = TEST_IR').replaceAll('CUSTOM_PIPELINE','TEST_IR');
 t=one(t," const parts=[];\n if(stage>1&&![11,12,23,24].includes(stage))"," const parts=[];\n if(stage===1)parts.push('APPLICATION-ENUMERATED STAGE 01 INTAKE COVERAGE MANIFEST — EVERY UNIT MUST BE ACCOUNTED\\n'+show(workflow.stage01IntakeManifest(state)));\n if(stage===3)parts.push('APPLICATION-DERIVED STAGE 03 EXHAUSTION STATUS\\n'+show(workflow.stage03ResearchCoverage(state)));\n if(stage===4)parts.push('APPLICATION-GENERATED STAGE 04 OBLIGATION MANIFEST — COMPLETE INPUT UNIVERSE; EVERY OBLIGATION MUST RECEIVE A DISPOSITION\\n'+show(workflow.stage04ObligationManifest(state)));\n if(stage>1&&![11,12,23,24].includes(stage))",'prompt manifest context');
 // Remove hard-coded project-domain branches; domain fixture belongs in tests only.
 t=reOne(t,/\$\{stage===1\?`STAGE 01 DOMAIN INTAKE ADAPTATION — CLARIFY AND NORMALIZE ONLY[\s\S]*?`:`DOMAIN \/ DELIVERABLE ADAPTATION — APPLY ONLY WHAT IS RELEVANT[\s\S]*?`\}/,'','domain branches');
 t=t.replace(/For PATENT \/ REGULATED FILING jobs,[\s\S]*?Do not turn researchable legal strategy into a human question\.\n/,'');
 t=one(t,"${stage===1?`STAGE 01 CLARIFICATION EXPERIENCE","${stage===1?`STAGE 01 SUBJECT-NEUTRAL ZERO-LOSS INTAKE\nDerive subject-specific human-only questions solely from the actual request, accessible supplied materials, and current canonical context. Do not use project-subject branches. Enumerate and preserve every meaning-bearing human unit. For every inputUnitId in the application intake manifest, either map its exact substantive meaning into one or more intentStatements or include one INTAKE_ACCOUNTING item using only inputUnitId, disposition, normalizedMeaning, reason. disposition: INCORPORATED_JOB_DEFINITION | RETAINED_CONTEXT | UNRESOLVED_HUMAN_ONLY | LATER_RESOLVABLE | INAPPLICABLE. Never omit a unit because it is repetitive, non-normative, explanatory, inconvenient, or already represented elsewhere; preserve traceability. The original intent file is used only during Stage 01 and never requested again after accepted capture.\n\nSTAGE 01 CLARIFICATION EXPERIENCE",'stage1 zero loss prompt');
 t=one(t,"${stage===2?`STAGE 02 SOURCE DISCOVERY GUIDANCE","${stage===4?`STAGE 04 CLOSED OBLIGATION ACCOUNTING — ZERO LOSS REQUIRED\nThe application-generated Stage 04 obligation manifest above is the complete compilation universe: current human intent and accepted Stage 01 meaning, Stage 03 research, candidate obligations, and source identities/evidence. Do not ask the human to attach, resend, retype, summarize, reconstruct, or reopen information already captured. For every obligationId return exactly one OBLIGATION_ACCOUNTING item using only obligationId, disposition, requirementTempKeys, reason. disposition: REQUIREMENT | RETAINED_NONNORMATIVE_CONTEXT | INAPPLICABLE | BLOCKED. REQUIREMENT must map to one or more proposed Stage 04 requirement tempKeys. Other dispositions require an explicit reason and no requirementTempKeys. No obligation may disappear. If required prior-stage content is genuinely absent, return BLOCKED with INADEQUATE_PRIOR_OUTPUT or MISSING_APPLICATION_CONTEXT and identify the earliest responsible stage; never convert an upstream capture defect into another user attachment request.\n\n`:''}${stage===2?`STAGE 02 SOURCE DISCOVERY GUIDANCE",'stage4 zero loss prompt');
 t=one(t,"const contextSignature=hash.sha256Value(contextManifest),bodyText=body(stage,state,operation,scope)","if(stage===1)contextManifest.stage01IntakeManifest=workflow.stage01IntakeManifest(state);if(stage===3)contextManifest.stage03ResearchCoverage=workflow.stage03ResearchCoverage(state);if(stage===4)contextManifest.stage04ObligationManifest=workflow.stage04ObligationManifest(state);const contextSignature=hash.sha256Value(contextManifest),bodyText=body(stage,state,operation,scope)",'manifest prompt identity');
 return t;
});

const accountingValidation=String.raw`
function validateStageAccounting(project,envelope,stageNumber,issues){if(envelope.responseType!=='DATA_PROPOSAL')return;const exact=(obj,allowed,path)=>{if(!object(obj)){issues.push(issue('INVALID_ACCOUNTING_ITEM',path,'Accounting item must be an object.'));return false;}unknownKeys(obj,allowed,path,issues);return true;};if(stageNumber===1){const manifest=workflow.stage01IntakeManifest(project),items=safe(envelope.stageData?.INTAKE_ACCOUNTING),expected=new Set(manifest.units.map(x=>x.inputUnitId)),seen=new Set();if(!Array.isArray(envelope.stageData?.INTAKE_ACCOUNTING))issues.push(issue('MISSING_INTAKE_ACCOUNTING','/stageData/INTAKE_ACCOUNTING','Stage 01 requires exhaustive intake accounting.'));items.forEach((item,i)=>{const p='/stageData/INTAKE_ACCOUNTING/'+i;if(!exact(item,['inputUnitId','disposition','normalizedMeaning','reason'],p))return;const id=String(item.inputUnitId||'');if(!expected.has(id))issues.push(issue('UNKNOWN_INTAKE_UNIT',p+'/inputUnitId','Unknown intake unit '+id+'.'));if(seen.has(id))issues.push(issue('DUPLICATE_INTAKE_DISPOSITION',p+'/inputUnitId','Duplicate intake disposition.'));seen.add(id);if(!workflow.INTAKE_DISPOSITIONS.includes(item.disposition))issues.push(issue('INVALID_INTAKE_DISPOSITION',p+'/disposition','Invalid intake disposition.'));if(item.disposition!=='INAPPLICABLE'&&!String(item.normalizedMeaning||'').trim())issues.push(issue('MISSING_NORMALIZED_MEANING',p+'/normalizedMeaning','Preserved meaning is required.'));if(['UNRESOLVED_HUMAN_ONLY','LATER_RESOLVABLE','INAPPLICABLE'].includes(item.disposition)&&!String(item.reason||'').trim())issues.push(issue('MISSING_ACCOUNTING_REASON',p+'/reason','Reason is required.'));});for(const id of expected)if(!seen.has(id)&&!manifest.complete)issues.push(issue('INCOMPLETE_INTAKE_ACCOUNTING','/stageData/INTAKE_ACCOUNTING','Controlled input unit '+id+' is not accounted for.'));}
if(stageNumber===4){const manifest=workflow.stage04ObligationManifest(project),items=safe(envelope.stageData?.OBLIGATION_ACCOUNTING),expected=new Set(manifest.obligations.map(x=>x.obligationId)),seen=new Set(),reqs=new Set(safe(envelope.records?.requirements).map(x=>String(x?.tempKey||'')));if(!Array.isArray(envelope.stageData?.OBLIGATION_ACCOUNTING))issues.push(issue('MISSING_OBLIGATION_ACCOUNTING','/stageData/OBLIGATION_ACCOUNTING','Stage 04 requires exhaustive obligation accounting.'));items.forEach((item,i)=>{const p='/stageData/OBLIGATION_ACCOUNTING/'+i;if(!exact(item,['obligationId','disposition','requirementTempKeys','reason'],p))return;const id=String(item.obligationId||'');if(!expected.has(id))issues.push(issue('UNKNOWN_OBLIGATION_ID',p+'/obligationId','Unknown obligation '+id+'.'));if(seen.has(id))issues.push(issue('DUPLICATE_OBLIGATION_DISPOSITION',p+'/obligationId','Duplicate obligation disposition.'));seen.add(id);if(!workflow.OBLIGATION_DISPOSITIONS.includes(item.disposition))issues.push(issue('INVALID_OBLIGATION_DISPOSITION',p+'/disposition','Invalid obligation disposition.'));const refs=safe(item.requirementTempKeys);if(!Array.isArray(item.requirementTempKeys))issues.push(issue('INVALID_REQUIREMENT_MAPPING',p+'/requirementTempKeys','requirementTempKeys must be an array.'));if(item.disposition==='REQUIREMENT'&&!refs.length)issues.push(issue('MISSING_REQUIREMENT_MAPPING',p+'/requirementTempKeys','REQUIREMENT must map to at least one requirement.'));if(item.disposition!=='REQUIREMENT'&&refs.length)issues.push(issue('INVALID_REQUIREMENT_MAPPING',p+'/requirementTempKeys','Only REQUIREMENT may map to requirements.'));for(const ref of refs)if(!reqs.has(String(ref)))issues.push(issue('UNRESOLVED_REQUIREMENT_MAPPING',p+'/requirementTempKeys','Unknown proposed requirement '+ref+'.'));if(item.disposition!=='REQUIREMENT'&&!String(item.reason||'').trim())issues.push(issue('MISSING_ACCOUNTING_REASON',p+'/reason','Non-requirement disposition requires a reason.'));});for(const id of expected)if(!seen.has(id))issues.push(issue('INCOMPLETE_OBLIGATION_ACCOUNTING','/stageData/OBLIGATION_ACCOUNTING','Obligation '+id+' is omitted.'));}}
`;
edit('response-ingestion.js',t=>{t=one(t,'function validateEnvelope(project,envelope,{stage,promptRecord,rawSha256,files=[]}={}){',accountingValidation+'\nfunction validateEnvelope(project,envelope,{stage,promptRecord,rawSha256,files=[]}={}){','accounting validation function');t=one(t,"  if(envelope.responseType==='EXECUTION_FAILED'){if(Object.keys(envelope.stageData||{}).length","  validateStageAccounting(project,envelope,stageNumber,issues);\n  if(envelope.responseType==='EXECUTION_FAILED'){if(Object.keys(envelope.stageData||{}).length",'accounting validator call');return t;});

edit('test-runtime.js',t=>{
 t=reOne(t,/const OPS=Object\.freeze\(\[[\s\S]*?\]\);/,"const OPS=Object.freeze(['LOAD_ARTIFACT','READ_BYTES','DECODE_UTF8','PARSE_JSON','PARSE_CSV','PARSE_XML','SELECT_JSON_PATH','SELECT_XML','COUNT','SUM','MIN','MAX','SORT','UNIQUE','HASH_SHA256','REGEX','COMPARE','ASSERT_EQ','ASSERT_GT','ASSERT_GTE','ASSERT_LT','ASSERT_LTE','ASSERT_MATCH','ASSERT_CONTAINS','ASSERT_NOT_CONTAINS','ASSERT_SET_EQUAL','BYTE_COMPARE']);",'runtime ops');
 t=one(t,"const LIMITS=Object.freeze({maxSteps:64,maxTextBytes:16*1024*1024,maxCollectionItems:100000,maxRegexLength:2000,maxCsvCells:250000});","const LIMITS=Object.freeze({maxInputBytes:16*1024*1024,maxDecompressedBytes:32*1024*1024,maxSteps:64,maxSelectorDepth:32,maxParsedDepth:64,maxCollectionItems:100000,maxRegexLength:2000,maxRegexInputBytes:4*1024*1024,maxCsvCells:250000,maxWorkerDurationMs:10000,maxArchiveExpansionBytes:32*1024*1024});",'runtime limits');
 t=one(t,"function parseCsv(text){","function parseCsv(text,config){\n  if(!config||typeof config!=='object')throw new Error('PARSE_CSV requires explicit config.');const delimiter=String(config.delimiter??'');if(delimiter.length!==1)throw new Error('PARSE_CSV delimiter must be exactly one character.');if(typeof config.header!=='boolean')throw new Error('PARSE_CSV header must be BOOLEAN.');if(!['RFC4180_DOUBLE_QUOTE'].includes(config.quoting))throw new Error('PARSE_CSV quoting must be RFC4180_DOUBLE_QUOTE.');if(!['LF','CRLF','CR_OR_LF'].includes(config.newline))throw new Error('PARSE_CSV newline must be explicit.');if(config.encoding!=='UTF-8')throw new Error('PARSE_CSV supports only UTF-8.');");
 t=t.replace("if(ch===','){row.push(cell);cell='';cells++;}","if(ch===delimiter){row.push(cell);cell='';cells++;}");
 t=t.replace("  return rows;\n}\nfunction selectJsonPath", "  if(config.header&&rows.length){const header=rows.shift();return rows.map(row=>Object.fromEntries(header.map((name,i)=>[name,row[i]??''])));}return rows;\n}\nfunction parseXml(text){const parser=typeof DOMParser!=='undefined'?new DOMParser():null;if(!parser)throw new Error('PARSE_XML is unavailable in this runtime context.');const doc=parser.parseFromString(String(text),'application/xml');if(doc.querySelector('parsererror'))throw new Error('Malformed XML.');return doc;}\nfunction selectXml(value,path){const text=String(path||'').trim();if(!/^\/[A-Za-z_][\\w.-]*(?:\/[A-Za-z_][\\w.-]*)*$/.test(text))throw new Error('SELECT_XML supports only absolute child-element selectors.');let nodes=[value.documentElement];const parts=text.split('/').filter(Boolean);if(!nodes[0]||nodes[0].tagName!==parts[0])return [];for(const name of parts.slice(1))nodes=nodes.flatMap(n=>Array.from(n.children||[]).filter(c=>c.tagName===name));return nodes;}\nfunction selectJsonPath");
 t=t.replace("case 'PARSE_CSV':value=parseCsv(String(value));break;","case 'PARSE_CSV':value=parseCsv(String(value),step.config);break;\n      case 'PARSE_XML':value=parseXml(String(value));break;\n      case 'SELECT_XML':value=selectXml(value,step.path);break;");
 t=t.replace("if(String(field(test,'EXECUTABLE_KIND')||'').toUpperCase()!=='CUSTOM_PIPELINE')return false;","if(String(field(test,'EXECUTABLE_KIND')||'').toUpperCase()!=='TEST_IR')return false;");
 t=t.replace(/\n      case 'ASSERT_EXISTS':[\s\S]*?case 'ASSERT_NE':[\s\S]*?break;/,'');
 return t;
});

edit('index.html',t=>{
 t=one(t,"connect-src 'self'; object-src 'none';","connect-src 'self'; worker-src 'self'; object-src 'none';",'worker CSP');
 t=one(t,'<script defer src="workflow-schema.js?v=runtime-c2144fb2d4d5b4c5"></script>\n<script defer src="workflow-engine.js?v=runtime-c2144fb2d4d5b4c5"></script>','<script defer src="workflow-schema.js?v=runtime-c2144fb2d4d5b4c5"></script>\n<script defer src="test-runtime.js?v=runtime-c2144fb2d4d5b4c5"></script>\n<script defer src="workflow-engine.js?v=runtime-c2144fb2d4d5b4c5"></script>','runtime order');
 return t;
});

// Stage 4 must not expose generic file selection; files are captured once upstream and later files only when execution routing requires them.
edit('app-core.js',t=>t.replace("const applicable=[10,17,20,21,25].includes(n),files=safe(current.stages[n].authorizedFiles)","const applicable=[10,17,20,21,25].includes(n),files=safe(current.stages[n].authorizedFiles)").replace("return `<div class=\"panel\"><h2 class=\"section-title\">${applicable?'Artifact control':'Authorized files for this stage'}</h2>","if(n===4)return '';return `<div class=\"panel\"><h2 class=\"section-title\">${applicable?'Artifact control':'Authorized files for this stage'}</h2>"));
console.log('apply-v3-current complete');
