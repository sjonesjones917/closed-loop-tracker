import fs from 'node:fs';
const read=p=>fs.readFileSync(p,'utf8'),write=(p,s)=>fs.writeFileSync(p,s),must=(s,n,l)=>{if(!s.includes(n))throw new Error('Missing '+l);},rep=(s,a,b,l)=>{must(s,a,l);return s.replace(a,b)};

let s=read('workflow-schema.js');
const stageFieldsOld=`const STAGE_FIELDS=Object.freeze(Object.fromEntries(core.STAGES.map(stage=>[\n  stage.number,\n  Object.freeze(Object.fromEntries(stage.fields.map(name=>[name,stageFieldDefinition(stage.number,name)])))\n])));`;
const stageFieldsNew=`const ACCOUNTING_STAGE_FIELD_DEFINITIONS=Object.freeze({\n  1:Object.freeze({INTAKE_ACCOUNTING:field('INTAKE_ACCOUNTING',PRODUCER.AGENT,{requiredAtStage:1,valueType:'OBJECT_ARRAY',closedProperties:['unitId','disposition','reason'],help:'Classify every application-enumerated intake unit exactly once.'})}),\n  4:Object.freeze({OBLIGATION_ACCOUNTING:field('OBLIGATION_ACCOUNTING',PRODUCER.AGENT,{requiredAtStage:4,valueType:'OBJECT_ARRAY',closedProperties:['obligationId','disposition','requirementTempKeys','reason'],help:'Account for every application-enumerated obligation exactly once.'})})\n});\nconst STAGE_FIELDS=Object.freeze(Object.fromEntries(core.STAGES.map(stage=>[\n  stage.number,\n  Object.freeze({...Object.fromEntries(stage.fields.map(name=>[name,stageFieldDefinition(stage.number,name)])),...(ACCOUNTING_STAGE_FIELD_DEFINITIONS[stage.number]||{})})\n])));`;
s=rep(s,stageFieldsOld,stageFieldsNew,'stage accounting fields');
write('workflow-schema.js',s);

let e=read('workflow-engine.js');
// Make manifest derivation pure; durability happens when the prompt is registered and when the response is accepted.
e=e.replace("const manifest={manifestId:'INTAKE-MANIFEST-'+manifestHash.slice(0,20).toUpperCase(),inputVersion,manifestHash,units,createdAt:now(),applicationOwned:true};project.projectData.intakeCoverageManifests.push(manifest);return manifest;","return {manifestId:'INTAKE-MANIFEST-'+manifestHash.slice(0,20).toUpperCase(),inputVersion,manifestHash,units,applicationOwned:true};");
e=e.replace("const manifest={manifestId:'OBLIGATION-MANIFEST-'+manifestHash.slice(0,20).toUpperCase(),scope,manifestHash,items,createdAt:now(),applicationOwned:true};project.projectData.obligationManifests.push(manifest);return manifest;","return {manifestId:'OBLIGATION-MANIFEST-'+manifestHash.slice(0,20).toUpperCase(),scope,manifestHash,items,applicationOwned:true};");
const registerAnchor="  const record={...clone(promptRecord),source:'APPLICATION_PROMPT_REGISTRATION'};project.projectData.generatedPrompts.push(record);project.stages[Number(record.stage)].currentPromptId=record.instructionId;";
const registerReplacement="  const record={...clone(promptRecord),source:'APPLICATION_PROMPT_REGISTRATION'};project.projectData.generatedPrompts.push(record);project.stages[Number(record.stage)].currentPromptId=record.instructionId;\n  const intake=record.contextManifest?.intakeCoverageManifest;if(intake&&!safe(project.projectData.intakeCoverageManifests).some(x=>x.manifestId===intake.manifestId))project.projectData.intakeCoverageManifests.push({...clone(intake),createdAt:now(),promptId:record.instructionId,promptContextSignature:record.contextSignature});\n  const obligations=record.contextManifest?.obligationManifest;if(obligations&&!safe(project.projectData.obligationManifests).some(x=>x.manifestId===obligations.manifestId))project.projectData.obligationManifests.push({...clone(obligations),createdAt:now(),promptId:record.instructionId,promptContextSignature:record.contextSignature});";
e=rep(e,registerAnchor,registerReplacement,'durable prompt manifests');
write('workflow-engine.js',e);

let p=read('prompt-engine.js');
const buildAnchor="const contextSignature=hash.sha256Value(contextManifest),bodyText=body(stage,state,operation,scope),bodySha256=hash.sha256Text(bodyText),descriptor=responseContractDescriptor(stage,operation),contractSha256=hash.sha256Value(descriptor);";
const buildReplacement="if(stage===1)contextManifest.intakeCoverageManifest=workflow.intakeCoverageManifest(state);if(stage===4)contextManifest.obligationManifest=workflow.obligationManifest(state);const contextSignature=hash.sha256Value(contextManifest),bodyText=body(stage,state,operation,scope),bodySha256=hash.sha256Text(bodyText),descriptor=responseContractDescriptor(stage,operation),contractSha256=hash.sha256Value(descriptor);";
p=rep(p,buildAnchor,buildReplacement,'manifest context identity');
p=p.replace("1:'Initialize only this current job from this current job’s exact user input.","1:'Initialize only this current job from this current job’s exact user input. For every unit in APPLICATION-ENUMERATED INTAKE COVERAGE MANIFEST, return exactly one INTAKE_ACCOUNTING item with exactly unitId, disposition, and reason. disposition must be INCORPORATED, RETAINED_CONTEXT, UNRESOLVED_HUMAN_ONLY, LATER_RESOLVABLE, or INAPPLICABLE. Never omit or duplicate a unit identity. ");
p=p.replace("4:'Compile atomic requirement proposals only from the complete current canonical input union selected by the application:","4:'For every item in APPLICATION-ENUMERATED OBLIGATION UNIVERSE, return exactly one OBLIGATION_ACCOUNTING item with exactly obligationId, disposition, requirementTempKeys, and reason. disposition must be REQUIREMENTS, RETAINED_NONNORMATIVE_CONTEXT, INAPPLICABLE, or BLOCKED. REQUIREMENTS requires one or more tempKeys that resolve to requirement proposals in this same response; all other dispositions require an empty requirementTempKeys array and a substantive reason. Never omit or duplicate an obligation identity. Compile atomic requirement proposals only from the complete current canonical input union selected by the application:");
const descriptorAnchor="return {contractVersion:'closed-loop-response-contract/2.4',schema:schema.RESPONSE_SCHEMA";
p=rep(p,descriptorAnchor,"return {contractVersion:'closed-loop-response-contract/3.0',schema:schema.RESPONSE_SCHEMA,accountingContract:stage===1?{field:'INTAKE_ACCOUNTING',exactItemKeys:['unitId','disposition','reason'],dispositions:['INCORPORATED','RETAINED_CONTEXT','UNRESOLVED_HUMAN_ONLY','LATER_RESOLVABLE','INAPPLICABLE'],closure:'Every current intake-manifest unit exactly once'}:stage===4?{field:'OBLIGATION_ACCOUNTING',exactItemKeys:['obligationId','disposition','requirementTempKeys','reason'],dispositions:['REQUIREMENTS','RETAINED_NONNORMATIVE_CONTEXT','INAPPLICABLE','BLOCKED'],closure:'Every current obligation-manifest item exactly once'}:null",'accounting response contract');
write('prompt-engine.js',p);

let r=read('response-ingestion.js');
r=rep(r,"const hash=globalThis.closedLoopHash;\nif(!schema||!workflow||!hash)","const hash=globalThis.closedLoopHash;\nconst testRuntime=globalThis.closedLoopTestRuntime;\nif(!schema||!workflow||!hash||!testRuntime)",'runtime ingestion dependency');
const helperAnchor="function currentScope(project,promptRecord){return clone(promptRecord?.scope||{});}";
const helpers=`const INTAKE_DISPOSITIONS=Object.freeze(['INCORPORATED','RETAINED_CONTEXT','UNRESOLVED_HUMAN_ONLY','LATER_RESOLVABLE','INAPPLICABLE']);\nconst OBLIGATION_DISPOSITIONS=Object.freeze(['REQUIREMENTS','RETAINED_NONNORMATIVE_CONTEXT','INAPPLICABLE','BLOCKED']);\nfunction validateAccountingClosure(project,envelope,stageNumber,issues){\n  if(envelope.responseType!=='DATA_PROPOSAL')return;\n  if(stageNumber===1){const manifest=workflow.intakeCoverageManifest(project),list=envelope.stageData?.INTAKE_ACCOUNTING,path='/stageData/INTAKE_ACCOUNTING';if(!Array.isArray(list)){issues.push(issue('MISSING_INTAKE_ACCOUNTING',path,'Stage 01 DATA_PROPOSAL must classify every controlled input unit.'));return;}const expected=new Set(manifest.units.map(x=>x.unitId)),seen=new Set();for(const [i,item] of list.entries()){const ip=path+'/'+i;if(!object(item)){issues.push(issue('INVALID_INTAKE_ACCOUNTING',ip,'Accounting item must be an object.'));continue;}unknownKeys(item,['unitId','disposition','reason'],ip,issues);const id=String(item.unitId||'');if(!expected.has(id))issues.push(issue('UNKNOWN_INTAKE_UNIT',ip+'/unitId','Unknown intake unit identity.'));if(seen.has(id))issues.push(issue('DUPLICATE_INTAKE_UNIT',ip+'/unitId','Each intake unit must be classified exactly once.'));seen.add(id);if(!INTAKE_DISPOSITIONS.includes(item.disposition))issues.push(issue('INVALID_INTAKE_DISPOSITION',ip+'/disposition','Invalid intake disposition.'));if(['UNRESOLVED_HUMAN_ONLY','LATER_RESOLVABLE','INAPPLICABLE'].includes(item.disposition)&&!String(item.reason||'').trim())issues.push(issue('MISSING_ACCOUNTING_REASON',ip+'/reason','This disposition requires a reason.'));}for(const id of expected)if(!seen.has(id))issues.push(issue('OMITTED_INTAKE_UNIT',path,'Stage 01 omitted controlled input unit '+id+'.'));}\n  if(stageNumber===4){const manifest=workflow.obligationManifest(project),list=envelope.stageData?.OBLIGATION_ACCOUNTING,path='/stageData/OBLIGATION_ACCOUNTING',requirements=safe(envelope.records?.requirements),tempKeys=new Set(requirements.map(x=>String(x?.tempKey||'')).filter(Boolean));if(!Array.isArray(list)){issues.push(issue('MISSING_OBLIGATION_ACCOUNTING',path,'Stage 04 DATA_PROPOSAL must account for every application-enumerated obligation.'));return;}const expected=new Set(manifest.items.map(x=>x.obligationId)),seen=new Set();for(const [i,item] of list.entries()){const ip=path+'/'+i;if(!object(item)){issues.push(issue('INVALID_OBLIGATION_ACCOUNTING',ip,'Accounting item must be an object.'));continue;}unknownKeys(item,['obligationId','disposition','requirementTempKeys','reason'],ip,issues);const id=String(item.obligationId||'');if(!expected.has(id))issues.push(issue('UNKNOWN_OBLIGATION_ID',ip+'/obligationId','Unknown obligation identity.'));if(seen.has(id))issues.push(issue('DUPLICATE_OBLIGATION_ID',ip+'/obligationId','Each obligation must be accounted exactly once.'));seen.add(id);if(!OBLIGATION_DISPOSITIONS.includes(item.disposition))issues.push(issue('INVALID_OBLIGATION_DISPOSITION',ip+'/disposition','Invalid obligation disposition.'));if(!Array.isArray(item.requirementTempKeys))issues.push(issue('INVALID_REQUIREMENT_MAPPING',ip+'/requirementTempKeys','requirementTempKeys must be an array.'));else if(item.disposition==='REQUIREMENTS'){if(!item.requirementTempKeys.length)issues.push(issue('MISSING_REQUIREMENT_MAPPING',ip+'/requirementTempKeys','REQUIREMENTS disposition must map to at least one requirement proposal.'));for(const key of item.requirementTempKeys){if(!tempKeys.has(String(key)))issues.push(issue('UNRESOLVED_REQUIREMENT_MAPPING',ip+'/requirementTempKeys','Mapped requirement tempKey '+key+' does not exist in this response.'));}if(new Set(item.requirementTempKeys.map(String)).size!==item.requirementTempKeys.length)issues.push(issue('DUPLICATE_REQUIREMENT_MAPPING',ip+'/requirementTempKeys','Requirement mapping tempKeys must be unique.'));}else{if(item.requirementTempKeys.length)issues.push(issue('PROHIBITED_REQUIREMENT_MAPPING',ip+'/requirementTempKeys','Non-requirement dispositions cannot map requirement proposals.'));if(!String(item.reason||'').trim())issues.push(issue('MISSING_ACCOUNTING_REASON',ip+'/reason','Non-requirement disposition requires a reason.'));}}for(const id of expected)if(!seen.has(id))issues.push(issue('OMITTED_OBLIGATION',path,'Stage 04 omitted obligation '+id+'.'));}\n}\nfunction currentScope(project,promptRecord){return clone(promptRecord?.scope||{});}`;
r=rep(r,helperAnchor,helpers,'closure helpers');
const testRecordAnchor="        if(collection==='sources')for(const message of schema.sourceClassificationIssues(record.fields))issues.push(issue('INVALID_EXTERNAL_SOURCE',path,message));";
const testRecordReplacement=testRecordAnchor+"\n        if(collection==='tests'){const kind=String(record?.fields?.EXECUTABLE_KIND||'NONE').toUpperCase(),mode=String(record?.fields?.EXECUTION_MODE||'').toUpperCase();if(kind==='TEST_IR'){if(mode!=='APPLICATION_DETERMINISTIC')issues.push(issue('INVALID_TEST_IR_ROUTE',path+'/fields/EXECUTION_MODE','TEST_IR requires APPLICATION_DETERMINISTIC.'));if(String(record?.fields?.REQUIRED_CAPABILITY||'').toUpperCase()!==testRuntime.CAPABILITY)issues.push(issue('INVALID_TEST_IR_CAPABILITY',path+'/fields/REQUIRED_CAPABILITY','TEST_IR requires the registered application capability.'));for(const message of testRuntime.validateSpec(record?.fields?.EXECUTABLE_SPEC).issues)issues.push(issue('INVALID_TEST_IR_SPEC',path+'/fields/EXECUTABLE_SPEC',message));for(const message of testRuntime.validateBindings(record?.fields?.EXECUTABLE_INPUT_BINDINGS).issues)issues.push(issue('INVALID_TEST_IR_BINDING',path+'/fields/EXECUTABLE_INPUT_BINDINGS',message));}else if(mode==='APPLICATION_DETERMINISTIC')issues.push(issue('UNSUPPORTED_NATIVE_TEST',path+'/fields/EXECUTION_MODE','APPLICATION_DETERMINISTIC requires a valid supported TEST_IR specification.'));}";
r=rep(r,testRecordAnchor,testRecordReplacement,'test ir ingestion');
const proposalAnchor="  if(envelope.responseType==='DATA_PROPOSAL'){";
r=rep(r,proposalAnchor,"  validateAccountingClosure(project,envelope,stageNumber,issues);\n  if(envelope.responseType==='DATA_PROPOSAL'){",'accounting validation call');
const canonicalFieldsAnchor="      const fields={...workflow.applicationInitialFields(collection),...clone(proposed.fields||{})};\n      fields[definition.idField]=id;";
const canonicalFieldsReplacement="      const fields={...workflow.applicationInitialFields(collection),...clone(proposed.fields||{})};\n      fields[definition.idField]=id;if(collection==='tests'){const kind=String(fields.EXECUTABLE_KIND||'NONE').toUpperCase();fields.EXECUTABLE_SPEC_VERSION=kind==='TEST_IR'?testRuntime.SPEC_VERSION:null;fields.EXECUTABLE_SPEC_SHA256=kind==='TEST_IR'?hash.sha256Value(fields.EXECUTABLE_SPEC):null;}";
r=rep(r,canonicalFieldsAnchor,canonicalFieldsReplacement,'application test ir fields');
const commitAnchor="  const manifestId=workflow.allocateInfrastructureId(next,'EXTRACTION-MANIFEST','extractionManifests');";
const commitPrefix="  if(stage===1){const current=workflow.intakeCoverageManifest(next),stored=safe(next.projectData.intakeCoverageManifests).find(x=>x.manifestId===current.manifestId);if(stored){stored.acceptedAccounting=clone(proposal.proposedStageData.INTAKE_ACCOUNTING||[]);stored.coverage=1;stored.rawResponseId=proposal.rawResponseId;stored.acceptedChangeId=changeId;stored.acceptedProjectRevision=Number(next.revision||0);stored.acceptedAt=stamp;}}\n  if(stage===4){const current=workflow.obligationManifest(next),stored=safe(next.projectData.obligationManifests).find(x=>x.manifestId===current.manifestId);if(stored){const map=proposal.tempToCanonical||{};stored.acceptedAccounting=clone(proposal.proposedStageData.OBLIGATION_ACCOUNTING||[]).map(item=>({...item,canonicalRequirementIds:safe(item.requirementTempKeys).map(key=>map[key]?.id).filter(Boolean)}));stored.coverage=1;stored.rawResponseId=proposal.rawResponseId;stored.acceptedChangeId=changeId;stored.acceptedProjectRevision=Number(next.revision||0);stored.acceptedAt=stamp;}}\n"+commitAnchor;
r=rep(r,commitAnchor,commitPrefix,'accounting manifest commit');
write('response-ingestion.js',r);

let v=read('verify-spec-conformance.mjs');
v += `\n// Accounting closure must fail closed before canonical mutation.\n{const p=core.createBlankState('JOB-CLOSURE');Object.assign(p.job,{CURRENT_INPUT_VERSION:'INPUT-v001',CURRENT_SOURCE_SET_VERSION:'SOURCE-SET-v001',EXACT_USER_OBJECTIVE_VERBATIM:'Do X',EXPLICIT_USER_REQUIREMENTS:'Keep A\\nKeep B'});engine.ensureShape(p);const m=engine.intakeCoverageManifest(p);assert(m.units.length>=3);const om=engine.obligationManifest(p);assert(om.items.length>=3);assert(schema.STAGE_FIELDS[1].INTAKE_ACCOUNTING);assert(schema.STAGE_FIELDS[4].OBLIGATION_ACCOUNTING);}\nconsole.log(JSON.stringify({intakeAccountingContract:true,obligationAccountingContract:true}));\n`;
write('verify-spec-conformance.mjs',v);
