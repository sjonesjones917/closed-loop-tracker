JOB_ID: {{JOB_ID}}

PROJECT_ID: {{PROJECT_ID}}

INPUT_VERSION: INPUT-v001

EXACT USER OBJECTIVE:
Build, verify, release, and deploy one phone-first, domain-general Closed-Loop Agent Reliability application that accepts an arbitrary user job and drives it through the exact 31-stage forward pipeline from lossless user intent through independent external research, external authority, atomic requirements, tests, production, independent verification, correction, acceptance, release-hash verification, and release, while keeping user job input, external research sources, and workflow-generated artifacts strictly separate and prohibiting circular authority.

EXACT DELIVERABLE OR DELIVERABLES:
A working deployed Closed-Loop Agent Reliability web application at https://sjonesjones917.github.io/closed-loop-tracker/; the exact standalone UTF-8 release HTML; the exact project JSON exported through the application's visible Export this project control and automatically reloaded as a separate sidecar; and reproducible browser, process, audit, and SHA-256 evidence establishing the complete 31-stage workflow, the required independent execution and verification cycles, an ACCEPTED decision, and byte-identical release.

REQUESTED ACTIONS:
Correct the existing repository implementation without redefining the job as a repair task; implement the complete domain-general application described by the user's build instruction; preserve the exact 31-stage order; implement lossless intake, external source discovery, external requirement research, atomic requirement compilation, conflict resolution, test construction, production, independent verification, defect correction, convergence, audit, acceptance, release hashing, and exact release; operate the finished application through its real rendered UI from a new 0/31 project; export the resulting project through the visible control; publish the accepted application and matching sidecar; deploy them to the configured GitHub Pages URL; and verify the live bytes and behavior.

SUBJECT AND TARGET:
The subject is the complete Closed-Loop Agent Reliability application as a domain-general agent-workflow system. The target is a working phone-first application that can accept software, legal, scientific, engineering, research, comparison, travel, document, mathematical, and other agent-capable jobs without assuming one domain and without using its own work products as authority for their requirements.

PROBLEM AND QUESTION SET:
Determine and prove whether the application can preserve an arbitrary user's complete intent, discover the correct independent external authorities for the job's actual domain, derive traceable requirements from those authorities and the user's explicit requirements, construct tests before production, execute and verify work independently, correct defects at the responsible layer, converge, audit the exact product, make an evidence-based release decision, and deploy only byte-identical accepted output. Determine whether the three information classes remain separate throughout every stage and whether each stage performs only its assigned operation.

SCOPE BOUNDARIES:
INCLUDED: the complete domain-general workflow; all 31 fixed stages; the three information classes; non-circular research; project creation at 0/31; exact intake preservation; dynamic domain and research-question identification; external research; requirements and traceability; acceptance and failure testing; production instructions; independent executions and verifications; defect, regression, correction, convergence, and unchanged confirmation; project persistence, import, export, and separate sidecar loading; phone-first rendered behavior; exact artifact generation; deterministic, semantic, adversarial, representation, process, and product verification; ACCEPTED, REJECTED, or BLOCKED decisions; SHA-256 identity; repository publication; and live GitHub Pages deployment. EXCLUDED: treating the project as merely a patch, migration, demonstration, fixture, sidecar test, or version-number exercise; deriving requirements from the unfinished product; pre-completing project state; fabricating evidence; and releasing unaudited bytes.

SUPPLIED INFORMATION AND INPUTS:
The complete user-issued application build instruction; the exact 31 stage names and order; the definitions of USER JOB INPUT, EXTERNAL RESEARCH SOURCES, and WORKFLOW-GENERATED ARTIFACTS; the absolute non-circularity rule; the Stage 1 twenty-scope intake specification; the Stage 2 external-source-discovery specification; the Stage 3 external-requirement-research specification; the requirements-compilation and conflict-resolution rules; the existing repository implementation and its files classified only as EXISTING_WORK_PRODUCT implementation inputs; the configured repository and GitHub Pages URL; external-research capability; browser automation; visible application controls; SHA-256; and GitHub Actions.

PROVENANCE CLASSIFICATION:
USER_ASSERTION: the desired application purpose, architecture, workflow, constraints, and acceptance conditions. USER_OBSERVATION: reports that prior project framing was a narrow repair/version task and contained out-of-scope details. USER_SUPPLIED_DATA: the exact 31 stage names and order, required information classes, Stage 1 scopes, stage boundaries, and deployment target. USER_SUPPLIED_DOCUMENT: the corrected complete build instruction in the conversation. USER_SUPPLIED_URL: https://sjonesjones917.github.io/closed-loop-tracker/ and the connected repository location. THIRD_PARTY_QUOTATION: none material. PRIOR_AGENT_OUTPUT: prior descriptions, project definitions, source inventories, and conclusions, retained only as conversation history and never as external authority. EXISTING_WORK_PRODUCT: repository HTML, JavaScript, build scripts, workflow files, tests, generated project exports, evidence, and prior deployment artifacts. CANDIDATE_ARTIFACT: any product candidate generated after requirements and tests are established. OTHER_USER_SUPPLIED_INPUT: the available tools and platform names identified by the user.

PRIOR CONVERSATION DEPENDENCIES:
Preserve the user's correction that the project is the complete application build, not a Stage 16-style fix record and not a fictional public version. Preserve the forward direction USER INTENT → EXTERNAL RESEARCH → EXTERNAL AUTHORITY → REQUIREMENTS → TESTS → PRODUCTION → INDEPENDENT VERIFICATION → CORRECTION → ACCEPTED PRODUCT → RELEASE. Preserve the separation between supplied job inputs, independent external sources, and workflow-generated artifacts. Preserve the requirement to do the work through the application and deploy the result rather than merely describe it.

USER-DEFINED TERMINOLOGY:
USER JOB INPUT means information originating from the user or supplied as part of the job. EXTERNAL RESEARCH SOURCE means an independent source discovered outside the artifact and outside this workflow's generated records. WORKFLOW-GENERATED ARTIFACT means any source inventory, research note, requirement, test, prompt, candidate, response, defect record, audit, hash, product, or other record produced by the workflow. FORWARD PIPELINE means requirements are established from user intent and independent authority before production. ACCEPTED means every mandatory requirement has affirmative evidence and every mandatory validator passed. BLOCKED means a mandatory requirement cannot be established. RELEASE means publication of only the exact accepted bytes.

CONSTRAINTS:
Keep exactly the 31 stage numbers, names, and order supplied by the user. Make the application domain-general and phone-first. Start every new project with no completed stages. Preserve complete user intent and all supplied inputs. Require Stage 2 to search outside the project and Stage 3 to derive findings from the resulting external source set. Keep user requirements and externally governed requirements distinct until Stage 4 combines them. Require actual completed responses and evidence before stage completion. Preserve producer/verifier separation and independent run contexts. Revalidate imported and stored state under current rules. Keep the completed project outside the HTML. Preserve exact filenames, bytes, versions, and hashes where the job requires them. Deploy to the configured URL only after acceptance and exact release-hash equality. Do not create a public application version merely because implementation defects were corrected.

PROHIBITED ACTIONS:
Do not redefine the project as repairing a supposed version 13. Do not make a particular implementation defect, filename, candidate, or test fixture the Stage 1 objective. Do not use source code, HTML, JavaScript, tests, generated JSON, generated prompts, candidates, prior workflow output, implementation behavior, or the product itself as external authority for its requirements. Do not skip, silently complete, or reorder stages. Do not fabricate sources, retrieval, executions, tests, evidence, defects, corrections, audits, acceptance, hashes, or deployment. Do not allow the producer to be the sole verifier. Do not release bytes different from the accepted audited product.

REQUIRED METHODS AND PROCESS CONDITIONS:
Use the application's actual rendered UI and visible controls for the self-build verification project. Perform real external retrieval in Stage 2. Derive Stage 3 findings only from the Stage 2 external source set and additional independent authority discovered through the permitted feedback path. Compile atomic traceable requirements before tests and production. Build affirmative acceptance tests and failure/mutation tests. Freeze candidates before independent runs. Use fresh producer and verifier contexts, preserve each output, and verify every run against every mandatory requirement. Root-cause defects to the earliest responsible layer, add regression tests, correct that layer, rerun, converge, and perform unchanged confirmation. Generate the actual product, verify its exact representation and bytes, audit process and product independently, export through the visible project control, deploy through GitHub Pages, and verify the live application and sidecar against the accepted hashes.

REQUIRED OUTPUT PROPERTIES:
A working phone-first Closed-Loop Agent Reliability application with no public version label; a standalone UTF-8 HTML release served as index.html at the configured GitHub Pages URL; an exact visible-UI project export named SELF_VERIFIED_PROJECT.json; automatic same-origin loading of that separate project export without embedding completed project state in the HTML; a persistent project list and sequential workflow UI; all 31 stages; required producer and verifier fields; complete traceability and evidence; machine-readable audit and release records; an ACCEPTED, REJECTED, or BLOCKED decision; matching 64-hex SHA-256 audited and release hashes for accepted output; and reproducible phone-width browser evidence.

TEMPORAL SCOPE:
Current verified release. Time-sensitive external authorities, browser/runtime behavior, platform documentation, repository state, and deployed content must be checked at the time they are used. Preserve retrieval, execution, export, audit, publication, deployment, and verification timestamps.

LOCATION AND JURISDICTION:
The technical deployment location is the configured GitHub Pages project URL. No single legal jurisdiction is asserted as universally controlling for every future user job. Stage 1 of each future project must capture any user-supplied location or jurisdiction, and Stage 2 must research its consequences when relevant. For this application build, legal or regulatory applicability that depends on operator, user, customer, procurement, or data-processing facts remains externally researchable rather than assumed.

SUCCESS AND ACCEPTANCE CONDITIONS:
The deployed application represents the complete domain-general build instruction rather than a narrow repair/version project. Its visible project name, objective, deliverable, Stage 1 result, and downstream work all remain within the complete application-build scope. The app preserves the three information classes and blocks circular authority. A new project begins at 0/31. All 31 stages execute in sequence through the rendered UI with the required substantive stage, producer, and verifier records. External research is actually performed. Mandatory requirements receive tests and evidence. Confirmed defects are corrected through regression and rerun. The final application is independently verified and audited. RELEASE_DECISION is ACCEPTED only when all mandatory evidence passes. AUDITED_HASH and RELEASE_HASH equal the exact released application bytes. The project is exported through the visible control, reloads from a separate sidecar in a clean browser, is not hardcoded into the HTML, and the live URL serves the exact accepted application and matching project.

PRIORITIES AND OPTIMIZATION CRITERIA:
1. Correct scope and complete user intent. 2. Reality and externally observable execution. 3. Independent external authority before requirements and production. 4. Strict information-class separation and non-circularity. 5. Complete stage-specific execution without substitution or shortcuts. 6. Independent verification, correction, and traceability. 7. Exact artifact identity and live deployment. 8. Phone-first usability. 9. Avoidance of arbitrary public versioning and implementation-history framing.

KNOWN UNCERTAINTIES, AMBIGUITIES, CONTRADICTIONS, AND MISSING INFORMATION:
EXTERNALLY_RESEARCHABLE: current web-platform specifications; browser and storage behavior; accessibility requirements; cryptographic specifications; GitHub Actions and Pages behavior; secure development, requirements, testing, V&V, audit, and provenance references; and AI producer/verifier reliability evidence. NONMATERIAL: internal repository filenames that do not define the public application identity. USER_CLARIFICATION_REQUIRED: none required to begin because the application purpose, workflow, information classes, repository, deployment target, and prohibitions are established. BLOCKING: none at Stage 1; any mandatory unavailable external authority or execution capability must be recorded as BLOCKED in its proper later stage.

EXTERNAL RESEARCH QUESTIONS AND DOMAINS:
Which official web-platform specifications govern the application document, controls, events, encoding, files, downloads, URLs, fetch, storage, accessibility tree, and cryptographic digest behavior? Which current browser and automation documentation governs real UI interaction, clean contexts, downloads, screenshots, console/network evidence, mobile viewport behavior, and sidecar loading? Which ECMAScript and Node.js authorities govern build execution, modules, filesystem operations, paths, JSON, byte handling, and SHA-256? Which GitHub Actions and GitHub Pages authorities govern reproducible workflow execution, artifacts, permissions, publication, deployment, and live verification? Which requirements-engineering, testing, V&V, audit, provenance, secure-development, and reproducibility standards or professional references apply? Which external literature governs independent producer/verifier evaluation, evaluator bias, mutation testing, convergence, and correction loops? Which legal, privacy, accessibility, records, procurement, or jurisdictional rules could apply once the relevant operator, user, data, and deployment facts are known? What external evidence is necessary to show the application is complete across arbitrary future job domains rather than only software jobs?

ASSUMPTIONS:
NONE. User-supplied statements define the requested application and scope; external truth and externally governed requirements remain unresolved until researched.

BLOCKERS:
NONE AT STAGE 1.

STAGE_1_EXTERNAL_RESEARCH_PERFORMED: false