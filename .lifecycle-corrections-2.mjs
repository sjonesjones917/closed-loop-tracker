import fs from 'node:fs';
const read=p=>fs.readFileSync(p,'utf8'),write=(p,s)=>fs.writeFileSync(p,s);

// Keep abandonment inside the existing response-disposition/history model; do not add a new project collection.
let s=read('response-ingestion.js');
const re=/function abandon\(project,proposalId,\{operator='HUMAN_OPERATOR',reason='Operator abandoned the pending attempt\.'\}=\{\}\)\{[\s\S]*?\n\}\n\nfunction answerHumanInput/;
if(!re.test(s))throw new Error('Generated abandon() not found for minimal correction.');
const replacement=`function abandon(project,proposalId,{operator='HUMAN_OPERATOR',reason='Operator abandoned the pending attempt.'}={}){\n  const next=clone(project);workflow.ensureShape(next);const proposal=findProposal(next,proposalId);if(!proposal)throw new Error('Response proposal does not exist.');if(proposal.status!=='PENDING_OPERATOR_REVIEW')throw new Error(\`Response proposal is \${proposal.status}, not pending review.\`);\n  const at=now(),note=String(reason||'Operator abandoned the pending attempt.');proposal.status='ABANDONED_BY_OPERATOR';proposal.reviewedAt=at;proposal.reviewedBy=operator;proposal.reviewNote=note;const validation=findValidation(next,proposal.validationId);if(validation)validation.status='ABANDONED_BY_OPERATOR';const raw=findRaw(next,proposal.rawResponseId);if(raw)raw.status='ABANDONED_BY_OPERATOR';const receipt=findReceipt(next,proposal.receiptId);if(receipt){receipt.completionState='ABANDONED_BY_OPERATOR';receipt.nextRequiredVerificationStage=\`REGENERATE STAGE \${String(proposal.stage).padStart(2,'0')} RESPONSE\`;}\n  const d=disposition(next,'ABANDONED_RESPONSE',{stage:proposal.stage,rawResponseId:proposal.rawResponseId,promptId:proposal.promptId,validationId:proposal.validationId,proposalId,receiptId:proposal.receiptId,details:{operator,reason:note,canonicalAcceptedWorkChanged:false,newPromptRequired:true}});workflow.addHistory(next,'RESPONSE_ATTEMPT_ABANDONED',{stage:proposal.stage,rawResponseId:proposal.rawResponseId,proposalId,dispositionId:d.dispositionId,operator,reason:note,canonicalAcceptedWorkChanged:false,newPromptRequired:true});\n  const controlling=safe(next.projectData.generatedPrompts).find(x=>(x.instructionId||x.promptId)===proposal.promptId&&!x.invalidatedBy);if(controlling)controlling.invalidatedBy=d.dispositionId;const promptEngine=globalThis.closedLoopPromptEngine;if(promptEngine){const preview=clone(next);preview.revision=Number(next.revision||0)+1;const generated=promptEngine.buildPromptRecord(proposal.stage,preview,{operation:proposal.envelope.operation,scope:clone(proposal.envelope.scope||{})});workflow.registerGeneratedPrompt(next,{...generated,generatedAt:now()});}\n  workflow.recalculate(next);return {project:next,disposition:d,receipt};\n}\n\nfunction answerHumanInput`;
s=s.replace(re,replacement);write('response-ingestion.js',s);

// Do not increment canonical project revision for a healthy verification that has nothing to repair.
s=read('app-core.js');
const old=`if(verification){const next=clone(current);engine.reconcileArtifactCustodyVerification(next,{report:verification,stage:Number(current.activeStage||1)});await persistReplacement(next);announce(verification.verified?'stored files verified':'stored file integrity mismatch; release blocked and delivery authorization revoked');}`;
const neu=`if(verification){const openCustodyBlocker=engine.records(current,'blockers',{active:false}).some(record=>record.source==='APPLICATION'&&record.applicationBlockerKind==='ARTIFACT_CUSTODY'&&engine.isActiveRecord(record)&&['OPEN','BLOCKED','IN PROGRESS','UNKNOWN'].includes(String(engine.recordValue(record,'STATUS')||'OPEN').toUpperCase()));if(!verification.verified||openCustodyBlocker){const next=clone(current);engine.reconcileArtifactCustodyVerification(next,{report:verification,stage:Number(current.activeStage||1)});await persistReplacement(next);}announce(verification.verified?'stored files verified':'stored file integrity mismatch; release blocked and delivery authorization revoked');}`;
if(!s.includes(old))throw new Error('Generated storage reconciliation block not found.');s=s.replace(old,neu);write('app-core.js',s);

// Ensure the visible stage state explicitly distinguishes abandonment from acceptance/rejection.
s=read('app-core.js');
const oldMode=`function interactionModeMarkup(n){const pending=pendingProposal(),latestValidation=safe(current.projectData.responseValidations).filter(x=>Number(x.stage)===n).at(-1),accepted=acceptedLaneChanges(n).at(-1),requests=safe(current.projectData.humanInputRequests).filter(x=>Number(x.stage)===n&&String(x.status||'OPEN').toUpperCase()==='OPEN');if(accepted)return`;
const newMode=`function interactionModeMarkup(n){const pending=pendingProposal(),latestValidation=safe(current.projectData.responseValidations).filter(x=>Number(x.stage)===n).at(-1),latestDisposition=safe(current.projectData.operationDispositions).filter(x=>Number(x.stage)===n).at(-1),accepted=acceptedLaneChanges(n).at(-1),requests=safe(current.projectData.humanInputRequests).filter(x=>Number(x.stage)===n&&String(x.status||'OPEN').toUpperCase()==='OPEN');if(latestDisposition?.status==='ABANDONED_RESPONSE')return \`<div class="notice"><strong>The pending attempt was discarded.</strong><br>Canonical accepted work changed: NO · Audit state changed: YES · New prompt required: YES. Use only the newly generated authoritative prompt; the abandoned response cannot be accepted later.</div>\`;if(accepted)return`;
if(!s.includes(oldMode))throw new Error('interactionModeMarkup target not found.');s=s.replace(oldMode,newMode);write('app-core.js',s);
