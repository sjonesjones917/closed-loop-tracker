import fs from 'node:fs';
import vm from 'node:vm';
import assert from 'node:assert/strict';
globalThis.Event=globalThis.Event||class Event{constructor(type){this.type=type;}};globalThis.dispatchEvent=globalThis.dispatchEvent||(()=>true);
for(const file of ['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js','prompt-engine.js'])vm.runInThisContext(fs.readFileSync(file,'utf8'),{filename:file});
const core=globalThis.closedLoopCore,schema=globalThis.closedLoopWorkflowSchema,runtime=globalThis.closedLoopTestRuntime,engine=globalThis.closedLoopWorkflowEngine,prompts=globalThis.closedLoopPromptEngine;
assert.equal(core.WORKFLOW_ID,'mobile-closed-loop/30');assert.equal(core.STAGE_COUNT,30);assert.equal(core.PROJECT_SCHEMA,'closed-loop-project/3');assert.equal(schema.RESPONSE_SCHEMA,'closed-loop-stage-response/3');assert.equal(schema.TEST_IR.version,'closed-loop-test-spec/1');
assert.equal(core.STAGES[15].title,'CORRECT THE ROOT CAUSE');assert.equal(schema.JOB_FIELDS.JOB_TITLE.producer,'HUMAN_DECISION');assert.equal(schema.JOB_FIELDS.JOB_OWNER.producer,'HUMAN_DECISION');
assert.deepEqual(schema.TEST_IR.executableKinds,['NONE','TEST_IR']);assert.deepEqual(engine.applicationTestCapabilities(),runtime.capabilities());
const html=fs.readFileSync('index.html','utf8');assert.match(html,/worker-src 'self'/);const order=['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js'];const loaded=[...html.matchAll(/<script defer src="([^"]+)/g)].map(m=>m[1].split('?')[0]);assert.deepEqual(loaded,order);assert.doesNotMatch(html,/max-height:s*88px/);
const p=core.createBlankState('JOB-NEUTRAL');Object.assign(p.job,{EXACT_USER_OBJECTIVE_VERBATIM:'Create the requested deliverable from the supplied project intent.',SUPPLIED_MATERIALS_INVENTORY:'intent.file',CURRENT_INPUT_VERSION:'INPUT-v001',CURRENT_SOURCE_SET_VERSION:'SOURCE-SET-v001',EXACT_DELIVERABLE_REQUESTED:'Requested deliverable',ASSUMPTIONS:'NONE',UNKNOWN_INFORMATION:'NONE',INPUT_SET_CONTENTS:'Captured project-specific human authority and constraints.'});engine.ensureShape(p);engine.recalculate(p);for(const stage of [1,4,6,8,21,23,24]){const r=prompts.buildPromptRecord(stage,p);for(const prohibited of ['PATENT / REGULATED FILING','SOFTWARE / MULTI-FILE SYSTEM','BUILDING / ARCHITECTURE / AEC','PHYSICAL / MECHANICAL / CAD / CAM / CNC / ADDITIVE'])assert.ok(!r.prompt.includes(prohibited),stage+' leaked '+prohibited);assert.match(r.prompt,/PROJECT-SUBJECT-NEUTRAL/);}
const v2=JSON.parse(JSON.stringify(p));v2.schema='closed-loop-project/2';const migrated=core.migrateState(v2);assert.equal(migrated.schema,'closed-loop-project/3');assert.equal(migrated.projectData.migrationArchives.at(-1).schema,'closed-loop-project/2');
const action=engine.operationalNextAction(p,1);assert.equal(typeof action,'object');assert.ok(action.actionType&&Object.prototype.hasOwnProperty.call(action,'canonicalStateChanged'));
console.log('verify-controlling-spec: PASS');
