import fs from 'node:fs';const assert=(v,m)=>{if(!v)throw new Error(m)};const wb=fs.readFileSync('workbook.js','utf8'),schema=fs.readFileSync('workflow-schema.js','utf8'),prompt=fs.readFileSync('prompt-engine.js','utf8'),engine=fs.readFileSync('workflow-engine.js','utf8'),ingest=fs.readFileSync('response-ingestion.js','utf8'),html=fs.readFileSync('index.html','utf8');assert(wb.includes("PROJECT_SCHEMA='closed-loop-project/3'"),'project schema');assert(schema.includes("RESPONSE_SCHEMA='closed-loop-stage-response/3'"),'response schema');assert(wb.includes("p.schema==='closed-loop-project/2'"),'migration');assert(wb.includes("'CORRECT THE ROOT CAUSE'"),'stage16 title');assert(!/PATENT \/ REGULATED FILING|SOFTWARE \/ MULTI-FILE SYSTEM|BUILDING \/ ARCHITECTURE \/ AEC|PHYSICAL \/ MECHANICAL/.test(prompt),'hard-coded subject branch');assert(prompt.includes('PROJECT DATA IS INPUT, NOT THE TASK'),'task directive');assert(prompt.includes('never ask the human to retype it, re-explain it, resend it, or re-attach the same material'),'capture once');assert(prompt.includes('APPLICATION-ENUMERATED STAGE 04 OBLIGATION MANIFEST'),'stage4 manifest prompt');assert(prompt.includes('APPLICATION-ENUMERATED INTAKE COVERAGE MANIFEST'),'stage1 manifest prompt');assert(!prompt.includes('EXECUTABLE_KIND = CUSTOM_PIPELINE'),'custom pipeline');assert(prompt.includes('EXECUTABLE_KIND = TEST_IR'),'test ir');assert(engine.includes('function buildIntakeCoverageManifest'),'intake manifest');assert(engine.includes('function buildObligationManifest'),'obligation manifest');assert(engine.includes('function commitAccountingClosure'),'durable accounting commit');assert(ingest.includes('INCOMPLETE_INTAKE_ACCOUNTING'),'intake closure');assert(ingest.includes('INCOMPLETE_OBLIGATION_ACCOUNTING'),'obligation closure');assert(html.includes("worker-src 'self'"),'worker csp');const scripts=[...html.matchAll(/<script\s+defer\s+src="([^"?]+)\?v=[^"]+"\s*><\/script>/g)].map(m=>m[1]);assert(JSON.stringify(scripts)===JSON.stringify(['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js']),'script order');assert(html.includes('.expandable-prompt{max-height:280px}'),'280px prompt preview changed');assert(!/\.expandable-prompt\{max-height:88px\}/.test(html),'88px prompt override');console.log('verify-controlling-spec: PASS');