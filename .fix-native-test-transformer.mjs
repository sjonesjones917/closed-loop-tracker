import fs from 'node:fs';
const path='.apply-native-test-capability-truth.mjs';
let text=fs.readFileSync(path,'utf8');
const startMarker='// Operator guidance must expose unsupported application-native claims as a correction, not as "no action required".';
const endMarker='// Semantic and gate regressions close both false-native-executor and canonical test-artifact linkage instructions.';
const start=text.indexOf(startMarker),end=text.indexOf(endMarker);
if(start<0||end<0||end<=start)throw new Error('Malformed app-core transformer block markers were not found.');
const replacement=`// Operator guidance must expose unsupported application-native claims as a correction, not as "no action required".\ninsertBefore('app-core.js',\n  "  const intro=n===6?'Stage 6 defines how each requirement will be verified. A test definition is not automatically executable code and does not mean a test has run.':'Execution responsibility comes from the accepted test definitions; use only the executor and capability actually declared for each test.';",\n  "  if(plan.unsupportedApplicationTestIds.length)actionRows.unshift({mode:'Invalid application executor claim',tests:plan.unsupportedApplicationTestIds.length,nextAction:'Request a corrected Stage 6 test definition. No registered application-native executor exists for the declared capability; do not run or accept a substitute execution.'});\\n");\n\n`;
text=text.slice(0,start)+replacement+text.slice(end);
text=text.replace("if(!ui.includes('application-native execution without a registered executor'))throw new Error('Operator UI does not fail unsupported application-native test claims closed.');","if(!ui.includes('Invalid application executor claim')||!ui.includes('No registered application-native executor exists'))throw new Error('Operator UI does not fail unsupported application-native test claims closed.');");
fs.writeFileSync(path,text);
