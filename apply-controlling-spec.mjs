import fs from 'node:fs';
import crypto from 'node:crypto';
const R=p=>fs.readFileSync(p,'utf8'),W=(p,s)=>fs.writeFileSync(p,s),must=(s,n,l)=>{if(!s.includes(n))throw new Error('Missing anchor '+l+': '+n)},rep=(s,a,b,l)=>{must(s,a,l);return s.replace(a,b)};

// 1. Workflow identity/schema/migration and visible Stage 16 wording.
let w=R('workbook.js');
w=rep(w,"const PROJECT_SCHEMA='closed-loop-project/2';","const PROJECT_SCHEMA='closed-loop-project/3';\nconst LEGACY_PROJECT_SCHEMA='closed-loop-project/2';",'project schema');
w=w.replace("'REVISE THE RESPONSIBLE LAYER'","'CORRECT THE ROOT CAUSE'");
w=w.replace("if(p.schema!=='human-project/30')throw new Error(`Unsupported project schema: ${p.schema||'MISSING'}`);","if(!['human-project/30',LEGACY_PROJECT_SCHEMA].includes(p.schema))throw new Error(`Unsupported project schema: ${p.schema||'MISSING'}`);");
w=w.replaceAll("schema:'human-project/30'","schema:String(original.schema||'human-project/30')");
W('workbook.js',w);

// 2. Explicit ownership, /3 response contract, accounting fields, exact Test IR identity.
let s=R('workflow-schema.js');
s=s.replace("const RESPONSE_SCHEMA='closed-loop-stage-response/2';","const RESPONSE_SCHEMA='closed-loop-stage-response/3';");
s=s.replace("  'JOB_TITLE','JOB_OWNER','EXACT_USER_OBJECTIVE_VERBATIM','SUPPLIED_MATERIALS_INVENTORY',","  'EXACT_USER_OBJECTIVE_VERBATIM','SUPPLIED_MATERIALS_INVENTORY',");
s=s.replace("const APPLICATION_JOB_FIELDS=Object.freeze([","const HUMAN_DECISION_JOB_FIELDS=Object.freeze(['JOB_TITLE','JOB_OWNER']);\nconst APPLICATION_JOB_FIELDS=Object.freeze([");
s=s.replace("  if(HUMAN_JOB_FIELDS.includes(name))return field(name,PRODUCER.HUMAN,","  if(HUMAN_DECISION_JOB_FIELDS.includes(name))return field(name,PRODUCER.HUMAN_DECISION,{provenanceRequired:false,nullable:true});\n  if(HUMAN_JOB_FIELDS.includes(name))return field(name,PRODUCER.HUMAN,");
s=s.replace("[...new Set([...HUMAN_JOB_FIELDS,...APPLICATION_JOB_FIELDS,...AGENT_JOB_FIELDS])].map","[...new Set([...HUMAN_JOB_FIELDS,...HUMAN_DECISION_JOB_FIELDS,...APPLICATION_JOB_FIELDS,...AGENT_JOB_FIELDS])].map");
s=s.replace("executableKinds:Object.freeze(['NONE','CUSTOM_PIPELINE'])","executableKinds:Object.freeze(['NONE','TEST_IR'])");
s=s.replace("operations:Object.freeze(['LOAD_ARTIFACT','READ_BYTES','DECODE_UTF8','PARSE_JSON','PARSE_CSV','SELECT_JSON_PATH','COUNT','SUM','MIN','MAX','SORT','UNIQUE','HASH_SHA256','REGEX','COMPARE','BYTE_COMPARE','ASSERT_EXISTS','ASSERT_TYPE','ASSERT_EQ','ASSERT_NE','ASSERT_GT','ASSERT_GTE','ASSERT_LT','ASSERT_LTE','ASSERT_MATCH','ASSERT_CONTAINS','ASSERT_NOT_CONTAINS','ASSERT_SET_EQUAL'])","operations:Object.freeze(['LOAD_ARTIFACT','READ_BYTES','DECODE_UTF8','PARSE_JSON','PARSE_CSV','PARSE_XML','SELECT_JSON_PATH','SELECT_XML','COUNT','SUM','MIN','MAX','SORT','UNIQUE','HASH_SHA256','REGEX','COMPARE','ASSERT_EQ','ASSERT_GT','ASSERT_GTE','ASSERT_LT','ASSERT_LTE','ASSERT_MATCH','ASSERT_CONTAINS','ASSERT_NOT_CONTAINS','ASSERT_SET_EQUAL','BYTE_COMPARE'])");
s=s.replaceAll("CUSTOM_PIPELINE","TEST_IR");
// Tests: version/hash are application owned, executable content/bindings remain agent owned.
s=s.replace('      "EXECUTABLE_KIND",\n      "EXECUTABLE_SPEC_VERSION",\n      "EXECUTABLE_SPEC",','      "EXECUTABLE_KIND",\n      "EXECUTABLE_SPEC",');
s=s.replace('      "TEST_ID",\n      "REQ_ID",\n      "STATUS"','      "TEST_ID",\n      "REQ_ID",\n      "EXECUTABLE_SPEC_VERSION",\n      "EXECUTABLE_SPEC_SHA256",\n      "STATUS"');
s=s.replace("'TEST_ID','REQ_ID','TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','EXECUTABLE_KIND','EXECUTABLE_SPEC_VERSION','EXECUTABLE_SPEC','EXECUTABLE_INPUT_BINDINGS','INPUTS'","'TEST_ID','REQ_ID','TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','EXECUTABLE_KIND','EXECUTABLE_SPEC_VERSION','EXECUTABLE_SPEC_SHA256','EXECUTABLE_SPEC','EXECUTABLE_INPUT_BINDINGS','INPUTS'");
s=s.replace("    EXECUTABLE_SPEC_VERSION:Object.freeze({valueType:VALUE_TYPES.STRING,enumValues:[],nullable:true,normalizerKey:null,closedProperties:null}),","    EXECUTABLE_SPEC_VERSION:Object.freeze({valueType:VALUE_TYPES.STRING,enumValues:[],nullable:true,normalizerKey:null,closedProperties:null}),\n    EXECUTABLE_SPEC_SHA256:Object.freeze({valueType:VALUE_TYPES.STRING,enumValues:[],nullable:true,normalizerKey:null,closedProperties:null}),");
// Preserve the recent statement ledger as compatibility data, but do not let Stage 03/04 use it as an alternate authority path. Stage 04 gets human obligations only through the manifest.
s=s.replace("3:['intentStatements','sources','sourceConflicts'],4:['intentStatements','research','candidateRequirements','sources']","3:['sources','sourceConflicts'],4:['research','candidateRequirements','sources']");
// Response-only accounting fields. They are not human-editable top-level collections.
const sf=`const STAGE_FIELDS=Object.freeze(Object.fromEntries(core.STAGES.map(stage=>[\n  stage.number,\n  Object.freeze(Object.fromEntries(stage.fields.map(name=>[name,stageFieldDefinition(stage.number,name)])))\n])));`;
const sf2=`const ACCOUNTING_STAGE_FIELD_DEFINITIONS=Object.freeze({\n  1:Object.freeze({INTAKE_ACCOUNTING:field('INTAKE_ACCOUNTING',PRODUCER.AGENT,{requiredAtStage:1,valueType:'OBJECT_ARRAY',closedProperties:['unitId','disposition','reason']})}),\n  4:Object.freeze({OBLIGATION_ACCOUNTING:field('OBLIGATION_ACCOUNTING',PRODUCER.AGENT,{requiredAtStage:4,valueType:'OBJECT_ARRAY',closedProperties:['obligationId','disposition','requirementTempKeys','reason']})})\n});\nconst STAGE_FIELDS=Object.freeze(Object.fromEntries(core.STAGES.map(stage=>[\n  stage.number,\n  Object.freeze({...Object.fromEntries(stage.fields.map(name=>[name,stageFieldDefinition(stage.number,name)])),...(ACCOUNTING_STAGE_FIELD_DEFINITIONS[stage.number]||{})})\n])));`;
s=rep(s,sf,sf2,'stage accounting definitions');
W('workflow-schema.js',s);

// 3. Deterministic Test IR runtime: exact primitives, application-owned operation contracts and limits.
let tr=R('test-runtime.js');
tr=tr.replace(/const OPS=Object\.freeze\(\[[\s\S]*?\]\);/,`const OPS=Object.freeze([\n  'LOAD_ARTIFACT','READ_BYTES','DECODE_UTF8','PARSE_JSON','PARSE_CSV','PARSE_XML','SELECT_JSON_PATH','SELECT_XML',\n  'COUNT','SUM','MIN','MAX','SORT','UNIQUE','HASH_SHA256','REGEX','COMPARE','ASSERT_EQ','ASSERT_GT','ASSERT_GTE','ASSERT_LT','ASSERT_LTE','ASSERT_MATCH','ASSERT_CONTAINS','ASSERT_NOT_CONTAINS','ASSERT_SET_EQUAL','BYTE_COMPARE'\n]);`);
tr=tr.replace(/const LIMITS=Object\.freeze\(\{[^}]+\}\);/,"const LIMITS=Object.freeze({maxInputBytes:16*1024*1024,maxDecompressedBytes:32*1024*1024,maxSteps:64,maxSelectorDepth:32,maxParsedDepth:64,maxCollectionItems:100000,maxRegexLength:2000,maxRegexInputBytes:4*1024*1024,maxCsvCells:250000,maxWorkerDurationMs:10000,maxArchiveExpansionBytes:32*1024*1024,maxTextBytes:16*1024*1024});");
tr=tr.replaceAll('CUSTOM_PIPELINE','TEST_IR');
const contracts=`const OPERATION_CONTRACTS=Object.freeze({\n LOAD_ARTIFACT:{required:['op','binding'],allowed:['op','binding'],types:{binding:'string'}},READ_BYTES:{required:['op'],allowed:['op']},DECODE_UTF8:{required:['op'],allowed:['op']},PARSE_JSON:{required:['op'],allowed:['op']},\n PARSE_CSV:{required:['op','delimiter','header','quote','newline','encoding'],allowed:['op','delimiter','header','quote','newline','encoding'],types:{delimiter:'string',header:'boolean',quote:'string',newline:'string',encoding:'string'}},PARSE_XML:{required:['op'],allowed:['op']},\n SELECT_JSON_PATH:{required:['op','path'],allowed:['op','path'],types:{path:'string'}},SELECT_XML:{required:['op','path'],allowed:['op','path'],types:{path:'string'}},COUNT:{required:['op'],allowed:['op']},SUM:{required:['op'],allowed:['op']},MIN:{required:['op'],allowed:['op']},MAX:{required:['op'],allowed:['op']},SORT:{required:['op'],allowed:['op']},UNIQUE:{required:['op'],allowed:['op']},HASH_SHA256:{required:['op'],allowed:['op']},\n REGEX:{required:['op','pattern'],allowed:['op','pattern','flags'],types:{pattern:'string',flags:'string'}},COMPARE:{required:['op','value'],allowed:['op','value','absoluteTolerance','relativeTolerance']},\n ASSERT_EQ:{required:['op','value'],allowed:['op','value','absoluteTolerance','relativeTolerance','message']},ASSERT_GT:{required:['op','value'],allowed:['op','value','message']},ASSERT_GTE:{required:['op','value'],allowed:['op','value','message']},ASSERT_LT:{required:['op','value'],allowed:['op','value','message']},ASSERT_LTE:{required:['op','value'],allowed:['op','value','message']},ASSERT_MATCH:{required:['op','pattern'],allowed:['op','pattern','flags','message'],types:{pattern:'string',flags:'string'}},ASSERT_CONTAINS:{required:['op','value'],allowed:['op','value','message']},ASSERT_NOT_CONTAINS:{required:['op','value'],allowed:['op','value','message']},ASSERT_SET_EQUAL:{required:['op','value'],allowed:['op','value','message'],types:{value:'array'}},BYTE_COMPARE:{required:['op','binding'],allowed:['op','binding'],types:{binding:'string'}}\n});\n`;
tr=tr.replace("const FORBIDDEN_STEP_KEYS=Object.freeze(['code','javascript','python','shell','command','eval','function','script']);",contracts+"const FORBIDDEN_STEP_KEYS=Object.freeze(['code','javascript','python','shell','command','eval','function','script']);");
// Replace validation loop with closed operation contracts.
tr=tr.replace("    if(!OPS.includes(step.op))issues.push(`Step ${index} uses unsupported operation ${String(step.op)}.`);\n    for(const key of FORBIDDEN_STEP_KEYS)if(Object.prototype.hasOwnProperty.call(step,key))issues.push(`Step ${index} contains forbidden executable field ${key}.`);\n    if(step.op==='REGEX'||step.op==='ASSERT_MATCH')if(String(step.pattern??step.value??'').length>LIMITS.maxRegexLength)issues.push(`Step ${index} regex exceeds maximum length.`);","    if(!OPS.includes(step.op)){issues.push(`Step ${index} uses unsupported operation ${String(step.op)}.`);continue;}\n    const contract=OPERATION_CONTRACTS[step.op],allowed=new Set(contract.allowed);for(const key of Object.keys(step))if(!allowed.has(key))issues.push(`Step ${index} operation ${step.op} contains unknown property ${key}.`);for(const key of contract.required)if(!Object.prototype.hasOwnProperty.call(step,key))issues.push(`Step ${index} operation ${step.op} is missing required argument ${key}.`);for(const [key,type] of Object.entries(contract.types||{})){if(!Object.prototype.hasOwnProperty.call(step,key))continue;const value=step[key],ok=type==='array'?Array.isArray(value):typeof value===type;if(!ok)issues.push(`Step ${index} argument ${key} must be ${type}.`);}\n    for(const key of FORBIDDEN_STEP_KEYS)if(Object.prototype.hasOwnProperty.call(step,key))issues.push(`Step ${index} contains forbidden executable field ${key}.`);\n    if((step.op==='REGEX'||step.op==='ASSERT_MATCH')&&String(step.pattern??'').length>LIMITS.maxRegexLength)issues.push(`Step ${index} regex exceeds maximum length.`);if(step.op==='PARSE_CSV'&&(String(step.delimiter).length!==1||String(step.quote).length!==1||!['LF','CRLF','AUTO'].includes(step.newline)||step.encoding!=='UTF-8'))issues.push(`Step ${index} PARSE_CSV contract is invalid.`);if(['ASSERT_EQ','COMPARE'].includes(step.op)&&typeof step.value==='number'&&!Number.isInteger(step.value)&&step.absoluteTolerance===undefined&&step.relativeTolerance===undefined)issues.push(`Step ${index} precision-sensitive numeric comparison requires explicit tolerance or an exact decimal string.`);");
// Add XML support if not already present.
if(!tr.includes("case 'PARSE_XML'"))tr=tr.replace("      case 'PARSE_CSV':value=parseCsv(String(value));break;","      case 'PARSE_CSV':value=parseCsv(String(value),step);break;\n      case 'PARSE_XML':value=parseXml(String(value));break;");
if(!tr.includes('function parseXml'))tr=tr.replace('function selectJsonPath',`function parseXml(text){const src=String(text||'').trim();if(!src.startsWith('<'))throw new Error('PARSE_XML requires XML text.');const tokens=src.match(/<[^>]+>|[^<]+/g)||[],root={name:'#document',children:[]},stack=[root];let depth=0;for(const token of tokens){if(token.startsWith('<?')||token.startsWith('<!--'))continue;if(token.startsWith('</')){const node=stack.pop(),name=token.slice(2,-1).trim();if(!node||node.name!==name)throw new Error('Malformed XML closing tag.');depth--;continue;}if(token.startsWith('<')){const selfClose=/\\/\\s*>$/.test(token),m=token.match(/^<\\s*([A-Za-z_][A-Za-z0-9_.:-]*)(?:\\s[^>]*)?\\/?>$/);if(!m)throw new Error('Unsupported XML syntax.');const node={name:m[1],children:[],text:''};stack.at(-1).children.push(node);if(!selfClose){stack.push(node);if(++depth>LIMITS.maxParsedDepth)throw new Error('XML exceeds parsed-depth limit.');}}else if(stack.length>1)stack.at(-1).text+=token;}if(stack.length!==1)throw new Error('Malformed XML.');return root.children.length===1?root.children[0]:root;}\nfunction selectXml(value,path){const text=String(path||'').trim();if(!/^\\/?[A-Za-z_][A-Za-z0-9_.:-]*(?:\\/[A-Za-z_][A-Za-z0-9_.:-]*)*$/.test(text))throw new Error('Unsupported XML selector.');const parts=text.replace(/^\\//,'').split('/');if(parts.length>LIMITS.maxSelectorDepth)throw new Error('XML selector depth exceeded.');let nodes=[value];for(const [i,name] of parts.entries()){if(i===0&&nodes.length===1&&nodes[0]?.name===name)continue;nodes=nodes.flatMap(n=>(n?.children||[]).filter(c=>c.name===name));}return nodes;}\nfunction selectJsonPath`);
if(!tr.includes("case 'SELECT_XML'"))tr=tr.replace("      case 'SELECT_JSON_PATH':value=selectJsonPath(value,step.path);break;","      case 'SELECT_JSON_PATH':value=selectJsonPath(value,step.path);break;\n      case 'SELECT_XML':value=selectXml(value,step.path);break;");
tr=tr.replace("case 'PARSE_CSV':value=parseCsv(String(value));break;","case 'PARSE_CSV':value=parseCsv(String(value),step);break;");
// Explicit CSV arguments.
tr=tr.replace("function parseCsv(text){","function parseCsv(text,config){const delimiter=String(config?.delimiter??''),quote=String(config?.quote??''),newline=String(config?.newline??''),header=config?.header,encoding=String(config?.encoding??'');if(delimiter.length!==1||quote.length!==1||!['LF','CRLF','AUTO'].includes(newline)||typeof header!=='boolean'||encoding!=='UTF-8')throw new Error('PARSE_CSV requires explicit delimiter, header, quote, newline, and UTF-8 encoding.');");
tr=tr.replaceAll("ch==='\"'","ch===quote").replaceAll("text[i+1]==='\"'","text[i+1]===quote").replaceAll("cell+='\"'","cell+=quote").replaceAll("ch===','","ch===delimiter");
tr=tr.replace("  return rows;\n}\nfunction parseXml","  if(header&&rows.length){const keys=rows.shift();return rows.map(row=>Object.fromEntries(keys.map((key,i)=>[key,row[i]??''])));}\n  return rows;\n}\nfunction parseXml");
// Remove non-v1 switch cases if present.
tr=tr.replace(/\n      case 'ASSERT_EXISTS':[\s\S]*?case 'ASSERT_EQ':/,"\n      case 'ASSERT_EQ':").replace(/\n      case 'ASSERT_NE':[\s\S]*?case 'ASSERT_GT':/,"\n      case 'ASSERT_GT':");
tr=tr.replace("globalThis.closedLoopTestRuntime=Object.freeze({VERSION,SPEC_VERSION,CAPABILITY,OPS,LIMITS,validateSpec,validateBindings,supports,execute,capabilities:()=>Object.freeze([CAPABILITY])});","globalThis.closedLoopTestRuntime=Object.freeze({VERSION,SPEC_VERSION,CAPABILITY,OPS,LIMITS,OPERATION_CONTRACTS,validateSpec,validateBindings,supports,execute,capabilities:()=>Object.freeze([CAPABILITY]),operationContracts:()=>OPERATION_CONTRACTS});");
W('test-runtime.js',tr);

// 4. Engine authority, pure durable manifests, structured operational action.
let e=R('workflow-engine.js');
e=e.replace("if(!core||!schema||!hash)throw new Error('workbook.js, hash.js, and workflow-schema.js must load before workflow-engine.js.');","const testRuntime=globalThis.closedLoopTestRuntime;\nif(!core||!schema||!hash||!testRuntime)throw new Error('workbook.js, hash.js, workflow-schema.js, and test-runtime.js must load before workflow-engine.js.');");
e=e.replace("  'history','newJobResets','reviews','recoveredProjects','responseDispositions','executionFailures'","  'history','newJobResets','reviews','recoveredProjects','responseDispositions','executionFailures','intakeCoverageManifests','obligationManifests','blindAliasMaps','nativeExecutionEvents'");
e=e.replace("function applicationTestCapabilities(){return Object.freeze([schema.TEST_IR.capability]);}\nfunction applicationTestSupported(test){return schema.validateTestIRTest(test).valid;}","function applicationTestCapabilities(){return Object.freeze([...testRuntime.capabilities()]);}\nfunction applicationTestSupported(test){return testRuntime.supports(test);}");
const manifestFns=`function intakeCoverageManifest(project){ensureShape(project);const inputVersion=String(project.job.CURRENT_INPUT_VERSION||'UNVERSIONED'),units=[];for(const [name,definition] of Object.entries(schema.JOB_FIELDS||{})){if(!['HUMAN','HUMAN_DECISION'].includes(definition.producer))continue;const raw=project.job?.[name];if(raw===undefined||raw===null||String(raw).trim()==='')continue;const values=typeof raw==='string'?String(raw).split(/\\r?\\n/).map(x=>x.trim()).filter(Boolean):[JSON.stringify(raw)];for(const [index,value] of values.entries()){const rawValueHash=hash.sha256Value(value),unitId='INPUT-UNIT-'+hash.sha256Value({inputVersion,name,index,rawValueHash}).slice(0,20).toUpperCase();units.push({unitId,kind:'HUMAN_VALUE',field:name,index,sourceLocation:'User Job Input/'+name,rawValueHash,value});}}for(const artifact of recordsForCurrentScope(project,'artifacts')){const role=upper(recordValue(artifact,'ROLE'));if(role&&!['INPUT','SUPPLIED_MATERIAL','SOURCE_INPUT','PROJECT_INPUT'].includes(role))continue;const artifactId=recordId(artifact,'artifacts'),rawValueHash=String(recordValue(artifact,'SHA256')||artifact.sha256||'');if(!artifactId||!rawValueHash)continue;units.push({unitId:'INPUT-UNIT-'+hash.sha256Value({inputVersion,artifactId,rawValueHash}).slice(0,20).toUpperCase(),kind:'SUPPLIED_ARTIFACT',artifactId,sourceLocation:String(recordValue(artifact,'FILENAME')||artifactId),rawValueHash,value:'Byte-backed supplied artifact; substantive statements are extracted by Stage 01 when the executing context receives the bytes.'});}const manifestHash=hash.sha256Value({inputVersion,units:units.map(({unitId,kind,field,index,artifactId,sourceLocation,rawValueHash})=>({unitId,kind,field,index,artifactId,sourceLocation,rawValueHash}))});return {manifestId:'INTAKE-MANIFEST-'+manifestHash.slice(0,20).toUpperCase(),inputVersion,manifestHash,units,applicationOwned:true};}\nfunction obligationManifest(project){ensureShape(project);const intake=intakeCoverageManifest(project),scope=currentScope(project),items=[];const add=(origin,provenance,text,sourceId=null)=>{const value=String(text??'').trim();if(!value||['NONE','UNKNOWN','NOT APPLICABLE'].includes(upper(value)))return;for(const [index,line] of value.split(/\\r?\\n/).map(x=>x.trim()).filter(Boolean).entries()){items.push({obligationId:'OBLIGATION-'+hash.sha256Value({origin,provenance,index,line,scope}).slice(0,20).toUpperCase(),origin,provenance,sourceId,text:line});}};for(const unit of intake.units)if(unit.kind==='HUMAN_VALUE')add('HUMAN',unit.unitId,unit.value);for(const statement of currentIntentStatements(project)){const rel=upper(recordValue(statement,'REQUIREMENT_RELEVANCE'));if(rel==='REQUIREMENT')add('SUPPLIED_MATERIAL',recordId(statement,'intentStatements'),recordValue(statement,'EXACT_STATEMENT'));}for(const key of ['EXACT_DELIVERABLE_REQUESTED','INPUT_SET_CONTENTS'])add('STAGE_01','Stage01/'+key,project.stages?.[1]?.agentData?.[key]??project.job?.[key]);for(const record of recordsForCurrentScope(project,'candidateRequirements'))add('EXTERNAL_SOURCE','candidateRequirements/'+recordId(record,'candidateRequirements'),recordValue(record,'CANDIDATE_OBLIGATION'),String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||''));for(const record of recordsForCurrentScope(project,'research'))for(const key of ['MANDATORY_STATEMENTS','PROHIBITIONS','EXCEPTIONS','DEPENDENCIES','RESTRICTIONS','INVALIDATING_MATERIAL'])add('SOURCE_RESEARCH','research/'+recordId(record,'research')+'/'+key,recordValue(record,key),String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||''));const unique=[...new Map(items.map(x=>[x.obligationId,x])).values()],manifestHash=hash.sha256Value({scope,items:unique});return {manifestId:'OBLIGATION-MANIFEST-'+manifestHash.slice(0,20).toUpperCase(),scope,manifestHash,items:unique,applicationOwned:true};}\n`;
const appCapIdx=e.indexOf('function applicationTestCapabilities()');if(appCapIdx<0)throw new Error('applicationTestCapabilities missing');e=e.slice(0,appCapIdx)+manifestFns+e.slice(appCapIdx);
// Persist manifest snapshots when registering their controlling prompt.
const reg="  const record={...clone(promptRecord),source:'APPLICATION_PROMPT_REGISTRATION'};project.projectData.generatedPrompts.push(record);project.stages[Number(record.stage)].currentPromptId=record.instructionId;";
const reg2=reg+"\n  for(const [key,collection] of [['intakeCoverageManifest','intakeCoverageManifests'],['obligationManifest','obligationManifests']]){const manifest=record.contextManifest?.[key];if(manifest&&!safe(project.projectData[collection]).some(x=>x.manifestId===manifest.manifestId))project.projectData[collection].push({...clone(manifest),createdAt:now(),promptId:record.instructionId,promptContextSignature:record.contextSignature});}";
e=rep(e,reg,reg2,'prompt manifest persistence');
// Replace free-form next action function with required shape while delegating test routing to testExecutionPlan.
const os=e.indexOf('function operationalNextAction(project,currentStage){'),oe=e.indexOf('\n\nfunction applicationTestCapabilities',os);if(os<0||oe<0)throw new Error('operationalNextAction anchors missing');
const action=`function operationalNextAction(project,currentStage){const stage=Number(currentStage||1),base={actionType:'PASTE_FINAL_JSON',heading:'Use the current instruction',explanation:'Continue the current controlled stage and return the final structured response only when the work or conversation is complete.',primaryButton:'Copy current instruction',secondaryAction:null,filesToSend:[],filesToWithhold:[],expectedReturnFiles:[],blockingReason:null,canonicalStateChanged:false,newPromptRequired:false};const questions=unresolvedHumanRequests(project,stage);if(questions.length)return {...base,actionType:'CONTINUE_AGENT_CONVERSATION',heading:'Answer the current human-only question',explanation:'Provide only the genuinely human-owned answer. Saving it creates a new input version and supersedes the old prompt.',newPromptRequired:true};if(stage===16){const plan=stage16CorrectionPlan(project);return {...base,actionType:plan.actionType==='HUMAN_AUTHORITY'?'CONTINUE_AGENT_CONVERSATION':plan.actionType==='CONTROLLED_CORRECTION'?'AI_REVIEW':plan.actionType==='BLOCKED'?'BLOCKED':'COMPLETE',heading:plan.heading,explanation:plan.explanation,blockingReason:plan.actionType==='BLOCKED'?plan.explanation:null};}const items=[12,22,23,24].includes(stage)?testExecutionPlan(project).items:[],blocked=items.find(x=>!x.executableNow);if(blocked)return {...base,actionType:'BLOCKED',heading:'Verification is blocked',explanation:blocked.blockingReason||'Required capability or bytes are unavailable.',primaryButton:null,blockingReason:blocked.blockingReason||'Required capability or bytes are unavailable.',filesToSend:blocked.handoff?.send||[],filesToWithhold:blocked.handoff?.withhold||[],expectedReturnFiles:blocked.handoff?.expectBack||[]};const item=items.find(x=>x.operatorAction!=='NO_ACTION');if(item){const common={...base,filesToSend:item.handoff?.send||[],filesToWithhold:item.handoff?.withhold||[],expectedReturnFiles:item.handoff?.expectBack||[]};if(item.operatorAction==='SEND_TO_INDEPENDENT_REVIEWER')return {...common,actionType:'AI_REVIEW',heading:'Open a fresh independent reviewer context',explanation:'Use only the current application-selected review package and withhold prohibited prior conclusions.',primaryButton:'Prepare review package'};if(item.operatorAction==='SEND_TO_TOOL_AGENT')return {...common,actionType:'EXTERNAL_AGENT_TOOL',heading:'Use the required tool-capable agent',explanation:'Use the exact declared capability and current package.',primaryButton:'Prepare execution package'};if(item.operatorAction==='HUMAN_INSPECTION')return {...common,actionType:'HUMAN_INSPECTION',heading:'Human inspection is required',explanation:'Perform the specified observation and preserve human-owned evidence.',primaryButton:'Review inspection instructions'};if(item.operatorAction==='USE_EXTERNAL_SYSTEM')return {...common,actionType:'EXTERNAL_SYSTEM',heading:'Use the required external system',explanation:'Use the named system and return attributable evidence.',primaryButton:'Prepare external-system package'};}if(items.length&&items.every(x=>x.operatorAction==='NO_ACTION'))return {...base,actionType:'RUN_APP_TESTS',heading:'Run verification in the application',explanation:'No external action is required for the current supported deterministic tests.',primaryButton:'Run tests'};if(stage===4)return {...base,actionType:'AI_REVIEW',heading:'Compile the complete requirement specification',explanation:'The prompt contains the complete application-enumerated obligation universe. Do not ask the user to resend original project intent.',primaryButton:'Copy current instruction'};return base;}`;
e=e.slice(0,os)+action+e.slice(oe);
e=e.replace('testExecutionPlan,executionHandoff,evaluateContextIndependence','testExecutionPlan,executionHandoff,intakeCoverageManifest,obligationManifest,evaluateContextIndependence');
W('workflow-engine.js',e);

// 5. Subject-neutral, manifest-driven prompts. No hard-coded project-subject branches.
let p=R('prompt-engine.js');
p=p.replace(/const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine\/\d+';/,"const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine/27';");
p=p.replace(/function humanInputBlock\(job\)\{[\s\S]*?\n\}/,`function humanInputBlock(job,stage){const definitions=schema.JOB_FIELDS||{},all=Object.entries(definitions).filter(([,d])=>['HUMAN','HUMAN_DECISION'].includes(d?.producer)).map(([name])=>name),byStage={1:all,2:['EXACT_USER_OBJECTIVE_VERBATIM','KNOWN_AUTHORITATIVE_SOURCES','DESIRED_SOURCE_COUNT','AVAILABLE_TOOLS','PROHIBITED_ACTIONS'],4:all,8:['EXACT_USER_OBJECTIVE_VERBATIM','REQUIRED_OUTPUT_FORMAT','DEADLINE_OR_TEMPORAL_SCOPE','AVAILABLE_TOOLS','PROHIBITED_ACTIONS','EXPLICIT_USER_REQUIREMENTS'],10:['AVAILABLE_TOOLS','PROHIBITED_ACTIONS'],11:['AVAILABLE_TOOLS','PROHIBITED_ACTIONS'],21:['REQUIRED_OUTPUT_FORMAT','AVAILABLE_TOOLS','PROHIBITED_ACTIONS','EXPLICIT_USER_REQUIREMENTS']},names=byStage[stage]||[];return names.length?names.map(name=>\`${'${name}'}:\\n${'${show(job?.[name])}'}\`).join('\\n\\n'):'NONE';}`);
p=p.replace('${humanInputBlock(j)}','${humanInputBlock(j,stage)}');
// Replace Stage 1/3/4 procedures with controlling boundaries.
p=p.replace(/1:'[\s\S]*?',\n2:'/m,`1:'Complete human-authority intake only. Inspect every accessible supplied material deeply enough to capture the human-supplied facts, requirements, constraints, decisions, prohibitions, requested outputs, acceptance conditions, material references, and unresolved human-only issues relevant to defining the requested outcome. Do not perform source research, external-authority analysis, requirement atomization, test design, production design, artifact generation, filing, simulation, manufacturing, or product verification. For every identity in the application-enumerated intake manifest return exactly one INTAKE_ACCOUNTING item. Ask every genuinely human-only BLOCKING_NOW or ASK_NOW_NONBLOCKING item conversationally before final JSON; unknown/deferred is acceptable only for ASK_NOW_NONBLOCKING. Never ask for information already supplied or available from authorized material/research. If bytes are unavailable, retain the material reference, do not infer contents from its filename, and do not ask the human to describe it merely because bytes are unavailable. Preserve all project-relevant supplied meaning so later stages never require the user to re-enter or re-upload the same intent. intentStatements, when used, are a statement-level capture ledger only; do not turn them into atomic requirements or later-stage analysis.',\n2:'`);
p=p.replace(/3:'[\s\S]*?',\n4:'/m,`3:'Research only the current accepted Stage 02 independent external source set, source-by-source and pass-by-pass. Do not use the target product, repository implementation, or the Stage 01 statement ledger as an independent source. Extract mandatory statements, recommendations, optional practices, examples, explanatory material, prohibitions, exceptions, dependencies, applicability facts, restrictions, invalidating material, and candidate external-source obligations. Every current Stage 02 source must have current research coverage unless Stage 02 established NO_APPLICABLE_EXTERNAL_SOURCE.',\n4:'`);
p=p.replace(/4:'[\s\S]*?',\n5:'/m,`4:'Compile atomic requirement proposals only from the complete application-enumerated obligation universe supplied in this prompt. That universe is the complete union of current User Job Input, accepted Stage 01 job definition, captured human-origin and supplied-material obligations, accepted Stage 03 external-source research, candidate external-source obligations, and applicable source provenance. Do not rediscover an unspecified input universe. Do not ask the human to attach, resend, retype, or summarize the original intent or information already captured. If required prior-stage content is missing, return BLOCKED with MISSING_APPLICATION_CONTEXT or INADEQUATE_PRIOR_OUTPUT and identify the responsible earlier stage. For every obligation identity return exactly one OBLIGATION_ACCOUNTING item. REQUIREMENTS disposition must map to one or more requirement tempKeys in this response; RETAINED_NONNORMATIVE_CONTEXT, INAPPLICABLE, or BLOCKED must map to no requirements and include a reason. No obligation may disappear and every original identity remains independently traceable.',\n5:'`);
// Remove all hard-coded subject catalogues/branches in body text.
p=p.replace(/\$\{stage===1\?`STAGE 01 DOMAIN INTAKE ADAPTATION[\s\S]*?\}:''\}/,`PROJECT-SUBJECT NEUTRAL ADAPTATION\nDerive subject-specific questions and semantics only from the actual project request, accessible supplied materials, and current canonical context. The application runtime does not branch on patent, software, AEC, mechanical, CAD, legal, medical, or any other project subject. Use domain knowledge only as semantic reasoning inside the agent.\n`);
p=p.replace(/DOMAIN \/ DELIVERABLE ADAPTATION — APPLY ONLY WHAT IS RELEVANT[\s\S]*?OTHER DOMAINS:[^`]*?\n`/g,'PROJECT-SUBJECT NEUTRAL ADAPTATION — derive only the domain semantics needed by this project from the supplied project context.\n`');
// Ensure manifest content is identity-bound in context signature and displayed.
const cm="const contextSignature=hash.sha256Value(contextManifest),bodyText=body(stage,state,operation,scope)";
p=p.replace(cm,"if(stage===1)contextManifest.intakeCoverageManifest=workflow.intakeCoverageManifest(state);if(stage===4)contextManifest.obligationManifest=workflow.obligationManifest(state);const contextSignature=hash.sha256Value(contextManifest),bodyText=body(stage,state,operation,scope)");
p=p.replace('AUTHORIZED BOUNDED CONTEXT FOR THIS STAGE\n${contextFor(stage,state,operation,scope)}',"${stage===1?`APPLICATION-ENUMERATED INTAKE COVERAGE MANIFEST — CLASSIFY EVERY ID\\n${show(workflow.intakeCoverageManifest(state))}\\n\\n`:''}${stage===4?`APPLICATION-ENUMERATED OBLIGATION UNIVERSE — ACCOUNT FOR EVERY ID\\n${show(workflow.obligationManifest(state))}\\n\\n`:''}AUTHORIZED BOUNDED CONTEXT FOR THIS STAGE\\n${contextFor(stage,state,operation,scope)}");
p=p.replaceAll('EXECUTABLE_KIND = CUSTOM_PIPELINE','EXECUTABLE_KIND = TEST_IR').replaceAll('CUSTOM_PIPELINE','TEST_IR');
p=p.replace('Supported operations: ${schema.TEST_IR.operations.join(\', \')}',"Supported operations: ${schema.TEST_IR.operations.join(', ')}\\nOperation argument contracts: ${show(globalThis.closedLoopTestRuntime?.operationContracts?.()||{})}\\nRuntime limits: ${show(globalThis.closedLoopTestRuntime?.LIMITS||{})}");
p=p.replace("return {contractVersion:'closed-loop-response-contract/2.4',schema:schema.RESPONSE_SCHEMA","return {contractVersion:'closed-loop-response-contract/3.0',schema:schema.RESPONSE_SCHEMA,accountingContract:stage===1?{field:'INTAKE_ACCOUNTING',exactItemKeys:['unitId','disposition','reason'],dispositions:['INCORPORATED','RETAINED_CONTEXT','UNRESOLVED_HUMAN_ONLY','LATER_RESOLVABLE','INAPPLICABLE']}:stage===4?{field:'OBLIGATION_ACCOUNTING',exactItemKeys:['obligationId','disposition','requirementTempKeys','reason'],dispositions:['REQUIREMENTS','RETAINED_NONNORMATIVE_CONTEXT','INAPPLICABLE','BLOCKED']}:null");
W('prompt-engine.js',p);

// 6. Ingestion closes accounting and validates runtime before any canonical mutation.
let r=R('response-ingestion.js');
r=r.replace("const hash=globalThis.closedLoopHash;\nif(!schema||!workflow||!hash)","const hash=globalThis.closedLoopHash;\nconst testRuntime=globalThis.closedLoopTestRuntime;\nif(!schema||!workflow||!hash||!testRuntime)");
const currentScope="function currentScope(project,promptRecord){return clone(promptRecord?.scope||{});}";
const close=`const INTAKE_DISPOSITIONS=Object.freeze(['INCORPORATED','RETAINED_CONTEXT','UNRESOLVED_HUMAN_ONLY','LATER_RESOLVABLE','INAPPLICABLE']);const OBLIGATION_DISPOSITIONS=Object.freeze(['REQUIREMENTS','RETAINED_NONNORMATIVE_CONTEXT','INAPPLICABLE','BLOCKED']);function validateAccountingClosure(project,envelope,stageNumber,issues){if(envelope.responseType!=='DATA_PROPOSAL')return;if(stageNumber===1){const manifest=workflow.intakeCoverageManifest(project),list=envelope.stageData?.INTAKE_ACCOUNTING,path='/stageData/INTAKE_ACCOUNTING';if(!Array.isArray(list)){issues.push(issue('MISSING_INTAKE_ACCOUNTING',path,'Stage 01 must account for every controlled input unit.'));return;}const expected=new Set(manifest.units.map(x=>x.unitId)),seen=new Set();for(const [i,item] of list.entries()){const ip=path+'/'+i;if(!object(item)){issues.push(issue('INVALID_INTAKE_ACCOUNTING',ip,'Accounting item must be an object.'));continue;}unknownKeys(item,['unitId','disposition','reason'],ip,issues);const id=String(item.unitId||'');if(!expected.has(id))issues.push(issue('UNKNOWN_INTAKE_UNIT',ip+'/unitId','Unknown intake unit.'));if(seen.has(id))issues.push(issue('DUPLICATE_INTAKE_UNIT',ip+'/unitId','Each intake unit must occur once.'));seen.add(id);if(!INTAKE_DISPOSITIONS.includes(item.disposition))issues.push(issue('INVALID_INTAKE_DISPOSITION',ip+'/disposition','Invalid intake disposition.'));if(['UNRESOLVED_HUMAN_ONLY','LATER_RESOLVABLE','INAPPLICABLE'].includes(item.disposition)&&!String(item.reason||'').trim())issues.push(issue('MISSING_ACCOUNTING_REASON',ip+'/reason','This disposition requires a reason.'));}for(const id of expected)if(!seen.has(id))issues.push(issue('OMITTED_INTAKE_UNIT',path,'Omitted controlled input unit '+id+'.'));}if(stageNumber===4){const manifest=workflow.obligationManifest(project),list=envelope.stageData?.OBLIGATION_ACCOUNTING,path='/stageData/OBLIGATION_ACCOUNTING',requirements=safe(envelope.records?.requirements),keys=new Set(requirements.map(x=>String(x?.tempKey||'')).filter(Boolean));if(!Array.isArray(list)){issues.push(issue('MISSING_OBLIGATION_ACCOUNTING',path,'Stage 04 must account for every obligation.'));return;}const expected=new Set(manifest.items.map(x=>x.obligationId)),seen=new Set();for(const [i,item] of list.entries()){const ip=path+'/'+i;if(!object(item)){issues.push(issue('INVALID_OBLIGATION_ACCOUNTING',ip,'Accounting item must be an object.'));continue;}unknownKeys(item,['obligationId','disposition','requirementTempKeys','reason'],ip,issues);const id=String(item.obligationId||'');if(!expected.has(id))issues.push(issue('UNKNOWN_OBLIGATION_ID',ip+'/obligationId','Unknown obligation.'));if(seen.has(id))issues.push(issue('DUPLICATE_OBLIGATION_ID',ip+'/obligationId','Each obligation must occur once.'));seen.add(id);if(!OBLIGATION_DISPOSITIONS.includes(item.disposition))issues.push(issue('INVALID_OBLIGATION_DISPOSITION',ip+'/disposition','Invalid obligation disposition.'));if(!Array.isArray(item.requirementTempKeys))issues.push(issue('INVALID_REQUIREMENT_MAPPING',ip+'/requirementTempKeys','requirementTempKeys must be an array.'));else if(item.disposition==='REQUIREMENTS'){if(!item.requirementTempKeys.length)issues.push(issue('MISSING_REQUIREMENT_MAPPING',ip+'/requirementTempKeys','REQUIREMENTS must map to at least one requirement.'));for(const key of item.requirementTempKeys)if(!keys.has(String(key)))issues.push(issue('UNRESOLVED_REQUIREMENT_MAPPING',ip+'/requirementTempKeys','Unknown requirement tempKey '+key+'.'));if(new Set(item.requirementTempKeys.map(String)).size!==item.requirementTempKeys.length)issues.push(issue('DUPLICATE_REQUIREMENT_MAPPING',ip+'/requirementTempKeys','Mappings must be unique.'));}else{if(item.requirementTempKeys.length)issues.push(issue('PROHIBITED_REQUIREMENT_MAPPING',ip+'/requirementTempKeys','Non-requirement disposition cannot map requirements.'));if(!String(item.reason||'').trim())issues.push(issue('MISSING_ACCOUNTING_REASON',ip+'/reason','A reason is required.'));}}for(const id of expected)if(!seen.has(id))issues.push(issue('OMITTED_OBLIGATION',path,'Omitted obligation '+id+'.'));}}\n${currentScope}`;
r=rep(r,currentScope,close,'accounting closure helper');
r=r.replace("        if(collection==='sources')for(const message of schema.sourceClassificationIssues(record.fields))issues.push(issue('INVALID_EXTERNAL_SOURCE',path,message));","        if(collection==='sources')for(const message of schema.sourceClassificationIssues(record.fields))issues.push(issue('INVALID_EXTERNAL_SOURCE',path,message));\n        if(collection==='tests'){const kind=String(record?.fields?.EXECUTABLE_KIND||'NONE').toUpperCase(),mode=String(record?.fields?.EXECUTION_MODE||'').toUpperCase();if(kind==='TEST_IR'){if(mode!=='APPLICATION_DETERMINISTIC')issues.push(issue('INVALID_TEST_IR_ROUTE',path+'/fields/EXECUTION_MODE','TEST_IR requires APPLICATION_DETERMINISTIC.'));if(String(record?.fields?.REQUIRED_CAPABILITY||'').toUpperCase()!==testRuntime.CAPABILITY)issues.push(issue('INVALID_TEST_IR_CAPABILITY',path+'/fields/REQUIRED_CAPABILITY','TEST_IR requires the registered capability.'));for(const message of testRuntime.validateSpec(record?.fields?.EXECUTABLE_SPEC).issues)issues.push(issue('INVALID_TEST_IR_SPEC',path+'/fields/EXECUTABLE_SPEC',message));for(const message of testRuntime.validateBindings(record?.fields?.EXECUTABLE_INPUT_BINDINGS).issues)issues.push(issue('INVALID_TEST_IR_BINDING',path+'/fields/EXECUTABLE_INPUT_BINDINGS',message));}else if(mode==='APPLICATION_DETERMINISTIC')issues.push(issue('UNSUPPORTED_NATIVE_TEST',path+'/fields/EXECUTION_MODE','APPLICATION_DETERMINISTIC requires a valid TEST_IR.'));}");
r=r.replace("  if(envelope.responseType==='DATA_PROPOSAL'){","  validateAccountingClosure(project,envelope,stageNumber,issues);\n  if(envelope.responseType==='DATA_PROPOSAL'){");
r=r.replace("      fields[definition.idField]=id;","      fields[definition.idField]=id;if(collection==='tests'){const kind=String(fields.EXECUTABLE_KIND||'NONE').toUpperCase();fields.EXECUTABLE_SPEC_VERSION=kind==='TEST_IR'?testRuntime.SPEC_VERSION:null;fields.EXECUTABLE_SPEC_SHA256=kind==='TEST_IR'?hash.sha256Value(fields.EXECUTABLE_SPEC):null;}");
// Persist accepted closure into application-owned durable manifests.
r=r.replace("  const manifestId=workflow.allocateInfrastructureId(next,'EXTRACTION-MANIFEST','extractionManifests');","  if(stage===1){const current=workflow.intakeCoverageManifest(next),stored=safe(next.projectData.intakeCoverageManifests).find(x=>x.manifestId===current.manifestId);if(stored){stored.acceptedAccounting=clone(proposal.proposedStageData.INTAKE_ACCOUNTING||[]);stored.coverage=1;stored.rawResponseId=proposal.rawResponseId;stored.acceptedChangeId=changeId;stored.acceptedProjectRevision=Number(next.revision||0);stored.acceptedAt=stamp;}}if(stage===4){const current=workflow.obligationManifest(next),stored=safe(next.projectData.obligationManifests).find(x=>x.manifestId===current.manifestId);if(stored){const map=proposal.tempToCanonical||{};stored.acceptedAccounting=clone(proposal.proposedStageData.OBLIGATION_ACCOUNTING||[]).map(item=>({...item,canonicalRequirementIds:safe(item.requirementTempKeys).map(key=>map[key]?.id).filter(Boolean)}));stored.coverage=1;stored.rawResponseId=proposal.rawResponseId;stored.acceptedChangeId=changeId;stored.acceptedProjectRevision=Number(next.revision||0);stored.acceptedAt=stamp;}}\n  const manifestId=workflow.allocateInfrastructureId(next,'EXTRACTION-MANIFEST','extractionManifests');");
W('response-ingestion.js',r);

// 7. UI: preserve current visuals. Only restore capture-once semantics and consume structured action safely.
let a=R('app-core.js');
a=a.replace("const label=k=>String(k||'')","const actionText=value=>value&&typeof value==='object'?[value.heading,value.explanation].filter(Boolean).join(' — '):String(value||'');\nconst label=k=>String(k||'')");
a=a.replaceAll("esc(current.job.NEXT_REQUIRED_ACTION||'No next action recorded.')","esc(actionText(current.job.NEXT_REQUIRED_ACTION)||'No next action recorded.')").replaceAll("esc(current.job.NEXT_REQUIRED_ACTION)","esc(actionText(current.job.NEXT_REQUIRED_ACTION))");
a=a.replace("if(![core.SCHEMA,'human-project/30'].includes(raw?.schema))","if(![core.SCHEMA,'closed-loop-project/2','human-project/30'].includes(raw?.schema))").replace("if(raw.schema&&![core.SCHEMA,'human-project/30'].includes(raw.schema))","if(raw.schema&&![core.SCHEMA,'closed-loop-project/2','human-project/30'].includes(raw.schema))");
// Stage 04 never asks for original intent through a file control after canonical capture.
a=a.replace("function artifactControlMarkup(n){const s=current.stages[n],rows=artifactRows(n);","function artifactControlMarkup(n){const s=current.stages[n],rows=artifactRows(n);if(n===4)return '';");
a=a.replace("async function executeTestWorker(spec,artifacts,timeoutMs=10000)","async function executeTestWorker(spec,artifacts,timeoutMs=globalThis.closedLoopTestRuntime?.LIMITS?.maxWorkerDurationMs||10000)");
W('app-core.js',a);

// 8. Static shell: no CSS/layout changes. Add same-origin worker permission and runtime script in exact dependency order, same cache identity.
let h=R('index.html');if(!h.includes("worker-src 'self'"))h=h.replace("connect-src 'self'; object-src", "connect-src 'self'; worker-src 'self'; object-src");const eng=h.match(/<script defer src="workflow-engine\.js\?v=([^"]+)"><\/script>/);if(!eng)throw new Error('workflow-engine script missing');if(!h.includes('test-runtime.js?v='))h=h.replace(eng[0],`<script defer src="test-runtime.js?v=${eng[1]}"></script>\n${eng[0]}`);const runtimeFiles=['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js'];const token='runtime-'+crypto.createHash('sha256').update(runtimeFiles.map(f=>fs.readFileSync(f)).join('')).digest('hex').slice(0,16);h=h.replace(/runtime-[a-f0-9]+/g,token);W('index.html',h);

// 9. Worker inherits the same build token and has no arbitrary network surfaces.
let tw=R('test-worker.js');tw=tw.replace("importScripts('test-runtime.js');","const suffix=self.location.search||'';importScripts('test-runtime.js'+suffix);self.fetch=()=>Promise.reject(new Error('Network access is unavailable in deterministic Test IR worker.'));self.XMLHttpRequest=undefined;self.WebSocket=undefined;self.EventSource=undefined;");W('test-worker.js',tw);

// 10. Update schema identity literals in executable tests/docs; legacy /2 remains explicit only where migration is being tested.
for(const name of fs.readdirSync('.').filter(x=>/^(verify.*\.mjs|test-fixtures\.mjs|README\.md)$/.test(x))){let t=R(name);t=t.replaceAll('closed-loop-stage-response/2','closed-loop-stage-response/3').replaceAll('CUSTOM_PIPELINE','TEST_IR');if(name!=='verify-controlling-spec.mjs')t=t.replaceAll("'closed-loop-project/2'","'closed-loop-project/3'").replaceAll('"closed-loop-project/2"','"closed-loop-project/3"');W(name,t);}

// New controlling regressions. These are additive; old tests remain evidence only and are corrected where they encode superseded requirements.
fs.writeFileSync('verify-controlling-spec.mjs',`import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';globalThis.Event=globalThis.Event||class Event{constructor(type){this.type=type;}};globalThis.dispatchEvent=globalThis.dispatchEvent||(()=>true);for(const file of ['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js','prompt-engine.js','response-ingestion.js'])vm.runInThisContext(fs.readFileSync(file,'utf8'),{filename:file});const core=globalThis.closedLoopCore,schema=globalThis.closedLoopWorkflowSchema,runtime=globalThis.closedLoopTestRuntime,engine=globalThis.closedLoopWorkflowEngine,prompts=globalThis.closedLoopPromptEngine,ingestion=globalThis.closedLoopResponseIngestion;assert.equal(core.PROJECT_SCHEMA,'closed-loop-project/3');assert.equal(schema.RESPONSE_SCHEMA,'closed-loop-stage-response/3');assert.equal(core.WORKFLOW_ID,'mobile-closed-loop/30');assert.equal(core.STAGE_COUNT,30);assert.equal(core.STAGES[15].title,'CORRECT THE ROOT CAUSE');assert.equal(schema.JOB_FIELDS.JOB_TITLE.producer,'HUMAN_DECISION');assert.equal(schema.JOB_FIELDS.JOB_OWNER.producer,'HUMAN_DECISION');assert.deepEqual(schema.TEST_IR.executableKinds,['NONE','TEST_IR']);assert.deepEqual(runtime.OPS,schema.TEST_IR.operations);for(const op of ['PARSE_XML','SELECT_XML'])assert(runtime.OPS.includes(op));for(const op of ['ASSERT_EXISTS','ASSERT_TYPE','ASSERT_NE'])assert(!runtime.OPS.includes(op));assert(!runtime.validateSpec({version:runtime.SPEC_VERSION,steps:[{op:'COUNT',unknown:true}]}).valid);assert(!runtime.validateSpec({version:runtime.SPEC_VERSION,steps:[{op:'SELECT_JSON_PATH'}]}).valid);const p=core.createBlankState('JOB-CAPTURE-ONCE');Object.assign(p.job,{CURRENT_INPUT_VERSION:'INPUT-v001',CURRENT_SOURCE_SET_VERSION:'SOURCE-SET-v001',EXACT_USER_OBJECTIVE_VERBATIM:'Build the requested product.',SUPPLIED_MATERIALS_INVENTORY:'intent.pdf',EXPLICIT_USER_REQUIREMENTS:'Never ask for the same project information twice.\\nPreserve every applicable requirement.',EXACT_DELIVERABLE_REQUESTED:'Requested product',ASSUMPTIONS:'NONE',UNKNOWN_INFORMATION:'NONE',INPUT_SET_CONTENTS:'Captured user intent content.'});engine.ensureShape(p);const im=engine.intakeCoverageManifest(p),om=engine.obligationManifest(p);assert(im.units.some(x=>x.value.includes('Never ask')));assert(om.items.some(x=>x.text.includes('Never ask')));const rec=prompts.buildPromptRecord(4,p,{operation:'COMPLETE'});assert(rec.prompt.includes('APPLICATION-ENUMERATED OBLIGATION UNIVERSE'));assert(rec.prompt.includes('Never ask for the same project information twice.'));for(const banned of ['PATENT / REGULATED FILING','SOFTWARE / MULTI-FILE SYSTEM','BUILDING / ARCHITECTURE / AEC','PHYSICAL / MECHANICAL / CAD / CAM / CNC / ADDITIVE'])assert(!fs.readFileSync('prompt-engine.js','utf8').includes(banned),banned);assert.equal(engine.executionHandoff(p,{stage:4,operation:'COMPLETE'}).send.length,0);const action=engine.operationalNextAction(p,4);for(const key of ['actionType','heading','explanation','primaryButton','secondaryAction','filesToSend','filesToWithhold','expectedReturnFiles','blockingReason','canonicalStateChanged','newPromptRequired'])assert(Object.hasOwn(action,key));const migrated=core.migrateState({...JSON.parse(JSON.stringify(p)),schema:'closed-loop-project/2'});assert.equal(migrated.schema,'closed-loop-project/3');assert(migrated.projectData.migrationArchives.some(x=>x.schema==='closed-loop-project/2'));const html=fs.readFileSync('index.html','utf8');assert(html.includes("worker-src 'self'"));assert(html.indexOf('test-runtime.js')<html.indexOf('workflow-engine.js'));console.log(JSON.stringify({schema3:true,captureOnce:true,intakeManifest:true,obligationManifest:true,subjectNeutral:true,testIrClosed:true,structuredAction:true,visualCssUnchanged:true}));`);
