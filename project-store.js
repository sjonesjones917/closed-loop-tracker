(()=>{
'use strict';

const DB_NAME='closed-loop-reliability';
const DB_VERSION=2;
const PROJECTS='projects';
const ARTIFACTS='artifacts';
const META='meta';
const STAGING='artifactStaging';
// The import-only node ceiling remains finite and is sized beneath the 256 MiB expanded-byte ceiling (about 52 bytes/node at the joint limits). External response limits are separate and unchanged.
const STORE_LIMITS=Object.freeze({maxImportCompressedBytes:64*1024*1024,maxImportExpandedBytes:256*1024*1024,maxPackageArtifacts:500,maxArtifactBytes:128*1024*1024,maxFilenameLength:240,maxPathDepth:16,maxObjectDepth:80,maxImportObjectNodes:5000000,maxArrayLength:100000,maxObjectMembers:100000,maxStringLength:16*1024*1024,maxNumberTokenLength:256,maxHistoryRecords:250000,maxExtensionBytes:8*1024*1024,storageSafetyMarginBytes:16*1024*1024});
const UNICODE_DEFAULT_CASE_FOLD_VERSION='Unicode-15.0.0';
const UNICODE_DEFAULT_CASE_FOLD_MAP=new Map('41=a;42=b;43=c;44=d;45=e;46=f;47=g;48=h;49=i;4a=j;4b=k;4c=l;4d=m;4e=n;4f=o;50=p;51=q;52=r;53=s;54=t;55=u;56=v;57=w;58=x;59=y;5a=z;b5=\u{3bc};c0=\u{e0};c1=\u{e1};c2=\u{e2};c3=\u{e3};c4=\u{e4};c5=\u{e5};c6=\u{e6};c7=\u{e7};c8=\u{e8};c9=\u{e9};ca=\u{ea};cb=\u{eb};cc=\u{ec};cd=\u{ed};ce=\u{ee};cf=\u{ef};d0=\u{f0};d1=\u{f1};d2=\u{f2};d3=\u{f3};d4=\u{f4};d5=\u{f5};d6=\u{f6};d8=\u{f8};d9=\u{f9};da=\u{fa};db=\u{fb};dc=\u{fc};dd=\u{fd};de=\u{fe};df=ss;100=\u{101};102=\u{103};104=\u{105};106=\u{107};108=\u{109};10a=\u{10b};10c=\u{10d};10e=\u{10f};110=\u{111};112=\u{113};114=\u{115};116=\u{117};118=\u{119};11a=\u{11b};11c=\u{11d};11e=\u{11f};120=\u{121};122=\u{123};124=\u{125};126=\u{127};128=\u{129};12a=\u{12b};12c=\u{12d};12e=\u{12f};130=i\u{307};132=\u{133};134=\u{135};136=\u{137};139=\u{13a};13b=\u{13c};13d=\u{13e};13f=\u{140};141=\u{142};143=\u{144};145=\u{146};147=\u{148};149=\u{2bc}n;14a=\u{14b};14c=\u{14d};14e=\u{14f};150=\u{151};152=\u{153};154=\u{155};156=\u{157};158=\u{159};15a=\u{15b};15c=\u{15d};15e=\u{15f};160=\u{161};162=\u{163};164=\u{165};166=\u{167};168=\u{169};16a=\u{16b};16c=\u{16d};16e=\u{16f};170=\u{171};172=\u{173};174=\u{175};176=\u{177};178=\u{ff};179=\u{17a};17b=\u{17c};17d=\u{17e};17f=s;181=\u{253};182=\u{183};184=\u{185};186=\u{254};187=\u{188};189=\u{256};18a=\u{257};18b=\u{18c};18e=\u{1dd};18f=\u{259};190=\u{25b};191=\u{192};193=\u{260};194=\u{263};196=\u{269};197=\u{268};198=\u{199};19c=\u{26f};19d=\u{272};19f=\u{275};1a0=\u{1a1};1a2=\u{1a3};1a4=\u{1a5};1a6=\u{280};1a7=\u{1a8};1a9=\u{283};1ac=\u{1ad};1ae=\u{288};1af=\u{1b0};1b1=\u{28a};1b2=\u{28b};1b3=\u{1b4};1b5=\u{1b6};1b7=\u{292};1b8=\u{1b9};1bc=\u{1bd};1c4=\u{1c6};1c5=\u{1c6};1c7=\u{1c9};1c8=\u{1c9};1ca=\u{1cc};1cb=\u{1cc};1cd=\u{1ce};1cf=\u{1d0};1d1=\u{1d2};1d3=\u{1d4};1d5=\u{1d6};1d7=\u{1d8};1d9=\u{1da};1db=\u{1dc};1de=\u{1df};1e0=\u{1e1};1e2=\u{1e3};1e4=\u{1e5};1e6=\u{1e7};1e8=\u{1e9};1ea=\u{1eb};1ec=\u{1ed};1ee=\u{1ef};1f0=j\u{30c};1f1=\u{1f3};1f2=\u{1f3};1f4=\u{1f5};1f6=\u{195};1f7=\u{1bf};1f8=\u{1f9};1fa=\u{1fb};1fc=\u{1fd};1fe=\u{1ff};200=\u{201};202=\u{203};204=\u{205};206=\u{207};208=\u{209};20a=\u{20b};20c=\u{20d};20e=\u{20f};210=\u{211};212=\u{213};214=\u{215};216=\u{217};218=\u{219};21a=\u{21b};21c=\u{21d};21e=\u{21f};220=\u{19e};222=\u{223};224=\u{225};226=\u{227};228=\u{229};22a=\u{22b};22c=\u{22d};22e=\u{22f};230=\u{231};232=\u{233};23a=\u{2c65};23b=\u{23c};23d=\u{19a};23e=\u{2c66};241=\u{242};243=\u{180};244=\u{289};245=\u{28c};246=\u{247};248=\u{249};24a=\u{24b};24c=\u{24d};24e=\u{24f};345=\u{3b9};370=\u{371};372=\u{373};376=\u{377};37f=\u{3f3};386=\u{3ac};388=\u{3ad};389=\u{3ae};38a=\u{3af};38c=\u{3cc};38e=\u{3cd};38f=\u{3ce};390=\u{3b9}\u{308}\u{301};391=\u{3b1};392=\u{3b2};393=\u{3b3};394=\u{3b4};395=\u{3b5};396=\u{3b6};397=\u{3b7};398=\u{3b8};399=\u{3b9};39a=\u{3ba};39b=\u{3bb};39c=\u{3bc};39d=\u{3bd};39e=\u{3be};39f=\u{3bf};3a0=\u{3c0};3a1=\u{3c1};3a3=\u{3c3};3a4=\u{3c4};3a5=\u{3c5};3a6=\u{3c6};3a7=\u{3c7};3a8=\u{3c8};3a9=\u{3c9};3aa=\u{3ca};3ab=\u{3cb};3b0=\u{3c5}\u{308}\u{301};3c2=\u{3c3};3cf=\u{3d7};3d0=\u{3b2};3d1=\u{3b8};3d5=\u{3c6};3d6=\u{3c0};3d8=\u{3d9};3da=\u{3db};3dc=\u{3dd};3de=\u{3df};3e0=\u{3e1};3e2=\u{3e3};3e4=\u{3e5};3e6=\u{3e7};3e8=\u{3e9};3ea=\u{3eb};3ec=\u{3ed};3ee=\u{3ef};3f0=\u{3ba};3f1=\u{3c1};3f4=\u{3b8};3f5=\u{3b5};3f7=\u{3f8};3f9=\u{3f2};3fa=\u{3fb};3fd=\u{37b};3fe=\u{37c};3ff=\u{37d};400=\u{450};401=\u{451};402=\u{452};403=\u{453};404=\u{454};405=\u{455};406=\u{456};407=\u{457};408=\u{458};409=\u{459};40a=\u{45a};40b=\u{45b};40c=\u{45c};40d=\u{45d};40e=\u{45e};40f=\u{45f};410=\u{430};411=\u{431};412=\u{432};413=\u{433};414=\u{434};415=\u{435};416=\u{436};417=\u{437};418=\u{438};419=\u{439};41a=\u{43a};41b=\u{43b};41c=\u{43c};41d=\u{43d};41e=\u{43e};41f=\u{43f};420=\u{440};421=\u{441};422=\u{442};423=\u{443};424=\u{444};425=\u{445};426=\u{446};427=\u{447};428=\u{448};429=\u{449};42a=\u{44a};42b=\u{44b};42c=\u{44c};42d=\u{44d};42e=\u{44e};42f=\u{44f};460=\u{461};462=\u{463};464=\u{465};466=\u{467};468=\u{469};46a=\u{46b};46c=\u{46d};46e=\u{46f};470=\u{471};472=\u{473};474=\u{475};476=\u{477};478=\u{479};47a=\u{47b};47c=\u{47d};47e=\u{47f};480=\u{481};48a=\u{48b};48c=\u{48d};48e=\u{48f};490=\u{491};492=\u{493};494=\u{495};496=\u{497};498=\u{499};49a=\u{49b};49c=\u{49d};49e=\u{49f};4a0=\u{4a1};4a2=\u{4a3};4a4=\u{4a5};4a6=\u{4a7};4a8=\u{4a9};4aa=\u{4ab};4ac=\u{4ad};4ae=\u{4af};4b0=\u{4b1};4b2=\u{4b3};4b4=\u{4b5};4b6=\u{4b7};4b8=\u{4b9};4ba=\u{4bb};4bc=\u{4bd};4be=\u{4bf};4c0=\u{4cf};4c1=\u{4c2};4c3=\u{4c4};4c5=\u{4c6};4c7=\u{4c8};4c9=\u{4ca};4cb=\u{4cc};4cd=\u{4ce};4d0=\u{4d1};4d2=\u{4d3};4d4=\u{4d5};4d6=\u{4d7};4d8=\u{4d9};4da=\u{4db};4dc=\u{4dd};4de=\u{4df};4e0=\u{4e1};4e2=\u{4e3};4e4=\u{4e5};4e6=\u{4e7};4e8=\u{4e9};4ea=\u{4eb};4ec=\u{4ed};4ee=\u{4ef};4f0=\u{4f1};4f2=\u{4f3};4f4=\u{4f5};4f6=\u{4f7};4f8=\u{4f9};4fa=\u{4fb};4fc=\u{4fd};4fe=\u{4ff};500=\u{501};502=\u{503};504=\u{505};506=\u{507};508=\u{509};50a=\u{50b};50c=\u{50d};50e=\u{50f};510=\u{511};512=\u{513};514=\u{515};516=\u{517};518=\u{519};51a=\u{51b};51c=\u{51d};51e=\u{51f};520=\u{521};522=\u{523};524=\u{525};526=\u{527};528=\u{529};52a=\u{52b};52c=\u{52d};52e=\u{52f};531=\u{561};532=\u{562};533=\u{563};534=\u{564};535=\u{565};536=\u{566};537=\u{567};538=\u{568};539=\u{569};53a=\u{56a};53b=\u{56b};53c=\u{56c};53d=\u{56d};53e=\u{56e};53f=\u{56f};540=\u{570};541=\u{571};542=\u{572};543=\u{573};544=\u{574};545=\u{575};546=\u{576};547=\u{577};548=\u{578};549=\u{579};54a=\u{57a};54b=\u{57b};54c=\u{57c};54d=\u{57d};54e=\u{57e};54f=\u{57f};550=\u{580};551=\u{581};552=\u{582};553=\u{583};554=\u{584};555=\u{585};556=\u{586};587=\u{565}\u{582};10a0=\u{2d00};10a1=\u{2d01};10a2=\u{2d02};10a3=\u{2d03};10a4=\u{2d04};10a5=\u{2d05};10a6=\u{2d06};10a7=\u{2d07};10a8=\u{2d08};10a9=\u{2d09};10aa=\u{2d0a};10ab=\u{2d0b};10ac=\u{2d0c};10ad=\u{2d0d};10ae=\u{2d0e};10af=\u{2d0f};10b0=\u{2d10};10b1=\u{2d11};10b2=\u{2d12};10b3=\u{2d13};10b4=\u{2d14};10b5=\u{2d15};10b6=\u{2d16};10b7=\u{2d17};10b8=\u{2d18};10b9=\u{2d19};10ba=\u{2d1a};10bb=\u{2d1b};10bc=\u{2d1c};10bd=\u{2d1d};10be=\u{2d1e};10bf=\u{2d1f};10c0=\u{2d20};10c1=\u{2d21};10c2=\u{2d22};10c3=\u{2d23};10c4=\u{2d24};10c5=\u{2d25};10c7=\u{2d27};10cd=\u{2d2d};13f8=\u{13f0};13f9=\u{13f1};13fa=\u{13f2};13fb=\u{13f3};13fc=\u{13f4};13fd=\u{13f5};1c80=\u{432};1c81=\u{434};1c82=\u{43e};1c83=\u{441};1c84=\u{442};1c85=\u{442};1c86=\u{44a};1c87=\u{463};1c88=\u{a64b};1c90=\u{10d0};1c91=\u{10d1};1c92=\u{10d2};1c93=\u{10d3};1c94=\u{10d4};1c95=\u{10d5};1c96=\u{10d6};1c97=\u{10d7};1c98=\u{10d8};1c99=\u{10d9};1c9a=\u{10da};1c9b=\u{10db};1c9c=\u{10dc};1c9d=\u{10dd};1c9e=\u{10de};1c9f=\u{10df};1ca0=\u{10e0};1ca1=\u{10e1};1ca2=\u{10e2};1ca3=\u{10e3};1ca4=\u{10e4};1ca5=\u{10e5};1ca6=\u{10e6};1ca7=\u{10e7};1ca8=\u{10e8};1ca9=\u{10e9};1caa=\u{10ea};1cab=\u{10eb};1cac=\u{10ec};1cad=\u{10ed};1cae=\u{10ee};1caf=\u{10ef};1cb0=\u{10f0};1cb1=\u{10f1};1cb2=\u{10f2};1cb3=\u{10f3};1cb4=\u{10f4};1cb5=\u{10f5};1cb6=\u{10f6};1cb7=\u{10f7};1cb8=\u{10f8};1cb9=\u{10f9};1cba=\u{10fa};1cbd=\u{10fd};1cbe=\u{10fe};1cbf=\u{10ff};1e00=\u{1e01};1e02=\u{1e03};1e04=\u{1e05};1e06=\u{1e07};1e08=\u{1e09};1e0a=\u{1e0b};1e0c=\u{1e0d};1e0e=\u{1e0f};1e10=\u{1e11};1e12=\u{1e13};1e14=\u{1e15};1e16=\u{1e17};1e18=\u{1e19};1e1a=\u{1e1b};1e1c=\u{1e1d};1e1e=\u{1e1f};1e20=\u{1e21};1e22=\u{1e23};1e24=\u{1e25};1e26=\u{1e27};1e28=\u{1e29};1e2a=\u{1e2b};1e2c=\u{1e2d};1e2e=\u{1e2f};1e30=\u{1e31};1e32=\u{1e33};1e34=\u{1e35};1e36=\u{1e37};1e38=\u{1e39};1e3a=\u{1e3b};1e3c=\u{1e3d};1e3e=\u{1e3f};1e40=\u{1e41};1e42=\u{1e43};1e44=\u{1e45};1e46=\u{1e47};1e48=\u{1e49};1e4a=\u{1e4b};1e4c=\u{1e4d};1e4e=\u{1e4f};1e50=\u{1e51};1e52=\u{1e53};1e54=\u{1e55};1e56=\u{1e57};1e58=\u{1e59};1e5a=\u{1e5b};1e5c=\u{1e5d};1e5e=\u{1e5f};1e60=\u{1e61};1e62=\u{1e63};1e64=\u{1e65};1e66=\u{1e67};1e68=\u{1e69};1e6a=\u{1e6b};1e6c=\u{1e6d};1e6e=\u{1e6f};1e70=\u{1e71};1e72=\u{1e73};1e74=\u{1e75};1e76=\u{1e77};1e78=\u{1e79};1e7a=\u{1e7b};1e7c=\u{1e7d};1e7e=\u{1e7f};1e80=\u{1e81};1e82=\u{1e83};1e84=\u{1e85};1e86=\u{1e87};1e88=\u{1e89};1e8a=\u{1e8b};1e8c=\u{1e8d};1e8e=\u{1e8f};1e90=\u{1e91};1e92=\u{1e93};1e94=\u{1e95};1e96=h\u{331};1e97=t\u{308};1e98=w\u{30a};1e99=y\u{30a};1e9a=a\u{2be};1e9b=\u{1e61};1e9e=ss;1ea0=\u{1ea1};1ea2=\u{1ea3};1ea4=\u{1ea5};1ea6=\u{1ea7};1ea8=\u{1ea9};1eaa=\u{1eab};1eac=\u{1ead};1eae=\u{1eaf};1eb0=\u{1eb1};1eb2=\u{1eb3};1eb4=\u{1eb5};1eb6=\u{1eb7};1eb8=\u{1eb9};1eba=\u{1ebb};1ebc=\u{1ebd};1ebe=\u{1ebf};1ec0=\u{1ec1};1ec2=\u{1ec3};1ec4=\u{1ec5};1ec6=\u{1ec7};1ec8=\u{1ec9};1eca=\u{1ecb};1ecc=\u{1ecd};1ece=\u{1ecf};1ed0=\u{1ed1};1ed2=\u{1ed3};1ed4=\u{1ed5};1ed6=\u{1ed7};1ed8=\u{1ed9};1eda=\u{1edb};1edc=\u{1edd};1ede=\u{1edf};1ee0=\u{1ee1};1ee2=\u{1ee3};1ee4=\u{1ee5};1ee6=\u{1ee7};1ee8=\u{1ee9};1eea=\u{1eeb};1eec=\u{1eed};1eee=\u{1eef};1ef0=\u{1ef1};1ef2=\u{1ef3};1ef4=\u{1ef5};1ef6=\u{1ef7};1ef8=\u{1ef9};1efa=\u{1efb};1efc=\u{1efd};1efe=\u{1eff};1f08=\u{1f00};1f09=\u{1f01};1f0a=\u{1f02};1f0b=\u{1f03};1f0c=\u{1f04};1f0d=\u{1f05};1f0e=\u{1f06};1f0f=\u{1f07};1f18=\u{1f10};1f19=\u{1f11};1f1a=\u{1f12};1f1b=\u{1f13};1f1c=\u{1f14};1f1d=\u{1f15};1f28=\u{1f20};1f29=\u{1f21};1f2a=\u{1f22};1f2b=\u{1f23};1f2c=\u{1f24};1f2d=\u{1f25};1f2e=\u{1f26};1f2f=\u{1f27};1f38=\u{1f30};1f39=\u{1f31};1f3a=\u{1f32};1f3b=\u{1f33};1f3c=\u{1f34};1f3d=\u{1f35};1f3e=\u{1f36};1f3f=\u{1f37};1f48=\u{1f40};1f49=\u{1f41};1f4a=\u{1f42};1f4b=\u{1f43};1f4c=\u{1f44};1f4d=\u{1f45};1f50=\u{3c5}\u{313};1f52=\u{3c5}\u{313}\u{300};1f54=\u{3c5}\u{313}\u{301};1f56=\u{3c5}\u{313}\u{342};1f59=\u{1f51};1f5b=\u{1f53};1f5d=\u{1f55};1f5f=\u{1f57};1f68=\u{1f60};1f69=\u{1f61};1f6a=\u{1f62};1f6b=\u{1f63};1f6c=\u{1f64};1f6d=\u{1f65};1f6e=\u{1f66};1f6f=\u{1f67};1f80=\u{1f00}\u{3b9};1f81=\u{1f01}\u{3b9};1f82=\u{1f02}\u{3b9};1f83=\u{1f03}\u{3b9};1f84=\u{1f04}\u{3b9};1f85=\u{1f05}\u{3b9};1f86=\u{1f06}\u{3b9};1f87=\u{1f07}\u{3b9};1f88=\u{1f00}\u{3b9};1f89=\u{1f01}\u{3b9};1f8a=\u{1f02}\u{3b9};1f8b=\u{1f03}\u{3b9};1f8c=\u{1f04}\u{3b9};1f8d=\u{1f05}\u{3b9};1f8e=\u{1f06}\u{3b9};1f8f=\u{1f07}\u{3b9};1f90=\u{1f20}\u{3b9};1f91=\u{1f21}\u{3b9};1f92=\u{1f22}\u{3b9};1f93=\u{1f23}\u{3b9};1f94=\u{1f24}\u{3b9};1f95=\u{1f25}\u{3b9};1f96=\u{1f26}\u{3b9};1f97=\u{1f27}\u{3b9};1f98=\u{1f20}\u{3b9};1f99=\u{1f21}\u{3b9};1f9a=\u{1f22}\u{3b9};1f9b=\u{1f23}\u{3b9};1f9c=\u{1f24}\u{3b9};1f9d=\u{1f25}\u{3b9};1f9e=\u{1f26}\u{3b9};1f9f=\u{1f27}\u{3b9};1fa0=\u{1f60}\u{3b9};1fa1=\u{1f61}\u{3b9};1fa2=\u{1f62}\u{3b9};1fa3=\u{1f63}\u{3b9};1fa4=\u{1f64}\u{3b9};1fa5=\u{1f65}\u{3b9};1fa6=\u{1f66}\u{3b9};1fa7=\u{1f67}\u{3b9};1fa8=\u{1f60}\u{3b9};1fa9=\u{1f61}\u{3b9};1faa=\u{1f62}\u{3b9};1fab=\u{1f63}\u{3b9};1fac=\u{1f64}\u{3b9};1fad=\u{1f65}\u{3b9};1fae=\u{1f66}\u{3b9};1faf=\u{1f67}\u{3b9};1fb2=\u{1f70}\u{3b9};1fb3=\u{3b1}\u{3b9};1fb4=\u{3ac}\u{3b9};1fb6=\u{3b1}\u{342};1fb7=\u{3b1}\u{342}\u{3b9};1fb8=\u{1fb0};1fb9=\u{1fb1};1fba=\u{1f70};1fbb=\u{1f71};1fbc=\u{3b1}\u{3b9};1fbe=\u{3b9};1fc2=\u{1f74}\u{3b9};1fc3=\u{3b7}\u{3b9};1fc4=\u{3ae}\u{3b9};1fc6=\u{3b7}\u{342};1fc7=\u{3b7}\u{342}\u{3b9};1fc8=\u{1f72};1fc9=\u{1f73};1fca=\u{1f74};1fcb=\u{1f75};1fcc=\u{3b7}\u{3b9};1fd2=\u{3b9}\u{308}\u{300};1fd3=\u{3b9}\u{308}\u{301};1fd6=\u{3b9}\u{342};1fd7=\u{3b9}\u{308}\u{342};1fd8=\u{1fd0};1fd9=\u{1fd1};1fda=\u{1f76};1fdb=\u{1f77};1fe2=\u{3c5}\u{308}\u{300};1fe3=\u{3c5}\u{308}\u{301};1fe4=\u{3c1}\u{313};1fe6=\u{3c5}\u{342};1fe7=\u{3c5}\u{308}\u{342};1fe8=\u{1fe0};1fe9=\u{1fe1};1fea=\u{1f7a};1feb=\u{1f7b};1fec=\u{1fe5};1ff2=\u{1f7c}\u{3b9};1ff3=\u{3c9}\u{3b9};1ff4=\u{3ce}\u{3b9};1ff6=\u{3c9}\u{342};1ff7=\u{3c9}\u{342}\u{3b9};1ff8=\u{1f78};1ff9=\u{1f79};1ffa=\u{1f7c};1ffb=\u{1f7d};1ffc=\u{3c9}\u{3b9};2126=\u{3c9};212a=k;212b=\u{e5};2132=\u{214e};2160=\u{2170};2161=\u{2171};2162=\u{2172};2163=\u{2173};2164=\u{2174};2165=\u{2175};2166=\u{2176};2167=\u{2177};2168=\u{2178};2169=\u{2179};216a=\u{217a};216b=\u{217b};216c=\u{217c};216d=\u{217d};216e=\u{217e};216f=\u{217f};2183=\u{2184};24b6=\u{24d0};24b7=\u{24d1};24b8=\u{24d2};24b9=\u{24d3};24ba=\u{24d4};24bb=\u{24d5};24bc=\u{24d6};24bd=\u{24d7};24be=\u{24d8};24bf=\u{24d9};24c0=\u{24da};24c1=\u{24db};24c2=\u{24dc};24c3=\u{24dd};24c4=\u{24de};24c5=\u{24df};24c6=\u{24e0};24c7=\u{24e1};24c8=\u{24e2};24c9=\u{24e3};24ca=\u{24e4};24cb=\u{24e5};24cc=\u{24e6};24cd=\u{24e7};24ce=\u{24e8};24cf=\u{24e9};2c00=\u{2c30};2c01=\u{2c31};2c02=\u{2c32};2c03=\u{2c33};2c04=\u{2c34};2c05=\u{2c35};2c06=\u{2c36};2c07=\u{2c37};2c08=\u{2c38};2c09=\u{2c39};2c0a=\u{2c3a};2c0b=\u{2c3b};2c0c=\u{2c3c};2c0d=\u{2c3d};2c0e=\u{2c3e};2c0f=\u{2c3f};2c10=\u{2c40};2c11=\u{2c41};2c12=\u{2c42};2c13=\u{2c43};2c14=\u{2c44};2c15=\u{2c45};2c16=\u{2c46};2c17=\u{2c47};2c18=\u{2c48};2c19=\u{2c49};2c1a=\u{2c4a};2c1b=\u{2c4b};2c1c=\u{2c4c};2c1d=\u{2c4d};2c1e=\u{2c4e};2c1f=\u{2c4f};2c20=\u{2c50};2c21=\u{2c51};2c22=\u{2c52};2c23=\u{2c53};2c24=\u{2c54};2c25=\u{2c55};2c26=\u{2c56};2c27=\u{2c57};2c28=\u{2c58};2c29=\u{2c59};2c2a=\u{2c5a};2c2b=\u{2c5b};2c2c=\u{2c5c};2c2d=\u{2c5d};2c2e=\u{2c5e};2c2f=\u{2c5f};2c60=\u{2c61};2c62=\u{26b};2c63=\u{1d7d};2c64=\u{27d};2c67=\u{2c68};2c69=\u{2c6a};2c6b=\u{2c6c};2c6d=\u{251};2c6e=\u{271};2c6f=\u{250};2c70=\u{252};2c72=\u{2c73};2c75=\u{2c76};2c7e=\u{23f};2c7f=\u{240};2c80=\u{2c81};2c82=\u{2c83};2c84=\u{2c85};2c86=\u{2c87};2c88=\u{2c89};2c8a=\u{2c8b};2c8c=\u{2c8d};2c8e=\u{2c8f};2c90=\u{2c91};2c92=\u{2c93};2c94=\u{2c95};2c96=\u{2c97};2c98=\u{2c99};2c9a=\u{2c9b};2c9c=\u{2c9d};2c9e=\u{2c9f};2ca0=\u{2ca1};2ca2=\u{2ca3};2ca4=\u{2ca5};2ca6=\u{2ca7};2ca8=\u{2ca9};2caa=\u{2cab};2cac=\u{2cad};2cae=\u{2caf};2cb0=\u{2cb1};2cb2=\u{2cb3};2cb4=\u{2cb5};2cb6=\u{2cb7};2cb8=\u{2cb9};2cba=\u{2cbb};2cbc=\u{2cbd};2cbe=\u{2cbf};2cc0=\u{2cc1};2cc2=\u{2cc3};2cc4=\u{2cc5};2cc6=\u{2cc7};2cc8=\u{2cc9};2cca=\u{2ccb};2ccc=\u{2ccd};2cce=\u{2ccf};2cd0=\u{2cd1};2cd2=\u{2cd3};2cd4=\u{2cd5};2cd6=\u{2cd7};2cd8=\u{2cd9};2cda=\u{2cdb};2cdc=\u{2cdd};2cde=\u{2cdf};2ce0=\u{2ce1};2ce2=\u{2ce3};2ceb=\u{2cec};2ced=\u{2cee};2cf2=\u{2cf3};a640=\u{a641};a642=\u{a643};a644=\u{a645};a646=\u{a647};a648=\u{a649};a64a=\u{a64b};a64c=\u{a64d};a64e=\u{a64f};a650=\u{a651};a652=\u{a653};a654=\u{a655};a656=\u{a657};a658=\u{a659};a65a=\u{a65b};a65c=\u{a65d};a65e=\u{a65f};a660=\u{a661};a662=\u{a663};a664=\u{a665};a666=\u{a667};a668=\u{a669};a66a=\u{a66b};a66c=\u{a66d};a680=\u{a681};a682=\u{a683};a684=\u{a685};a686=\u{a687};a688=\u{a689};a68a=\u{a68b};a68c=\u{a68d};a68e=\u{a68f};a690=\u{a691};a692=\u{a693};a694=\u{a695};a696=\u{a697};a698=\u{a699};a69a=\u{a69b};a722=\u{a723};a724=\u{a725};a726=\u{a727};a728=\u{a729};a72a=\u{a72b};a72c=\u{a72d};a72e=\u{a72f};a732=\u{a733};a734=\u{a735};a736=\u{a737};a738=\u{a739};a73a=\u{a73b};a73c=\u{a73d};a73e=\u{a73f};a740=\u{a741};a742=\u{a743};a744=\u{a745};a746=\u{a747};a748=\u{a749};a74a=\u{a74b};a74c=\u{a74d};a74e=\u{a74f};a750=\u{a751};a752=\u{a753};a754=\u{a755};a756=\u{a757};a758=\u{a759};a75a=\u{a75b};a75c=\u{a75d};a75e=\u{a75f};a760=\u{a761};a762=\u{a763};a764=\u{a765};a766=\u{a767};a768=\u{a769};a76a=\u{a76b};a76c=\u{a76d};a76e=\u{a76f};a779=\u{a77a};a77b=\u{a77c};a77d=\u{1d79};a77e=\u{a77f};a780=\u{a781};a782=\u{a783};a784=\u{a785};a786=\u{a787};a78b=\u{a78c};a78d=\u{265};a790=\u{a791};a792=\u{a793};a796=\u{a797};a798=\u{a799};a79a=\u{a79b};a79c=\u{a79d};a79e=\u{a79f};a7a0=\u{a7a1};a7a2=\u{a7a3};a7a4=\u{a7a5};a7a6=\u{a7a7};a7a8=\u{a7a9};a7aa=\u{266};a7ab=\u{25c};a7ac=\u{261};a7ad=\u{26c};a7ae=\u{26a};a7b0=\u{29e};a7b1=\u{287};a7b2=\u{29d};a7b3=\u{ab53};a7b4=\u{a7b5};a7b6=\u{a7b7};a7b8=\u{a7b9};a7ba=\u{a7bb};a7bc=\u{a7bd};a7be=\u{a7bf};a7c0=\u{a7c1};a7c2=\u{a7c3};a7c4=\u{a794};a7c5=\u{282};a7c6=\u{1d8e};a7c7=\u{a7c8};a7c9=\u{a7ca};a7d0=\u{a7d1};a7d6=\u{a7d7};a7d8=\u{a7d9};a7f5=\u{a7f6};ab70=\u{13a0};ab71=\u{13a1};ab72=\u{13a2};ab73=\u{13a3};ab74=\u{13a4};ab75=\u{13a5};ab76=\u{13a6};ab77=\u{13a7};ab78=\u{13a8};ab79=\u{13a9};ab7a=\u{13aa};ab7b=\u{13ab};ab7c=\u{13ac};ab7d=\u{13ad};ab7e=\u{13ae};ab7f=\u{13af};ab80=\u{13b0};ab81=\u{13b1};ab82=\u{13b2};ab83=\u{13b3};ab84=\u{13b4};ab85=\u{13b5};ab86=\u{13b6};ab87=\u{13b7};ab88=\u{13b8};ab89=\u{13b9};ab8a=\u{13ba};ab8b=\u{13bb};ab8c=\u{13bc};ab8d=\u{13bd};ab8e=\u{13be};ab8f=\u{13bf};ab90=\u{13c0};ab91=\u{13c1};ab92=\u{13c2};ab93=\u{13c3};ab94=\u{13c4};ab95=\u{13c5};ab96=\u{13c6};ab97=\u{13c7};ab98=\u{13c8};ab99=\u{13c9};ab9a=\u{13ca};ab9b=\u{13cb};ab9c=\u{13cc};ab9d=\u{13cd};ab9e=\u{13ce};ab9f=\u{13cf};aba0=\u{13d0};aba1=\u{13d1};aba2=\u{13d2};aba3=\u{13d3};aba4=\u{13d4};aba5=\u{13d5};aba6=\u{13d6};aba7=\u{13d7};aba8=\u{13d8};aba9=\u{13d9};abaa=\u{13da};abab=\u{13db};abac=\u{13dc};abad=\u{13dd};abae=\u{13de};abaf=\u{13df};abb0=\u{13e0};abb1=\u{13e1};abb2=\u{13e2};abb3=\u{13e3};abb4=\u{13e4};abb5=\u{13e5};abb6=\u{13e6};abb7=\u{13e7};abb8=\u{13e8};abb9=\u{13e9};abba=\u{13ea};abbb=\u{13eb};abbc=\u{13ec};abbd=\u{13ed};abbe=\u{13ee};abbf=\u{13ef};fb00=ff;fb01=fi;fb02=fl;fb03=ffi;fb04=ffl;fb05=st;fb06=st;fb13=\u{574}\u{576};fb14=\u{574}\u{565};fb15=\u{574}\u{56b};fb16=\u{57e}\u{576};fb17=\u{574}\u{56d};ff21=\u{ff41};ff22=\u{ff42};ff23=\u{ff43};ff24=\u{ff44};ff25=\u{ff45};ff26=\u{ff46};ff27=\u{ff47};ff28=\u{ff48};ff29=\u{ff49};ff2a=\u{ff4a};ff2b=\u{ff4b};ff2c=\u{ff4c};ff2d=\u{ff4d};ff2e=\u{ff4e};ff2f=\u{ff4f};ff30=\u{ff50};ff31=\u{ff51};ff32=\u{ff52};ff33=\u{ff53};ff34=\u{ff54};ff35=\u{ff55};ff36=\u{ff56};ff37=\u{ff57};ff38=\u{ff58};ff39=\u{ff59};ff3a=\u{ff5a};10400=\u{10428};10401=\u{10429};10402=\u{1042a};10403=\u{1042b};10404=\u{1042c};10405=\u{1042d};10406=\u{1042e};10407=\u{1042f};10408=\u{10430};10409=\u{10431};1040a=\u{10432};1040b=\u{10433};1040c=\u{10434};1040d=\u{10435};1040e=\u{10436};1040f=\u{10437};10410=\u{10438};10411=\u{10439};10412=\u{1043a};10413=\u{1043b};10414=\u{1043c};10415=\u{1043d};10416=\u{1043e};10417=\u{1043f};10418=\u{10440};10419=\u{10441};1041a=\u{10442};1041b=\u{10443};1041c=\u{10444};1041d=\u{10445};1041e=\u{10446};1041f=\u{10447};10420=\u{10448};10421=\u{10449};10422=\u{1044a};10423=\u{1044b};10424=\u{1044c};10425=\u{1044d};10426=\u{1044e};10427=\u{1044f};104b0=\u{104d8};104b1=\u{104d9};104b2=\u{104da};104b3=\u{104db};104b4=\u{104dc};104b5=\u{104dd};104b6=\u{104de};104b7=\u{104df};104b8=\u{104e0};104b9=\u{104e1};104ba=\u{104e2};104bb=\u{104e3};104bc=\u{104e4};104bd=\u{104e5};104be=\u{104e6};104bf=\u{104e7};104c0=\u{104e8};104c1=\u{104e9};104c2=\u{104ea};104c3=\u{104eb};104c4=\u{104ec};104c5=\u{104ed};104c6=\u{104ee};104c7=\u{104ef};104c8=\u{104f0};104c9=\u{104f1};104ca=\u{104f2};104cb=\u{104f3};104cc=\u{104f4};104cd=\u{104f5};104ce=\u{104f6};104cf=\u{104f7};104d0=\u{104f8};104d1=\u{104f9};104d2=\u{104fa};104d3=\u{104fb};10570=\u{10597};10571=\u{10598};10572=\u{10599};10573=\u{1059a};10574=\u{1059b};10575=\u{1059c};10576=\u{1059d};10577=\u{1059e};10578=\u{1059f};10579=\u{105a0};1057a=\u{105a1};1057c=\u{105a3};1057d=\u{105a4};1057e=\u{105a5};1057f=\u{105a6};10580=\u{105a7};10581=\u{105a8};10582=\u{105a9};10583=\u{105aa};10584=\u{105ab};10585=\u{105ac};10586=\u{105ad};10587=\u{105ae};10588=\u{105af};10589=\u{105b0};1058a=\u{105b1};1058c=\u{105b3};1058d=\u{105b4};1058e=\u{105b5};1058f=\u{105b6};10590=\u{105b7};10591=\u{105b8};10592=\u{105b9};10594=\u{105bb};10595=\u{105bc};10c80=\u{10cc0};10c81=\u{10cc1};10c82=\u{10cc2};10c83=\u{10cc3};10c84=\u{10cc4};10c85=\u{10cc5};10c86=\u{10cc6};10c87=\u{10cc7};10c88=\u{10cc8};10c89=\u{10cc9};10c8a=\u{10cca};10c8b=\u{10ccb};10c8c=\u{10ccc};10c8d=\u{10ccd};10c8e=\u{10cce};10c8f=\u{10ccf};10c90=\u{10cd0};10c91=\u{10cd1};10c92=\u{10cd2};10c93=\u{10cd3};10c94=\u{10cd4};10c95=\u{10cd5};10c96=\u{10cd6};10c97=\u{10cd7};10c98=\u{10cd8};10c99=\u{10cd9};10c9a=\u{10cda};10c9b=\u{10cdb};10c9c=\u{10cdc};10c9d=\u{10cdd};10c9e=\u{10cde};10c9f=\u{10cdf};10ca0=\u{10ce0};10ca1=\u{10ce1};10ca2=\u{10ce2};10ca3=\u{10ce3};10ca4=\u{10ce4};10ca5=\u{10ce5};10ca6=\u{10ce6};10ca7=\u{10ce7};10ca8=\u{10ce8};10ca9=\u{10ce9};10caa=\u{10cea};10cab=\u{10ceb};10cac=\u{10cec};10cad=\u{10ced};10cae=\u{10cee};10caf=\u{10cef};10cb0=\u{10cf0};10cb1=\u{10cf1};10cb2=\u{10cf2};118a0=\u{118c0};118a1=\u{118c1};118a2=\u{118c2};118a3=\u{118c3};118a4=\u{118c4};118a5=\u{118c5};118a6=\u{118c6};118a7=\u{118c7};118a8=\u{118c8};118a9=\u{118c9};118aa=\u{118ca};118ab=\u{118cb};118ac=\u{118cc};118ad=\u{118cd};118ae=\u{118ce};118af=\u{118cf};118b0=\u{118d0};118b1=\u{118d1};118b2=\u{118d2};118b3=\u{118d3};118b4=\u{118d4};118b5=\u{118d5};118b6=\u{118d6};118b7=\u{118d7};118b8=\u{118d8};118b9=\u{118d9};118ba=\u{118da};118bb=\u{118db};118bc=\u{118dc};118bd=\u{118dd};118be=\u{118de};118bf=\u{118df};16e40=\u{16e60};16e41=\u{16e61};16e42=\u{16e62};16e43=\u{16e63};16e44=\u{16e64};16e45=\u{16e65};16e46=\u{16e66};16e47=\u{16e67};16e48=\u{16e68};16e49=\u{16e69};16e4a=\u{16e6a};16e4b=\u{16e6b};16e4c=\u{16e6c};16e4d=\u{16e6d};16e4e=\u{16e6e};16e4f=\u{16e6f};16e50=\u{16e70};16e51=\u{16e71};16e52=\u{16e72};16e53=\u{16e73};16e54=\u{16e74};16e55=\u{16e75};16e56=\u{16e76};16e57=\u{16e77};16e58=\u{16e78};16e59=\u{16e79};16e5a=\u{16e7a};16e5b=\u{16e7b};16e5c=\u{16e7c};16e5d=\u{16e7d};16e5e=\u{16e7e};16e5f=\u{16e7f};1e900=\u{1e922};1e901=\u{1e923};1e902=\u{1e924};1e903=\u{1e925};1e904=\u{1e926};1e905=\u{1e927};1e906=\u{1e928};1e907=\u{1e929};1e908=\u{1e92a};1e909=\u{1e92b};1e90a=\u{1e92c};1e90b=\u{1e92d};1e90c=\u{1e92e};1e90d=\u{1e92f};1e90e=\u{1e930};1e90f=\u{1e931};1e910=\u{1e932};1e911=\u{1e933};1e912=\u{1e934};1e913=\u{1e935};1e914=\u{1e936};1e915=\u{1e937};1e916=\u{1e938};1e917=\u{1e939};1e918=\u{1e93a};1e919=\u{1e93b};1e91a=\u{1e93c};1e91b=\u{1e93d};1e91c=\u{1e93e};1e91d=\u{1e93f};1e91e=\u{1e940};1e91f=\u{1e941};1e920=\u{1e942};1e921=\u{1e943}'.split(';').map(entry=>{const [source,replacement]=entry.split('=');return [Number.parseInt(source,16),replacement];}));
const SECRET_SCAN_CONTRACT=Object.freeze({version:'closed-loop-secret-scan/1',chunkBytes:1024*1024,boundaryCharacters:1024});
const DISCLOSURE_VALUES=Object.freeze(['PUBLIC','INTERNAL','CONFIDENTIAL','RESTRICTED','CREDENTIAL_SECRET','UNKNOWN']);
const LEGACY_KEYS=Object.freeze(['closed-loop-reliability-projects-v4','closed-loop-reliability-projects-v3','closed-loop-reliability-projects-v2','closed-loop-reliability-projects']);
const STORE_KEY=LEGACY_KEYS[0];
const hash=globalThis.closedLoopHash;
const clone=value=>value===undefined?undefined:(typeof structuredClone==='function'?structuredClone(value):JSON.parse(JSON.stringify(value)));
const projectIdentity=project=>String(project?.job?.JOB_ID||project?.jobId||'').trim();
const now=()=>new Date().toISOString();
const fault=phase=>{const configured=globalThis.__closedLoopStorageFault;if(configured===phase||configured?.phase===phase){const error=new Error(`Injected storage failure at ${phase}.`);error.code='INJECTED_STORAGE_FAILURE';throw error;}};
const request=req=>new Promise((resolve,reject)=>{req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error||new Error('IndexedDB request failed.'));});
const complete=tx=>new Promise((resolve,reject)=>{tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error||new Error('IndexedDB transaction failed.'));tx.onabort=()=>reject(tx.error||new Error('IndexedDB transaction aborted.'));});
const canonicalProject=project=>{const copy=clone(project);delete copy.projectSha256;return copy;};
const projectSha256=project=>hash.sha256Value(canonicalProject(project));
const storageError=(message,code)=>Object.assign(new Error(message),{code});
const utf8Length=value=>new TextEncoder().encode(String(value??'')).byteLength;
function unicodeDefaultCaseFold(value){let folded='';for(const character of String(value??'')){const codePoint=character.codePointAt(0);folded+=UNICODE_DEFAULT_CASE_FOLD_MAP.get(codePoint)??character;}return folded;}
function canonicalFilename(raw){
  const rawFilename=String(raw??'');if(!rawFilename||rawFilename.length>STORE_LIMITS.maxFilenameLength)throw storageError('Artifact filename is empty or exceeds the centralized filename limit.','INVALID_ARTIFACT_FILENAME');if(/[\0-\x1f\x7f]/.test(rawFilename))throw storageError('Artifact filename contains a control character.','UNSAFE_ARTIFACT_PATH');
  const normalized=rawFilename.replaceAll('\\','/');if(normalized.startsWith('/')||/^[A-Za-z]:\//.test(normalized))throw storageError('Absolute artifact paths are prohibited.','UNSAFE_ARTIFACT_PATH');const segments=normalized.split('/');if(segments.length>STORE_LIMITS.maxPathDepth||segments.some(segment=>!segment||segment==='.'||segment==='..'))throw storageError('Artifact path contains an empty, traversal, or excessive-depth segment.','UNSAFE_ARTIFACT_PATH');
  const canonicalSegments=segments.map(segment=>segment.normalize('NFC')),canonicalPath=canonicalSegments.join('/'),caseFoldKey=unicodeDefaultCaseFold(canonicalPath),platformRiskKey=canonicalSegments.map(segment=>unicodeDefaultCaseFold(segment.replace(/[ .]+$/g,''))).join('/');
  if(!canonicalPath||platformRiskKey.split('/').some(segment=>!segment))throw storageError('Artifact path is unsafe after normalization.','UNSAFE_ARTIFACT_PATH');return {rawFilename,displayFilename:segments.at(-1),canonicalPath,filenameNormalizationVersion:'closed-loop-filename/1',unicodeDefaultCaseFoldVersion:UNICODE_DEFAULT_CASE_FOLD_VERSION,collisionKeys:{canonical:canonicalPath,caseFold:caseFoldKey,platformRisk:platformRiskKey}};
}
function assertNoFilenameCollisions(items){const indexes={canonical:new Map(),caseFold:new Map(),platformRisk:new Map()};for(const item of items){const identity=String(item.artifactId||item.filename||item.rawFilename||'UNKNOWN'),contract=canonicalFilename(item.rawFilename??item.filename);for(const key of Object.keys(indexes)){const value=contract.collisionKeys[key],prior=indexes[key].get(value);if(prior&&prior!==identity)throw storageError(`Artifact filenames ${prior} and ${identity} collide under ${key} comparison.`,'ARTIFACT_FILENAME_COLLISION');indexes[key].set(value,identity);}item.filenameContract=contract;}return items;}
function randomNonce(){if(!globalThis.crypto?.getRandomValues)throw storageError('Cryptographically secure randomness is required for an external operation reservation.','SECURE_RANDOM_UNAVAILABLE');const bytes=new Uint8Array(16);globalThis.crypto.getRandomValues(bytes);return [...bytes].map(value=>value.toString(16).padStart(2,'0')).join('');}
function disclosureClassification(record){const engine=globalThis.closedLoopWorkflowEngine,value=engine?.recordValue?.(record,'DISCLOSURE_CLASSIFICATION')??record?.disclosureClassification??record?.classification??'UNKNOWN',normalized=String(value).toUpperCase();return DISCLOSURE_VALUES.includes(normalized)?normalized:'UNKNOWN';}
function scanKnownSecrets(filename,bytes){if(!(bytes instanceof Uint8Array))throw storageError('Secret scanning requires exact artifact bytes.','SECRET_SCAN_BYTES_REQUIRED');const findings=[];if(/(?:^|[._-])(env|pem|key|p12|pfx|credentials?|secrets?)(?:[._-]|$)/i.test(String(filename)))findings.push('HIGH_RISK_FILENAME');const patterns=[['PRIVATE_KEY',/-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----/],['AWS_ACCESS_KEY',/\bAKIA[0-9A-Z]{16}\b/],['GENERIC_SECRET',/\b(?:api[_-]?key|client[_-]?secret|password)\s{0,256}[:=]\s{0,256}[^\s,;]{8,}/i]],decoder=new TextDecoder('utf-8',{fatal:false});let carry='',bytesScanned=0;for(let offset=0;offset<bytes.byteLength;offset+=SECRET_SCAN_CONTRACT.chunkBytes){const end=Math.min(bytes.byteLength,offset+SECRET_SCAN_CONTRACT.chunkBytes),text=decoder.decode(bytes.subarray(offset,end),{stream:end<bytes.byteLength}),window=carry+text;for(const [code,pattern] of patterns)if(pattern.test(window))findings.push(code);carry=window.slice(-SECRET_SCAN_CONTRACT.boundaryCharacters);bytesScanned=end;}if(bytes.byteLength===0)decoder.decode();const complete=bytesScanned===bytes.byteLength;return {contractVersion:SECRET_SCAN_CONTRACT.version,findings:[...new Set(findings)],bytesScanned,totalBytes:bytes.byteLength,complete};}
function validateHostileStructure(value){const stack=[{value,depth:0}],seen=new Set();let nodes=0;while(stack.length){const current=stack.pop(),item=current.value;if(++nodes>STORE_LIMITS.maxImportObjectNodes)throw storageError('Imported structure exceeds the centralized node limit.','IMPORT_RESOURCE_LIMIT');if(current.depth>STORE_LIMITS.maxObjectDepth)throw storageError('Imported structure exceeds the centralized depth limit.','IMPORT_RESOURCE_LIMIT');if(typeof item==='string'&&item.length>STORE_LIMITS.maxStringLength)throw storageError('Imported string exceeds the centralized length limit.','IMPORT_RESOURCE_LIMIT');if(!item||typeof item!=='object')continue;if(seen.has(item))throw storageError('Cyclic imported structures are prohibited.','IMPORT_CYCLE');seen.add(item);if(Array.isArray(item)){if(item.length>STORE_LIMITS.maxArrayLength)throw storageError('Imported array exceeds the centralized length limit.','IMPORT_RESOURCE_LIMIT');for(const child of item)stack.push({value:child,depth:current.depth+1});}else for(const child of Object.values(item))stack.push({value:child,depth:current.depth+1});}return true;}
function reconcileRestoredOperationReservations(project,importMode){if(String(importMode||'').toUpperCase()!=='RESTORE')return {orphanedReservationIds:[],changed:false};const engine=globalThis.closedLoopWorkflowEngine;if(!engine)throw storageError('Workflow engine is required to reconcile imported operation reservations.','IMPORT_RESERVATION_ENGINE_UNAVAILABLE');const controlledStatuses=new Set(['RESERVED','ACTIVE','RESUMED','ORPHANED']),authoritativeStatuses=new Set(['RESERVED','ACTIVE','RESUMED']),controlled=(project?.projectData?.operationReservations||[]).filter(record=>record?.active!==false&&!record?.invalidatedBy&&controlledStatuses.has(String(engine.recordValue(record,'STATUS')||record.status||'').toUpperCase())),targets=new Map();for(const reservation of controlled){const id=String(engine.recordId(reservation,'operationReservations')||''),targetKey=String(engine.recordValue(reservation,'TARGET_KEY')||'').trim();if(!id||!targetKey)throw storageError('An imported controlled operation reservation lacks its canonical identity or target key.','IMPORT_OPERATION_RESERVATION_INVALID');if(targets.has(targetKey))throw storageError(`Imported operation reservations ${targets.get(targetKey)} and ${id} both claim controlled target ${targetKey}.`,'IMPORT_DUPLICATE_AUTHORITATIVE_RESERVATION');targets.set(targetKey,id);}const reservations=controlled.filter(record=>authoritativeStatuses.has(String(engine.recordValue(record,'STATUS')||record.status||'').toUpperCase())),orphanedReservationIds=[];for(const reservation of reservations){const id=String(engine.recordId(reservation,'operationReservations')),reason='RESTORE activated this lineage in a new browser session. Explicit resume or cancellation is required before external work continues.';reservation.fields={...(reservation.fields||{}),STATUS:'ORPHANED',TRANSITION_REASON:reason};reservation.STATUS='ORPHANED';reservation.TRANSITION_REASON=reason;reservation.status='ORPHANED';reservation.active=true;engine.refreshRecordHashes(reservation,'operationReservations');orphanedReservationIds.push(id);}if(orphanedReservationIds.length){engine.addHistory(project,'RESTORE_OPERATION_RESERVATIONS_ORPHANED',{reservationIds:orphanedReservationIds,reason:'Imported reservations cannot remain authoritative across browser-session custody boundaries.'});engine.recalculate(project);}return {orphanedReservationIds,changed:orphanedReservationIds.length>0};}
function assertProjectResourceLimits(project,{artifactCount=null}={}){
  const history=project?.projectData?.history;
  if(Array.isArray(history)&&history.length>STORE_LIMITS.maxHistoryRecords)throw storageError('Project history exceeds the centralized history-record limit.','HISTORY_RESOURCE_LIMIT');
  if(artifactCount!==null&&Number(artifactCount)>STORE_LIMITS.maxPackageArtifacts)throw storageError('Package artifact count exceeds the centralized package-artifact limit.','PACKAGE_ARTIFACT_LIMIT');
  return true;
}
function normalizedDisclosureAuthorization(value){
  if(!value||typeof value!=='object'||Array.isArray(value))return null;
  const normalized={artifactIds:[...new Set((Array.isArray(value.artifactIds)?value.artifactIds:[]).map(String).filter(Boolean))].sort(),recipientSuitability:String(value.recipientSuitability||'').toUpperCase(),humanAuthorizationId:String(value.humanAuthorizationId||'')};return normalized.artifactIds.length||normalized.recipientSuitability||normalized.humanAuthorizationId?normalized:null;
}
function normalizeExecutionPackageDescriptor(value){
  if(!value||typeof value!=='object'||Array.isArray(value))throw storageError('An execution-package descriptor must be an object.','EXECUTION_PACKAGE_DESCRIPTOR_INVALID');
  const normalizeArtifacts=items=>(Array.isArray(items)?items:[]).map(item=>({artifactId:String(item?.artifactId||''),filename:String(item?.filename||''),byteSize:Number(item?.byteSize??0),sha256:String(item?.sha256||''),role:String(item?.role||''),availability:String(item?.availability||''),disclosureClassification:String(item?.disclosureClassification||'UNKNOWN')})).sort((a,b)=>a.artifactId<b.artifactId?-1:a.artifactId>b.artifactId?1:0);
  return {schema:'closed-loop-execution-package-descriptor/1',jobId:String(value.jobId||''),stage:Number(value.stage),operation:String(value.operation||'COMPLETE'),targetSlot:String(value.targetSlot||''),scopeSha256:String(value.scopeSha256||''),testIds:[...new Set((Array.isArray(value.testIds)?value.testIds:[]).map(String).filter(Boolean))].sort(),artifacts:normalizeArtifacts(value.artifacts),filesToWithhold:clone(Array.isArray(value.filesToWithhold)?value.filesToWithhold:[]),expectedReturnFiles:clone(Array.isArray(value.expectedReturnFiles)?value.expectedReturnFiles:[]),disclosureAuthorization:normalizedDisclosureAuthorization(value.disclosureAuthorization),runId:value.runId===null||value.runId===undefined?null:String(value.runId),productId:value.productId===null||value.productId===undefined?null:String(value.productId)};
}
const executionPackageDescriptorSha256=value=>hash.sha256Value(normalizeExecutionPackageDescriptor(value));
function preserveOpaqueExtensions(project,source){const known=new Set(['schema','workflow','stageCount','job','stages','projectData','revision','projectSha256','migratedFrom','migrationHistory']);const unknown=Object.fromEntries(Object.entries(source||{}).filter(([key])=>!known.has(key)));if(!Object.keys(unknown).length)return;const serialized=JSON.stringify(unknown);if(utf8Length(serialized)>STORE_LIMITS.maxExtensionBytes)throw storageError('Unknown extension data exceeds the centralized import limit.','IMPORT_EXTENSION_LIMIT');project.projectData=project.projectData&&typeof project.projectData==='object'?project.projectData:{};project.projectData.importedOpaqueExtensions=Array.isArray(project.projectData.importedOpaqueExtensions)?project.projectData.importedOpaqueExtensions:[];project.projectData.importedOpaqueExtensions.push({schema:'closed-loop-opaque-extension/1',operational:false,executable:false,gating:false,promptVisible:false,currentScopeEligible:false,rawCanonicalJson:serialized,sha256:hash.sha256Text(serialized),importedAt:now()});}
async function preflightStorage(requiredBytes){let estimate=null;try{estimate=await globalThis.navigator?.storage?.estimate?.();}catch{}const required=Math.max(0,Number(requiredBytes||0))+STORE_LIMITS.storageSafetyMarginBytes;if(Number.isFinite(estimate?.quota)&&Number.isFinite(estimate?.usage)&&estimate.quota-estimate.usage<required)throw storageError(`Insufficient estimated browser storage for ${requiredBytes} bytes plus the centralized safety margin.`,'INSUFFICIENT_STORAGE_ESTIMATE');return {requiredBytes:Number(requiredBytes||0),safetyMarginBytes:STORE_LIMITS.storageSafetyMarginBytes,usage:estimate?.usage??null,quota:estimate?.quota??null,status:Number.isFinite(estimate?.quota)?'ESTIMATED':'UNKNOWN',warning:Number.isFinite(estimate?.quota)?null:'Storage estimate is unavailable; actual quota failure will still commit completely or not at all.'};}
const runSynchronousMutator=(mutator,next,before)=>{const result=mutator(next,before);if(result&&typeof result.then==='function')throw storageError('Project transaction mutators must be synchronous. Complete asynchronous work before opening the canonical IndexedDB transaction.','ASYNC_TRANSACTION_MUTATOR');return result;};
function finalizeCommandReceiptsForCommit(next,before,expectedRevision,committedRevision){
  const priorIds=new Set((before?.projectData?.commandReceipts||[]).map(receipt=>String(receipt.receiptId||''))),created=(next?.projectData?.commandReceipts||[]).filter(receipt=>!priorIds.has(String(receipt.receiptId||'')));for(const receipt of created){if(Number(receipt.expectedRevision)!==Number(expectedRevision))throw storageError('A canonical command receipt is not bound to the transaction precondition revision.','COMMAND_RECEIPT_REVISION_MISMATCH');receipt.committedRevision=Number(committedRevision);for(const event of next?.projectData?.history||[])if(event.type==='IDEMPOTENT_COMMAND_COMMITTED'&&String(event.commandId||'')===String(receipt.commandId||''))event.committedRevision=Number(committedRevision);}return created;
}
const PLACEHOLDER_REFERENCES=new Set(['','NONE','NOT APPLICABLE','UNKNOWN','PENDING','UNASSIGNED']);
const equivalent=(left,right)=>hash.sha256Value(left)===hash.sha256Value(right);
const OBSERVATION_ORIGIN_FIELDS=Object.freeze({native:Object.freeze(['NATIVE_OBSERVED_VALUE','NATIVE_OBSERVED_LOCATION','NATIVE_METHOD_OR_TOOL']),external:Object.freeze(['EXTERNAL_OBSERVED_VALUE','EXTERNAL_OBSERVED_LOCATION','EXTERNAL_METHOD_OR_TOOL','EXTERNAL_LIMITATIONS']),human:Object.freeze(['HUMAN_OBSERVED_VALUE','HUMAN_OBSERVED_LOCATION','HUMAN_METHOD_OR_TOOL'])});
function validateObservationOriginFields(record,engine){
  const id=engine.recordId(record,'observationRecords')||'UNKNOWN',origin=String(engine.recordValue(record,'ORIGIN_CLASS')||'').toUpperCase(),active=origin==='NATIVE_APPLICATION_OBSERVATION'?'native':['VERIFIED_EXTERNAL_OBSERVATION','EXTERNAL_CLAIM','AGENT_SEMANTIC_OBSERVATION'].includes(origin)?'external':origin==='HUMAN_OBSERVATION'?'human':null,issues=[];
  if(!active)return [`observationRecords record ${id} has no recognized application-normalized ORIGIN_CLASS.`];
  const required=OBSERVATION_ORIGIN_FIELDS[active].slice(0,3),observed=engine.recordValue(record,required[0]),location=engine.recordValue(record,required[1]),method=engine.recordValue(record,required[2]);if(!observed||typeof observed!=='object'||Array.isArray(observed))issues.push(`observationRecords record ${id} ${origin} lacks its origin-specific observed value object.`);if(typeof location!=='string'||!location.trim())issues.push(`observationRecords record ${id} ${origin} lacks its origin-specific observed location.`);if(typeof method!=='string'||!method.trim())issues.push(`observationRecords record ${id} ${origin} lacks its origin-specific method or tool identity.`);
  for(const [group,fields] of Object.entries(OBSERVATION_ORIGIN_FIELDS))if(group!==active)for(const field of fields){const nestedPresent=Boolean(record?.fields&&Object.prototype.hasOwnProperty.call(record.fields,field)),topPresent=Object.prototype.hasOwnProperty.call(record||{},field);if((nestedPresent||topPresent)&&engine.recordValue(record,field)!==null)issues.push(`observationRecords record ${id} ${origin} contains non-null ${field} from a different origin class.`);}
  return issues;
}
function validateProjectIntegrity(project,{verifyDerived=true}={}){
  const issues=[],schemaApi=globalThis.closedLoopWorkflowSchema,engine=globalThis.closedLoopWorkflowEngine;
  if(!project||typeof project!=='object')return {valid:false,issues:['Project is not an object.']};
  if(Array.isArray(project.projectData?.history)&&project.projectData.history.length>STORE_LIMITS.maxHistoryRecords)issues.push('Project history exceeds the centralized history-record limit.');
  if(!schemaApi||!engine)return {valid:false,issues:['Workflow schema and engine are required for canonical project integrity validation.']};
  if(project.schema!==schemaApi.PROJECT_SCHEMA)issues.push(`Project schema ${project.schema||'UNKNOWN'} does not match ${schemaApi.PROJECT_SCHEMA}.`);
  if(project.workflow!==schemaApi.WORKFLOW_ID)issues.push(`Workflow ${project.workflow||'UNKNOWN'} does not match ${schemaApi.WORKFLOW_ID}.`);
  if(Number(project.stageCount)!==Number(schemaApi.STAGE_COUNT))issues.push(`Stage count ${project.stageCount} does not match ${schemaApi.STAGE_COUNT}.`);
  if(Object.keys(project.stages||{}).length!==Number(schemaApi.STAGE_COUNT))issues.push('Canonical project does not contain exactly the declared workflow stages.');
  if(!projectIdentity(project))issues.push('Canonical project has no JOB_ID.');
  const idsByCollection=new Map();
  for(const [collection,definition] of Object.entries(schemaApi.RECORD_SCHEMAS)){
    const list=project.projectData?.[collection];if(list!==undefined&&!Array.isArray(list)){issues.push(`${collection} is not an array.`);continue;}
    const seen=new Set();for(const record of Array.isArray(list)?list:[]){const id=engine.recordId(record,collection),fieldId=String(engine.recordValue(record,definition.idField)||'').trim();if(!id){issues.push(`${collection} contains a record without ${definition.idField}.`);continue;}if(seen.has(id))issues.push(`${collection} contains duplicate canonical ID ${id}.`);seen.add(id);if(record?.id&&fieldId&&String(record.id)!==fieldId)issues.push(`${collection} record ${id} contradicts its ${definition.idField}.`);if(record?.contentSha256&&String(record.contentSha256)!==String(hash.contentRecordSha256(record,definition.idField)))issues.push(`${collection} record ${id} contentSha256 does not match canonical content.`);if(record?.recordSha256&&String(record.recordSha256)!==String(hash.recordSha256(record)))issues.push(`${collection} record ${id} recordSha256 does not match the persisted canonical record.`);if(record?.recordSha256&&record?.sha256&&String(record.sha256)!==String(record.recordSha256))issues.push(`${collection} record ${id} sha256 does not mirror recordSha256.`);}
    idsByCollection.set(collection,seen);
  }
  const ingestion=globalThis.closedLoopResponseIngestion;
  if(!ingestion?.validateValue)issues.push('Response-ingestion value validator is required for canonical project integrity validation.');
  else for(const [collection,definition] of Object.entries(schemaApi.RECORD_SCHEMAS))for(const record of Array.isArray(project.projectData?.[collection])?project.projectData[collection]:[]){const id=engine.recordId(record,collection)||'UNKNOWN',nested=record?.fields&&typeof record.fields==='object'&&!Array.isArray(record.fields)?record.fields:null;for(const [fieldName,fieldDefinition] of Object.entries(definition.fieldDefinitions||{})){const nestedPresent=Boolean(nested&&Object.prototype.hasOwnProperty.call(nested,fieldName)),topPresent=Object.prototype.hasOwnProperty.call(record||{},fieldName);if(!nestedPresent&&!topPresent)continue;if(nestedPresent&&topPresent&&!equivalent(nested[fieldName],record[fieldName]))issues.push(`${collection} record ${id} has contradictory mirrored value for ${fieldName}.`);const value=engine.recordValue(record,fieldName),fieldIssues=[];ingestion.validateValue(fieldDefinition,value,`/${collection}/${id}/${fieldName}`,fieldIssues);for(const item of fieldIssues)issues.push(`${collection} record ${id} field ${fieldName}: ${item.message}`);}}
  for(const observation of Array.isArray(project.projectData?.observationRecords)?project.projectData.observationRecords:[])issues.push(...validateObservationOriginFields(observation,engine));
  {const controlledStatuses=new Set(['RESERVED','ACTIVE','RESUMED','ORPHANED']),targets=new Map();for(const reservation of Array.isArray(project.projectData?.operationReservations)?project.projectData.operationReservations:[]){const status=String(engine.recordValue(reservation,'STATUS')||reservation.status||'').toUpperCase();if(reservation?.active===false||reservation?.invalidatedBy||!controlledStatuses.has(status))continue;const id=engine.recordId(reservation,'operationReservations')||'UNKNOWN',targetKey=String(engine.recordValue(reservation,'TARGET_KEY')||'').trim();if(!targetKey)issues.push(`Controlled operation reservation ${id} has no TARGET_KEY.`);else if(targets.has(targetKey))issues.push(`Controlled operation reservations ${targets.get(targetKey)} and ${id} duplicate TARGET_KEY ${targetKey}.`);else targets.set(targetKey,id);}}
  for(const [collection,definition] of Object.entries(schemaApi.RECORD_SCHEMAS))for(const record of Array.isArray(project.projectData?.[collection])?project.projectData[collection]:[])for(const [fieldName,targetCollection] of Object.entries(definition.relationships||{})){const raw=record?.relationships?.[fieldName]??engine.recordValue(record,fieldName);for(const value of Array.isArray(raw)?raw:[raw]){const ref=String(value??'').trim();if(PLACEHOLDER_REFERENCES.has(ref.toUpperCase()))continue;if(!idsByCollection.get(targetCollection)?.has(ref))issues.push(`${collection}.${fieldName} references missing ${targetCollection} record ${ref}.`);}}
  let previousSequence=0;const eventIds=new Set();for(const event of Array.isArray(project.projectData?.history)?project.projectData.history:[]){const sequence=Number(event?.eventSequence);if(!Number.isInteger(sequence)||sequence<=previousSequence)issues.push('History eventSequence is missing, duplicated, or non-monotonic.');previousSequence=Math.max(previousSequence,Number.isFinite(sequence)?sequence:0);const eventId=String(event?.eventId||'');if(eventId&&eventIds.has(eventId))issues.push(`History contains duplicate event ID ${eventId}.`);if(eventId)eventIds.add(eventId);}if(Number(project.projectData?.eventSequence||0)<previousSequence)issues.push('Project eventSequence is behind committed history.');
  if(verifyDerived){const expected=clone(project);delete expected.projectSha256;engine.ensureShape(expected);engine.recalculate(expected);for(const name of ['CURRENT_STAGE','CURRENT_STATE','CURRENT_BLOCKERS','NEXT_REQUIRED_ACTION','JOB_RECORD_STATUS','STATUS_EVIDENCE'])if(!equivalent(project.job?.[name],expected.job?.[name]))issues.push(`Application-derived job field ${name} does not match deterministic recalculation.`);for(let stage=1;stage<=Number(schemaApi.STAGE_COUNT);stage++){if(String(project.stages?.[stage]?.status||'')!==String(expected.stages?.[stage]?.status||''))issues.push(`Stage ${stage} status does not match deterministic recalculation.`);if(!equivalent(project.stages?.[stage]?.derivedData||{},expected.stages?.[stage]?.derivedData||{}))issues.push(`Stage ${stage} derivedData does not match deterministic recalculation.`);}const releaseRecords=(project.projectData?.releaseRecords||[]).filter(record=>record?.active!==false&&!record?.invalidatedBy),latestRelease=releaseRecords.at(-1);if(latestRelease){const actual=String(engine.recordValue(latestRelease,'DETERMINATION')||''),calculated=String(engine.releaseMetrics(expected).determination||'');if(actual!==calculated)issues.push(`Current release determination ${actual||'UNKNOWN'} does not match deterministic release calculation ${calculated||'UNKNOWN'}.`);}}
  const artifacts=new Map((project.projectData?.artifacts||[]).map(record=>[engine.recordId(record,'artifacts'),record]));for(const identity of (project.projectData?.artifactIdentities||[]).filter(record=>record?.active!==false&&!record?.invalidatedBy)){const artifact=artifacts.get(String(engine.recordValue(identity,'ARTIFACT_ID')||''));const auditedHash=String(engine.recordValue(identity,'AUDITED_SHA256')||''),deliveryHash=String(engine.recordValue(identity,'PRE_DELIVERY_SHA256')||''),auditedSize=Number(engine.recordValue(identity,'AUDITED_BYTE_SIZE')),deliverySize=Number(engine.recordValue(identity,'RELEASE_BYTE_SIZE')),sameHash=Boolean(auditedHash&&deliveryHash&&auditedHash===deliveryHash),sameSize=Number.isFinite(auditedSize)&&Number.isFinite(deliverySize)&&auditedSize===deliverySize,sameName=String(engine.recordValue(identity,'AUDITED_FILENAME')||'')===String(engine.recordValue(identity,'RELEASE_FILENAME')||''),authorized=sameHash&&sameSize&&sameName;if(Boolean(engine.recordValue(identity,'EXACT_HASH_MATCH'))!==sameHash||Boolean(engine.recordValue(identity,'EXACT_SIZE_MATCH'))!==sameSize||String(engine.recordValue(identity,'AUTHORIZATION')||'')!==(authorized?'AUTHORIZED':'NOT AUTHORIZED'))issues.push(`Artifact identity ${engine.recordId(identity,'artifactIdentities')||'UNKNOWN'} contradicts its deterministic comparison.`);if(artifact){if(String(engine.recordValue(artifact,'SHA256')||'')!==auditedHash||Number(engine.recordValue(artifact,'BYTE_SIZE'))!==auditedSize||String(engine.recordValue(artifact,'FILENAME')||'')!==String(engine.recordValue(identity,'AUDITED_FILENAME')||''))issues.push(`Artifact identity ${engine.recordId(identity,'artifactIdentities')||'UNKNOWN'} does not match its canonical artifact.`);}}
  return {valid:issues.length===0,issues};
}
function assertProjectIntegrity(project,options){const result=validateProjectIntegrity(project,options);if(!result.valid){const error=storageError(`Canonical project integrity validation failed: ${result.issues.join(' | ')}`,'PROJECT_INTEGRITY_FAILED');error.issues=result.issues;throw error;}return result;}

let databasePromise=null;
let databaseHandle=null;
function resetDatabaseConnection(db=null){
  if(db&&databaseHandle&&db!==databaseHandle)return;
  databaseHandle=null;
  databasePromise=null;
}
function openDatabase(){
  if(!globalThis.indexedDB)return Promise.reject(Object.assign(new Error('IndexedDB is required by the supported browser contract.'),{code:'INDEXEDDB_REQUIRED'}));
  if(databasePromise)return databasePromise;
  const opening=new Promise((resolve,reject)=>{
    const req=indexedDB.open(DB_NAME,DB_VERSION);
    req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains(PROJECTS))db.createObjectStore(PROJECTS,{keyPath:'jobId'});if(!db.objectStoreNames.contains(ARTIFACTS))db.createObjectStore(ARTIFACTS,{keyPath:'artifactId'});if(!db.objectStoreNames.contains(STAGING))db.createObjectStore(STAGING,{keyPath:'stagedArtifactId'});if(!db.objectStoreNames.contains(META))db.createObjectStore(META,{keyPath:'key'});};
    req.onsuccess=()=>{const db=req.result;databaseHandle=db;db.onclose=()=>resetDatabaseConnection(db);db.onversionchange=()=>{resetDatabaseConnection(db);try{db.close();}catch{}};resolve(db);};
    req.onerror=()=>{resetDatabaseConnection();reject(req.error||new Error('IndexedDB open failed.'));};
    req.onblocked=()=>{resetDatabaseConnection();reject(Object.assign(new Error('IndexedDB upgrade is blocked by another tab.'),{code:'INDEXEDDB_BLOCKED'}));};
  });
  databasePromise=opening;
  return opening;
}
async function openTransaction(stores,mode='readonly'){
  for(let attempt=0;attempt<2;attempt++){
    const db=await openDatabase();
    try{return db.transaction(stores,mode);}
    catch(error){if(attempt===0&&error?.name==='InvalidStateError'){resetDatabaseConnection(db);continue;}throw error;}
  }
  throw storageError('IndexedDB connection could not be reopened for a transaction.','INDEXEDDB_CONNECTION_UNAVAILABLE');
}

function parseLegacy(storage=globalThis.localStorage){const out=[],seen=new Set();if(!storage)return out;for(const key of LEGACY_KEYS){let raw=null;try{raw=storage.getItem(key);}catch(error){throw storageError(`Legacy project storage could not be read from ${key}: ${error.message||error}`,'LEGACY_MIGRATION_READ_FAILED');}if(!raw)continue;let parsed;try{parsed=JSON.parse(raw);}catch(error){throw storageError(`Legacy project storage ${key} contains malformed JSON: ${error.message||error}`,'LEGACY_MIGRATION_PARSE_FAILED');}const items=Array.isArray(parsed)?parsed:[parsed];for(let index=0;index<items.length;index++){const item=items[index];if(!item||typeof item!=='object'||Array.isArray(item))throw storageError(`Legacy project storage ${key}[${index}] is not a project object.`,'LEGACY_MIGRATION_INVALID_PROJECT');const id=projectIdentity(item);if(!id)throw storageError(`Legacy project storage ${key}[${index}] has no JOB_ID.`,'LEGACY_MIGRATION_INVALID_PROJECT');if(seen.has(id))continue;seen.add(id);out.push(item);}}return out;}
function readAllLegacy(storage){return parseLegacy(storage);}
function writeAllLegacy(projects,storage){if(!storage)throw new Error('Legacy test storage is unavailable.');const payload=JSON.stringify(projects),prior=storage.getItem(STORE_KEY);try{fault('before-final-write');storage.setItem(STORE_KEY,payload);fault('after-final-write');return {changed:prior!==payload};}catch(error){try{if(prior===null)storage.removeItem(STORE_KEY);else storage.setItem(STORE_KEY,prior);}catch{}throw error;}}

async function metaPut(key,value,tx=null){const own=tx||await openTransaction(META,'readwrite');own.objectStore(META).put({key,value,updatedAt:now()});if(!tx)await complete(own);return value;}
async function metaGet(key){const tx=await openTransaction(META,'readonly');const row=await request(tx.objectStore(META).get(key));await complete(tx);return row?.value;}
async function freezeExecutionPackageDescriptor({operationReservationId,packageId,descriptor,requiredSha256=''}){
  const normalized=normalizeExecutionPackageDescriptor(descriptor),descriptorSha256=hash.sha256Value(normalized),required=String(requiredSha256||'');
  if(!required)throw storageError('The canonical operation reservation does not contain PACKAGE_MANIFEST_HASH. Cancel it and create a new descriptor-bound reservation before packaging.','EXECUTION_PACKAGE_DESCRIPTOR_UNBOUND');
  if(required!==descriptorSha256)throw storageError('The execution-package descriptor does not match the manifest hash frozen in the operation reservation.','EXECUTION_PACKAGE_DESCRIPTOR_MISMATCH');
  const reservationKey=`operationPackageDescriptor:${String(operationReservationId||'')}`,packageKey=`executionPackageDescriptor:${String(packageId||'')}`,tx=await openTransaction(META,'readwrite'),meta=tx.objectStore(META);
  try{const priorReservation=await request(meta.get(reservationKey)),priorPackage=await request(meta.get(packageKey)),priors=[priorReservation?.value,priorPackage?.value].filter(Boolean);for(const prior of priors)if(String(prior.operationReservationId)!==String(operationReservationId)||String(prior.packageId)!==String(packageId)||String(prior.descriptorSha256)!==descriptorSha256)throw storageError('The same operation reservation or PACKAGE_ID cannot be reused with a different test, file, withholding, return, alias, or disclosure descriptor.','EXECUTION_PACKAGE_DESCRIPTOR_CONFLICT');const value=clone(priors[0]||{operationReservationId:String(operationReservationId||''),packageId:String(packageId||''),descriptorSha256,descriptor:normalized,createdAt:now(),bindingBasis:'RESERVATION_PACKAGE_MANIFEST_HASH',canonicalReservationBound:true});meta.put({key:reservationKey,value,updatedAt:value.createdAt});meta.put({key:packageKey,value,updatedAt:value.createdAt});await complete(tx);return value;}catch(error){try{tx.abort();}catch{}throw error;}
}
async function quarantine(row,reason){const tx=await openTransaction([PROJECTS,META],'readwrite'),projects=tx.objectStore(PROJECTS),meta=tx.objectStore(META);try{const current=await request(projects.get(String(row?.jobId||'')));if(!current){await complete(tx);return false;}const currentStoredHash=String(current.projectSha256||''),rowStoredHash=String(row?.projectSha256||'');if(currentStoredHash!==rowStoredHash){await complete(tx);return false;}const corruptHash=rowStoredHash||projectSha256(row.project),key=`quarantine:${row?.jobId||'UNKNOWN'}:${corruptHash}`;meta.put({key,value:{reason,row:clone(row)},updatedAt:now()});projects.delete(String(row.jobId));await complete(tx);return true;}catch(error){try{tx.abort();}catch{}throw error;}}

async function migrateLegacy(){
  const countTx=await openTransaction(PROJECTS,'readonly'),count=await request(countTx.objectStore(PROJECTS).count());await complete(countTx);if(count)return {migrated:0};
  const legacy=parseLegacy();if(!legacy.length){await metaPut('migrationStatus',{status:'NONE',at:now()});return {migrated:0};}
  const tx=await openTransaction([PROJECTS,META],'readwrite');let migrated=0;
  try{fault('before-legacy-migration');const core=globalThis.closedLoopCore,engine=globalThis.closedLoopWorkflowEngine,schemaApi=globalThis.closedLoopWorkflowSchema;if(!core?.migrateState||!engine)throw storageError('Canonical workflow migration logic is unavailable.','LEGACY_MIGRATION_UNAVAILABLE');for(const source of legacy){let project=core.migrateState(clone(source));if(typeof schemaApi?.migrateProjectToCurrent==='function')project=schemaApi.migrateProjectToCurrent(project);preserveOpaqueExtensions(project,source);engine.ensureShape(project);engine.recalculate(project);assertProjectIntegrity(project,{verifyDerived:true});const id=projectIdentity(project);if(!id)throw storageError('Migrated legacy project has no JOB_ID.','LEGACY_MIGRATION_INVALID_PROJECT');const revision=Number(project.revision||0);tx.objectStore(PROJECTS).put({jobId:id,revision,project,projectSha256:projectSha256(project),updatedAt:now()});migrated++;}tx.objectStore(META).put({key:'migrationStatus',value:{status:'COMPLETE',migrated,at:now()},updatedAt:now()});fault('during-legacy-migration');await complete(tx);for(const key of LEGACY_KEYS)try{localStorage.removeItem(key);}catch{}return {migrated};}catch(error){try{tx.abort();}catch{}await metaPut('migrationStatus',{status:'FAILED',message:String(error.message||error),originalPreserved:true,at:now()});throw error;}
}

async function readAllIndexed(){try{await migrateLegacy();globalThis.closedLoopLegacyMigrationError=null;}catch(error){globalThis.closedLoopLegacyMigrationError=String(error?.stack||error);console.error('Legacy migration failed without deleting the preserved legacy payload; continuing with the canonical IndexedDB store.',error);}const tx=await openTransaction(PROJECTS,'readonly'),rows=await request(tx.objectStore(PROJECTS).getAll());await complete(tx);const valid=[];for(const row of rows){const actual=projectSha256(row.project);if(actual!==row.projectSha256){await quarantine(row,'PROJECT_HASH_MISMATCH');continue;}const project=clone(row.project);project.revision=Number(row.revision||project.revision||0);if(project.schema===globalThis.closedLoopWorkflowSchema?.PROJECT_SCHEMA){const integrity=validateProjectIntegrity(project,{verifyDerived:false});if(!integrity.valid){await quarantine(row,'PROJECT_CANONICAL_INTEGRITY_FAILED: '+integrity.issues.join(' | '));continue;}}project.projectSha256=row.projectSha256;valid.push(project);}return valid;}
function readAll(storage){return storage?readAllLegacy(storage):readAllIndexed();}

async function writeProject(project,{expectedProjectRevision=null,incrementRevision=true}={}){
  const id=projectIdentity(project);if(!id)throw new Error('A project without a JOB_ID cannot be committed.');fault('before-project-transaction');const tx=await openTransaction([PROJECTS,META],'readwrite');const store=tx.objectStore(PROJECTS);try{const prior=await request(store.get(id));const currentRevision=Number(prior?.revision||0);if(expectedProjectRevision!==null&&Number(expectedProjectRevision)!==currentRevision){const e=new Error(`Project revision conflict for ${id}: expected ${expectedProjectRevision}, found ${currentRevision}.`);e.code='STALE_PROJECT_REVISION';throw e;}const next=clone(project);next.revision=incrementRevision?currentRevision+1:currentRevision;delete next.projectSha256;const engine=globalThis.closedLoopWorkflowEngine;engine?.ensureShape?.(next);engine?.recalculate?.(next);assertProjectIntegrity(next);const digest=projectSha256(next);fault('during-project-write');store.put({jobId:id,revision:next.revision,project:next,projectSha256:digest,updatedAt:now()});tx.objectStore(META).put({key:'selectedProject',value:id,updatedAt:now()});tx.objectStore(META).put({key:'lastCommittedRevision',value:{jobId:id,revision:next.revision,projectSha256:digest},updatedAt:now()});fault('before-transaction-commit');await complete(tx);next.projectSha256=digest;try{new BroadcastChannel('closed-loop-reliability').postMessage({type:'PROJECT_CHANGED',jobId:id,revision:next.revision});}catch{}return next;}catch(error){try{tx.abort();}catch{}throw error;}
}

async function writeAllIndexed(projects){if(!Array.isArray(projects))throw new TypeError('Project storage payload must be an array.');const saved=[];for(const project of projects){const id=projectIdentity(project);if(!id)continue;const tx=await openTransaction(PROJECTS,'readonly'),prior=await request(tx.objectStore(PROJECTS).get(id));await complete(tx);const incoming=clone(project);delete incoming.projectSha256;const digest=projectSha256(incoming);if(prior?.projectSha256===digest){incoming.revision=Number(prior.revision||0);incoming.projectSha256=digest;saved.push(incoming);}else saved.push(await writeProject(incoming,{expectedProjectRevision:Number(prior?.revision||0),incrementRevision:Boolean(prior)}));}return saved;}
function writeAll(projects,storage){return storage?writeAllLegacy(projects,storage):writeAllIndexed(projects);}

async function replaceProjectIndexed(projectsOrProject,projectOrOptions){
  if(Array.isArray(projectsOrProject)){const project=projectOrOptions;return writeProject(project,{expectedProjectRevision:Number(project?.revision||0)});}
  return writeProject(projectsOrProject,projectOrOptions||{});
}
function replaceProject(projectsOrProject,projectOrOptions,storage){if(Array.isArray(projectsOrProject)&&storage){const next=clone(projectsOrProject),project=projectOrOptions,id=projectIdentity(project),i=next.findIndex(x=>projectIdentity(x)===id);if(i<0)next.unshift(clone(project));else next[i]=clone(project);writeAllLegacy(next,storage);return next;}return replaceProjectIndexed(projectsOrProject,projectOrOptions);}

async function transactIndexed(projectsOrJobId,jobIdOrExpected,mutatorOrOptions){
  const jobId=String(projectsOrJobId||''),expected=Number(jobIdOrExpected);const mutator=mutatorOrOptions;if(typeof mutator!=='function')throw new TypeError('Transaction mutator must be a function.');const tx=await openTransaction([PROJECTS,META],'readwrite'),store=tx.objectStore(PROJECTS);try{const prior=await request(store.get(jobId));if(!prior)throw new Error(`Project ${jobId} is not available for transaction.`);const before=clone(prior.project),next=clone(prior.project);if(Number(prior.revision)!==expected){const replay=runSynchronousMutator(mutator,next,before),unchanged=projectSha256(next)===projectSha256(before);if(replay?.replayed===true&&replay.receipt&&unchanged){const receipt=safe(before.projectData?.commandReceipts).find(item=>String(item.receiptId||'')===String(replay.receipt.receiptId||''));if(!receipt||hash.sha256Value(receipt)!==hash.sha256Value(replay.receipt))throw storageError('A stale command retry did not resolve to its exact durable canonical receipt.','IDEMPOTENT_REPLAY_RECEIPT_MISMATCH');await complete(tx);const project=clone(prior.project);project.revision=Number(prior.revision);project.projectSha256=prior.projectSha256;return {project,result:replay,idempotent:true};}const e=new Error(`Project revision conflict for ${jobId}: expected ${expected}, found ${prior.revision}.`);e.code='STALE_PROJECT_REVISION';throw e;}const result=runSynchronousMutator(mutator,next,before),committedRevision=expected+1;const engine=globalThis.closedLoopWorkflowEngine;engine?.ensureShape?.(next);engine?.recalculate?.(next);next.revision=committedRevision;finalizeCommandReceiptsForCommit(next,before,expected,committedRevision);assertProjectIntegrity(next);const digest=projectSha256(next);store.put({jobId,revision:next.revision,project:next,projectSha256:digest,updatedAt:now()});tx.objectStore(META).put({key:'lastCommittedRevision',value:{jobId,revision:next.revision,projectSha256:digest},updatedAt:now()});await complete(tx);next.projectSha256=digest;return {project:next,result};}catch(error){try{tx.abort();}catch{}throw error;}
}
function transact(projectsOrJobId,jobIdOrExpected,mutatorOrOptions,storage){if(Array.isArray(projectsOrJobId)&&storage){const next=clone(projectsOrJobId),id=String(jobIdOrExpected||''),i=next.findIndex(x=>projectIdentity(x)===id);if(i<0)throw new Error(`Project ${id} is not available for transaction.`);const before=clone(next[i]),result=runSynchronousMutator(mutatorOrOptions,next[i],before);writeAllLegacy(next,storage);return {projects:next,project:next[i],result};}return transactIndexed(projectsOrJobId,jobIdOrExpected,mutatorOrOptions);}

async function reserveOperationPersistent({jobId,expectedProjectRevision,stage,operation,targetSlot,promptIdentity=null,scope={},tabInstanceId='UNKNOWN',packageDescriptor=null,commandId=null,idempotencyKey=null}={}){
  /* Reservation is one CAS commit; prompt registration is the immediately following
     CAS commit. Bind the reservation to the revision on which that saved prompt and
     its eventual package will be current, rather than to either intermediate state. */
  const owner=String(jobId||''),normalizedStage=Number(stage),normalizedOperation=String(operation||'COMPLETE'),normalizedTarget=String(targetSlot||''),normalizedPromptIdentity=String(promptIdentity?.instructionId||promptIdentity?.promptId||promptIdentity||'').trim(),promptProjectRevision=Number(expectedProjectRevision)+2,normalizedScope={...clone(scope||{}),projectRevision:promptProjectRevision},scopeSha256=hash.sha256Value(normalizedScope);if(!normalizedPromptIdentity)throw storageError('Persistent operation reservation requires the exact deterministic future instruction identity.','OPERATION_RESERVATION_PROMPT_IDENTITY_REQUIRED');
  let packageManifestHash='',descriptor=null;
  if(packageDescriptor){
    descriptor=normalizeExecutionPackageDescriptor({...clone(packageDescriptor),jobId:owner,stage:normalizedStage,operation:normalizedOperation,targetSlot:normalizedTarget,scopeSha256});
    packageManifestHash=hash.sha256Value(descriptor);
  }
  const packageId=packageManifestHash?`PACKAGE-${packageManifestHash.slice(0,24).toUpperCase()}`:`PACKAGE-${hash.sha256Value({owner,stage:normalizedStage,operation:normalizedOperation,targetSlot:normalizedTarget,scopeSha256,expectedProjectRevision}).slice(0,24).toUpperCase()}`;
  const payloadHash=hash.sha256Value({stage:normalizedStage,operation:normalizedOperation,targetSlot:normalizedTarget,promptIdentity,scope:normalizedScope,scopeSha256,packageId,packageManifestHash}),normalizedCommandId=String(commandId||`COMMAND-RESERVE-${payloadHash.slice(0,20).toUpperCase()}`),normalizedIdempotencyKey=String(idempotencyKey||hash.sha256Value({commandType:'RESERVE_OPERATION',target:normalizedTarget,expectedProjectRevision:Number(expectedProjectRevision),payloadHash}));
  return transact(owner,Number(expectedProjectRevision),(project)=>{const engine=globalThis.closedLoopWorkflowEngine;if(typeof engine?.reserveOperation!=='function')throw storageError('The workflow operation-reservation authority is unavailable.','OPERATION_RESERVATION_ENGINE_UNAVAILABLE');return engine.reserveOperation(project,{stage:normalizedStage,operation:normalizedOperation,targetSlot:normalizedTarget,promptIdentity:normalizedPromptIdentity,contextSignature:String(promptIdentity?.contextSignature||''),scope:normalizedScope,expectedRevision:Number(expectedProjectRevision),promptProjectRevision,packageId,packageManifestHash,packageDescriptor:descriptor,owningBrowserTabInstance:String(tabInstanceId||'UNKNOWN')});});
}
async function changeOperationReservationPersistent(action,{jobId,expectedProjectRevision,operationReservationId,tabInstanceId='UNKNOWN',reason='',commandId=null,idempotencyKey=null}={}){
  const owner=String(jobId||''),id=String(operationReservationId||''),engine=globalThis.closedLoopWorkflowEngine,methods={CANCEL:'cancelOperationReservation',ACCEPT:'acceptOperationReservation'},method=methods[action];if(!method||typeof engine?.[method]!=='function')throw storageError(`Operation reservation action ${action} is unavailable.`,'OPERATION_RESERVATION_ACTION_UNAVAILABLE');return transact(owner,Number(expectedProjectRevision),project=>engine[method](project,id,{owningBrowserTabInstance:String(tabInstanceId||'UNKNOWN'),reason:String(reason||''),expectedRevision:Number(expectedProjectRevision)}));
}
async function resumeOperationReservationPersistent({jobId,expectedProjectRevision,operationReservationId,tabInstanceId='UNKNOWN',reason='',stage,operation,targetSlot,promptIdentity=null,scope={},packageDescriptor=null,commandId=null,idempotencyKey=null}={}){
  const owner=String(jobId||''),id=String(operationReservationId||''),normalizedStage=Number(stage),normalizedOperation=String(operation||'').toUpperCase(),normalizedTarget=String(targetSlot||''),normalizedPromptIdentity=String(promptIdentity?.instructionId||promptIdentity?.promptId||promptIdentity||'').trim(),commandRevision=Number(expectedProjectRevision),promptProjectRevision=commandRevision+2,normalizedScope={...clone(scope||{}),projectRevision:promptProjectRevision},scopeSha256=hash.sha256Value(normalizedScope);if(!id)throw storageError('Operation reservation identity is required for resume.','OPERATION_RESERVATION_ID_REQUIRED');if(!normalizedPromptIdentity)throw storageError('Resuming an operation reservation requires the exact deterministic replacement instruction identity.','OPERATION_RESERVATION_PROMPT_IDENTITY_REQUIRED');
  let packageManifestHash='',descriptor=null;if(packageDescriptor){descriptor=normalizeExecutionPackageDescriptor({...clone(packageDescriptor),jobId:owner,stage:normalizedStage,operation:normalizedOperation,targetSlot:normalizedTarget,scopeSha256});packageManifestHash=hash.sha256Value(descriptor);}
  const packageId=packageManifestHash?`PACKAGE-${packageManifestHash.slice(0,24).toUpperCase()}`:`PACKAGE-${hash.sha256Value({owner,reservationId:id,stage:normalizedStage,operation:normalizedOperation,targetSlot:normalizedTarget,scopeSha256,commandRevision}).slice(0,24).toUpperCase()}`,payloadHash=hash.sha256Value({reservationId:id,stage:normalizedStage,operation:normalizedOperation,targetSlot:normalizedTarget,promptIdentity:normalizedPromptIdentity,scope:normalizedScope,scopeSha256,packageId,packageManifestHash,tabInstanceId:String(tabInstanceId||'UNKNOWN')}),normalizedCommandId=String(commandId||`COMMAND-RESUME-${payloadHash.slice(0,20).toUpperCase()}`),normalizedIdempotencyKey=String(idempotencyKey||hash.sha256Value({commandType:'RESUME_OPERATION_RESERVATION',target:id,expectedProjectRevision:commandRevision,payloadHash}));
  return transact(owner,commandRevision,project=>{const engine=globalThis.closedLoopWorkflowEngine;if(typeof engine?.resumeOperationReservation!=='function')throw storageError('The workflow operation-reservation resume authority is unavailable.','OPERATION_RESERVATION_ENGINE_UNAVAILABLE');return engine.resumeOperationReservation(project,id,{expectedRevision:commandRevision,promptProjectRevision,owningBrowserTabInstance:String(tabInstanceId||'UNKNOWN'),reason:String(reason||''),stage:normalizedStage,operation:normalizedOperation,targetSlot:normalizedTarget,promptIdentity:normalizedPromptIdentity,contextSignature:String(promptIdentity?.contextSignature||''),scope:normalizedScope,packageId,packageManifestHash,packageDescriptor:descriptor,commandId:normalizedCommandId,idempotencyKey:normalizedIdempotencyKey});});
}
const cancelOperationReservationPersistent=options=>changeOperationReservationPersistent('CANCEL',options);
const acceptOperationReservationPersistent=options=>changeOperationReservationPersistent('ACCEPT',options);
async function orphanOperationReservationsPersistent({jobId,expectedProjectRevision,tabInstanceId=null}={}){const owner=String(jobId||''),engine=globalThis.closedLoopWorkflowEngine;if(typeof engine?.orphanActiveOperationReservations!=='function')throw storageError('The operation-reservation orphaning authority is unavailable.','OPERATION_RESERVATION_ENGINE_UNAVAILABLE');return transact(owner,Number(expectedProjectRevision),project=>engine.orphanActiveOperationReservations(project,{owningBrowserTabInstance:tabInstanceId,expectedRevision:Number(expectedProjectRevision)}));}

async function removeProjectIndexed(projectsOrJobId,options={}){
  const jobId=String(projectsOrJobId||'').trim(),expected=options?.expectedProjectRevision,replacementSelectedProjectId=String(options?.replacementSelectedProjectId||'').trim(),suppressRetainedProject=Boolean(options?.suppressRetainedProject);
  if(!jobId)throw storageError('JOB_ID is required for project deletion.','PROJECT_DELETE_JOB_ID_REQUIRED');
  if(replacementSelectedProjectId===jobId)throw storageError('The deleted project cannot also be the replacement selected project.','INVALID_PROJECT_DELETE_REPLACEMENT');
  const tx=await openTransaction([PROJECTS,ARTIFACTS,STAGING,META],'readwrite'),projects=tx.objectStore(PROJECTS),artifacts=tx.objectStore(ARTIFACTS),staging=tx.objectStore(STAGING),meta=tx.objectStore(META);
  try{
    const prior=await request(projects.get(jobId));if(!prior){await complete(tx);return false;}
    if(expected!==undefined&&expected!==null&&Number(prior.revision)!==Number(expected)){const error=storageError(`Project revision conflict for ${jobId}: expected ${expected}, found ${prior.revision}.`,'STALE_PROJECT_REVISION');throw error;}
    if(replacementSelectedProjectId){const replacement=await request(projects.get(replacementSelectedProjectId));if(!replacement)throw storageError(`Replacement project ${replacementSelectedProjectId} is not stored.`,'PROJECT_DELETE_REPLACEMENT_MISSING');}
    const artifactRows=await request(artifacts.getAll()),stagingRows=await request(staging.getAll());projects.delete(jobId);for(const artifact of artifactRows)if(String(artifact.jobId)===jobId)artifacts.delete(artifact.artifactId);for(const artifact of stagingRows)if(String(artifact.jobId)===jobId)staging.delete(artifact.stagedArtifactId);
    const selected=await request(meta.get('selectedProject'));if(String(selected?.value||'')===jobId){if(replacementSelectedProjectId)meta.put({key:'selectedProject',value:replacementSelectedProjectId,updatedAt:now()});else meta.delete('selectedProject');}
    const projectUiRow=await request(meta.get('projectUi'));if(projectUiRow?.value&&typeof projectUiRow.value==='object'&&!Array.isArray(projectUiRow.value)&&Object.prototype.hasOwnProperty.call(projectUiRow.value,jobId)){const nextProjectUi=clone(projectUiRow.value);delete nextProjectUi[jobId];meta.put({key:'projectUi',value:nextProjectUi,updatedAt:now()});}
    if(suppressRetainedProject)meta.put({key:'retainedProjectSuppressed',value:{jobId,at:now()},updatedAt:now()});
    const lastCommitted=await request(meta.get('lastCommittedRevision'));if(String(lastCommitted?.value?.jobId||'')===jobId)meta.delete('lastCommittedRevision');for(const key of ['lastVerifiedExport:'+jobId,'lastArtifactVerification:'+jobId])meta.delete(key);
    fault('during-project-delete');await complete(tx);try{new BroadcastChannel('closed-loop-reliability').postMessage({type:'PROJECT_DELETED',jobId,replacementSelectedProjectId:replacementSelectedProjectId||null});}catch{}return true;
  }catch(error){try{tx.abort();}catch{}throw error;}
}
function removeProject(projectsOrJobId,jobIdOrStorage,storage){if(Array.isArray(projectsOrJobId)&&storage){const next=clone(projectsOrJobId).filter(project=>projectIdentity(project)!==String(jobIdOrStorage||''));writeAllLegacy(next,storage);return next;}return removeProjectIndexed(projectsOrJobId,jobIdOrStorage||{});}

async function stageArtifact({artifactId=null,jobId,blob,filename,mediaType,lineage={},role='RETURNED_ATTACHMENT',notes='',disclosureClassification:classification='UNKNOWN',commandId=null,idempotencyKey=null}={}){
  if(!(blob instanceof Blob))throw new TypeError('Artifact staging requires Blob bytes.');const owner=String(jobId||'').trim();if(!owner)throw storageError('jobId is required for artifact staging.','ARTIFACT_STAGE_JOB_REQUIRED');if(blob.size>STORE_LIMITS.maxArtifactBytes)throw storageError('Artifact exceeds the centralized per-artifact byte limit.','ARTIFACT_RESOURCE_LIMIT');await preflightStorage(blob.size);const disclosure=String(classification||'UNKNOWN').toUpperCase();if(!DISCLOSURE_VALUES.includes(disclosure))throw storageError(`Unknown disclosure classification ${classification}.`,'INVALID_DISCLOSURE_CLASSIFICATION');const name=canonicalFilename(filename||artifactId||'artifact.bin'),bytes=new Uint8Array(await blob.arrayBuffer()),sha256=await hash.sha256Bytes(bytes),canonicalArtifactId=String(artifactId||`ARTIFACT-${sha256.slice(0,20).toUpperCase()}`),normalizedCommandId=String(commandId||`COMMAND-STAGE-${sha256.slice(0,20).toUpperCase()}`),payloadSha256=hash.sha256Value({owner,canonicalArtifactId,sha256,byteSize:bytes.byteLength,canonicalPath:name.canonicalPath,lineage,role:String(role||'RETURNED_ATTACHMENT'),notes:String(notes||''),disclosureClassification:disclosure}),normalizedIdempotencyKey=String(idempotencyKey||hash.sha256Value({commandType:'STAGE_ARTIFACT',target:canonicalArtifactId,owner,payloadSha256})),stagedArtifactId=`STAGED-${hash.sha256Value({normalizedIdempotencyKey,payloadSha256}).slice(0,24).toUpperCase()}`;
  const tx=await openTransaction(STAGING,'readwrite'),store=tx.objectStore(STAGING);try{
    const rows=await request(store.getAll()),sameKeys=rows.filter(row=>String(row.jobId)===owner&&String(row.idempotencyKey)===normalizedIdempotencyKey),sameCommands=rows.filter(row=>String(row.jobId)===owner&&String(row.commandId)===normalizedCommandId);
    if(sameKeys.length>1||sameCommands.length>1)throw storageError('Artifact staging contains duplicate durable command or idempotency identities.','STAGING_IDEMPOTENCY_CORRUPTION');
    const prior=sameKeys[0]||sameCommands[0]||await request(store.get(stagedArtifactId));
    if(prior&&(String(prior.idempotencyKey)!==normalizedIdempotencyKey||String(prior.commandId)!==normalizedCommandId||String(prior.payloadSha256)!==payloadSha256))throw storageError('Staging command or idempotency identity was reused with different bytes or metadata.','IDEMPOTENCY_CONFLICT');
    if(!prior)store.put({stagedArtifactId,artifactId:canonicalArtifactId,jobId:owner,blob:new Blob([bytes],{type:String(mediaType||blob.type||'application/octet-stream')}),filename:name.rawFilename,displayFilename:name.displayFilename,canonicalPath:name.canonicalPath,filenameNormalizationVersion:name.filenameNormalizationVersion,collisionKeys:name.collisionKeys,mediaType:String(mediaType||blob.type||'application/octet-stream'),byteSize:bytes.byteLength,sha256,lineage:clone(lineage),role:String(role||'RETURNED_ATTACHMENT'),notes:String(notes||''),disclosureClassification:disclosure,status:'PENDING_BYTES',commandId:normalizedCommandId,idempotencyKey:normalizedIdempotencyKey,payloadSha256,createdAt:now()});
    await complete(tx);
  }catch(error){try{tx.abort();}catch{}throw error;}
  const readTx=await openTransaction(STAGING,'readonly'),readback=await request(readTx.objectStore(STAGING).get(stagedArtifactId));await complete(readTx);if(!readback)throw storageError('Staged artifact disappeared before read-back verification.','ARTIFACT_STAGE_READBACK_FAILED');const readBytes=new Uint8Array(await readback.blob.arrayBuffer()),readHash=await hash.sha256Bytes(readBytes);if(readBytes.byteLength!==bytes.byteLength||readHash!==sha256){const cleanup=await openTransaction(STAGING,'readwrite');cleanup.objectStore(STAGING).delete(stagedArtifactId);await complete(cleanup);throw storageError('Staged artifact failed byte read-back verification.','ARTIFACT_STAGE_READBACK_FAILED');}
  const readyTx=await openTransaction(STAGING,'readwrite'),readyStore=readyTx.objectStore(STAGING),current=await request(readyStore.get(stagedArtifactId));if(!current||current.payloadSha256!==payloadSha256){try{readyTx.abort();}catch{}throw storageError('Staged artifact changed before it became ready.','ARTIFACT_STAGE_CHANGED');}current.status='READY_FOR_PROMOTION';current.reverifiedAt=now();readyStore.put(current);await complete(readyTx);return current;
}
function promoteCanonicalArtifactMetadata(project,row){const engine=globalThis.closedLoopWorkflowEngine;if(!engine)throw storageError('The canonical artifact authority is unavailable.','ARTIFACT_METADATA_ENGINE_UNAVAILABLE');engine.ensureShape(project);const current=engine.records(project,'artifacts',{active:false}).find(record=>engine.recordId(record,'artifacts')===String(row.artifactId)),fields={ARTIFACT_ID:String(row.artifactId),FILENAME:String(row.filename),TYPE:String(row.mediaType||'application/octet-stream'),VERSION:String(current?engine.recordValue(current,'VERSION')||'ARTIFACT-v001':'ARTIFACT-v001'),BYTE_SIZE:Number(row.byteSize),SHA256:String(row.sha256),ROLE:String(row.role||'RETURNED_ATTACHMENT'),STORAGE_REFERENCE:'INDEXEDDB:artifacts',AVAILABILITY:'BYTES_PERSISTED_AND_VERIFIED',NOTES:String(row.notes||'Application-staged bytes were hashed, read back, rehashed, and atomically promoted with their canonical response.'),RAW_FILENAME:String(row.filename),DISPLAY_FILENAME:String(row.displayFilename||row.filename),CANONICAL_PATH:String(row.canonicalPath||row.filename),FILENAME_NORMALIZATION_VERSION:String(row.filenameNormalizationVersion||'closed-loop-filename/1'),COLLISION_KEYS:clone(row.collisionKeys||[]),AUTHORIZED_DELIVERY_FILENAME:null,HASH_ALGORITHM:'SHA-256',DECLARED_MEDIA_TYPE:String(row.mediaType||'application/octet-stream'),DETECTED_FORMAT:'UNDETERMINED',PARSER_OR_ADAPTER_IDENTITY:'NONE',FORMAT_AMBIGUITY_STATUS:'UNKNOWN',DISCLOSURE_CLASSIFICATION:String(row.disclosureClassification||'UNKNOWN')};if(current){const expectedSha=String(engine.recordValue(current,'SHA256')||''),expectedSize=Number(engine.recordValue(current,'BYTE_SIZE'));if((expectedSha&&expectedSha!==fields.SHA256)||(Number.isFinite(expectedSize)&&expectedSize>0&&expectedSize!==fields.BYTE_SIZE))throw storageError(`Canonical artifact ${row.artifactId} metadata does not match the staged bytes.`,'ARTIFACT_METADATA_MISMATCH');current.fields={...(current.fields||{}),...fields};Object.assign(current,fields,{active:true,invalidatedBy:null,validity:'CURRENT',updatedAt:now()});engine.refreshRecordHashes(current,'artifacts');return current;}const record={id:String(row.artifactId),stage:Number(project.activeStage||0)||null,active:true,createdAt:now(),scope:engine.currentScope(project),fields,...fields,lineage:clone(row.lineage||{}),source:'APPLICATION_STAGED_ATTACHMENT_PROMOTION'};engine.refreshRecordHashes(record,'artifacts');project.projectData.artifacts.push(record);return record;}
async function promoteStagedAttachmentsAtomic({jobId,expectedProjectRevision,stagedArtifactIds=[],mutateProject=null,commandContext={},commandId,idempotencyKey}={}){
  const owner=String(jobId||''),ids=[...new Set(stagedArtifactIds.map(String).filter(Boolean))].sort();
  if(!owner||!ids.length)throw storageError('jobId and stagedArtifactIds are required for atomic promotion.','ARTIFACT_PROMOTION_INPUT_REQUIRED');
  if(typeof mutateProject!=='function')throw storageError('Atomic promotion requires one synchronous canonical project mutator.','ARTIFACT_PROMOTION_MUTATOR_REQUIRED');
  const commandRequestHash=hash.sha256Value({jobId:owner,expectedProjectRevision:Number(expectedProjectRevision),stagedArtifactIds:ids,commandContext:clone(commandContext||{})}),normalizedCommandId=String(commandId||`COMMAND-PROMOTE-${commandRequestHash.slice(0,20).toUpperCase()}`),normalizedIdempotencyKey=String(idempotencyKey||hash.sha256Value({commandType:'PROMOTE_STAGED_ATTACHMENTS',target:owner,expectedProjectRevision:Number(expectedProjectRevision),commandRequestHash}));
  const replayResult=(stored,receipt)=>{const next=clone(stored.project);next.projectSha256=stored.projectSha256;return {project:next,result:clone(receipt.resultSnapshot),receipt:clone(receipt),idempotent:true,artifacts:clone(receipt.resultingArtifacts||[])};};
  const findReceipt=project=>{const receipts=Array.isArray(project?.projectData?.commandReceipts)?project.projectData.commandReceipts:[],byKey=receipts.filter(receipt=>String(receipt.idempotencyKey)===normalizedIdempotencyKey),byCommand=receipts.filter(receipt=>String(receipt.commandId)===normalizedCommandId);if(byKey.length>1||byCommand.length>1)throw storageError('Canonical command receipts contain duplicate promotion command or idempotency identities.','IDEMPOTENCY_RECEIPT_CORRUPTION');const receipt=byKey[0]||byCommand[0]||null;if(receipt&&(String(receipt.commandType)!=='PROMOTE_STAGED_ATTACHMENTS'||String(receipt.target)!==owner||String(receipt.commandRequestHash||'')!==commandRequestHash||String(receipt.idempotencyKey)!==normalizedIdempotencyKey||String(receipt.commandId)!==normalizedCommandId))throw storageError('Promotion command or idempotency identity was reused with different content.','IDEMPOTENCY_CONFLICT');return receipt;};

  /* Exact retries are resolved from the durable canonical receipt before the
     staging store is consulted; successfully promoted staging rows no longer exist. */
  const receiptTx=await openTransaction(PROJECTS,'readonly'),storedBefore=await request(receiptTx.objectStore(PROJECTS).get(owner));await complete(receiptTx);
  if(!storedBefore)throw storageError(`Project ${owner} is not stored.`,'ARTIFACT_PROMOTION_PROJECT_MISSING');
  const replay=findReceipt(storedBefore.project);if(replay)return replayResult(storedBefore,replay);
  if(Number(storedBefore.revision)!==Number(expectedProjectRevision))throw storageError(`Project revision conflict for ${owner}: expected ${expectedProjectRevision}, found ${storedBefore.revision}.`,'STALE_PROJECT_REVISION');

  const staged=[];
  for(const id of ids){const stageTx=await openTransaction(STAGING,'readonly'),row=await request(stageTx.objectStore(STAGING).get(id));await complete(stageTx);if(!row||String(row.jobId)!==owner||row.status!=='READY_FOR_PROMOTION')throw storageError(`Staged artifact ${id} is unavailable or not ready for promotion.`,'ARTIFACT_STAGE_NOT_READY');const bytes=new Uint8Array(await row.blob.arrayBuffer()),digest=await hash.sha256Bytes(bytes);if(bytes.byteLength!==Number(row.byteSize)||digest!==String(row.sha256))throw storageError(`Staged artifact ${id} failed precommit re-verification.`,'ARTIFACT_STAGE_READBACK_FAILED');staged.push(row);}
  assertNoFilenameCollisions(staged.map(row=>({artifactId:row.artifactId,filename:row.filename})));
  const normalizedPayloadHash=hash.sha256Value({commandRequestHash,stagedArtifacts:staged.map(row=>({stagedArtifactId:row.stagedArtifactId,artifactId:row.artifactId,sha256:row.sha256,byteSize:row.byteSize,canonicalPath:row.canonicalPath,payloadSha256:row.payloadSha256}))});
  const tx=await openTransaction([PROJECTS,ARTIFACTS,STAGING,META],'readwrite'),projects=tx.objectStore(PROJECTS),artifacts=tx.objectStore(ARTIFACTS),staging=tx.objectStore(STAGING);
  try{
    const prior=await request(projects.get(owner));if(!prior)throw storageError(`Project ${owner} is not stored.`,'ARTIFACT_PROMOTION_PROJECT_MISSING');
    const racedReplay=findReceipt(prior.project);if(racedReplay){await complete(tx);return replayResult(prior,racedReplay);}
    if(Number(prior.revision)!==Number(expectedProjectRevision))throw storageError(`Project revision conflict for ${owner}: expected ${expectedProjectRevision}, found ${prior.revision}.`,'STALE_PROJECT_REVISION');
    const next=clone(prior.project);next.projectData=next.projectData||{};next.projectData.commandReceipts=Array.isArray(next.projectData.commandReceipts)?next.projectData.commandReceipts:[];
    for(const row of staged){const current=await request(staging.get(row.stagedArtifactId));if(!current||current.status!=='READY_FOR_PROMOTION'||current.payloadSha256!==row.payloadSha256)throw storageError(`Staged artifact ${row.stagedArtifactId} changed before atomic promotion.`,'ARTIFACT_STAGE_CHANGED');const existing=await request(artifacts.get(row.artifactId));if(existing&&(String(existing.jobId)!==owner||String(existing.sha256)!==String(row.sha256)||Number(existing.byteSize)!==Number(row.byteSize)))throw storageError(`Artifact identity ${row.artifactId} cannot be reused for different bytes or project.`,'ARTIFACT_ID_REUSE');artifacts.put({...clone(row),stagedArtifactId:undefined,status:'CANONICAL',promotedAt:now()});staging.delete(row.stagedArtifactId);promoteCanonicalArtifactMetadata(next,row);}
    const result=runSynchronousMutator(mutateProject,next,clone(prior.project)),createdAt=now(),receipt={receiptId:`COMMAND-RECEIPT-PROMOTE-${hash.sha256Value({owner,normalizedCommandId,normalizedIdempotencyKey,commandRequestHash}).slice(0,20).toUpperCase()}`,commandId:normalizedCommandId,idempotencyKey:normalizedIdempotencyKey,commandType:'PROMOTE_STAGED_ATTACHMENTS',target:owner,expectedRevision:Number(expectedProjectRevision),committedRevision:Number(expectedProjectRevision)+1,commandRequestHash,normalizedPayloadHash,resultSnapshot:clone(result),resultingArtifacts:staged.map(row=>({artifactId:row.artifactId,filename:row.filename,byteSize:row.byteSize,sha256:row.sha256})),deviceTimestamp:createdAt,createdAt,status:'COMMITTED'};
    next.projectData.commandReceipts.push(receipt);next.revision=Number(expectedProjectRevision)+1;const engine=globalThis.closedLoopWorkflowEngine;engine?.ensureShape?.(next);engine?.recalculate?.(next);delete next.projectSha256;assertProjectResourceLimits(next);assertProjectIntegrity(next);const digest=projectSha256(next);projects.put({jobId:owner,revision:next.revision,project:next,projectSha256:digest,updatedAt:now()});tx.objectStore(META).put({key:'lastCommittedRevision',value:{jobId:owner,revision:next.revision,projectSha256:digest},updatedAt:now()});await complete(tx);next.projectSha256=digest;return {project:next,result,receipt,artifacts:receipt.resultingArtifacts,idempotent:false};
  }catch(error){try{tx.abort();}catch{}throw error;}
}
async function commitResponseWithStagedAttachments({jobId,expectedProjectRevision,proposalId,stagedArtifactIds=[],operator='HUMAN_OPERATOR',reviewNote='Accepted after operator review.',commandId=null,idempotencyKey=null}={}){const ingestion=globalThis.closedLoopResponseIngestion;if(typeof ingestion?.commit!=='function')throw storageError('The one canonical response-ingestion authority is unavailable.','RESPONSE_INGESTION_UNAVAILABLE');return promoteStagedAttachmentsAtomic({jobId,expectedProjectRevision,stagedArtifactIds,commandId,idempotencyKey,commandContext:{proposalId:String(proposalId||''),operator:String(operator||''),reviewNote:String(reviewNote||'')},mutateProject:project=>{const outcome=ingestion.commit(project,String(proposalId||''),{operator,reviewNote}),committed=outcome.project;if(!committed||typeof committed!=='object')throw storageError('Response ingestion did not return a complete canonical project.','RESPONSE_COMMIT_FAILED');for(const key of Object.keys(project))delete project[key];Object.assign(project,clone(committed));return {proposalId:String(proposalId||''),acceptedChangeId:outcome.acceptedChange?.changeId||null,dispositionId:outcome.disposition?.dispositionId||null,manifestId:outcome.manifest?.manifestId||null,receiptId:outcome.receipt?.receiptId||null,idempotent:Boolean(outcome.idempotent)};}});}
async function recoverArtifactLifecycle(){
  const projects=await readAll(),projectById=new Map(projects.map(project=>[projectIdentity(project),project])),artifactTx=await openTransaction(ARTIFACTS,'readonly'),artifacts=await request(artifactTx.objectStore(ARTIFACTS).getAll());await complete(artifactTx);const stageTx=await openTransaction(STAGING,'readonly'),staged=await request(stageTx.objectStore(STAGING).getAll());await complete(stageTx);const canonicalById=new Map();for(const project of projects)for(const record of (project.projectData?.artifacts||[]).filter(item=>item?.active!==false&&!item?.invalidatedBy)){const id=String(globalThis.closedLoopWorkflowEngine?.recordId?.(record,'artifacts')||'');if(id)canonicalById.set(id,{project,record});}const findings=[];for(const row of artifacts)if(!canonicalById.has(String(row.artifactId)))findings.push({kind:'ORPHAN_CANONICAL_BLOB',artifactId:row.artifactId,jobId:row.jobId});for(const [artifactId,{project}] of canonicalById)if(!artifacts.some(row=>String(row.artifactId)===artifactId))findings.push({kind:'CANONICAL_METADATA_WITHOUT_BYTES',artifactId,jobId:projectIdentity(project)});for(const row of staged)findings.push({kind:row.status==='READY_FOR_PROMOTION'?'ORPHAN_READY_STAGING':'INTERRUPTED_STAGING',stagedArtifactId:row.stagedArtifactId,artifactId:row.artifactId,jobId:row.jobId,status:row.status});const report={schema:'closed-loop-artifact-recovery/1',projects:projectById.size,canonicalBlobCount:artifacts.length,stagedBlobCount:staged.length,findings,at:now()};await metaPut('lastArtifactRecovery',report);return report;
}

async function putArtifact({artifactId,jobId,blob,filename,mediaType,lineage={}}){
  if(!artifactId||!jobId)throw new Error('artifactId and jobId are required.');const staged=await stageArtifact({artifactId,jobId,blob,filename,mediaType,lineage});fault('during-artifact-blob-write');const tx=await openTransaction([ARTIFACTS,STAGING],'readwrite'),canonical=tx.objectStore(ARTIFACTS),staging=tx.objectStore(STAGING);try{const current=await request(staging.get(staged.stagedArtifactId));if(!current||current.status!=='READY_FOR_PROMOTION'||current.payloadSha256!==staged.payloadSha256)throw storageError('Staged artifact is not ready for canonical promotion.','ARTIFACT_STAGE_NOT_READY');const prior=await request(canonical.get(String(artifactId)));if(prior){if(String(prior.jobId)!==String(jobId)||String(prior.sha256)!==String(current.sha256)||Number(prior.byteSize)!==Number(current.byteSize))throw storageError(`Artifact identity ${artifactId} cannot be reused for different bytes or project.`,'ARTIFACT_ID_REUSE');staging.delete(current.stagedArtifactId);await complete(tx);const priorDigest=await hash.sha256Bytes(await prior.blob.arrayBuffer());if(priorDigest!==String(prior.sha256))throw storageError(`Artifact identity ${artifactId} failed read-back verification.`,'ARTIFACT_INTEGRITY_MISMATCH');return prior;}canonical.put({artifactId:String(artifactId),jobId:String(jobId),blob:current.blob,filename:current.filename,displayFilename:current.displayFilename,canonicalPath:current.canonicalPath,filenameNormalizationVersion:current.filenameNormalizationVersion,collisionKeys:current.collisionKeys,mediaType:current.mediaType,byteSize:current.byteSize,sha256:current.sha256,lineage:clone(current.lineage),status:'CANONICAL',createdAt:current.createdAt,promotedAt:now()});staging.delete(current.stagedArtifactId);await complete(tx);}catch(error){try{tx.abort();}catch{}throw error;}const verified=await getArtifact(artifactId);if(!verified||String(verified.jobId)!==String(jobId)||await hash.sha256Bytes(await verified.blob.arrayBuffer())!==String(staged.sha256))throw storageError('Artifact byte read-back verification failed after canonical promotion.','ARTIFACT_INTEGRITY_MISMATCH');return verified;
}
async function getArtifact(artifactId){const tx=await openTransaction(ARTIFACTS,'readonly'),row=await request(tx.objectStore(ARTIFACTS).get(String(artifactId)));await complete(tx);return row||null;}
async function deleteArtifact(artifactId,jobId){const id=String(artifactId),owner=String(jobId||''),tx=await openTransaction(ARTIFACTS,'readwrite'),store=tx.objectStore(ARTIFACTS);try{const row=await request(store.get(id));if(!row){await complete(tx);return false;}if(owner&&String(row.jobId)!==owner)throw storageError(`Artifact ${id} belongs to another project and was not deleted.`,'CROSS_PROJECT_ARTIFACT_DELETE');store.delete(id);await complete(tx);return true;}catch(error){try{tx.abort();}catch{}throw error;}}
async function listArtifacts(jobId){const tx=await openTransaction(ARTIFACTS,'readonly'),rows=await request(tx.objectStore(ARTIFACTS).getAll());await complete(tx);return rows.filter(r=>r.jobId===String(jobId));}
async function verifyProjectArtifacts(jobId){
  const id=String(jobId||'').trim();if(!id)throw storageError('JOB_ID is required for artifact verification.','ARTIFACT_VERIFY_JOB_ID_REQUIRED');const projects=await readAll(),project=projects.find(item=>projectIdentity(item)===id);if(!project)throw storageError(`Project ${id} is not stored.`,'ARTIFACT_VERIFY_PROJECT_MISSING');const rows=await listArtifacts(id),rowById=new Map(rows.map(row=>[String(row.artifactId),row])),engine=globalThis.closedLoopWorkflowEngine,expected=engine.records(project,'artifacts').filter(record=>engine.isActiveRecord(record)&&String(engine.recordValue(record,'AVAILABILITY')||'').toUpperCase()==='BYTES_PERSISTED_AND_VERIFIED'),expectedById=new Map(expected.map(record=>[engine.recordId(record,'artifacts'),record])),results=[];let byteSize=0;
  for(const record of expected){const artifactId=engine.recordId(record,'artifacts'),row=rowById.get(artifactId),canonicalFilename=String(engine.recordValue(record,'FILENAME')||''),canonicalSize=Number(engine.recordValue(record,'BYTE_SIZE')),canonicalSha256=String(engine.recordValue(record,'SHA256')||'');if(!row){results.push({artifactId,filename:canonicalFilename,storedByteSize:null,actualByteSize:null,storedSha256:null,actualSha256:null,verified:false,issue:'MISSING_STORED_BLOB'});continue;}const bytes=new Uint8Array(await row.blob.arrayBuffer()),actualSize=bytes.byteLength,actualSha256=await hash.sha256Bytes(bytes);byteSize+=actualSize;const verified=String(row.jobId)===id&&String(row.filename)===canonicalFilename&&Number(row.byteSize)===canonicalSize&&String(row.sha256)===canonicalSha256&&actualSize===canonicalSize&&actualSha256===canonicalSha256;results.push({artifactId,filename:canonicalFilename,storedByteSize:Number(row.byteSize),actualByteSize:actualSize,storedSha256:String(row.sha256||''),actualSha256,verified,issue:verified?null:'CANONICAL_BLOB_IDENTITY_MISMATCH'});}
  for(const row of rows)if(!expectedById.has(String(row.artifactId))){const bytes=new Uint8Array(await row.blob.arrayBuffer()),actualSize=bytes.byteLength,actualSha256=await hash.sha256Bytes(bytes);byteSize+=actualSize;const verified=actualSize===Number(row.byteSize)&&actualSha256===String(row.sha256);results.push({artifactId:String(row.artifactId),filename:String(row.filename||row.artifactId),storedByteSize:Number(row.byteSize),actualByteSize:actualSize,storedSha256:String(row.sha256||''),actualSha256,verified,issue:verified?null:'UNREFERENCED_STORED_BLOB_MISMATCH',canonicalExpectation:false});}
  const mismatches=results.filter(item=>!item.verified),report={jobId:id,artifactCount:rows.length,expectedCanonicalArtifactCount:expected.length,byteSize,verifiedCount:results.length-mismatches.length,mismatchCount:mismatches.length,verified:mismatches.length===0,artifacts:results,at:now()};await metaPut('lastArtifactVerification:'+id,{jobId:id,artifactCount:report.artifactCount,expectedCanonicalArtifactCount:report.expectedCanonicalArtifactCount,byteSize:report.byteSize,mismatchCount:report.mismatchCount,verified:report.verified,at:report.at});return report;
}

function holdIndexedTransactionOpen(metaStore){let active=true;const pump=()=>{if(!active)return;let pending;try{pending=metaStore.get('__closed_loop_delivery_transaction_keepalive__');}catch{return;}pending.onsuccess=pending.onerror=()=>{if(active)pump();};};pump();return ()=>{active=false;};}
function sameStringSet(left,right){const normalize=value=>[...new Set((value||[]).map(String).filter(Boolean))].sort();return hash.sha256Value(normalize(left))===hash.sha256Value(normalize(right));}
function deliveryArtifactExpectations(project,state,engine){
  const artifactIds=[...new Set((state?.artifactIds||[]).map(String).filter(Boolean))].sort();if(!artifactIds.length||artifactIds.length!==(state?.artifactIds||[]).length)throw storageError('Final delivery requires one nonduplicate exact authorized artifact set.','DELIVERY_ARTIFACT_SET_INVALID');const identities=engine.recordsForCurrentScope(project,'artifactIdentities'),identityIds=identities.map(item=>String(engine.recordValue(item,'ARTIFACT_ID')||item.relationships?.ARTIFACT_ID||'')).filter(Boolean);if(!sameStringSet(artifactIds,identityIds)||identities.length!==artifactIds.length)throw storageError('The current Stage 28 identity records do not match the exact terminal authorized artifact set.','DELIVERY_ARTIFACT_SET_MISMATCH');
  const artifacts=engine.recordsForCurrentScope(project,'artifacts'),artifactById=new Map(artifacts.map(item=>[engine.recordId(item,'artifacts'),item])),identityById=new Map(identities.map(item=>[String(engine.recordValue(item,'ARTIFACT_ID')||item.relationships?.ARTIFACT_ID||''),item])),products=engine.recordsForCurrentScope(project,'products'),product=products.find(item=>engine.recordId(item,'products')===String(state.productId||'')),declaredInventory=(engine.recordValue(product,'GENERATED_ARTIFACT_INVENTORY')||[]).map(item=>typeof item==='string'?item:String(item?.artifactId||item?.ARTIFACT_ID||'')).filter(Boolean),productScopedIds=artifacts.filter(item=>String(item.scope?.productId||engine.recordValue(item,'PRODUCT_ID')||item.relationships?.PRODUCT_ID||'')===String(state.productId||'')).map(item=>engine.recordId(item,'artifacts')),inventory=declaredInventory.length?declaredInventory:productScopedIds;if(!product||!inventory.length||!sameStringSet(artifactIds,inventory))throw storageError('The authorized delivery artifact set is not the exact current finished-product inventory.','DELIVERY_PRODUCT_ARTIFACT_SET_MISMATCH');
  return artifactIds.map(artifactId=>{const artifact=artifactById.get(artifactId),identity=identityById.get(artifactId);if(!artifact||!identity)throw storageError(`Authorized delivery artifact ${artifactId} is not current canonical state.`,'DELIVERY_ARTIFACT_STALE');const filename=String(engine.recordValue(artifact,'FILENAME')||''),byteSize=Number(engine.recordValue(artifact,'BYTE_SIZE')),sha256=String(engine.recordValue(artifact,'SHA256')||'').toLowerCase(),availability=String(engine.recordValue(artifact,'AVAILABILITY')||'').toUpperCase(),identityFilename=String(engine.recordValue(identity,'RELEASE_FILENAME')||engine.recordValue(identity,'AUDITED_FILENAME')||''),identitySize=Number(engine.recordValue(identity,'RELEASE_BYTE_SIZE')??engine.recordValue(identity,'AUDITED_BYTE_SIZE')),identitySha256=String(engine.recordValue(identity,'PRE_DELIVERY_SHA256')||engine.recordValue(identity,'AUDITED_SHA256')||'').toLowerCase();if(!filename||!Number.isSafeInteger(byteSize)||byteSize<0||!/^[a-f0-9]{64}$/.test(sha256)||availability!=='BYTES_PERSISTED_AND_VERIFIED'||filename!==identityFilename||byteSize!==identitySize||sha256!==identitySha256)throw storageError(`Authorized delivery artifact ${artifactId} has stale or contradictory canonical identity metadata.`,'DELIVERY_ARTIFACT_IDENTITY_MISMATCH');return {artifactId,filename,byteSize,sha256,artifactRecordSha256:String(artifact.recordSha256||artifact.sha256||''),identityRecordId:engine.recordId(identity,'artifactIdentities'),identityRecordSha256:String(identity.recordSha256||identity.sha256||'')};});
}
async function createDeliveryRecordPersistent({jobId,expectedProjectRevision,commandId=null,idempotencyKey=null}={}){
  const owner=String(jobId||'').trim(),expected=Number(expectedProjectRevision),engine=globalThis.closedLoopWorkflowEngine;if(!owner||!Number.isSafeInteger(expected)||expected<0)throw storageError('Final delivery creation requires a JOB_ID and current expected project revision.','DELIVERY_COMMAND_INPUT_REQUIRED');if(typeof engine?.terminalDeliveryState!=='function'||typeof engine?.recordDelivery!=='function')throw storageError('The terminal delivery authority is unavailable.','DELIVERY_ENGINE_UNAVAILABLE');const tx=await openTransaction([PROJECTS,ARTIFACTS,META],'readwrite'),projects=tx.objectStore(PROJECTS),artifacts=tx.objectStore(ARTIFACTS),meta=tx.objectStore(META),releaseKeepalive=holdIndexedTransactionOpen(meta);
  try{
    const prior=await request(projects.get(owner));if(!prior)throw storageError(`Project ${owner} is not stored.`,'DELIVERY_PROJECT_MISSING');const before=clone(prior.project),next=clone(prior.project),currentRevision=Number(prior.revision),receipts=safe(before.projectData?.commandReceipts),explicitMatches=receipts.filter(receipt=>(commandId&&String(receipt.commandId||'')===String(commandId))||(idempotencyKey&&String(receipt.idempotencyKey||'')===String(idempotencyKey))),implicitMatches=!commandId&&!idempotencyKey?receipts.filter(receipt=>String(receipt.commandType||'')==='CREATE_DELIVERY_RECORD'&&String(receipt.target||'')===owner&&Number(receipt.expectedRevision)===expected):[],receiptMatches=explicitMatches.length?explicitMatches:implicitMatches;if(receiptMatches.length>1)throw storageError('More than one durable receipt claims the same final-delivery command identity.','DELIVERY_IDEMPOTENCY_RECEIPT_CONFLICT');const replayReceipt=receiptMatches[0]||null;if(!replayReceipt&&currentRevision!==expected)throw storageError(`Project revision conflict for ${owner}: expected ${expected}, found ${currentRevision}.`,'STALE_PROJECT_REVISION');const state=engine.terminalDeliveryState(next,{forCreation:!replayReceipt});if(!replayReceipt&&!state.eligibleToCreate)throw storageError('Final delivery is blocked: '+state.reasons.join(' '),'DELIVERY_PREREQUISITES_BLOCKED');if(replayReceipt&&String(replayReceipt.commandType||'')!=='CREATE_DELIVERY_RECORD')throw storageError('Command or idempotency identity was reused for a different command.','DELIVERY_IDEMPOTENCY_CONTENT_CONFLICT');const expectedArtifacts=deliveryArtifactExpectations(next,state,engine),verified=[];
    for(const expectation of expectedArtifacts){const row=await request(artifacts.get(expectation.artifactId));if(!row||String(row.artifactId)!==expectation.artifactId||String(row.jobId)!==owner||String(row.status||'').toUpperCase()!=='CANONICAL')throw storageError(`Authorized artifact ${expectation.artifactId} has no exact canonical Blob in this project.`,'DELIVERY_ARTIFACT_BYTES_MISSING');if(!(row.blob instanceof Blob))throw storageError(`Authorized artifact ${expectation.artifactId} does not contain stored Blob bytes.`,'DELIVERY_ARTIFACT_BYTES_MISSING');const bytes=new Uint8Array(await row.blob.arrayBuffer()),actualSha256=await hash.sha256Bytes(bytes);if(String(row.filename||'')!==expectation.filename||Number(row.byteSize)!==expectation.byteSize||String(row.sha256||'').toLowerCase()!==expectation.sha256||bytes.byteLength!==expectation.byteSize||actualSha256!==expectation.sha256)throw storageError(`Authorized artifact ${expectation.artifactId} failed immediate filename, size, or SHA-256 read-back verification.`,'DELIVERY_ARTIFACT_READBACK_MISMATCH');verified.push({...expectation,actualByteSize:bytes.byteLength,actualSha256,storageStatus:'CANONICAL_READ_BACK_AND_REHASHED'});}
    if(!sameStringSet(verified.map(item=>item.artifactId),state.artifactIds)||verified.length!==state.artifactIds.length)throw storageError('The byte read-back proof does not cover the exact current authorized artifact set.','DELIVERY_ARTIFACT_SET_MISMATCH');const artifactSetSha256=hash.sha256Value(verified),proof={schema:'closed-loop-delivery-byte-readback/1',jobId:owner,commandExpectedRevision:expected,terminalEvidenceHash:String(state.terminalEvidenceHash||''),artifactIdentityIds:[...state.artifactIdentityIds].map(String).sort(),artifacts:verified,artifactSetSha256},basis={commandType:'CREATE_DELIVERY_RECORD',target:owner,expectedRevision:expected,terminalEvidenceHash:proof.terminalEvidenceHash,artifactSetSha256},normalizedCommandId=String(commandId||`COMMAND-CREATE-DELIVERY-${hash.sha256Value(basis).slice(0,20).toUpperCase()}`),normalizedIdempotencyKey=String(idempotencyKey||hash.sha256Value(basis)),proofSha256=hash.sha256Value(proof),commandPayloadHash=hash.sha256Value({terminalEvidenceHash:proof.terminalEvidenceHash,expectedRevision:expected,artifactIds:[...state.artifactIds].sort(),artifactReadBackProof:proof});
    if(replayReceipt){const replayProject=clone(prior.project),result=engine.records(replayProject,'deliveryRecords',{active:false}).find(item=>engine.recordId(item,'deliveryRecords')===String(replayReceipt.resultIdentity||''));if(String(replayReceipt.commandId||'')!==normalizedCommandId||String(replayReceipt.idempotencyKey||'')!==normalizedIdempotencyKey||String(replayReceipt.target||'')!==owner||Number(replayReceipt.expectedRevision)!==expected||Number(replayReceipt.committedRevision)!==expected+1||Number(replayReceipt.committedRevision)>currentRevision||String(replayReceipt.normalizedPayloadHash||'')!==commandPayloadHash||String(replayReceipt.artifactReadBackProofSha256||'')!==proofSha256||!result||String(engine.recordValue(result,'TERMINAL_EVIDENCE_HASH')||'')!==proof.terminalEvidenceHash)throw storageError('An existing delivery command does not match the exact current byte-readback proof, command content, or canonical result.','DELIVERY_IDEMPOTENCY_CONTENT_CONFLICT');releaseKeepalive();await complete(tx);replayProject.revision=currentRevision;replayProject.projectSha256=prior.projectSha256;return {project:replayProject,result,receipt:clone(replayReceipt),readBackProof:proof,idempotent:true};}
    const priorReceiptIds=new Set((before.projectData?.commandReceipts||[]).map(receipt=>String(receipt.receiptId||''))),result=engine.recordDelivery(next,{expectedRevision:expected,committedRevision:expected+1,artifactReadBackProof:proof,commandId:normalizedCommandId,idempotencyKey:normalizedIdempotencyKey}),createdReceipts=(next.projectData?.commandReceipts||[]).filter(receipt=>!priorReceiptIds.has(String(receipt.receiptId||'')));
    if(createdReceipts.length!==1||Number(prior.revision)!==expected)throw storageError('Final delivery command could not bind one new receipt to the current canonical revision.','DELIVERY_COMMAND_REVISION_CONFLICT');const committedRevision=expected+1;next.revision=committedRevision;finalizeCommandReceiptsForCommit(next,before,expected,committedRevision);engine.ensureShape(next);engine.recalculate(next);assertProjectIntegrity(next);const digest=projectSha256(next);fault('during-delivery-record-write');projects.put({jobId:owner,revision:committedRevision,project:next,projectSha256:digest,updatedAt:now()});meta.put({key:'lastCommittedRevision',value:{jobId:owner,revision:committedRevision,projectSha256:digest},updatedAt:now()});releaseKeepalive();await complete(tx);next.projectSha256=digest;return {project:next,result,receipt:clone(createdReceipts[0]),readBackProof:proof,idempotent:false};
  }catch(error){releaseKeepalive();try{tx.abort();}catch{}throw error;}
}

const bytesToBase64=bytes=>{let s='';for(let i=0;i<bytes.length;i+=0x8000)s+=String.fromCharCode(...bytes.subarray(i,i+0x8000));return btoa(s);};
const base64ToBytes=text=>{const s=atob(text),out=new Uint8Array(s.length);for(let i=0;i<s.length;i++)out[i]=s.charCodeAt(i);return out;};
async function compressBytes(bytes){if(typeof CompressionStream!=='function')throw Object.assign(new Error('CompressionStream is required for complete package export.'),{code:'COMPRESSION_STREAM_REQUIRED'});const stream=new Blob([bytes]).stream().pipeThrough(new CompressionStream('gzip'));return new Uint8Array(await new Response(stream).arrayBuffer());}
async function decompressBytes(bytes,maxExpandedBytes=STORE_LIMITS.maxImportExpandedBytes){
  if(typeof DecompressionStream!=='function')throw Object.assign(new Error('DecompressionStream is required for complete package import.'),{code:'DECOMPRESSION_STREAM_REQUIRED'});
  const reader=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip')).getReader(),chunks=[];let total=0;
  try{
    while(true){const {done,value}=await reader.read();if(done)break;const chunk=value instanceof Uint8Array?value:new Uint8Array(value);total+=chunk.byteLength;if(total>Number(maxExpandedBytes)){try{await reader.cancel('expanded-size limit exceeded');}catch{}throw storageError('Import package exceeds the centralized expanded-size limit.','IMPORT_RESOURCE_LIMIT');}chunks.push(chunk);}
  }catch(error){try{await reader.cancel(error);}catch{}throw error;}
  const expanded=new Uint8Array(total);let offset=0;for(const chunk of chunks){expanded.set(chunk,offset);offset+=chunk.byteLength;}return expanded;
}
function backupMilestoneFor(project,requested='MANUAL'){
  const value=String(requested||'MANUAL').toUpperCase();if(value!=='AUTO')return ['BASELINE_APPROVAL','FINAL_DELIVERY','MANUAL'].includes(value)?value:'MANUAL';const stage=Number(String(project?.job?.CURRENT_STAGE||'').match(/\d+/)?.[0]||0);return stage===20?'BASELINE_APPROVAL':stage===30?'FINAL_DELIVERY':'MANUAL';
}
function currentBackupDisclosureDecision(project){
  const engine=globalThis.closedLoopWorkflowEngine,sensitive=engine?.evaluateBackupDisclosure?.(project,{})?.sensitiveArtifactIds||[];if(!sensitive.length)return null;for(const decision of [...(project?.projectData?.disclosureDecisions||[])].reverse()){const id=String(decision?.decisionId||'');if(id&&engine.evaluateBackupDisclosure(project,{humanAuthorizationId:id}).allowed)return decision;}return null;
}
function evaluateBackupExportDisclosure(project,{encryptionContractId=null,humanAuthorizationId=null}={}){
  const engine=globalThis.closedLoopWorkflowEngine;if(typeof engine?.evaluateBackupDisclosure!=='function')throw storageError('The backup disclosure authority is unavailable.','BACKUP_DISCLOSURE_AUTHORITY_UNAVAILABLE');const currentDecision=humanAuthorizationId?null:currentBackupDisclosureDecision(project);return engine.evaluateBackupDisclosure(project,{encryptionContractId,humanAuthorizationId:humanAuthorizationId||currentDecision?.decisionId||null});
}
async function verifiedBackupArtifactEntries(project,artifactRows){
  const engine=globalThis.closedLoopWorkflowEngine,canonical=engine.backupArtifactManifest(project),canonicalById=new Map(canonical.map(item=>[item.artifactId,item])),rowsById=new Map();for(const row of artifactRows){const id=String(row?.artifactId||'');if(!id||rowsById.has(id))throw storageError('Stored backup artifacts contain a missing or duplicate identity.','BACKUP_ARTIFACT_ALLOWLIST_MISMATCH');rowsById.set(id,row);}for(const id of rowsById.keys())if(!canonicalById.has(id))throw storageError(`Stored artifact ${id} is not in the exact canonical backup allowlist and cannot be exported.`,'BACKUP_UNMANIFESTED_ARTIFACT');
  const entries=[];for(const expected of canonical){const row=rowsById.get(expected.artifactId);if(!row)throw storageError(`Canonical artifact ${expected.artifactId} has no stored bytes for complete backup.`,'BACKUP_CANONICAL_ARTIFACT_MISSING');const bytes=new Uint8Array(await row.blob.arrayBuffer()),actualSize=bytes.byteLength,actualSha256=await hash.sha256Bytes(bytes);if(String(row.jobId)!==projectIdentity(project)||String(row.filename)!==expected.filename||actualSize!==Number(expected.byteSize)||actualSha256!==String(expected.sha256)||Number(row.byteSize)!==actualSize||String(row.sha256)!==actualSha256)throw storageError(`Stored artifact ${expected.artifactId} failed exact export-time byte and metadata verification.`,'ARTIFACT_INTEGRITY_MISMATCH');entries.push({artifactId:expected.artifactId,jobId:String(row.jobId),filename:expected.filename,mediaType:String(row.mediaType||'application/octet-stream'),byteSize:actualSize,sha256:actualSha256,lineage:clone(row.lineage||{}),createdAt:row.createdAt||null,disclosureClassification:expected.disclosureClassification,base64:bytesToBase64(bytes)});}
  assertNoFilenameCollisions(entries);return entries;
}
function backupArtifactVerificationManifest(entries){return entries.map(item=>({artifactId:item.artifactId,filename:item.filename,byteSize:item.byteSize,sha256:item.sha256})).sort((a,b)=>a.artifactId.localeCompare(b.artifactId));}
async function validateBackupArtifactCandidate(project,artifactEntries,{milestone='MANUAL'}={}){
  const engine=globalThis.closedLoopWorkflowEngine,integrity=validateProjectIntegrity(project,{verifyDerived:true});if(!integrity.valid)throw storageError(`Backup restore test rejected canonical project state: ${integrity.issues.join(' | ')}`,'BACKUP_RESTORE_PROJECT_INTEGRITY_FAILED');
  const normalized=clone(artifactEntries);if(!Array.isArray(normalized))throw storageError('Backup restore artifacts must be an array.','BACKUP_RESTORE_ARTIFACT_MISMATCH');assertNoFilenameCollisions(normalized);const canonicalManifest=engine.backupArtifactManifest(project),canonicalById=new Map(canonicalManifest.map(item=>[item.artifactId,item])),seen=new Set(),rehydrated=[];
  for(const entry of normalized){const artifactId=String(entry?.artifactId||'');if(!artifactId||seen.has(artifactId))throw storageError('Backup restore contains a missing or duplicate artifact identity.','BACKUP_RESTORE_ARTIFACT_MISMATCH');seen.add(artifactId);const expected=canonicalById.get(artifactId);if(!expected)throw storageError(`Backup restore contains unmanifested artifact ${artifactId}.`,'BACKUP_RESTORE_EXTRA_ARTIFACT');if(typeof entry.base64!=='string')throw storageError(`Backup restore artifact ${artifactId} has no encoded bytes.`,'BACKUP_RESTORE_BYTES_MISSING');let bytes;try{bytes=base64ToBytes(entry.base64);}catch{throw storageError(`Backup restore artifact ${artifactId} has invalid encoded bytes.`,'BACKUP_RESTORE_BYTES_INVALID');}const digest=await hash.sha256Bytes(bytes),byteSize=bytes.byteLength;if(String(entry.filename)!==expected.filename||Number(entry.byteSize)!==Number(expected.byteSize)||String(entry.sha256)!==expected.sha256||byteSize!==Number(expected.byteSize)||digest!==expected.sha256)throw storageError(`Backup restore cannot rehydrate exact canonical artifact ${artifactId}.`,'BACKUP_RESTORE_ARTIFACT_MISMATCH');const blob=new Blob([bytes],{type:String(entry.mediaType||'application/octet-stream')}),readBack=new Uint8Array(await blob.arrayBuffer()),readBackDigest=await hash.sha256Bytes(readBack);if(readBack.byteLength!==byteSize||readBackDigest!==digest)throw storageError(`Backup restore read-back failed for artifact ${artifactId}.`,'BACKUP_RESTORE_REHYDRATION_FAILED');rehydrated.push({artifactId,filename:expected.filename,byteSize,sha256:digest});}
  if(seen.size!==canonicalById.size)for(const artifactId of canonicalById.keys())if(!seen.has(artifactId))throw storageError(`Backup restore is missing canonical artifact bytes ${artifactId}.`,'BACKUP_RESTORE_BYTES_MISSING');
  const encoded=new TextEncoder().encode(JSON.stringify(canonicalProject(project))),decoded=JSON.parse(new TextDecoder('utf-8',{fatal:true}).decode(encoded));if(projectSha256(decoded)!==projectSha256(project))throw storageError('Backup restore test changed canonical project bytes during JSON round trip.','BACKUP_RESTORE_PROJECT_ROUNDTRIP_FAILED');const restoredIntegrity=validateProjectIntegrity(decoded,{verifyDerived:true});if(!restoredIntegrity.valid)throw storageError(`Backup restore produced invalid canonical project state: ${restoredIntegrity.issues.join(' | ')}`,'BACKUP_RESTORE_PROJECT_INTEGRITY_FAILED');const manifest=backupArtifactVerificationManifest(rehydrated),projectSubjectSha256=engine.backupCheckpointSubject(project,String(milestone)).subjectSha256,restoreBinding={contract:'closed-loop-backup-restore-binding/1',milestone:String(milestone).toUpperCase(),jobId:projectIdentity(project),projectSubjectSha256,artifactManifestSha256:hash.sha256Value(manifest),artifactCount:manifest.length,artifactByteIdentities:manifest,status:'SUCCEEDED'};return {project:decoded,manifest,restoreBinding,restoreBindingSha256:hash.sha256Value(restoreBinding),sourceProjectSha256:projectSha256(project),restoredProjectSha256:projectSha256(decoded)};
}
function buildProjectPackagePayload(project,artifactEntries,exportedAt,{disclosureHandling='NO_SENSITIVE_MATERIAL',humanAuthorizationId=null}={}){
  const exportedProject=canonicalProject(project),packageManifest={jobId:projectIdentity(project),projectSha256:projectSha256(exportedProject),artifactCount:artifactEntries.length,filenameNormalizationVersion:'closed-loop-filename/1',artifacts:artifactEntries.map(item=>({artifactId:item.artifactId,filename:item.filename,canonicalPath:item.filenameContract?.canonicalPath||item.filename,mediaType:item.mediaType,byteSize:item.byteSize,sha256:item.sha256}))},exportSecurity={encryption:'NONE',encrypted:false,plaintext:true,disclosureHandling:String(disclosureHandling),humanAuthorizationId:humanAuthorizationId||null},body={schema:'closed-loop-project-package/1',projectSchema:project.schema,workflow:project.workflow,responseSchema:globalThis.closedLoopWorkflowSchema?.RESPONSE_SCHEMA,project:exportedProject,artifacts:artifactEntries.map(({filenameContract,disclosureClassification,...entry})=>entry),packageManifest,exportSecurity,exportedAt},packageSha256=hash.sha256Value(body);return {...body,packageSha256};
}
async function verifySerializedBackupPayload(payload,{milestone='MANUAL'}={}){
  const expandedBytes=new TextEncoder().encode(JSON.stringify(payload));if(expandedBytes.byteLength>STORE_LIMITS.maxImportExpandedBytes)throw storageError('Export package would exceed the supported expanded import limit.','EXPORT_RESOURCE_LIMIT');const compressed=await compressBytes(expandedBytes);if(compressed.byteLength>STORE_LIMITS.maxImportCompressedBytes)throw storageError('Export package would exceed the supported compressed import limit.','EXPORT_RESOURCE_LIMIT');const restored=JSON.parse(new TextDecoder('utf-8',{fatal:true}).decode(await decompressBytes(compressed,STORE_LIMITS.maxImportExpandedBytes))),{packageSha256,...body}=restored;if(hash.sha256Value(body)!==packageSha256||packageSha256!==payload.packageSha256)throw storageError('Backup restore test detected a package digest mismatch after compression round trip.','BACKUP_RESTORE_PACKAGE_MISMATCH');if(projectSha256(body.project)!==body.packageManifest?.projectSha256)throw storageError('Backup restore test detected a project manifest mismatch.','BACKUP_RESTORE_MANIFEST_MISMATCH');const restore=await runIsolatedRestoreTest(new Blob([compressed],{type:'application/gzip'}),{milestone});return {compressed,expandedByteLength:expandedBytes.byteLength,packageSha256,restore};
}
async function authorizeUnencryptedSensitiveExport({jobId,expectedProjectRevision,operatorLabel='HUMAN_OPERATOR'}={}){
  const engine=globalThis.closedLoopWorkflowEngine;if(typeof engine?.recordBackupDisclosureDecision!=='function')throw storageError('The sensitive-backup human-decision command is unavailable.','BACKUP_DISCLOSURE_DECISION_UNAVAILABLE');return transact(String(jobId),Number(expectedProjectRevision),project=>engine.recordBackupDisclosureDecision(project,{operatorLabel,acknowledged:true}));
}
async function exportPackage(jobId,{milestone='AUTO',encryptionContractId=null,humanAuthorizationId=null,acknowledgeUnencryptedSensitiveExport=false,operatorLabel='HUMAN_OPERATOR'}={}){
  const owner=String(jobId),engine=globalThis.closedLoopWorkflowEngine;let projects=await readAll(),project=projects.find(item=>projectIdentity(item)===owner);if(!project)throw new Error(`Project ${jobId} is not stored.`);let disclosure=evaluateBackupExportDisclosure(project,{encryptionContractId,humanAuthorizationId});if(!disclosure.allowed&&acknowledgeUnencryptedSensitiveExport===true){const decision=await authorizeUnencryptedSensitiveExport({jobId:owner,expectedProjectRevision:Number(project.revision||0),operatorLabel});project=decision.project;humanAuthorizationId=decision.result?.decisionId||null;disclosure=evaluateBackupExportDisclosure(project,{encryptionContractId,humanAuthorizationId});}if(!disclosure.allowed)throw storageError(disclosure.reasons.join(' ')||'Complete export is blocked by the current disclosure policy.','BACKUP_DISCLOSURE_BLOCKED');const normalizedMilestone=backupMilestoneFor(project,milestone),artifactRows=await listArtifacts(owner);assertProjectResourceLimits(project,{artifactCount:artifactRows.length});await preflightStorage(artifactRows.reduce((sum,row)=>sum+Number(row.byteSize||0),0));const artifactEntries=await verifiedBackupArtifactEntries(project,artifactRows),timestamp=now(),payload=buildProjectPackagePayload(project,artifactEntries,timestamp,{disclosureHandling:disclosure.handling,humanAuthorizationId:disclosure.humanAuthorizationId}),verifiedPayload=await verifySerializedBackupPayload(payload,{milestone:normalizedMilestone}),packageByteSha256=await hash.sha256Bytes(verifiedPayload.compressed),packageBinding={schema:'closed-loop-backup-package-binding/1',packageSha256:verifiedPayload.packageSha256,packageByteSha256,packageByteLength:verifiedPayload.compressed.byteLength,expandedByteLength:verifiedPayload.expandedByteLength};let pendingExportId=null;
  if(normalizedMilestone!=='MANUAL'){const committed=await transact(owner,Number(project.revision||0),next=>engine.recordPendingBackupExport(next,{milestone:normalizedMilestone,verifiedArtifactManifest:backupArtifactVerificationManifest(artifactEntries),restoreProof:verifiedPayload.restore,packageBinding,disclosureHandling:disclosure.handling,humanAuthorizationId:disclosure.humanAuthorizationId,encryptionContractId:null,createdAt:timestamp}));pendingExportId=committed.result?.pendingExportId||null;if(!pendingExportId)throw storageError('The exact exported package was not bound to a pending canonical backup record.','BACKUP_PENDING_EXPORT_COMMIT_FAILED');}
  const exportRecord={jobId:owner,packageSha256:verifiedPayload.packageSha256,packageByteSha256,packageByteLength:verifiedPayload.compressed.byteLength,expandedByteLength:verifiedPayload.expandedByteLength,projectSha256:payload.packageManifest.projectSha256,artifactManifestSha256:verifiedPayload.restore.artifactManifestSha256,artifactCount:artifactEntries.length,milestone:normalizedMilestone,pendingExportId,checkpointId:null,externalCustodyStatus:pendingExportId?'AWAITING_OPERATOR_ACKNOWLEDGMENT':'NOT_A_CHECKPOINT',restoreTestStatus:'SUCCEEDED',restoreTestEvidenceSha256:verifiedPayload.restore.restoreTestEvidenceSha256,restoreBindingSha256:verifiedPayload.restore.restoreBindingSha256,disclosureHandling:disclosure.handling,plaintextExport:true,browserDownloadInitiationIsCustodyProof:false,at:timestamp};await metaPut('lastVerifiedExport',exportRecord);await metaPut('lastVerifiedExport:'+owner,exportRecord);return new Blob([verifiedPayload.compressed],{type:'application/gzip'});
}
async function confirmBackupExternalCustodyPersistent({jobId,expectedProjectRevision,pendingExportId,operatorLabel='HUMAN_OPERATOR'}={}){
  const engine=globalThis.closedLoopWorkflowEngine;if(typeof engine?.confirmBackupExternalCustody!=='function')throw storageError('The backup external-custody confirmation authority is unavailable.','BACKUP_CUSTODY_AUTHORITY_UNAVAILABLE');return transact(String(jobId),Number(expectedProjectRevision),project=>engine.confirmBackupExternalCustody(project,{pendingExportId:String(pendingExportId||''),operatorLabel:String(operatorLabel||'HUMAN_OPERATOR'),acknowledged:true}));
}
const INFRASTRUCTURE_ID_FIELDS=Object.freeze({
  inputVersions:['id','inputVersionId'],rawResponses:['id','rawResponseId'],responseProposals:['id','proposalId'],responseValidations:['id','validationId'],acceptedChanges:['id','changeId'],rejectedResponses:['id','rejectedResponseId'],extractionManifests:['id','manifestId'],humanInputRequests:['id','requestId'],humanInputAnswers:['id','answerId'],stageConfirmations:['id','confirmationId'],artifactVersions:['id','artifactVersionId','versionId'],generatedPrompts:['id','instructionId','promptId'],generatedOutputs:['id','outputId'],outputReceipts:['id','receiptId'],history:['id','eventId'],newJobResets:['id','resetId'],reviews:['id','reviewId'],recoveredProjects:['id','recoveryId'],responseDispositions:['id','dispositionId'],executionFailures:['id','executionFailureId','failureId'],commandReceipts:['id','receiptId','commandId','idempotencyKey'],humanDecisions:['id','decisionId','humanDecisionId'],nativeExecutionEvents:['id','executionEventId','eventId'],intakeCoverageManifests:['id','manifestId','intakeCoverageManifestId'],obligationManifests:['id','manifestId','obligationManifestId'],promptContextManifests:['id','manifestId','contextManifestId'],blindAliasMaps:['id','mapId','blindAliasMapId'],migrationArchives:['id','archiveId']
});
function cloneProjectLineage(project,packageSha256){
  const source=clone(project),sourceJobId=projectIdentity(source);if(!sourceJobId)throw storageError('A source JOB_ID is required to clone project lineage.','CLONE_SOURCE_JOB_ID_REQUIRED');
  const newJobId=`JOB-CLONE-${hash.sha256Value({sourceJobId,packageSha256,nonce:randomNonce()}).slice(0,24).toUpperCase()}`,mapping=new Map([[sourceJobId,newJobId]]),schemaApi=globalThis.closedLoopWorkflowSchema,engine=globalThis.closedLoopWorkflowEngine;
  const allocateCloneIdentity=(oldId,family,key='ID')=>{const value=String(oldId??'').trim();if(!value)return null;if(mapping.has(value))return mapping.get(value);const prefix=String(key||family||'ID').replace(/_?ID(?:S)?$/i,'').replace(/[^A-Za-z0-9]+/g,'-').replace(/^-+|-+$/g,'').toUpperCase()||'ID',next=`${prefix}-CLONE-${hash.sha256Value({newJobId,family,key,value}).slice(0,20).toUpperCase()}`;mapping.set(value,next);return next;};

  for(const [collection,definition] of Object.entries(schemaApi?.RECORD_SCHEMAS||{}))for(const record of source.projectData?.[collection]||[]){const oldId=engine?.recordId?.(record,collection);if(oldId)allocateCloneIdentity(oldId,collection,definition.idField||'ID');}
  for(const [collection,keys] of Object.entries(INFRASTRUCTURE_ID_FIELDS))for(const record of source.projectData?.[collection]||[])for(const key of keys)if(typeof record?.[key]==='string'&&record[key].trim())allocateCloneIdentity(record[key],collection,key);
  for(const [key,value] of Object.entries(source.job||{}))if(['CURRENT_INPUT_VERSION','CURRENT_SOURCE_SET_VERSION','CURRENT_REQUIREMENTS_VERSION','CURRENT_TEST_SUITE_VERSION','CURRENT_INSTRUCTION_VERSION','CURRENT_ITERATION','CURRENT_BASELINE_ID','CURRENT_PRODUCT_ID','LATEST_EVIDENCE_REFERENCE'].includes(key)&&typeof value==='string'&&value.trim()&&!PLACEHOLDER_REFERENCES.has(value.trim().toUpperCase()))allocateCloneIdentity(value,'job',key);
  for(const reservation of source.projectData?.operationReservations||[]){for(const key of ['PACKAGE_ID','CHALLENGE_NONCE','TARGET_SLOT','TARGET_KEY','IDEMPOTENCY_KEY']){const value=engine?.recordValue?.(reservation,key)??reservation?.[key];if(typeof value==='string'&&value.trim())allocateCloneIdentity(value,'operationReservations',key);}}
  for(const mapRecord of source.projectData?.blindAliasMaps||[])for(const entry of Array.isArray(mapRecord?.entries)?mapRecord.entries:Array.isArray(mapRecord?.mappings)?mapRecord.mappings:[]){if(typeof entry?.alias==='string'&&entry.alias.trim())allocateCloneIdentity(entry.alias,'blindAliasMaps','alias');}

  const exactAuditKeys=new Set(['rawText','rawResponseText','responseText','output','prompt','text','rawCanonicalJson','sourceSnapshot']);
  const remap=(value,key='',insideAudit=false)=>{
    if(typeof value==='string')return insideAudit||exactAuditKeys.has(key)?value:(mapping.get(value)||value);
    if(Array.isArray(value))return value.map(item=>remap(item,key,insideAudit));
    if(!value||typeof value!=='object')return value;
    const audit=insideAudit||key==='payload'&&value.operational===false;
    return Object.fromEntries(Object.entries(value).map(([childKey,item])=>[childKey,remap(item,childKey,audit||childKey==='payload'&&value.operational===false)]));
  };
  const preservedImportedPayloads=clone(source.projectData?.nonOperationalImportedPayloads||[]),cloned=remap(source);
  cloned.job=cloned.job||{};cloned.job.JOB_ID=newJobId;cloned.revision=0;delete cloned.projectSha256;cloned.projectData=cloned.projectData||{};cloned.projectData.idCounters={};cloned.projectData.nonOperationalImportedPayloads=preservedImportedPayloads;
  for(const prompt of cloned.projectData.generatedPrompts||[]){prompt.invalidatedBy='PROJECT_CLONED_NEW_LINEAGE';prompt.operational=false;prompt.currentScopeStatus='STALE';prompt.cloneSourcePrompt=true;}
  for(const proposal of cloned.projectData.responseProposals||[])if(['PENDING_OPERATOR_REVIEW','READY_FOR_ACCEPTANCE'].includes(String(proposal.status||'').toUpperCase())){proposal.status='STALE';proposal.invalidatedBy='PROJECT_CLONED_NEW_LINEAGE';proposal.operational=false;}
  for(const reservation of cloned.projectData.operationReservations||[]){reservation.active=false;reservation.invalidatedBy='PROJECT_CLONED_NEW_LINEAGE';reservation.status='SUPERSEDED';if(reservation.fields){reservation.fields.STATUS='SUPERSEDED';reservation.STATUS='SUPERSEDED';}}
  for(const receipt of cloned.projectData.commandReceipts||[]){receipt.operational=false;receipt.replayEligible=false;receipt.cloneSourceCommandEvidence=true;}
  for(const state of Object.values(cloned.stages||{})){if(!state||typeof state!=='object')continue;state.currentPromptId=null;state.controllingPromptIdentity=null;state.pendingProposal=null;}
  for(const collection of Object.keys(schemaApi?.RECORD_SCHEMAS||{}))for(const record of cloned.projectData?.[collection]||[])engine?.refreshRecordHashes?.(record,collection);
  const sourceDigest=projectSha256(source),snapshotId=`CLONE-SOURCE-${hash.sha256Value({sourceJobId,packageSha256}).slice(0,20).toUpperCase()}`,snapshotRecord={snapshotId,sourceSchema:String(source.schema||''),sourceRevision:Number(source.revision||0),sourceJobId,sourceProjectSha256:sourceDigest,sourcePackageSha256:packageSha256,operational:false,executable:false,gating:false,promptVisible:false,currentScopeEligible:false,purpose:'CLONE_SOURCE_SNAPSHOT_AUDIT_EVIDENCE',payload:source};
  cloned.projectData.nonOperationalImportedPayloads.push(snapshotRecord);cloned.projectData.migrationArchives=Array.isArray(cloned.projectData.migrationArchives)?cloned.projectData.migrationArchives:[];cloned.projectData.migrationArchives.push({archiveId:snapshotId,kind:'CLONE_SOURCE_SNAPSHOT',sourceJobId,sourceProjectSha256:sourceDigest,sourcePackageSha256:packageSha256,operational:false,gating:false,promptVisible:false,importedAt:now()});cloned.projectData.cloneLineage={sourceJobId,sourceProjectSha256:sourceDigest,sourcePackageSha256:packageSha256,identityMappingSha256:hash.sha256Value([...mapping.entries()].sort(([a],[b])=>a<b?-1:a>b?1:0)),operationalSourceSnapshot:false};return {project:cloned,mapping,newJobId};
}
async function prepareImportPackage(blob,{mode='RESTORE'}={}){
  try{
    const importMode=String(mode||'').toUpperCase();if(!['RESTORE','CLONE'].includes(importMode))throw storageError('Import mode must be explicitly RESTORE or CLONE.','INVALID_IMPORT_MODE');if(!(blob instanceof Blob)||blob.size>STORE_LIMITS.maxImportCompressedBytes)throw storageError('Import package exceeds the centralized compressed-size limit.','IMPORT_RESOURCE_LIMIT');
    const compressed=new Uint8Array(await blob.arrayBuffer()),expanded=await decompressBytes(compressed,STORE_LIMITS.maxImportExpandedBytes);await preflightStorage(expanded.byteLength);const json=new TextDecoder('utf-8',{fatal:true}).decode(expanded),scanner=globalThis.closedLoopResponseIngestion?.scanJsonAmbiguity;if(typeof scanner!=='function')throw storageError('The mandatory bounded JSON pre-parse scanner is unavailable.','IMPORT_PREPARSE_SCANNER_UNAVAILABLE');scanner(json,STORE_LIMITS.maxObjectDepth,{maxNodes:STORE_LIMITS.maxImportObjectNodes,maxArrayLength:STORE_LIMITS.maxArrayLength,maxObjectMembers:STORE_LIMITS.maxObjectMembers,maxStringLength:STORE_LIMITS.maxStringLength,maxNumberTokenLength:STORE_LIMITS.maxNumberTokenLength});const payload=JSON.parse(json);assertProjectResourceLimits(payload?.project,{artifactCount:Array.isArray(payload?.artifacts)?payload.artifacts.length:0});validateHostileStructure(payload);const allowedPackageKeys=new Set(['schema','projectSchema','workflow','responseSchema','project','artifacts','packageManifest','exportSecurity','exportedAt','packageSha256']);for(const key of Object.keys(payload||{}))if(!allowedPackageKeys.has(key))throw storageError(`Unmanifested package member ${key} is prohibited.`,'IMPORT_UNMANIFESTED_MEMBER');const {packageSha256,...body}=payload;
    if(hash.sha256Value(body)!==packageSha256)throw storageError('Project package hash mismatch.','IMPORT_PACKAGE_HASH_MISMATCH');if(body.schema!=='closed-loop-project-package/1')throw storageError('Unsupported project package schema.','IMPORT_PACKAGE_SCHEMA_UNSUPPORTED');const security=body.exportSecurity;if(!security||security.encryption!=='NONE'||security.encrypted!==false||security.plaintext!==true||!['NO_SENSITIVE_MATERIAL','EXPLICIT_UNENCRYPTED_ACKNOWLEDGMENT'].includes(String(security.disclosureHandling||'')))throw storageError('Package encryption and plaintext disclosure labeling is missing or contradictory.','IMPORT_EXPORT_SECURITY_INVALID');
    const schemaApi=globalThis.closedLoopWorkflowSchema,sourceProject=clone(body.project),sourceId=projectIdentity(sourceProject),sourceProjectSha256=projectSha256(sourceProject);let project=typeof schemaApi?.migrateProjectToCurrent==='function'?schemaApi.migrateProjectToCurrent(clone(sourceProject)):clone(sourceProject);preserveOpaqueExtensions(project,sourceProject);let id=projectIdentity(project);if(project?.schema!=='closed-loop-project/3'||project?.workflow!=='mobile-closed-loop/30'||Number(project?.stageCount)!==30||Object.keys(project?.stages||{}).length!==30)throw storageError('Imported project identity or stage count is invalid.','IMPORT_PROJECT_IDENTITY_INVALID');if(body.projectSchema!==project.schema||body.workflow!==project.workflow||body.responseSchema!==schemaApi?.RESPONSE_SCHEMA)throw storageError('Package schema manifest does not match the embedded project.','IMPORT_MANIFEST_SCHEMA_MISMATCH');if(!id)throw storageError('Imported project has no JOB_ID.','IMPORT_JOB_ID_MISSING');reconcileRestoredOperationReservations(project,importMode);
    const packageArtifacts=Array.isArray(body.artifacts)?body.artifacts:[];assertProjectResourceLimits(project,{artifactCount:packageArtifacts.length});const artifactIds=packageArtifacts.map(a=>String(a?.artifactId||''));if(artifactIds.some(x=>!x)||new Set(artifactIds).size!==artifactIds.length)throw storageError('Package artifacts contain a missing or duplicate artifact identity.','IMPORT_ARTIFACT_IDENTITY_INVALID');const verifiedArtifacts=[];
    for(const a of packageArtifacts){if(a.jobId!==undefined&&String(a.jobId)!==sourceId)throw storageError(`Artifact ${a.artifactId} belongs to a different JOB_ID than the package project.`,'IMPORT_ARTIFACT_PROJECT_MISMATCH');canonicalFilename(a.filename);let bytes;try{bytes=base64ToBytes(a.base64);}catch{throw storageError(`Artifact ${a.artifactId} contains invalid encoded bytes.`,'IMPORT_ARTIFACT_BYTES_INVALID');}if(bytes.byteLength>STORE_LIMITS.maxArtifactBytes||bytes.byteLength!==Number(a.byteSize))throw storageError(`Artifact ${a.artifactId} byte size mismatch or resource-limit violation.`,'IMPORT_ARTIFACT_SIZE_MISMATCH');const digest=await hash.sha256Bytes(bytes);if(digest!==a.sha256)throw storageError(`Artifact ${a.artifactId} hash mismatch.`,'IMPORT_ARTIFACT_HASH_MISMATCH');verifiedArtifacts.push({...clone(a),jobId:sourceId,blob:new Blob([bytes],{type:a.mediaType||'application/octet-stream'})});}
    assertNoFilenameCollisions(verifiedArtifacts);const manifest=body.packageManifest||{},manifestArtifacts=Array.isArray(manifest.artifacts)?manifest.artifacts:[];if(manifestArtifacts.length>STORE_LIMITS.maxPackageArtifacts)throw storageError('Package manifest exceeds the centralized package-artifact limit.','PACKAGE_ARTIFACT_LIMIT');const manifestIds=manifestArtifacts.map(a=>String(a?.artifactId||''));if(manifestIds.some(x=>!x)||new Set(manifestIds).size!==manifestIds.length)throw storageError('Package manifest contains a missing or duplicate artifact identity.','IMPORT_MANIFEST_ARTIFACT_IDENTITY_INVALID');if(manifest.jobId!==sourceId||Number(manifest.artifactCount)!==verifiedArtifacts.length||manifestArtifacts.length!==verifiedArtifacts.length||manifest.projectSha256!==sourceProjectSha256)throw storageError('Package manifest does not reconcile with the embedded project and artifacts.','IMPORT_MANIFEST_RECONCILIATION_FAILED');
    const manifestById=new Map(manifestArtifacts.map(a=>[String(a.artifactId),a]));for(const a of verifiedArtifacts){const m=manifestById.get(String(a.artifactId));if(!m||m.sha256!==a.sha256||Number(m.byteSize)!==Number(a.byteSize)||m.filename!==a.filename||String(m.mediaType||'')!==String(a.mediaType||''))throw storageError(`Package manifest mismatch for artifact ${a.artifactId}.`,'IMPORT_MANIFEST_ARTIFACT_MISMATCH');}
    const packagedById=new Map(verifiedArtifacts.map(a=>[String(a.artifactId),a]));for(const artifact of (sourceProject.projectData?.artifacts||[]).filter(record=>record?.active!==false&&!record?.invalidatedBy)){const artifactId=String(globalThis.closedLoopWorkflowEngine?.recordId?.(artifact,'artifacts')||''),availability=String(globalThis.closedLoopWorkflowEngine?.recordValue?.(artifact,'AVAILABILITY')||'').toUpperCase();if(availability!=='BYTES_PERSISTED_AND_VERIFIED')continue;const packaged=packagedById.get(artifactId);if(!packaged||String(globalThis.closedLoopWorkflowEngine.recordValue(artifact,'FILENAME')||'')!==String(packaged.filename)||Number(globalThis.closedLoopWorkflowEngine.recordValue(artifact,'BYTE_SIZE'))!==Number(packaged.byteSize)||String(globalThis.closedLoopWorkflowEngine.recordValue(artifact,'SHA256')||'')!==String(packaged.sha256))throw storageError(`Canonical artifact ${artifactId||'UNKNOWN'} does not reconcile with verified package bytes.`,'IMPORT_CANONICAL_ARTIFACT_MISMATCH');}
    if(importMode==='CLONE'){const cloned=cloneProjectLineage(project,packageSha256);project=cloned.project;id=cloned.newJobId;for(const artifact of verifiedArtifacts){artifact.artifactId=cloned.mapping.get(String(artifact.artifactId))||artifact.artifactId;artifact.jobId=id;artifact.lineage={...clone(artifact.lineage||{}),clonedFromArtifactId:packageArtifacts.find(source=>source.sha256===artifact.sha256)?.artifactId||null,sourceJobId:sourceId};}}assertProjectIntegrity(project);return {contract:'closed-loop-prepared-import/1',importMode,packageSha256,sourceId,sourceProject,sourceProjectSha256,project,id,packageArtifacts,verifiedArtifacts,expandedByteLength:expanded.byteLength,compressedByteLength:compressed.byteLength};
  }catch(error){throw Object.assign(error,{existingProjectsUnchanged:true});}
}
async function applyPreparedImportTransaction(prepared,io){
  const {id,project,verifiedArtifacts,importMode,packageSha256}=prepared,prior=await io.getProject(id);if(prior&&importMode==='RESTORE')throw storageError(`RESTORE cannot silently overwrite active project ${id}; delete/archive the active lineage or choose CLONE.`,'RESTORE_ACTIVE_PROJECT_CONFLICT');if(prior)throw storageError(`Imported project identity ${id} already exists.`,'IMPORT_PROJECT_ID_COLLISION');const importedRevision=Math.max(0,Number(project.revision||0)),next=clone(project);next.revision=importedRevision+1;delete next.projectSha256;assertProjectIntegrity(next);const digest=projectSha256(next),existingArtifacts=await io.getAllArtifacts(),incomingIds=new Set(verifiedArtifacts.map(a=>String(a.artifactId)));
  for(const existing of existingArtifacts){const artifactId=String(existing.artifactId);if(String(existing.jobId)===id&&!incomingIds.has(artifactId))await io.deleteArtifact(artifactId);else if(String(existing.jobId)!==id&&incomingIds.has(artifactId))throw storageError(`Imported artifact identity ${artifactId} already belongs to another project.`,'CROSS_PROJECT_ARTIFACT_ID_COLLISION');}
  fault('during-import-project-write');await io.putProject({jobId:id,revision:next.revision,project:next,projectSha256:digest,updatedAt:now()});for(const a of verifiedArtifacts){fault('during-import-artifact-write');await io.putArtifact({artifactId:String(a.artifactId),jobId:id,blob:a.blob,filename:String(a.filename),mediaType:String(a.mediaType||'application/octet-stream'),byteSize:Number(a.byteSize),sha256:String(a.sha256),lineage:clone(a.lineage||{}),createdAt:a.createdAt||now()});}await io.putMeta('selectedProject',id);await io.putMeta('lastCommittedRevision',{jobId:id,revision:next.revision,projectSha256:digest});await io.putMeta('lastVerifiedImport',{jobId:id,packageSha256,artifactCount:verifiedArtifacts.length,at:now()});return {project:next,projectSha256:digest,artifactCount:verifiedArtifacts.length};
}
function indexedDbImportIo(tx){const projects=tx.objectStore(PROJECTS),artifacts=tx.objectStore(ARTIFACTS),meta=tx.objectStore(META);return {getProject:id=>request(projects.get(id)),getAllArtifacts:()=>request(artifacts.getAll()),deleteArtifact:id=>request(artifacts.delete(id)),putProject:row=>request(projects.put(row)),putArtifact:row=>request(artifacts.put(row)),putMeta:(key,value)=>request(meta.put({key,value,updatedAt:now()}))};}
async function activatePreparedImport(prepared,{database=null,broadcast=true}={}){
  fault('before-import-transaction');const tx=database?database.transaction([PROJECTS,ARTIFACTS,META],'readwrite'):await openTransaction([PROJECTS,ARTIFACTS,META],'readwrite');try{const activated=await applyPreparedImportTransaction(prepared,indexedDbImportIo(tx));fault('before-import-commit');await complete(tx);activated.project.projectSha256=activated.projectSha256;if(broadcast)try{new BroadcastChannel('closed-loop-reliability').postMessage({type:'PROJECT_CHANGED',jobId:prepared.id,revision:activated.project.revision});}catch{}return activated;}catch(error){try{tx.abort();}catch{}throw Object.assign(error,{existingProjectsUnchanged:true});}
}
function openIsolatedRestoreDatabase(name){return new Promise((resolve,reject)=>{const req=indexedDB.open(name,DB_VERSION);req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains(PROJECTS))db.createObjectStore(PROJECTS,{keyPath:'jobId'});if(!db.objectStoreNames.contains(ARTIFACTS))db.createObjectStore(ARTIFACTS,{keyPath:'artifactId'});if(!db.objectStoreNames.contains(STAGING))db.createObjectStore(STAGING,{keyPath:'stagedArtifactId'});if(!db.objectStoreNames.contains(META))db.createObjectStore(META,{keyPath:'key'});};req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error||storageError('Isolated restore-test database could not be opened.','BACKUP_RESTORE_ISOLATED_DB_FAILED'));req.onblocked=()=>reject(storageError('Isolated restore-test database open was blocked.','BACKUP_RESTORE_ISOLATED_DB_BLOCKED'));});}
function deleteIsolatedRestoreDatabase(name){return new Promise((resolve,reject)=>{const req=indexedDB.deleteDatabase(name);req.onsuccess=()=>resolve(true);req.onerror=()=>reject(req.error||storageError('Isolated restore-test database could not be deleted.','BACKUP_RESTORE_ISOLATED_DELETE_FAILED'));req.onblocked=()=>reject(storageError('Isolated restore-test database deletion was blocked.','BACKUP_RESTORE_ISOLATED_DELETE_BLOCKED'));});}
async function verifyActivatedImportRows(database,prepared,activated){
  const tx=database.transaction([PROJECTS,ARTIFACTS,STAGING],'readonly'),projectRow=await request(tx.objectStore(PROJECTS).get(prepared.id)),artifactRows=await request(tx.objectStore(ARTIFACTS).getAll()),stagingCount=await request(tx.objectStore(STAGING).count());await complete(tx);if(!projectRow||Number(projectRow.revision)!==Number(activated.project.revision)||String(projectRow.projectSha256)!==String(activated.projectSha256)||projectSha256(projectRow.project)!==String(activated.projectSha256))throw storageError('Isolated restore activation did not persist the exact validated project row.','BACKUP_RESTORE_PROJECT_ROW_MISMATCH');const integrity=validateProjectIntegrity(projectRow.project,{verifyDerived:true});if(!integrity.valid)throw storageError(`Isolated restored project row failed integrity: ${integrity.issues.join(' | ')}`,'BACKUP_RESTORE_PROJECT_ROW_MISMATCH');if(stagingCount!==0||artifactRows.length!==prepared.verifiedArtifacts.length)throw storageError('Isolated restore activation produced an unexpected staged or canonical artifact row count.','BACKUP_RESTORE_ARTIFACT_ROW_MISMATCH');const expectedById=new Map(prepared.verifiedArtifacts.map(item=>[String(item.artifactId),item]));for(const row of artifactRows){const expected=expectedById.get(String(row.artifactId));if(!expected||String(row.jobId)!==prepared.id||String(row.filename)!==String(expected.filename)||Number(row.byteSize)!==Number(expected.byteSize)||String(row.sha256)!==String(expected.sha256)||!(row.blob instanceof Blob))throw storageError(`Isolated restore artifact row ${row.artifactId||'UNKNOWN'} does not match the validated import.`, 'BACKUP_RESTORE_ARTIFACT_ROW_MISMATCH');const bytes=new Uint8Array(await row.blob.arrayBuffer()),digest=await hash.sha256Bytes(bytes);if(bytes.byteLength!==Number(expected.byteSize)||digest!==String(expected.sha256))throw storageError(`Isolated restore artifact row ${row.artifactId} failed byte read-back.`, 'BACKUP_RESTORE_ARTIFACT_ROW_MISMATCH');}
  return {restoredProjectRowCount:1,restoredArtifactRowCount:artifactRows.length,stagingRowCount:stagingCount,activatedProjectSha256:activated.projectSha256};
}
async function removeIsolatedRestoreLineage(database){const cleanup=database.transaction([PROJECTS,ARTIFACTS,STAGING,META],'readwrite');for(const name of [PROJECTS,ARTIFACTS,STAGING,META])cleanup.objectStore(name).clear();await complete(cleanup);const verify=database.transaction([PROJECTS,ARTIFACTS,STAGING,META],'readonly'),counts={};for(const name of [PROJECTS,ARTIFACTS,STAGING,META])counts[name]=await request(verify.objectStore(name).count());await complete(verify);if(Object.values(counts).some(Number))throw storageError('Isolated restore-test lineage could not be completely removed.','BACKUP_RESTORE_ISOLATED_CLEANUP_FAILED');return counts;}
async function runIndexedDbIsolatedActivation(prepared){
  const suffix=hash.sha256Value({jobId:prepared.id,packageSha256:prepared.packageSha256,nonce:randomNonce()}).slice(0,32),databaseName=`${DB_NAME}-restore-test-${suffix}`;let database=null,deleted=false,activated,verification,removedRows;try{database=await openIsolatedRestoreDatabase(databaseName);activated=await activatePreparedImport(prepared,{database,broadcast:false});verification=await verifyActivatedImportRows(database,prepared,activated);removedRows=await removeIsolatedRestoreLineage(database);database.close();database=null;await deleteIsolatedRestoreDatabase(databaseName);deleted=true;}finally{if(database)try{database.close();}catch{}if(!deleted)try{await deleteIsolatedRestoreDatabase(databaseName);}catch{}}
  if(!deleted)throw storageError('Isolated restore-test database cleanup did not complete.','BACKUP_RESTORE_ISOLATED_CLEANUP_FAILED');return {...verification,activationPath:'SHARED_IMPORT_ACTIVATION_TRANSACTION',activationStore:'ISOLATED_INDEXEDDB',isolatedDatabaseDistinctFromActive:true,activeDatabaseWriteCount:0,temporaryLineageRemoved:Object.values(removedRows||{}).every(count=>count===0),isolatedDatabaseDeleted:true};
}
async function runMemoryIsolatedActivation(prepared){
  const projects=new Map(),artifacts=new Map(),meta=new Map(),io={getProject:async id=>clone(projects.get(id)),getAllArtifacts:async()=>[...artifacts.values()].map(clone),deleteArtifact:async id=>{artifacts.delete(id);},putProject:async row=>{projects.set(row.jobId,clone(row));},putArtifact:async row=>{artifacts.set(row.artifactId,{...clone(row),blob:row.blob});},putMeta:async(key,value)=>{meta.set(key,clone(value));}},activated=await applyPreparedImportTransaction(prepared,io),projectRow=projects.get(prepared.id);if(!projectRow||projectSha256(projectRow.project)!==activated.projectSha256||artifacts.size!==prepared.verifiedArtifacts.length)throw storageError('Memory restore-test adapter did not persist the exact validated rows.','BACKUP_RESTORE_PROJECT_ROW_MISMATCH');for(const row of artifacts.values()){const bytes=new Uint8Array(await row.blob.arrayBuffer()),digest=await hash.sha256Bytes(bytes);if(bytes.byteLength!==Number(row.byteSize)||digest!==String(row.sha256))throw storageError('Memory restore-test adapter artifact read-back failed.','BACKUP_RESTORE_ARTIFACT_ROW_MISMATCH');}projects.clear();artifacts.clear();meta.clear();return {restoredProjectRowCount:1,restoredArtifactRowCount:prepared.verifiedArtifacts.length,stagingRowCount:0,activatedProjectSha256:activated.projectSha256,activationPath:'SHARED_IMPORT_ACTIVATION_TRANSACTION',activationStore:'ISOLATED_MEMORY_TEST_ADAPTER',isolatedDatabaseDistinctFromActive:true,activeDatabaseWriteCount:0,temporaryLineageRemoved:projects.size===0&&artifacts.size===0&&meta.size===0,isolatedDatabaseDeleted:true};
}
async function runIsolatedRestoreTest(blob,{milestone='MANUAL'}={}){
  const prepared=await prepareImportPackage(blob,{mode:'RESTORE'}),candidate=await validateBackupArtifactCandidate(prepared.sourceProject,prepared.packageArtifacts,{milestone}),activation=globalThis.indexedDB?await runIndexedDbIsolatedActivation(prepared):await runMemoryIsolatedActivation(prepared);if(!activation.temporaryLineageRemoved||!activation.isolatedDatabaseDeleted||activation.activeDatabaseWriteCount!==0)throw storageError('Restore test did not prove isolated activation and complete temporary-lineage cleanup.','BACKUP_RESTORE_ISOLATION_FAILED');const evidence={...candidate.restoreBinding,contract:'closed-loop-backup-restore-test/1',restoreBindingSha256:candidate.restoreBindingSha256,sourceProjectSha256:candidate.sourceProjectSha256,restoredProjectSha256:candidate.restoredProjectSha256,activatedProjectSha256:activation.activatedProjectSha256,roundTripEncoding:'UTF-8_JSON_GZIP',artifactRehydration:'DECODED_REHASHED_AND_READ_BACK',importValidationContract:'SHARED_COMPLETE_IMPORT_VALIDATION',activationPath:activation.activationPath,activationStore:activation.activationStore,restoredProjectRowCount:activation.restoredProjectRowCount,restoredArtifactRowCount:activation.restoredArtifactRowCount,stagingRowCount:activation.stagingRowCount,isolatedDatabaseDistinctFromActive:activation.isolatedDatabaseDistinctFromActive,activeDatabaseWriteCount:activation.activeDatabaseWriteCount,temporaryLineageRemoved:activation.temporaryLineageRemoved,isolatedDatabaseDeleted:activation.isolatedDatabaseDeleted,packageSha256:prepared.packageSha256,status:'SUCCEEDED'};return {...evidence,restoreTestEvidenceSha256:hash.sha256Value(evidence)};
}
async function validateBackupRestoreCandidate(project,artifactEntries,{milestone='MANUAL'}={}){const candidate=await validateBackupArtifactCandidate(project,artifactEntries,{milestone}),payload=buildProjectPackagePayload(project,clone(artifactEntries),now(),{disclosureHandling:'NO_SENSITIVE_MATERIAL'}),expanded=new TextEncoder().encode(JSON.stringify(payload));if(expanded.byteLength>STORE_LIMITS.maxImportExpandedBytes)throw storageError('Backup restore candidate exceeds the centralized expanded import limit.','BACKUP_RESTORE_RESOURCE_LIMIT');const compressed=await compressBytes(expanded);if(compressed.byteLength>STORE_LIMITS.maxImportCompressedBytes)throw storageError('Backup restore candidate exceeds the centralized compressed import limit.','BACKUP_RESTORE_RESOURCE_LIMIT');const proof=await runIsolatedRestoreTest(new Blob([compressed],{type:'application/gzip'}),{milestone});if(String(proof.restoreBindingSha256)!==String(candidate.restoreBindingSha256))throw storageError('Full isolated restore result does not match the candidate backup binding.','BACKUP_RESTORE_BINDING_MISMATCH');return proof;}
async function importPackage(blob,{mode='RESTORE'}={}){const prepared=await prepareImportPackage(blob,{mode});const activated=await activatePreparedImport(prepared,{broadcast:true});return activated.project;}

async function createExecutionPackage({project=null,jobId=null,stage,operation=null,testIds=[],productId=null,runId=null,reviewerAliasContext=null,instructionId=null,operationReservationId=null,disclosureAuthorization=null}={}){
  if(!project&&jobId){const all=await readAll();project=all.find(item=>projectIdentity(item)===String(jobId))||null;}
  if(!project||typeof project!=='object')throw new Error('A canonical project is required for an execution package.');
  const engine=globalThis.closedLoopWorkflowEngine,promptEngine=globalThis.closedLoopPromptEngine,canonicalJobId=projectIdentity(project);if(jobId&&String(jobId)!==canonicalJobId)throw storageError(`Execution-package job ${jobId} does not match canonical project ${canonicalJobId}.`,'EXECUTION_PACKAGE_JOB_MISMATCH');
  const normalizedStage=Number(stage),normalizedOperation=String(operation||globalThis.closedLoopWorkflowSchema?.STAGE_CONTRACTS?.[normalizedStage]?.operations?.[0]||'COMPLETE'),normalizedRunId=runId?String(runId):null,requestedTestIds=[...new Set(testIds.map(String).filter(Boolean))];
  if(!promptEngine?.responseContractDescriptor||typeof promptEngine.applyBlindReviewAliases!=='function')throw storageError('The prompt authority or its shared blind-alias projection is unavailable for execution-package construction.','EXECUTION_PACKAGE_PROMPT_AUTHORITY_UNAVAILABLE');
  const prompts=(project.projectData?.generatedPrompts||[]).filter(record=>Number(record?.stage)===normalizedStage&&!record?.invalidatedBy&&String(record?.operation||'COMPLETE')===normalizedOperation&&String(record?.promptEngineVersion||'')===String(promptEngine.version||''));
  const lanePrompts=prompts.filter(record=>!normalizedRunId||String(record?.scope?.runId||'')===normalizedRunId),selectedPrompt=instructionId?lanePrompts.find(record=>String(record?.instructionId||record?.promptId||'')===String(instructionId)):lanePrompts.at(-1);
  if(!selectedPrompt)throw storageError('Save the current controlling instruction before preparing this execution package. No current saved instruction exists for this exact stage, operation, and run lane.','EXECUTION_PACKAGE_CURRENT_PROMPT_REQUIRED');
  const promptTestIds=[...new Set((selectedPrompt.contextManifest?.readCollections?.tests||[]).map(item=>String(item?.id||'')).filter(Boolean))],sortedPromptTestIds=[...promptTestIds].sort(),sortedRequestedTestIds=[...requestedTestIds].sort();if(requestedTestIds.length&&hash.sha256Value(sortedRequestedTestIds)!==hash.sha256Value(sortedPromptTestIds))throw storageError('The requested execution-package test set must exactly equal the current prompt test allowlist. Generate a distinct reservation and prompt for a different test set.','EXECUTION_PACKAGE_TEST_ALLOWLIST_MISMATCH');const ids=sortedPromptTestIds;
  const exactPrompt=String(selectedPrompt.prompt||'');if(!exactPrompt)throw storageError('The saved controlling instruction has no exact prompt text.','EXECUTION_PACKAGE_PROMPT_TEXT_MISSING');
  const fullTextSha256=hash.sha256Text(exactPrompt);if(selectedPrompt.fullTextSha256&&String(selectedPrompt.fullTextSha256)!==fullTextSha256)throw storageError('The saved controlling instruction text no longer matches its recorded identity.','EXECUTION_PACKAGE_PROMPT_IDENTITY_MISMATCH');
  const responseContract=promptEngine.responseContractDescriptor(normalizedStage,normalizedOperation),contractSha256=hash.sha256Value(responseContract);if(String(selectedPrompt.contractSha256||'')!==contractSha256)throw storageError('The saved controlling instruction response contract is stale. Save the current instruction again before preparing the package.','EXECUTION_PACKAGE_CONTRACT_STALE');
  if(normalizedRunId&&String(selectedPrompt.scope?.runId||'')!==normalizedRunId)throw storageError('The saved controlling instruction is bound to a different run lane.','EXECUTION_PACKAGE_RUN_MISMATCH');
  const reservations=engine.records(project,'operationReservations',{active:false}).filter(record=>['ACTIVE','RESUMED','RESERVED'].includes(String(engine.recordValue(record,'STATUS')||record.status||'').toUpperCase())).filter(record=>Number(engine.recordValue(record,'STAGE')||record.stage)===normalizedStage&&String(engine.recordValue(record,'OPERATION')||record.operation||'COMPLETE')===normalizedOperation),reservation=operationReservationId?reservations.find(record=>engine.recordId(record,'operationReservations')===String(operationReservationId)):reservations.find(record=>String(engine.recordValue(record,'PROMPT_IDENTITY')||'')===String(selectedPrompt.instructionId||selectedPrompt.promptId||''))||reservations.at(-1);if(!reservation)throw storageError('Create and persist one current operation reservation before generating the prompt and execution package.','EXECUTION_PACKAGE_RESERVATION_REQUIRED');const reservationId=engine.recordId(reservation,'operationReservations'),packageId=String(engine.recordValue(reservation,'PACKAGE_ID')||''),challengeNonce=String(engine.recordValue(reservation,'CHALLENGE_NONCE')||''),targetSlot=String(engine.recordValue(reservation,'TARGET_SLOT')||''),scopeSha256=String(engine.recordValue(reservation,'SCOPE_HASH')||hash.sha256Value(selectedPrompt.scope||{})),reservationPromptIdentity=String(engine.recordValue(reservation,'PROMPT_IDENTITY')||''),selectedPromptIdentity=String(selectedPrompt.instructionId||selectedPrompt.promptId||''),reservationManifestHash=String(engine.recordValue(reservation,'PACKAGE_MANIFEST_HASH')||'');if(!packageId||!reservationId||!/^[0-9a-f]{32}$/i.test(challengeNonce)||!targetSlot)throw storageError('The controlling reservation is incomplete or has no 128-bit challenge nonce.','EXECUTION_PACKAGE_RESERVATION_INVALID');if(reservationPromptIdentity&&reservationPromptIdentity!==selectedPromptIdentity)throw storageError('The selected instruction is not the prompt identity frozen in the operation reservation.','EXECUTION_PACKAGE_RESERVATION_PROMPT_MISMATCH');if(Number(selectedPrompt.scope?.projectRevision)!==Number(project.revision||0)||scopeSha256!==hash.sha256Value(selectedPrompt.scope||{}))throw storageError('The saved prompt is not bound to the current reservation revision and scope. Regenerate it after reserving the operation.','EXECUTION_PACKAGE_PROMPT_RESERVATION_STALE');const expectedBinding={packageId,operationReservationId:reservationId,challengeNonce,projectRevision:Number(project.revision||0),scopeSha256,targetSlot};if(hash.sha256Value(selectedPrompt.operationBinding||selectedPrompt.contextManifest?.operationBinding||null)!==hash.sha256Value(expectedBinding))throw storageError('The saved prompt does not contain the exact current operation binding. Regenerate and save the prompt before packaging.','EXECUTION_PACKAGE_PROMPT_BINDING_MISMATCH');
  const reservationDescriptor=engine.recordValue(reservation,'PACKAGE_DESCRIPTOR'),reservationArtifactInputs=engine.recordValue(reservation,'REQUIRED_ARTIFACT_INPUTS'),reservationArtifactInputSetSha256=String(engine.recordValue(reservation,'REQUIRED_ARTIFACT_INPUT_SET_SHA256')||'');if(!reservationManifestHash||!reservationDescriptor||typeof reservationDescriptor!=='object'||!Array.isArray(reservationArtifactInputs)||!reservationArtifactInputSetSha256)throw storageError('The canonical operation reservation is not bound to an immutable package descriptor and exact artifact-input set. Cancel it and reserve again with packageDescriptor.','EXECUTION_PACKAGE_DESCRIPTOR_UNBOUND');if(packageId!==`PACKAGE-${reservationManifestHash.slice(0,24).toUpperCase()}`||hash.sha256Value(reservationDescriptor)!==reservationManifestHash)throw storageError('PACKAGE_ID or canonical descriptor bytes do not match the reservation PACKAGE_MANIFEST_HASH.','EXECUTION_PACKAGE_ID_DESCRIPTOR_MISMATCH');
  const savedPlan=selectedPrompt.contextManifest?.executionHandoff;if(!savedPlan||!Array.isArray(savedPlan.send)||!Array.isArray(savedPlan.withhold)||!Array.isArray(savedPlan.expectBack))throw storageError('The current prompt has no complete application-derived execution handoff allowlist. Regenerate and save the prompt.','EXECUTION_PACKAGE_HANDOFF_MISSING');const recomputedPlan=promptEngine.executionHandoffProjection?.(project,normalizedStage,normalizedOperation,selectedPrompt.contextManifest?.verificationBatchPlan||null);if(!recomputedPlan)throw storageError('The prompt authority cannot recalculate the current execution handoff.','EXECUTION_PACKAGE_HANDOFF_AUTHORITY_UNAVAILABLE');const planProjection=value=>({send:clone(value.send||[]),withhold:clone(value.withhold||[]),expectBack:clone(value.expectBack||[])});if(hash.sha256Value(planProjection(savedPlan))!==hash.sha256Value(planProjection(recomputedPlan)))throw storageError('The saved prompt execution handoff is stale or no longer matches the exact current allowlist.','EXECUTION_PACKAGE_HANDOFF_STALE');const plan=planProjection(savedPlan),listedArtifactIds=plan.send.map(x=>String(x.artifactId||'')).filter(Boolean),artifactIds=[...new Set(listedArtifactIds)],artifactEntries=[];if(artifactIds.length!==listedArtifactIds.length)throw storageError('The execution-package handoff contains duplicate artifact identities.','EXECUTION_PACKAGE_ALLOWLIST_MISMATCH');if(artifactIds.length>STORE_LIMITS.maxPackageArtifacts)throw storageError('Execution package exceeds the centralized package-artifact limit.','PACKAGE_ARTIFACT_LIMIT');
  for(const artifactId of artifactIds){
    const canonical=engine.records(project,'artifacts').find(r=>engine.recordId(r,'artifacts')===artifactId&&engine.isActiveRecord(r));if(!canonical)throw storageError(`Execution-package artifact ${artifactId} is not current canonical state.`,'EXECUTION_PACKAGE_ARTIFACT_STALE');const row=await getArtifact(artifactId);if(!row||String(row.jobId)!==canonicalJobId)throw storageError(`Execution-package artifact ${artifactId} has no stored bytes for ${canonicalJobId}.`,'EXECUTION_PACKAGE_BYTES_MISSING');const bytes=new Uint8Array(await row.blob.arrayBuffer()),sha256=await hash.sha256Bytes(bytes),byteSize=bytes.byteLength,expectedSha=String(engine.recordValue(canonical,'SHA256')||''),expectedSize=Number(engine.recordValue(canonical,'BYTE_SIZE'));if(sha256!==expectedSha||byteSize!==expectedSize)throw storageError(`Execution-package artifact ${artifactId} failed byte identity verification.`,'EXECUTION_PACKAGE_ARTIFACT_MISMATCH');const filename=String(engine.recordValue(canonical,'FILENAME')||row.filename||artifactId),classification=disclosureClassification(canonical),secretScan=scanKnownSecrets(filename,bytes);if(!secretScan.complete||secretScan.bytesScanned!==byteSize)throw storageError(`Execution package blocked because the complete ${artifactId} byte set was not scanned for known secret patterns.`,'EXECUTION_PACKAGE_SECRET_SCAN_INCOMPLETE');if(classification==='CREDENTIAL_SECRET'||secretScan.findings.length)throw storageError(`Execution package blocked because ${artifactId} is credential/secret material or matched a high-risk secret pattern.`,'EXECUTION_PACKAGE_SECRET_BLOCKED');if(['RESTRICTED','UNKNOWN'].includes(classification)){const authorizedIds=new Set((disclosureAuthorization?.artifactIds||[]).map(String)),suitable=String(disclosureAuthorization?.recipientSuitability||'').toUpperCase()==='ACCEPTED',humanDecisionId=String(disclosureAuthorization?.humanAuthorizationId||'');if(!authorizedIds.has(artifactId)||!suitable||!humanDecisionId)throw storageError(`Execution package blocked because ${artifactId} is ${classification} and lacks current human disclosure authorization and recipient suitability.`,'EXECUTION_PACKAGE_DISCLOSURE_BLOCKED');}
    artifactEntries.push({artifactId,filename,mediaType:String(row.mediaType||'application/octet-stream'),byteSize,sha256,role:String(engine.recordValue(canonical,'ROLE')||''),disclosureClassification:classification,secretScan:{...secretScan,clear:secretScan.complete&&!secretScan.findings.length,epistemicLimit:'Heuristic scan is not proof that no secret exists.'},base64:bytesToBase64(bytes)});
  }
  assertNoFilenameCollisions(artifactEntries);
  if(artifactEntries.length!==artifactIds.length||artifactEntries.some(entry=>!artifactIds.includes(entry.artifactId)))throw storageError('Execution-package allowlist reconciliation failed.','EXECUTION_PACKAGE_ALLOWLIST_MISMATCH');
  await preflightStorage(artifactEntries.reduce((sum,entry)=>sum+Number(entry.byteSize||0),0));
  const tests=engine.records(project,'tests').filter(t=>ids.includes(engine.recordId(t,'tests'))).map(t=>({testId:engine.recordId(t,'tests'),requirementId:String(engine.recordValue(t,'REQ_ID')||t.relationships?.REQ_ID||''),recordSha256:String(t.recordSha256||t.sha256||''),fields:clone(t.fields||{}),relationships:clone(t.relationships||{})}));if(tests.length!==ids.length)throw storageError('The execution-package test allowlist does not resolve one current test record per identity.','EXECUTION_PACKAGE_TEST_ALLOWLIST_MISMATCH');
  const promptAliases=Array.isArray(selectedPrompt.contextManifest?.blindAliasMap)?selectedPrompt.contextManifest.blindAliasMap:[],providedAlias=reviewerAliasContext&&typeof reviewerAliasContext==='object'?reviewerAliasContext:null;if(providedAlias&&!promptAliases.some(entry=>hash.sha256Value({canonicalId:String(entry?.canonicalId||''),alias:String(entry?.alias||entry?.reviewerAlias||'')})===hash.sha256Value({canonicalId:String(providedAlias?.canonicalId||''),alias:String(providedAlias?.alias||providedAlias?.reviewerAlias||'')})))throw storageError('Caller-provided reviewer alias context is not the exact prompt-controlled blind alias.','EXECUTION_PACKAGE_ALIAS_MISMATCH');const reviewerAliasEntries=promptAliases,targetAlias=selectedPrompt.contextManifest?.operationTargetAlias&&typeof selectedPrompt.contextManifest.operationTargetAlias==='object'?selectedPrompt.contextManifest.operationTargetAlias:null,aliasEntries=[...reviewerAliasEntries,...(targetAlias?[targetAlias]:[])].filter((entry,index,list)=>entry?.canonicalId&&entry?.alias&&list.findIndex(item=>String(item?.canonicalId||'')===String(entry.canonicalId))===index),reviewerAlias=String(reviewerAliasEntries[0]?.alias||reviewerAliasEntries[0]?.reviewerAlias||'').trim()||null;
  const publicValue=value=>promptEngine.applyBlindReviewAliases(value,aliasEntries,'PUBLIC'),publicIdentity=value=>publicValue(value),publicScope=publicValue(selectedPrompt.scope||{}),publicBinding=publicValue(expectedBinding);
  const instruction={instructionId:String(selectedPrompt.instructionId||selectedPrompt.promptId||''),promptEngineVersion:String(selectedPrompt.promptEngineVersion||''),bodySha256:String(selectedPrompt.bodySha256||selectedPrompt.sha256||''),contractSha256:String(selectedPrompt.contractSha256||''),contextSignature:String(selectedPrompt.contextSignature||''),scope:clone(publicScope),operationBinding:clone(publicBinding),fullTextSha256,text:exactPrompt};
  const descriptor=normalizeExecutionPackageDescriptor({jobId:canonicalJobId,stage:normalizedStage,operation:normalizedOperation,targetSlot,scopeSha256,promptIdentity:instruction.instructionId,promptFullTextSha256:fullTextSha256,testIds:ids,testRecordHashes:tests.map(test=>test.recordSha256),artifacts:artifactEntries.map(entry=>({...entry,availability:String(plan.send.find(item=>String(item.artifactId||'')===entry.artifactId)?.availability||'')})),filesToWithhold:plan.withhold,expectedReturnFiles:plan.expectBack,disclosureAuthorization,reviewerAliasContextSha256:hash.sha256Value(aliasEntries),runId:normalizedRunId,productId:productId||project.job?.CURRENT_PRODUCT_ID||null});if(hash.sha256Value(descriptor)!==hash.sha256Value(reservationDescriptor))throw storageError('The exact package descriptor no longer equals the descriptor frozen in canonical reservation state.','EXECUTION_PACKAGE_DESCRIPTOR_CANONICAL_MISMATCH');const descriptorBinding=await freezeExecutionPackageDescriptor({operationReservationId:reservationId,packageId,descriptor,requiredSha256:reservationManifestHash});
  const expectedStructuredResponse=JSON.parse(promptEngine.responseContract(normalizedStage,normalizedOperation,instruction.instructionId,instruction.bodySha256,instruction.contractSha256,instruction.contextSignature,publicScope,canonicalJobId,publicBinding)),manifestCore={schema:'closed-loop-verification-package/1',packageId,operationReservationId:reservationId,challengeNonce,workflow:project.workflow,projectSchema:project.schema,responseSchema:globalThis.closedLoopWorkflowSchema?.RESPONSE_SCHEMA,jobId:canonicalJobId,stage:normalizedStage,operation:normalizedOperation,currentProjectRevision:Number(project.revision||0),scope:clone(publicScope),scopeSha256,packageDescriptorSha256:descriptorBinding.descriptorSha256,packageDescriptorBindingBasis:descriptorBinding.bindingBasis,packageValidityCondition:'Valid only while the bound project revision, scope, prompt identity, operation reservation, nonce, exact descriptor, bytes, and disclosure authorization remain current.',runId:publicIdentity(normalizedRunId),reviewerAlias,productId:publicIdentity(productId||project.job?.CURRENT_PRODUCT_ID||null),testIds:ids,instructionId:instruction.instructionId,instructionFullTextSha256:fullTextSha256,responseContractSha256:contractSha256,artifacts:artifactEntries.map(({base64,filenameContract,...x})=>publicValue({...x,canonicalPath:filenameContract?.canonicalPath||x.filename,filenameNormalizationVersion:'closed-loop-filename/1'})),filesToWithhold:publicValue(plan.withhold||[]),expectedReturnContract:{files:publicValue(plan.expectBack||[]),structuredResponse:expectedStructuredResponse},disclosureAuthorizationBasis:normalizedDisclosureAuthorization(disclosureAuthorization),createdAt:descriptorBinding.createdAt},manifestSha256=hash.sha256Value(manifestCore),manifest={...manifestCore,manifestSha256},body={manifest,instruction,responseContract,expectedStructuredResponse,tests:publicValue(tests),artifacts:publicValue(artifactEntries)},publicBodyText=JSON.stringify(body);for(const entry of reviewerAliasEntries){const canonicalId=String(entry?.canonicalId||'');if(canonicalId&&publicBodyText.includes(canonicalId))throw storageError(`Blind execution package still exposes canonical identity ${canonicalId}.`,'EXECUTION_PACKAGE_BLIND_ALIAS_LEAK');}const packageSha256=hash.sha256Value(body),payload={...body,packageSha256},expandedBytes=new TextEncoder().encode(JSON.stringify(payload));if(expandedBytes.byteLength>STORE_LIMITS.maxImportExpandedBytes)throw storageError('Execution package exceeds the centralized expanded-size limit.','PACKAGE_RESOURCE_LIMIT');const compressed=await compressBytes(expandedBytes);if(compressed.byteLength>STORE_LIMITS.maxImportCompressedBytes)throw storageError('Execution package exceeds the centralized compressed-size limit.','PACKAGE_RESOURCE_LIMIT');return {blob:new Blob([compressed],{type:'application/gzip'}),filename:`VERIFY-${canonicalJobId}-STAGE-${String(normalizedStage).padStart(2,'0')}.clverify.gz`,manifest,packageSha256,packageId,operationReservationId:reservationId,challengeNonce,operationBinding:publicBinding};
}
async function storageHealth(){let persistent=false,estimate={usage:null,quota:null};try{persistent=await navigator.storage.persist();estimate=await navigator.storage.estimate();}catch{}return {database:DB_NAME,persistent:Boolean(persistent),usage:estimate.usage??null,quota:estimate.quota??null,integrityClaim:'INTEGRITY_CHECKED_NOT_TAMPER_PROOF',browserLocalCustodyLimit:'Device, browser profile, origin, or storage loss remains possible.',lastCommittedRevision:await metaGet('lastCommittedRevision'),lastVerifiedExport:await metaGet('lastVerifiedExport'),lastArtifactRecovery:await metaGet('lastArtifactRecovery'),migrationStatus:await metaGet('migrationStatus')};}
function archiveMigrationPayload(project,archive){if(!project||typeof project!=='object')throw new TypeError('A project is required.');project.projectData=project.projectData&&typeof project.projectData==='object'?project.projectData:{};project.projectData.migrationArchives=Array.isArray(project.projectData.migrationArchives)?project.projectData.migrationArchives:[];const record={...clone(archive),operational:false};project.projectData.migrationArchives.push(record);return record;}
function clearLegacy(storage=globalThis.localStorage){if(!storage)return;for(const key of LEGACY_KEYS)try{storage.removeItem(key);}catch{}}

const ready=(async()=>{if(globalThis.indexedDB)try{await migrateLegacy();globalThis.closedLoopLegacyMigrationError=null;await recoverArtifactLifecycle();const projects=await readAll();for(const project of projects){const active=(project.projectData?.operationReservations||[]).some(record=>record?.active!==false&&!record?.invalidatedBy&&String(globalThis.closedLoopWorkflowEngine?.recordValue?.(record,'STATUS')||record.status||'').toUpperCase()==='ACTIVE');if(active)await orphanOperationReservationsPersistent({jobId:projectIdentity(project),expectedProjectRevision:Number(project.revision||0)});}}catch(error){globalThis.closedLoopLegacyMigrationError=String(error?.stack||error);console.error('Store startup reconciliation failed without deleting preserved project or staged bytes; application startup will continue.',error);}return true;})();
globalThis.closedLoopProjectStore=Object.freeze({archiveMigrationPayload,version:'closed-loop-project-store/70',DB_NAME,DB_VERSION,stores:Object.freeze({projects:PROJECTS,artifacts:ARTIFACTS,artifactStaging:STAGING,meta:META}),limits:STORE_LIMITS,STORE_KEY,LEGACY_KEYS,clone,projectIdentity,projectSha256,validateProjectIntegrity,canonicalFilename,assertNoFilenameCollisions,preflightStorage,normalizeExecutionPackageDescriptor,executionPackageDescriptorSha256,openDatabase,ready,readAll,writeAll,writeProject,replaceProject,transact,removeProject,stageArtifact,promoteStagedAttachmentsAtomic,commitResponseWithStagedAttachments,recoverArtifactLifecycle,putArtifact,getArtifact,deleteArtifact,listArtifacts,verifyProjectArtifacts,createDeliveryRecordPersistent,reserveOperationPersistent,resumeOperationReservationPersistent,cancelOperationReservationPersistent,acceptOperationReservationPersistent,orphanOperationReservationsPersistent,createExecutionPackage,backupMilestoneFor,evaluateBackupExportDisclosure,backupArtifactVerificationManifest,validateBackupRestoreCandidate,prepareImportPackage,runIsolatedRestoreTest,authorizeUnencryptedSensitiveExport,confirmBackupExternalCustodyPersistent,exportPackage,importPackage,cloneProjectLineage,storageHealth,metaGet,metaPut,clearLegacy});
})();
;(()=>{
'use strict';
const CLOSED_LOOP_V3_STORE_MIGRATION_EXPORT=true;
const store=globalThis.closedLoopProjectStore;
const schema=globalThis.closedLoopWorkflowSchema;
if(store&&schema&&typeof schema.migrateProjectToCurrent==='function'&&typeof store.migrateProjectToCurrent!=='function')globalThis.closedLoopProjectStore=Object.freeze({...store,migrateProjectToCurrent:project=>schema.migrateProjectToCurrent(project)});
})();
