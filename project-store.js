(()=>{
'use strict';

const DB_NAME='closed-loop-reliability';
const DB_VERSION=1;
const PROJECTS='projects';
const ARTIFACTS='artifacts';
const META='meta';
const ARTIFACT_LIFECYCLE=Object.freeze({PENDING_BYTES:'PENDING_BYTES',HASHED_AND_REVERIFIED:'HASHED_AND_REVERIFIED',READY_FOR_PROMOTION:'READY_FOR_PROMOTION',CANONICAL:'CANONICAL',UNAVAILABLE:'UNAVAILABLE'});
const FILENAME_NORMALIZATION_VERSION='closed-loop-filename/1';
const FORMAT_SNIFFER_VERSION='closed-loop-format-sniffer/1';
const SECURITY_LIMITS=Object.freeze({
  storageSafetyMarginBytes:16*1024*1024,
  maxPackageCompressedBytes:64*1024*1024,
  maxPackageExpandedBytes:256*1024*1024,
  maxArtifactCount:2048,
  maxArtifactBytes:128*1024*1024,
  maxJsonDepth:64,
  maxJsonNodes:1_000_000,
  maxArrayLength:100_000,
  /* Base64 for one maximum-size artifact is about 171 MiB. Keep the string
     limit explicit and below the expanded-package limit without silently
     making the per-artifact limit unreachable. */
  maxStringLength:180*1024*1024,
  maxHistoryRecords:250_000,
  maxExtensionBytes:16*1024*1024,
  maxRelationships:2_000_000,
  maxFilenameBytes:4096,
  maxPathDepth:64,
  maxPathSegmentCodePoints:255
});
const LEGACY_KEYS=Object.freeze(['closed-loop-reliability-projects-v4','closed-loop-reliability-projects-v3','closed-loop-reliability-projects-v2','closed-loop-reliability-projects']);
const STORE_KEY=LEGACY_KEYS[0];
const hash=globalThis.closedLoopHash;
const clone=value=>value===undefined?undefined:(typeof structuredClone==='function'?structuredClone(value):JSON.parse(JSON.stringify(value)));
const projectIdentity=project=>String(project?.job?.JOB_ID||project?.jobId||'').trim();
const now=()=>new Date().toISOString();
const fault=phase=>{const configured=globalThis.__closedLoopStorageFault;if(configured===phase||configured?.phase===phase){const error=new Error(`Injected storage failure at ${phase}.`);error.code='INJECTED_STORAGE_FAILURE';throw error;}};
const request=req=>new Promise((resolve,reject)=>{req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error||new Error('IndexedDB request failed.'));});
const complete=tx=>new Promise((resolve,reject)=>{tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error||new Error('IndexedDB transaction failed.'));tx.onabort=()=>reject(tx.error||new Error('IndexedDB transaction aborted.'));});
const canonicalProject=project=>{const copy=clone(project);delete copy.projectSha256;return copy;};
const projectSha256=project=>hash.sha256Value(canonicalProject(project));
const storageError=(message,code)=>Object.assign(new Error(message),{code});
const artifactLifecycle=row=>String(row?.lifecycleStatus||ARTIFACT_LIFECYCLE.CANONICAL);
const utf8Length=value=>new TextEncoder().encode(String(value??'')).byteLength;
const WINDOWS_RESERVED_SEGMENT=/^(?:con|prn|aux|nul|com[1-9]|lpt[1-9])(?:\..*)?$/i;
const CONFUSABLE_ASCII=Object.freeze({'\u0430':'a','\u0410':'a','\u03b1':'a','\u0391':'a','\u0435':'e','\u0415':'e','\u0395':'e','\u043e':'o','\u041e':'o','\u039f':'o','\u0440':'p','\u0420':'p','\u03a1':'p','\u0441':'c','\u0421':'c','\u03f2':'c','\u0445':'x','\u0425':'x','\u03a7':'x','\u0443':'y','\u0423':'y','\u03a5':'y','\u0456':'i','\u0406':'i','\u2170':'i','\u04cf':'l'});
/* Locale-independent upper/lower closure implements the expansion behavior
   needed by the pinned default-case-fold collision key (for example ß/SS,
   ligatures, and final sigma) without locale collation. */
const unicodeDefaultCaseFold=value=>String(value).normalize('NFC').toUpperCase().toLowerCase().normalize('NFC');
function normalizeArtifactPath(rawFilename){
  const raw=String(rawFilename??'');if(!raw)throw storageError('Artifact filename is required.','UNSAFE_ARTIFACT_PATH');if(utf8Length(raw)>SECURITY_LIMITS.maxFilenameBytes)throw storageError('Artifact filename exceeds the centralized byte limit.','UNSAFE_ARTIFACT_PATH');
  const slashes=raw.replaceAll('\\','/');if(slashes.startsWith('/')||slashes.startsWith('//')||/^[A-Za-z]:/.test(slashes))throw storageError(`Absolute artifact path is prohibited: ${raw}.`,'UNSAFE_ARTIFACT_PATH');if(/[\u0000-\u001f\u007f]/u.test(slashes))throw storageError(`Artifact path contains a NUL or control character: ${raw}.`,'UNSAFE_ARTIFACT_PATH');
  const rawSegments=slashes.split('/');if(!rawSegments.length||rawSegments.length>SECURITY_LIMITS.maxPathDepth||rawSegments.some(segment=>!segment||segment==='.'||segment==='..'))throw storageError(`Artifact path contains an empty, dot, traversal, or excessive-depth segment: ${raw}.`,'UNSAFE_ARTIFACT_PATH');
  const segments=rawSegments.map(segment=>{const normalized=segment.normalize('NFC');if([...normalized].length>SECURITY_LIMITS.maxPathSegmentCodePoints)throw storageError(`Artifact path segment is too long: ${segment}.`,'UNSAFE_ARTIFACT_PATH');if(/[. ]$/u.test(normalized)||WINDOWS_RESERVED_SEGMENT.test(normalized)||/[<>:"|?*]/u.test(normalized)||/[\u202a-\u202e\u2066-\u2069]/u.test(normalized))throw storageError(`Artifact path is unsafe or visually deceptive on a supported extraction platform: ${raw}.`,'UNSAFE_ARTIFACT_PATH');return normalized;});
  const canonicalPath=segments.join('/'),caseFoldKey=unicodeDefaultCaseFold(canonicalPath),platformRiskKey=segments.map(segment=>unicodeDefaultCaseFold(segment.replace(/[. ]+$/u,''))).join('/'),confusableKey=unicodeDefaultCaseFold(canonicalPath.normalize('NFKC')).replace(/[\u0300-\u036f]/gu,'').replace(/[\u0400-\u052f\u0370-\u03ff\u2170\u04cf]/gu,char=>CONFUSABLE_ASCII[char]||char);
  return Object.freeze({rawFilename:raw,displayFilename:segments.at(-1),canonicalPath,filenameNormalizationVersion:FILENAME_NORMALIZATION_VERSION,collisionKeys:Object.freeze({canonical:canonicalPath,caseFold:caseFoldKey,platformRisk:platformRiskKey,confusable:confusableKey})});
}
function assertNoArtifactPathCollisions(items,{label='artifact set'}={}){
  const entries=(items||[]).map(item=>({identity:String(item?.artifactId||item?.id||item?.ARTIFACT_ID||''),...normalizeArtifactPath(item?.filename??item?.rawFilename??item?.FILENAME??item?.canonicalPath??'')})),dimensions=['canonical','caseFold','platformRisk','confusable'];
  for(const dimension of dimensions){const seen=new Map();for(const entry of entries){const key=entry.collisionKeys[dimension],prior=seen.get(key);if(prior&&prior.identity!==entry.identity)throw storageError(`${label} contains a ${dimension} filename/path collision between ${prior.rawFilename} and ${entry.rawFilename}.`,'ARTIFACT_PATH_COLLISION');seen.set(key,entry);}}
  return entries;
}
const byteStartsWith=(bytes,signature)=>signature.every((value,index)=>bytes[index]===value);
function sniffArtifactFormat(bytes,filename='',declaredMediaType=''){
  const input=bytes instanceof Uint8Array?bytes:new Uint8Array(bytes||0),prefix=input.subarray(0,Math.min(input.length,8192)),text=(()=>{try{return new TextDecoder('utf-8',{fatal:true}).decode(prefix);}catch{return null;}})(),trimmed=text?.replace(/^\uFEFF/u,'').trimStart()||'',extension=String(filename).toLowerCase().match(/\.([a-z0-9]+)$/)?.[1]||'',declared=String(declaredMediaType||'').toLowerCase();let detected='BINARY_UNKNOWN',parser='BYTE_SIGNATURE_ONLY';
  if(byteStartsWith(prefix,[0x1f,0x8b]))detected='GZIP';else if(byteStartsWith(prefix,[0x50,0x4b,0x03,0x04])||byteStartsWith(prefix,[0x50,0x4b,0x05,0x06])||byteStartsWith(prefix,[0x50,0x4b,0x07,0x08]))detected='ZIP';else if(text?.startsWith('%PDF-'))detected='PDF';else if(byteStartsWith(prefix,[0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a]))detected='PNG';else if(byteStartsWith(prefix,[0xff,0xd8,0xff]))detected='JPEG';else if(trimmed.startsWith('{')||trimmed.startsWith('[')){parser='UTF8_JSON_CLOSED';try{const complete=new TextDecoder('utf-8',{fatal:true}).decode(input);globalThis.closedLoopResponseIngestion?.scanJsonAmbiguity?.(complete,SECURITY_LIMITS.maxJsonDepth);JSON.parse(complete);detected='JSON';}catch{detected='JSON_INVALID';}}else if(/^<\?xml\b|^<[A-Za-z_][^>]*>/u.test(trimmed)){parser='UTF8_XML_SAFE_PREFIX';try{const complete=new TextDecoder('utf-8',{fatal:true}).decode(input);detected=/<!DOCTYPE|<!ENTITY/i.test(complete)?'XML_UNSAFE_DECLARATION':'XML';}catch{detected='XML_INVALID';}}else if(text!==null){detected='UTF8_TEXT';parser='UTF8_STRICT';}
  const strictExpected={pdf:'PDF',png:'PNG',jpg:'JPEG',jpeg:'JPEG',zip:'ZIP',gz:'GZIP',json:'JSON',xml:'XML'}[extension]||({'application/pdf':'PDF','image/png':'PNG','image/jpeg':'JPEG','application/zip':'ZIP','application/gzip':'GZIP','application/json':'JSON','application/xml':'XML','text/xml':'XML'}[declared]||null),secondaryZip=input.length>4&&input.subarray(1).some((value,index)=>value===0x50&&input[index+2]===0x4b&&input[index+3]===0x03&&input[index+4]===0x04),polyglotStatus=strictExpected&&detected!==strictExpected||secondaryZip&&detected!=='ZIP'?'MISMATCHED_OR_AMBIGUOUS':detected==='BINARY_UNKNOWN'?'UNKNOWN':'CLEAR_FOR_REGISTERED_SNIFFER';
  return Object.freeze({detectedFormat:detected,parserAdapterIdentity:`${FORMAT_SNIFFER_VERSION}:${parser}`,parseContract:'BYTE_SIGNATURE_AND_STRICT_UTF8_PREFIX/1',polyglotStatus,strictExpectedFormat:strictExpected});
}
const SECRET_PATTERNS=Object.freeze([
  ['PRIVATE_KEY_BLOCK',/-----BEGIN (?:RSA |EC |OPENSSH |DSA )?PRIVATE KEY-----/i],
  ['AWS_ACCESS_KEY',/\b(?:AKIA|ASIA)[A-Z0-9]{16}\b/],
  ['GITHUB_TOKEN',/\bgh[pousr]_[A-Za-z0-9]{30,}\b/],
  ['GITLAB_TOKEN',/\bglpat-[A-Za-z0-9_-]{20,}\b/],
  ['SLACK_TOKEN',/\bxox[baprs]-[A-Za-z0-9-]{20,}\b/],
  ['STRIPE_SECRET_KEY',/\bsk_(?:live|test)_[A-Za-z0-9]{16,}\b/],
  ['OPENAI_API_KEY',/\bsk-(?:proj-)?[A-Za-z0-9_-]{20,}\b/],
  ['BEARER_TOKEN',/\bBearer\s+[A-Za-z0-9._~+/=-]{20,}/i],
  ['PASSWORD_ASSIGNMENT',/\b(?:password|passwd|pwd|client_secret|api[_-]?key)\s*[:=]\s*[^\s"']{8,}/i],
  ['JSON_CREDENTIAL_FIELD',/"(?:password|passwd|pwd|client_secret|api[_-]?key)"\s*:\s*"[^"\\]{8,}"/i]
]);
function scanArtifactForKnownSecrets(bytes,filename=''){
  const name=String(filename),findings=[];if(/(?:^|\/)(?:\.env(?:\..*)?|\.npmrc|\.pypirc|\.netrc|\.htpasswd|kubeconfig|id_(?:rsa|dsa|ecdsa|ed25519)|credentials(?:\.json)?|service[-_]?account.*\.json|secrets?(?:\..*)?|.*\.(?:pem|p12|pfx|key))$/i.test(name))findings.push({kind:'HIGH_RISK_FILENAME',location:'filename'});
  let text=null;try{text=new TextDecoder('utf-8',{fatal:true}).decode((bytes instanceof Uint8Array?bytes:new Uint8Array(bytes||0)).subarray(0,Math.min(Number(bytes?.byteLength||bytes?.length||0),2*1024*1024)));}catch{}
  if(text!==null)for(const [kind,pattern] of SECRET_PATTERNS)if(pattern.test(text))findings.push({kind,location:'content-prefix'});
  return Object.freeze({status:findings.length?'LEAKAGE_DETECTED':'NO_KNOWN_PATTERN_DETECTED_HEURISTIC_ONLY',findings:Object.freeze(findings),scanVersion:'closed-loop-secret-scan/1',completeAbsenceProven:false,bytesScanned:text===null?0:Math.min(Number(bytes?.byteLength||bytes?.length||0),2*1024*1024)});
}
function classifyOutboundPayload(value,{componentId='NON_ARTIFACT_PACKAGE_PAYLOAD',kind='NON_ARTIFACT_PACKAGE_PAYLOAD'}={}){
  const canonical=hash.stableStringify(value),findings=[];for(const [findingKind,pattern] of SECRET_PATTERNS)if(pattern.test(canonical))findings.push({kind:findingKind,location:'serialized-non-artifact-payload'});
  const declared=new Set(),walk=(item,depth=0)=>{if(depth>SECURITY_LIMITS.maxJsonDepth||item===null||item===undefined)return;if(Array.isArray(item)){for(const child of item)walk(child,depth+1);return;}if(typeof item!=='object')return;for(const [key,child] of Object.entries(item)){if(/^(?:DISCLOSURE_CLASSIFICATION|disclosureClassification)$/u.test(key)){const classification=String(child||'').toUpperCase();if(DISCLOSURE_CLASSIFICATIONS.has(classification))declared.add(classification);}walk(child,depth+1);}};walk(value);
  const classification=findings.length||declared.has('CREDENTIAL_SECRET')?'CREDENTIAL_SECRET':declared.has('RESTRICTED')?'RESTRICTED':declared.has('UNKNOWN')?'UNKNOWN':'UNKNOWN',byteSize=utf8Length(canonical),sha256=hash.sha256Value(value);
  return Object.freeze({componentId:String(componentId),kind:String(kind),byteSize,sha256,classification,secretScanStatus:findings.length?'LEAKAGE_DETECTED':'NO_KNOWN_PATTERN_DETECTED_HEURISTIC_ONLY',secretScanFindings:Object.freeze(findings),secretScanVersion:'closed-loop-outbound-payload-secret-scan/1',scanCompleteAbsenceProven:false,bytesScanned:byteSize,classificationBasis:findings.length?'KNOWN_CREDENTIAL_OR_SECRET_PATTERN':'UNKNOWN_BECAUSE_HEURISTIC_SCAN_CANNOT_PROVE_ABSENCE'});
}
async function storageCapacityPreflight(requiredBytes,{operation='storage operation'}={}){
  const needed=Math.max(0,Number(requiredBytes)||0)+SECURITY_LIMITS.storageSafetyMarginBytes;if(!globalThis.navigator?.storage?.estimate)return {status:'UNKNOWN',requiredBytes:needed,reason:'Browser storage estimate is unavailable; the operation remains atomic on quota failure.'};let estimate;try{estimate=await navigator.storage.estimate();}catch{return {status:'UNKNOWN',requiredBytes:needed,reason:'Browser storage estimate failed; the operation remains atomic on quota failure.'};}const quota=Number(estimate?.quota),usage=Number(estimate?.usage);if(Number.isFinite(quota)&&Number.isFinite(usage)&&quota-usage<needed)throw storageError(`${operation} requires approximately ${needed} free bytes including the centralized safety margin, but the browser reports ${Math.max(0,quota-usage)} available.`,'INSUFFICIENT_STORAGE_CAPACITY');return {status:Number.isFinite(quota)&&Number.isFinite(usage)?'SUFFICIENT_ESTIMATE':'UNKNOWN',requiredBytes:needed,usage:Number.isFinite(usage)?usage:null,quota:Number.isFinite(quota)?quota:null,reason:'A favorable estimate is not a quota guarantee; commit remains atomic.'};
}
const runSynchronousMutator=(mutator,next,before)=>{const result=mutator(next,before);if(result&&typeof result.then==='function')throw storageError('Project transaction mutators must be synchronous. Complete asynchronous work before opening the canonical IndexedDB transaction.','ASYNC_TRANSACTION_MUTATOR');return result;};
const PLACEHOLDER_REFERENCES=new Set(['','NONE','NOT APPLICABLE','UNKNOWN','PENDING','UNASSIGNED']);
const equivalent=(left,right)=>hash.sha256Value(left)===hash.sha256Value(right);
function validateProjectIntegrity(project,{verifyDerived=true}={}){
  const issues=[],schemaApi=globalThis.closedLoopWorkflowSchema,engine=globalThis.closedLoopWorkflowEngine;
  if(!project||typeof project!=='object')return {valid:false,issues:['Project is not an object.']};
  if(!schemaApi||!engine)return {valid:false,issues:['Workflow schema and engine are required for canonical project integrity validation.']};
  if(project.schema!==schemaApi.PROJECT_SCHEMA)issues.push(`Project schema ${project.schema||'UNKNOWN'} does not match ${schemaApi.PROJECT_SCHEMA}.`);
  if(project.workflow!==schemaApi.WORKFLOW_ID)issues.push(`Workflow ${project.workflow||'UNKNOWN'} does not match ${schemaApi.WORKFLOW_ID}.`);
  if(Number(project.stageCount)!==Number(schemaApi.STAGE_COUNT))issues.push(`Stage count ${project.stageCount} does not match ${schemaApi.STAGE_COUNT}.`);
  if(Object.keys(project.stages||{}).length!==Number(schemaApi.STAGE_COUNT))issues.push('Canonical project does not contain exactly the declared workflow stages.');
  if(!projectIdentity(project))issues.push('Canonical project has no JOB_ID.');
  const idsByCollection=new Map();
  for(const [collection,definition] of Object.entries(schemaApi.RECORD_SCHEMAS)){
    const list=project.projectData?.[collection];if(list!==undefined&&!Array.isArray(list)){issues.push(`${collection} is not an array.`);continue;}
    const seen=new Set();for(const record of Array.isArray(list)?list:[]){const id=engine.recordId(record,collection),fieldId=String(engine.recordValue(record,definition.idField)||'').trim();if(!id){issues.push(`${collection} contains a record without ${definition.idField}.`);continue;}if(seen.has(id))issues.push(`${collection} contains duplicate canonical ID ${id}.`);seen.add(id);if(record?.id&&fieldId&&String(record.id)!==fieldId)issues.push(`${collection} record ${id} contradicts its ${definition.idField}.`);if(record?.contentSha256&&String(record.contentSha256)!==String(hash.contentRecordSha256(record,definition.idField)))issues.push(`${collection} record ${id} contentSha256 does not match canonical content.`);if(record?.recordSha256&&String(record.recordSha256)!==String(hash.recordSha256(record)))issues.push(`${collection} record ${id} recordSha256 does not match the persisted canonical record.`);if(record?.recordSha256&&record?.sha256&&String(record.sha256)!==String(record.recordSha256))issues.push(`${collection} record ${id} sha256 does not mirror recordSha256.`);}
    idsByCollection.set(collection,seen);
  }
  const ingestion=globalThis.closedLoopResponseIngestion;
  if(!ingestion?.validateValue)issues.push('Response-ingestion value validator is required for canonical project integrity validation.');
  else for(const [collection,definition] of Object.entries(schemaApi.RECORD_SCHEMAS))for(const record of Array.isArray(project.projectData?.[collection])?project.projectData[collection]:[]){const id=engine.recordId(record,collection)||'UNKNOWN',nested=record?.fields&&typeof record.fields==='object'&&!Array.isArray(record.fields)?record.fields:null;for(const [fieldName,fieldDefinition] of Object.entries(definition.fieldDefinitions||{})){const nestedPresent=Boolean(nested&&Object.prototype.hasOwnProperty.call(nested,fieldName)),topPresent=Object.prototype.hasOwnProperty.call(record||{},fieldName);if(!nestedPresent&&!topPresent)continue;if(nestedPresent&&topPresent&&!equivalent(nested[fieldName],record[fieldName]))issues.push(`${collection} record ${id} has contradictory mirrored value for ${fieldName}.`);const value=engine.recordValue(record,fieldName),fieldIssues=[];ingestion.validateValue(fieldDefinition,value,`/${collection}/${id}/${fieldName}`,fieldIssues);for(const item of fieldIssues)issues.push(`${collection} record ${id} field ${fieldName}: ${item.message}`);}}
  for(const [collection,definition] of Object.entries(schemaApi.RECORD_SCHEMAS))for(const record of Array.isArray(project.projectData?.[collection])?project.projectData[collection]:[])for(const [fieldName,targetCollection] of Object.entries(definition.relationships||{})){const raw=record?.relationships?.[fieldName]??engine.recordValue(record,fieldName);for(const value of Array.isArray(raw)?raw:[raw]){const ref=String(value??'').trim();if(PLACEHOLDER_REFERENCES.has(ref.toUpperCase()))continue;if(!idsByCollection.get(targetCollection)?.has(ref))issues.push(`${collection}.${fieldName} references missing ${targetCollection} record ${ref}.`);}}
  let previousSequence=0;const eventIds=new Set();for(const event of Array.isArray(project.projectData?.history)?project.projectData.history:[]){const sequence=Number(event?.eventSequence);if(!Number.isInteger(sequence)||sequence<=previousSequence)issues.push('History eventSequence is missing, duplicated, or non-monotonic.');previousSequence=Math.max(previousSequence,Number.isFinite(sequence)?sequence:0);const eventId=String(event?.eventId||'');if(eventId&&eventIds.has(eventId))issues.push(`History contains duplicate event ID ${eventId}.`);if(eventId)eventIds.add(eventId);}if(Number(project.projectData?.eventSequence||0)<previousSequence)issues.push('Project eventSequence is behind committed history.');
  if(verifyDerived){const expected=clone(project);delete expected.projectSha256;engine.ensureShape(expected);engine.recalculate(expected);for(const name of ['CURRENT_STAGE','CURRENT_STATE','CURRENT_BLOCKERS','NEXT_REQUIRED_ACTION','JOB_RECORD_STATUS','STATUS_EVIDENCE'])if(!equivalent(project.job?.[name],expected.job?.[name]))issues.push(`Application-derived job field ${name} does not match deterministic recalculation.`);for(let stage=1;stage<=Number(schemaApi.STAGE_COUNT);stage++){if(String(project.stages?.[stage]?.status||'')!==String(expected.stages?.[stage]?.status||''))issues.push(`Stage ${stage} status does not match deterministic recalculation.`);if(!equivalent(project.stages?.[stage]?.derivedData||{},expected.stages?.[stage]?.derivedData||{}))issues.push(`Stage ${stage} derivedData does not match deterministic recalculation.`);}const releaseRecords=(project.projectData?.releaseRecords||[]).filter(record=>record?.active!==false&&!record?.invalidatedBy),latestRelease=releaseRecords.at(-1);if(latestRelease){const actual=String(engine.recordValue(latestRelease,'DETERMINATION')||''),calculated=String(engine.releaseMetrics(expected).determination||'');if(actual!==calculated)issues.push(`Current release determination ${actual||'UNKNOWN'} does not match deterministic release calculation ${calculated||'UNKNOWN'}.`);}}
  const artifacts=new Map((project.projectData?.artifacts||[]).map(record=>[engine.recordId(record,'artifacts'),record]));for(const identity of (project.projectData?.artifactIdentities||[]).filter(record=>record?.active!==false&&!record?.invalidatedBy)){const artifact=artifacts.get(String(engine.recordValue(identity,'ARTIFACT_ID')||''));const auditedHash=String(engine.recordValue(identity,'AUDITED_SHA256')||''),deliveryHash=String(engine.recordValue(identity,'PRE_DELIVERY_SHA256')||''),auditedSize=Number(engine.recordValue(identity,'AUDITED_BYTE_SIZE')),deliverySize=Number(engine.recordValue(identity,'RELEASE_BYTE_SIZE')),sameHash=Boolean(auditedHash&&deliveryHash&&auditedHash===deliveryHash),sameSize=Number.isFinite(auditedSize)&&Number.isFinite(deliverySize)&&auditedSize===deliverySize,sameName=String(engine.recordValue(identity,'AUDITED_FILENAME')||'')===String(engine.recordValue(identity,'RELEASE_FILENAME')||''),authorized=sameHash&&sameSize&&sameName;if(Boolean(engine.recordValue(identity,'EXACT_HASH_MATCH'))!==sameHash||Boolean(engine.recordValue(identity,'EXACT_SIZE_MATCH'))!==sameSize||String(engine.recordValue(identity,'AUTHORIZATION')||'')!==(authorized?'AUTHORIZED':'NOT AUTHORIZED'))issues.push(`Artifact identity ${engine.recordId(identity,'artifactIdentities')||'UNKNOWN'} contradicts its deterministic comparison.`);if(artifact){if(String(engine.recordValue(artifact,'SHA256')||'')!==auditedHash||Number(engine.recordValue(artifact,'BYTE_SIZE'))!==auditedSize||String(engine.recordValue(artifact,'FILENAME')||'')!==String(engine.recordValue(identity,'AUDITED_FILENAME')||''))issues.push(`Artifact identity ${engine.recordId(identity,'artifactIdentities')||'UNKNOWN'} does not match its canonical artifact.`);}}
  return {valid:issues.length===0,issues};
}
function assertProjectIntegrity(project,options){const result=validateProjectIntegrity(project,options);if(!result.valid){const error=storageError(`Canonical project integrity validation failed: ${result.issues.join(' | ')}`,'PROJECT_INTEGRITY_FAILED');error.issues=result.issues;throw error;}return result;}

let databasePromise=null;
let databaseHandle=null;
function resetDatabaseConnection(db=null){
  if(db&&databaseHandle&&db!==databaseHandle)return;
  databaseHandle=null;
  databasePromise=null;
}
function openDatabase(){
  if(!globalThis.indexedDB)return Promise.reject(Object.assign(new Error('IndexedDB is required by the supported browser contract.'),{code:'INDEXEDDB_REQUIRED'}));
  if(databasePromise)return databasePromise;
  const opening=new Promise((resolve,reject)=>{
    const req=indexedDB.open(DB_NAME,DB_VERSION);
    req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains(PROJECTS))db.createObjectStore(PROJECTS,{keyPath:'jobId'});if(!db.objectStoreNames.contains(ARTIFACTS))db.createObjectStore(ARTIFACTS,{keyPath:'artifactId'});if(!db.objectStoreNames.contains(META))db.createObjectStore(META,{keyPath:'key'});};
    req.onsuccess=()=>{const db=req.result;databaseHandle=db;db.onclose=()=>resetDatabaseConnection(db);db.onversionchange=()=>{resetDatabaseConnection(db);try{db.close();}catch{}};resolve(db);};
    req.onerror=()=>{resetDatabaseConnection();reject(req.error||new Error('IndexedDB open failed.'));};
    req.onblocked=()=>{resetDatabaseConnection();reject(Object.assign(new Error('IndexedDB upgrade is blocked by another tab.'),{code:'INDEXEDDB_BLOCKED'}));};
  });
  databasePromise=opening;
  return opening;
}
async function openTransaction(stores,mode='readonly'){
  for(let attempt=0;attempt<2;attempt++){
    const db=await openDatabase();
    try{return db.transaction(stores,mode);}
    catch(error){if(attempt===0&&error?.name==='InvalidStateError'){resetDatabaseConnection(db);continue;}throw error;}
  }
  throw storageError('IndexedDB connection could not be reopened for a transaction.','INDEXEDDB_CONNECTION_UNAVAILABLE');
}

function parseLegacy(storage=globalThis.localStorage){const out=[],seen=new Set();if(!storage)return out;for(const key of LEGACY_KEYS){let raw=null;try{raw=storage.getItem(key);}catch(error){throw storageError(`Legacy project storage could not be read from ${key}: ${error.message||error}`,'LEGACY_MIGRATION_READ_FAILED');}if(!raw)continue;let parsed;try{parsed=JSON.parse(raw);}catch(error){throw storageError(`Legacy project storage ${key} contains malformed JSON: ${error.message||error}`,'LEGACY_MIGRATION_PARSE_FAILED');}const items=Array.isArray(parsed)?parsed:[parsed];for(let index=0;index<items.length;index++){const item=items[index];if(!item||typeof item!=='object'||Array.isArray(item))throw storageError(`Legacy project storage ${key}[${index}] is not a project object.`,'LEGACY_MIGRATION_INVALID_PROJECT');const id=projectIdentity(item);if(!id)throw storageError(`Legacy project storage ${key}[${index}] has no JOB_ID.`,'LEGACY_MIGRATION_INVALID_PROJECT');if(seen.has(id))continue;seen.add(id);out.push(item);}}return out;}
function readAllLegacy(storage){return parseLegacy(storage);}
function writeAllLegacy(projects,storage){if(!storage)throw new Error('Legacy test storage is unavailable.');const payload=JSON.stringify(projects),prior=storage.getItem(STORE_KEY);try{fault('before-final-write');storage.setItem(STORE_KEY,payload);fault('after-final-write');return {changed:prior!==payload};}catch(error){try{if(prior===null)storage.removeItem(STORE_KEY);else storage.setItem(STORE_KEY,prior);}catch{}throw error;}}

async function metaPut(key,value,tx=null){const own=tx||await openTransaction(META,'readwrite');own.objectStore(META).put({key,value,updatedAt:now()});if(!tx)await complete(own);return value;}
async function metaGet(key){const tx=await openTransaction(META,'readonly');const row=await request(tx.objectStore(META).get(key));await complete(tx);return row?.value;}
async function quarantine(row,reason){const tx=await openTransaction([PROJECTS,META],'readwrite'),projects=tx.objectStore(PROJECTS),meta=tx.objectStore(META);try{const current=await request(projects.get(String(row?.jobId||'')));if(!current){await complete(tx);return false;}const currentStoredHash=String(current.projectSha256||''),rowStoredHash=String(row?.projectSha256||'');if(currentStoredHash!==rowStoredHash){await complete(tx);return false;}const corruptHash=rowStoredHash||projectSha256(row.project),key=`quarantine:${row?.jobId||'UNKNOWN'}:${corruptHash}`;meta.put({key,value:{reason,row:clone(row)},updatedAt:now()});projects.delete(String(row.jobId));await complete(tx);return true;}catch(error){try{tx.abort();}catch{}throw error;}}

async function migrateLegacy(){
  const countTx=await openTransaction(PROJECTS,'readonly'),count=await request(countTx.objectStore(PROJECTS).count());await complete(countTx);if(count)return {migrated:0};
  const legacy=parseLegacy();if(!legacy.length){await metaPut('migrationStatus',{status:'NONE',at:now()});return {migrated:0};}
  const tx=await openTransaction([PROJECTS,META],'readwrite');let migrated=0;
  try{fault('before-legacy-migration');const core=globalThis.closedLoopCore,engine=globalThis.closedLoopWorkflowEngine;if(!core?.migrateState||!engine)throw storageError('Canonical workflow migration logic is unavailable.','LEGACY_MIGRATION_UNAVAILABLE');for(const source of legacy){const project=core.migrateState(clone(source));engine.ensureShape(project);engine.recalculate(project);assertProjectIntegrity(project,{verifyDerived:true});const id=projectIdentity(project);if(!id)throw storageError('Migrated legacy project has no JOB_ID.','LEGACY_MIGRATION_INVALID_PROJECT');const revision=Number(project.revision||0);tx.objectStore(PROJECTS).put({jobId:id,revision,project,projectSha256:projectSha256(project),updatedAt:now()});migrated++;}tx.objectStore(META).put({key:'migrationStatus',value:{status:'COMPLETE',migrated,at:now()},updatedAt:now()});fault('during-legacy-migration');await complete(tx);for(const key of LEGACY_KEYS)try{localStorage.removeItem(key);}catch{}return {migrated};}catch(error){try{tx.abort();}catch{}await metaPut('migrationStatus',{status:'FAILED',message:String(error.message||error),originalPreserved:true,at:now()});throw error;}
}

async function readAllIndexed(){try{await migrateLegacy();globalThis.closedLoopLegacyMigrationError=null;}catch(error){globalThis.closedLoopLegacyMigrationError=String(error?.stack||error);console.error('Legacy migration failed without deleting the preserved legacy payload; continuing with the canonical IndexedDB store.',error);}const tx=await openTransaction(PROJECTS,'readonly'),rows=await request(tx.objectStore(PROJECTS).getAll());await complete(tx);const valid=[];for(const row of rows){const actual=projectSha256(row.project);if(actual!==row.projectSha256){await quarantine(row,'PROJECT_HASH_MISMATCH');continue;}const project=clone(row.project);project.revision=Number(row.revision||project.revision||0);if(project.schema===globalThis.closedLoopWorkflowSchema?.PROJECT_SCHEMA){const integrity=validateProjectIntegrity(project,{verifyDerived:false});if(!integrity.valid){await quarantine(row,'PROJECT_CANONICAL_INTEGRITY_FAILED: '+integrity.issues.join(' | '));continue;}}project.projectSha256=row.projectSha256;valid.push(project);}return valid;}
function readAll(storage){return storage?readAllLegacy(storage):readAllIndexed();}

async function writeProject(project,{expectedProjectRevision=null,incrementRevision=true}={}){
  const id=projectIdentity(project);if(!id)throw new Error('A project without a JOB_ID cannot be committed.');fault('before-project-transaction');const tx=await openTransaction([PROJECTS,META],'readwrite');const store=tx.objectStore(PROJECTS);try{const prior=await request(store.get(id));const currentRevision=Number(prior?.revision||0);if(expectedProjectRevision!==null&&Number(expectedProjectRevision)!==currentRevision){const e=new Error(`Project revision conflict for ${id}: expected ${expectedProjectRevision}, found ${currentRevision}.`);e.code='STALE_PROJECT_REVISION';throw e;}const next=clone(project);next.revision=incrementRevision?currentRevision+1:currentRevision;delete next.projectSha256;const engine=globalThis.closedLoopWorkflowEngine;engine?.ensureShape?.(next);engine?.recalculate?.(next);assertProjectIntegrity(next);const digest=projectSha256(next);fault('during-project-write');store.put({jobId:id,revision:next.revision,project:next,projectSha256:digest,updatedAt:now()});tx.objectStore(META).put({key:'selectedProject',value:id,updatedAt:now()});tx.objectStore(META).put({key:'lastCommittedRevision',value:{jobId:id,revision:next.revision,projectSha256:digest},updatedAt:now()});fault('before-transaction-commit');await complete(tx);next.projectSha256=digest;try{new BroadcastChannel('closed-loop-reliability').postMessage({type:'PROJECT_CHANGED',jobId:id,revision:next.revision});}catch{}return next;}catch(error){try{tx.abort();}catch{}throw error;}
}

async function writeAllIndexed(projects){if(!Array.isArray(projects))throw new TypeError('Project storage payload must be an array.');const saved=[];for(const project of projects){const id=projectIdentity(project);if(!id)continue;const tx=await openTransaction(PROJECTS,'readonly'),prior=await request(tx.objectStore(PROJECTS).get(id));await complete(tx);const incoming=clone(project);delete incoming.projectSha256;const digest=projectSha256(incoming);if(prior?.projectSha256===digest){incoming.revision=Number(prior.revision||0);incoming.projectSha256=digest;saved.push(incoming);}else saved.push(await writeProject(incoming,{expectedProjectRevision:Number(prior?.revision||0),incrementRevision:Boolean(prior)}));}return saved;}
function writeAll(projects,storage){return storage?writeAllLegacy(projects,storage):writeAllIndexed(projects);}

async function replaceProjectIndexed(projectsOrProject,projectOrOptions){
  if(Array.isArray(projectsOrProject)){const project=projectOrOptions;return writeProject(project,{expectedProjectRevision:Number(project?.revision||0)});}
  return writeProject(projectsOrProject,projectOrOptions||{});
}
function replaceProject(projectsOrProject,projectOrOptions,storage){if(Array.isArray(projectsOrProject)&&storage){const next=clone(projectsOrProject),project=projectOrOptions,id=projectIdentity(project),i=next.findIndex(x=>projectIdentity(x)===id);if(i<0)next.unshift(clone(project));else next[i]=clone(project);writeAllLegacy(next,storage);return next;}return replaceProjectIndexed(projectsOrProject,projectOrOptions);}

async function transactIndexed(projectsOrJobId,jobIdOrExpected,mutatorOrOptions){
  const jobId=String(projectsOrJobId||''),expected=Number(jobIdOrExpected);const mutator=mutatorOrOptions;if(typeof mutator!=='function')throw new TypeError('Transaction mutator must be a function.');const tx=await openTransaction([PROJECTS,META],'readwrite'),store=tx.objectStore(PROJECTS);try{const prior=await request(store.get(jobId));if(!prior)throw new Error(`Project ${jobId} is not available for transaction.`);if(Number(prior.revision)!==expected){const e=new Error(`Project revision conflict for ${jobId}: expected ${expected}, found ${prior.revision}.`);e.code='STALE_PROJECT_REVISION';throw e;}const before=clone(prior.project),next=clone(prior.project),result=runSynchronousMutator(mutator,next,before);const engine=globalThis.closedLoopWorkflowEngine;engine?.ensureShape?.(next);engine?.recalculate?.(next);next.revision=expected+1;assertProjectIntegrity(next);const digest=projectSha256(next);store.put({jobId,revision:next.revision,project:next,projectSha256:digest,updatedAt:now()});tx.objectStore(META).put({key:'lastCommittedRevision',value:{jobId,revision:next.revision,projectSha256:digest},updatedAt:now()});await complete(tx);next.projectSha256=digest;return {project:next,result};}catch(error){try{tx.abort();}catch{}throw error;}
}
function transact(projectsOrJobId,jobIdOrExpected,mutatorOrOptions,storage){if(Array.isArray(projectsOrJobId)&&storage){const next=clone(projectsOrJobId),id=String(jobIdOrExpected||''),i=next.findIndex(x=>projectIdentity(x)===id);if(i<0)throw new Error(`Project ${id} is not available for transaction.`);const before=clone(next[i]),result=runSynchronousMutator(mutatorOrOptions,next[i],before);writeAllLegacy(next,storage);return {projects:next,project:next[i],result};}return transactIndexed(projectsOrJobId,jobIdOrExpected,mutatorOrOptions);}

async function removeProjectIndexed(projectsOrJobId,options={}){
  const jobId=String(projectsOrJobId||'').trim(),expected=options?.expectedProjectRevision,replacementSelectedProjectId=String(options?.replacementSelectedProjectId||'').trim(),suppressRetainedProject=Boolean(options?.suppressRetainedProject);
  if(!jobId)throw storageError('JOB_ID is required for project deletion.','PROJECT_DELETE_JOB_ID_REQUIRED');
  if(replacementSelectedProjectId===jobId)throw storageError('The deleted project cannot also be the replacement selected project.','INVALID_PROJECT_DELETE_REPLACEMENT');
  const tx=await openTransaction([PROJECTS,ARTIFACTS,META],'readwrite'),projects=tx.objectStore(PROJECTS),artifacts=tx.objectStore(ARTIFACTS),meta=tx.objectStore(META);
  try{
    const prior=await request(projects.get(jobId));if(!prior){await complete(tx);return false;}
    if(expected!==undefined&&expected!==null&&Number(prior.revision)!==Number(expected)){const error=storageError(`Project revision conflict for ${jobId}: expected ${expected}, found ${prior.revision}.`,'STALE_PROJECT_REVISION');throw error;}
    if(replacementSelectedProjectId){const replacement=await request(projects.get(replacementSelectedProjectId));if(!replacement)throw storageError(`Replacement project ${replacementSelectedProjectId} is not stored.`,'PROJECT_DELETE_REPLACEMENT_MISSING');}
    const artifactRows=await request(artifacts.getAll());projects.delete(jobId);for(const artifact of artifactRows)if(String(artifact.jobId)===jobId)artifacts.delete(artifact.artifactId);
    const selected=await request(meta.get('selectedProject'));if(String(selected?.value||'')===jobId){if(replacementSelectedProjectId)meta.put({key:'selectedProject',value:replacementSelectedProjectId,updatedAt:now()});else meta.delete('selectedProject');}
    const projectUiRow=await request(meta.get('projectUi'));if(projectUiRow?.value&&typeof projectUiRow.value==='object'&&!Array.isArray(projectUiRow.value)&&Object.prototype.hasOwnProperty.call(projectUiRow.value,jobId)){const nextProjectUi=clone(projectUiRow.value);delete nextProjectUi[jobId];meta.put({key:'projectUi',value:nextProjectUi,updatedAt:now()});}
    if(suppressRetainedProject)meta.put({key:'retainedProjectSuppressed',value:{jobId,at:now()},updatedAt:now()});
    const lastCommitted=await request(meta.get('lastCommittedRevision'));if(String(lastCommitted?.value?.jobId||'')===jobId)meta.delete('lastCommittedRevision');for(const key of ['lastVerifiedExport','lastVerifiedImport']){const row=await request(meta.get(key));if(String(row?.value?.jobId||'')===jobId||String(row?.value?.sourceJobId||'')===jobId)meta.delete(key);}for(const key of ['lastVerifiedExport:'+jobId,'lastArtifactVerification:'+jobId])meta.delete(key);
    fault('during-project-delete');await complete(tx);try{new BroadcastChannel('closed-loop-reliability').postMessage({type:'PROJECT_DELETED',jobId,replacementSelectedProjectId:replacementSelectedProjectId||null});}catch{}return true;
  }catch(error){try{tx.abort();}catch{}throw error;}
}
function removeProject(projectsOrJobId,jobIdOrStorage,storage){if(Array.isArray(projectsOrJobId)&&storage){const next=clone(projectsOrJobId).filter(project=>projectIdentity(project)!==String(jobIdOrStorage||''));writeAllLegacy(next,storage);return next;}return removeProjectIndexed(projectsOrJobId,jobIdOrStorage||{});}

async function rawArtifact(artifactId){const tx=await openTransaction(ARTIFACTS,'readonly'),row=await request(tx.objectStore(ARTIFACTS).get(String(artifactId)));await complete(tx);return row||null;}
async function replaceArtifactLifecycle(artifactId,jobId,expectedStatus,nextStatus,updates={}){const id=String(artifactId),owner=String(jobId),tx=await openTransaction(ARTIFACTS,'readwrite'),store=tx.objectStore(ARTIFACTS);try{const row=await request(store.get(id));if(!row||String(row.jobId)!==owner)throw storageError(`Staged artifact ${id} is unavailable for ${owner}.`,'STAGED_ARTIFACT_MISSING');if(artifactLifecycle(row)!==expectedStatus)throw storageError(`Staged artifact ${id} expected ${expectedStatus}, found ${artifactLifecycle(row)}.`,'STAGED_ARTIFACT_STATE_CONFLICT');const next={...row,...clone(updates),lifecycleStatus:nextStatus,updatedAt:now()};store.put(next);await complete(tx);return next;}catch(error){try{tx.abort();}catch{}throw error;}}
async function stageArtifact({artifactId,jobId,blob,filename,mediaType,lineage={}}){
  if(!(blob instanceof Blob))throw new TypeError('Artifact bytes must be a Blob.');if(!artifactId||!jobId)throw new Error('artifactId and jobId are required.');
  if(Number(blob.size)>SECURITY_LIMITS.maxArtifactBytes)throw storageError(`Artifact ${artifactId} exceeds the centralized per-artifact byte limit.`,'ARTIFACT_RESOURCE_LIMIT');await storageCapacityPreflight(Number(blob.size)*2,{operation:`Artifact intake for ${artifactId}`});
  const id=String(artifactId),owner=String(jobId),name=normalizeArtifactPath(filename||id),createdAt=now(),stagingId=`STAGING-${id}-${createdAt}`,initialBytes=new Uint8Array(await blob.arrayBuffer()),secretScan=scanArtifactForKnownSecrets(initialBytes,name.canonicalPath),format=sniffArtifactFormat(initialBytes,name.canonicalPath,mediaType||blob.type||'application/octet-stream');
  const projectTx=await openTransaction(PROJECTS,'readonly'),projectRow=await request(projectTx.objectStore(PROJECTS).get(owner));await complete(projectTx);const engine=globalThis.closedLoopWorkflowEngine,activeCanonical=projectRow&&engine?engine.records(projectRow.project,'artifacts').filter(record=>engine.isActiveRecord(record)&&engine.recordId(record,'artifacts')!==id).map(record=>({artifactId:engine.recordId(record,'artifacts'),filename:String(engine.recordValue(record,'CANONICAL_PATH')||engine.recordValue(record,'FILENAME')||'')})):[],stagedRows=await listArtifacts(owner,{includeStaged:true}),otherStaged=stagedRows.filter(row=>String(row.artifactId)!==id&&artifactLifecycle(row)!==ARTIFACT_LIFECYCLE.CANONICAL).map(row=>({artifactId:String(row.artifactId),filename:String(row.canonicalPath||row.filename||'') }));assertNoArtifactPathCollisions([...activeCanonical,...otherStaged,{artifactId:id,filename:name.canonicalPath}],{label:`Project ${owner} artifact custody`});
  const pending={artifactId:id,jobId:owner,blob,filename:name.canonicalPath,rawFilename:name.rawFilename,displayFilename:name.displayFilename,canonicalPath:name.canonicalPath,filenameNormalizationVersion:name.filenameNormalizationVersion,collisionKeys:name.collisionKeys,mediaType:String(mediaType||blob.type||'application/octet-stream'),declaredMediaType:String(mediaType||blob.type||'application/octet-stream'),detectedFormat:format.detectedFormat,parserAdapterIdentity:format.parserAdapterIdentity,parseContract:format.parseContract,polyglotStatus:format.polyglotStatus,secretScanStatus:secretScan.status,secretScanFindings:clone(secretScan.findings),disclosureClassification:secretScan.status==='LEAKAGE_DETECTED'?'CREDENTIAL_SECRET':'UNKNOWN',byteSize:Number(blob.size),sha256:null,lineage:clone(lineage),lifecycleStatus:ARTIFACT_LIFECYCLE.PENDING_BYTES,stagingId,createdAt,updatedAt:createdAt};
  fault('before-artifact-pending-write');const pendingTx=await openTransaction(ARTIFACTS,'readwrite'),pendingStore=pendingTx.objectStore(ARTIFACTS);let prior=null;
  try{prior=await request(pendingStore.get(id));if(prior){if(String(prior.jobId)!==owner)throw storageError(`Artifact identity ${id} already belongs to another project.`,'CROSS_PROJECT_ARTIFACT_ID_COLLISION');if(artifactLifecycle(prior)!==ARTIFACT_LIFECYCLE.CANONICAL)throw storageError(`Artifact identity ${id} already has an unfinished staged lifecycle.`,'ARTIFACT_STAGE_COLLISION');await complete(pendingTx);}else{pendingStore.put(pending);await complete(pendingTx);}}catch(error){try{pendingTx.abort();}catch{}throw error;}
  if(prior){const actual=await hash.sha256Bytes(await prior.blob.arrayBuffer()),incoming=await hash.sha256Bytes(await blob.arrayBuffer());if(actual!==String(prior.sha256)||actual!==incoming||Number(prior.byteSize)!==blob.size)throw storageError(`Artifact identity ${id} cannot be reused for different or unverified bytes.`,'ARTIFACT_ID_REUSE');return prior;}
  fault('after-artifact-pending-write');const persisted=await rawArtifact(id);if(!persisted||artifactLifecycle(persisted)!==ARTIFACT_LIFECYCLE.PENDING_BYTES||String(persisted.stagingId)!==stagingId)throw storageError(`Pending artifact ${id} could not be read back exactly.`,'ARTIFACT_PENDING_READBACK_FAILED');const persistedBytes=await persisted.blob.arrayBuffer(),byteSize=persistedBytes.byteLength,sha256=await hash.sha256Bytes(persistedBytes);if(byteSize!==Number(blob.size)){await abortStagedArtifacts([id],owner);throw storageError(`Pending artifact ${id} byte size changed during storage.`,'ARTIFACT_PENDING_READBACK_FAILED');}
  await replaceArtifactLifecycle(id,owner,ARTIFACT_LIFECYCLE.PENDING_BYTES,ARTIFACT_LIFECYCLE.HASHED_AND_REVERIFIED,{byteSize,sha256,hashedAt:now()});fault('after-artifact-hashed-and-reverified');const reverified=await rawArtifact(id),reverifiedBytes=await reverified.blob.arrayBuffer(),reverifiedSha256=await hash.sha256Bytes(reverifiedBytes);if(reverifiedBytes.byteLength!==byteSize||reverifiedSha256!==sha256){await abortStagedArtifacts([id],owner);throw storageError(`Staged artifact ${id} failed the second byte read-back verification.`,'ARTIFACT_SECOND_READBACK_FAILED');}
  return replaceArtifactLifecycle(id,owner,ARTIFACT_LIFECYCLE.HASHED_AND_REVERIFIED,ARTIFACT_LIFECYCLE.READY_FOR_PROMOTION,{reverifiedAt:now()});
}
async function stageDerivedArtifact({jobId,sourceArtifactId,blob,filename,mediaType,transformationKind,removedContentDescription='',testedPropositionUnaffected=false,targetPropositionIds=[]}={}){
  const owner=String(jobId||'').trim(),sourceId=String(sourceArtifactId||'').trim(),kind=String(transformationKind||'').toUpperCase();if(!owner||!sourceId)throw storageError('A derived artifact requires the exact source project and source artifact identity.','DERIVED_ARTIFACT_SOURCE_REQUIRED');if(!['REDACTION','METADATA_REMOVAL','DEIDENTIFICATION'].includes(kind))throw storageError('Derived artifact transformation must be REDACTION, METADATA_REMOVAL, or DEIDENTIFICATION.','DERIVED_ARTIFACT_TRANSFORMATION_INVALID');if(!(blob instanceof Blob))throw storageError('Derived artifact bytes are required.','DERIVED_ARTIFACT_BYTES_REQUIRED');
  const source=await getArtifact(sourceId);if(!source||String(source.jobId)!==owner)throw storageError(`Source artifact ${sourceId} is unavailable or belongs to another project.`,'DERIVED_ARTIFACT_SOURCE_UNAVAILABLE');const sourceBytes=new Uint8Array(await source.blob.arrayBuffer()),sourceSha256=await hash.sha256Bytes(sourceBytes);if(sourceSha256!==String(source.sha256)||sourceBytes.byteLength!==Number(source.byteSize))throw storageError(`Source artifact ${sourceId} failed current byte verification.`,'DERIVED_ARTIFACT_SOURCE_MISMATCH');const projectTx=await openTransaction(PROJECTS,'readonly'),storedProject=await request(projectTx.objectStore(PROJECTS).get(owner));await complete(projectTx);const engine=globalThis.closedLoopWorkflowEngine,canonicalSource=storedProject&&engine?.records(storedProject.project,'artifacts').find(record=>engine.recordId(record,'artifacts')===sourceId&&engine.isActiveRecord(record));if(!canonicalSource||String(engine.recordValue(canonicalSource,'SHA256')||'')!==sourceSha256||Number(engine.recordValue(canonicalSource,'BYTE_SIZE'))!==sourceBytes.byteLength)throw storageError(`Source artifact ${sourceId} is not exact current canonical project state.`,'DERIVED_ARTIFACT_SOURCE_STALE');
  const randomPart=globalThis.crypto?.randomUUID?.().replaceAll('-','').toUpperCase()||hash.sha256Value({owner,sourceId,kind,filename:String(filename||''),deviceTime:now(),byteSize:blob.size}).slice(0,32).toUpperCase(),artifactId=`ARTIFACT-DERIVED-${randomPart}`,lineage={relationship:'DERIVED_FROM',sourceArtifactId:sourceId,sourceArtifactSha256:sourceSha256,sourceArtifactByteSize:sourceBytes.byteLength,transformationKind:kind,removedContentDescription:String(removedContentDescription||''),targetPropositionIds:[...new Set((targetPropositionIds||[]).map(String).filter(Boolean))],submittedClaimThatTestedPropositionIsUnaffected:Boolean(testedPropositionUnaffected),externalTransferEligibility:'BLOCKED_PENDING_ACCEPTED_SEMANTIC_REVIEW',createdAt:now()};
  const staged=await stageArtifact({artifactId,jobId:owner,blob,filename,mediaType,lineage});return {...staged,sourceArtifactPreserved:true,derivedArtifactId:artifactId,lineage:clone(lineage)};
}
async function promoteArtifactWithoutProject(artifactId,jobId){const id=String(artifactId),owner=String(jobId),row=await rawArtifact(id);if(!row)throw storageError(`Staged artifact ${id} is unavailable.`,'STAGED_ARTIFACT_MISSING');if(artifactLifecycle(row)===ARTIFACT_LIFECYCLE.CANONICAL)return row;const bytes=await row.blob.arrayBuffer(),actualSha256=await hash.sha256Bytes(bytes);if(artifactLifecycle(row)!==ARTIFACT_LIFECYCLE.READY_FOR_PROMOTION||bytes.byteLength!==Number(row.byteSize)||actualSha256!==String(row.sha256))throw storageError(`Staged artifact ${id} is not ready for canonical promotion.`,'STAGED_ARTIFACT_NOT_READY');return replaceArtifactLifecycle(id,owner,ARTIFACT_LIFECYCLE.READY_FOR_PROMOTION,ARTIFACT_LIFECYCLE.CANONICAL,{promotedAt:now(),promotionBasis:'VERIFIED_BYTES_WITHOUT_PROJECT_RELATION'});}
async function putArtifact(options){const staged=await stageArtifact(options);return artifactLifecycle(staged)===ARTIFACT_LIFECYCLE.CANONICAL?staged:promoteArtifactWithoutProject(staged.artifactId,staged.jobId);}
async function getArtifact(artifactId,{includeStaged=false}={}){const row=await rawArtifact(artifactId);if(!row)return null;return includeStaged||artifactLifecycle(row)===ARTIFACT_LIFECYCLE.CANONICAL?row:null;}
async function deleteArtifact(artifactId,jobId){const id=String(artifactId),owner=String(jobId||''),tx=await openTransaction(ARTIFACTS,'readwrite'),store=tx.objectStore(ARTIFACTS);try{const row=await request(store.get(id));if(!row){await complete(tx);return false;}if(owner&&String(row.jobId)!==owner)throw storageError(`Artifact ${id} belongs to another project and was not deleted.`,'CROSS_PROJECT_ARTIFACT_DELETE');store.delete(id);await complete(tx);return true;}catch(error){try{tx.abort();}catch{}throw error;}}
async function abortStagedArtifacts(artifactIds,jobId){const ids=[...new Set((artifactIds||[]).map(String).filter(Boolean))],owner=String(jobId||''),tx=await openTransaction(ARTIFACTS,'readwrite'),store=tx.objectStore(ARTIFACTS),removed=[];try{for(const id of ids){const row=await request(store.get(id));if(!row)continue;if(owner&&String(row.jobId)!==owner)throw storageError(`Staged artifact ${id} belongs to another project.`,'CROSS_PROJECT_ARTIFACT_DELETE');if(artifactLifecycle(row)===ARTIFACT_LIFECYCLE.CANONICAL)continue;store.delete(id);removed.push(id);}await complete(tx);return removed;}catch(error){try{tx.abort();}catch{}throw error;}}
async function listArtifacts(jobId,{includeStaged=false}={}){const tx=await openTransaction(ARTIFACTS,'readonly'),rows=await request(tx.objectStore(ARTIFACTS).getAll());await complete(tx);return rows.filter(row=>row.jobId===String(jobId)&&(includeStaged||artifactLifecycle(row)===ARTIFACT_LIFECYCLE.CANONICAL));}
async function promoteStagedArtifacts(project,{artifactIds=[],expectedProjectRevision=null}={}){
  const jobId=projectIdentity(project),ids=[...new Set(artifactIds.map(String).filter(Boolean))];if(!jobId)throw storageError('A canonical project is required for staged artifact promotion.','ARTIFACT_PROMOTION_PROJECT_REQUIRED');if(!ids.length)throw storageError('At least one staged artifact is required for promotion.','ARTIFACT_PROMOTION_EMPTY');
  const engine=globalThis.closedLoopWorkflowEngine,canonicalById=new Map(engine.records(project,'artifacts').filter(record=>engine.isActiveRecord(record)).map(record=>[engine.recordId(record,'artifacts'),record])),verified=[];
  for(const id of ids){const row=await rawArtifact(id),record=canonicalById.get(id);if(!row||String(row.jobId)!==jobId)throw storageError(`Staged artifact ${id} is unavailable for canonical promotion.`,'STAGED_ARTIFACT_MISSING');if(artifactLifecycle(row)!==ARTIFACT_LIFECYCLE.READY_FOR_PROMOTION)throw storageError(`Staged artifact ${id} is ${artifactLifecycle(row)}, not READY_FOR_PROMOTION.`,'STAGED_ARTIFACT_NOT_READY');if(!record)throw storageError(`Project metadata does not contain staged artifact ${id}.`,'ARTIFACT_PROMOTION_METADATA_MISSING');const bytes=await row.blob.arrayBuffer(),actualSha256=await hash.sha256Bytes(bytes),expectedSha256=String(engine.recordValue(record,'SHA256')||''),expectedSize=Number(engine.recordValue(record,'BYTE_SIZE')),expectedFilename=String(engine.recordValue(record,'FILENAME')||'');if(bytes.byteLength!==Number(row.byteSize)||actualSha256!==String(row.sha256)||actualSha256!==expectedSha256||bytes.byteLength!==expectedSize||String(row.filename)!==expectedFilename)throw storageError(`Staged artifact ${id} does not match its canonical project metadata.`,'ARTIFACT_PROMOTION_IDENTITY_MISMATCH');verified.push({id,stagingId:String(row.stagingId||''),sha256:actualSha256,byteSize:bytes.byteLength});}
  fault('before-artifact-promotion-transaction');const tx=await openTransaction([PROJECTS,ARTIFACTS,META],'readwrite'),projects=tx.objectStore(PROJECTS),artifacts=tx.objectStore(ARTIFACTS),meta=tx.objectStore(META);try{const prior=await request(projects.get(jobId)),currentRevision=Number(prior?.revision||0),expected=expectedProjectRevision===null?Number(project.revision||0):Number(expectedProjectRevision);if(currentRevision!==expected){const error=storageError(`Project revision conflict for ${jobId}: expected ${expected}, found ${currentRevision}.`,'STALE_PROJECT_REVISION');throw error;}const next=clone(project);next.revision=currentRevision+1;delete next.projectSha256;engine.ensureShape(next);engine.recalculate(next);assertProjectIntegrity(next);const digest=projectSha256(next);for(const identity of verified){const row=await request(artifacts.get(identity.id));if(!row||String(row.jobId)!==jobId||artifactLifecycle(row)!==ARTIFACT_LIFECYCLE.READY_FOR_PROMOTION||String(row.stagingId||'')!==identity.stagingId||String(row.sha256)!==identity.sha256||Number(row.byteSize)!==identity.byteSize)throw storageError(`Staged artifact ${identity.id} changed before atomic promotion.`,'STAGED_ARTIFACT_STATE_CONFLICT');artifacts.put({...row,lifecycleStatus:ARTIFACT_LIFECYCLE.CANONICAL,promotedAt:now(),canonicalProjectRevision:next.revision,promotionBasis:'ATOMIC_PROJECT_AND_BYTES_COMMIT',updatedAt:now()});}fault('during-artifact-promotion');projects.put({jobId,revision:next.revision,project:next,projectSha256:digest,updatedAt:now()});meta.put({key:'selectedProject',value:jobId,updatedAt:now()});meta.put({key:'lastCommittedRevision',value:{jobId,revision:next.revision,projectSha256:digest},updatedAt:now()});fault('before-artifact-promotion-commit');await complete(tx);next.projectSha256=digest;try{new BroadcastChannel('closed-loop-reliability').postMessage({type:'PROJECT_CHANGED',jobId,revision:next.revision});}catch{}return next;}catch(error){try{tx.abort();}catch{}throw error;}
}
async function reconcileArtifactStorage(){
  /* Recovery classes are explicit audit facts: ORPHAN_STAGED_BYTES,
     ORPHAN_CANONICAL_BYTES, METADATA_WITHOUT_BYTES, INTERRUPTED_PROMOTION,
     and STALE_STAGING_RESERVATION. */
  const readTx=await openTransaction([PROJECTS,ARTIFACTS],'readonly'),projectRows=await request(readTx.objectStore(PROJECTS).getAll()),artifactRows=await request(readTx.objectStore(ARTIFACTS).getAll());await complete(readTx);const engine=globalThis.closedLoopWorkflowEngine,expected=new Map(),actions=[],rowIds=new Set(artifactRows.map(row=>String(row.artifactId))),unavailableByJob=new Map(),markUnavailable=(jobId,artifactId,issue)=>{const key=String(jobId),items=unavailableByJob.get(key)||[];items.push({artifactId:String(artifactId),issue:String(issue)});unavailableByJob.set(key,items);};
  for(const projectRow of projectRows)for(const record of engine.records(projectRow.project,'artifacts').filter(item=>engine.isActiveRecord(item))){const artifactId=engine.recordId(record,'artifacts'),jobId=String(projectRow.jobId);expected.set(artifactId,{jobId,record});if(!rowIds.has(artifactId)&&String(engine.recordValue(record,'AVAILABILITY')||'').toUpperCase()!=='UNAVAILABLE'){actions.push({kind:'METADATA_WITHOUT_BYTES',artifactId,jobId,action:'MARK_UNAVAILABLE'});markUnavailable(jobId,artifactId,'MISSING_STORED_BLOB');}}
  const mutations=[];for(const row of artifactRows){const artifactId=String(row.artifactId),want=expected.get(artifactId),status=artifactLifecycle(row);let actualSha256='',actualSize=-1;try{const bytes=await row.blob.arrayBuffer();actualSize=bytes.byteLength;actualSha256=await hash.sha256Bytes(bytes);}catch{}const byteValid=actualSize===Number(row.byteSize)&&Boolean(row.sha256)&&actualSha256===String(row.sha256),metadataValid=want&&String(row.jobId)===want.jobId&&String(row.filename)===String(engine.recordValue(want.record,'FILENAME')||'')&&actualSize===Number(engine.recordValue(want.record,'BYTE_SIZE'))&&actualSha256===String(engine.recordValue(want.record,'SHA256')||'');if(status!==ARTIFACT_LIFECYCLE.CANONICAL&&status!==ARTIFACT_LIFECYCLE.UNAVAILABLE){if(byteValid&&metadataValid&&status===ARTIFACT_LIFECYCLE.READY_FOR_PROMOTION){mutations.push({type:'PUT',row:{...row,lifecycleStatus:ARTIFACT_LIFECYCLE.CANONICAL,promotedAt:now(),promotionBasis:'INTERRUPTED_PROMOTION_RECOVERY',updatedAt:now()}});actions.push({kind:'INTERRUPTED_PROMOTION',artifactId,jobId:String(row.jobId),action:'PROMOTED'});}else{mutations.push({type:'DELETE',artifactId});actions.push({kind:status===ARTIFACT_LIFECYCLE.PENDING_BYTES?'STALE_STAGING_RESERVATION':'ORPHAN_STAGED_BYTES',artifactId,jobId:String(row.jobId),action:'REMOVED'});}continue;}if(!want){mutations.push({type:'DELETE',artifactId});actions.push({kind:'ORPHAN_CANONICAL_BYTES',artifactId,jobId:String(row.jobId),action:'REMOVED'});continue;}if(!byteValid||!metadataValid){if(status!==ARTIFACT_LIFECYCLE.UNAVAILABLE)mutations.push({type:'PUT',row:{...row,lifecycleStatus:ARTIFACT_LIFECYCLE.UNAVAILABLE,unavailableReason:'RECOVERY_IDENTITY_MISMATCH',updatedAt:now()}});if(String(engine.recordValue(want.record,'AVAILABILITY')||'').toUpperCase()!=='UNAVAILABLE'){actions.push({kind:'CANONICAL_BYTES_INVALID',artifactId,jobId:String(row.jobId),action:'MARK_UNAVAILABLE'});markUnavailable(want.jobId,artifactId,'CANONICAL_BLOB_IDENTITY_MISMATCH');}}else if(!row.lifecycleStatus)mutations.push({type:'PUT',row:{...row,lifecycleStatus:ARTIFACT_LIFECYCLE.CANONICAL,updatedAt:now()}});}
  if(mutations.length||actions.length){const tx=await openTransaction([PROJECTS,ARTIFACTS,META],'readwrite'),projects=tx.objectStore(PROJECTS),artifacts=tx.objectStore(ARTIFACTS),meta=tx.objectStore(META),updatedProjects=[];try{for(const mutation of mutations){if(mutation.type==='DELETE')artifacts.delete(mutation.artifactId);else artifacts.put(mutation.row);}for(const [jobId,failures] of unavailableByJob){const stored=await request(projects.get(jobId));if(!stored)continue;const next=clone(stored.project),canonicalById=new Map(engine.records(next,'artifacts').filter(record=>engine.isActiveRecord(record)).map(record=>[engine.recordId(record,'artifacts'),record])),currentFailures=[];for(const failure of failures){const canonical=canonicalById.get(failure.artifactId);if(!canonical)continue;const latest=await request(artifacts.get(failure.artifactId)),restored=latest&&artifactLifecycle(latest)===ARTIFACT_LIFECYCLE.CANONICAL&&String(latest.jobId)===jobId&&String(latest.filename)===String(engine.recordValue(canonical,'FILENAME')||'')&&Number(latest.byteSize)===Number(engine.recordValue(canonical,'BYTE_SIZE'))&&String(latest.sha256)===String(engine.recordValue(canonical,'SHA256')||'');if(restored)continue;canonical.fields=canonical.fields&&typeof canonical.fields==='object'?canonical.fields:{};const priorNotes=String(engine.recordValue(canonical,'NOTES')||'').trim(),recoveryNote=`Artifact bytes unavailable after startup custody reconciliation: ${failure.issue}.`,note=priorNotes?`${priorNotes}\n${recoveryNote}`:recoveryNote;canonical.fields.AVAILABILITY='UNAVAILABLE';canonical.AVAILABILITY='UNAVAILABLE';canonical.fields.STORAGE_REFERENCE='UNAVAILABLE';canonical.STORAGE_REFERENCE='UNAVAILABLE';canonical.fields.NOTES=note;canonical.NOTES=note;engine.refreshRecordHashes(canonical,'artifacts');currentFailures.push({artifactId:failure.artifactId,verified:false,issue:failure.issue});actions.push({kind:'CANONICAL_METADATA_MARKED_UNAVAILABLE',artifactId:failure.artifactId,jobId,action:'BLOCK_DEPENDENT_WORK'});}if(!currentFailures.length)continue;engine.reconcileArtifactCustodyVerification(next,{report:{jobId,verified:false,artifactCount:engine.records(next,'artifacts').length,artifacts:currentFailures,at:now()},stage:Number(next.activeStage||1)});next.revision=Number(stored.revision||next.revision||0)+1;delete next.projectSha256;engine.ensureShape(next);engine.recalculate(next);assertProjectIntegrity(next);const digest=projectSha256(next);projects.put({jobId,revision:next.revision,project:next,projectSha256:digest,updatedAt:now()});updatedProjects.push({jobId,revision:next.revision,projectSha256:digest});}const report={schema:'closed-loop-artifact-recovery/1',actions,updatedProjects,reconciledAt:now()};meta.put({key:'lastArtifactRecovery',value:report,updatedAt:now()});if(updatedProjects.length)meta.put({key:'lastCommittedRevision',value:updatedProjects.at(-1),updatedAt:now()});await complete(tx);for(const update of updatedProjects)try{new BroadcastChannel('closed-loop-reliability').postMessage({type:'PROJECT_CHANGED',jobId:update.jobId,revision:update.revision});}catch{}return report;}catch(error){try{tx.abort();}catch{}throw error;}}
  const report={schema:'closed-loop-artifact-recovery/1',actions:[],reconciledAt:now()};await metaPut('lastArtifactRecovery',report);return report;
}
async function verifyProjectArtifacts(jobId){
  const id=String(jobId||'').trim();if(!id)throw storageError('JOB_ID is required for artifact verification.','ARTIFACT_VERIFY_JOB_ID_REQUIRED');const projects=await readAll(),project=projects.find(item=>projectIdentity(item)===id);if(!project)throw storageError(`Project ${id} is not stored.`,'ARTIFACT_VERIFY_PROJECT_MISSING');const rows=await listArtifacts(id),rowById=new Map(rows.map(row=>[String(row.artifactId),row])),engine=globalThis.closedLoopWorkflowEngine,expected=engine.records(project,'artifacts').filter(record=>engine.isActiveRecord(record)&&String(engine.recordValue(record,'AVAILABILITY')||'').toUpperCase()==='BYTES_PERSISTED_AND_VERIFIED'),expectedById=new Map(expected.map(record=>[engine.recordId(record,'artifacts'),record])),results=[];let byteSize=0;
  for(const record of expected){const artifactId=engine.recordId(record,'artifacts'),row=rowById.get(artifactId),canonicalFilename=String(engine.recordValue(record,'FILENAME')||''),canonicalSize=Number(engine.recordValue(record,'BYTE_SIZE')),canonicalSha256=String(engine.recordValue(record,'SHA256')||'');if(!row){results.push({artifactId,filename:canonicalFilename,storedByteSize:null,actualByteSize:null,storedSha256:null,actualSha256:null,verified:false,issue:'MISSING_STORED_BLOB'});continue;}const bytes=new Uint8Array(await row.blob.arrayBuffer()),actualSize=bytes.byteLength,actualSha256=await hash.sha256Bytes(bytes);byteSize+=actualSize;const verified=String(row.jobId)===id&&String(row.filename)===canonicalFilename&&Number(row.byteSize)===canonicalSize&&String(row.sha256)===canonicalSha256&&actualSize===canonicalSize&&actualSha256===canonicalSha256;results.push({artifactId,filename:canonicalFilename,storedByteSize:Number(row.byteSize),actualByteSize:actualSize,storedSha256:String(row.sha256||''),actualSha256,verified,issue:verified?null:'CANONICAL_BLOB_IDENTITY_MISMATCH'});}
  for(const row of rows)if(!expectedById.has(String(row.artifactId))){const bytes=new Uint8Array(await row.blob.arrayBuffer()),actualSize=bytes.byteLength,actualSha256=await hash.sha256Bytes(bytes);byteSize+=actualSize;const verified=actualSize===Number(row.byteSize)&&actualSha256===String(row.sha256);results.push({artifactId:String(row.artifactId),filename:String(row.filename||row.artifactId),storedByteSize:Number(row.byteSize),actualByteSize:actualSize,storedSha256:String(row.sha256||''),actualSha256,verified,issue:verified?null:'UNREFERENCED_STORED_BLOB_MISMATCH',canonicalExpectation:false});}
  const mismatches=results.filter(item=>!item.verified),report={jobId:id,artifactCount:rows.length,expectedCanonicalArtifactCount:expected.length,byteSize,verifiedCount:results.length-mismatches.length,mismatchCount:mismatches.length,verified:mismatches.length===0,artifacts:results,at:now()};await metaPut('lastArtifactVerification:'+id,{jobId:id,artifactCount:report.artifactCount,expectedCanonicalArtifactCount:report.expectedCanonicalArtifactCount,byteSize:report.byteSize,mismatchCount:report.mismatchCount,verified:report.verified,at:report.at});return report;
}

const bytesToBase64=bytes=>{let s='';for(let i=0;i<bytes.length;i+=0x8000)s+=String.fromCharCode(...bytes.subarray(i,i+0x8000));return btoa(s);};
const base64ToBytes=text=>{const s=atob(text),out=new Uint8Array(s.length);for(let i=0;i<s.length;i++)out[i]=s.charCodeAt(i);return out;};
async function compressBytes(bytes){if(typeof CompressionStream!=='function')throw Object.assign(new Error('CompressionStream is required for complete package export.'),{code:'COMPRESSION_STREAM_REQUIRED'});const stream=new Blob([bytes]).stream().pipeThrough(new CompressionStream('gzip'));return new Uint8Array(await new Response(stream).arrayBuffer());}
async function decompressBytes(bytes,{maxOutputBytes=SECURITY_LIMITS.maxPackageExpandedBytes}={}){if(typeof DecompressionStream!=='function')throw Object.assign(new Error('DecompressionStream is required for complete package import.'),{code:'DECOMPRESSION_STREAM_REQUIRED'});const reader=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip')).getReader(),chunks=[];let total=0;try{for(;;){const {done,value}=await reader.read();if(done)break;total+=value.byteLength;if(total>maxOutputBytes){await reader.cancel();throw storageError('Expanded package exceeds the centralized decompressed-byte limit.','IMPORT_RESOURCE_LIMIT');}chunks.push(value);}}finally{try{reader.releaseLock();}catch{}}const output=new Uint8Array(total);let offset=0;for(const chunk of chunks){output.set(chunk,offset);offset+=chunk.byteLength;}return output;}
function scanJsonResourceLimits(text){let depth=0,nodes=0,inString=false,escaped=false,stringLength=0,maxDepth=0;for(let index=0;index<text.length;index++){const char=text[index];if(inString){if(escaped){escaped=false;continue;}if(char==='\\'){escaped=true;continue;}if(char==='"'){inString=false;stringLength=0;continue;}stringLength++;if(stringLength>SECURITY_LIMITS.maxStringLength)throw storageError('Imported JSON contains a string beyond the centralized limit.','IMPORT_RESOURCE_LIMIT');continue;}if(char==='"'){inString=true;stringLength=0;nodes++;}else if(char==='{'||char==='['){depth++;maxDepth=Math.max(maxDepth,depth);nodes++;if(depth>SECURITY_LIMITS.maxJsonDepth)throw storageError('Imported JSON exceeds the centralized structure-depth limit.','IMPORT_RESOURCE_LIMIT');}else if(char==='}'||char===']')depth--;else if(char===','||char===':')nodes++;if(nodes>SECURITY_LIMITS.maxJsonNodes)throw storageError('Imported JSON exceeds the centralized node/work-unit limit.','IMPORT_RESOURCE_LIMIT');if(depth<0)throw storageError('Imported JSON structure is malformed.','IMPORT_MALFORMED_JSON');}if(inString||depth!==0)throw storageError('Imported JSON is truncated or structurally malformed.','IMPORT_MALFORMED_JSON');return {maxDepth,nodes};}
function validateMaterializedImportLimits(project,artifacts){let nodes=0,relationships=0,extensionBytes=0;const visit=(value,key='',depth=0)=>{if(depth>SECURITY_LIMITS.maxJsonDepth)throw storageError('Imported value exceeds the centralized depth limit.','IMPORT_RESOURCE_LIMIT');nodes++;if(nodes>SECURITY_LIMITS.maxJsonNodes)throw storageError('Imported value exceeds the centralized node limit.','IMPORT_RESOURCE_LIMIT');if(typeof value==='string'){if(value.length>SECURITY_LIMITS.maxStringLength)throw storageError('Imported value exceeds the centralized string limit.','IMPORT_RESOURCE_LIMIT');if(/extension|unknown/i.test(key))extensionBytes+=utf8Length(value);return;}if(Array.isArray(value)){if(value.length>SECURITY_LIMITS.maxArrayLength)throw storageError(`Imported array ${key||'<root>'} exceeds the centralized length limit.`,'IMPORT_RESOURCE_LIMIT');if(/relationship|_IDS?$|relationships/i.test(key))relationships+=value.length;for(const item of value)visit(item,key,depth+1);return;}if(value&&typeof value==='object'){for(const [childKey,child] of Object.entries(value)){if(/relationship|_ID$|_IDS$|relationships/i.test(childKey))relationships+=Array.isArray(child)?child.length:1;if(/extension|unknown/i.test(childKey))extensionBytes+=utf8Length(JSON.stringify(child));visit(child,childKey,depth+1);}}};visit(project);if((project?.projectData?.history||[]).length>SECURITY_LIMITS.maxHistoryRecords)throw storageError('Imported history exceeds the centralized record limit.','IMPORT_RESOURCE_LIMIT');if((artifacts||[]).length>SECURITY_LIMITS.maxArtifactCount)throw storageError('Imported artifact count exceeds the centralized limit.','IMPORT_RESOURCE_LIMIT');if(relationships>SECURITY_LIMITS.maxRelationships)throw storageError('Imported relationship count exceeds the centralized limit.','IMPORT_RESOURCE_LIMIT');if(extensionBytes>SECURITY_LIMITS.maxExtensionBytes)throw storageError('Imported opaque extension audit data exceeds the centralized limit.','IMPORT_RESOURCE_LIMIT');return {nodes,relationships,extensionBytes};}
const PROJECT_PACKAGE_ROOT_KEYS=Object.freeze(['schema','projectSchema','workflow','responseSchema','project','artifacts','packageManifest','backupPolicy','checkpoint','exportedAt','packageSha256']);
const PROJECT_PACKAGE_ARTIFACT_KEYS=Object.freeze(['artifactId','jobId','filename','rawFilename','displayFilename','canonicalPath','filenameNormalizationVersion','collisionKeys','mediaType','declaredMediaType','detectedFormat','parserAdapterIdentity','parseContract','polyglotStatus','disclosureClassification','secretScanStatus','secretScanFindings','byteSize','sha256','lineage','createdAt','base64']);
const PROJECT_PACKAGE_MANIFEST_KEYS=Object.freeze(['schema','jobId','projectSha256','artifactCount','artifactManifestSha256','filenameNormalizationVersion','archiveEntryModel','encryptionStatus','sensitiveHandlingBasis','artifacts']);
const PROJECT_PACKAGE_MANIFEST_ARTIFACT_KEYS=Object.freeze(PROJECT_PACKAGE_ARTIFACT_KEYS.filter(key=>!['base64','secretScanFindings'].includes(key)).concat('secretScanFindingCount'));
const PROJECT_PACKAGE_BACKUP_POLICY_KEYS=Object.freeze(['schema','purpose','artifactBytesIncluded','encryptionRequirement','encryptionStatus','authoritativeRestoreRule','recoveryPointObjective','restoreTestStatus','browserLocalCopyIsDisasterRecoveryBackup']);
const PROJECT_PACKAGE_CHECKPOINT_KEYS=Object.freeze(['schema','jobId','revision','projectDigest','artifactManifestDigest','hashAlgorithm','observedAt','observedAtBasis','authenticationStatus']);
const DISCLOSURE_CLASSIFICATIONS=Object.freeze(new Set(['PUBLIC','INTERNAL','CONFIDENTIAL','RESTRICTED','CREDENTIAL_SECRET','UNKNOWN']));
function rejectUnknownKeys(value,allowed,label){if(!value||typeof value!=='object'||Array.isArray(value))throw storageError(`${label} must be an object.`,'IMPORT_CLOSED_SCHEMA_VIOLATION');const unknown=Object.keys(value).filter(key=>!allowed.includes(key));if(unknown.length)throw storageError(`${label} contains unmanifested properties: ${unknown.join(', ')}.`,'IMPORT_CLOSED_SCHEMA_VIOLATION');}
function importFailure(error){return Object.assign(error instanceof Error?error:new Error(String(error)),{existingProjectsUnchanged:true});}
function artifactSecurityMetadata(project,artifactId,row,bytes){const engine=globalThis.closedLoopWorkflowEngine,canonical=engine?.records(project,'artifacts',{active:false}).find(record=>engine.recordId(record,'artifacts')===String(artifactId)),value=field=>canonical?engine.recordValue(canonical,field):undefined,name=normalizeArtifactPath(value('CANONICAL_PATH')||value('FILENAME')||row.canonicalPath||row.filename||artifactId),scan=scanArtifactForKnownSecrets(bytes,name.canonicalPath),format=sniffArtifactFormat(bytes,name.canonicalPath,row.mediaType||'application/octet-stream'),classification=String(value('DISCLOSURE_CLASSIFICATION')||row.disclosureClassification||(scan.status==='LEAKAGE_DETECTED'?'CREDENTIAL_SECRET':'UNKNOWN')).toUpperCase();if(!DISCLOSURE_CLASSIFICATIONS.has(classification))throw storageError(`Artifact ${artifactId} has unsupported disclosure classification ${classification||'EMPTY'}.`,'ARTIFACT_DISCLOSURE_CLASSIFICATION_INVALID');return {canonical,name,scan,format,classification:scan.status==='LEAKAGE_DETECTED'?'CREDENTIAL_SECRET':classification};}
async function exportPackage(jobId,{purpose='MANUAL_EXPORT',unencryptedSensitiveExportAcknowledged=false}={}){
  const projects=await readAll(),project=projects.find(p=>projectIdentity(p)===String(jobId));if(!project)throw new Error(`Project ${jobId} is not stored.`);const artifacts=await listArtifacts(jobId),artifactEntries=[];
  if(artifacts.length>SECURITY_LIMITS.maxArtifactCount)throw storageError('Project artifact count exceeds the centralized export/import limit.','ARTIFACT_RESOURCE_LIMIT');
  for(const a of artifacts){const bytes=new Uint8Array(await a.blob.arrayBuffer()),actualSize=bytes.byteLength,actualSha256=await hash.sha256Bytes(bytes);if(actualSize!==Number(a.byteSize)||actualSha256!==String(a.sha256))throw storageError(`Stored artifact ${a.artifactId} failed export-time byte verification.`,'ARTIFACT_INTEGRITY_MISMATCH');const security=artifactSecurityMetadata(project,a.artifactId,a,bytes);artifactEntries.push({artifactId:String(a.artifactId),jobId:String(a.jobId),filename:security.name.canonicalPath,rawFilename:String(a.rawFilename||security.name.rawFilename),displayFilename:security.name.displayFilename,canonicalPath:security.name.canonicalPath,filenameNormalizationVersion:FILENAME_NORMALIZATION_VERSION,collisionKeys:security.name.collisionKeys,mediaType:String(a.mediaType||'application/octet-stream'),declaredMediaType:String(a.declaredMediaType||a.mediaType||'application/octet-stream'),detectedFormat:security.format.detectedFormat,parserAdapterIdentity:security.format.parserAdapterIdentity,parseContract:security.format.parseContract,polyglotStatus:security.format.polyglotStatus,disclosureClassification:security.classification,secretScanStatus:security.scan.status,secretScanFindings:clone(security.scan.findings),byteSize:actualSize,sha256:actualSha256,lineage:clone(a.lineage||{}),createdAt:a.createdAt,base64:bytesToBase64(bytes)});}
  assertNoArtifactPathCollisions(artifactEntries,{label:'Complete project package'});const sensitive=artifactEntries.filter(item=>['CONFIDENTIAL','RESTRICTED','CREDENTIAL_SECRET','UNKNOWN'].includes(item.disclosureClassification));if(sensitive.length&&!unencryptedSensitiveExportAcknowledged)throw storageError(`This unencrypted export contains ${sensitive.length} confidential, restricted, credential/secret, or unclassified artifact(s). Explicitly acknowledge unencrypted handling or use a registered encrypted export contract.`,'UNENCRYPTED_SENSITIVE_EXPORT_ACKNOWLEDGEMENT_REQUIRED');
  const exportedProject=canonicalProject(project),manifestArtifacts=artifactEntries.map(({base64,secretScanFindings,...item})=>({...item,secretScanFindingCount:(secretScanFindings||[]).length})),artifactManifestSha256=hash.sha256Value(manifestArtifacts),packageManifest={schema:'closed-loop-project-package-manifest/1',jobId:String(jobId),projectSha256:projectSha256(exportedProject),artifactCount:artifactEntries.length,artifactManifestSha256,filenameNormalizationVersion:FILENAME_NORMALIZATION_VERSION,archiveEntryModel:'SINGLE_GZIP_JSON; EMBEDDED ARTIFACT ARCHIVES ARE STORED AS BYTES AND NEVER EXPANDED',encryptionStatus:'UNENCRYPTED',sensitiveHandlingBasis:sensitive.length?'EXPLICIT_SELF_ASSERTED_HUMAN_ACKNOWLEDGEMENT':'NO_SENSITIVE_CLASSIFICATION_PRESENT',artifacts:manifestArtifacts},backupPolicy={schema:'closed-loop-backup-policy/1',purpose:String(purpose),artifactBytesIncluded:true,encryptionRequirement:sensitive.length?'REGISTERED_ENCRYPTION_OR_EXPLICIT_UNENCRYPTED_ACKNOWLEDGEMENT':'NOT_APPLICABLE',encryptionStatus:'UNENCRYPTED',authoritativeRestoreRule:'RESTORE_PRESERVES_LINEAGE_WITHOUT_SILENT_OVERWRITE; CLONE_ALLOCATES_NEW_ACTIVE IDENTITIES',recoveryPointObjective:'LATEST_REQUIRED_SUCCESSFUL_CHECKPOINT',restoreTestStatus:'NOT_PERFORMED_BY_EXPORT',browserLocalCopyIsDisasterRecoveryBackup:false},checkpoint={schema:'closed-loop-checkpoint/1',jobId:String(jobId),revision:Number(exportedProject.revision||0),projectDigest:projectSha256(exportedProject),artifactManifestDigest:artifactManifestSha256,hashAlgorithm:'SHA-256',observedAt:now(),observedAtBasis:'DEVICE_REPORTED',authenticationStatus:'SELF_CONSISTENCY_ONLY; INDEPENDENT STORAGE OR SIGNATURE NOT VERIFIED'},body={schema:'closed-loop-project-package/1',projectSchema:project.schema,workflow:project.workflow,responseSchema:globalThis.closedLoopWorkflowSchema?.RESPONSE_SCHEMA,project:exportedProject,artifacts:artifactEntries,packageManifest,backupPolicy,checkpoint,exportedAt:now()},packageSha256=hash.sha256Value(body),payload={...body,packageSha256},payloadBytes=new TextEncoder().encode(JSON.stringify(payload));if(payloadBytes.byteLength>SECURITY_LIMITS.maxPackageExpandedBytes)throw storageError('Complete package exceeds the centralized expanded-package limit.','EXPORT_RESOURCE_LIMIT');await storageCapacityPreflight(payloadBytes.byteLength*2,{operation:'Complete project export'});const compressed=await compressBytes(payloadBytes);if(compressed.byteLength>SECURITY_LIMITS.maxPackageCompressedBytes)throw storageError('Complete package exceeds the centralized compressed-package limit.','EXPORT_RESOURCE_LIMIT');const exportRecord={jobId:String(jobId),packageSha256,artifactCount:artifactEntries.length,artifactManifestSha256,purpose:String(purpose),backupPolicy,checkpoint,at:now()};await metaPut('lastVerifiedExport',exportRecord);await metaPut('lastVerifiedExport:'+jobId,exportRecord);return new Blob([compressed],{type:'application/gzip'});
}
async function deterministicCloneArtifactId(newJobId,sourceArtifactId){return `ARTIFACT-CLONE-${(await hash.sha256Text(`${newJobId}\u0000${sourceArtifactId}`)).slice(0,24).toUpperCase()}`;}
async function buildImportedClone(sourceProject,verifiedArtifacts,packageSha256){const core=globalThis.closedLoopCore,schemaApi=globalThis.closedLoopWorkflowSchema,engine=globalThis.closedLoopWorkflowEngine;if(!core?.createBlankState||!schemaApi||!engine)throw storageError('The canonical clone runtime is unavailable.','CLONE_RUNTIME_UNAVAILABLE');const newJobId=`JOB-${crypto.randomUUID().toUpperCase()}`,project=core.createBlankState(newJobId);engine.ensureShape(project);for(const [field,definition] of Object.entries(schemaApi.JOB_FIELDS||{}))if(['HUMAN','HUMAN_DECISION'].includes(definition?.producer)&&field!=='JOB_ID'&&Object.prototype.hasOwnProperty.call(sourceProject.job||{},field))project.job[field]=clone(sourceProject.job[field]);project.job.JOB_TITLE=`${String(sourceProject.job?.JOB_TITLE||'Imported project')} — clone`;project.job.DATE_OPENED=now();project.projectData.userEntered=clone(sourceProject.projectData?.userEntered||{});project.projectData.migrationArchives=Array.isArray(project.projectData.migrationArchives)?project.projectData.migrationArchives:[];const artifactIdMap={},clonedArtifacts=[];for(const source of verifiedArtifacts){const targetId=await deterministicCloneArtifactId(newJobId,source.artifactId);artifactIdMap[String(source.artifactId)]=targetId;const bytes=new Uint8Array(await source.blob.arrayBuffer()),scan=scanArtifactForKnownSecrets(bytes,source.filename),format=sniffArtifactFormat(bytes,source.filename,source.mediaType),classification=scan.status==='LEAKAGE_DETECTED'?'CREDENTIAL_SECRET':String(source.disclosureClassification||'UNKNOWN').toUpperCase();engine.registerArtifactBytes(project,{stage:1,artifactId:targetId,filename:source.filename,rawFilename:source.rawFilename||source.filename,displayFilename:source.displayFilename||source.filename.split('/').at(-1),canonicalPath:source.canonicalPath||source.filename,filenameNormalizationVersion:FILENAME_NORMALIZATION_VERSION,collisionKeys:clone(source.collisionKeys||normalizeArtifactPath(source.filename).collisionKeys),mediaType:source.mediaType,detectedFormat:format.detectedFormat,parserAdapterIdentity:format.parserAdapterIdentity,parseContract:format.parseContract,polyglotStatus:format.polyglotStatus,secretScanStatus:scan.status,secretScanFindings:scan.findings,disclosureClassification:classification,byteSize:source.byteSize,sha256:source.sha256,lineage:{sourceProjectJobId:projectIdentity(sourceProject),sourceArtifactId:String(source.artifactId),sourcePackageSha256:packageSha256,cloneMode:'CLONE'},role:'CLONED_SUPPLIED_ARTIFACT'});const row={...source,artifactId:targetId,jobId:newJobId,lineage:{sourceProjectJobId:projectIdentity(sourceProject),sourceArtifactId:String(source.artifactId),sourcePackageSha256:packageSha256,cloneMode:'CLONE'},disclosureClassification:classification,secretScanStatus:scan.status,secretScanFindings:clone(scan.findings),detectedFormat:format.detectedFormat,parserAdapterIdentity:format.parserAdapterIdentity,parseContract:format.parseContract,polyglotStatus:format.polyglotStatus};clonedArtifacts.push(row);project.stages[1].authorizedFiles.push({artifactId:targetId,filename:source.filename,byteSize:source.byteSize,sha256:source.sha256,mediaType:source.mediaType,sourceArtifactId:String(source.artifactId)});}
  project.projectData.migrationArchives.push({kind:'CLONE_SOURCE_SNAPSHOT',schema:String(sourceProject.schema||'UNKNOWN'),sourceJobId:projectIdentity(sourceProject),sourcePackageSha256:packageSha256,preservedAt:now(),operational:false,sourceProject:clone(sourceProject),artifactIdMap:clone(artifactIdMap),activeStateCarriedForward:false,statement:'Historical signatures and external references remain evidence about the source project. The clone restarts at Stage 01 with only human input and verified copied bytes active.'});engine.addHistory(project,'PROJECT_CLONED_FROM_VERIFIED_IMPORT',{sourceJobId:projectIdentity(sourceProject),sourcePackageSha256:packageSha256,newJobId,artifactIdMap,activeWorkflowStateReused:false});engine.recalculate(project);return {project,verifiedArtifacts:clonedArtifacts,sourceJobId:projectIdentity(sourceProject),artifactIdMap};}
async function importPackage(blob,{mode='RESTORE'}={}){
  try{
    const normalizedMode=String(mode||'').toUpperCase();if(!['RESTORE','CLONE'].includes(normalizedMode))throw storageError('Import mode must be explicitly RESTORE or CLONE.','IMPORT_MODE_REQUIRED');if(!(blob instanceof Blob))throw storageError('A project-package Blob is required.','IMPORT_PACKAGE_REQUIRED');if(blob.size>SECURITY_LIMITS.maxPackageCompressedBytes)throw storageError('Compressed package exceeds the centralized pre-activation limit.','IMPORT_RESOURCE_LIMIT');await storageCapacityPreflight(Math.min(SECURITY_LIMITS.maxPackageExpandedBytes,Math.max(blob.size*4,blob.size)),{operation:'Project-package import'});const compressed=new Uint8Array(await blob.arrayBuffer()),expanded=await decompressBytes(compressed),json=new TextDecoder('utf-8',{fatal:true}).decode(expanded);scanJsonResourceLimits(json);const ambiguityScanner=globalThis.closedLoopResponseIngestion?.scanJsonAmbiguity;if(typeof ambiguityScanner!=='function')throw storageError('The duplicate-member ambiguity scanner is unavailable; import fails closed.','IMPORT_VALIDATOR_UNAVAILABLE');ambiguityScanner(json,SECURITY_LIMITS.maxJsonDepth);const payload=JSON.parse(json);rejectUnknownKeys(payload,PROJECT_PACKAGE_ROOT_KEYS,'Project package');const {packageSha256,...body}=payload;if(!/^[a-f0-9]{64}$/i.test(String(packageSha256||''))||hash.sha256Value(body)!==packageSha256)throw storageError('Project package hash mismatch.','IMPORT_PACKAGE_HASH_MISMATCH');if(body.schema!=='closed-loop-project-package/1')throw storageError('Unsupported project package schema.','IMPORT_PACKAGE_SCHEMA_UNSUPPORTED');
    const schemaApi=globalThis.closedLoopWorkflowSchema,project=clone(body.project),sourceId=projectIdentity(project),packageArtifacts=Array.isArray(body.artifacts)?body.artifacts:[];if(project?.schema!=='closed-loop-project/3'||project?.workflow!=='mobile-closed-loop/30'||Number(project?.stageCount)!==30||Object.keys(project?.stages||{}).length!==30)throw storageError('Imported project identity or stage count is invalid.','IMPORT_PROJECT_IDENTITY_INVALID');if(body.projectSchema!==project.schema||body.workflow!==project.workflow||body.responseSchema!==schemaApi?.RESPONSE_SCHEMA)throw storageError('Package schema manifest does not match the embedded project.','IMPORT_MANIFEST_SCHEMA_MISMATCH');if(!sourceId)throw storageError('Imported project has no JOB_ID.','IMPORT_PROJECT_IDENTITY_INVALID');validateMaterializedImportLimits(project,packageArtifacts);if(packageArtifacts.length>SECURITY_LIMITS.maxArtifactCount)throw storageError('Package artifact count exceeds the centralized limit.','IMPORT_RESOURCE_LIMIT');assertProjectIntegrity(project);const artifactIds=packageArtifacts.map(a=>String(a?.artifactId||''));if(artifactIds.some(x=>!x)||new Set(artifactIds).size!==artifactIds.length)throw storageError('Package artifacts contain a missing or duplicate artifact identity.','IMPORT_ARTIFACT_IDENTITY_INVALID');const verifiedArtifacts=[];
    for(const a of packageArtifacts){rejectUnknownKeys(a,PROJECT_PACKAGE_ARTIFACT_KEYS,`Package artifact ${a?.artifactId||'UNKNOWN'}`);if(a.jobId!==undefined&&String(a.jobId)!==sourceId)throw storageError(`Artifact ${a.artifactId} belongs to a different JOB_ID than the package project.`,'IMPORT_CROSS_PROJECT_ARTIFACT');if(!Number.isSafeInteger(Number(a.byteSize))||Number(a.byteSize)<0||Number(a.byteSize)>SECURITY_LIMITS.maxArtifactBytes)throw storageError(`Artifact ${a.artifactId} byte size is outside the centralized limit.`,'IMPORT_RESOURCE_LIMIT');if(typeof a.base64!=='string'||a.base64.length>Math.ceil(Number(a.byteSize)*4/3)+8)throw storageError(`Artifact ${a.artifactId} base64 representation is invalid or excessive.`,'IMPORT_ARTIFACT_ENCODING_INVALID');const path=normalizeArtifactPath(a.canonicalPath||a.filename),bytes=base64ToBytes(a.base64);if(bytes.byteLength!==Number(a.byteSize)||bytesToBase64(bytes)!==a.base64)throw storageError(`Artifact ${a.artifactId} byte size or canonical base64 representation is invalid.`,'IMPORT_ARTIFACT_IDENTITY_MISMATCH');const digest=await hash.sha256Bytes(bytes);if(digest!==a.sha256)throw storageError(`Artifact ${a.artifactId} hash mismatch.`,'IMPORT_ARTIFACT_IDENTITY_MISMATCH');const scan=scanArtifactForKnownSecrets(bytes,path.canonicalPath),format=sniffArtifactFormat(bytes,path.canonicalPath,a.mediaType||'application/octet-stream'),classification=scan.status==='LEAKAGE_DETECTED'?'CREDENTIAL_SECRET':String(a.disclosureClassification||'UNKNOWN').toUpperCase();if(!DISCLOSURE_CLASSIFICATIONS.has(classification))throw storageError(`Artifact ${a.artifactId} has unsupported disclosure classification ${classification||'EMPTY'}.`,'ARTIFACT_DISCLOSURE_CLASSIFICATION_INVALID');if(format.polyglotStatus==='MISMATCHED_OR_AMBIGUOUS')throw storageError(`Artifact ${a.artifactId} has a misleading filename/media interpretation.`,'IMPORT_FORMAT_AMBIGUOUS');verifiedArtifacts.push({...clone(a),jobId:sourceId,filename:path.canonicalPath,rawFilename:String(a.rawFilename||path.rawFilename),displayFilename:path.displayFilename,canonicalPath:path.canonicalPath,filenameNormalizationVersion:FILENAME_NORMALIZATION_VERSION,collisionKeys:path.collisionKeys,declaredMediaType:String(a.declaredMediaType||a.mediaType||'application/octet-stream'),detectedFormat:format.detectedFormat,parserAdapterIdentity:format.parserAdapterIdentity,parseContract:format.parseContract,polyglotStatus:format.polyglotStatus,disclosureClassification:classification,secretScanStatus:scan.status,secretScanFindings:clone(scan.findings),blob:new Blob([bytes],{type:a.mediaType||'application/octet-stream'})});}
    assertNoArtifactPathCollisions(verifiedArtifacts,{label:'Imported project package'});const manifest=body.packageManifest||{};rejectUnknownKeys(manifest,PROJECT_PACKAGE_MANIFEST_KEYS,'Project package manifest');if(manifest.schema!=='closed-loop-project-package-manifest/1'||manifest.filenameNormalizationVersion!==FILENAME_NORMALIZATION_VERSION||manifest.archiveEntryModel!=='SINGLE_GZIP_JSON; EMBEDDED ARTIFACT ARCHIVES ARE STORED AS BYTES AND NEVER EXPANDED')throw storageError('Package manifest schema, filename contract, or archive-entry model is unsupported.','IMPORT_MANIFEST_SCHEMA_MISMATCH');const manifestArtifacts=Array.isArray(manifest.artifacts)?manifest.artifacts:[],manifestIds=manifestArtifacts.map(a=>String(a?.artifactId||''));for(const entry of manifestArtifacts)rejectUnknownKeys(entry,PROJECT_PACKAGE_MANIFEST_ARTIFACT_KEYS,`Package manifest artifact ${entry?.artifactId||'UNKNOWN'}`);if(manifestIds.some(x=>!x)||new Set(manifestIds).size!==manifestIds.length)throw storageError('Package manifest contains a missing or duplicate artifact identity.','IMPORT_MANIFEST_INVALID');if(manifest.jobId!==sourceId||Number(manifest.artifactCount)!==verifiedArtifacts.length||manifestArtifacts.length!==verifiedArtifacts.length||manifest.projectSha256!==projectSha256(project))throw storageError('Package manifest does not reconcile with the embedded project and artifacts.','IMPORT_MANIFEST_MISMATCH');if(!/^[a-f0-9]{64}$/i.test(String(manifest.artifactManifestSha256||''))||String(manifest.artifactManifestSha256)!==hash.sha256Value(manifestArtifacts))throw storageError('Package artifact-manifest digest mismatch.','IMPORT_MANIFEST_MISMATCH');const manifestById=new Map(manifestArtifacts.map(a=>[String(a.artifactId),a]));for(const a of verifiedArtifacts){const {base64,blob,secretScanFindings,...identity}=a;const expectedManifest={...identity,secretScanFindingCount:(secretScanFindings||[]).length},m=manifestById.get(String(a.artifactId));if(!m||hash.sha256Value(m)!==hash.sha256Value(expectedManifest))throw storageError(`Package manifest mismatch for artifact ${a.artifactId}.`,'IMPORT_MANIFEST_MISMATCH');}
    const backupPolicy=body.backupPolicy||{},checkpoint=body.checkpoint||{};rejectUnknownKeys(backupPolicy,PROJECT_PACKAGE_BACKUP_POLICY_KEYS,'Project backup policy');rejectUnknownKeys(checkpoint,PROJECT_PACKAGE_CHECKPOINT_KEYS,'Project checkpoint');if(backupPolicy.schema!=='closed-loop-backup-policy/1'||backupPolicy.artifactBytesIncluded!==true||backupPolicy.browserLocalCopyIsDisasterRecoveryBackup!==false)throw storageError('Backup policy is missing required artifact custody or disaster-recovery truth.','IMPORT_BACKUP_POLICY_INVALID');if(checkpoint.schema!=='closed-loop-checkpoint/1'||checkpoint.jobId!==sourceId||Number(checkpoint.revision)!==Number(project.revision||0)||checkpoint.projectDigest!==projectSha256(project)||checkpoint.artifactManifestDigest!==manifest.artifactManifestSha256||checkpoint.hashAlgorithm!=='SHA-256'||!String(checkpoint.authenticationStatus||'').includes('SELF_CONSISTENCY_ONLY'))throw storageError('Package checkpoint is not self-consistent with the exact imported project and artifacts.','IMPORT_CHECKPOINT_MISMATCH');
    const engine=globalThis.closedLoopWorkflowEngine,packagedById=new Map(verifiedArtifacts.map(a=>[String(a.artifactId),a]));for(const artifact of (project.projectData?.artifacts||[]).filter(record=>record?.active!==false&&!record?.invalidatedBy)){const artifactId=String(engine?.recordId?.(artifact,'artifacts')||''),availability=String(engine?.recordValue?.(artifact,'AVAILABILITY')||'').toUpperCase();if(availability!=='BYTES_PERSISTED_AND_VERIFIED')continue;const packaged=packagedById.get(artifactId);if(!packaged||String(engine.recordValue(artifact,'FILENAME')||'')!==String(packaged.filename)||Number(engine.recordValue(artifact,'BYTE_SIZE'))!==Number(packaged.byteSize)||String(engine.recordValue(artifact,'SHA256')||'')!==String(packaged.sha256))throw storageError(`Canonical artifact ${artifactId||'UNKNOWN'} does not reconcile with verified package bytes.`,'IMPORT_CANONICAL_ARTIFACT_MISMATCH');}
    let next=project,targetId=sourceId,importArtifacts=verifiedArtifacts,cloneInfo=null;if(normalizedMode==='CLONE'){cloneInfo=await buildImportedClone(project,verifiedArtifacts,packageSha256);next=cloneInfo.project;targetId=projectIdentity(next);importArtifacts=cloneInfo.verifiedArtifacts;assertProjectIntegrity(next);}const requiredStorage=importArtifacts.reduce((total,item)=>total+Number(item.byteSize||0),0)+utf8Length(JSON.stringify(canonicalProject(next)));await storageCapacityPreflight(requiredStorage*2,{operation:`Project ${normalizedMode.toLowerCase()}`});
    fault('before-import-transaction');const tx=await openTransaction([PROJECTS,ARTIFACTS,META],'readwrite'),projects=tx.objectStore(PROJECTS),artifacts=tx.objectStore(ARTIFACTS),meta=tx.objectStore(META);try{const prior=await request(projects.get(targetId));if(prior){if(normalizedMode==='RESTORE'&&String(prior.projectSha256)===projectSha256(next)){const existingRows=(await request(artifacts.getAll())).filter(row=>String(row.jobId)===targetId),byId=new Map(existingRows.map(row=>[String(row.artifactId),row]));if(existingRows.length!==importArtifacts.length)throw storageError('RESTORE conflicts with the active project artifact set; no existing data was overwritten.','RESTORE_PROJECT_CONFLICT');for(const incoming of importArtifacts){const current=byId.get(String(incoming.artifactId));if(!current||String(current.sha256)!==String(incoming.sha256)||Number(current.byteSize)!==Number(incoming.byteSize)||String(current.filename)!==String(incoming.filename))throw storageError(`RESTORE conflicts with current stored bytes for ${incoming.artifactId}.`,'RESTORE_PROJECT_CONFLICT');}await complete(tx);const unchanged=clone(prior.project);unchanged.revision=Number(prior.revision||unchanged.revision||0);unchanged.projectSha256=prior.projectSha256;return unchanged;}throw storageError(normalizedMode==='RESTORE'?`RESTORE cannot silently overwrite the active project ${targetId}. Delete the conflicting local project or choose CLONE.`:`The application-generated clone identity ${targetId} unexpectedly collides with an active project.`,'RESTORE_PROJECT_CONFLICT');}const existingArtifacts=await request(artifacts.getAll()),incomingIds=new Set(importArtifacts.map(a=>String(a.artifactId)));for(const existing of existingArtifacts)if(incomingIds.has(String(existing.artifactId)))throw storageError(`Imported artifact identity ${existing.artifactId} already belongs to a stored project.`,'CROSS_PROJECT_ARTIFACT_ID_COLLISION');next=clone(next);delete next.projectSha256;if(normalizedMode==='CLONE')next.revision=1;assertProjectIntegrity(next);const digest=projectSha256(next);fault('during-import-project-write');projects.put({jobId:targetId,revision:Number(next.revision||0),project:next,projectSha256:digest,updatedAt:now()});for(const a of importArtifacts){fault('during-import-artifact-write');artifacts.put({artifactId:String(a.artifactId),jobId:targetId,blob:a.blob,filename:String(a.filename),rawFilename:String(a.rawFilename||a.filename),displayFilename:String(a.displayFilename||a.filename.split('/').at(-1)),canonicalPath:String(a.canonicalPath||a.filename),filenameNormalizationVersion:FILENAME_NORMALIZATION_VERSION,collisionKeys:clone(a.collisionKeys||{}),mediaType:String(a.mediaType||'application/octet-stream'),declaredMediaType:String(a.declaredMediaType||a.mediaType||'application/octet-stream'),detectedFormat:String(a.detectedFormat||'BINARY_UNKNOWN'),parserAdapterIdentity:String(a.parserAdapterIdentity||FORMAT_SNIFFER_VERSION),parseContract:String(a.parseContract||''),polyglotStatus:String(a.polyglotStatus||'UNKNOWN'),disclosureClassification:String(a.disclosureClassification||'UNKNOWN'),secretScanStatus:String(a.secretScanStatus||'NO_KNOWN_PATTERN_DETECTED_HEURISTIC_ONLY'),secretScanFindings:clone(a.secretScanFindings||[]),byteSize:Number(a.byteSize),sha256:String(a.sha256),lineage:clone(a.lineage||{}),lifecycleStatus:ARTIFACT_LIFECYCLE.CANONICAL,promotedAt:now(),promotionBasis:`VERIFIED_ATOMIC_${normalizedMode}_IMPORT`,createdAt:a.createdAt||now(),updatedAt:now()});}meta.put({key:'selectedProject',value:targetId,updatedAt:now()});meta.put({key:'lastCommittedRevision',value:{jobId:targetId,revision:Number(next.revision||0),projectSha256:digest},updatedAt:now()});meta.put({key:'lastVerifiedImport',value:{jobId:targetId,sourceJobId:sourceId,mode:normalizedMode,packageSha256,artifactCount:importArtifacts.length,artifactIdMap:cloneInfo?.artifactIdMap||null,at:now()},updatedAt:now()});fault('before-import-commit');await complete(tx);next.projectSha256=digest;try{new BroadcastChannel('closed-loop-reliability').postMessage({type:'PROJECT_CHANGED',jobId:targetId,revision:next.revision});}catch{}return next;}catch(error){try{tx.abort();}catch{}throw error;}
  }catch(error){throw importFailure(error);}
}

async function createExecutionPackage({project=null,jobId=null,stage,operation=null,testIds=[],productId=null,runId=null,reviewerAliasContext=null,instructionId=null,operationReservationId=null,packageId=null,disclosureOnly=false}={}){
  if(!project&&jobId){const all=await readAll();project=all.find(item=>projectIdentity(item)===String(jobId))||null;}
  if(!project||typeof project!=='object')throw new Error('A canonical project is required for an execution package.');
  const engine=globalThis.closedLoopWorkflowEngine,promptEngine=globalThis.closedLoopPromptEngine,canonicalJobId=projectIdentity(project);if(jobId&&String(jobId)!==canonicalJobId)throw storageError(`Execution-package job ${jobId} does not match canonical project ${canonicalJobId}.`,'EXECUTION_PACKAGE_JOB_MISMATCH');
  const normalizedStage=Number(stage),normalizedOperation=String(operation||globalThis.closedLoopWorkflowSchema?.STAGE_CONTRACTS?.[normalizedStage]?.operations?.[0]||'COMPLETE'),normalizedRunId=runId?String(runId):null,ids=[...new Set(testIds.map(String).filter(Boolean))];
  if(!promptEngine?.responseContractDescriptor)throw storageError('The prompt authority is unavailable for execution-package construction.','EXECUTION_PACKAGE_PROMPT_AUTHORITY_UNAVAILABLE');
  const prompts=(project.projectData?.generatedPrompts||[]).filter(record=>Number(record?.stage)===normalizedStage&&!record?.invalidatedBy&&String(record?.operation||'COMPLETE')===normalizedOperation&&String(record?.promptEngineVersion||'')===String(promptEngine.version||''));
  const lanePrompts=prompts.filter(record=>!normalizedRunId||String(record?.scope?.runId||'')===normalizedRunId),selectedPrompt=instructionId?lanePrompts.find(record=>String(record?.instructionId||record?.promptId||'')===String(instructionId)):lanePrompts.at(-1);
  if(!selectedPrompt)throw storageError('Save the current controlling instruction before preparing this execution package. No current saved instruction exists for this exact stage, operation, and run lane.','EXECUTION_PACKAGE_CURRENT_PROMPT_REQUIRED');
  const reservationId=String(operationReservationId||'').trim();if(!reservationId)throw storageError('A current operation reservation is required before execution-package construction.','EXECUTION_PACKAGE_RESERVATION_REQUIRED');
  const operationReservations=engine.records(project,'operationReservations'),reservation=operationReservations.find(record=>engine.recordId(record,'operationReservations')===reservationId&&engine.isActiveRecord(record));if(!reservation)throw storageError(`Operation reservation ${reservationId} is missing, inactive, or stale.`,'EXECUTION_PACKAGE_RESERVATION_STALE');
  const reservationValue=field=>engine.recordValue(reservation,field),reservationStatus=String(reservationValue('STATUS')||'').toUpperCase(),reservationPackageId=String(reservationValue('PACKAGE_ID')||''),challengeNonce=String(reservationValue('CHALLENGE_NONCE')||''),reservationScope=reservationValue('SCOPE')||{},reservationExpectedRevision=Number(reservationValue('EXPECTED_REVISION')),bindingReceiptId=String(reservationValue('BINDING_RECEIPT_ID')||''),bindingReceiptSha256=String(reservationValue('BINDING_RECEIPT_SHA256')||'');
  if(reservationStatus!=='ACTIVE')throw storageError(`Operation reservation ${reservationId} is ${reservationStatus||'UNKNOWN'}, not ACTIVE.`,'EXECUTION_PACKAGE_RESERVATION_STALE');if(Number(reservationValue('STAGE'))!==normalizedStage||String(reservationValue('OPERATION'))!==normalizedOperation)throw storageError('Operation reservation targets a different stage or operation.','EXECUTION_PACKAGE_RESERVATION_MISMATCH');if(!reservationPackageId||reservationPackageId!==String(packageId||''))throw storageError('Execution-package identity does not match the operation reservation.','EXECUTION_PACKAGE_ID_MISMATCH');if(String(reservationValue('PROMPT_ID')||'')!==String(selectedPrompt.instructionId||selectedPrompt.promptId||''))throw storageError('Operation reservation is bound to a different controlling prompt.','EXECUTION_PACKAGE_PROMPT_MISMATCH');if(!/^[0-9a-f]{32,}$/i.test(challengeNonce))throw storageError('Operation reservation challenge nonce is missing or weaker than 128 bits.','EXECUTION_PACKAGE_NONCE_INVALID');const projectRevision=Number(project.revision);if(!Number.isSafeInteger(reservationExpectedRevision)||reservationExpectedRevision<0||(disclosureOnly?![reservationExpectedRevision,reservationExpectedRevision+1].includes(projectRevision):projectRevision!==reservationExpectedRevision+1))throw storageError('Operation reservation is not bound to the immediately committed current project revision.','EXECUTION_PACKAGE_REVISION_STALE');
  const exactPrompt=String(selectedPrompt.prompt||'');if(!exactPrompt)throw storageError('The saved controlling instruction has no exact prompt text.','EXECUTION_PACKAGE_PROMPT_TEXT_MISSING');
  const fullTextSha256=hash.sha256Text(exactPrompt);if(selectedPrompt.fullTextSha256&&String(selectedPrompt.fullTextSha256)!==fullTextSha256)throw storageError('The saved controlling instruction text no longer matches its recorded identity.','EXECUTION_PACKAGE_PROMPT_IDENTITY_MISMATCH');
  const promptReservationBinding=selectedPrompt.operationReservation||null;if(selectedPrompt.operationReservationBound!==true||!promptReservationBinding||String(promptReservationBinding.operationReservationId||'')!==reservationId||String(promptReservationBinding.packageId||'')!==reservationPackageId||String(promptReservationBinding.challengeNonce||'')!==challengeNonce||Number(promptReservationBinding.expectedRevision)!==reservationExpectedRevision||String(promptReservationBinding.scopeHash||'')!==hash.sha256Value(reservationScope)||String(promptReservationBinding.targetSlot||'')!==String(reservationValue('TARGET_SLOT')||''))throw storageError('The controlling prompt does not carry the exact current operation-reservation binding.','EXECUTION_PACKAGE_PROMPT_RESERVATION_MISMATCH');const responseContract=promptEngine.responseContractDescriptor(normalizedStage,normalizedOperation,promptReservationBinding),contractSha256=hash.sha256Value(responseContract);if(String(selectedPrompt.contractSha256||'')!==contractSha256)throw storageError('The saved controlling instruction response contract is stale. Save the current instruction again before preparing the package.','EXECUTION_PACKAGE_CONTRACT_STALE');
  if(normalizedRunId&&String(selectedPrompt.scope?.runId||'')!==normalizedRunId)throw storageError('The saved controlling instruction is bound to a different run lane.','EXECUTION_PACKAGE_RUN_MISMATCH');
  const plan=engine.executionHandoff(project,{stage:normalizedStage,operation:normalizedOperation,testIds:ids,runIds:normalizedRunId?[normalizedRunId]:null}),knownPlanIds=new Set(engine.testExecutionPlan(project).items.map(item=>String(item.testId||'')));if(ids.some(id=>!knownPlanIds.has(id)))throw storageError('Execution-package test allowlist contains an unknown, stale, or unroutable test identity.','EXECUTION_PACKAGE_TEST_ALLOWLIST_MISMATCH');const artifactIds=[...new Set(plan.send.map(x=>String(x.artifactId||'')).filter(Boolean))],withheldArtifactIds=new Set((plan.withhold||[]).map(item=>String(item?.artifactId||'')).filter(Boolean));if(artifactIds.some(id=>withheldArtifactIds.has(id)))throw storageError('An artifact is simultaneously required and withheld; package creation fails closed.','EXECUTION_PACKAGE_DISCLOSURE_CONFLICT');
  const artifactEntries=[];
  for(const artifactId of artifactIds){const canonical=engine.records(project,'artifacts').find(r=>engine.recordId(r,'artifacts')===artifactId&&engine.isActiveRecord(r));if(!canonical)throw storageError(`Execution-package artifact ${artifactId} is not current canonical state.`,'EXECUTION_PACKAGE_ARTIFACT_STALE');const row=await getArtifact(artifactId);if(!row||String(row.jobId)!==canonicalJobId)throw storageError(`Execution-package artifact ${artifactId} has no stored bytes for ${canonicalJobId}.`,'EXECUTION_PACKAGE_BYTES_MISSING');if(['REDACTION','METADATA_REMOVAL','DEIDENTIFICATION'].includes(String(row.lineage?.transformationKind||'').toUpperCase())&&row.lineage?.externalTransferEligibility!=='REQUIRES_CURRENT_DISCLOSURE_POLICY')throw storageError(`Derived artifact ${artifactId} cannot be transferred until accepted semantic reasoning establishes that removed content is unnecessary for the tested proposition.`,'DERIVED_ARTIFACT_PROPOSITION_EFFECT_UNKNOWN');const bytes=new Uint8Array(await row.blob.arrayBuffer()),sha256=await hash.sha256Bytes(bytes),byteSize=bytes.byteLength,expectedSha=String(engine.recordValue(canonical,'SHA256')||''),expectedSize=Number(engine.recordValue(canonical,'BYTE_SIZE')),filename=String(engine.recordValue(canonical,'CANONICAL_PATH')||engine.recordValue(canonical,'FILENAME')||row.filename||artifactId),normalizedName=normalizeArtifactPath(filename),secretScan=scanArtifactForKnownSecrets(bytes,filename),classification=String(engine.recordValue(canonical,'DISCLOSURE_CLASSIFICATION')||row.disclosureClassification||'UNKNOWN').toUpperCase(),format=sniffArtifactFormat(bytes,filename,row.mediaType||'application/octet-stream');if(sha256!==expectedSha||byteSize!==expectedSize)throw storageError(`Execution-package artifact ${artifactId} failed byte identity verification.`,'EXECUTION_PACKAGE_ARTIFACT_MISMATCH');if(classification==='CREDENTIAL_SECRET'||secretScan.status==='LEAKAGE_DETECTED')throw storageError(`Execution-package artifact ${artifactId} (${filename}) contains credential/secret material and must not be transferred externally.`,'CREDENTIAL_SECRET_EXTERNAL_TRANSFER_PROHIBITED');if(format.polyglotStatus==='MISMATCHED_OR_AMBIGUOUS')throw storageError(`Execution-package artifact ${artifactId} format does not match its authorized filename/media interpretation.`,'EXECUTION_PACKAGE_FORMAT_AMBIGUOUS');artifactEntries.push({artifactId,filename:normalizedName.canonicalPath,rawFilename:String(engine.recordValue(canonical,'RAW_FILENAME')||row.rawFilename||filename),filenameNormalizationVersion:FILENAME_NORMALIZATION_VERSION,collisionKeys:normalizedName.collisionKeys,mediaType:String(row.mediaType||'application/octet-stream'),detectedFormat:format.detectedFormat,parserAdapterIdentity:format.parserAdapterIdentity,polyglotStatus:format.polyglotStatus,disclosureClassification:classification,secretScanStatus:secretScan.status,secretScanLimitation:'A clean heuristic scan is not proof that no secret exists.',byteSize,sha256,role:String(engine.recordValue(canonical,'ROLE')||''),lineage:clone(row.lineage||{}),base64:bytesToBase64(bytes)});}
  assertNoArtifactPathCollisions(artifactEntries,{label:'Execution package allowlist'});const tests=engine.records(project,'tests').filter(t=>ids.includes(engine.recordId(t,'tests'))).map(t=>({testId:engine.recordId(t,'tests'),requirementId:String(engine.recordValue(t,'REQ_ID')||t.relationships?.REQ_ID||''),fields:clone(t.fields||{}),relationships:clone(t.relationships||{})}));if(tests.length!==ids.length)throw storageError('Execution package contains an unknown, stale, or duplicated requested test identity.','EXECUTION_PACKAGE_TEST_ALLOWLIST_MISMATCH');
  const selectedScope=selectedPrompt.scope||{},scopeHash=hash.sha256Value(selectedScope);if(hash.sha256Value(reservationScope)!==scopeHash)throw storageError('Operation reservation scope does not match the controlling prompt scope.','EXECUTION_PACKAGE_SCOPE_MISMATCH');const promptScopeHash=String(selectedPrompt.scopeSha256||scopeHash),promptId=String(selectedPrompt.instructionId||selectedPrompt.promptId||''),binding={reservationId,promptId,packageId:reservationPackageId||null,bodySha256:String(selectedPrompt.bodySha256||selectedPrompt.sha256||''),contractSha256:String(selectedPrompt.contractSha256||''),contextSignature:String(selectedPrompt.contextSignature||''),promptScopeHash,expectedRevision:reservationExpectedRevision,stage:normalizedStage,operation:normalizedOperation,targetSlot:String(reservationValue('TARGET_SLOT')||''),scope:reservationScope,challengeNonce};if(!bindingReceiptId||!/^[0-9a-f]{64}$/i.test(bindingReceiptSha256)||hash.sha256Value(binding)!==bindingReceiptSha256)throw storageError('Operation reservation lacks a valid receipt binding the exact current prompt, contract, context, scope, package, and nonce.','EXECUTION_PACKAGE_RESERVATION_UNBOUND');for(const [field,value] of [['PROMPT_BODY_SHA256',binding.bodySha256],['PROMPT_CONTRACT_SHA256',binding.contractSha256],['PROMPT_CONTEXT_SIGNATURE',binding.contextSignature],['PROMPT_SCOPE_HASH',binding.promptScopeHash]])if(String(reservationValue(field)||'')!==value)throw storageError(`Operation reservation ${field} does not match the controlling prompt.`,'EXECUTION_PACKAGE_PROMPT_MISMATCH');const promptAliases=Array.isArray(selectedPrompt.contextManifest?.blindAliasMap)?selectedPrompt.contextManifest.blindAliasMap:[],providedAlias=reviewerAliasContext&&typeof reviewerAliasContext==='object'?reviewerAliasContext:null,aliasEntries=providedAlias?[providedAlias]:promptAliases,reviewerAlias=String(aliasEntries[0]?.alias||aliasEntries[0]?.reviewerAlias||'').trim()||null,publicIdentity=value=>{const text=String(value??'');const match=aliasEntries.find(entry=>String(entry.canonicalId||'')===text);return match?String(match.alias):value;},publicScope=Object.fromEntries(Object.entries(selectedScope).map(([key,value])=>[key,publicIdentity(value)]));
  const instruction={instructionId:String(selectedPrompt.instructionId||selectedPrompt.promptId||''),promptEngineVersion:String(selectedPrompt.promptEngineVersion||''),bodySha256:String(selectedPrompt.bodySha256||selectedPrompt.sha256||''),contractSha256:String(selectedPrompt.contractSha256||''),contextSignature:String(selectedPrompt.contextSignature||''),scope:clone(publicScope),fullTextSha256,text:exactPrompt};
  const promptIdentity={instructionId:instruction.instructionId,bodySha256:instruction.bodySha256,contractSha256:instruction.contractSha256,contextSignature:instruction.contextSignature,fullTextSha256},createdAt=String(reservationValue('BOUND_AT_DEVICE_TIME')||reservation.createdAt||''),artifactManifest=artifactEntries.map(({base64,...x})=>x),manifest={schema:'closed-loop-verification-package/1',workflow:project.workflow,projectSchema:project.schema,responseSchema:globalThis.closedLoopWorkflowSchema?.RESPONSE_SCHEMA,PACKAGE_ID:reservationPackageId,OPERATION_RESERVATION_ID:reservationId,packageId:reservationPackageId,operationReservationId:reservationId,challengeNonce,currentProjectRevision:Number(selectedScope.projectRevision),reservationExpectedRevision,scopeHash,promptIdentity,contextSignature:instruction.contextSignature,reservationBindingReceiptId:bindingReceiptId,reservationBindingReceiptSha256:bindingReceiptSha256,packageValidity:'CURRENT_REVISION_AND_SCOPE_BOUND',jobId:canonicalJobId,stage:normalizedStage,operation:normalizedOperation,runId:publicIdentity(normalizedRunId),reviewerAlias,productId:publicIdentity(productId||project.job?.CURRENT_PRODUCT_ID||null),testIds:ids,instructionId:instruction.instructionId,instructionFullTextSha256:fullTextSha256,responseContractSha256:contractSha256,artifacts:artifactManifest,handoff:clone(plan),createdAt,disclosurePolicy:'ONE_DERIVED_ALLOWLIST; CREDENTIAL_SECRET prohibited; RESTRICTED/UNKNOWN artifacts or serialized non-artifact payload require an exact current human decision.',secretScanLimit:'Known-pattern scanning cannot prove that no secret exists; clean serialized non-artifact payload remains UNKNOWN and requires exact authorization.'},packageBody={manifest,instruction,responseContract,tests,artifacts:artifactEntries},packageSha256=hash.sha256Value(packageBody),nonArtifactPayload={manifest,instruction,responseContract,tests,packageSha256},outboundComponent=classifyOutboundPayload(nonArtifactPayload,{componentId:'SERIALIZED_NON_ARTIFACT_PACKAGE_PAYLOAD'}),outboundComponents=[outboundComponent],disclosure=engine.currentDisclosureAuthorization(project,{stage:normalizedStage,operation:normalizedOperation,artifactIds,outboundComponents});
  if(disclosureOnly)return {artifactIds,outboundComponents,disclosure,nonArtifactPayloadSha256:outboundComponent.sha256,nonArtifactSecretScanStatus:outboundComponent.secretScanStatus};
  if(disclosure.prohibited)throw storageError(`External package blocked: ${disclosure.reasons?.join(' ')||'credential or secret material can never be transferred externally.'}`,'CREDENTIAL_SECRET_EXTERNAL_TRANSFER_PROHIBITED');if(disclosure.required&&!disclosure.authorized)throw storageError(`External package blocked: ${disclosure.reasons?.join(' ')||'restricted or unknown outbound material requires an exact current human decision and recipient suitability confirmation.'}`,'EXTERNAL_DISCLOSURE_AUTHORIZATION_REQUIRED');const finalNonArtifactScan=classifyOutboundPayload(nonArtifactPayload,{componentId:outboundComponent.componentId});if(finalNonArtifactScan.sha256!==outboundComponent.sha256||finalNonArtifactScan.byteSize!==outboundComponent.byteSize||finalNonArtifactScan.classification!==outboundComponent.classification||finalNonArtifactScan.secretScanStatus!==outboundComponent.secretScanStatus)throw storageError('Execution package non-artifact payload changed after its disclosure decision.','EXECUTION_PACKAGE_DISCLOSURE_BINDING_MISMATCH');if(finalNonArtifactScan.classification==='CREDENTIAL_SECRET'||finalNonArtifactScan.secretScanStatus==='LEAKAGE_DETECTED')throw storageError('Execution package serialized non-artifact payload contains credential/secret material and must not be transferred externally.','CREDENTIAL_SECRET_EXTERNAL_TRANSFER_PROHIBITED');
  const payload={...packageBody,packageSha256},payloadBytes=new TextEncoder().encode(JSON.stringify(payload));await storageCapacityPreflight(payloadBytes.byteLength*2,{operation:'Execution-package creation'});const compressed=await compressBytes(payloadBytes);return {blob:new Blob([compressed],{type:'application/gzip'}),filename:`VERIFY-${canonicalJobId}-STAGE-${String(normalizedStage).padStart(2,'0')}.clverify.gz`,manifest,packageSha256,disclosureAuthorizationId:disclosure.authorization?engine.recordId(disclosure.authorization,'disclosureAuthorizations'):null,outboundAllowlist:clone(disclosure.binding.outboundAllowlist),outboundAllowlistSha256:disclosure.binding.outboundAllowlistSha256};
}
async function storageHealth(){let persistent=false,estimate={usage:null,quota:null};try{persistent=await navigator.storage.persist();estimate=await navigator.storage.estimate();}catch{}return {database:DB_NAME,persistent:Boolean(persistent),usage:estimate.usage??null,quota:estimate.quota??null,lastCommittedRevision:await metaGet('lastCommittedRevision'),lastVerifiedExport:await metaGet('lastVerifiedExport'),migrationStatus:await metaGet('migrationStatus')};}
function archiveMigrationPayload(project,archive){if(!project||typeof project!=='object')throw new TypeError('A project is required.');project.projectData=project.projectData&&typeof project.projectData==='object'?project.projectData:{};project.projectData.migrationArchives=Array.isArray(project.projectData.migrationArchives)?project.projectData.migrationArchives:[];const record={...clone(archive),operational:false};project.projectData.migrationArchives.push(record);return record;}
function clearLegacy(storage=globalThis.localStorage){if(!storage)return;for(const key of LEGACY_KEYS)try{storage.removeItem(key);}catch{}}

const ready=(async()=>{if(globalThis.indexedDB)try{await migrateLegacy();await reconcileArtifactStorage();globalThis.closedLoopLegacyMigrationError=null;}catch(error){globalThis.closedLoopLegacyMigrationError=String(error?.stack||error);console.error('Legacy migration or artifact recovery failed without deleting the preserved project payload; application startup will continue.',error);}return true;})();
const migrateProjectToCurrent=project=>{const migrate=globalThis.closedLoopWorkflowSchema?.migrateProjectToCurrent;if(typeof migrate!=='function')throw storageError('The current deterministic project migration is unavailable.','PROJECT_MIGRATION_UNAVAILABLE');return migrate(project);};
globalThis.closedLoopProjectStore=Object.freeze({archiveMigrationPayload,version:'closed-loop-project-store/3',DB_NAME,DB_VERSION,stores:Object.freeze({projects:PROJECTS,artifacts:ARTIFACTS,meta:META}),artifactLifecycle:ARTIFACT_LIFECYCLE,filenameNormalizationVersion:FILENAME_NORMALIZATION_VERSION,securityLimits:SECURITY_LIMITS,STORE_KEY,LEGACY_KEYS,clone,projectIdentity,projectSha256,validateProjectIntegrity,normalizeArtifactPath,assertNoArtifactPathCollisions,sniffArtifactFormat,scanArtifactForKnownSecrets,classifyOutboundPayload,storageCapacityPreflight,migrateProjectToCurrent,openDatabase,ready,readAll,writeAll,writeProject,replaceProject,transact,removeProject,stageArtifact,stageDerivedArtifact,promoteStagedArtifacts,abortStagedArtifacts,reconcileArtifactStorage,putArtifact,getArtifact,deleteArtifact,listArtifacts,verifyProjectArtifacts,createExecutionPackage,exportPackage,importPackage,storageHealth,metaGet,metaPut,clearLegacy});
})();
