import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
const context={console,TextDecoder,TextEncoder,Uint8Array,ArrayBuffer,structuredClone,crypto:globalThis.crypto};context.globalThis=context;vm.createContext(context);vm.runInContext(fs.readFileSync('hash.js','utf8'),context,{filename:'hash.js'});vm.runInContext(fs.readFileSync('test-runtime.js','utf8'),context,{filename:'test-runtime.js'});const r=context.closedLoopTestRuntime;
const base={version:r.SPEC_VERSION,languageVersion:r.TEST_IR_LANGUAGE_VERSION,operationRegistryVersion:r.OPERATION_REGISTRY_VERSION,operationRegistrySha256:r.OPERATION_REGISTRY_SHA256,steps:[{stepId:'S001',op:'LOAD_ARTIFACT',inputs:{binding:{bindingRef:'PRODUCT'}}},{stepId:'S002',op:'READ_BYTES',inputs:{artifact:{stepRef:'S001',output:'artifact'}}},{stepId:'S003',op:'DECODE_UTF8',inputs:{bytes:{stepRef:'S002',output:'bytes'}}}],result:{stepRef:'S003',output:'text'}};
assert.equal(r.validateSpec(base,{PRODUCT:'ARTIFACT-1'}).valid,true);
const wrong={...base,steps:[...base.steps,{stepId:'S004',op:'PARSE_JSON',inputs:{text:{stepRef:'S003',output:'text'}}},{stepId:'S005',op:'COUNT',inputs:{value:{stepRef:'S004',output:'value'}}},{stepId:'S006',op:'DECODE_UTF8',inputs:{bytes:{stepRef:'S005',output:'count'}}}],result:{stepRef:'S006',output:'text'}};
const check=r.validateSpec(wrong,{PRODUCT:'ARTIFACT-1'});assert.equal(check.valid,false);assert(check.issues.some(x=>/requires BYTES.*produces INTEGER/.test(x)),check.issues.join('\n'));
const wrongBytes={...base,steps:[{stepId:'S001',op:'LOAD_ARTIFACT',inputs:{binding:{bindingRef:'PRODUCT'}}},{stepId:'S002',op:'READ_BYTES',inputs:{artifact:{stepRef:'S001',output:'artifact'}}},{stepId:'S003',op:'HASH_SHA256',inputs:{bytes:{stepRef:'S001',output:'artifact'}}}],result:{stepRef:'S003',output:'sha256'}};const check2=r.validateSpec(wrongBytes,{PRODUCT:'ARTIFACT-1'});assert.equal(check2.valid,false);assert(check2.issues.some(x=>/requires BYTES.*produces ARTIFACT/.test(x)),check2.issues.join('\n'));
const header={version:r.SPEC_VERSION,languageVersion:r.TEST_IR_LANGUAGE_VERSION,operationRegistryVersion:r.OPERATION_REGISTRY_VERSION,operationRegistrySha256:r.OPERATION_REGISTRY_SHA256};
const sumString={...header,steps:[{stepId:'S001',op:'LOAD_ARTIFACT',inputs:{binding:{bindingRef:'PRODUCT'}}},{stepId:'S002',op:'READ_BYTES',inputs:{artifact:{stepRef:'S001',output:'artifact'}}},{stepId:'S003',op:'HASH_SHA256',inputs:{bytes:{stepRef:'S002',output:'bytes'}}},{stepId:'S004',op:'SUM',inputs:{value:{stepRef:'S003',output:'sha256'}}}],result:{stepRef:'S004',output:'value'}};
const sumCheck=r.validateSpec(sumString,{PRODUCT:'ARTIFACT-1'});assert.equal(sumCheck.valid,false);assert(sumCheck.issues.some(x=>/input value requires VALUE.*produces STRING/.test(x)),sumCheck.issues.join('\n'));
const xmlPathBytes={...header,steps:[{stepId:'S001',op:'LOAD_ARTIFACT',inputs:{binding:{bindingRef:'PRODUCT'}}},{stepId:'S002',op:'READ_BYTES',inputs:{artifact:{stepRef:'S001',output:'artifact'}}},{stepId:'S003',op:'DECODE_UTF8',inputs:{bytes:{stepRef:'S002',output:'bytes'}}},{stepId:'S004',op:'PARSE_XML',inputs:{text:{stepRef:'S003',output:'text'}}},{stepId:'S005',op:'SELECT_XML',inputs:{value:{stepRef:'S004',output:'value'},path:{stepRef:'S002',output:'bytes'}}}],result:{stepRef:'S005',output:'selection'}};
const xmlCheck=r.validateSpec(xmlPathBytes,{PRODUCT:'ARTIFACT-1'});assert.equal(xmlCheck.valid,false);assert(xmlCheck.issues.some(x=>/input path requires STRING.*produces BYTES/.test(x)),xmlCheck.issues.join('\n'));
const regexPatternBytes={...header,steps:[{stepId:'S001',op:'LOAD_ARTIFACT',inputs:{binding:{bindingRef:'PRODUCT'}}},{stepId:'S002',op:'READ_BYTES',inputs:{artifact:{stepRef:'S001',output:'artifact'}}},{stepId:'S003',op:'DECODE_UTF8',inputs:{bytes:{stepRef:'S002',output:'bytes'}}},{stepId:'S004',op:'REGEX',inputs:{value:{stepRef:'S003',output:'text'},pattern:{stepRef:'S002',output:'bytes'}}}],result:{stepRef:'S004',output:'match'}};
const regexCheck=r.validateSpec(regexPatternBytes,{PRODUCT:'ARTIFACT-1'});assert.equal(regexCheck.valid,false);assert(regexCheck.issues.some(x=>/input pattern requires STRING.*produces BYTES/.test(x)),regexCheck.issues.join('\n'));
const numericSelector={...header,steps:[{stepId:'S001',op:'LOAD_ARTIFACT',inputs:{binding:{bindingRef:'PRODUCT'}}},{stepId:'S002',op:'READ_BYTES',inputs:{artifact:{stepRef:'S001',output:'artifact'}}},{stepId:'S003',op:'DECODE_UTF8',inputs:{bytes:{stepRef:'S002',output:'bytes'}}},{stepId:'S004',op:'PARSE_JSON',inputs:{text:{stepRef:'S003',output:'text'}}},{stepId:'S005',op:'SELECT_JSON_PATH',inputs:{value:{stepRef:'S004',output:'value'},path:{literal:7}}}],result:{stepRef:'S005',output:'selection'}};
const selectorCheck=r.validateSpec(numericSelector,{PRODUCT:'ARTIFACT-1'});assert.equal(selectorCheck.valid,false);assert(selectorCheck.issues.some(x=>/input path requires STRING.*literal produces INTEGER/.test(x)),selectorCheck.issues.join('\n'));
const canonicalSum={...header,steps:[{stepId:'S001',op:'LOAD_ARTIFACT',inputs:{binding:{bindingRef:'VALUES'}}},{stepId:'S002',op:'SUM',inputs:{value:{stepRef:'S001',output:'artifact'}}}],result:{stepRef:'S002',output:'value'}};assert.equal(r.validateSpec(canonicalSum,{VALUES:{kind:'CANONICAL_VALUE',canonicalKey:'VALUES'}}).valid,true);
const artifactSum=r.validateSpec(canonicalSum,{VALUES:{kind:'ARTIFACT',artifactId:'ARTIFACT-1'}});assert.equal(artifactSum.valid,false);assert(artifactSum.issues.some(x=>/input value requires VALUE.*produces ARTIFACT/.test(x)),artifactSum.issues.join('\n'));
const literalLoad={...header,steps:[{stepId:'S001',op:'LOAD_ARTIFACT',inputs:{binding:{literal:{value:[1,2,3]}}}}],result:{stepRef:'S001',output:'artifact'}};const literalLoadCheck=r.validateSpec(literalLoad);assert.equal(literalLoadCheck.valid,false);assert(literalLoadCheck.issues.some(x=>/must be an explicit bindingRef/.test(x)),literalLoadCheck.issues.join('\n'));
const operationContracts=r.operationContracts();for(const [op,contract] of Object.entries(operationContracts)){for(const port of [...contract.requiredInputs,...contract.optionalInputs])assert(Array.isArray(contract.inputTypes[port])&&contract.inputTypes[port].length,`${op}.${port} lacks a closed input-type contract`);}
assert.deepEqual([...r.OPERATION_REGISTRY_DEFINITION_ISSUES],[]);assert.equal(await r.sha256Canonical(operationContracts),r.OPERATION_REGISTRY_SHA256,'Operation-registry digest must bind the complete port contract.');
console.log(JSON.stringify({testIrPortTypes:'PASS',wrongPortTypeRejected:true,allInputPortsTyped:true,literalsTyped:true,bindingKindsTyped:true}));
