from pathlib import Path
p=Path('response-ingestion.js');s=p.read_text();old="if(typeof value==='string'&&value.length>200000)issues.push(issue('TEXT_FIELD_TOO_LARGE',path,'Text field exceeds the configured maximum length.'));";new="if(typeof value==='string'&&value.length>schema.DEFAULT_RESOURCE_LIMITS.maxTextFieldLength)issues.push(issue('TEXT_FIELD_TOO_LARGE',path,'Text field exceeds the configured maximum length.'));";assert s.count(old)==1;s=s.replace(old,new,1);p.write_text(s)
p=Path('verify-ingestion.mjs');s=p.read_text();s += "\nif(!globalThis.closedLoopResponseIngestion.validateValue){} // Resource-limit authority is exercised through normal ingestion.\n{const src=fs.readFileSync('response-ingestion.js','utf8');if(src.includes('value.length>200000'))throw new Error('Text-field resource limit is duplicated outside workflow-schema.js.');if(!src.includes('schema.DEFAULT_RESOURCE_LIMITS.maxTextFieldLength'))throw new Error('Ingestion does not use the schema text-field resource limit.');}\n";p.write_text(s)
