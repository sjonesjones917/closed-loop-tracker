import fs from 'node:fs';
import {createHash} from 'node:crypto';

function replaceOnce(file,from,to){
  const src=fs.readFileSync(file,'utf8');
  if(!src.includes(from))throw new Error(`${file}: repair anchor not found`);
  if(src.split(from).length!==2)throw new Error(`${file}: repair anchor is not unique`);
  fs.writeFileSync(file,src.replace(from,to));
}

replaceOnce('prompt-engine.js',
"1:'Initialize only this current job from this current job’s exact user input. Preserve the verbatim objective, requested deliverable, supplied-material inventory and provenance, inspection state, format requirements, temporal and geographic scope, user-supplied authority, tools, prohibitions, explicit user requirements, assumptions, unresolved unknowns, and any human decisions genuinely required to define the work. Determine whether the requested deliverable is feasible in the authorized execution environment and identify the smallest missing human-only information needed to proceed reliably. If direct implementation or fabrication is unavailable or materially too large but a complete reliable self-contained substitute such as an implementation-ready, design-ready, manufacturing-ready, research, architecture, or other specification deliverable can satisfy the underlying objective, propose that feasible substitute as EXACT_DELIVERABLE_REQUESTED for human intent confirmation and state the execution limitation explicitly. The application already owns JOB_ID and controlled input identity; do not assign or invent them. Do not create or prescribe a reusable master job or prompt for unrelated jobs. Do not begin substantive external-source research or downstream production work.',",
"1:'Initialize only this current job from this current job’s exact user input. Preserve the verbatim objective, requested deliverable, supplied-material inventory and provenance, inspection state, format requirements, temporal and geographic scope, user-supplied authority, tools, prohibitions, explicit user requirements, assumptions, unresolved unknowns, and any human decisions genuinely required to define the work. First perform a human-input sufficiency pass. A Stage 01 DATA_PROPOSAL is permitted only when the objective and intended deliverable are sufficiently specific to represent the human intent without inventing a material fact, preference, decision, legal assertion, engineering parameter, required artifact, or execution constraint. Missing, vague, ambiguous, internally conflicting, or decision-dependent human input must produce HUMAN_INPUT_REQUIRED instead of empty fields, UNKNOWN placeholders, guessed assumptions, or an incomplete proposal. Ask only the smallest set of questions that actually blocks reliable definition of the job. Each question must be plain-language, self-contained, ask for one human fact or decision, explain why it is needed, use the narrowest correct answerType, include allowedValues when the answer is a controlled choice, and set blocking=true when the stage cannot reliably proceed without it. Determine whether the requested deliverable is feasible in the authorized execution environment and identify the smallest missing human-only information needed to proceed reliably. If direct implementation or fabrication is unavailable or materially too large but a complete reliable self-contained substitute such as an implementation-ready, design-ready, manufacturing-ready, research, architecture, or other specification deliverable can satisfy the underlying objective, propose that feasible substitute as EXACT_DELIVERABLE_REQUESTED for human intent confirmation and state the execution limitation explicitly. The application already owns JOB_ID and controlled input identity; do not assign or invent them. Do not create or prescribe a reusable master job or prompt for unrelated jobs. Do not begin substantive external-source research or downstream production work.',");

replaceOnce('prompt-engine.js',
"- Return exactly one JSON object and no Markdown fence, preamble, or trailing prose.\n",
"- At Stage 01, do not return DATA_PROPOSAL merely because the response skeleton shows that shape. If any material human-only information is missing, vague, ambiguous, conflicting, or requires a human decision, return HUMAN_INPUT_REQUIRED with the exact questions instead. Empty or UNKNOWN agent fields are not a substitute for clarification.\n- Return exactly one JSON object and no Markdown fence, preamble, or trailing prose.\n");

replaceOnce('prompt-engine.js',
"The JSON shown below is an empty shape skeleton, not a complete answer: add only actual stage-relevant values and records supported by evidence. Do not return angle-bracket placeholders, invented sample values, or keys that are not needed for this response.",
"The JSON shown below is a transport shape skeleton, not a default disposition and not a complete answer. Choose responseType from the actual situation before filling it. At Stage 01, HUMAN_INPUT_REQUIRED takes precedence over DATA_PROPOSAL whenever the human intent cannot yet be represented completely without guessing. Add only actual stage-relevant values and records supported by evidence. Do not return angle-bracket placeholders, invented sample values, empty required values, or keys that are not needed for this response.");

replaceOnce('app-core.js',
"<p class=\"section-intro\">Supply the smallest genuine human-owned intake. The agent performs normalization and asks controlled clarification questions when needed.</p>",
"<p class=\"section-intro\">Start with only what you actually know. If a material detail is missing or ambiguous, the agent must return specific clarification questions instead of guessing; paste that structured response once and the application will turn those questions into normal answer fields here.</p>");

replaceOnce('app-core.js',
"<div class=\"panel\"><h2 class=\"section-title\">Agent loop</h2><div class=\"operator-loop\" aria-label=\"Normal stage workflow\"><div class=\"operator-step\"><b>1</b><span>Send instruction</span></div><div class=\"operator-step\"><b>2</b><span>Paste full response</span></div><div class=\"operator-step\"><b>3</b><span>Parse and validate</span></div><div class=\"operator-step\"><b>4</b><span>Review and accept</span></div></div></div>",
"<div class=\"panel\"><h2 class=\"section-title\">Agent loop</h2>${n===1?'<p class=\"section-intro\"><strong>First response:</strong> the agent must either return a complete Stage 01 proposal or tell you exactly what human information is still required. If it asks questions, paste that response here once; answer the rendered questions in the application, then send the regenerated Stage 01 instruction.</p>':''}<div class=\"operator-loop\" aria-label=\"Normal stage workflow\"><div class=\"operator-step\"><b>1</b><span>Send instruction</span></div><div class=\"operator-step\"><b>2</b><span>Paste full response</span></div><div class=\"operator-step\"><b>3</b><span>Parse and validate</span></div><div class=\"operator-step\"><b>4</b><span>Review and accept</span></div></div></div>");

replaceOnce('app-core.js',
"<div class=\"panel\"><h2 class=\"section-title\">Returned agent response</h2><p class=\"section-intro\">Paste the complete strict JSON response. Parse / validate preserves the raw response first, then validates it without changing canonical project records. If the response declares returned files, attach those exact files in Authorized files for this stage before parsing.</p>",
"<div class=\"panel\"><h2 class=\"section-title\">Returned agent response</h2><p class=\"section-intro\">${n===1?'If the agent says more human information is required, paste its complete HUMAN_INPUT_REQUIRED JSON here once. The application will render the questions as normal inputs; do not manually edit the JSON or invent answers. Otherwise paste the complete Stage 01 DATA_PROPOSAL JSON.':'Paste the complete strict JSON response.'} Parse / validate preserves the raw response first, then validates it without changing canonical project records. If the response declares returned files, attach those exact files in Authorized files for this stage before parsing.</p>");

replaceOnce('verify-prompt-semantics.mjs',
"  if(record.stage===6){\n",
"  if(record.stage===1){\n    for(const token of ['First perform a human-input sufficiency pass','Missing, vague, ambiguous, internally conflicting, or decision-dependent human input must produce HUMAN_INPUT_REQUIRED','Empty or UNKNOWN agent fields are not a substitute for clarification','HUMAN_INPUT_REQUIRED takes precedence over DATA_PROPOSAL'])if(!record.prompt.includes(token))issues.push(`STAGE01_CLARIFICATION_EXPERIENCE_MISSING_${token.slice(0,24)}`);\n  }\n  if(record.stage===6){\n");

replaceOnce('verify-prompt-semantics.mjs',
" const ui=fs.readFileSync('app-core.js','utf8');\n if(!ui.includes('Verification execution')||!ui.includes('a filename, hash claim, or code block is not file possession')||!ui.includes('Who performs the current tests'))throw new Error('Operator UI does not explain test execution responsibility and returned-file transfer.');\n",
" const ui=fs.readFileSync('app-core.js','utf8');\n if(!ui.includes('Verification execution')||!ui.includes('a filename, hash claim, or code block is not file possession')||!ui.includes('Who performs the current tests'))throw new Error('Operator UI does not explain test execution responsibility and returned-file transfer.');\n if(!ui.includes('tell you exactly what human information is still required')||!ui.includes('HUMAN_INPUT_REQUIRED JSON here once')||!ui.includes('turn those questions into normal answer fields'))throw new Error('Stage 01 operator UI does not explain the clarification loop in plain language.');\n");

replaceOnce('verify-browser.mjs',
" await click(cdp,'#new-project');await waitExpr(cdp,`document.body.innerText.includes('Save User Job Input')`);const afterNew=await allProjects(cdp);const newest=await activeProject(cdp);assert(newest.job.JOB_ID!==userJob&&newest.job.JOB_ID!=='JOB-20260823144121','New JOB_ID was not unique.');assert(newest.job.CURRENT_STAGE==='STAGE 01'&&(newest.projectData.sources||[]).length===0,'New job did not begin cleanly at Stage 01.');\n",
" await click(cdp,'#new-project');await waitExpr(cdp,`document.body.innerText.includes('Save User Job Input')`);const afterNew=await allProjects(cdp);const newest=await activeProject(cdp);assert(newest.job.JOB_ID!==userJob&&newest.job.JOB_ID!=='JOB-20260823144121','New JOB_ID was not unique.');assert(newest.job.CURRENT_STAGE==='STAGE 01'&&(newest.projectData.sources||[]).length===0,'New job did not begin cleanly at Stage 01.');await openStage(cdp,1);const stageOneText=(await snapshot(cdp)).text;for(const token of ['First response:','tell you exactly what human information is still required','HUMAN_INPUT_REQUIRED JSON here once','application will turn those questions into normal answer fields'])assert(stageOneText.includes(token),`Stage 01 clarification experience missing ${token}.`);\n");

const runtimeFiles=['workbook.js','hash.js','workflow-schema.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js'];
const gitBlobSha=file=>{const bytes=fs.readFileSync(file);return createHash('sha1').update(`blob ${bytes.length}\0`).update(bytes).digest('hex');};
const runtimeManifest=runtimeFiles.map(file=>`${file}:${gitBlobSha(file)}\n`).join('');
const runtimeBuildIdentity=`runtime-${createHash('sha256').update(runtimeManifest).digest('hex').slice(0,16)}`;
const index=fs.readFileSync('index.html','utf8');
const nextIndex=index.replace(/(src=\"(?:workbook|hash|workflow-schema|workflow-engine|prompt-engine|response-ingestion|project-store|app-core)\.js\?v=)[^\"]+/g,`$1${runtimeBuildIdentity}`);
if(nextIndex===index)throw new Error('index.html build-token anchor not found');
fs.writeFileSync('index.html',nextIndex);

console.log(`Applied minimum Stage 01 clarification-experience repair with ${runtimeBuildIdentity}.`);
