(()=>{
'use strict';

const core=globalThis.closedLoopCore;
const schema=globalThis.closedLoopWorkflowSchema;
const hash=globalThis.closedLoopHash;
if(!core||!schema||!hash)throw new Error('workbook.js, hash.js, and workflow-schema.js, test-runtime.js must load before workflow-engine.js.');

const STAGE_STATES=Object.freeze(['NOT STARTED','IN PROGRESS','BLOCKED','READY','COMPLETE']);
const FORMAL_STATES=Object.freeze(['UNKNOWN','NONE','NOT APPLICABLE','TRUE','FALSE','SATISFIED','VIOLATED','UNDETERMINED','ACCEPTED','REJECTED','BLOCKED']);
const INFRA_COLLECTIONS=Object.freeze([
  'inputVersions','rawResponses','responseProposals','responseValidations','acceptedChanges','rejectedResponses','extractionManifests',
  'humanInputRequests','humanInputAnswers','stageConfirmations','artifactVersions','generatedPrompts','generatedOutputs','outputReceipts',
  'history','newJobResets','reviews','recoveredProjects','responseDispositions','executionFailures'
]);
const ALL_COLLECTIONS=Object.freeze([...new Set([...Object.keys(schema.RECORD_SCHEMAS),...INFRA_COLLECTIONS])]);

const clone=value=>value===undefined?undefined:JSON.parse(JSON.stringify(value));
const now=()=>new Date().toISOString();
const safe=value=>Array.isArray(value)?value:[];
const upper=value=>String(value??'').trim().toUpperCase();
const truth=value=>['TRUE','YES','SATISFIED','ACCEPTED','AUTHORIZED','CONFIRMED','CONVERGED','COMPLETE','SUCCESS','SUCCEEDED','EFFECTIVE'].includes(upper(value));
const falsey=value=>['FALSE','NO','NONE','0','NOT APPLICABLE'].includes(upper(value));
const numeric=value=>{const parsed=Number(String(value??'').replace(/[^0-9.-]/g,''));return Number.isFinite(parsed)?parsed:0;};
const recordFields=record=>record?.fields&&typeof record.fields==='object'?record.fields:record||{};
const recordValue=(record,key)=>recordFields(record)?.[key]??record?.[key];
const recordId=(record,collection)=>{
  const idField=schema.RECORD_SCHEMAS[collection]?.idField;
  return String(record?.id||record?.recordId||(idField?recordValue(record,idField):'')||'').trim();
};
const isActiveRecord=record=>record?.active!==false&&!record?.invalidatedBy&&!['RETIRED','INVALIDATED','SUPERSEDED'].includes(upper(recordValue(record,'STATUS')||record?.validity));
const records=(project,collection,{stage,active=true}={})=>safe(project?.projectData?.[collection]).filter(record=>(stage===undefined||Number(record?.stage)===Number(stage))&&(!active||isActiveRecord(record)));

function ensureShape(project){
  if(!project||typeof project!=='object')throw new TypeError('Project must be an object.');
  project.projectData=project.projectData&&typeof project.projectData==='object'?project.projectData:{};
  for(const collection of ALL_COLLECTIONS)project.projectData[collection]=safe(project.projectData[collection]);
  project.projectData.permanentRegistry=project.projectData.permanentRegistry&&typeof project.projectData.permanentRegistry==='object'?project.projectData.permanentRegistry:{};
  project.projectData.stageRecords=project.projectData.stageRecords&&typeof project.projectData.stageRecords==='object'?project.projectData.stageRecords:{};
  project.projectData.userEntered=project.projectData.userEntered&&typeof project.projectData.userEntered==='object'?project.projectData.userEntered:{};
  project.projectData.idCounters=project.projectData.idCounters&&typeof project.projectData.idCounters==='object'?project.projectData.idCounters:{};
  project.projectData.eventSequence=Number.isInteger(project.projectData.eventSequence)?project.projectData.eventSequence:0;
  project.stages=project.stages&&typeof project.stages==='object'?project.stages:{};
  for(let stage=1;stage<=30;stage++){
    const prior=project.stages[stage]||project.stages[String(stage)]||{};
    project.stages[stage]={
      number:stage,status:'NOT STARTED',decision:'',decisionEvidence:'',nextStage:'',decidedBy:'',dateTime:'',
      draftRecord:prior.draftRecord||core.stageTemplate(core.STAGES[stage-1]),responseDraft:'',authorizedFiles:[],humanChecks:{},gateChecks:{},evidenceChecks:{},revisions:[],
      agentData:{},humanData:{},derivedData:{},acceptedData:{},acceptedDataChangeIds:[],acceptedControlEventIds:[],acceptedResponseIds:[],currentPromptId:null,gate:{complete:false,blocked:false,reasons:[]},invalidatedBy:null,
      ...prior,
      number:stage,
      authorizedFiles:safe(prior.authorizedFiles),revisions:safe(prior.revisions),agentData:prior.agentData&&typeof prior.agentData==='object'?prior.agentData:(prior.acceptedData&&typeof prior.acceptedData==='object'?prior.acceptedData:{}),humanData:prior.humanData&&typeof prior.humanData==='object'?prior.humanData:{},derivedData:prior.derivedData&&typeof prior.derivedData==='object'?prior.derivedData:{},acceptedDataChangeIds:safe(prior.acceptedDataChangeIds),acceptedControlEventIds:safe(prior.acceptedControlEventIds),acceptedResponseIds:safe(prior.acceptedResponseIds)
    };
  }
  project.job=project.job&&typeof project.job==='object'?project.job:{};
  project.release={gateState:'',auditedDraft:[],releaseDraft:[],comparisons:[],authorization:'NOT AUTHORIZED',authorizedArtifactIds:[],...(project.release||{})};
  for(const name of ['auditedDraft','releaseDraft','comparisons','authorizedArtifactIds'])project.release[name]=safe(project.release[name]);
  return project;
}

const APPLICATION_INITIAL_FIELDS=Object.freeze({
  requirements:Object.freeze({STATUS:'ACTIVE'}),candidateRequirements:Object.freeze({STATUS:'ACTIVE'}),requirementResolutions:Object.freeze({STATUS:'RESOLVED'}),tests:Object.freeze({STATUS:'READY'}),instructions:Object.freeze({STATUS:'CURRENT'}),instructionTraces:Object.freeze({STATUS:'COMPLETE'}),preflightRecords:Object.freeze({STATUS:'COMPLETE'}),iterations:Object.freeze({STATUS:'FROZEN'}),candidateFreezes:Object.freeze({STATUS:'FROZEN'}),defects:Object.freeze({STATUS:'CONFIRMED'}),regressions:Object.freeze({ACTIVE_RETIRED_STATE:'ACTIVE'}),baselines:Object.freeze({STATUS:'FROZEN'}),products:Object.freeze({STATUS:'RESERVED'}),blockers:Object.freeze({STATUS:'OPEN'}),evidenceRecords:Object.freeze({STATUS:'PRESERVED'})
});
function applicationInitialFields(collection){return clone(APPLICATION_INITIAL_FIELDS[collection]||{});}

function addHistory(project,type,details={}){
  ensureShape(project);
  const eventSequence=++project.projectData.eventSequence;
  const deviceTimestamp=now();
  const event={eventId:`EVENT-${String(eventSequence).padStart(9,'0')}`,eventSequence,deviceTimestamp,createdAt:deviceTimestamp,type,...clone(details)};
  project.projectData.history.push(event);
  return event;
}

function existingIds(project,collection){return new Set(records(project,collection,{active:false}).map(record=>recordId(record,collection)).filter(Boolean));}
function allocateId(project,collection){
  ensureShape(project);
  const definition=schema.RECORD_SCHEMAS[collection];
  if(!definition)throw new Error(`Unknown canonical collection: ${collection}.`);
  const existing=existingIds(project,collection);
  let counter=Math.max(0,Number(project.projectData.idCounters[collection]||0));
  let candidate='';
  do{counter+=1;candidate=`${definition.prefix}-${String(counter).padStart(6,'0')}`;}while(existing.has(candidate));
  project.projectData.idCounters[collection]=counter;
  return candidate;
}
function allocateInfrastructureId(project,prefix,collection){
  ensureShape(project);
  const key=`infra:${collection}:${prefix}`;
  let counter=Math.max(0,Number(project.projectData.idCounters[key]||0));
  const existing=new Set(safe(project.projectData[collection]).map(record=>String(record?.id||record?.rawResponseId||record?.proposalId||record?.validationId||record?.changeId||record?.manifestId||record?.receiptId||record?.promptId||'')).filter(Boolean));
  let candidate='';
  do{counter+=1;candidate=`${prefix}-${String(counter).padStart(6,'0')}`;}while(existing.has(candidate));
  project.projectData.idCounters[key]=counter;
  return candidate;
}

function nextVersion(current,prefix){
  const match=String(current||'').match(/v(\d+)$/i);
  const number=match?Number(match[1])+1:1;
  return `${prefix}-v${String(number).padStart(3,'0')}`;
}
// CURRENT_INPUT_VERSION is versioned only by application-controlled human-input mutation paths.
// Accepting the Stage 01 agent interpretation must not create a new User Job Input version,
// otherwise the accepted intake manifest instantly becomes stale and the user is asked to repeat information.
const VERSION_BY_STAGE=Object.freeze({
  2:['CURRENT_SOURCE_SET_VERSION','SOURCE-SET'],3:['CURRENT_RESEARCH_VERSION','RESEARCH'],
  4:['CURRENT_REQUIREMENTS_VERSION','REQUIREMENTS'],6:['CURRENT_TEST_SUITE_VERSION','TEST-SUITE'],
  7:['CURRENT_MUTATION_SUITE_VERSION','MUTATION-SUITE'],8:['CURRENT_INSTRUCTION_VERSION','INSTRUCTION']
});
function versionCollections(stage){return schema.STAGE_CONTRACTS[stage]?.primaryCollections||[];}
const VERSION_SCOPE_KEY_BY_STAGE=Object.freeze({2:'sourceSetVersion',4:'requirementsVersion',5:'requirementsVersion',6:'testSuiteVersion',8:'instructionVersion'});
function stampCurrentVersionMembership(project,stage,version){const key=VERSION_SCOPE_KEY_BY_STAGE[stage];if(!key||!version)return;const collections=stage===5?[...new Set(['requirements',...versionCollections(stage)])]:versionCollections(stage);for(const collection of collections)for(const record of records(project,collection)){record.scope={...(record.scope||{}),[key]:version};refreshRecordHashes(record,collection);}}
function registerStageVersion(project,stage,acceptedChangeId){
  ensureShape(project);
  const config=VERSION_BY_STAGE[stage];
  if(!config)return null;
  if(Number(stage)===2&&upper(project.stages[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE'){project.job.CURRENT_SOURCE_SET_VERSION='NOT APPLICABLE';return null;}
  const [jobField,prefix]=config;
  const payload={stage,collections:Object.fromEntries(versionCollections(stage).map(collection=>[collection,records(project,collection).map(record=>({id:recordId(record,collection),fields:recordFields(record),relationships:record.relationships||{},sha256:record.sha256||null}))])),acceptedData:project.stages[stage].acceptedData};
  const sha256=hash.sha256Value(payload);
  const latest=safe(project.projectData.artifactVersions).filter(item=>item.stage===stage&&item.kind===prefix).at(-1);
  if(latest?.sha256===sha256){project.job[jobField]=latest.version;stampCurrentVersionMembership(project,stage,latest.version);return latest;}
  const version=nextVersion(project.job[jobField],prefix);
  const record={versionId:allocateInfrastructureId(project,'VERSION','artifactVersions'),stage,kind:prefix,version,sha256,createdAt:now(),acceptedChangeId,payload};
  project.projectData.artifactVersions.push(record);
  project.job[jobField]=version;
  stampCurrentVersionMembership(project,stage,version);
  return record;
}

function unresolvedHumanRequests(project,stage){return safe(project?.projectData?.humanInputRequests).filter(request=>Number(request.stage)===Number(stage)&&upper(request.status||'OPEN')==='OPEN'&&request.blocking!==false);}
function openBlockers(project,stage){
  return records(project,'blockers').filter(blocker=>{
    const status=upper(recordValue(blocker,'STATUS')||blocker.status||'OPEN');
    if(!['OPEN','BLOCKED','IN PROGRESS','UNKNOWN'].includes(status))return false;
    if(stage===undefined)return true;
    const blockerStage=Number(blocker.stage||recordValue(blocker,'STAGE_DISCOVERED')||0);
    if(blockerStage===Number(stage))return true;
    const affected=String(recordValue(blocker,'AFFECTED_ARTIFACTS')||recordValue(blocker,'DOWNSTREAM_WORK_STOPPED')||'');
    return new RegExp(`(?:STAGE\\s*0?${stage}\\b|\\b${stage}\\b)`,`i`).test(affected);
  });
}
function acceptedChanges(project,stage){return safe(project?.projectData?.acceptedChanges).filter(change=>Number(change.stage)===Number(stage)&&change.status==='COMMITTED'&&change.responseType==='DATA_PROPOSAL'&&!change.invalidatedBy);}
function acceptedControlEvents(project,stage){return safe(project?.projectData?.responseDispositions).filter(item=>Number(item.stage)===Number(stage)&&['ACCEPTED_HUMAN_QUESTION_SET','ACCEPTED_BLOCKER_EVENT','ACCEPTED_EXECUTION_FAILURE'].includes(item.type)&&!item.invalidatedBy);}

function hasStageActivity(project,stage){
  if(acceptedChanges(project,stage).length)return true;
  if(safe(project?.projectData?.rawResponses).some(item=>Number(item.stage)===Number(stage)))return true;
  if(unresolvedHumanRequests(project,stage).length)return true;
  if((schema.STAGE_CONTRACTS[stage]?.allowedCollections||[]).some(collection=>records(project,collection,{stage}).length))return true;
  return Boolean(Object.keys(project?.stages?.[stage]?.acceptedData||{}).length);
}
function latestIteration(project,stages=[10,17,19]){
  const candidates=records(project,'iterations').filter(record=>stages.includes(Number(record.stage)));
  return candidates.at(-1)||null;
}
function mandatoryRequirements(project,scopeRule=null){
  const source=scopeRule?recordsForScope(project,'requirements',scopeRule):records(project,'requirements');
  return source.filter(record=>{
    const value=upper(recordValue(record,'MANDATORY_OPTIONAL_STATUS')||recordValue(record,'MANDATORY')||'MANDATORY');
    return value!=='OPTIONAL'&&value!=='FALSE'&&value!=='NO';
  });
}
function confirmedDefects(project){
  return records(project,'defects').filter(record=>{
    const status=upper(recordValue(record,'STATUS')||'CONFIRMED');
    return !['REJECTED','DUPLICATE','NOT A DEFECT','RETIRED'].includes(status);
  });
}
function defectResolvedByRegression(project,defect){const defectId=recordId(defect,'defects'),linked=records(project,'regressions').filter(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===defectId&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED');if(!linked.length)return false;const iterationId=recordId(latestIteration(project,[10,17,19]),'iterations')||String(project.job.CURRENT_ITERATION||'').trim(),source=iterationId?currentRegressionExecutions(project,iterationId):records(project,'regressionExecutions');return linked.every(reg=>{const id=recordId(reg,'regressions'),executions=source.filter(x=>String(recordValue(x,'REG_ID')||x.relationships?.REG_ID||'')===id&&upper(recordValue(x,'PHASE'))!=='PRE_CORRECTION');return executions.length===1&&effectiveRegressionDetermination(project,executions[0]).determination==='SATISFIED';});}
function unresolvedMaterialDefects(project){return confirmedDefects(project).filter(record=>{const severity=upper(recordValue(record,'SEVERITY')),status=upper(recordValue(record,'STATUS'));return ['CRITICAL','MAJOR'].includes(severity)&&!['CLOSED','CLOSED VERIFIED','CLOSED_VERIFIED','RESOLVED','CORRECTED AND VERIFIED','RETIRED'].includes(status)&&!defectResolvedByRegression(project,record);});}
function requirementId(record){return recordId(record,'requirements');}
function testRequirementId(record){return String(recordValue(record,'REQ_ID')||record.relationships?.REQ_ID||'');}
function resultRequirementId(project,record){const direct=String(recordValue(record,'REQ_ID')||record.relationships?.REQ_ID||'').trim();if(direct)return direct;const testId=String(recordValue(record,'TEST_ID')||record.relationships?.TEST_ID||'').trim();if(!testId)return '';const test=records(project,'tests').find(item=>recordId(item,'tests')===testId);return test?testRequirementId(test):'';}
function runIterationId(record){return String(recordValue(record,'ITERATION_ID')||record.relationships?.ITERATION_ID||'');}
function verificationKey(record){return [recordValue(record,'REQ_ID')||record.relationships?.REQ_ID,recordValue(record,'RUN_ID')||record.relationships?.RUN_ID,recordValue(record,'TEST_ID')||record.relationships?.TEST_ID].join('|');}
function iterationCandidateId(project,iterationId){const iteration=records(project,'iterations').find(record=>recordId(record,'iterations')===String(iterationId||''));return String(recordValue(iteration,'CANDIDATE_ID')||iteration?.relationships?.CANDIDATE_ID||'');}
function candidateComponentIdentity(project,candidateId){const candidate=records(project,'candidateFreezes').find(record=>recordId(record,'candidateFreezes')===String(candidateId||''));if(!candidate)return '';const manifest=recordValue(candidate,'COMPONENT_MANIFEST')||recordValue(candidate,'FROZEN_COMPONENT_RECORDS')||recordValue(candidate,'COMPONENT_HASHES')||candidate.componentManifest||candidate.hashes||'';return hash.sha256Value(manifest);}
function contextExternalId(context){return String(recordValue(context,'EXTERNAL_CONTEXT_ID')||recordValue(context,'FRESH_CONTEXT_RECORD')||recordValue(context,'CONTEXT_REFERENCE')||'').trim();}
function contextAuthorizedInputs(context){return recordValue(context,'AUTHORIZED_INPUTS')||recordValue(context,'INPUT_MANIFEST')||recordValue(context,'AUTHORIZED_INPUT_FACTS')||'';}
function contextHasForbiddenContent(value,patterns){const text=typeof value==='string'?value:JSON.stringify(value||'');return patterns.some(pattern=>pattern.test(text));}

function currentScope(project){
  ensureShape(project);
  return {
    inputVersion:project.job.CURRENT_INPUT_VERSION||null,
    sourceSetVersion:project.job.CURRENT_SOURCE_SET_VERSION||null,
    requirementsVersion:project.job.CURRENT_REQUIREMENTS_VERSION||null,
    testSuiteVersion:project.job.CURRENT_TEST_SUITE_VERSION||null,
    instructionVersion:project.job.CURRENT_INSTRUCTION_VERSION||null,
    iterationId:project.job.CURRENT_ITERATION||null,
    baselineId:project.job.CURRENT_BASELINE_ID||null,
    productId:project.job.CURRENT_PRODUCT_ID||null
  };
}
function scopeForIteration(project,iterationId){
  const iteration=records(project,'iterations').find(record=>recordId(record,'iterations')===String(iterationId||''));
  const versions=recordValue(iteration,'FROZEN_VERSIONS')||recordValue(iteration,'VERSIONS')||iteration?.versions||{};
  return {
    inputVersion:versions.inputVersion||versions.CURRENT_INPUT_VERSION||project.job.CURRENT_INPUT_VERSION||null,
    sourceSetVersion:versions.sourceSetVersion||versions.CURRENT_SOURCE_SET_VERSION||project.job.CURRENT_SOURCE_SET_VERSION||null,
    requirementsVersion:versions.requirementsVersion||versions.CURRENT_REQUIREMENTS_VERSION||project.job.CURRENT_REQUIREMENTS_VERSION||null,
    testSuiteVersion:versions.testSuiteVersion||versions.CURRENT_TEST_SUITE_VERSION||project.job.CURRENT_TEST_SUITE_VERSION||null,
    instructionVersion:versions.instructionVersion||versions.CURRENT_INSTRUCTION_VERSION||project.job.CURRENT_INSTRUCTION_VERSION||null,
    iterationId:String(iterationId||''),
    candidateId:iterationCandidateId(project,iterationId)||null
  };
}
function scopeMatches(record,scopeRule={}){
  const scope=record?.scope||{};
  const keys=['inputVersion','sourceSetVersion','requirementsVersion','testSuiteVersion','instructionVersion','iterationId','candidateId','runId','contextId','baselineId','productId'];
  return keys.every(key=>scopeRule[key]===undefined||scopeRule[key]===null||String(scope[key]||'')===String(scopeRule[key]||''));
}
function recordsForScope(project,collection,scopeRule={}){return records(project,collection).filter(record=>scopeMatches(record,scopeRule));}
function recordsForCurrentScope(project,collection){return recordsForScope(project,collection,currentScope(project));}
function recordsForIteration(project,collection,iterationId){return recordsForScope(project,collection,scopeForIteration(project,iterationId));}

function refreshRecordHashes(record,collection){const definition=schema.RECORD_SCHEMAS[collection],fields=recordFields(record),relationships=record.relationships||{},contentPayload={collection,fields,relationships,scope:record.scope||{}};record.contentSha256=hash.sha256Value(contentPayload);record.recordSha256=hash.sha256Value({id:recordId(record,collection),...contentPayload,evidenceRefs:safe(record.evidenceRefs),active:record.active!==false,invalidatedBy:record.invalidatedBy||null,createdAt:record.createdAt||null,updatedAt:record.updatedAt||null});record.sha256=record.recordSha256;if(definition?.idField&&!record.id)record.id=String(fields[definition.idField]||record.recordId||'');return record;}
function commandRecord(project,collection,fields={},options={}){ensureShape(project);const definition=schema.RECORD_SCHEMAS[collection];if(!definition)throw new Error('Unknown canonical collection: '+collection);const id=options.id||allocateId(project,collection),record={id,stage:options.stage??definition.stage??Number(project.activeStage||0),source:options.source||'APPLICATION',active:true,createdAt:now(),updatedAt:now(),scope:clone(options.scope||currentScope(project)),fields:{...applicationInitialFields(collection),...clone(fields)},relationships:clone(options.relationships||{}),evidenceRefs:safe(options.evidenceRefs)};if(definition.idField)record.fields[definition.idField]=id;refreshRecordHashes(record,collection);project.projectData[collection].push(record);return record;}

function evaluateContextIndependence(project,{role,iterationId,runId,verifierContextId,reviewerContextId,generatorContextId,productionContextId,promptContextManifest}={}){
  ensureShape(project);const reasons=[],evidence=[],kind=upper(role||''),contexts=records(project,'freshContexts'),findContext=id=>contexts.find(record=>recordId(record,'freshContexts')===String(id||''));
  const contaminated=context=>['YES','TRUE','CONTAMINATED','VIOLATED'].includes(upper(recordValue(context,'CONTAMINATION_STATUS')||recordValue(context,'CONTAMINATION_CHECK')));
  const forbiddenPatterns=[/prior[-_\s]?run/i,/reviewer\s+feedback/i,/verification\s+result/i,/comparison\s+finding/i,/root[-_\s]?cause|\bRCA\b/i,/proposed\s+correction|changeset|correction/i];
  if(kind.includes('RUN')){
    const runs=recordsForIteration(project,'runs',iterationId),runContexts=runs.map(run=>String(recordValue(run,'CONTEXT_ID')||run.relationships?.CONTEXT_ID||run.contextId||'')),externalIds=runContexts.map(id=>contextExternalId(findContext(id))).filter(Boolean),candidateIds=runs.map(run=>String(recordValue(run,'CANDIDATE_ID')||run.relationships?.CANDIDATE_ID||run.candidateId||'')),candidateHashes=candidateIds.map(id=>candidateComponentIdentity(project,id));
    if(runs.length!==10)reasons.push(`Exactly ten current runs are required; found ${runs.length}.`);
    if(runContexts.some(id=>!id))reasons.push('Every current run requires one application context identity.');
    if(new Set(runContexts.filter(Boolean)).size!==runContexts.filter(Boolean).length)reasons.push('A run context identity is reused.');
    if(externalIds.length&&new Set(externalIds).size!==externalIds.length)reasons.push('An external context identifier is reused across runs.');
    if(new Set(candidateIds.filter(Boolean)).size>1)reasons.push('Runs do not use one frozen candidate identity.');
    if(new Set(candidateHashes.filter(Boolean)).size>1)reasons.push('Runs do not use identical candidate component hashes.');
    for(const [index,id] of runContexts.entries()){
      const context=findContext(id);if(!context){reasons.push(`Run ${recordId(runs[index],'runs')} has no current context record.`);continue;}
      const linkedRun=String(recordValue(context,'RUN_ID')||context.relationships?.RUN_ID||context.runId||'');if(linkedRun&&linkedRun!==recordId(runs[index],'runs'))reasons.push(`Context ${id} is linked to another run.`);
      const input=contextAuthorizedInputs(context);if(contextHasForbiddenContent(input,forbiddenPatterns))reasons.push(`Context ${id} contains prohibited prior output, review, RCA, or correction material.`);
      if(contaminated(context))reasons.push(`Context ${id} is contaminated.`);
    }
    evidence.push({runIds:runs.map(r=>recordId(r,'runs')),contextIds:runContexts,externalContextIds:externalIds,candidateIds,candidateComponentSha256:candidateHashes});
  }else{
    const reviewerId=String(verifierContextId||reviewerContextId||''),generatorId=String(generatorContextId||productionContextId||''),reviewer=findContext(reviewerId),generator=findContext(generatorId);
    if(!reviewerId)reasons.push('Reviewer context identity is missing.');
    if(reviewerId&&generatorId&&reviewerId===generatorId)reasons.push('Reviewer context reuses the generator context.');
    if(reviewerId&&!reviewer)reasons.push('Reviewer context is not application-recorded current context.');
    if(reviewer&&contaminated(reviewer))reasons.push('Reviewer context is contaminated.');
    const manifest=promptContextManifest||reviewer?.promptContextManifest||recordValue(reviewer,'PROMPT_CONTEXT_MANIFEST')||contextAuthorizedInputs(reviewer);
    const reviewerPatterns=kind.includes('MEANING')?[/generator\s+self[- ]?evaluation/i,/adversarial\s+finding/i,/prior\s+meaning[- ]?review/i]:kind.includes('ADVERSARIAL')?[/generator\s+reasoning|generator\s+self[- ]?evaluation/i,/prior\s+reviewer\s+conclusion/i]:[/other\s+verifier/i,/stage\s*13|comparison/i,/root[-_\s]?cause|\bRCA\b/i,/proposed\s+correction|changeset/i];
    if(manifest&&contextHasForbiddenContent(manifest,reviewerPatterns))reasons.push('Reviewer context includes prohibited prior conclusions, comparison, RCA, or correction material.');
    evidence.push({reviewerContextId:reviewerId,generatorContextId:generatorId||null,reviewerRecorded:Boolean(reviewer),generatorRecorded:Boolean(generator)});
  }
  const explicitExternalSupport=(kind&&!kind.includes('RUN'))&&reasons.every(reason=>/not application-recorded current context|missing/i.test(reason))&&Boolean(verifierContextId||reviewerContextId)&&Boolean(promptContextManifest);
  const determination=reasons.length?(explicitExternalSupport?'EXTERNALLY_SUPPORTED':'VIOLATED'):'APPLICATION_ESTABLISHED';
  return {determination,reasons,evidence};
}

function evidenceAuthority(record){return upper(recordValue(record,'AUTHORITY_TYPE')||recordValue(record,'EVIDENCE_AUTHORITY_TYPE')||recordValue(record,'SOURCE_AUTHORITY')||record?.source||'AGENT_CLAIM');}
function artifactVerified(record){return upper(recordValue(record,'AVAILABILITY'))==='BYTES_PERSISTED_AND_VERIFIED'&&/^[a-f0-9]{64}$/i.test(String(recordValue(record,'SHA256')||''))&&Number(recordValue(record,'BYTE_SIZE'))>=0;}
function evidenceRecordsFor(project,result){const ids=new Set(safe(result?.evidenceRefs).map(String));const inline=recordValue(result,'EVIDENCE_ID')||result?.relationships?.EVIDENCE_ID;if(inline)ids.add(String(inline));return records(project,'evidenceRecords').filter(record=>ids.has(recordId(record,'evidenceRecords')));}
function evidenceArtifactIds(evidence){const ids=new Set();for(const item of evidence){const direct=recordValue(item,'ATTACHMENT_ID')||item.relationships?.ATTACHMENT_ID||recordValue(item,'ARTIFACT_ID')||item.relationships?.ARTIFACT_ID;if(direct)ids.add(String(direct));for(const id of safe(recordValue(item,'ARTIFACT_IDS')))ids.add(String(id));}return [...ids];}
function evidenceKindText(record){return upper([recordValue(record,'APPLICATION_EVIDENCE_KIND'),recordValue(record,'KIND'),recordValue(record,'DESCRIPTION'),recordValue(record,'APPLICATION_EVIDENCE_DESCRIPTION')].filter(Boolean).join(' '));}
function canonicalExecutionId(record){return String(recordValue(record,'EXECUTION_ID')||recordValue(record,'SYSTEM_EXECUTION_ID')||recordValue(record,'TRANSACTION_ID')||recordValue(record,'RECEIPT_ID')||'').trim();}
function canonicalContextId(record){return String(recordValue(record,'CONTEXT_ID')||recordValue(record,'REVIEW_CONTEXT_ID')||recordValue(record,'VERIFIER_CONTEXT_ID')||'').trim();}
function canonicalEvidencePackage(evidence){return evidence.map(e=>({id:recordId(e,'evidenceRecords'),authority:evidenceAuthority(e),kind:evidenceKindText(e),artifactId:String(recordValue(e,'ATTACHMENT_ID')||e.relationships?.ATTACHMENT_ID||recordValue(e,'ARTIFACT_ID')||e.relationships?.ARTIFACT_ID||''),executionId:canonicalExecutionId(e),contextId:canonicalContextId(e),location:String(recordValue(e,'LOCATION')||recordValue(e,'APPLICATION_EVIDENCE_LOCATION')||''),content:String(recordValue(e,'CONTENT')||recordValue(e,'APPLICATION_EVIDENCE_CONTENT')||'')}));}
function parseDeclaredArtifactRequirements(test){const raw=recordValue(test,'ARTIFACT_REQUIREMENTS');if(Array.isArray(raw))return raw.map(String).filter(Boolean);if(raw&&typeof raw==='object')return Object.values(raw).flat().map(String).filter(Boolean);return String(raw||'').split(/[\n,;]+/).map(x=>x.trim()).filter(Boolean);}
function requiredArtifactIds(test){const bindings=recordValue(test,'EXECUTABLE_INPUT_BINDINGS');if(bindings&&typeof bindings==='object'&&!Array.isArray(bindings)){const ids=[];for(const value of Object.values(bindings)){if(typeof value==='string')ids.push(value);else if(value&&typeof value==='object')ids.push(value.artifactId||value.ARTIFACT_ID||value.id||'');}return [...new Set(ids.map(String).filter(Boolean))];}return [...new Set(parseDeclaredArtifactRequirements(test).filter(x=>/^(?:ARTIFACT|PRODUCT|FILE)-/i.test(x)))];}
function configuredRequiredCapability(test){return String(recordValue(test,'REQUIRED_CAPABILITY')||recordValue(test,'REQUIRED_CAPABILITY_TYPE')||'').trim();}
function claimedEvidenceRefs(result){return safe(result?.evidenceRefs).map(String).filter(Boolean);}
function evaluateEvidenceContract(test,result,executionPlan,project){
  const reasons=[],evidence=evidenceRecordsFor(project,result),authoritySet=new Set(evidence.map(evidenceAuthority)),kindSet=evidence.map(evidenceKindText),artifactIds=evidenceArtifactIds(evidence),artifacts=recordsForCurrentScope(project,'artifacts').filter(a=>artifactIds.includes(recordId(a,'artifacts'))),verifiedArtifacts=artifacts.filter(artifactVerified),mode=upper(executionPlan?.executionMode||recordValue(test,'EXECUTION_MODE')||''),type=upper(recordValue(test,'TEST_TYPE')||recordValue(result,'TEST_TYPE')||''),requiredArtifacts=requiredArtifactIds(test),executionId=canonicalExecutionId(result)||evidence.map(canonicalExecutionId).find(Boolean)||'',contextId=canonicalContextId(result)||evidence.map(canonicalContextId).find(Boolean)||'';
  const needsByteEvidence=requiredArtifacts.length>0||/BYTE|HASH|FILE|ARTIFACT/.test(type)||mode==='APPLICATION_DETERMINISTIC';
  if(!evidence.length)reasons.push('No canonical evidence record is linked.');
  if(needsByteEvidence){if(!verifiedArtifacts.length)reasons.push('Byte/artifact proposition lacks a linked application-verified artifact.');for(const required of requiredArtifacts)if(!verifiedArtifacts.some(a=>recordId(a,'artifacts')===required))reasons.push(`Required artifact ${required} is not linked as verified evidence.`);}
  if(mode==='APPLICATION_DETERMINISTIC'){if(!authoritySet.has('APPLICATION_NATIVE_RUNTIME'))reasons.push('Application deterministic result lacks application-native runtime evidence authority.');if(!executionId&&!String(recordValue(result,'TEST_SPEC_SHA256')||''))reasons.push('Application deterministic result lacks runtime execution/spec identity.');}
  if(mode==='HUMAN_INSPECTION'){if(!authoritySet.has('HUMAN_OBSERVATION'))reasons.push('Human inspection lacks human-owned observation evidence.');}
  if(mode==='EXTERNAL_SYSTEM'){if(![...authoritySet].some(a=>a==='EXTERNAL_SYSTEM'||a==='ATTRIBUTABLE_EXTERNAL_SYSTEM')&&!kindSet.some(k=>/EXTERNAL SYSTEM|SYSTEM RECEIPT|SYSTEM RESULT/.test(k)))reasons.push('External-system result lacks attributable external-system evidence.');}
  if(mode==='EXTERNAL_AGENT_TOOL'){if(!executionId&&!kindSet.some(k=>/TOOL EXECUTION|EXECUTION RECEIPT|TOOL RESULT/.test(k)))reasons.push('External-tool result lacks execution-attributable evidence.');}
  if(mode==='INDEPENDENT_AGENT_REVIEW'||type==='MEANING'){const location=String(recordValue(result,'PRODUCT_LOCATION')||recordValue(result,'LOCATION')||''),observed=String(recordValue(result,'OBSERVED_MEANING')||recordValue(result,'OBSERVATION')||''),comparison=String(recordValue(result,'COMPARISON')||recordValue(result,'ANALYSIS')||'');if(!location)reasons.push('Meaning/review result lacks product location.');if(!observed)reasons.push('Meaning/review result lacks observed meaning.');if(!comparison)reasons.push('Meaning/review result lacks evidence-based comparison.');}
  if(mode==='UNAVAILABLE')reasons.push('UNAVAILABLE execution mode can never provide sufficient satisfaction evidence.');
  if(['AGENT_CLAIM'].some(a=>authoritySet.has(a))&&authoritySet.size===1&&needsByteEvidence)reasons.push('Agent prose alone cannot prove a byte/artifact proposition.');
  return {sufficient:reasons.length===0,reasons,evidenceIds:evidence.map(e=>recordId(e,'evidenceRecords')),executionId,contextId};
}
function claimedDetermination(collection,record){if(collection==='deterministicResults'&&record?.source==='APPLICATION_TEST_RUNTIME')return upper(recordValue(record,'APPLICATION_DETERMINATION'));if(collection==='processAudits')return upper(recordValue(record,'PROCESS_DETERMINATION'));if(collection==='productAudits')return upper(recordValue(record,'PRODUCT_DETERMINATION'));if(collection==='regressionExecutions')return upper(recordValue(record,'RESULT'));if(collection==='products')return upper(recordValue(record,'STATUS'));return upper(recordValue(record,'DETERMINATION'));}
function testForResult(project,record){const id=String(recordValue(record,'TEST_ID')||record?.relationships?.TEST_ID||'').trim();return id?records(project,'tests').find(t=>recordId(t,'tests')===id):null;}
function effectiveRegressionDetermination(project,record){const evidence=evaluateEvidenceContract(null,record,null,project);if(!evidence.sufficient)return {determination:'UNDETERMINED',reasons:evidence.reasons};const phase=upper(recordValue(record,'PHASE')),out=controlledOutcome(recordValue(record,'RESULT'));if(!out)return {determination:'UNDETERMINED',reasons:['Regression RESULT is not a controlled application-evaluable outcome.']};if(phase==='PRE_CORRECTION')return {determination:out==='VIOLATED'?'SATISFIED':'VIOLATED',reasons:out==='VIOLATED'?[]:['Pre-correction regression did not demonstrate the expected failure.']};return {determination:out,reasons:out==='SATISFIED'?[]:['Post-correction regression did not establish success.']};}
function comparisonFacts(project,reqId,iterationId){const rows=(iterationId?recordsForIteration(project,'verification',iterationId):recordsForCurrentScope(project,'verification')).filter(r=>String(recordValue(r,'REQ_ID')||r.relationships?.REQ_ID||'')===reqId),determinations=rows.map(r=>effectiveDetermination('verification',r,testForResult(project,r),project)),ds=determinations;return {rows,determinations,runDeterminations:rows.map((r,i)=>({runId:String(recordValue(r,'RUN_ID')||r.relationships?.RUN_ID||''),testId:String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||''),determination:ds[i]})),allSatisfied:rows.length>0&&ds.every(d=>d==='SATISFIED'),anyViolation:ds.includes('VIOLATED'),anyUndetermined:ds.some(d=>d!=='SATISFIED'&&d!=='VIOLATED')};}
function validateRootCauseRecord(record,project){const reasons=[],defectId=String(recordValue(record,'DEFECT_ID')||record?.relationships?.DEFECT_ID||'').trim(),defect=records(project,'defects').find(d=>recordId(d,'defects')===defectId);if(!defect)reasons.push('RCA defect identity is missing or does not resolve.');for(const key of ['LAYER_TRACE','EARLIEST_DEFECTIVE_LAYER','ROOT_CAUSE','DOWNSTREAM_INVALIDATION'])if(adjudicationEmpty(recordValue(record,key)))reasons.push('RCA '+key+' is missing.');if(adjudicationEmpty(recordValue(record,'EVIDENCE'))&&!safe(record?.evidenceRefs).length)reasons.push('RCA evidence linkage is missing.');return {valid:reasons.length===0,reasons,defectId,responsibleLayer:String(recordValue(record,'EARLIEST_DEFECTIVE_LAYER')||'').trim()};}
function validateChangeTrace(record,project){const reasons=[],triggerRaw=recordValue(record,'TRIGGERING_DEFECT_IDS'),triggerIds=Array.isArray(triggerRaw)?triggerRaw.map(String):String(triggerRaw||'').split(/[;,\s]+/).filter(Boolean),layer=String(recordValue(record,'RESPONSIBLE_LAYER')||'').trim();if(!triggerIds.length)reasons.push('Changeset has no triggering defect identity.');if(!layer)reasons.push('Changeset responsible layer is missing.');if(adjudicationEmpty(recordValue(record,'EXACT_MODIFICATION')))reasons.push('Changeset exact modification is missing.');if(adjudicationEmpty(recordValue(record,'NEW_ARTIFACT_VERSION')))reasons.push('Changeset changed artifact/version identity is missing.');if(adjudicationEmpty(recordValue(record,'DOWNSTREAM_INVALIDATION')))reasons.push('Changeset downstream invalidation statement is missing.');for(const id of triggerIds){const rca=records(project,'rootCauses').find(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===id);if(!rca){reasons.push('No RCA is linked to '+id+'.');continue;}const rv=validateRootCauseRecord(rca,project);if(!rv.valid)reasons.push(...rv.reasons.map(x=>id+': '+x));if(rv.responsibleLayer&&layer&&upper(rv.responsibleLayer)!==upper(layer)&&upper(recordValue(record,'AUTHORIZATION'))!=='CONTROLLED OVERRIDE')reasons.push('Changeset responsible layer does not match RCA for '+id+' and no controlled override exists.');}return {valid:reasons.length===0,reasons,triggerIds};}
function confirmationDetermination(project,record){const reasons=[],iterationId=String(recordValue(record,'CONFIRMATION_ITERATION_ID')||record?.relationships?.CONFIRMATION_ITERATION_ID||record?.scope?.iterationId||'').trim(),iteration=records(project,'iterations').find(r=>recordId(r,'iterations')===iterationId);if(!iteration){reasons.push('Confirmation iteration identity is missing.');return {determination:'UNDETERMINED',reasons};}const ev=evaluateIteration(project,iterationId,'UNCHANGED_CONFIRMATION');if(!ev.complete)reasons.push(...ev.reasons);const matrix=verificationMatrix(project,iterationId);if(matrix.expected.length===0||matrix.missing.length||matrix.duplicates.length||matrix.invalid.length)reasons.push('Confirmation verification matrix is incomplete or invalid.');const bad=matrix.verification.map(r=>effectiveDetermination('verification',r,testForResult(project,r),project)).filter(x=>x!=='SATISFIED');if(bad.length)reasons.push('Confirmation contains a non-satisfied effective verification result.');const regs=records(project,'regressions').filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED');for(const reg of regs){const id=recordId(reg,'regressions'),exec=currentRegressionExecutions(project,iterationId).filter(x=>String(recordValue(x,'REG_ID')||x.relationships?.REG_ID||'')===id&&upper(recordValue(x,'PHASE'))!=='PRE_CORRECTION');if(exec.length!==1||effectiveRegressionDetermination(project,exec[0]).determination!=='SATISFIED')reasons.push('Active regression '+id+' is not successfully evidenced in confirmation.');}const defects=confirmedDefects(project).filter(d=>String(d.scope?.iterationId||'')===iterationId);if(defects.length)reasons.push('Confirmation iteration has new confirmed defects.');for(const req of mandatoryRequirements(project,scopeForIteration(project,iterationId))){const f=comparisonFacts(project,requirementId(req),iterationId);if(!f.allSatisfied)reasons.push('Confirmation comparison is not all-satisfied for '+requirementId(req)+'.');}return {determination:reasons.length?'UNDETERMINED':'SATISFIED',reasons};}
function auditDetermination(collection,record,project){const reasons=[],e=evaluateEvidenceContract(null,record,null,project);if(!e.sufficient)reasons.push(...e.reasons);if(collection==='processAudits'){for(const key of ['UNAUTHORIZED_MODIFICATION','PROCESS_DEFECTS','BLOCKERS'])if(adjudicationAdverse(recordValue(record,key)))reasons.push(key+' is materially adverse.');for(const key of ['APPROVED_INPUTS_VS_ACTUAL','APPROVED_INSTRUCTION_VS_ACTUAL','APPROVED_TOOLS_VS_ACTUAL','REQUIRED_TESTS_VS_EXECUTED','CHAIN_OF_CUSTODY']){const v=recordValue(record,key),out=controlledOutcome(v);if(out!=='SATISFIED'&& !['MATCH','MATCHED','IDENTICAL','SAME','COMPLIANT','COMPLETE','COMPLETED','INTACT'].includes(upper(v)))reasons.push(key+' is not application-establishable as satisfied.');}}else{for(const key of ['CRITICAL_DEFECTS','MAJOR_DEFECTS','MANDATORY_UNKNOWNS'])if(numeric(recordValue(record,key))>0)reasons.push(key+' is nonzero.');for(const key of ['PRODUCT_DEFECTS','BLOCKERS'])if(adjudicationAdverse(recordValue(record,key)))reasons.push(key+' is materially adverse.');for(const key of ['VALIDATOR_RESULTS','MEANING_VERIFICATION_RESULTS'])if(controlledOutcome(recordValue(record,key))!=='SATISFIED')reasons.push(key+' does not establish success.');const mandatory=numeric(recordValue(record,'MANDATORY_REQUIREMENT_COUNT')),affirmative=numeric(recordValue(record,'AFFIRMATIVE_SATISFACTION_COUNT'));if(mandatory>0&&affirmative!==mandatory)reasons.push('Affirmative satisfaction count does not equal mandatory requirement count.');}return {determination:reasons.length?'UNDETERMINED':'SATISFIED',reasons};}
function evaluateResultConsistency(collection,record,test,project){
  const reasons=[],claimed=claimedDetermination(collection,record),evidence=evaluateEvidenceContract(test,record,null,project);if(['SATISFIED','ACCEPTED','PASSED','PASS','SUCCESS','SUCCEEDED'].includes(claimed)&&!evidence.sufficient)reasons.push(...evidence.reasons);
  if(collection==='verification'||collection==='meaningResults'||collection==='adversarialResults'){const ctx=resultContextIdentity(project,record);if(!ctx)reasons.push('Independent result lacks a reviewer/verifier context identity.');}
  if(collection==='verification'){const runId=String(recordValue(record,'RUN_ID')||record.relationships?.RUN_ID||''),run=records(project,'runs').find(r=>recordId(r,'runs')===runId),generator=String(recordValue(run,'CONTEXT_ID')||run?.relationships?.CONTEXT_ID||''),reviewer=resultContextIdentity(project,record),ind=evaluateContextIndependence(project,{role:'RUN_VERIFIER',verifierContextId:reviewer,generatorContextId:generator});if(ind.determination==='VIOLATED')reasons.push(...ind.reasons);}
  if(collection==='meaningResults'||collection==='adversarialResults'){const productId=String(recordValue(record,'PRODUCT_ID')||record.relationships?.PRODUCT_ID||''),product=records(project,'products').find(p=>recordId(p,'products')===productId),producer=String(recordValue(product,'PRODUCTION_CONTEXT_ID')||product?.relationships?.PRODUCTION_CONTEXT_ID||''),reviewer=resultContextIdentity(project,record),ind=evaluateContextIndependence(project,{role:collection==='meaningResults'?'MEANING_REVIEW':'ADVERSARIAL_REVIEW',reviewerContextId:reviewer,productionContextId:producer});if(ind.determination==='VIOLATED')reasons.push(...ind.reasons);}
  const controlled=claimed?controlledOutcome(claimed):'';return {determination:reasons.length?'UNDETERMINED':controlled||'UNDETERMINED',reasons,evidence};
}
function effectiveDetermination(collection,record,test,project){return evaluateResultConsistency(collection,record,test,project).determination;}

function releaseVerificationTrust(project,record){const runId=String(recordValue(record,'RUN_ID')||record.relationships?.RUN_ID||''),run=records(project,'runs').find(r=>recordId(r,'runs')===runId),generator=String(recordValue(run,'CONTEXT_ID')||run?.relationships?.CONTEXT_ID||''),reviewer=resultContextIdentity(project,record);return evaluateContextIndependence(project,{role:'RUN_VERIFIER',verifierContextId:reviewer,generatorContextId:generator});}
function resultContextIdentity(project,record){return String(recordValue(record,'VERIFIER_CONTEXT_ID')||recordValue(record,'REVIEW_CONTEXT_ID')||recordValue(record,'CONTEXT_ID')||record.relationships?.CONTEXT_ID||record.scope?.contextId||'').trim();}
function resultPropositionKey(project,collection,record){const req=resultRequirementId(project,record),run=String(recordValue(record,'RUN_ID')||record.relationships?.RUN_ID||''),test=String(recordValue(record,'TEST_ID')||record.relationships?.TEST_ID||'');return `${collection==='verification'?'RUN':collection}|${req}|${run}|${test}`;}

function evaluateEvidenceSufficiency(project,{requirement,test,result,executionPlan}={}){
  ensureShape(project);const reasons=[],requiredEvidenceClasses=[],presentEvidence=evidenceRecordsFor(project,result),presentIds=presentEvidence.map(e=>recordId(e,'evidenceRecords')),currentArtifactIds=evidenceArtifactIds(presentEvidence),mode=upper(executionPlan?.executionMode||recordValue(test,'EXECUTION_MODE')||''),type=upper(recordValue(test,'TEST_TYPE')||''),contract=evaluateEvidenceContract(test,result,executionPlan,project);
  if(requiredArtifactIds(test).length||/BYTE|HASH|FILE|ARTIFACT/.test(type))requiredEvidenceClasses.push('APPLICATION_VERIFIED_ARTIFACT_BYTES');
  if(mode==='APPLICATION_DETERMINISTIC'||mode==='EXTERNAL_AGENT_TOOL')requiredEvidenceClasses.push('EXECUTION_RESULT_IDENTITY');
  if(mode==='INDEPENDENT_AGENT_REVIEW'||type==='MEANING')requiredEvidenceClasses.push('MEANING_OBSERVATION_AND_COMPARISON');
  if(mode==='HUMAN_INSPECTION')requiredEvidenceClasses.push('HUMAN_OBSERVATION');
  if(mode==='EXTERNAL_SYSTEM')requiredEvidenceClasses.push('ATTRIBUTABLE_EXTERNAL_SYSTEM_EVIDENCE');
  reasons.push(...contract.reasons);return {sufficient:reasons.length===0,reasons,requiredEvidenceClasses:[...new Set(requiredEvidenceClasses)],presentEvidenceIds:presentIds,currentArtifactIds};
}

function detectCurrentContradictions(project){
  ensureShape(project);const contradictions=[];const collections=['verification','deterministicResults','meaningResults','adversarialResults'];
  const propositionGroups=new Map();for(const collection of collections)for(const record of recordsForCurrentScope(project,collection)){const key=resultPropositionKey(project,collection,record),determination=effectiveDetermination(collection,record,testForResult(project,record),project);if(!propositionGroups.has(key))propositionGroups.set(key,[]);propositionGroups.get(key).push({collection,recordId:recordId(record,collection),determination,requirementId:resultRequirementId(project,record)});}
  for(const [key,items] of propositionGroups){const ds=new Set(items.map(x=>x.determination).filter(x=>['SATISFIED','VIOLATED'].includes(x)));if(ds.size>1)contradictions.push({type:'SAME_PROPOSITION',key,items,severity:'RELEASE_MATERIAL'});}
  const reqGroups=new Map();for(const collection of ['deterministicResults','meaningResults','adversarialResults'])for(const record of recordsForCurrentScope(project,collection)){const req=resultRequirementId(project,record);if(!req)continue;const test=testForResult(project,record),type=upper(recordValue(test,'TEST_TYPE')||collection),prop=String(recordValue(test,'EXPECTED_RESULT')||recordValue(test,'PROCEDURE')||recordValue(test,'FAILURE_CONDITION')||'').trim(),key=`REQ|${req}|${hash.sha256Text(prop||type)}`;if(!reqGroups.has(key))reqGroups.set(key,[]);reqGroups.get(key).push({collection,recordId:recordId(record,collection),determination:effectiveDetermination(collection,record,test,project),testId:recordId(test,'tests')});}
  for(const [key,items] of reqGroups){const ds=new Set(items.map(x=>x.determination).filter(x=>['SATISFIED','VIOLATED'].includes(x)));if(ds.size>1)contradictions.push({type:'SAME_REQUIREMENT_PROPOSITION',key,items,severity:'RELEASE_MATERIAL'});}
  const artifactGroups=new Map();for(const artifact of recordsForCurrentScope(project,'artifacts')){const id=recordId(artifact,'artifacts'),version=String(recordValue(artifact,'VERSION')||artifact.scope?.productId||artifact.scope?.baselineId||''),key=`${id}|${version}`;if(!artifactGroups.has(key))artifactGroups.set(key,[]);artifactGroups.get(key).push({sha256:String(recordValue(artifact,'SHA256')||''),byteSize:Number(recordValue(artifact,'BYTE_SIZE')),lineage:String(recordValue(artifact,'LINEAGE')||recordValue(artifact,'ROLE')||'')});}
  for(const [key,items] of artifactGroups){if(new Set(items.map(x=>`${x.sha256}|${x.byteSize}|${x.lineage}`)).size>1)contradictions.push({type:'ARTIFACT_IDENTITY',key,items,severity:'RELEASE_MATERIAL'});}
  const product=recordsForCurrentScope(project,'products').at(-1),baseline=recordsForCurrentScope(project,'baselines').at(-1);if(product&&baseline){const productBaseline=String(recordValue(product,'BASELINE_ID')||product.relationships?.BASELINE_ID||''),currentBaseline=recordId(baseline,'baselines');if(productBaseline&&currentBaseline&&productBaseline!==currentBaseline)contradictions.push({type:'BASELINE_PRODUCT_LINEAGE',productId:recordId(product,'products'),productBaseline,currentBaseline,severity:'RELEASE_MATERIAL'});}
  const acceptedRelease=recordsForCurrentScope(project,'releaseRecords').find(r=>upper(recordValue(r,'DETERMINATION'))==='ACCEPTED'),blockers=openBlockers(project);if(acceptedRelease&&blockers.length)contradictions.push({type:'RELEASE_WITH_BLOCKER',releaseId:recordId(acceptedRelease,'releaseRecords'),blockerIds:blockers.map(b=>recordId(b,'blockers')),severity:'RELEASE_MATERIAL'});
  const releaseViolations=[...recordsForCurrentScope(project,'deterministicResults'),...recordsForCurrentScope(project,'meaningResults'),...recordsForCurrentScope(project,'adversarialResults')].filter(r=>effectiveDetermination(r.stage===22?'deterministicResults':r.stage===23?'meaningResults':'adversarialResults',r,testForResult(project,r),project)==='VIOLATED');if(acceptedRelease&&releaseViolations.length)contradictions.push({type:'RELEASE_WITH_VIOLATION',releaseId:recordId(acceptedRelease,'releaseRecords'),resultIds:releaseViolations.map(r=>r.id),severity:'RELEASE_MATERIAL'});
  return contradictions;
}

function executionStability(project,iterationId){
  const runs=recordsForIteration(project,'runs',iterationId),verification=recordsForIteration(project,'verification',iterationId),reqs=mandatoryRequirements(project,scopeForIteration(project,iterationId)),tests=recordsForScope(project,'tests',scopeForIteration(project,iterationId)),requirementStability={},testStability={};
  for(const req of reqs){const id=requirementId(req),rows=verification.filter(r=>String(recordValue(r,'REQ_ID')||r.relationships?.REQ_ID||'')===id),ds=rows.map(r=>effectiveDetermination('verification',r,testForResult(project,r),project)),counts={satisfied:ds.filter(x=>x==='SATISFIED').length,violated:ds.filter(x=>x==='VIOLATED').length,undetermined:ds.filter(x=>!['SATISFIED','VIOLATED'].includes(x)).length};requirementStability[id]={...counts,agreementRate:ds.length?Math.max(counts.satisfied,counts.violated,counts.undetermined)/ds.length:0};}
  for(const test of tests){const id=recordId(test,'tests'),rows=verification.filter(r=>String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'')===id),ds=rows.map(r=>effectiveDetermination('verification',r,test,project)),counts={satisfied:ds.filter(x=>x==='SATISFIED').length,violated:ds.filter(x=>x==='VIOLATED').length,undetermined:ds.filter(x=>!['SATISFIED','VIOLATED'].includes(x)).length};testStability[id]={...counts,agreementRate:ds.length?Math.max(counts.satisfied,counts.violated,counts.undetermined)/ds.length:0};}
  const defects=recordsForIteration(project,'defects',iterationId),groups=new Map(),newDefectsByRun={};for(const defect of defects){const key=String(recordValue(defect,'DUPLICATE_GROUP')||recordValue(defect,'ROOT_CAUSE_CATEGORY')||recordValue(defect,'OBSERVED_FAILURE')||recordId(defect,'defects')),run=String(recordValue(defect,'RUN_ID')||defect.relationships?.RUN_ID||'UNASSIGNED');groups.set(key,(groups.get(key)||0)+1);newDefectsByRun[run]=(newDefectsByRun[run]||0)+1;}
  const repeatedDefectCount=[...groups.values()].filter(n=>n>1).reduce((a,b)=>a+b,0),uniqueDefectCount=[...groups.values()].filter(n=>n===1).length,requirementsWithCompleteAgreement=Object.values(requirementStability).filter(x=>x.agreementRate===1&&x.undetermined===0).length,requirementsWithDisagreement=Object.values(requirementStability).filter(x=>x.agreementRate<1).length,unexplainedVarianceCount=recordsForIteration(project,'comparisons',iterationId).filter(r=>truth(recordValue(r,'CORRECTNESS_AFFECTING_VARIANCE'))&&!String(recordValue(r,'EXPLANATION')||recordValue(r,'EVIDENCE')||'').trim()).length;
  return {runCount:runs.length,requirementStability,testStability,totalDistinctDefects:groups.size,repeatedDefectCount,uniqueDefectCount,newDefectsByRun,unexplainedVarianceCount,requirementsWithCompleteAgreement,requirementsWithDisagreement};
}

function verificationMatrix(project,iterationId){
  const scope=scopeForIteration(project,iterationId),requirements=mandatoryRequirements(project,scope),tests=recordsForScope(project,'tests',scope).filter(t=>!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(t,'STATUS')||'READY'))),runs=recordsForIteration(project,'runs',iterationId),verification=recordsForIteration(project,'verification',iterationId),expected=[];
  for(const req of requirements)for(const run of runs)for(const test of tests.filter(t=>testRequirementId(t)===requirementId(req)))expected.push(`${requirementId(req)}|${recordId(run,'runs')}|${recordId(test,'tests')}`);
  const grouped=new Map();for(const record of verification){const key=verificationKey(record);if(!grouped.has(key))grouped.set(key,[]);grouped.get(key).push(record);}
  const missing=expected.filter(key=>!grouped.has(key)),duplicates=[...grouped.entries()].filter(([key,items])=>expected.includes(key)&&items.length!==1).map(([key,items])=>({key,count:items.length})),invalid=[];
  for(const [key,items] of grouped.entries()){if(!expected.includes(key))continue;for(const item of items){const runId=String(recordValue(item,'RUN_ID')||item.relationships?.RUN_ID||''),run=runs.find(r=>recordId(r,'runs')===runId),generator=String(recordValue(run,'CONTEXT_ID')||run?.relationships?.CONTEXT_ID||''),reviewer=resultContextIdentity(project,item),ind=evaluateContextIndependence(project,{role:'RUN_VERIFIER',verifierContextId:reviewer,generatorContextId:generator}),evidence=evaluateEvidenceSufficiency(project,{test:testForResult(project,item),result:item});if(ind.determination!=='APPLICATION_ESTABLISHED'&&!['EXTERNALLY_SUPPORTED'].includes(ind.determination))invalid.push({key,reason:'independence',determination:ind.determination});if(!evidence.sufficient)invalid.push({key,reason:'evidence',details:evidence.reasons});}}
  return {requirements,tests,runs,verification,expected,missing,duplicates,invalid,actual:verification,coverage:expected.length?(expected.length-missing.length-duplicates.length-invalid.length)/expected.length:0};
}

function evaluateIteration(project,iterationId,kind='STANDARD'){
  ensureShape(project);const reasons=[],runs=recordsForIteration(project,'runs',iterationId),independence=evaluateContextIndependence(project,{role:'RUN_BATCH',iterationId}),matrix=verificationMatrix(project,iterationId),candidateId=iterationCandidateId(project,iterationId),candidateHashes=new Set(runs.map(r=>candidateComponentIdentity(project,String(recordValue(r,'CANDIDATE_ID')||r.relationships?.CANDIDATE_ID||''))).filter(Boolean));
  if(runs.length!==10)reasons.push(`Exactly ten current runs are required; found ${runs.length}.`);if(independence.determination!=='APPLICATION_ESTABLISHED')reasons.push(...independence.reasons);if(!candidateId)reasons.push('Iteration has no frozen candidate identity.');if(candidateHashes.size>1)reasons.push('Run candidate hashes differ.');if(matrix.expected.length===0||matrix.missing.length||matrix.duplicates.length||matrix.invalid.length)reasons.push('Verification matrix is incomplete or invalid.');
  return {iterationId,kind,runs,matrix,independence,candidateId,complete:reasons.length===0,reasons,contextCount:new Set(runs.map(r=>String(recordValue(r,'CONTEXT_ID')||r.relationships?.CONTEXT_ID||'')).filter(Boolean)).size,stability:executionStability(project,iterationId)};
}

function coverageMetrics(project){
  const requirements=mandatoryRequirements(project),tests=recordsForCurrentScope(project,'tests'),coverage=new Map();for(const req of requirements)coverage.set(requirementId(req),[]);for(const test of tests){const id=testRequirementId(test);if(coverage.has(id))coverage.get(id).push(test);}
  const readyTest=t=>{const mode=upper(recordValue(t,'EXECUTION_MODE')),status=upper(recordValue(t,'STATUS')||'READY');if(['RETIRED','BLOCKED','NOT READY'].includes(status))return false;if(mode==='APPLICATION_DETERMINISTIC'&&!applicationTestSupported(t))return false;if(mode==='UNAVAILABLE')return false;return Boolean(mode&&configuredRequiredCapability(t));};
  const readyRequirementCount=[...coverage.values()].filter(list=>list.some(readyTest)).length,mandatoryRequirementCount=requirements.length,testedRequirements=[...coverage.values()].filter(list=>list.length).length;return {mandatoryRequirementCount,requirementsWithTests:testedRequirements,readyRequirementCount,requirementCoverage:mandatoryRequirementCount?readyRequirementCount/mandatoryRequirementCount:1};
}
function currentRegressionExecutions(project,iterationId){const scope=iterationId?scopeForIteration(project,iterationId):currentScope(project);return recordsForScope(project,'regressionExecutions',scope);}
function convergenceMetrics(project){
  const iteration=latestIteration(project,[17]),iterationId=recordId(iteration,'iterations'),scope=iterationId?scopeForIteration(project,iterationId):currentScope(project),requirements=mandatoryRequirements(project,scope),tests=recordsForScope(project,'tests',scope),matrix=iterationId?verificationMatrix(project,iterationId):{expected:[],missing:[],duplicates:[],invalid:[],verification:[],coverage:0},regressions=records(project,'regressions').filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),regExec=currentRegressionExecutions(project,iterationId),successfulRegs=regressions.filter(reg=>{const id=recordId(reg,'regressions'),e=regExec.filter(x=>String(recordValue(x,'REG_ID')||x.relationships?.REG_ID||'')===id&&upper(recordValue(x,'PHASE'))!=='PRE_CORRECTION');return e.length===1&&effectiveRegressionDetermination(project,e[0]).determination==='SATISFIED';}),defects=unresolvedMaterialDefects(project),contradictions=detectCurrentContradictions(project),blockers=openBlockers(project),unknowns=requirements.filter(r=>['UNKNOWN','UNDETERMINED'].includes(upper(recordValue(r,'APPLICABILITY')))),ambiguities=requirements.filter(r=>!String(recordValue(r,'OBLIGATION')||'').trim()||!String(recordValue(r,'OBSERVABLE_SATISFACTION_CONDITION')||'').trim()),stability=iterationId?executionStability(project,iterationId):{unexplainedVarianceCount:0};
  const requirementCoverage=requirements.length?requirements.filter(r=>{const id=requirementId(r);return tests.some(t=>testRequirementId(t)===id);}).length/requirements.length:1,verificationCoverage=matrix.expected.length&&matrix.missing.length===0&&matrix.duplicates.length===0&&matrix.invalid.length===0?1:0,regressionSuccess=regressions.length?successfulRegs.length/regressions.length:1;
  const criticalDefects=defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='CRITICAL').length,majorDefects=defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='MAJOR').length,mandatoryUnresolvedUnknowns=unknowns.length,knownCorrectnessAffectingContradictions=contradictions.length,knownCorrectnessAffectingAmbiguities=ambiguities.length,unexplainedCorrectnessAffectingExecutionVariance=stability.unexplainedVarianceCount;
  const converged=Boolean(iterationId)&&requirementCoverage===1&&verificationCoverage===1&&regressionSuccess===1&&criticalDefects===0&&majorDefects===0&&mandatoryUnresolvedUnknowns===0&&knownCorrectnessAffectingContradictions===0&&knownCorrectnessAffectingAmbiguities===0&&unexplainedCorrectnessAffectingExecutionVariance===0&&blockers.length===0;
  return {iterationId,requirementCoverage,verificationCoverage,regressionSuccess,criticalDefects,majorDefects,mandatoryUnresolvedUnknowns,contradictions:knownCorrectnessAffectingContradictions,ambiguities:knownCorrectnessAffectingAmbiguities,unexplainedVariance:unexplainedCorrectnessAffectingExecutionVariance,blockers:blockers.length,converged};
}

function releaseMetrics(project){
  const requirements=mandatoryRequirements(project),results=[...recordsForCurrentScope(project,'deterministicResults').map(r=>['deterministicResults',r]),...recordsForCurrentScope(project,'meaningResults').map(r=>['meaningResults',r]),...recordsForCurrentScope(project,'adversarialResults').map(r=>['adversarialResults',r])],byReq=new Map(requirements.map(r=>[requirementId(r),[]]));for(const [collection,result] of results){const req=resultRequirementId(project,result);if(byReq.has(req))byReq.get(req).push({collection,result,determination:effectiveDetermination(collection,result,testForResult(project,result),project)});}const satisfied=[],violated=[],undetermined=[];for(const [req,items] of byReq){if(items.some(x=>x.determination==='VIOLATED'))violated.push(req);else if(items.length&&items.every(x=>x.determination==='SATISFIED'))satisfied.push(req);else undetermined.push(req);}const tests=recordsForCurrentScope(project,'tests').filter(t=>!['OPTIONAL','RETIRED'].includes(upper(recordValue(t,'STATUS')))),failedValidatorIds=[],unknownValidatorIds=[];for(const test of tests){const id=recordId(test,'tests'),all=results.filter(([,r])=>String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'')===id).map(([c,r])=>effectiveDetermination(c,r,test,project));if(all.includes('VIOLATED'))failedValidatorIds.push(id);else if(all.length!==1||all[0]!=='SATISFIED')unknownValidatorIds.push(id);}const defects=unresolvedMaterialDefects(project),blockers=openBlockers(project),contradictions=detectCurrentContradictions(project),process=recordsForCurrentScope(project,'processAudits'),products=recordsForCurrentScope(project,'productAudits'),auditBad=[...process.map(r=>auditDetermination('processAudits',r,project)),...products.map(r=>auditDetermination('productAudits',r,project))].filter(x=>x.determination!=='SATISFIED'),baselineId=project.job.CURRENT_BASELINE_ID||'',productId=project.job.CURRENT_PRODUCT_ID||'',product=recordsForCurrentScope(project,'products').find(r=>recordId(r,'products')===productId),productBaseline=String(recordValue(product,'BASELINE_ID')||product?.relationships?.BASELINE_ID||''),identityOk=!productId||!baselineId||productBaseline===baselineId;
  const criticalDefects=defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='CRITICAL').length,majorDefects=defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='MAJOR').length,affirmativeViolation=violated.length>0||failedValidatorIds.length>0||criticalDefects>0||majorDefects>0,auditUncertain=auditBad.length>0,uncertain=undetermined.length>0||unknownValidatorIds.length>0||blockers.length>0||contradictions.length>0||auditUncertain||!identityOk;const determination=affirmativeViolation?'REJECTED':uncertain?'BLOCKED':'ACCEPTED';return {determination,mandatoryRequirementCount:requirements.length,satisfied:satisfied.length,violated:violated.length,undetermined:undetermined.length,validatorCount:tests.length,failedValidatorIds,unknownValidatorIds,criticalDefects,majorDefects,blockingRequirements:undetermined,violatedRequirements:violated,blockerIds:blockers.map(b=>recordId(b,'blockers')),contradictions,baselineId,productId,identityOk,auditUncertain,inputReferences:{requirements:requirements.map(r=>recordId(r,'requirements')),results:results.map(([c,r])=>`${c}:${recordId(r,c)}`),defects:defects.map(d=>recordId(d,'defects')),blockers:blockers.map(b=>recordId(b,'blockers'))}};
}

function representationInspectionCoverage(project){
  ensureShape(project);const delivery=recordsForCurrentScope(project,'artifacts').filter(a=>['DELIVERY','FINAL_PRODUCT','FINISHED_PRODUCT','PRODUCT'].includes(upper(recordValue(a,'ROLE')))||String(a.scope?.productId||'')===String(project.job.CURRENT_PRODUCT_ID||'')),inspections=recordsForCurrentScope(project,'representationInspections'),byArtifact=new Map();for(const r of inspections){const id=String(recordValue(r,'ARTIFACT_ID')||r.relationships?.ARTIFACT_ID||'');if(!byArtifact.has(id))byArtifact.set(id,[]);byArtifact.get(id).push(r);}const details=delivery.map(a=>{const id=recordId(a,'artifacts'),rows=byArtifact.get(id)||[],pages=numeric(recordValue(a,'REQUIRED_PAGES_OR_VIEWS')||1),packageFiles=numeric(recordValue(a,'REQUIRED_PACKAGED_FILES')||0),viewed=rows.reduce((sum,r)=>sum+numeric(recordValue(r,'PAGES_OR_VIEWS_INSPECTED')||1),0),opened=rows.reduce((sum,r)=>sum+numeric(recordValue(r,'PACKAGED_FILES_OPENED_OR_TESTED')||0),0),determinations=rows.map(r=>controlledOutcome(recordValue(r,'DETERMINATION')));return {artifactId:id,requiredPagesOrViews:pages,inspectedPagesOrViews:viewed,requiredPackagedFiles:packageFiles,openedOrTestedPackagedFiles:opened,complete:rows.length>0&&viewed>=pages&&opened>=packageFiles&&determinations.every(d=>d==='SATISFIED')};}),reasons=details.filter(x=>!x.complete).map(x=>`${x.artifactId} representation inspection is incomplete or non-satisfied.`);return {complete:delivery.length>0&&reasons.length===0,reasons,totalDeliveryArtifacts:delivery.length,totalPagesOrViewsRequired:details.reduce((a,b)=>a+b.requiredPagesOrViews,0),totalPagesOrViewsInspected:details.reduce((a,b)=>a+b.inspectedPagesOrViews,0),totalPackagedFilesRequired:details.reduce((a,b)=>a+b.requiredPackagedFiles,0),totalPackagedFilesOpenedOrTested:details.reduce((a,b)=>a+b.openedOrTestedPackagedFiles,0),totalTransformationsInspected:inspections.filter(r=>!adjudicationEmpty(recordValue(r,'TRANSFORMATION_CHAIN'))).length,details};
}

function adjudicationEmpty(value){if(value===undefined||value===null)return true;if(typeof value==='string')return !value.trim()||['UNKNOWN','UNDETERMINED','PENDING','TBD','NOT RECORDED'].includes(upper(value));if(Array.isArray(value))return value.length===0;if(typeof value==='object')return Object.keys(value).length===0;return false;}
function controlledOutcome(value){const v=upper(value);if(['SATISFIED','PASS','PASSED','SUCCESS','SUCCEEDED','ACCEPTED','VALID','COMPLIANT','COMPLETE','COMPLETED','MATCH','MATCHED','IDENTICAL','SAME','INTACT','EFFECTIVE'].includes(v))return 'SATISFIED';if(['VIOLATED','FAIL','FAILED','REJECTED','INVALID','NONCOMPLIANT','NON-COMPLIANT','MISMATCH','MISMATCHED','DETECTED','DEFECTIVE'].includes(v))return 'VIOLATED';if(['UNDETERMINED','UNKNOWN','BLOCKED','NOT RUN','NOT_RUN','NOT EXECUTED','PENDING','UNAVAILABLE','EXECUTION_FAILED'].includes(v))return 'UNDETERMINED';return '';}
function adjudicationAdverse(value){const v=upper(value);if(['VIOLATED','FAIL','FAILED','REJECTED','INVALID','NONCOMPLIANT','NON-COMPLIANT','MISMATCH','MISMATCHED','TRUE','YES','FOUND','PRESENT','DETECTED','DEFECTIVE'].includes(v))return true;if(Array.isArray(value))return value.length>0;if(value&&typeof value==='object')return Object.keys(value).length>0;return false;}
function genericAdjudication(record){for(const key of ['STATUS','STATE','RESULT','DETERMINATION','DECISION','OUTCOME']){const out=controlledOutcome(recordValue(record,key));if(out)return out;}return '';}
function requiredAgentFieldsMissing(project,collection,record){const definition=schema.RECORD_SCHEMAS[collection],fields=definition?.fieldDefinitions||{};return Object.entries(fields).filter(([,meta])=>meta.producer==='AGENT'&&meta.required).map(([name])=>name).filter(name=>adjudicationEmpty(recordValue(record,name)));}
function recordAdjudication(collection,record,project){
  const missing=requiredAgentFieldsMissing(project,collection,record);if(missing.length)return {determination:'UNDETERMINED',reasons:['Missing required agent fields: '+missing.join(', ')+'.']};
  if(collection==='requirements'){if(['FALSE','NO','NOT APPLICABLE'].includes(upper(recordValue(record,'APPLICABILITY'))))return {determination:'SATISFIED',reasons:[]};if(['BLOCKED','UNSUPPORTED'].includes(upper(recordValue(record,'STATUS'))))return {determination:'UNDETERMINED',reasons:['Requirement is blocked or unsupported.']};return {determination:'SATISFIED',reasons:[]};}
  if(collection==='sourceConflicts'){const status=upper(recordValue(record,'RESOLUTION_STATUS'));return {determination:['RESOLVED','NOT APPLICABLE'].includes(status)?'SATISFIED':'UNDETERMINED',reasons:['Source conflict is not resolved.'].filter(()=>!['RESOLVED','NOT APPLICABLE'].includes(status))};}
  if(collection==='requirementResolutions'){const status=upper(recordValue(record,'STATUS'));return {determination:['RESOLVED','CLOSED','SATISFIED'].includes(status)?'SATISFIED':'UNDETERMINED',reasons:['Requirement-set defect is unresolved.'].filter(()=>!['RESOLVED','CLOSED','SATISFIED'].includes(status))};}
  if(collection==='preflightRecords'){const out=controlledOutcome(recordValue(record,'DETERMINATION'));return {determination:out||'UNDETERMINED',reasons:out?[]:['Preflight determination is not controlled.']};}
  if(collection==='failureTests'){const expected=upper(recordValue(record,'EXPECTED_REJECTION')),actual=upper(recordValue(record,'ACTUAL_RESULT'));if(!actual)return {determination:'UNDETERMINED',reasons:['Failure fixture has not been executed.']};if(expected.includes('REJECT')){if(/\b(?:REJECTED|BLOCKED|FAILED|INVALID)\b/.test(actual))return {determination:'SATISFIED',reasons:[]};if(/\b(?:ACCEPTED|ALLOWED|PASSED|VALID)\b/.test(actual))return {determination:'VIOLATED',reasons:['Invalid fixture was accepted.']};}return {determination:'UNDETERMINED',reasons:['Failure-test actual result does not establish the expected rejection.']};}
  if(collection==='rootCauses'){const v=validateRootCauseRecord(record,project);return {determination:v.valid?'SATISFIED':'UNDETERMINED',reasons:v.reasons};}
  if(collection==='changes'){const v=validateChangeTrace(record,project);return {determination:v.valid?'SATISFIED':'UNDETERMINED',reasons:v.reasons};}
  if(collection==='regressions'){const id=recordId(record,'regressions'),executions=records(project,'regressionExecutions').filter(e=>String(recordValue(e,'REG_ID')||e.relationships?.REG_ID||'')===id),pre=executions.filter(e=>upper(recordValue(e,'PHASE'))==='PRE_CORRECTION'),post=executions.filter(e=>upper(recordValue(e,'PHASE'))!=='PRE_CORRECTION'),preOk=pre.length>0&&pre.some(e=>effectiveRegressionDetermination(project,e).determination==='SATISFIED'),postOk=post.length>0&&post.some(e=>effectiveRegressionDetermination(project,e).determination==='SATISFIED');return {determination:preOk&&postOk?'SATISFIED':'UNDETERMINED',reasons:[...(!preOk?['Regression pre-correction failure has not been proven.']:[]),...(!postOk?['Regression corrected execution has not been proven.']:[])]};}
  if(collection==='verification'||collection==='deterministicResults'||collection==='meaningResults'||collection==='adversarialResults'||collection==='regressionExecutions'||collection==='processAudits'||collection==='productAudits')return evaluateResultConsistency(collection,record,testForResult(project,record),project);
  if(collection==='confirmationRecords')return confirmationDetermination(project,record);
  if(collection==='representationInspections'){const out=controlledOutcome(recordValue(record,'DETERMINATION'));return {determination:out||'UNDETERMINED',reasons:out?[]:['Representation inspection determination is not controlled.']};}
  if(collection==='releaseGateReviews')return {determination:'SATISFIED',reasons:[]};
  if(collection==='evidenceInvestigations'){const missing=adjudicationEmpty(recordValue(record,'MISSING_LINK'));return {determination:missing?'SATISFIED':'UNDETERMINED',reasons:missing?[]:['Evidence investigation reports a missing link.']};}
  const generic=genericAdjudication(record);return {determination:generic||'SATISFIED',reasons:generic==='UNDETERMINED'?['Record remains undetermined.']:[]};
}
function adjudicationGate(project,stage,reasons){
  const add=items=>{for(const item of items)if(item&&!reasons.includes(item))reasons.push(item);},collections=schema.STAGE_CONTRACTS[stage]?.primaryCollections||[];
  for(const collection of collections)for(const record of recordsForCurrentScope(project,collection)){const result=recordAdjudication(collection,record,project);if(result.determination==='VIOLATED')add([`${collection}/${recordId(record,collection)} is materially nonconforming: ${result.reasons.join('; ')||'violation established'}.`]);else if(result.determination==='UNDETERMINED')add([`${collection}/${recordId(record,collection)} is not established: ${result.reasons.join('; ')||'required facts remain undetermined'}.`]);}
  if(stage===6){const mandatory=new Set(mandatoryRequirements(project).map(requirementId));for(const item of testExecutionPlan(project).items.filter(x=>mandatory.has(x.requirementId)))if(!item.requiredCapability||!item.executionMode)add([`${item.testId}: executor/capability is not determinable.`]);else if(item.executionMode==='UNAVAILABLE')add([`${item.testId}: mandatory test capability is unavailable.`]);else if(item.executionMode==='APPLICATION_DETERMINISTIC'&&!item.applicationExecutorSupported)add([`${item.testId}: APPLICATION_DETERMINISTIC is not faithfully executable by the registered Test IR runtime.`]);else if(!item.artifactReady&&item.requiredArtifactIds.length)add([`${item.testId}: required artifact bytes are unavailable or unverified.`]);}
  if(stage===7){for(const mutation of recordsForCurrentScope(project,'failureTests')){const id=recordId(mutation,'failureTests'),reqId=String(recordValue(mutation,'REQ_ID')||mutation.relationships?.REQ_ID||''),evidence=evaluateEvidenceContract(null,mutation,null,project),plans=testExecutionPlan(project).items.filter(item=>String(item.requirementId||'')===reqId),capable=plans.some(item=>item.operatorAction!=='BLOCKED'&&item.executionRoute!=='BLOCKED'&&item.capabilityReady!==false);if(!evidence.sufficient)add(evidence.reasons.map(reason=>id+': '+reason));if(!capable)add([id+': no capable current execution route is established for the mutated requirement.']);if(adjudicationEmpty(recordValue(mutation,'FIXTURE')))add([id+': actual failure fixture is not identifiable.']);if(adjudicationEmpty(recordValue(mutation,'EXPECTED_REJECTION')))add([id+': expected rejection is missing.']);if(adjudicationEmpty(recordValue(mutation,'ACTUAL_RESULT')))add([id+': actual execution result is missing.']);}}
  if(stage===9)for(const r of recordsForCurrentScope(project,'preflightRecords')){const e=evaluateResultConsistency('preflightRecords',r,null,project);if(e.determination!=='SATISFIED')add([recordId(r,'preflightRecords')+': preflight review is not conclusively satisfied.']);}
  if(stage===12){const it=latestIteration(project,[10,17,19]),id=recordId(it,'iterations'),matrix=id?verificationMatrix(project,id):null;if(!matrix||matrix.expected.length===0)add(['Exact REQ_ID × RUN_ID × TEST_ID matrix is not established.']);else{add(matrix.missing.map(x=>'Missing verification triple '+x+'.'));add(matrix.duplicates.map(x=>'Duplicate verification triple '+x.key+'.'));add(matrix.invalid.map(x=>'Invalid verification triple '+x.key+': '+x.reason+'.'));}}
  if(stage===13){const it=latestIteration(project,[10,17,19]),id=recordId(it,'iterations'),reqs=id?mandatoryRequirements(project,scopeForIteration(project,id)):[];for(const req of reqs){const f=comparisonFacts(project,requirementId(req),id);if(f.runDeterminations.length!==10)add([requirementId(req)+': comparison does not cover exactly ten current runs.']);if(f.runDeterminations.some(x=>!['SATISFIED','VIOLATED','UNDETERMINED'].includes(x.determination)))add([requirementId(req)+': comparison includes an invalid determination.']);}}
  if(stage===14){for(const defect of confirmedDefects(project)){const id=recordId(defect,'defects'),rows=recordsForCurrentScope(project,'rootCauses').filter(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===id);if(rows.length!==1)add([id+': exactly one current RCA is required.']);else{const result=validateRootCauseRecord(rows[0],project);if(!result.valid)add(result.reasons.map(r=>id+': '+r));}}}
  if(stage===15){for(const defect of confirmedDefects(project)){const id=recordId(defect,'defects'),regs=records(project,'regressions').filter(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===id&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED');if(!regs.length)add([id+': confirmed defect has no permanent regression.']);for(const reg of regs){const execs=records(project,'regressionExecutions').filter(e=>String(recordValue(e,'REG_ID')||e.relationships?.REG_ID||'')===recordId(reg,'regressions')&&upper(recordValue(e,'PHASE'))==='PRE_CORRECTION');if(!execs.some(e=>effectiveRegressionDetermination(project,e).determination==='SATISFIED'))add([recordId(reg,'regressions')+': preserved failure has not been reproduced by actual pre-correction execution.']);}}}
  if(stage===16){for(const defect of confirmedDefects(project)){const id=recordId(defect,'defects'),changes=recordsForCurrentScope(project,'changes').filter(c=>{const raw=recordValue(c,'TRIGGERING_DEFECT_IDS');return (Array.isArray(raw)?raw.map(String):String(raw||'').split(/[;,\s]+/)).includes(id);});if(!changes.length&&!openBlockers(project,16).some(b=>String(recordValue(b,'AFFECTED_ARTIFACTS')||'').includes(id)))add([id+': confirmed RCA has neither controlled correction nor blocker.']);for(const change of changes){const v=validateChangeTrace(change,project);if(!v.valid)add(v.reasons.map(r=>recordId(change,'changes')+': '+r));}}}
  if(stage===17){const it=latestIteration(project,[17]),id=recordId(it,'iterations'),ev=id?evaluateIteration(project,id,'CORRECTED'):null;if(!ev||!ev.complete)add(['Corrected iteration is incomplete: '+(ev?.reasons.join('; ')||'no Stage 17 iteration exists')]);}
  if(stage===18){const c=convergenceMetrics(project);if(!c.converged)add([`Convergence is not established: coverage=${c.requirementCoverage}, verification=${c.verificationCoverage}, regression=${c.regressionSuccess}, critical=${c.criticalDefects}, major=${c.majorDefects}, unknowns=${c.mandatoryUnresolvedUnknowns}, contradictions=${c.contradictions}, ambiguities=${c.ambiguities}, variance=${c.unexplainedVariance}.`]);}
  if(stage===19){const confirmation=recordsForCurrentScope(project,'confirmationRecords').at(-1),result=confirmationDetermination(project,confirmation);if(result.determination!=='SATISFIED')add(result.reasons.length?result.reasons:['Unchanged confirmation is not satisfied.']);}
  if(stage===20){const confirmation=recordsForCurrentScope(project,'confirmationRecords').filter(r=>confirmationDetermination(project,r).determination==='SATISFIED').at(-1),baseline=recordsForCurrentScope(project,'baselines').at(-1);if(!confirmation)add(['A satisfied unchanged confirmation is required before baseline freeze.']);if(!baseline)add(['No current baseline exists.']);else{if(!truth(recordValue(baseline,'HUMAN_AUTHORIZATION')))add(['Baseline human authorization is missing.']);const approved=recordValue(baseline,'APPROVED_VERSIONS');if(adjudicationEmpty(approved))add(['Baseline approved versions are missing.']);}}
  if(stage===21){const product=recordsForCurrentScope(project,'products').at(-1);if(!product)add(['No current finished product record exists.']);else{const baseline=String(recordValue(product,'BASELINE_ID')||product.relationships?.BASELINE_ID||''),context=String(recordValue(product,'PRODUCTION_CONTEXT_ID')||product.relationships?.PRODUCTION_CONTEXT_ID||''),artifacts=recordsForCurrentScope(project,'artifacts').filter(a=>String(a.scope?.productId||'')===recordId(product,'products'));if(baseline!==String(project.job.CURRENT_BASELINE_ID||''))add(['Finished product lineage does not match the current approved baseline.']);if(!context)add(['Finished product production context identity is missing.']);if(!artifacts.length)add(['Finished-product bytes are not stored as current product artifacts.']);if(artifacts.some(a=>!artifactVerified(a)))add(['One or more finished-product artifacts are not application-verified bytes.']);}}
  if(stage===22){const d=deriveStageData(project,22);if(d.MISSING_TEST_RESULTS.length)add(['Mandatory deterministic tests are missing current results: '+d.MISSING_TEST_RESULTS.join(', ')+'.']);if(d.VIOLATED)add(['Mandatory deterministic verification contains violations.']);if(d.UNDETERMINED)add(['Mandatory deterministic verification contains undetermined results.']);if(d.EXECUTED!==d.APPLICABLE_MANDATORY_DETERMINISTIC_TESTS.length)add(['Not every applicable mandatory deterministic test has exactly one conclusive execution result.']);}
  if(stage===23){const d=deriveStageData(project,23);if(d.MEANING_RECORDS_COMPLETED!==d.ACTIVE_MEANING_REQUIREMENTS)add(['Meaning review is incomplete.']);if(d.VIOLATED||d.UNDETERMINED||d.UNSUPPORTED_BARE_CONCLUSIONS)add(['Meaning review contains violation, undetermined, or insufficient-evidence result.']);if(!['APPLICATION_ESTABLISHED','EXTERNALLY_SUPPORTED'].includes(d.EVALUATOR_INDEPENDENT_FROM_GENERATOR))add(['Meaning reviewer independence is not established.']);}
  if(stage===24){const d=deriveStageData(project,24);if(d.UNDETERMINED_ATTACKS)add(['Adversarial review contains undetermined attacks.']);if(d.MANDATORY_DEFECTS_FOUND||d.RETURN_TO_ROOT_CAUSE_REQUIRED)add(['Adversarial review found unresolved mandatory failures.']);if(!['APPLICATION_ESTABLISHED','EXTERNALLY_SUPPORTED'].includes(d.REVIEWER_INDEPENDENT))add(['Adversarial reviewer independence is not established.']);}
  if(stage===25){const c=representationInspectionCoverage(project);if(!c.complete)add(c.reasons.length?c.reasons:['Final representation inspection is incomplete.']);}
  if(stage===26){const process=recordsForCurrentScope(project,'processAudits'),products=recordsForCurrentScope(project,'productAudits'),contradictions=detectCurrentContradictions(project);if(!process.length||!products.length)add(['Current process and product audits are both required.']);for(const r of process){const d=auditDetermination('processAudits',r,project);if(d.determination!=='SATISFIED')add(d.reasons.map(x=>recordId(r,'processAudits')+': '+x));}for(const r of products){const d=auditDetermination('productAudits',r,project);if(d.determination!=='SATISFIED')add(d.reasons.map(x=>recordId(r,'productAudits')+': '+x));}if(contradictions.length)add(['Current process/product evidence remains contradictory.']);}
  if(stage===27){const r=releaseMetrics(project);if(r.determination!=='ACCEPTED')add([`Application release determination is ${r.determination}.`]);}
  if(stage===28){const identity=DERIVATIONS['stage28.artifactIdentity'](project).value;if(identity.DELIVERY_AUTHORIZATION!=='AUTHORIZED')add(['Exact delivery-byte identity is not authorized.']);}
  if(stage===29){const chains=DERIVATIONS['stage29.evidenceChains'](project).value;if(!chains.ALL_MANDATORY_EVIDENCE_CHAINS_COMPLETE)add(['Mandatory evidence-chain structural and sufficiency coverage is incomplete.']);}
  if(stage===30){const d=deriveStageData(project,30);if(d.DEFECT_RECORDS_MISSING_REQUIRED_FIELDS.length)add(['Permanent defect history contains incomplete records.']);if(d.CONFIRMED_DEFECTS_MISSING_REGRESSION_TESTS.length)add(['A confirmed defect lacks a permanent regression test.']);if(d.FINAL_REGISTRY_DETERMINATION!=='SATISFIED')add(['Permanent defect/regression registry is not complete.']);}
}

function gate(project,stage){
  ensureShape(project);const reasons=[],previous=stage>1?project.stages[stage-1]:null;if(previous&&previous.status!=='COMPLETE')reasons.push(`Stage ${String(stage-1).padStart(2,'0')} is not complete.`);const human=unresolvedHumanRequests(project,stage),blockers=openBlockers(project,stage);if(human.length)reasons.push(`${human.length} blocking human request(s) remain unresolved.`);if(blockers.length)reasons.push(`${blockers.length} blocker(s) remain open.`);
  if(stage===1){const accepted=acceptedChanges(project,1).length>0,intake=evaluateIntakeAccounting(project),confirmation=safe(project.projectData.stageConfirmations).some(x=>Number(x.stage)===1&&x.confirmed&&!x.invalidatedBy&&x.inputVersion===project.job.CURRENT_INPUT_VERSION&&x.acceptedChangeId===acceptedChanges(project,1).at(-1)?.changeId);if(!project.job.CURRENT_INPUT_VERSION)reasons.push('No current User Job Input version exists.');if(!accepted)reasons.push('No current accepted Stage 01 DATA_PROPOSAL exists.');if(!intake.complete)reasons.push(...intake.reasons);for(const key of ['EXACT_DELIVERABLE_REQUESTED','ASSUMPTIONS','UNKNOWN_INFORMATION','INPUT_SET_CONTENTS'])if(adjudicationEmpty(project.stages[1].agentData?.[key]))reasons.push(`Stage 01 accepted ${key} is missing.`);if(!confirmation)reasons.push('Current input version and accepted Stage 01 change do not have human intent confirmation.');}
  if(stage===2){const sources=recordsForCurrentScope(project,'sources'),determination=upper(project.stages[2].agentData?.SOURCE_APPLICABILITY_DETERMINATION),conflicts=recordsForCurrentScope(project,'sourceConflicts').filter(r=>!['RESOLVED','NOT APPLICABLE'].includes(upper(recordValue(r,'RESOLUTION_STATUS'))));if(!sources.length&&determination!=='NO_APPLICABLE_EXTERNAL_SOURCE')reasons.push('Stage 02 requires at least one inspected external authority or NO_APPLICABLE_EXTERNAL_SOURCE.');if(sources.some(r=>['NOT INSPECTED','UNKNOWN',''].includes(upper(recordValue(r,'INSPECTION_STATUS')))))reasons.push('Every current source must be inspected.');if(conflicts.length)reasons.push('Controlling source conflicts remain unresolved.');}
  if(stage===3){const sourceIds=recordsForCurrentScope(project,'sources').map(r=>recordId(r,'sources')),researched=new Set(recordsForCurrentScope(project,'research').map(r=>String(recordValue(r,'SOURCE_ID')||r.relationships?.SOURCE_ID||''))),noSource=upper(project.stages[2].agentData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE';if(!noSource&&sourceIds.some(id=>!researched.has(id)))reasons.push('Every current Stage 02 source requires current Stage 03 research coverage.');if(!truth(project.stages[3].agentData?.SECOND_CONFLICT_AND_EXCEPTION_PASS_COMPLETED))reasons.push('Stage 03 conflict and exception pass is incomplete.');if(!truth(project.stages[3].agentData?.ALL_KNOWN_CONTROLLING_SOURCES_EXAMINED))reasons.push('Stage 03 does not establish complete current source coverage.');if(truth(project.stages[3].agentData?.NEW_MATERIAL_CATEGORY_FOUND_IN_LATEST_PASS))reasons.push('Stage 03 saturation is not reached because the latest pass found a new material category.');}
  if(stage===4){const accounting=evaluateObligationAccounting(project),reqs=recordsForCurrentScope(project,'requirements');if(!accounting.complete)reasons.push(...accounting.reasons);for(const req of reqs){if(adjudicationEmpty(recordValue(req,'OBLIGATION'))||adjudicationEmpty(recordValue(req,'OBSERVABLE_SATISFACTION_CONDITION'))||adjudicationEmpty(recordValue(req,'FAILURE_CONDITION')))reasons.push(`${recordId(req,'requirements')}: requirement is not independently testable.`);}}
  if(stage===5){const issues=recordsForCurrentScope(project,'requirementResolutions').filter(r=>!['RESOLVED','CLOSED','SATISFIED'].includes(upper(recordValue(r,'STATUS'))));if(issues.length)reasons.push('Requirement-set defects remain unresolved.');}
  adjudicationGate(project,stage,reasons);return {complete:reasons.length===0,blocked:blockers.length>0||reasons.some(r=>/blocked|unavailable|cannot|corrupt/i.test(r)),reasons};
}

function deriveStageData(project,stage){ensureShape(project);const derived={};const ids=collection=>recordsForCurrentScope(project,collection).filter(r=>Number(r.stage)===Number(stage)).map(r=>recordId(r,collection));const metrics=[4,6,12].includes(Number(stage))?coverageMetrics(project):null;const convergence=Number(stage)===18?convergenceMetrics(project):null;const release=Number(stage)===27?releaseMetrics(project):null;switch(stage){case 1:{const intake=evaluateIntakeAccounting(project);Object.assign(derived,{JOB_ID:project.job.JOB_ID,DATE_OPENED:project.job.DATE_OPENED,INPUT_SET_VERSION:project.job.CURRENT_INPUT_VERSION,INPUT_SET_HASH_OR_MANIFEST:project.job.INPUT_SET_HASH_OR_MANIFEST||intake.manifest?.manifestSha256||'UNKNOWN',JOB_RECORD_STATUS:intake.complete&&acceptedChanges(project,1).length?'READY':'NOT READY',STATUS_EVIDENCE:intake.complete?`Application accounted ${intake.accountedUnitCount}/${intake.expectedUnitCount} controlled input units.`:intake.reasons.join('; ')});break;}case 2:{const conflicts=recordsForCurrentScope(project,'sourceConflicts'),unresolved=conflicts.filter(r=>['UNRESOLVED','BLOCKED','UNKNOWN','OPEN'].includes(upper(recordValue(r,'RESOLUTION_STATUS'))));Object.assign(derived,{SOURCE_SET_VERSION:project.job.CURRENT_SOURCE_SET_VERSION||'NOT APPLICABLE',SOURCE_RECORDS:ids('sources'),SOURCE_CONFLICT_RECORDS:ids('sourceConflicts'),UNRESOLVED_CONTROLLING_CONFLICTS:unresolved.map(r=>recordId(r,'sourceConflicts'))});break;}case 3:{const sourceIds=recordsForCurrentScope(project,'sources').map(r=>recordId(r,'sources')),research=recordsForCurrentScope(project,'research'),researched=new Set(research.map(r=>String(recordValue(r,'SOURCE_ID')||r.relationships?.SOURCE_ID||''))),candidate=recordsForCurrentScope(project,'candidateRequirements');Object.assign(derived,{RESEARCH_VERSION:'RESEARCH-'+hash.sha256Value(research.map(r=>r.recordSha256||r.sha256||recordId(r,'research'))).slice(0,16).toUpperCase(),SOURCE_RESEARCH_RECORDS:research.map(r=>recordId(r,'research')),CANDIDATE_REQUIREMENT_RECORDS:candidate.map(r=>recordId(r,'candidateRequirements')),ALL_KNOWN_CONTROLLING_SOURCES_EXAMINED:sourceIds.length===0?upper(project.stages[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE':sourceIds.every(id=>researched.has(id))});break;}case 4:{const reqs=recordsForCurrentScope(project,'requirements'),accounting=evaluateObligationAccounting(project),atomicityIssues=reqs.filter(req=>{const obligation=String(recordValue(req,'OBLIGATION')||'').trim(),success=String(recordValue(req,'OBSERVABLE_SATISFACTION_CONDITION')||'').trim(),failure=String(recordValue(req,'FAILURE_CONDITION')||'').trim();return !obligation||!success||!failure;}).map(req=>recordId(req,'requirements'));Object.assign(derived,{REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NOT APPLICABLE',REQUIREMENT_RECORDS:reqs.map(r=>recordId(r,'requirements')),ATOMICITY_REVIEW_RESULTS:{reviewed:reqs.length,structurallyIncomplete:atomicityIssues,obligationAccountingComplete:accounting.complete,obligationAccountingCoverage:accounting.coverage},TOTAL_REQUIREMENTS:reqs.length,MANDATORY_REQUIREMENTS:metrics.mandatoryRequirementCount});break;}case 5:{const resolutions=recordsForCurrentScope(project,'requirementResolutions'),unresolved=resolutions.filter(r=>['OPEN','UNRESOLVED','BLOCKED','UNKNOWN'].includes(upper(recordValue(r,'STATUS')))),blockers=openBlockers(project,5);Object.assign(derived,{INPUT_REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NONE',OUTPUT_REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NONE',REQUIREMENT_DEFECT_RECORDS:resolutions.map(r=>recordId(r,'requirementResolutions')),UNRESOLVED_CONFLICTS:unresolved.map(r=>recordId(r,'requirementResolutions')),MISSING_PREREQUISITES:blockers.filter(b=>/prerequisite/i.test(String(recordValue(b,'MISSING_ITEM_TYPE')||recordValue(b,'WHY_WORK_CANNOT_CONTINUE')||''))).map(b=>recordId(b,'blockers')),MANDATORY_BLOCKERS:blockers.map(b=>recordId(b,'blockers'))});break;}case 6:{const tests=recordsForCurrentScope(project,'tests'),mandatoryIds=new Set(mandatoryRequirements(project).map(requirementId)),mandatoryTests=tests.filter(t=>mandatoryIds.has(testRequirementId(t))),blocked=mandatoryTests.filter(t=>upper(recordValue(t,'EXECUTION_MODE'))==='UNAVAILABLE'||upper(recordValue(t,'STATUS'))==='BLOCKED'||(upper(recordValue(t,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&!applicationTestSupported(t)));Object.assign(derived,{TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NOT APPLICABLE',TEST_RECORDS:tests.map(r=>recordId(r,'tests')),COVERAGE_RECORDS:mandatoryRequirements(project).map(req=>({requirementId:requirementId(req),testIds:tests.filter(t=>testRequirementId(t)===requirementId(req)).map(t=>recordId(t,'tests'))})),TOTAL_ACTIVE_MANDATORY_REQUIREMENTS:metrics.mandatoryRequirementCount,ACTIVE_MANDATORY_REQUIREMENTS_WITH_AT_LEAST_ONE_READY_TEST:metrics.requirementsWithTests,MANDATORY_TEST_COVERAGE:metrics.requirementCoverage,BLOCKED_MANDATORY_REQUIREMENTS:[...new Set(blocked.map(t=>testRequirementId(t)))]});break;}case 7:{const reqs=mandatoryRequirements(project),mutations=recordsForCurrentScope(project,'failureTests'),covered=new Set(mutations.map(testRequirementId)),acceptedInvalid=[],defective=[];for(const m of mutations){const actual=upper(recordValue(m,'ACTUAL_RESULT')),expected=upper(recordValue(m,'EXPECTED_REJECTION')),bad=expected.includes('REJECT')&&/\b(?:ACCEPTED|ALLOWED|PASSED|NOT REJECTED|VALID)\b/.test(actual)&&!/\b(?:REJECTED|BLOCKED|FAILED|INVALID)\b/.test(actual);if(bad){acceptedInvalid.push(recordId(m,'failureTests'));const defect=String(recordValue(m,'VALIDATOR_DEFECT_ID')||m.relationships?.VALIDATOR_DEFECT_ID||'');if(defect)defective.push(defect);}}Object.assign(derived,{MUTATION_SUITE_VERSION:'MUTATION-'+hash.sha256Value(mutations.map(r=>r.recordSha256||r.sha256||recordId(r,'failureTests'))).slice(0,16).toUpperCase(),FAILURE_TEST_RECORDS:mutations.map(r=>recordId(r,'failureTests')),ACTIVE_REQUIREMENTS:reqs.map(requirementId),REQUIREMENTS_WITH_AT_LEAST_ONE_FAILURE_TEST:reqs.filter(r=>covered.has(requirementId(r))).map(requirementId),FAILURE_TEST_COVERAGE:reqs.length?reqs.filter(r=>covered.has(requirementId(r))).length/reqs.length:1,INVALID_FIXTURES_ACCEPTED:acceptedInvalid,DEFECTIVE_VALIDATORS:[...new Set(defective)]});break;}case 8:{const traces=recordsForCurrentScope(project,'instructionTraces');Object.assign(derived,{DRAFT_INSTRUCTION_VERSION:project.job.CURRENT_INSTRUCTION_VERSION||'NONE',INSTRUCTION_TRACE_RECORDS:traces.map(r=>recordId(r,'instructionTraces'))});break;}case 9:{const reviews=recordsForCurrentScope(project,'preflightRecords'),change=acceptedChanges(project,9).at(-1),contextId=String(change?.scope?.contextId||''),ind=evaluateContextIndependence(project,{role:'PREFLIGHT_REVIEW',reviewerContextId:contextId});Object.assign(derived,{INPUT_INSTRUCTION_VERSION:project.job.CURRENT_INSTRUCTION_VERSION||'NONE',OUTPUT_INSTRUCTION_VERSION:project.job.CURRENT_INSTRUCTION_VERSION||'NONE',PREFLIGHT_REVIEWER_ID:contextId||'UNKNOWN',REVIEW_CONTEXT_INDEPENDENT_FROM_AUTHOR:ind.determination,SENTENCE_REVIEW_RECORDS:reviews.map(r=>recordId(r,'preflightRecords')),PREFLIGHT_ITERATION_RECORDS:safe(project.projectData.acceptedChanges).filter(c=>Number(c.stage)===9&&!c.invalidatedBy).map(c=>c.changeId)});break;}case 10:{const iteration=latestIteration(project,[10]),iterationId=recordId(iteration,'iterations'),candidateId=iterationCandidateId(project,iterationId),candidate=records(project,'candidateFreezes').find(r=>recordId(r,'candidateFreezes')===candidateId),manifest=safe(recordValue(candidate,'COMPONENT_MANIFEST')),hashes=recordValue(candidate,'COMPONENT_HASHES')||{};Object.assign(derived,{CANDIDATE_ID:candidateId||'NONE',ITERATION_ID:iterationId||'NONE',FREEZE_DATE:candidate?.createdAt||'NONE',FROZEN_COMPONENT_RECORDS:manifest.length,TOOL_CONFIGURATION_RECORDS:adjudicationEmpty(recordValue(candidate,'TOOL_CONFIGURATION'))?0:1,ALL_REQUIRED_COMPONENTS_PRESENT:manifest.length>0&&manifest.every(x=>x.artifactId&&x.filename&&x.sha256),ALL_RUNS_WILL_RECEIVE_IDENTICAL_FROZEN_MATERIALS:Boolean(candidateId&&candidateComponentIdentity(project,candidateId)),HASHES_RECORDED_WHERE_PRACTICAL:manifest.length>0&&Object.keys(hashes).length===manifest.length,CHANGES_ALLOWED_DURING_BATCH:'NO IN-PLACE MATERIAL CHANGES; ANY MATERIAL CHANGE REQUIRES A NEW CANDIDATE'});break;}case 11:case 19:{const it=latestIteration(project,[stage]),iterationId=recordId(it,'iterations'),ev=evaluateIteration(project,iterationId,stage===19?'UNCHANGED_CONFIRMATION':'INITIAL');Object.assign(derived,{ITERATION_ID:ev.iterationId,RUN_COUNT:ev.runs.length,FRESH_CONTEXT_COUNT:ev.contextCount,ITERATION_COMPLETE:ev.complete,ITERATION_REASONS:ev.reasons,STABILITY_SUMMARY:stage===19?ev.stability:null});if(stage===11){const runs=ev.runs,contexts=runs.map(r=>String(recordValue(r,'CONTEXT_ID')||r.relationships?.CONTEXT_ID||''));Object.assign(derived,{CANDIDATE_ID:iterationCandidateId(project,iterationId)||'NONE',RUN_RECORDS:runs.map(r=>recordId(r,'runs')),FRESH_CONTEXTS_CREATED:new Set(contexts.filter(Boolean)).size,RUNS_RECEIVING_EXACT_PACKAGE:ev.reasons.some(r=>/candidate|package/i.test(r))?0:runs.length,CONTAMINATED_RUNS:runs.filter(r=>{const c=records(project,'freshContexts').find(x=>recordId(x,'freshContexts')===String(recordValue(r,'CONTEXT_ID')||r.relationships?.CONTEXT_ID||''));return ['YES','TRUE','CONTAMINATED'].includes(upper(recordValue(c,'CONTAMINATION_STATUS')||recordValue(r,'CONTAMINATION_CHECK')));}).map(r=>recordId(r,'runs')),OUTPUTS_SAVED_SEPARATELY:runs.filter(r=>!adjudicationEmpty(recordValue(r,'COMPLETE_OUTPUT'))).length});}if(stage===19){const confirmation=recordsForCurrentScope(project,'confirmationRecords').at(-1),sourceId=String(recordValue(confirmation,'SOURCE_ITERATION_ID')||confirmation?.relationships?.SOURCE_ITERATION_ID||''),matrix=verificationMatrix(project,iterationId),regs=records(project,'regressions').filter(r=>isActiveRecord(r)&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),regExec=currentRegressionExecutions(project,iterationId),defects=recordsForIteration(project,'defects',iterationId),comparisonCount=recordsForIteration(project,'comparisons',iterationId).length,reqCount=mandatoryRequirements(project,scopeForIteration(project,iterationId)).length,determination=confirmationDetermination(project,confirmation);Object.assign(derived,{SOURCE_CONVERGED_ITERATION:sourceId||'NONE',CONFIRMATION_ITERATION_ID:iterationId||'NONE',ZERO_CHANGE_AUDIT:!ev.reasons.some(r=>/change|candidate|hash/i.test(r)),TEN_NEW_CONTEXTS_CREATED:ev.contextCount,SAME_FROZEN_PACKAGE_USED:!ev.reasons.some(r=>/package|candidate/i.test(r)),RUNS_COMPLETED:ev.runs.length,COMPLETE_TEST_SUITE_RUN:matrix.expected.length>0&&!matrix.missing.length&&!matrix.duplicates.length,ALL_REGRESSION_TESTS_RUN:regs.every(r=>regExec.some(e=>String(recordValue(e,'REG_ID')||e.relationships?.REG_ID||'')===recordId(r,'regressions'))),CROSS_RUN_COMPARISON_COMPLETED:comparisonCount===reqCount,NEW_CRITICAL_DEFECTS:defects.filter(d=>upper(recordValue(d,'SEVERITY'))==='CRITICAL').length,NEW_MAJOR_DEFECTS:defects.filter(d=>upper(recordValue(d,'SEVERITY'))==='MAJOR').length,NEW_REQUIREMENTS_DISCOVERED:0,INJECTED_DEFECTS_NOT_DETECTED:0,NEW_CORRECTNESS_AFFECTING_VARIANCE:ev.stability?.unexplainedVarianceCount||0,CONFIRMATION_ACCEPTANCE_CRITERIA_SATISFIED:determination.determination==='SATISFIED',REQUIRED_RETURN_STAGE:determination.determination==='SATISFIED'?'NONE':'STAGE 14'});}break;}case 12:{const iteration=latestIteration(project,[10,17,19]),iterationId=recordId(iteration,'iterations'),matrix=verificationMatrix(project,iterationId),rows=recordsForIteration(project,'verification',iterationId),ds=rows.map(r=>effectiveDetermination('verification',r,testForResult(project,r),project)),selfValidated=rows.filter(r=>{const runId=String(recordValue(r,'RUN_ID')||r.relationships?.RUN_ID||''),run=records(project,'runs').find(x=>recordId(x,'runs')===runId),generator=String(recordValue(run,'CONTEXT_ID')||run?.relationships?.CONTEXT_ID||''),reviewer=resultContextIdentity(project,r);return generator&&reviewer&&generator===reviewer;});Object.assign(derived,{ITERATION_ID:iterationId||'NONE',REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NONE',TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NONE',VERIFICATION_RECORDS:rows.map(r=>recordId(r,'verification')),ACTIVE_MANDATORY_REQUIREMENTS:mandatoryRequirements(project,scopeForIteration(project,iterationId)).map(requirementId),RUNS:matrix.runs.map(r=>recordId(r,'runs')),EXPECTED_MANDATORY_RECORDS:matrix.expected.length,ACTUAL_MANDATORY_RECORDS:matrix.actual.length,MISSING_RECORDS:matrix.missing.length,SATISFIED_RECORDS:ds.filter(d=>d==='SATISFIED').length,VIOLATED_RECORDS:ds.filter(d=>d==='VIOLATED').length,UNDETERMINED_RECORDS:ds.filter(d=>d==='UNDETERMINED').length,SELF_VALIDATED_RECORDS:selfValidated.length,VERIFICATION_COVERAGE:matrix.coverage});break;}case 13:{const it=latestIteration(project,[10,17,19]),iterationId=recordId(it,'iterations'),scope=iterationId?scopeForIteration(project,iterationId):currentScope(project),facts={},reqs=mandatoryRequirements(project,scope);for(const req of reqs){const f=comparisonFacts(project,requirementId(req),iterationId);facts[requirementId(req)]={RUN_DETERMINATIONS:f.runDeterminations,ALL_TEN_SATISFIED:f.allSatisfied,ANY_VIOLATION:f.anyViolation,ANY_UNDETERMINED:f.anyUndetermined,SATISFIED_COUNT:f.determinations.filter(x=>x==='SATISFIED').length,VIOLATED_COUNT:f.determinations.filter(x=>x==='VIOLATED').length,UNDETERMINED_COUNT:f.determinations.filter(x=>!['SATISFIED','VIOLATED'].includes(x)).length};}const stability=iterationId?executionStability(project,iterationId):{repeatedDefectCount:0,uniqueDefectCount:0},comparisons=iterationId?recordsForIteration(project,'comparisons',iterationId):[];Object.assign(derived,{ITERATION_ID:iterationId||'NONE',COMPARISON_VERSION:'COMPARISON-'+hash.sha256Value(facts).slice(0,16).toUpperCase(),REQUIREMENT_COMPARISON_RECORDS:comparisons.length,REQUIREMENTS_SATISFIED_BY_ALL_TEN:reqs.filter(r=>facts[requirementId(r)]?.ALL_TEN_SATISFIED).map(requirementId),REQUIREMENTS_WITH_AT_LEAST_ONE_VIOLATION:reqs.filter(r=>facts[requirementId(r)]?.ANY_VIOLATION).map(requirementId),REQUIREMENTS_WITH_AT_LEAST_ONE_UNDETERMINED:reqs.filter(r=>facts[requirementId(r)]?.ANY_UNDETERMINED).map(requirementId),CORRECTNESS_AFFECTING_DISAGREEMENTS:comparisons.filter(r=>truth(recordValue(r,'CORRECTNESS_AFFECTING_VARIANCE'))).map(r=>recordId(r,'comparisons')),PROHIBITED_OUTPUT_VARIANCES:comparisons.filter(r=>adjudicationAdverse(recordValue(r,'PROHIBITED_VARIANCE'))).map(r=>recordId(r,'comparisons')),INCONCLUSIVE_TESTS:reqs.filter(r=>facts[requirementId(r)]?.ANY_UNDETERMINED).map(requirementId),REPEATED_FAILURE_GROUPS:stability.repeatedDefectCount,UNIQUE_FAILURES:stability.uniqueDefectCount,APPLICATION_DERIVED_COMPARISON_FACTS:facts,STABILITY_SUMMARY:stability});break;}case 14:{const defects=confirmedDefects(project),rcas=recordsForCurrentScope(project,'rootCauses'),byDefect=new Map();for(const r of rcas){const id=String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'');if(!byDefect.has(id))byDefect.set(id,[]);byDefect.get(id).push(r);}const confirmed=[],unknown=[],blocked=[];for(const d of defects){const id=recordId(d,'defects'),rows=byDefect.get(id)||[];if(rows.length===1){const v=validateRootCauseRecord(rows[0],project);if(v.valid)confirmed.push(id);else unknown.push(id);}else if(rows.length===0)unknown.push(id);else blocked.push(id);}Object.assign(derived,{ROOT_CAUSE_ANALYSIS_VERSION:'RCA-'+hash.sha256Value(rcas.map(r=>r.sha256||r.recordSha256||recordId(r,'rootCauses'))).slice(0,16).toUpperCase(),DEFECT_ROOT_CAUSE_RECORDS:rcas.length,TOTAL_MATERIAL_DEFECTS:defects.length,CONFIRMED_ROOT_CAUSES:confirmed,UNDETERMINED_ROOT_CAUSES:unknown,BLOCKED_ANALYSES:blocked});break;}case 15:{const defects=confirmedDefects(project),regs=records(project,'regressions').filter(r=>isActiveRecord(r)),execs=records(project,'regressionExecutions'),covered=[],preProven=[],missing=[];for(const d of defects){const id=recordId(d,'defects'),rs=regs.filter(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===id);if(rs.length)covered.push(id);else missing.push(id);if(rs.some(r=>execs.some(e=>String(recordValue(e,'REG_ID')||e.relationships?.REG_ID||'')===recordId(r,'regressions')&&upper(recordValue(e,'PHASE'))==='PRE_CORRECTION'&&effectiveRegressionDetermination(project,e).determination==='SATISFIED')))preProven.push(id);}Object.assign(derived,{INPUT_TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NONE',OUTPUT_TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NONE',REGRESSION_FIXTURE_VERSION:'REGRESSION-'+hash.sha256Value(regs.map(r=>r.sha256||recordId(r,'regressions'))).slice(0,16).toUpperCase(),REGRESSION_RECORDS:regs.length,CONFIRMED_DEFECTS:defects.length,CONFIRMED_DEFECTS_WITH_REGRESSION_TEST:covered,PRE_CORRECTION_FAILURES_PROVEN:preProven,UNCONVERTED_CONFIRMED_DEFECTS:missing});break;}case 16:{const changes=recordsForCurrentScope(project,'changes').filter(r=>Number(r.stage)===16),defects=confirmedDefects(project),triggered=[...new Set(changes.flatMap(c=>{const raw=recordValue(c,'TRIGGERING_DEFECT_IDS');return Array.isArray(raw)?raw.map(String):String(raw||'').split(/[;,\s]+/).filter(Boolean);} ))],changedInstructions=changes.some(c=>/INSTRUCTION/i.test(String(recordValue(c,'RESPONSIBLE_LAYER')||''))),changedArtifacts=changes.filter(c=>String(recordValue(c,'NEW_ARTIFACT_VERSION')||'').trim()),inPlace=changes.filter(c=>truth(recordValue(c,'IN_PLACE_MODIFICATION')));Object.assign(derived,{CHANGE_SET_ID:changes.map(c=>recordId(c,'changes')),TRIGGERING_DEFECT_IDS:triggered,RCA_VERSION:'RCA-'+hash.sha256Value(recordsForCurrentScope(project,'rootCauses').map(r=>r.sha256||recordId(r,'rootCauses'))).slice(0,16).toUpperCase(),DATE:changes.at(-1)?.createdAt||now(),ARTIFACT_CHANGE_RECORDS:changedArtifacts.length,INSTRUCTION_CHANGED:changedInstructions,IF_EXECUTION_ONLY_DEFECT_WAS_INSTRUCTION_PRESERVED:!changedInstructions,PREFLIGHT_REPEATED_IF_CHANGED:changedInstructions?recordsForCurrentScope(project,'preflightRecords').length>0:true,ARTIFACTS_CHANGED:changedArtifacts.length>0,NEW_VERSIONS_CREATED:changedArtifacts.length,IN_PLACE_MODIFICATIONS:inPlace.length,DOWNSTREAM_VERIFICATIONS_INVALIDATED:safe(project.projectData.history).filter(h=>h.type==='DOWNSTREAM_INVALIDATED'&&h.stage===16).at(-1)?.invalidatedStages||[]});break;}case 17:{const iterations=records(project,'iterations').filter(r=>Number(r.stage)===17&&isActiveRecord(r)),currentIt=iterations.at(-1),previous=records(project,'iterations').filter(r=>isActiveRecord(r)&&recordId(r,'iterations')!==recordId(currentIt,'iterations')).at(-1),ev=evaluateIteration(project,recordId(currentIt,'iterations'),'CORRECTED'),matrix=recordId(currentIt,'iterations')?verificationMatrix(project,recordId(currentIt,'iterations')):{coverage:0},comparisons=recordId(currentIt,'iterations')?recordsForIteration(project,'comparisons',recordId(currentIt,'iterations')):[],defects=recordId(currentIt,'iterations')?recordsForIteration(project,'defects',recordId(currentIt,'iterations')):[],rcas=recordsForCurrentScope(project,'rootCauses'),regs=records(project,'regressions').filter(r=>isActiveRecord(r)),changes=recordsForCurrentScope(project,'changes');Object.assign(derived,{PREVIOUS_ITERATION_ID:recordId(previous,'iterations')||'NONE',NEW_ITERATION_ID:recordId(currentIt,'iterations')||'NONE',PREVIOUS_CANDIDATE_ID:previous?iterationCandidateId(project,recordId(previous,'iterations')):'NONE',NEW_CANDIDATE_ID:currentIt?iterationCandidateId(project,recordId(currentIt,'iterations')):'NONE',CHANGESET_ID:recordId(changes.at(-1),'changes')||'NONE',NEW_FROZEN_VERSIONS:recordValue(currentIt,'FROZEN_VERSIONS')||{},OLD_CONVERSATIONS_CONTINUED:false,TEN_NEW_CONTEXTS_CREATED:ev.contextCount,RUN_NAMESPACE:ev.runs.map(r=>recordId(r,'runs')),IDENTICAL_PACKAGE_CONFIRMED_FOR_ALL_RUNS:!ev.reasons.some(r=>/candidate|package/i.test(r)),PRIOR_OUTPUTS_WITHHELD:!ev.reasons.some(r=>/prior|contamin/i.test(r)),EXECUTE_COMPLETED:ev.runs.length===10,VERIFY_COMPLETED:matrix.coverage===1,COMPARE_COMPLETED:comparisons.length===mandatoryRequirements(project,scopeForIteration(project,recordId(currentIt,'iterations'))).length,ROOT_CAUSE_COMPLETED:defects.every(d=>rcas.some(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===recordId(d,'defects'))),REGRESSION_TESTS_ADDED:defects.every(d=>regs.some(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===recordId(d,'defects'))),CORRECTIONS_COMPLETED:defects.every(d=>changes.some(c=>String(recordValue(c,'TRIGGERING_DEFECT_IDS')||'').includes(recordId(d,'defects'))))});break;}case 18:{const reqs=mandatoryRequirements(project),regs=records(project,'regressions').filter(r=>isActiveRecord(r)&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),completeReqs=reqs.filter(r=>!adjudicationEmpty(recordValue(r,'OBLIGATION'))&&!adjudicationEmpty(recordValue(r,'APPLICABILITY'))&&!adjudicationEmpty(recordValue(r,'OBSERVABLE_SATISFACTION_CONDITION'))),failed=[];if(convergence.requirementCoverage!==1)failed.push('MANDATORY_REQUIREMENT_COVERAGE');if(convergence.verificationCoverage!==1)failed.push('MANDATORY_VERIFICATION_COVERAGE');if(convergence.regressionSuccess!==1)failed.push('REGRESSION_TEST_SUCCESS');if(convergence.criticalDefects)failed.push('CRITICAL_DEFECTS');if(convergence.majorDefects)failed.push('MAJOR_DEFECTS');if(convergence.mandatoryUnresolvedUnknowns)failed.push('MANDATORY_UNRESOLVED_UNKNOWNS');if(convergence.contradictions)failed.push('KNOWN_CORRECTNESS_AFFECTING_CONTRADICTIONS');if(convergence.ambiguities)failed.push('KNOWN_CORRECTNESS_AFFECTING_AMBIGUITIES');if(convergence.unexplainedVariance)failed.push('UNEXPLAINED_CORRECTNESS_AFFECTING_EXECUTION_VARIANCE');Object.assign(derived,{ITERATION_ID:convergence.iterationId||project.job.CURRENT_ITERATION||'NONE',METRICS_VERSION:'METRICS-'+hash.sha256Value(convergence).slice(0,16).toUpperCase(),TOTAL_MANDATORY_REQUIREMENTS:reqs.length,MANDATORY_REQUIREMENTS_WITH_COMPLETE_SPECIFICATION_AND_APPLICABILITY:completeReqs.length,MANDATORY_REQUIREMENT_COVERAGE:convergence.requirementCoverage,MANDATORY_REQUIREMENTS_WITH_AFFIRMATIVE_APPLICABLE_VERIFICATION:Math.round(reqs.length*convergence.verificationCoverage),MANDATORY_VERIFICATION_COVERAGE:convergence.verificationCoverage,TOTAL_STILL_APPLICABLE_REGRESSION_TESTS:regs.length,SUCCESSFUL_REGRESSION_TESTS:Math.round(regs.length*convergence.regressionSuccess),REGRESSION_TEST_SUCCESS:convergence.regressionSuccess,CRITICAL_DEFECTS:convergence.criticalDefects,MAJOR_DEFECTS:convergence.majorDefects,MANDATORY_UNRESOLVED_UNKNOWNS:convergence.mandatoryUnresolvedUnknowns,KNOWN_CORRECTNESS_AFFECTING_CONTRADICTIONS:convergence.contradictions,KNOWN_CORRECTNESS_AFFECTING_AMBIGUITIES:convergence.ambiguities,UNEXPLAINED_CORRECTNESS_AFFECTING_EXECUTION_VARIANCE:convergence.unexplainedVariance,ALL_CONDITIONS_SIMULTANEOUSLY_TRUE:convergence.converged,FAILED_CONDITIONS:failed,STABILITY_SUMMARY:convergence.iterationId?executionStability(project,convergence.iterationId):null});break;}case 20:{const confirmation=recordsForCurrentScope(project,'confirmationRecords').filter(r=>effectiveDetermination('confirmationRecords',r,null,project)==='SATISFIED').at(-1),baseline=recordsForCurrentScope(project,'baselines').at(-1),approved=recordValue(baseline,'APPROVED_VERSIONS')||{},candidateId=String(approved.candidateId||''),candidate=records(project,'candidateFreezes').find(r=>recordId(r,'candidateFreezes')===candidateId),manifest=safe(recordValue(candidate,'COMPONENT_MANIFEST'));Object.assign(derived,{BASELINE_ID:recordId(baseline,'baselines')||'NONE',BASELINE_APPROVAL_DATE:baseline?.createdAt||'NONE',SUPPORTING_CONFIRMATION_ITERATION:String(recordValue(confirmation,'CONFIRMATION_ITERATION_ID')||recordValue(confirmation,'ITERATION_ID')||confirmation?.scope?.iterationId||'NONE'),APPROVED_VERSIONS:approved,BASELINE_FILE_RECORDS:manifest,UNCHANGED_CONFIRMATION_SUCCEEDED:Boolean(confirmation),ALL_APPROVED_COMPONENTS_PRESENT:manifest.length>0&&manifest.every(x=>x.artifactId&&x.filename&&x.sha256),ALL_IMMUTABLE_FILES_HASHED:manifest.length>0&&manifest.every(x=>/^[a-f0-9]{64}$/i.test(String(x.sha256||''))),ANY_CHANGED_COMPONENT_RETAINS_BASELINE_STATUS:false});break;}case 21:{const product=recordsForCurrentScope(project,'products').at(-1),productId=recordId(product,'products'),baselineId=String(recordValue(product,'BASELINE_ID')||product?.relationships?.BASELINE_ID||project.job.CURRENT_BASELINE_ID||''),artifacts=recordsForCurrentScope(project,'artifacts').filter(a=>productId&&String(a.scope?.productId||'')===productId);Object.assign(derived,{PRODUCT_ID:productId||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',BASELINE_ID:baselineId||'NONE',EXECUTION_ID:recordValue(product,'EXECUTION_ID')||'NONE',OUTPUT_FILE_RECORDS:artifacts.map(a=>recordId(a,'artifacts'))});break;}case 22:{const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),tests=recordsForCurrentScope(project,'tests').filter(t=>mandatoryIds.has(testRequirementId(t))&&upper(recordValue(t,'TEST_TYPE'))==='DETERMINISTIC'&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(t,'STATUS')||'READY'))),results=recordsForCurrentScope(project,'deterministicResults'),byTest=new Map();for(const r of results){const id=String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'');if(!byTest.has(id))byTest.set(id,[]);byTest.get(id).push(r);}const determinations=tests.map(t=>{const rows=byTest.get(recordId(t,'tests'))||[];return rows.length===1?effectiveDetermination('deterministicResults',rows[0],t,project):'UNDETERMINED';}),product=recordsForCurrentScope(project,'products').at(-1),productArtifacts=recordsForCurrentScope(project,'artifacts').filter(a=>String(a.scope?.productId||'')===String(currentScope(project).productId||''));Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',PRODUCT_HASHES_BEFORE_TEST:Object.fromEntries(productArtifacts.map(a=>[recordId(a,'artifacts'),recordValue(a,'SHA256')||'UNKNOWN'])),TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NONE',VALIDATOR_VERSION:globalThis.closedLoopTestRuntime?.version||'EXTERNAL',DETERMINISTIC_TEST_RESULTS:results.length,APPLICABLE_MANDATORY_DETERMINISTIC_TESTS:tests.map(t=>recordId(t,'tests')),EXECUTED:determinations.filter(d=>d!=='UNDETERMINED').length,SATISFIED:determinations.filter(d=>d==='SATISFIED').length,VIOLATED:determinations.filter(d=>d==='VIOLATED').length,UNDETERMINED:determinations.filter(d=>d==='UNDETERMINED').length,MISSING_TEST_RESULTS:tests.filter(t=>(byTest.get(recordId(t,'tests'))||[]).length!==1).map(t=>recordId(t,'tests')),PRODUCT_REJECTED_BY_MANDATORY_FAILURE:determinations.includes('VIOLATED')});break;}case 23:{const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),tests=recordsForCurrentScope(project,'tests').filter(t=>mandatoryIds.has(testRequirementId(t))&&upper(recordValue(t,'TEST_TYPE'))==='MEANING'),results=recordsForCurrentScope(project,'meaningResults'),ds=results.map(r=>effectiveDetermination('meaningResults',r,testForResult(project,r),project)),change=acceptedChanges(project,23).at(-1),reviewer=String(change?.scope?.contextId||''),product=recordsForCurrentScope(project,'products').at(-1),producer=String(recordValue(product,'PRODUCTION_CONTEXT_ID')||product?.relationships?.PRODUCTION_CONTEXT_ID||''),ind=evaluateContextIndependence(project,{role:'MEANING_REVIEW',reviewerContextId:reviewer,productionContextId:producer});Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NONE',MEANING_RUBRIC_VERSION:'MEANING-'+hash.sha256Value(tests.map(t=>t.sha256||recordId(t,'tests'))).slice(0,16).toUpperCase(),EVALUATOR_ID:reviewer||'UNKNOWN',EVALUATOR_INDEPENDENT_FROM_GENERATOR:ind.determination,MEANING_REQUIREMENT_RECORDS:results.length,ACTIVE_MEANING_REQUIREMENTS:tests.length,MEANING_RECORDS_COMPLETED:results.length,SATISFIED:ds.filter(d=>d==='SATISFIED').length,VIOLATED:ds.filter(d=>d==='VIOLATED').length,UNDETERMINED:ds.filter(d=>d==='UNDETERMINED').length,UNSUPPORTED_BARE_CONCLUSIONS:results.filter(r=>!evaluateEvidenceSufficiency(project,{test:testForResult(project,r),result:r}).sufficient).length});break;}case 24:{const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),tests=recordsForCurrentScope(project,'tests').filter(t=>mandatoryIds.has(testRequirementId(t))&&upper(recordValue(t,'TEST_TYPE'))==='ADVERSARIAL'),regs=records(project,'regressions').filter(r=>isActiveRecord(r)&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),results=recordsForCurrentScope(project,'adversarialResults'),ds=results.map(r=>effectiveDetermination('adversarialResults',r,testForResult(project,r),project)),change=acceptedChanges(project,24).at(-1),reviewer=String(change?.scope?.contextId||''),product=recordsForCurrentScope(project,'products').at(-1),producer=String(recordValue(product,'PRODUCTION_CONTEXT_ID')||product?.relationships?.PRODUCTION_CONTEXT_ID||''),ind=evaluateContextIndependence(project,{role:'ADVERSARIAL_REVIEW',reviewerContextId:reviewer,productionContextId:producer}),linkedDefects=results.map(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')).filter(Boolean),defectRows=records(project,'defects').filter(d=>linkedDefects.includes(recordId(d,'defects')));Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',ADVERSARIAL_REVIEW_VERSION:'ADVERSARIAL-'+hash.sha256Value(results.map(r=>r.sha256||recordId(r,'adversarialResults'))).slice(0,16).toUpperCase(),REVIEWER_ID:reviewer||'UNKNOWN',REVIEWER_INDEPENDENT:ind.determination,ADVERSARIAL_CHECK_RECORDS:results.length,ATTACKS_EXECUTED:results.length,MANDATORY_DEFECTS_FOUND:ds.filter(d=>d==='VIOLATED').length,CRITICAL_DEFECTS_FOUND:defectRows.filter(d=>upper(recordValue(d,'SEVERITY'))==='CRITICAL').length,MAJOR_DEFECTS_FOUND:defectRows.filter(d=>upper(recordValue(d,'SEVERITY'))==='MAJOR').length,UNDETERMINED_ATTACKS:ds.filter(d=>d==='UNDETERMINED').length,REGRESSIONS_FOUND:results.filter(r=>String(recordValue(r,'REG_ID')||r.relationships?.REG_ID||'')).length,RETURN_TO_ROOT_CAUSE_REQUIRED:ds.some(d=>d!=='SATISFIED')});break;}case 25:{const coverage=representationInspectionCoverage(project),baseline=recordsForCurrentScope(project,'baselines').at(-1),product=recordsForCurrentScope(project,'products').at(-1),defects=unresolvedMaterialDefects(project);Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||project.job.CURRENT_PRODUCT_ID||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',REPRESENTATION_REVIEW_VERSION:'REPRESENTATION-'+hash.sha256Value(coverage.details).slice(0,16).toUpperCase(),APPROVED_BASELINE_ID:recordId(baseline,'baselines')||project.job.CURRENT_BASELINE_ID||'NONE',DELIVERY_ARTIFACT_INVENTORY:coverage.details.map(x=>x.artifactId),TRANSFORMATION_CHAIN_RECORDS:coverage.totalTransformationsInspected,PAGE_OR_VIEW_INSPECTION_RECORDS:coverage.totalPagesOrViewsInspected,PACKAGE_INSPECTION_RECORDS:coverage.totalPackagedFilesOpenedOrTested,TOTAL_DELIVERY_ARTIFACTS:coverage.totalDeliveryArtifacts,TOTAL_PAGES_OR_VIEWS_REQUIRED:coverage.totalPagesOrViewsRequired,TOTAL_PAGES_OR_VIEWS_INSPECTED:coverage.totalPagesOrViewsInspected,TOTAL_PACKAGED_FILES_REQUIRED:coverage.totalPackagedFilesRequired,TOTAL_PACKAGED_FILES_OPENED_OR_TESTED:coverage.totalPackagedFilesOpenedOrTested,UNRESOLVED_CRITICAL_DEFECTS:defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='CRITICAL').length,UNRESOLVED_MAJOR_DEFECTS:defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='MAJOR').length,UNRESOLVED_REPRESENTATION_UNKNOWNS:coverage.reasons.length,FINAL_REPRESENTATION_DETERMINATION:coverage.complete?'SATISFIED':'UNDETERMINED'});break;}case 26:{const process=recordsForCurrentScope(project,'processAudits'),productAudits=recordsForCurrentScope(project,'productAudits'),product=recordsForCurrentScope(project,'products').at(-1),baseline=recordsForCurrentScope(project,'baselines').at(-1),allAudits=[...process,...productAudits],missing=allAudits.filter(a=>!evaluateEvidenceSufficiency(project,{result:a}).sufficient).map(a=>recordId(a,process.includes(a)?'processAudits':'productAudits')),defects=[...new Set(allAudits.map(a=>String(recordValue(a,'DEFECT_ID')||a.relationships?.DEFECT_ID||'')).filter(Boolean))],blockers=openBlockers(project,26).map(b=>recordId(b,'blockers'));Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',BASELINE_ID:recordId(baseline,'baselines')||project.job.CURRENT_BASELINE_ID||'NONE',REVIEW_VERSION:'RECONCILIATION-'+hash.sha256Value(allAudits.map(a=>a.recordSha256||a.sha256||a.id)).slice(0,16).toUpperCase(),MISSING_EVIDENCE_LINKS:missing,RECONCILIATION_DEFECT_IDS:defects,RECONCILIATION_BLOCKER_IDS:blockers});break;}case 27:{const rr=recordsForCurrentScope(project,'releaseRecords').at(-1),allResults=[...recordsForCurrentScope(project,'deterministicResults'),...recordsForCurrentScope(project,'meaningResults'),...recordsForCurrentScope(project,'adversarialResults')];Object.assign(derived,{RELEASE_GATE_ID:recordId(rr,'releaseRecords')||'PENDING',DATE_AND_TIME:rr?.createdAt||now(),PRODUCT_ID:release.productId||'NONE',PRODUCT_VERSION:recordValue(recordsForCurrentScope(project,'products').at(-1),'PRODUCT_VERSION')||'NONE',BASELINE_ID:release.baselineId||'NONE',RECONCILED_REVIEW_VERSION:'RECONCILED-'+hash.sha256Value([...recordsForCurrentScope(project,'processAudits'),...recordsForCurrentScope(project,'productAudits')].map(r=>r.sha256||r.recordSha256||r.id)).slice(0,16).toUpperCase(),TOTAL_MANDATORY_REQUIREMENTS:release.mandatoryRequirementCount,MANDATORY_REQUIREMENTS_WITH_AFFIRMATIVE_SUPPORTING_EVIDENCE:release.satisfied,MANDATORY_REQUIREMENTS_DEMONSTRABLY_VIOLATED:release.violated,MANDATORY_REQUIREMENTS_NOT_ESTABLISHED:release.undetermined,TOTAL_MANDATORY_VALIDATORS:release.validatorCount,MANDATORY_VALIDATORS_SUCCEEDED:release.validatorCount-release.failedValidatorIds.length-release.unknownValidatorIds.length,MANDATORY_VALIDATORS_FAILED:release.failedValidatorIds.length,MANDATORY_VALIDATORS_UNDETERMINED_OR_NOT_RUN:release.unknownValidatorIds.length,UNRESOLVED_CRITICAL_DEFECTS:release.criticalDefects,UNRESOLVED_MAJOR_DEFECTS:release.majorDefects,BLOCKING_REQUIREMENT_IDS:release.blockingRequirements,VIOLATED_REQUIREMENT_IDS:release.violatedRequirements,FAILED_TEST_IDS:release.failedValidatorIds,UNDETERMINED_OR_MISSING_TEST_IDS:release.unknownValidatorIds,UNRESOLVED_DEFECT_IDS:unresolvedMaterialDefects(project).map(d=>recordId(d,'defects')),BLOCKER_IDS:release.blockerIds,SELECTED_RELEASE_STATE:release.determination,CONTROLLING_DECISION_RULE:release.determination==='REJECTED'?'AFFIRMATIVE_CURRENT_NONCONFORMANCE_REJECTS':release.determination==='BLOCKED'?'INSUFFICIENT_OR_CONTRADICTORY_CURRENT_EVIDENCE_BLOCKS':'ALL_CURRENT_RELEASE_CONDITIONS_SATISFIED',CONTROLLING_REASON:release.determination==='ACCEPTED'?'Every mandatory requirement is currently established with sufficient noncontradictory evidence and release-critical identity conditions are satisfied.':release.determination==='REJECTED'?'Current evidence establishes mandatory nonconformance.':'Current evidence is incomplete, insufficient, stale, blocked, or contradictory.',AFFIRMATIVE_EVIDENCE:release.inputReferences});break;}case 28:Object.assign(derived,DERIVATIONS['stage28.artifactIdentity'](project).value);break;case 29:Object.assign(derived,DERIVATIONS['stage29.evidenceChains'](project).value);break;case 30:{const defects=records(project,'defects',{active:false}),confirmed=confirmedDefects(project),regs=records(project,'regressions',{active:false}),activeRegs=regs.filter(r=>isActiveRecord(r)&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),retiredRegs=regs.filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE'))==='RETIRED'),execs=records(project,'regressionExecutions',{active:false}),missingFields=defects.filter(d=>['OBSERVED_FAILURE','EXPECTED_CONDITION','SEVERITY','STATUS'].some(k=>adjudicationEmpty(recordValue(d,k)))).map(d=>recordId(d,'defects')),covered=new Set(regs.map(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||''))),missingRegs=confirmed.filter(d=>!covered.has(recordId(d,'defects'))).map(d=>recordId(d,'defects'));Object.assign(derived,{DEFECT_REGISTRY_VERSION:'DEFECT-REGISTRY-'+hash.sha256Value(defects.map(d=>d.recordSha256||d.sha256||recordId(d,'defects'))).slice(0,16).toUpperCase(),REGRESSION_REGISTRY_VERSION:'REGRESSION-REGISTRY-'+hash.sha256Value(regs.map(r=>r.recordSha256||r.sha256||recordId(r,'regressions'))).slice(0,16).toUpperCase(),REGISTRY_IS_APPEND_ONLY:true,DEFECT_RECORDS:defects.length,REGRESSION_RECORDS:regs.length,FUTURE_BASELINE_REGRESSION_EXECUTION_RECORDS:execs.length,TOTAL_DEFECT_RECORDS:defects.length,TOTAL_CONFIRMED_DEFECTS_WITH_REGRESSION_TESTS:confirmed.length-missingRegs.length,TOTAL_ACTIVE_REGRESSION_TESTS:activeRegs.length,TOTAL_RETIRED_REGRESSION_TESTS:retiredRegs.length,DEFECT_RECORDS_MISSING_REQUIRED_FIELDS:missingFields,CONFIRMED_DEFECTS_MISSING_REGRESSION_TESTS:missingRegs,FINAL_REGISTRY_DETERMINATION:missingFields.length||missingRegs.length?'UNDETERMINED':'SATISFIED'});break;}}derived.STAGE_DECISION=project.stages[stage].status==='COMPLETE'?'READY TO PROCEED':project.stages[stage].status==='BLOCKED'?'BLOCKED':'NOT READY - CORRECTION REQUIRED';derived.DECISION_EVIDENCE=project.stages[stage].gate?.reasons?.length?project.stages[stage].gate.reasons.join('; '):'Canonical current-scope records and deterministic calculations satisfy the stage gate.';return derived;}
function recalculate(project){
  ensureShape(project);for(let stage=1;stage<=30;stage++){const g=gate(project,stage);project.stages[stage].gate=g;project.stages[stage].derivedData=deriveStageData(project,stage);if(g.complete)project.stages[stage].status='COMPLETE';else if(g.blocked)project.stages[stage].status='BLOCKED';else if(hasStageActivity(project,stage))project.stages[stage].status='IN PROGRESS';else project.stages[stage].status='NOT STARTED';project.stages[stage].acceptedData={...(project.stages[stage].agentData||{}),...(project.stages[stage].humanData||{}),...(project.stages[stage].derivedData||{})};}
  const currentStage=Object.values(project.stages).find(s=>s.status!=='COMPLETE')?.number||30;project.job.CURRENT_STAGE=String(currentStage);project.job.CURRENT_STATE=project.stages[currentStage].status;project.job.CURRENT_BLOCKERS=openBlockers(project).map(b=>recordId(b,'blockers')).join(', ')||'NONE';project.job.LATEST_EVIDENCE_REFERENCE=records(project,'evidenceRecords').at(-1)?recordId(records(project,'evidenceRecords').at(-1),'evidenceRecords'):'NONE';project.job.NEXT_REQUIRED_ACTION=operationalNextAction(project);project.activeStage=currentStage;return project;
}

function invalidateDownstream(project,stage,reason,options={}){
  ensureShape(project);const event=addHistory(project,'DOWNSTREAM_INVALIDATED',{stage,reason});const from=Number(stage)+1;
  for(let n=from;n<=30;n++){const target=project.stages[n];target.invalidatedBy=event.eventId;target.status='NOT STARTED';target.decision='';target.decisionEvidence='';target.acceptedData={};target.agentData={};target.humanData={};target.derivedData={};target.acceptedDataChangeIds=[];target.acceptedControlEventIds=[];target.acceptedResponseIds=[];target.gate={complete:false,blocked:false,reasons:['Invalidated by upstream change.']};target.currentPromptId=null;}
  const stageScopedVersionFields={2:['sourceSetVersion'],3:['sourceSetVersion'],4:['requirementsVersion'],5:['requirementsVersion'],6:['testSuiteVersion'],7:['testSuiteVersion'],8:['instructionVersion'],9:['instructionVersion'],10:['iterationId','candidateId'],11:['iterationId','candidateId','runId','contextId'],12:['iterationId','candidateId','runId','contextId'],13:['iterationId','candidateId'],14:['iterationId','candidateId','runId','productId'],15:['iterationId','candidateId','productId'],16:['iterationId','candidateId'],17:['iterationId','candidateId','runId','contextId'],18:['iterationId','candidateId'],19:['iterationId','candidateId','runId','contextId'],20:['baselineId'],21:['baselineId','productId'],22:['productId','testSuiteVersion'],23:['productId','requirementsVersion','testSuiteVersion','contextId'],24:['productId','requirementsVersion','testSuiteVersion','contextId'],25:['productId'],26:['productId','baselineId'],27:['productId','baselineId'],28:['productId','baselineId'],29:['productId','baselineId'],30:[]};
  for(const [collection,definition] of Object.entries(schema.RECORD_SCHEMAS)){const recordStage=definition.stage||0;if(recordStage>=from||definition.global){for(const record of records(project,collection,{active:false})){if(record.invalidatedBy||record.active===false)continue;if(recordStage>=from){record.active=false;record.validity='INVALIDATED';record.invalidatedBy=event.eventId;record.updatedAt=now();refreshRecordHashes(record,collection);continue;}const fields=stageScopedVersionFields[stage]||[];if(fields.some(field=>record.scope?.[field])){record.active=false;record.validity='INVALIDATED';record.invalidatedBy=event.eventId;record.updatedAt=now();refreshRecordHashes(record,collection);}}}}
  const invalidateInfra=['generatedPrompts','generatedOutputs','responseProposals','responseValidations','humanInputRequests','stageConfirmations','outputReceipts','responseDispositions','executionFailures'];for(const collection of invalidateInfra)for(const item of safe(project.projectData[collection])){if(Number(item.stage)>=from&&!item.invalidatedBy){item.invalidatedBy=event.eventId;if(item.status)item.status='INVALIDATED';}}
  for(const item of safe(project.projectData.acceptedChanges))if(Number(item.stage)>=from&&!item.invalidatedBy){item.invalidatedBy=event.eventId;item.status='INVALIDATED';}
  project.release={gateState:'',auditedDraft:[],releaseDraft:[],comparisons:[],authorization:'NOT AUTHORIZED',authorizedArtifactIds:[],invalidatedBy:event.eventId};
  if(stage<=19)project.job.CURRENT_BASELINE_ID='';if(stage<=20)project.job.CURRENT_PRODUCT_ID='';if(stage<=9)project.job.CURRENT_ITERATION='';if(stage<=1){project.job.CURRENT_SOURCE_SET_VERSION='';project.job.CURRENT_RESEARCH_VERSION='';project.job.CURRENT_REQUIREMENTS_VERSION='';project.job.CURRENT_TEST_SUITE_VERSION='';project.job.CURRENT_INSTRUCTION_VERSION='';}else if(stage<=3){project.job.CURRENT_REQUIREMENTS_VERSION='';project.job.CURRENT_TEST_SUITE_VERSION='';project.job.CURRENT_INSTRUCTION_VERSION='';}else if(stage<=5){project.job.CURRENT_TEST_SUITE_VERSION='';project.job.CURRENT_INSTRUCTION_VERSION='';}else if(stage<=7)project.job.CURRENT_INSTRUCTION_VERSION='';
  if(!options.skipRecalculate)recalculate(project);return event;
}

function preserveRawResponse(project,{stage,operation,promptId,promptIdentity,scope,rawText,rawSha256}){ensureShape(project);const rawResponseId=allocateInfrastructureId(project,'RAW','rawResponses'),record={rawResponseId,stage,operation,promptId,promptIdentity:clone(promptIdentity),scope:clone(scope||{}),rawText:String(rawText??''),rawSha256:String(rawSha256||hash.sha256Text(String(rawText??''))),capturedAt:now(),status:'CAPTURED'};project.projectData.rawResponses.push(record);addHistory(project,'RAW_RESPONSE_CAPTURED',{stage,operation,rawResponseId,promptId});return record;}
function recordRawCaptureFailure(project,{stage,operation,promptId,scope,error,rawText}){ensureShape(project);const failure={failureId:allocateInfrastructureId(project,'RAWFAIL','executionFailures'),stage,operation,promptId,scope:clone(scope||{}),error:String(error?.message||error||'Raw capture failed.'),rawTextPreserved:String(rawText??''),createdAt:now(),canonicalStateChanged:false,status:'RAW_CAPTURE_FAILED'};project.projectData.executionFailures.push(failure);addHistory(project,'RAW_CAPTURE_FAILED',{stage,operation,failureId:failure.failureId,promptId});return failure;}
function recordValidation(project,details){ensureShape(project);const validationId=allocateInfrastructureId(project,'VALIDATION','responseValidations'),record={validationId,createdAt:now(),...clone(details)};project.projectData.responseValidations.push(record);addHistory(project,'RESPONSE_VALIDATED',{stage:record.stage,operation:record.operation,validationId,valid:Boolean(record.valid),rawResponseId:record.rawResponseId});return record;}
function createResponseProposal(project,details){ensureShape(project);const proposalId=allocateInfrastructureId(project,'PROPOSAL','responseProposals'),record={proposalId,createdAt:now(),status:'PENDING',...clone(details)};project.projectData.responseProposals.push(record);addHistory(project,'PROPOSAL_CREATED',{stage:record.stage,operation:record.operation,proposalId,rawResponseId:record.rawResponseId});return record;}
function recordAcceptedChange(project,details){ensureShape(project);const changeId=allocateInfrastructureId(project,'CHANGE','acceptedChanges'),eventSequence=project.projectData.eventSequence+1,record={changeId,createdAt:now(),eventSequence,status:'COMMITTED',...clone(details)};project.projectData.acceptedChanges.push(record);addHistory(project,'DATA_PROPOSAL_ACCEPTED',{stage:record.stage,operation:record.operation,changeId,proposalId:record.proposalId,rawResponseId:record.rawResponseId});return record;}
function recordDisposition(project,details){ensureShape(project);const id=allocateInfrastructureId(project,'DISPOSITION','responseDispositions'),record={id,createdAt:now(),...clone(details)};project.projectData.responseDispositions.push(record);addHistory(project,record.type||'RESPONSE_DISPOSITION',{stage:record.stage,operation:record.operation,dispositionId:id,rawResponseId:record.rawResponseId});return record;}
function recordRejectedResponse(project,details){ensureShape(project);const rejectedResponseId=allocateInfrastructureId(project,'REJECTED','rejectedResponses'),record={rejectedResponseId,createdAt:now(),...clone(details)};project.projectData.rejectedResponses.push(record);addHistory(project,'RESPONSE_REJECTED',{stage:record.stage,operation:record.operation,rejectedResponseId,rawResponseId:record.rawResponseId,requestCorrection:Boolean(record.requestCorrection)});return record;}

function extractionValueLookup(envelope){const map=new Map();for(const [key,value] of Object.entries(envelope?.stageData||{}))map.set(`/stageData/${key}`,value);for(const [collection,list] of Object.entries(envelope?.records||{}))safe(list).forEach((record,index)=>{for(const [key,value] of Object.entries(record?.fields||{}))map.set(`/records/${collection}/${index}/fields/${key}`,value);for(const [key,value] of Object.entries(record?.relationships||{}))map.set(`/records/${collection}/${index}/relationships/${key}`,value);});return map;}
function buildExtractionManifest(project,{stage,operation,promptRecord,rawResponse,envelope,canonicalMapping,evidenceMapping,validationRules=[],validationId,proposalId,changeId}){ensureShape(project);const values=extractionValueLookup(envelope),entries=[];for(const mapping of safe(canonicalMapping)){const value=values.get(mapping.jsonPointer),normalized=mapping.normalizedValue??value;entries.push({origin:'AGENT_RESPONSE',rawResponseId:rawResponse.rawResponseId,rawResponseSha256:rawResponse.rawSha256,jsonPointer:mapping.jsonPointer,rawValueSha256:hash.sha256Value(value),normalizer:mapping.normalizer||'IDENTITY',normalizedValue:clone(normalized),temporaryKey:mapping.temporaryKey||null,canonicalCollection:mapping.canonicalCollection||null,canonicalRecordId:mapping.canonicalRecordId||null,canonicalField:mapping.canonicalField||null,canonicalRelationship:mapping.canonicalRelationship||null,relationshipTarget:mapping.relationshipTarget||null,evidenceIds:safe(evidenceMapping?.[mapping.jsonPointer]),validationRules:clone(validationRules)});}for(const mapping of safe(canonicalMapping).filter(m=>m.applicationAllocated)){entries.push({origin:'APPLICATION_ALLOCATION',canonicalCollection:mapping.canonicalCollection,canonicalRecordId:mapping.canonicalRecordId,description:'Canonical identity allocated by application; no agent response pointer exists.'});}const manifestId=allocateInfrastructureId(project,'EXTRACTION','extractionManifests'),manifest={manifestId,stage,operation,promptIdentity:clone(promptRecord.promptIdentity||{instructionId:promptRecord.instructionId,bodySha256:promptRecord.bodySha256,contractSha256:promptRecord.contractSha256,contextSignature:promptRecord.contextSignature}),rawResponseId:rawResponse.rawResponseId,rawResponseSha256:rawResponse.rawSha256,validationId,proposalId,changeId,projectRevision:project.revision,eventSequence:project.projectData.eventSequence+1,createdAt:now(),entries};project.projectData.extractionManifests.push(manifest);return manifest;}

function acceptAgentDataChange(project,{stage,operation,promptRecord,rawResponse,envelope,proposal,canonicalApplyResult,validationId}){
  ensureShape(project);const change=recordAcceptedChange(project,{stage,operation,promptId:promptRecord.instructionId,promptIdentity:{instructionId:promptRecord.instructionId,bodySha256:promptRecord.bodySha256,contractSha256:promptRecord.contractSha256,contextSignature:promptRecord.contextSignature},scope:clone(promptRecord.scope),rawResponseId:rawResponse.rawResponseId,proposalId:proposal.proposalId,responseType:'DATA_PROPOSAL',canonicalRecordIds:clone(canonicalApplyResult.canonicalRecordIds),canonicalMapping:clone(canonicalApplyResult.canonicalMapping),projectRevisionBefore:project.revision});project.stages[stage].agentData={...(project.stages[stage].agentData||{}),...clone(envelope.stageData||{})};project.stages[stage].acceptedDataChangeIds.push(change.changeId);project.stages[stage].acceptedResponseIds.push(rawResponse.rawResponseId);const extraction=buildExtractionManifest(project,{stage,operation,promptRecord,rawResponse,envelope,canonicalMapping:canonicalApplyResult.canonicalMapping,evidenceMapping:canonicalApplyResult.evidenceMapping,validationRules:canonicalApplyResult.validationRules,validationId,proposalId:proposal.proposalId,changeId:change.changeId});registerStageVersion(project,stage,change.changeId);invalidateDownstream(project,stage,'Accepted current-stage data changed.',{skipRecalculate:true});project.stages[stage].invalidatedBy=null;project.stages[stage].status='IN PROGRESS';project.stages[stage].agentData={...(project.stages[stage].agentData||{}),...clone(envelope.stageData||{})};project.stages[stage].acceptedDataChangeIds=[change.changeId];project.stages[stage].acceptedResponseIds=[rawResponse.rawResponseId];project.stages[stage].currentPromptId=promptRecord.instructionId;recalculate(project);return {change,extraction};}
function acceptControlEvent(project,{stage,operation,promptRecord,rawResponse,envelope,validationId,type}){ensureShape(project);const disposition=recordDisposition(project,{type,stage,operation,promptId:promptRecord.instructionId,scope:clone(promptRecord.scope),rawResponseId:rawResponse.rawResponseId,validationId,responseType:envelope.responseType,humanInputRequests:clone(envelope.humanInputRequests||[]),unresolved:clone(envelope.unresolved||[]),warnings:clone(envelope.warnings||[])});project.stages[stage].acceptedControlEventIds.push(disposition.id);project.stages[stage].acceptedResponseIds.push(rawResponse.rawResponseId);project.stages[stage].currentPromptId=promptRecord.instructionId;if(envelope.responseType==='HUMAN_INPUT_REQUIRED')for(const request of safe(envelope.humanInputRequests))project.projectData.humanInputRequests.push({...clone(request),requestId:request.requestId||allocateInfrastructureId(project,'HUMANREQ','humanInputRequests'),stage,operation,promptId:promptRecord.instructionId,status:'OPEN',createdAt:now()});recalculate(project);return disposition;}

function setHumanInput(project,field,value,{stage=1,source='USER_JOB_INPUT',reason='Human authority update'}={}){ensureShape(project);const definition=schema.JOB_FIELDS[field];if(!definition||!['HUMAN','HUMAN_DECISION'].includes(definition.producer))throw new Error(`Field ${field} is not human-owned.`);const previous=project.job[field];if(hash.stableStringify(previous)===hash.stableStringify(value))return {changed:false,previous,value};project.job[field]=clone(value);project.job.CURRENT_INPUT_VERSION=nextVersion(project.job.CURRENT_INPUT_VERSION,'INPUT');const manifest=intakeCoverageManifest(project);project.job.INPUT_SET_HASH_OR_MANIFEST=manifest.manifestSha256;const record={inputVersionId:allocateInfrastructureId(project,'INPUT','inputVersions'),version:project.job.CURRENT_INPUT_VERSION,field,previous:clone(previous),value:clone(value),source,reason,createdAt:now(),manifestSha256:manifest.manifestSha256};project.projectData.inputVersions.push(record);addHistory(project,'HUMAN_INPUT_VERSION_CREATED',{field,stage,inputVersion:record.version,inputVersionId:record.inputVersionId,reason});if(stage<=30)invalidateDownstream(project,Math.max(0,stage-1),reason,{skipRecalculate:true});project.stages[stage].invalidatedBy=null;recalculate(project);return {changed:true,previous,value,inputVersion:record.version,manifestSha256:manifest.manifestSha256};}
function setHumanDecision(project,{stage,field,value,reason='Human decision update'}={}){ensureShape(project);if(stage===1&&schema.JOB_FIELDS[field]?.producer==='HUMAN_DECISION')return setHumanInput(project,field,value,{stage,source:'HUMAN_DECISION',reason});const record={decisionId:allocateInfrastructureId(project,'HUMANDECISION','reviews'),stage,field,value:clone(value),reason,createdAt:now()};project.projectData.reviews.push(record);project.stages[stage].humanData[field]=clone(value);addHistory(project,'HUMAN_DECISION_RECORDED',{stage,field,decisionId:record.decisionId});invalidateDownstream(project,stage,reason,{skipRecalculate:true});project.stages[stage].invalidatedBy=null;recalculate(project);return record;}
function answerHumanInputRequest(project,{requestId,value}){ensureShape(project);const request=safe(project.projectData.humanInputRequests).find(r=>r.requestId===requestId&&r.status==='OPEN');if(!request)throw new Error('Open human-input request not found.');request.status='ANSWERED';request.answeredAt=now();request.answer=clone(value);const answer={answerId:allocateInfrastructureId(project,'HUMANANSWER','humanInputAnswers'),requestId,stage:request.stage,operation:request.operation,promptId:request.promptId,value:clone(value),createdAt:now()};project.projectData.humanInputAnswers.push(answer);project.job.CURRENT_INPUT_VERSION=nextVersion(project.job.CURRENT_INPUT_VERSION,'INPUT');const manifest=intakeCoverageManifest(project);project.job.INPUT_SET_HASH_OR_MANIFEST=manifest.manifestSha256;addHistory(project,'HUMAN_INPUT_ANSWERED',{requestId,answerId:answer.answerId,inputVersion:project.job.CURRENT_INPUT_VERSION});invalidateDownstream(project,Math.max(0,Number(request.stage)-1),'Human answer changed controlled input.',{skipRecalculate:true});project.stages[request.stage].invalidatedBy=null;recalculate(project);return answer;}
function confirmStage(project,{stage,confirmation='CONFIRMED',operatorLabel='HUMAN_OPERATOR',identityAssurance='SELF_ASSERTED'}={}){ensureShape(project);const allowed=stage===1||stage===20||stage===28;if(!allowed)throw new Error('Human confirmation is permitted only at human-decision stages.');const record={confirmationId:allocateInfrastructureId(project,'CONFIRM','stageConfirmations'),stage,confirmation,operatorLabel,identityAssurance,inputVersion:project.job.CURRENT_INPUT_VERSION,acceptedChangeId:acceptedChanges(project,stage).at(-1)?.changeId||null,createdAt:now(),confirmed:true};project.projectData.stageConfirmations.push(record);project.stages[stage].humanData.HUMAN_CONFIRMATION=confirmation;addHistory(project,'HUMAN_STAGE_CONFIRMED',{stage,confirmationId:record.confirmationId});recalculate(project);return record;}

function reserveRunBatch(project,{stage=11,iterationId,candidateId,operation='EXECUTE_RUN'}={}){ensureShape(project);if(![11,17,19].includes(stage))throw new Error('Run batches are supported only for Stages 11, 17, and 19.');const iid=String(iterationId||project.job.CURRENT_ITERATION||''),cid=String(candidateId||iterationCandidateId(project,iid)||'');if(!iid||!cid)throw new Error('Run batch requires current iteration and candidate identities.');const existing=recordsForIteration(project,'runs',iid).filter(r=>Number(r.stage)===stage);if(existing.length===10)return existing;if(existing.length)throw new Error('A partial current run batch exists; correct it before reserving another.');const created=[];for(let i=1;i<=10;i++){const runId=allocateId(project,'runs'),contextId=allocateId(project,'freshContexts'),scope={...scopeForIteration(project,iid),iterationId:iid,candidateId:cid,runId,contextId};const context=commandRecord(project,'freshContexts',{ITERATION_ID:iid,RUN_ID:runId,CONTEXT_ROLE:'GENERATOR',CONTAMINATION_STATUS:'UNKNOWN'},{id:contextId,stage,source:'APPLICATION',scope,relationships:{ITERATION_ID:iid,RUN_ID:runId}});const run=commandRecord(project,'runs',{ITERATION_ID:iid,CANDIDATE_ID:cid,CONTEXT_ID:contextId,EXECUTION_STATUS:'RESERVED',OUTPUT_HASHES:{}},{id:runId,stage,source:'APPLICATION',scope,relationships:{ITERATION_ID:iid,CANDIDATE_ID:cid,CONTEXT_ID:contextId}});run.operation=operation;context.operation=operation;created.push(run);}addHistory(project,'RUN_BATCH_RESERVED',{stage,iterationId:iid,candidateId:cid,runIds:created.map(r=>recordId(r,'runs'))});recalculate(project);return created;}
function registerFreshContext(project,{stage,contextId,externalContextId,runId,authorizedInputs,contaminationStatus='NO'}={}){ensureShape(project);const id=String(contextId||'');const context=records(project,'freshContexts').find(r=>recordId(r,'freshContexts')===id);if(!context)throw new Error('Application-reserved context slot not found.');const rid=String(runId||recordValue(context,'RUN_ID')||context.relationships?.RUN_ID||'');if(rid&&String(recordValue(context,'RUN_ID')||context.relationships?.RUN_ID||'')!==rid)throw new Error('Context/run identity mismatch.');context.fields={...recordFields(context),EXTERNAL_CONTEXT_ID:String(externalContextId||''),AUTHORIZED_INPUTS:clone(authorizedInputs||''),CONTAMINATION_STATUS:String(contaminationStatus||'NO').toUpperCase()};context.updatedAt=now();refreshRecordHashes(context,'freshContexts');addHistory(project,'FRESH_CONTEXT_REGISTERED',{stage:Number(stage||context.stage),contextId:id,runId:rid,externalContextId:String(externalContextId||'')});recalculate(project);return context;}
function reserveIterationCandidate(project,{stage=10,previousIterationId=null,changesetId=null,componentArtifactIds=[],toolConfiguration={}}={}){ensureShape(project);const artifacts=recordsForCurrentScope(project,'artifacts'),selected=[...new Set(componentArtifactIds.map(String))].map(id=>artifacts.find(a=>recordId(a,'artifacts')===id)).filter(Boolean);if(!selected.length)throw new Error('Candidate freeze requires at least one current verified artifact.');for(const artifact of selected)if(!artifactVerified(artifact))throw new Error(`Candidate component ${recordId(artifact,'artifacts')} is not application-verified bytes.`);const iterationId=allocateId(project,'iterations'),candidateId=allocateId(project,'candidateFreezes'),scope={...currentScope(project),iterationId,candidateId};const versions={inputVersion:project.job.CURRENT_INPUT_VERSION||null,sourceSetVersion:project.job.CURRENT_SOURCE_SET_VERSION||null,requirementsVersion:project.job.CURRENT_REQUIREMENTS_VERSION||null,testSuiteVersion:project.job.CURRENT_TEST_SUITE_VERSION||null,instructionVersion:project.job.CURRENT_INSTRUCTION_VERSION||null};const manifest=selected.map(a=>({artifactId:recordId(a,'artifacts'),filename:recordValue(a,'FILENAME'),byteSize:Number(recordValue(a,'BYTE_SIZE')),sha256:recordValue(a,'SHA256'),version:recordValue(a,'VERSION')||null,role:recordValue(a,'ROLE')||null})),componentHashes=Object.fromEntries(manifest.map(x=>[x.artifactId,x.sha256]));const iteration=commandRecord(project,'iterations',{CANDIDATE_ID:candidateId,PREVIOUS_ITERATION_ID:previousIterationId||'',CHANGESET_ID:changesetId||'',FROZEN_VERSIONS:versions,ITERATION_KIND:stage===19?'UNCHANGED_CONFIRMATION':stage===17?'CORRECTED':'INITIAL',STATUS:'FROZEN'},{id:iterationId,stage,source:'APPLICATION',scope,relationships:{CANDIDATE_ID:candidateId,...(previousIterationId?{PREVIOUS_ITERATION_ID:previousIterationId}:{}),...(changesetId?{CHANGESET_ID:changesetId}:{})}});const candidate=commandRecord(project,'candidateFreezes',{ITERATION_ID:iterationId,COMPONENT_MANIFEST:manifest,COMPONENT_HASHES:componentHashes,TOOL_CONFIGURATION:clone(toolConfiguration),STATUS:'FROZEN'},{id:candidateId,stage,source:'APPLICATION',scope,relationships:{ITERATION_ID:iterationId}});project.job.CURRENT_ITERATION=iterationId;addHistory(project,'CANDIDATE_FROZEN',{stage,iterationId,candidateId,artifactIds:manifest.map(x=>x.artifactId)});recalculate(project);return {iteration,candidate};}
function beginUnchangedConfirmation(project){ensureShape(project);const source=latestIteration(project,[17]);if(!source)throw new Error('A converged Stage 17 iteration is required before unchanged confirmation.');const sourceIterationId=recordId(source,'iterations'),sourceCandidateId=iterationCandidateId(project,sourceIterationId),sourceCandidate=records(project,'candidateFreezes').find(r=>recordId(r,'candidateFreezes')===sourceCandidateId);if(!sourceCandidate)throw new Error('Converged source candidate is missing.');const iterationId=allocateId(project,'iterations'),scope={...scopeForIteration(project,sourceIterationId),iterationId,candidateId:sourceCandidateId};const iteration=commandRecord(project,'iterations',{CANDIDATE_ID:sourceCandidateId,PREVIOUS_ITERATION_ID:sourceIterationId,FROZEN_VERSIONS:recordValue(source,'FROZEN_VERSIONS')||{},ITERATION_KIND:'UNCHANGED_CONFIRMATION',ZERO_CHANGE_SOURCE_IDENTITY:candidateComponentIdentity(project,sourceCandidateId),STATUS:'FROZEN'},{id:iterationId,stage:19,source:'APPLICATION',scope,relationships:{CANDIDATE_ID:sourceCandidateId,PREVIOUS_ITERATION_ID:sourceIterationId}});const confirmation=commandRecord(project,'confirmationRecords',{SOURCE_ITERATION_ID:sourceIterationId,CONFIRMATION_ITERATION_ID:iterationId,ZERO_CHANGE_IDENTITY_SHA256:candidateComponentIdentity(project,sourceCandidateId),STATUS:'RESERVED'},{stage:19,source:'APPLICATION',scope,relationships:{SOURCE_ITERATION_ID:sourceIterationId,CONFIRMATION_ITERATION_ID:iterationId}});project.job.CURRENT_ITERATION=iterationId;addHistory(project,'UNCHANGED_CONFIRMATION_RESERVED',{stage:19,sourceIterationId,confirmationIterationId:iterationId,candidateId:sourceCandidateId,confirmationId:recordId(confirmation,'confirmationRecords')});recalculate(project);return {iteration,confirmation};}
function freezeBaseline(project,{humanAuthorization,owner='HUMAN_OPERATOR'}={}){ensureShape(project);if(!humanAuthorization)throw new Error('Human baseline authorization is required.');const confirmation=recordsForCurrentScope(project,'confirmationRecords').filter(r=>confirmationDetermination(project,r).determination==='SATISFIED').at(-1);if(!confirmation)throw new Error('A satisfied unchanged confirmation is required.');const iterationId=String(recordValue(confirmation,'CONFIRMATION_ITERATION_ID')||confirmation.relationships?.CONFIRMATION_ITERATION_ID||confirmation.scope?.iterationId||''),candidateId=iterationCandidateId(project,iterationId),candidate=records(project,'candidateFreezes').find(r=>recordId(r,'candidateFreezes')===candidateId),manifest=safe(recordValue(candidate,'COMPONENT_MANIFEST'));if(!candidate||!manifest.length)throw new Error('Confirmed candidate bytes are unavailable.');const baselineId=allocateId(project,'baselines'),scope={...scopeForIteration(project,iterationId),baselineId};const baseline=commandRecord(project,'baselines',{SUPPORTING_CONFIRMATION_ID:recordId(confirmation,'confirmationRecords'),APPROVED_VERSIONS:{...scopeForIteration(project,iterationId),candidateId},BASELINE_OWNER:owner,HUMAN_AUTHORIZATION:true,BASELINE_FILE_RECORDS:manifest,STATUS:'FROZEN'},{id:baselineId,stage:20,source:'APPLICATION',scope,relationships:{SUPPORTING_CONFIRMATION_ID:recordId(confirmation,'confirmationRecords')}});project.job.CURRENT_BASELINE_ID=baselineId;addHistory(project,'BASELINE_FROZEN',{stage:20,baselineId,iterationId,candidateId});recalculate(project);return baseline;}
function reserveProductExecution(project,{productionContextId}={}){ensureShape(project);const baselineId=String(project.job.CURRENT_BASELINE_ID||''),baseline=recordsForCurrentScope(project,'baselines').find(r=>recordId(r,'baselines')===baselineId);if(!baseline)throw new Error('Current baseline is missing.');const ctxId=String(productionContextId||''),context=records(project,'freshContexts').find(r=>recordId(r,'freshContexts')===ctxId);if(!context)throw new Error('A registered fresh production context is required.');const productId=allocateId(project,'products'),executionId=allocateInfrastructureId(project,'EXECUTION','history'),scope={...currentScope(project),baselineId,productId,contextId:ctxId};const product=commandRecord(project,'products',{PRODUCT_VERSION:'PRODUCT-v001',BASELINE_ID:baselineId,EXECUTION_ID:executionId,PRODUCTION_CONTEXT_ID:ctxId,INSTRUCTION_VERSION:project.job.CURRENT_INSTRUCTION_VERSION||'',STATUS:'RESERVED'},{id:productId,stage:21,source:'APPLICATION',scope,relationships:{BASELINE_ID:baselineId,PRODUCTION_CONTEXT_ID:ctxId}});project.job.CURRENT_PRODUCT_ID=productId;addHistory(project,'PRODUCT_EXECUTION_RESERVED',{stage:21,productId,baselineId,executionId,productionContextId:ctxId});recalculate(project);return product;}
function recordHumanInspection(project,{testId,observation,evidenceDescription,operatorLabel='HUMAN_OPERATOR'}={}){ensureShape(project);const tid=String(testId||''),test=recordsForCurrentScope(project,'tests').find(r=>recordId(r,'tests')===tid);if(!test)throw new Error('Current test not found.');if(upper(recordValue(test,'EXECUTION_MODE'))!=='HUMAN_INSPECTION')throw new Error('The selected test is not a human-inspection test.');if(!String(observation||'').trim())throw new Error('Human observation is required.');const payload={testId:tid,observation:String(observation),evidenceDescription:String(evidenceDescription||''),operatorLabel,identityAssurance:'SELF_ASSERTED'},evidence=commandRecord(project,'evidenceRecords',{APPLICATION_EVIDENCE_KIND:'HUMAN_OBSERVATION',APPLICATION_EVIDENCE_DESCRIPTION:String(evidenceDescription||`Human observation for ${tid}`),APPLICATION_EVIDENCE_CONTENT:JSON.stringify(payload),STATUS:'CURRENT'},{stage:Number(project.activeStage||0),source:'HUMAN_OBSERVATION'});evidence.fields.AUTHORITY_TYPE='HUMAN_OBSERVATION';refreshRecordHashes(evidence,'evidenceRecords');addHistory(project,'HUMAN_INSPECTION_OBSERVATION_RECORDED',{stage:Number(project.activeStage||0),testId:tid,evidenceId:recordId(evidence,'evidenceRecords'),operatorLabel:payload.operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return evidence;
}
function stageArtifactIds(project,stage){return [...new Set([...safe(project.stages[stage]?.authorizedFiles).map(x=>String(x.artifactId||x.id||'')).filter(Boolean),...recordsForCurrentScope(project,'artifacts').filter(a=>Number(a.stage)===Number(stage)||([22,23,24,25].includes(Number(stage))&&String(a.scope?.productId||'')===String(project.job.CURRENT_PRODUCT_ID||''))).map(a=>recordId(a,'artifacts'))])];}
function canonicalArtifactById(project,id){return recordsForCurrentScope(project,'artifacts').find(a=>recordId(a,'artifacts')===String(id||''));}
function stageArtifactReadiness(project,stage){const ids=stageArtifactIds(project,stage),artifacts=ids.map(id=>canonicalArtifactById(project,id)).filter(Boolean),ready=artifacts.filter(artifactVerified);return {artifactIds:ids,artifacts,readyArtifactIds:ready.map(a=>recordId(a,'artifacts')),missingArtifactIds:ids.filter(id=>!artifacts.some(a=>recordId(a,'artifacts')===id)),unverifiedArtifactIds:artifacts.filter(a=>!artifactVerified(a)).map(a=>recordId(a,'artifacts'))};}
function canonicalTestBindingCatalog(project,test,scope=currentScope(project)){const bindings=recordValue(test,'EXECUTABLE_INPUT_BINDINGS'),catalog={},issues=[];if(bindings===undefined||bindings===null||bindings===''||Array.isArray(bindings)){if(upper(recordValue(test,'EXECUTABLE_KIND'))==='TEST_IR')issues.push('TEST_IR input bindings must be a closed object.');return {catalog,issues};}if(typeof bindings!=='object'){issues.push('TEST_IR input bindings must be an object.');return {catalog,issues};}for(const [name,binding] of Object.entries(bindings)){if(!/^[A-Z][A-Z0-9_]{0,63}$/.test(name)){issues.push('Invalid binding name '+name+'.');continue;}if(typeof binding==='string'){const artifact=canonicalArtifactById(project,binding);if(!artifact){issues.push(`Binding ${name} does not resolve to a current project artifact.`);continue;}catalog[name]={kind:'ARTIFACT',artifactId:recordId(artifact,'artifacts'),filename:String(recordValue(artifact,'FILENAME')||''),sha256:String(recordValue(artifact,'SHA256')||''),byteSize:Number(recordValue(artifact,'BYTE_SIZE')),available:artifactVerified(artifact)};continue;}if(!binding||typeof binding!=='object'||Array.isArray(binding)){issues.push(`Binding ${name} must be a registered artifact or canonical-value binding.`);continue;}const kind=upper(binding.kind||binding.type||'ARTIFACT');if(kind==='ARTIFACT'){const artifact=canonicalArtifactById(project,binding.artifactId||binding.id);if(!artifact){issues.push(`Binding ${name} does not resolve to a current project artifact.`);continue;}catalog[name]={kind:'ARTIFACT',artifactId:recordId(artifact,'artifacts'),filename:String(recordValue(artifact,'FILENAME')||''),sha256:String(recordValue(artifact,'SHA256')||''),byteSize:Number(recordValue(artifact,'BYTE_SIZE')),available:artifactVerified(artifact)};}else if(kind==='CANONICAL_VALUE'){const path=String(binding.path||''),allowed=['job.CURRENT_INPUT_VERSION','job.CURRENT_SOURCE_SET_VERSION','job.CURRENT_REQUIREMENTS_VERSION','job.CURRENT_TEST_SUITE_VERSION','job.CURRENT_INSTRUCTION_VERSION','job.CURRENT_BASELINE_ID','job.CURRENT_PRODUCT_ID'];if(!allowed.includes(path)){issues.push(`Binding ${name} canonical path is not registered.`);continue;}const [,field]=path.split('.'),value=project.job[field];catalog[name]={kind:'CANONICAL_VALUE',path,value,sha256:hash.sha256Value(value)};}else issues.push(`Binding ${name} uses unsupported binding kind ${kind}.`);}return {catalog,issues};}
function applicationTestCapabilities(){const runtime=globalThis.closedLoopTestRuntime;return runtime?.capabilities?runtime.capabilities():{version:schema.TEST_IR.version,operations:[],limits:{},workerSupported:false};}
function applicationTestSupported(test){const runtime=globalThis.closedLoopTestRuntime;if(!runtime||typeof runtime.supports!=='function')return false;return runtime.supports({EXECUTABLE_KIND:recordValue(test,'EXECUTABLE_KIND'),EXECUTABLE_SPEC_VERSION:recordValue(test,'EXECUTABLE_SPEC_VERSION'),EXECUTABLE_SPEC:recordValue(test,'EXECUTABLE_SPEC'),EXECUTABLE_INPUT_BINDINGS:recordValue(test,'EXECUTABLE_INPUT_BINDINGS')});}
function declaredCapabilities(project){const text=[project.job.AVAILABLE_TOOLS,...safe(project.projectData.capabilityInventory).map(x=>x.name||x.capability||x.id)].filter(Boolean).join('\n').toUpperCase();return text;}
function capabilityAffirmativelyAvailable(project,capability,mode){const cap=String(capability||'').trim(),upperCap=upper(cap);if(mode==='APPLICATION_DETERMINISTIC')return true;if(mode==='INDEPENDENT_AGENT_REVIEW'||mode==='HUMAN_INSPECTION')return true;if(mode==='UNAVAILABLE')return false;if(!cap||['NONE','NOT APPLICABLE','UNKNOWN','UNAVAILABLE'].includes(upperCap))return false;return declaredCapabilities(project).includes(upperCap);}
function handoffExpectations(test,mode){const evidence=String(recordValue(test,'EVIDENCE_TO_PRESERVE')||recordValue(test,'EXPECTED_EVIDENCE')||'').trim()||'Return evidence capable of establishing the tested proposition.',attachments=parseDeclaredArtifactRequirements(test).filter(x=>!/^ARTIFACT-/i.test(x)).map((name,index)=>({kind:'RETURNED_ARTIFACT',filenameOrPattern:name||`returned-evidence-${index+1}`,required:true}));return {structuredResponse:mode!=='APPLICATION_DETERMINISTIC',requiredAttachmentKinds:attachments,requiredEvidenceDescription:evidence};}
function testExecutionPlan(project){
  ensureShape(project);const runtime=globalThis.closedLoopTestRuntime,tests=recordsForCurrentScope(project,'tests'),artifacts=recordsForCurrentScope(project,'artifacts'),items=tests.map(test=>{const testId=recordId(test,'tests'),requirementId=testRequirementId(test),mode=upper(recordValue(test,'EXECUTION_MODE')),testType=upper(recordValue(test,'TEST_TYPE')),requiredCapability=configuredRequiredCapability(test),bindings=canonicalTestBindingCatalog(project,test),declaredIds=[...new Set([...requiredArtifactIds(test),...Object.values(bindings.catalog).filter(x=>x.kind==='ARTIFACT').map(x=>x.artifactId)])],resolved=declaredIds.map(id=>artifacts.find(a=>recordId(a,'artifacts')===id)).filter(Boolean),missingArtifactIds=declaredIds.filter(id=>!resolved.some(a=>recordId(a,'artifacts')===id)),unverifiedArtifactIds=resolved.filter(a=>!artifactVerified(a)).map(a=>recordId(a,'artifacts')),artifactReady=missingArtifactIds.length===0&&unverifiedArtifactIds.length===0,bindingReady=bindings.issues.length===0&&Object.values(bindings.catalog).every(x=>x.kind!=='ARTIFACT'||x.available),applicationExecutorSupported=mode==='APPLICATION_DETERMINISTIC'?Boolean(runtime&&applicationTestSupported(test)):true,capabilityReady=mode==='APPLICATION_DETERMINISTIC'?applicationExecutorSupported:capabilityAffirmativelyAvailable(project,requiredCapability,mode),validMode=['APPLICATION_DETERMINISTIC','EXTERNAL_AGENT_TOOL','INDEPENDENT_AGENT_REVIEW','HUMAN_INSPECTION','EXTERNAL_SYSTEM','UNAVAILABLE'].includes(mode),executionRoute=mode==='APPLICATION_DETERMINISTIC'?'APPLICATION':mode==='INDEPENDENT_AGENT_REVIEW'?'INDEPENDENT_AI':mode==='EXTERNAL_AGENT_TOOL'?'EXTERNAL_AGENT_TOOL':mode==='HUMAN_INSPECTION'?'HUMAN':mode==='EXTERNAL_SYSTEM'?'EXTERNAL_SYSTEM':'BLOCKED',executorClass=mode==='APPLICATION_DETERMINISTIC'?'APPLICATION':mode==='INDEPENDENT_AGENT_REVIEW'?'INDEPENDENT_REVIEWER':mode==='EXTERNAL_AGENT_TOOL'?'EXTERNAL_AGENT_TOOL':mode==='HUMAN_INSPECTION'?'HUMAN':mode==='EXTERNAL_SYSTEM'?'EXTERNAL_SYSTEM':'UNAVAILABLE',blockingReason=!validMode?'Execution mode is missing or invalid.':mode==='UNAVAILABLE'?'Required execution route is unavailable.':mode==='APPLICATION_DETERMINISTIC'&&!applicationExecutorSupported?'The application does not implement the declared Test IR operation set.':bindings.issues.length?bindings.issues.join(' '):!artifactReady?`Required artifact bytes are unavailable or unverified: ${[...missingArtifactIds,...unverifiedArtifactIds].join(', ')||'unknown artifact'}.`:!capabilityReady?`Required capability is unavailable: ${requiredCapability||'not declared'}.`:'',executableNow=validMode&&mode!=='UNAVAILABLE'&&artifactReady&&bindingReady&&capabilityReady,operatorAction=validMode?(executableNow?(mode==='APPLICATION_DETERMINISTIC'?'RUN_IN_APP':mode==='EXTERNAL_AGENT_TOOL'?'SEND_TO_TOOL_AGENT':mode==='INDEPENDENT_AGENT_REVIEW'?'SEND_TO_INDEPENDENT_REVIEWER':mode==='HUMAN_INSPECTION'?'HUMAN_INSPECTION':'USE_EXTERNAL_SYSTEM'):'BLOCKED'):'BLOCKED',returnRequirements=handoffExpectations(test,mode);return {testId,requirementId,testType,executionMode:mode,requiredCapability,executorClass,executionRoute,executableNow,capabilityReady,artifactReady,applicationExecutorSupported,blockingReason,requiredArtifactIds:declaredIds,requiredArtifactNames:resolved.map(a=>String(recordValue(a,'FILENAME')||recordId(a,'artifacts'))),missingArtifactIds,unverifiedArtifactIds,bindingReady,bindingIssues:bindings.issues,operatorAction,returnRequirements};});const counts=Object.fromEntries(['APPLICATION_DETERMINISTIC','EXTERNAL_AGENT_TOOL','INDEPENDENT_AGENT_REVIEW','HUMAN_INSPECTION','EXTERNAL_SYSTEM','UNAVAILABLE'].map(mode=>[mode,items.filter(item=>item.executionMode===mode).length]));return {items,counts};}
function executionHandoff(project,{stage,operation,testIds,productId,runId,reviewerAliasContext}={}){
  ensureShape(project);const op=String(operation||'').toUpperCase(),testStages=[7,12,15,22,23,24].includes(stage)||(stage===17&&['VERIFY','REGRESSION'].includes(op))||(stage===19&&['VERIFY','REGRESSION_VERIFY'].includes(op)),ids=testIds?new Set(testIds.map(String)):null,allItems=testExecutionPlan(project).items,defectRequirementIds=stage===15?new Set(confirmedDefects(project).map(d=>String(recordValue(d,'REQ_ID')||d.relationships?.REQ_ID||'')).filter(Boolean)):null,stageItems=stage===22?allItems.filter(i=>i.testType==='DETERMINISTIC'):stage===23?allItems.filter(i=>i.testType==='MEANING'):stage===24?allItems.filter(i=>i.testType==='ADVERSARIAL'):stage===15&&defectRequirementIds?.size?allItems.filter(i=>defectRequirementIds.has(String(i.requirementId))):allItems,items=testStages?stageItems.filter(i=>!ids||ids.has(i.testId)):[],send=new Map(),withhold=new Map(),expectBack=new Map(),artifacts=recordsForCurrentScope(project,'artifacts'),artifactsById=new Map(artifacts.map(a=>[recordId(a,'artifacts'),a]));
  const addSend=(artifact,role='REQUIRED_INPUT')=>{if(!artifact)return;const id=recordId(artifact,'artifacts'),available=artifactVerified(artifact);send.set(id,{artifactId:id,filename:String(recordValue(artifact,'FILENAME')||id),sha256:String(recordValue(artifact,'SHA256')||'UNAVAILABLE'),role,bytesAvailable:available});};
  if([22,23,24,25].includes(Number(stage))){const target=String(productId||project.job.CURRENT_PRODUCT_ID||'');for(const artifact of artifacts.filter(a=>String(a.scope?.productId||'')===target||['FINAL_PRODUCT','FINISHED_PRODUCT','PRODUCT'].includes(upper(recordValue(a,'ROLE')))))addSend(artifact,'FINISHED_PRODUCT');}
  for(const item of items){for(const id of item.requiredArtifactIds)addSend(artifactsById.get(id),'TEST_INPUT');for(const returned of item.returnRequirements.requiredAttachmentKinds)expectBack.set(`${item.testId}:${returned.filenameOrPattern}`,{testId:item.testId,...returned});if(item.executionMode==='INDEPENDENT_AGENT_REVIEW')withhold.set('OTHER_REVIEWER_CONCLUSIONS',{artifactIdOrCategory:'OTHER_REVIEWER_CONCLUSIONS',reason:'Independent review must not receive another reviewer conclusion.'});}
  if(stage===11||((stage===17||stage===19)&&op==='EXECUTE_RUN')){for(const artifact of artifacts.filter(a=>['CANDIDATE','FROZEN_CANDIDATE','CANDIDATE_COMPONENT'].includes(upper(recordValue(a,'ROLE')))||String(a.scope?.candidateId||'')===String(currentScope(project).candidateId||'')))addSend(artifact,'FROZEN_CANDIDATE');for(const category of ['PRIOR_RUN_OUTPUTS','REVIEWER_FEEDBACK','ROOT_CAUSE_ANALYSIS','PROPOSED_CORRECTIONS'])withhold.set(category,{artifactIdOrCategory:category,reason:'Execution context must remain uncontaminated.'});}
  if(stage===12){for(const category of ['GENERATOR_SELF_EVALUATION','OTHER_VERIFIER_DETERMINATIONS','STAGE_13_COMPARISON','ROOT_CAUSE_ANALYSIS','PROPOSED_CORRECTIONS'])withhold.set(category,{artifactIdOrCategory:category,reason:'Independent run verification context isolation.'});}
  if(stage===23){for(const category of ['GENERATOR_CORRECTNESS_CLAIMS','DETERMINISTIC_PASS_STATUS_WHEN_UNNECESSARY','ADVERSARIAL_FINDINGS','PRIOR_MEANING_REVIEW_CONCLUSIONS'])withhold.set(category,{artifactIdOrCategory:category,reason:'Independent meaning review must remain blind to unnecessary conclusions.'});}
  if(stage===24){for(const category of ['GENERATOR_REASONING','GENERATOR_SELF_EVALUATION','PRIOR_REVIEWER_CONCLUSIONS'])withhold.set(category,{artifactIdOrCategory:category,reason:'Adversarial review must not be steered by prior conclusions.'});}
  const sendList=[...send.values()],blocked=sendList.filter(x=>!x.bytesAvailable),instruction=blocked.length?`Required file bytes are unavailable in browser custody: ${blocked.map(x=>x.filename).join(', ')}.`:items.some(i=>i.operatorAction==='RUN_IN_APP')?'Application-native tests run in this application; do not send them to an external agent.':items.length?'Send only the exact listed files to the derived executor and return the listed evidence/files.':'No external test handoff is required for this operation.';
  return {stage,operation:op,productId:productId||project.job.CURRENT_PRODUCT_ID||null,runId:runId||null,reviewerAliasContext:reviewerAliasContext||null,send:sendList,withhold:[...withhold.values()],expectBack:[...expectBack.values()],structuredResponseRequired:items.some(i=>i.returnRequirements.structuredResponse),requiredEvidenceDescription:[...new Set(items.map(i=>i.returnRequirements.requiredEvidenceDescription).filter(Boolean))].join(' | ')||'NONE',instruction,blocked:blocked.length>0};
}
function evidenceChainExplanation(project,reqId){ensureShape(project);const requirement=recordsForCurrentScope(project,'requirements').find(r=>recordId(r,'requirements')===String(reqId||''));if(!requirement)return {proposition:`Unknown requirement ${reqId||''}`,support:[],unresolved:['Requirement identity does not resolve in current scope.']};const tests=recordsForCurrentScope(project,'tests').filter(t=>testRequirementId(t)===recordId(requirement,'requirements')),support=[],unresolved=[];for(const test of tests){const tid=recordId(test,'tests'),results=[...recordsForCurrentScope(project,'deterministicResults'),...recordsForCurrentScope(project,'meaningResults'),...recordsForCurrentScope(project,'adversarialResults'),...recordsForCurrentScope(project,'verification')].filter(r=>String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'')===tid);if(results.length!==1){unresolved.push(`${tid} has ${results.length} current results; exactly one applicable result is required.`);continue;}const result=results[0],collection=result.stage===22?'deterministicResults':result.stage===23?'meaningResults':result.stage===24?'adversarialResults':'verification',determination=effectiveDetermination(collection,result,test,project),e=evaluateEvidenceSufficiency(project,{requirement,test,result});support.push(`${tid} applies; ${recordId(result,collection)} is ${determination}; evidence ${e.presentEvidenceIds.join(', ')||'NONE'} is ${e.sufficient?'sufficient':'insufficient'}.`);if(determination!=='SATISFIED'||!e.sufficient)unresolved.push(`${tid} is not established.`);}const productArtifacts=recordsForCurrentScope(project,'artifacts').filter(a=>String(a.scope?.productId||'')===String(project.job.CURRENT_PRODUCT_ID||''));for(const artifact of productArtifacts)if(artifactVerified(artifact))support.push(`${recordId(artifact,'artifacts')} has application SHA-256 ${recordValue(artifact,'SHA256')}.`);return {proposition:`Delivered artifact satisfies ${recordId(requirement,'requirements')}: ${recordValue(requirement,'OBLIGATION')||''}`,support,unresolved};}
function stage16CorrectionPlan(project){ensureShape(project);const defects=confirmedDefects(project),rcas=recordsForCurrentScope(project,'rootCauses'),changes=recordsForCurrentScope(project,'changes');return defects.map(defect=>{const id=recordId(defect,'defects'),rca=rcas.find(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===id),layer=String(recordValue(rca,'EARLIEST_DEFECTIVE_LAYER')||''),executionOnly=/EXECUTION/i.test(layer)&&!/INSTRUCTION|REQUIREMENT|TEST|SOURCE|RESEARCH|HUMAN|CONFIG|EXPORT/i.test(layer),change=changes.find(c=>String(recordValue(c,'TRIGGERING_DEFECT_IDS')||'').includes(id));let actionType='REVIEW_AGENT_CORRECTION';if(!rca)actionType='BLOCKED';else if(/HUMAN/.test(upper(layer)))actionType='ANSWER_HUMAN_AUTHORITY';else if(executionOnly)actionType='RERUN_WITHOUT_CHANGING_INSTRUCTION';else if(/APPLICATION|VERSION|HASH|DERIVATION/.test(upper(layer)))actionType='APPLICATION_RECALCULATE';return {defectId:id,rcaId:recordId(rca,'rootCauses')||null,responsibleLayer:layer||'UNKNOWN',actionType,executionOnlyInstructionPreserved:executionOnly,changeSetId:recordId(change,'changes')||null,explanation:executionOnly?'The instruction remains unchanged; rerun the affected execution and preserve the regression.':layer?`Correct the earliest supported defective layer: ${layer}.`:'Root cause is not yet established.'};});}

const ACTION_TYPES=Object.freeze(['RUN_APP_TESTS','CALCULATE_RELEASE','BUILD_EVIDENCE_CHAINS','FREEZE_CANDIDATE','RESERVE_RUN_BATCH','BEGIN_UNCHANGED_CONFIRMATION','FREEZE_BASELINE','REGISTER_PRODUCTION_CONTEXT','RESERVE_PRODUCT_EXECUTION','AI_REVIEW','EXTERNAL_AGENT_TOOL','HUMAN_INSPECTION','EXTERNAL_SYSTEM','ATTACH_REQUIRED_FILES','CONTINUE_AGENT_CONVERSATION','PASTE_FINAL_JSON','REVIEW_PROPOSAL','BLOCKED','COMPLETE']);
function operationalNextAction(project){
  ensureShape(project);const stage=Number(project.job.CURRENT_STAGE||project.activeStage||1),stageState=project.stages[stage]?.status||'NOT STARTED',pending=safe(project.projectData.responseProposals).find(p=>Number(p.stage)===stage&&p.status==='PENDING'&&!p.invalidatedBy),raw=safe(project.projectData.rawResponses).filter(r=>Number(r.stage)===stage&&!r.invalidatedBy).at(-1),validation=safe(project.projectData.responseValidations).filter(v=>Number(v.stage)===stage&&(!raw||v.rawResponseId===raw.rawResponseId)).at(-1),prompt=safe(project.projectData.generatedPrompts).filter(p=>Number(p.stage)===stage&&!p.invalidatedBy).at(-1),human=unresolvedHumanRequests(project,stage),plan=testExecutionPlan(project),stageOperation=prompt?.operation||schema.STAGE_CONTRACTS[stage]?.operations?.[0]||'COMPLETE',handoff=executionHandoff(project,{stage,operation:stageOperation});
  const base={stage,stageState,actionType:'CONTINUE_AGENT_CONVERSATION',heading:'Continue the current stage',explanation:'Use the current instruction and continue the external conversation until the stage is ready for one final structured response.',primaryButton:'Open current instruction',secondaryAction:null,filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack,blockingReason:null,canonicalStateChanged:false,acceptedChange:null,downstreamInvalidated:[],newPromptRequired:false,promptId:prompt?.instructionId||null};
  const latestReceipt=safe(project.projectData.outputReceipts).filter(r=>Number(r.stage)===stage).at(-1),latestHistory=safe(project.projectData.history).at(-1);if(latestReceipt){base.canonicalStateChanged=Boolean(latestReceipt.canonicalStateChanged);base.acceptedChange=latestReceipt.acceptedChangeId||null;base.downstreamInvalidated=safe(latestReceipt.downstreamInvalidated);base.newPromptRequired=Boolean(latestReceipt.newPromptRequired);}
  if(stage===10&&!records(project,'iterations').some(r=>Number(r.stage)===10&&isActiveRecord(r))){return {...base,actionType:'FREEZE_CANDIDATE',heading:'Select and freeze the exact test candidate',explanation:'Select the exact verified files that make up this candidate. The application will assign the iteration/candidate identities and freeze their byte hashes.',primaryButton:'Freeze selected candidate'};}
  if(stage===11){const iteration=latestIteration(project,[10]),iterationId=recordId(iteration,'iterations'),runs=iterationId?recordsForIteration(project,'runs',iterationId):[];if(runs.length!==10)return {...base,actionType:'RESERVE_RUN_BATCH',heading:'Prepare the ten independent executions',explanation:'The application must reserve exactly ten RUN_ID and CONTEXT_ID pairs before any execution prompt is used.',primaryButton:'Reserve ten run/context slots'};}
  if(stage===17){const iteration=latestIteration(project,[17]);if(!iteration)return {...base,actionType:'FREEZE_CANDIDATE',heading:'Freeze the corrected candidate',explanation:'Select the exact corrected candidate components. The application creates a new iteration/candidate and preserves the prior versions.',primaryButton:'Freeze corrected candidate'};const iid=recordId(iteration,'iterations'),runs=recordsForIteration(project,'runs',iid);if(runs.length!==10)return {...base,actionType:'RESERVE_RUN_BATCH',heading:'Prepare the corrected ten-run iteration',explanation:'Reserve ten new run/context slots for the corrected candidate.',primaryButton:'Reserve ten new run/context slots'};}
  if(stage===19){const confirmation=records(project,'confirmationRecords').filter(r=>Number(r.stage)===19&&isActiveRecord(r)).at(-1);if(!confirmation)return {...base,actionType:'BEGIN_UNCHANGED_CONFIRMATION',heading:'Begin unchanged confirmation',explanation:'The application will create the confirmation iteration using the exact current converged candidate identity and hashes. No replacement files are selected.',primaryButton:'Begin unchanged confirmation'};const iid=String(recordValue(confirmation,'CONFIRMATION_ITERATION_ID')||confirmation.relationships?.CONFIRMATION_ITERATION_ID||'');const runs=iid?recordsForIteration(project,'runs',iid):[];if(runs.length!==10)return {...base,actionType:'RESERVE_RUN_BATCH',heading:'Prepare ten unchanged confirmation runs',explanation:'Reserve ten new context/run slots while keeping the candidate bytes unchanged.',primaryButton:'Reserve ten confirmation runs'};}
  if(stage===20&&!recordsForCurrentScope(project,'baselines').length)return {...base,actionType:'FREEZE_BASELINE',heading:'Authorize and freeze the production baseline',explanation:'Human baseline authorization is required. The application will freeze the exact confirmed candidate identities and hashes.',primaryButton:'Authorize and freeze baseline'};
  if(stage===21&&!recordsForCurrentScope(project,'products').length){const contexts=records(project,'freshContexts').filter(c=>upper(recordValue(c,'CONTEXT_ROLE'))==='PRODUCTION'&&isActiveRecord(c));if(!contexts.length)return {...base,actionType:'REGISTER_PRODUCTION_CONTEXT',heading:'Register a fresh production context',explanation:'Create or identify the fresh external production context that will receive only approved baseline material.',primaryButton:'Register production context'};return {...base,actionType:'RESERVE_PRODUCT_EXECUTION',heading:'Reserve the final product execution',explanation:'The application assigns PRODUCT_ID and EXECUTION_ID before finished-product bytes are attached.',primaryButton:'Reserve product execution'};}
  if(stage===27&&!recordsForCurrentScope(project,'releaseRecords').length)return {...base,actionType:'CALCULATE_RELEASE',heading:'Calculate the release gate',explanation:'The application will calculate ACCEPTED, REJECTED, or BLOCKED from the complete current evidence. Neither the agent nor the human chooses the release result.',primaryButton:'Calculate release gate'};
  if(stage===29&&!recordsForCurrentScope(project,'evidenceChains').length)return {...base,actionType:'BUILD_EVIDENCE_CHAINS',heading:'Build the evidence chains',explanation:'The application will build current mandatory requirement evidence chains from canonical identities and sufficient evidence.',primaryButton:'Build evidence chains'};
  const relevantItems=stage===22?plan.items.filter(i=>i.testType==='DETERMINISTIC'):stage===23?plan.items.filter(i=>i.testType==='MEANING'):stage===24?plan.items.filter(i=>i.testType==='ADVERSARIAL'):[];
  if([22,23,24].includes(stage)&&relevantItems.length){const blocked=relevantItems.find(i=>!i.executableNow);if(blocked)return {...base,actionType:blocked.requiredArtifactIds.length&&!blocked.artifactReady?'ATTACH_REQUIRED_FILES':'BLOCKED',heading:'Verification is blocked',explanation:blocked.blockingReason||'The required verification route is not executable.',primaryButton:blocked.requiredArtifactIds.length?'Attach required files':null,blockingReason:blocked.blockingReason};const native=relevantItems.some(i=>i.operatorAction==='RUN_IN_APP');if(native)return {...base,actionType:'RUN_APP_TESTS',heading:'Run the mechanical verification in the application',explanation:'No external action is required for the application-native tests. The application will run the registered Test IR against the exact verified product bytes.',primaryButton:'Run tests'};const reviewer=relevantItems.find(i=>i.operatorAction==='SEND_TO_INDEPENDENT_REVIEWER');if(reviewer)return {...base,actionType:'AI_REVIEW',heading:'Use a fresh independent reviewer',explanation:'Prepare and send only the exact listed files/context. Do not send withheld prior conclusions.',primaryButton:'Prepare reviewer package'};const tool=relevantItems.find(i=>i.operatorAction==='SEND_TO_TOOL_AGENT');if(tool)return {...base,actionType:'EXTERNAL_AGENT_TOOL',heading:'Use the required tool-capable executor',explanation:`This verification requires ${tool.requiredCapability}. Send the exact listed files and return the listed evidence.`,primaryButton:'Prepare tool package'};const humanItem=relevantItems.find(i=>i.operatorAction==='HUMAN_INSPECTION');if(humanItem)return {...base,actionType:'HUMAN_INSPECTION',heading:'Complete the required human inspection',explanation:'A human observation is required; an AI statement cannot substitute for it.',primaryButton:'Record human observation',testId:humanItem.testId};const system=relevantItems.find(i=>i.operatorAction==='USE_EXTERNAL_SYSTEM');if(system)return {...base,actionType:'EXTERNAL_SYSTEM',heading:'Use the required external system',explanation:`This verification requires ${system.requiredCapability}. Agent substitution is not allowed.`,primaryButton:'Prepare external-system package'};}
  if(human.length)return {...base,actionType:'CONTINUE_AGENT_CONVERSATION',heading:'Answer the human-authority question',explanation:'Continue the conversation with the agent. Do not paste final JSON until the agent says the stage is ready.',primaryButton:'Continue conversation'};
  if(pending)return {...base,actionType:'REVIEW_PROPOSAL',heading:'Review the validated proposal',explanation:'The response parsed and validated. Review the proposed canonical changes before accepting them.',primaryButton:'Review proposal'};
  if(validation&&validation.valid===false)return {...base,actionType:'PASTE_FINAL_JSON',heading:'Use the corrected current instruction',explanation:'The last response was rejected before canonical mutation. Send the updated correction instruction, then paste the replacement final JSON.',primaryButton:'Paste replacement JSON',canonicalStateChanged:false,newPromptRequired:true};
  if(stageState==='BLOCKED')return {...base,actionType:'BLOCKED',heading:'Correctness is blocked',explanation:project.stages[stage].gate?.reasons?.join(' ')||'The current stage is blocked.',primaryButton:null,blockingReason:project.stages[stage].gate?.reasons?.join(' ')||'Blocked'};
  if(stageState==='COMPLETE'&&stage===30)return {...base,actionType:'COMPLETE',heading:'Workflow complete',explanation:'All 30 stages are complete under the current canonical evidence.',primaryButton:null};
  if(raw&&!validation)return {...base,actionType:'PASTE_FINAL_JSON',heading:'Validate the captured response',explanation:'The response is captured but not yet validated. Continue processing the preserved raw response.',primaryButton:'Validate response'};
  return base;
}

function recordApplicationDeterministicResult(project,{testId,productId,runtimeResult,inputArtifacts=[]}={}){
  ensureShape(project);const tid=String(testId||''),pid=String(productId||'');const test=recordsForCurrentScope(project,'tests').find(r=>recordId(r,'tests')===tid),product=recordsForCurrentScope(project,'products').find(r=>recordId(r,'products')===pid);
  if(!test||!applicationTestSupported(test))throw new Error(`Test ${tid||'UNKNOWN'} is not a valid current application-native Test IR test.`);if(!product)throw new Error(`Product ${pid||'UNKNOWN'} is not the current product.`);
  const determination=upper(runtimeResult?.determination||'UNDETERMINED');if(!['SATISFIED','VIOLATED','UNDETERMINED'].includes(determination))throw new Error('Native deterministic runtime returned an invalid determination.');if(runtimeResult?.status!=='COMPLETE'&&determination!=='UNDETERMINED')throw new Error('Incomplete native runtime execution cannot claim a conclusive determination.');
  const spec=recordValue(test,'EXECUTABLE_SPEC'),specSha256=hash.sha256Value(spec),artifactIdentities=inputArtifacts.map(a=>({artifactId:String(a.artifactId),filename:String(a.filename||''),byteSize:Number(a.byteSize),sha256:String(a.sha256||'')}));
  if(!artifactIdentities.length)throw new Error('Application-native deterministic execution requires at least one exact input artifact identity.');for(const a of artifactIdentities){const canonical=recordsForCurrentScope(project,'artifacts').find(r=>recordId(r,'artifacts')===a.artifactId);if(!canonical||upper(recordValue(canonical,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||String(recordValue(canonical,'SHA256'))!==a.sha256||Number(recordValue(canonical,'BYTE_SIZE'))!==a.byteSize)throw new Error(`Native deterministic input ${a.artifactId} is not application-verified current bytes.`);}
  const evidencePayload={testId:tid,productId:pid,determination,runtimeVersion:String(runtimeResult?.runtimeVersion||runtimeResult?.executorVersion||'closed-loop-test-runtime/1'),specVersion:String(runtimeResult?.specVersion||schema.TEST_IR.version),testSpecSha256:specSha256,inputArtifacts:artifactIdentities,observations:safe(runtimeResult?.observations),expected:runtimeResult?.expected??null,actual:runtimeResult?.actual??null};
  const identityHash=hash.sha256Value(evidencePayload),existing=recordsForCurrentScope(project,'deterministicResults').find(r=>r.source==='APPLICATION_TEST_RUNTIME'&&String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'')===tid&&String(recordValue(r,'PRODUCT_ID')||r.relationships?.PRODUCT_ID||'')===pid&&String(recordValue(r,'TEST_SPEC_SHA256')||'')===specSha256&&r.nativeExecutionIdentitySha256===identityHash);if(existing)return existing;
  if(recordsForCurrentScope(project,'deterministicResults').some(r=>String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'')===tid))throw new Error(`A current deterministic result already exists for ${tid}; invalidate/correct it before another result is committed.`);
  const evidence=commandRecord(project,'evidenceRecords',{APPLICATION_EVIDENCE_KIND:'APPLICATION_DETERMINISTIC_EXECUTION',APPLICATION_EVIDENCE_DESCRIPTION:`Application-native deterministic execution evidence for ${tid}.`,APPLICATION_EVIDENCE_CONTENT:JSON.stringify(evidencePayload),SHA256:identityHash,STATUS:'CURRENT'},{stage:22,source:'APPLICATION_TEST_RUNTIME'});
  const result=commandRecord(project,'deterministicResults',{PRODUCT_ID:pid,PRODUCT_SHA256:hash.sha256Value(artifactIdentities),TEST_ID:tid,APPLICATION_DETERMINATION:determination,RUNTIME_VERSION:evidencePayload.runtimeVersion,TEST_SPEC_SHA256:specSha256,INPUT_ARTIFACT_IDENTITIES:JSON.stringify(artifactIdentities),RUNTIME_OBSERVATIONS:JSON.stringify(safe(runtimeResult?.observations))},{stage:22,source:'APPLICATION_TEST_RUNTIME'});result.relationships={...(result.relationships||{}),PRODUCT_ID:pid,TEST_ID:tid};result.evidenceRefs=[recordId(evidence,'evidenceRecords')];result.nativeExecutionIdentitySha256=identityHash;refreshRecordHashes(result,'deterministicResults');addHistory(project,'APPLICATION_TEST_EXECUTED',{stage:22,recordId:recordId(result,'deterministicResults'),testId:tid,productId:pid,determination,evidenceId:recordId(evidence,'evidenceRecords'),testSpecSha256:specSha256,inputArtifactIds:artifactIdentities.map(x=>x.artifactId)});recalculate(project);return result;
}
function operationalMetrics(project){
  ensureShape(project);const history=safe(project.projectData.history),validations=safe(project.projectData.responseValidations),proposals=safe(project.projectData.responseProposals),releases=safe(project.projectData.releaseRecords),accepted=safe(project.projectData.acceptedChanges).length,rejected=safe(project.projectData.rejectedResponses).length,stale=proposals.filter(x=>x.status==='STALE'||x.invalidatedBy).length,defects=confirmedDefects(project),baselineEvent=history.find(x=>x.type==='BASELINE_FROZEN')?.eventSequence||Infinity,identityMismatches=records(project,'artifactIdentities',{active:false}).filter(r=>!truth(recordValue(r,'EXACT_HASH_MATCH'))||!truth(recordValue(r,'EXACT_SIZE_MATCH'))).length,regressionRecurrences=records(project,'regressionExecutions',{active:false}).filter(r=>upper(recordValue(r,'PHASE'))!=='PRE_CORRECTION'&&['VIOLATED','FAILED','FAIL'].includes(upper(recordValue(r,'RESULT')))).length;
  const activeRuns=records(project,'runs'),iterationIds=[...new Set(activeRuns.map(r=>String(recordValue(r,'ITERATION_ID')||r.scope?.iterationId||'')).filter(Boolean))],establishedIterations=new Set(iterationIds.filter(iterationId=>evaluateContextIndependence(project,{role:'RUN_BATCH',iterationId}).determination==='APPLICATION_ESTABLISHED')),completedRuns=activeRuns.filter(r=>establishedIterations.has(String(recordValue(r,'ITERATION_ID')||r.scope?.iterationId||''))&&['COMPLETED','SATISFIED','SUCCESS','PASSED'].includes(upper(recordValue(r,'EXECUTION_STATUS')||r.completionState||r.status||''))).length,independentVerifications=records(project,'verification').filter(r=>releaseVerificationTrust(project,r).determination==='APPLICATION_ESTABLISHED').length,independentOps=completedRuns+independentVerifications,silentFailures=defects.filter(d=>Number(d.eventSequence||Infinity)>baselineEvent).length;
  return {rawResponses:safe(project.projectData.rawResponses).length,validationFailures:validations.filter(x=>x.valid===false).length,staleResponses:stale,rejectedProposals:rejected,acceptedDataChanges:accepted,clarificationCycles:safe(project.projectData.humanInputAnswers).length,controlledCorrections:history.filter(x=>['ACCEPTED_RESPONSE_INVALIDATED','STAGE_AUTHORITY_CHANGED','DOWNSTREAM_INVALIDATED'].includes(x.type)).length,storageFailures:history.filter(x=>String(x.type||'').includes('STORAGE')&&String(x.type||'').includes('FAIL')).length,gateRegressions:history.filter(x=>x.type==='DOWNSTREAM_INVALIDATED').length,releaseRejections:releases.filter(x=>upper(recordValue(x,'DETERMINATION'))==='REJECTED').length,releaseBlocks:releases.filter(x=>upper(recordValue(x,'DETERMINATION'))==='BLOCKED').length,artifactIdentityMismatches:identityMismatches,regressionRecurrences,humanCorrectionFrequency:accepted?history.filter(x=>['ACCEPTED_RESPONSE_INVALIDATED','STAGE_AUTHORITY_CHANGED'].includes(x.type)).length/accepted:0,materiallyIndependentAcceptedOperations:independentOps,observedSilentFailures:silentFailures,zeroFailure95PercentUpperBound:silentFailures===0&&independentOps>0?3/independentOps:null};
}

globalThis.closedLoopWorkflowEngine=Object.freeze({recordMigratedAcceptedChange,
  version:'closed-loop-workflow-engine/1',STAGE_STATES,FORMAL_STATES,ALL_COLLECTIONS,ACTION_TYPES,
  ensureShape,clone,now,safe,upper,truth,falsey,numeric,recordFields,recordValue,recordId,isActiveRecord,records,existingIds,allocateId,allocateInfrastructureId,refreshRecordHashes,commandRecord,applicationInitialFields,
  addHistory,unresolvedHumanRequests,openBlockers,acceptedChanges,acceptedControlEvents,hasStageActivity,latestIteration,mandatoryRequirements,confirmedDefects,unresolvedMaterialDefects,
  currentScope,recordsForScope,recordsForCurrentScope,scopeForIteration,recordsForIteration,verificationMatrix,evaluateIteration,DERIVATIONS,coverageMetrics,convergenceMetrics,releaseMetrics,applicationTestCapabilities,capabilityAffirmativelyAvailable,canonicalTestBindingCatalog,testExecutionPlan,executionHandoff,evaluateContextIndependence,evaluateEvidenceSufficiency,evaluateEvidenceContract,releaseVerificationTrust,evaluateResultConsistency,effectiveDetermination,validateTraceIntegrity,detectCurrentContradictions,executionStability,evidenceChainExplanation,stage16CorrectionPlan,operationalNextAction,operationalMetrics,intakeCoverageManifest,parseCapturedInputSet,evaluateIntakeAccounting,obligationManifest,evaluateObligationAccounting,representationInspectionCoverage,gate,deriveStageData,recalculate,invalidateDownstream,applicationInitialFields,
  preserveRawResponse,recordRawCaptureFailure,recordValidation,createResponseProposal,recordAcceptedChange,recordDisposition,recordRejectedResponse,buildExtractionManifest,acceptAgentDataChange,acceptControlEvent,
  setHumanInput,setHumanDecision,answerHumanInputRequest,confirmStage,reserveIterationCandidate,reserveRunBatch,registerFreshContext,beginUnchangedConfirmation,freezeBaseline,reserveProductExecution,recordHumanInspection,recordApplicationDeterministicResult,registerArtifactMetadata,reconcileArtifactCustody,applicationArtifactHash,artifactVerified,requiredArtifactIds,stageArtifactIds,stageArtifactReadiness,registerExternalDeterministicResult,registerMeaningResult,registerAdversarialResult,registerRepresentationInspection,calculateRelease,buildEvidenceChains,compareReleaseArtifacts,recordRegressionExecution
});
})();