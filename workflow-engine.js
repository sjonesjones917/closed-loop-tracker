(()=>{
'use strict';

const core=globalThis.closedLoopCore;
const schema=globalThis.closedLoopWorkflowSchema;
const hash=globalThis.closedLoopHash;
if(!core||!schema||!hash)throw new Error('workbook.js, hash.js, and workflow-schema.js, test-runtime.js must load before workflow-engine.js.');

const STAGE_STATES=Object.freeze(['NOT STARTED','IN PROGRESS','BLOCKED','READY','COMPLETE']);
const FORMAL_STATES=Object.freeze(['UNKNOWN','NONE','NOT APPLICABLE','TRUE','FALSE','SATISFIED','VIOLATED','UNDETERMINED','ACCEPTED','REJECTED','BLOCKED']);
const INFRA_COLLECTIONS=Object.freeze([
  'inputVersions','rawResponses','responseProposals','responseValidations','acceptedChanges','rejectedResponses','extractionManifests',
  'humanInputRequests','humanInputAnswers','stageConfirmations','artifactVersions','generatedPrompts','generatedOutputs','outputReceipts',
  'history','newJobResets','reviews','recoveredProjects','responseDispositions','executionFailures','commandReceipts','humanDecisions','nativeExecutionEvents'
]);
const ALL_COLLECTIONS=Object.freeze([...new Set([...Object.keys(schema.RECORD_SCHEMAS),...INFRA_COLLECTIONS])]);
const TRUTH_VALUES=Object.freeze(['TRUE','FALSE','UNKNOWN']);
const EPISTEMIC_BASES=Object.freeze(['APPLICATION_OBSERVED','VERIFIED_EXTERNAL','EXTERNALLY_SUPPORTED','SELF_ASSERTED','NONE']);
const FRESHNESS_STATES=Object.freeze(['CURRENT','EXPIRED','UNKNOWN','NOT_APPLICABLE']);
const APPLICABILITY_STATES=Object.freeze(['APPLICABLE','NOT_APPLICABLE','UNKNOWN']);
const ENTAILMENT_RELATIONS=Object.freeze(['ESTABLISHES','REFUTES','SUPPORTS_ONLY','CONTEXT_ONLY','DOES_NOT_ADDRESS','UNKNOWN']);
const PROOF_EXPRESSION_OPERATORS=Object.freeze(['LEAF','ALL_OF','ANY_OF','AT_LEAST_K']);
const INDEPENDENCE_DIMENSIONS=Object.freeze(['APPLICATION_SESSION_DISTINCTNESS','APPLICATION_INPUT_ISOLATION','USER_TRANSFER_CONFORMITY','EXTERNAL_CONTEXT_IDENTIFIER_DISTINCTNESS','PROVIDER_CONTEXT_INDEPENDENCE','EXECUTOR_OR_REVIEWER_ROLE_SEPARATION','ENVIRONMENT_INDEPENDENCE']);
const REQUIRED_EVIDENCE_CLASSES=Object.freeze(['CANONICAL_EVIDENCE','CANONICAL_OR_EXACT_EVIDENCE','OBSERVATION_AND_ENTAILMENT','APPLICATION_VERIFIED_BYTES','HUMAN_OBSERVATION','EXACT_ARTIFACT_BOUND_INSPECTION_RESERVATION','EXTERNAL_SYSTEM_ATTESTATION','EXTERNAL_SYSTEM_OUTPUT','MEANING_COMPARISON','CAPABILITY_EXECUTION','CAPABLE_EXECUTION','NATIVE_APPLICATION_RESULT','EQUIVALENT_SEMANTIC_COVERAGE','REJECTION_EVIDENCE']);
const REQUIRED_RUN_DIMENSIONS=Object.freeze(['RUN_ID','ITERATION_ID','CANDIDATE_ID','PRODUCT_ID','ENVIRONMENT_MANIFEST_ID']);
const FRESHNESS_REQUIREMENT_STATES=Object.freeze(['CURRENT','NOT_APPLICABLE','CURRENT_OR_NOT_APPLICABLE']);
const DELIVERY_STATES=Object.freeze(['AUTHORIZED','BLOCKED','WITHDRAWN_FOR_FUTURE_USE','SUPERSEDED']);
const ACTIVE_RESERVATION_STATES=Object.freeze(['RESERVED','ACTIVE','ORPHANED']);
const PLACEHOLDER_REFERENCES=Object.freeze(new Set(['','NONE','NOT APPLICABLE','N/A','UNKNOWN','PENDING','UNASSIGNED']));

const clone=value=>value===undefined?undefined:JSON.parse(JSON.stringify(value));
const now=()=>new Date().toISOString();
const safe=value=>Array.isArray(value)?value:[];
const upper=value=>String(value??'').trim().toUpperCase();
const canonicalReference=value=>{const text=String(value??'').trim();return PLACEHOLDER_REFERENCES.has(upper(text))?'':text;};
const truth=value=>['TRUE','YES','SATISFIED','ACCEPTED','AUTHORIZED','CONFIRMED','CONVERGED','COMPLETE','SUCCESS','SUCCEEDED','EFFECTIVE'].includes(upper(value));
const falsey=value=>['FALSE','NO','NONE','0','NOT APPLICABLE'].includes(upper(value));
const numeric=value=>{const parsed=Number(String(value??'').replace(/[^0-9.-]/g,''));return Number.isFinite(parsed)?parsed:0;};
const exactRatio=(numerator,denominator)=>{const n=Number(numerator),d=Number(denominator);if(!Number.isSafeInteger(n)||n<0||!Number.isSafeInteger(d)||d<=0||n>d)throw new Error('Exact ratio requires safe integers with 0 <= numerator <= denominator and a positive denominator.');if(typeof schema.makeExactRatio==='function')return schema.makeExactRatio(n,d);const gcd=(a,b)=>{a=Math.abs(a);b=Math.abs(b);while(b){const next=a%b;a=b;b=next;}return a||1;},factor=gcd(n,d);return Object.freeze({numberType:'RATIO',numerator:n/factor,denominator:d/factor});};
const ratioNumber=value=>value&&typeof value==='object'&&value.numberType==='RATIO'?Number(value.numerator)/Number(value.denominator):Number(value);
function canonicalStabilitySummary(value){
  const summary=clone(value);if(!summary||typeof summary!=='object')return summary;
  for(const key of ['requirementStability','testStability','propositionStability'])for(const row of Object.values(summary[key]||{})){if(!row||typeof row!=='object')continue;const satisfied=Number(row.satisfied||0),violated=Number(row.violated||0),undetermined=Number(row.undetermined||0),total=satisfied+violated+undetermined;if([satisfied,violated,undetermined,total].every(Number.isSafeInteger)&&satisfied>=0&&violated>=0&&undetermined>=0)row.agreementRate=exactRatio(total?Math.max(satisfied,violated,undetermined):0,total||1);}
  return summary;
}
const recordFields=record=>record?.fields&&typeof record.fields==='object'?record.fields:record||{};
const recordValue=(record,key)=>recordFields(record)?.[key]??record?.[key];
const recordId=(record,collection)=>{
  const idField=schema.RECORD_SCHEMAS[collection]?.idField;
  return String(record?.id||record?.recordId||(idField?recordValue(record,idField):'')||'').trim();
};
const isActiveRecord=record=>record?.active!==false&&!record?.invalidatedBy&&!['RETIRED','INVALIDATED','SUPERSEDED'].includes(upper(recordValue(record,'STATUS')||record?.validity));
const records=(project,collection,{stage,active=true}={})=>safe(project?.projectData?.[collection]).filter(record=>(stage===undefined||Number(record?.stage)===Number(stage))&&(!active||isActiveRecord(record)));
const collectionAvailable=name=>Boolean(schema.RECORD_SCHEMAS?.[name]);
const scalarList=value=>Array.isArray(value)?value.map(String).map(x=>x.trim()).filter(Boolean):String(value??'').split(/[;,\n]+/).map(x=>x.trim()).filter(Boolean);
const independenceDimensionList=value=>{const raw=Array.isArray(value)?value:value&&typeof value==='object'?(value.dimensions||value.requiredDimensions||[]):scalarList(value);return safe(raw).map(upper).filter(name=>INDEPENDENCE_DIMENSIONS.includes(name));};
const controlledTruth=value=>TRUTH_VALUES.includes(upper(value))?upper(value):'UNKNOWN';
const controlledFreshness=value=>FRESHNESS_STATES.includes(upper(value))?upper(value):'UNKNOWN';
const controlledBasis=value=>EPISTEMIC_BASES.includes(upper(value))?upper(value):'NONE';
const controlledApplicability=value=>APPLICABILITY_STATES.includes(upper(value))?upper(value):'UNKNOWN';
const currentRecord=(project,collection,predicate=()=>true)=>recordsForCurrentScope(project,collection).filter(predicate).at(-1)||null;

function ensureShape(project){
  if(!project||typeof project!=='object')throw new TypeError('Project must be an object.');
  project.projectData=project.projectData&&typeof project.projectData==='object'?project.projectData:{};
  for(const collection of ALL_COLLECTIONS)project.projectData[collection]=safe(project.projectData[collection]);
  for(const collection of ['backupPolicies','externalCheckpoints','pendingBackupExports','sourceSearchContracts','disclosureDecisions','registeredEncryptionContracts'])project.projectData[collection]=safe(project.projectData[collection]);
  project.projectData.permanentRegistry=project.projectData.permanentRegistry&&typeof project.projectData.permanentRegistry==='object'?project.projectData.permanentRegistry:{};
  project.projectData.stageRecords=project.projectData.stageRecords&&typeof project.projectData.stageRecords==='object'?project.projectData.stageRecords:{};
  project.projectData.userEntered=project.projectData.userEntered&&typeof project.projectData.userEntered==='object'?project.projectData.userEntered:{};
  project.projectData.idCounters=project.projectData.idCounters&&typeof project.projectData.idCounters==='object'?project.projectData.idCounters:{};
  project.projectData.eventSequence=Number.isInteger(project.projectData.eventSequence)?project.projectData.eventSequence:0;
  project.stages=project.stages&&typeof project.stages==='object'?project.stages:{};
  for(let stage=1;stage<=30;stage++){
    const prior=project.stages[stage]||project.stages[String(stage)]||{};
    project.stages[stage]={
      number:stage,status:'NOT STARTED',decision:'',decisionEvidence:'',nextStage:'',decidedBy:'',dateTime:'',
      draftRecord:prior.draftRecord||core.stageTemplate(core.STAGES[stage-1]),responseDraft:'',authorizedFiles:[],humanChecks:{},gateChecks:{},evidenceChecks:{},revisions:[],
      agentData:{},humanData:{},derivedData:{},acceptedData:{},acceptedDataChangeIds:[],acceptedControlEventIds:[],acceptedResponseIds:[],currentPromptId:null,gate:{complete:false,blocked:false,reasons:[]},invalidatedBy:null,
      ...prior,
      number:stage,
      authorizedFiles:safe(prior.authorizedFiles),revisions:safe(prior.revisions),agentData:prior.agentData&&typeof prior.agentData==='object'?prior.agentData:(prior.acceptedData&&typeof prior.acceptedData==='object'?prior.acceptedData:{}),humanData:prior.humanData&&typeof prior.humanData==='object'?prior.humanData:{},derivedData:prior.derivedData&&typeof prior.derivedData==='object'?prior.derivedData:{},acceptedDataChangeIds:safe(prior.acceptedDataChangeIds),acceptedControlEventIds:safe(prior.acceptedControlEventIds),acceptedResponseIds:safe(prior.acceptedResponseIds)
    };
  }
  project.job=project.job&&typeof project.job==='object'?project.job:{};
  project.release={gateState:'',auditedDraft:[],releaseDraft:[],comparisons:[],authorization:'NOT AUTHORIZED',authorizedArtifactIds:[],...(project.release||{})};
  for(const name of ['auditedDraft','releaseDraft','comparisons','authorizedArtifactIds'])project.release[name]=safe(project.release[name]);
  return project;
}

const APPLICATION_INITIAL_FIELDS=Object.freeze({
  requirements:Object.freeze({STATUS:'ACTIVE'}),candidateRequirements:Object.freeze({STATUS:'ACTIVE'}),requirementResolutions:Object.freeze({STATUS:'RESOLVED'}),tests:Object.freeze({STATUS:'READY'}),instructions:Object.freeze({STATUS:'CURRENT'}),instructionTraces:Object.freeze({STATUS:'COMPLETE'}),preflightRecords:Object.freeze({STATUS:'COMPLETE'}),iterations:Object.freeze({STATUS:'FROZEN'}),candidateFreezes:Object.freeze({STATUS:'FROZEN'}),defects:Object.freeze({STATUS:'CONFIRMED'}),regressions:Object.freeze({ACTIVE_RETIRED_STATE:'ACTIVE'}),baselines:Object.freeze({STATUS:'FROZEN'}),products:Object.freeze({STATUS:'RESERVED'}),blockers:Object.freeze({STATUS:'OPEN'}),evidenceRecords:Object.freeze({STATUS:'PRESERVED'})
});
function applicationInitialFields(collection){return clone(APPLICATION_INITIAL_FIELDS[collection]||{});}

function addHistory(project,type,details={}){
  ensureShape(project);
  const eventSequence=++project.projectData.eventSequence;
  const deviceTimestamp=now();
  const event={eventId:`EVENT-${String(eventSequence).padStart(9,'0')}`,eventSequence,deviceTimestamp,createdAt:deviceTimestamp,type,...clone(details)};
  project.projectData.history.push(event);
  return event;
}

function existingIds(project,collection){return new Set(records(project,collection,{active:false}).map(record=>recordId(record,collection)).filter(Boolean));}
function allocateId(project,collection){
  ensureShape(project);
  const definition=schema.RECORD_SCHEMAS[collection];
  if(!definition)throw new Error(`Unknown canonical collection: ${collection}.`);
  const existing=existingIds(project,collection);
  let counter=Math.max(0,Number(project.projectData.idCounters[collection]||0));
  let candidate='';
  do{counter+=1;candidate=`${definition.prefix}-${String(counter).padStart(6,'0')}`;}while(existing.has(candidate));
  project.projectData.idCounters[collection]=counter;
  return candidate;
}
function allocateInfrastructureId(project,prefix,collection){
  ensureShape(project);
  const key=`infra:${collection}:${prefix}`;
  let counter=Math.max(0,Number(project.projectData.idCounters[key]||0));
  const existing=new Set(safe(project.projectData[collection]).map(record=>String(record?.id||record?.rawResponseId||record?.proposalId||record?.validationId||record?.changeId||record?.manifestId||record?.receiptId||record?.promptId||'')).filter(Boolean));
  let candidate='';
  do{counter+=1;candidate=`${prefix}-${String(counter).padStart(6,'0')}`;}while(existing.has(candidate));
  project.projectData.idCounters[key]=counter;
  return candidate;
}

function nextVersion(current,prefix){
  const match=String(current||'').match(/v(\d+)$/i);
  const number=match?Number(match[1])+1:1;
  return `${prefix}-v${String(number).padStart(3,'0')}`;
}
// CURRENT_INPUT_VERSION is versioned only by application-controlled human-input mutation paths.
// Accepting the Stage 01 agent interpretation must not create a new User Job Input version,
// otherwise the accepted intake manifest instantly becomes stale and the user is asked to repeat information.
const VERSION_BY_STAGE=Object.freeze({
  2:['CURRENT_SOURCE_SET_VERSION','SOURCE-SET'],3:['CURRENT_RESEARCH_VERSION','RESEARCH'],
  4:['CURRENT_REQUIREMENTS_VERSION','REQUIREMENTS'],6:['CURRENT_TEST_SUITE_VERSION','TEST-SUITE'],
  7:['CURRENT_MUTATION_SUITE_VERSION','MUTATION-SUITE'],8:['CURRENT_INSTRUCTION_VERSION','INSTRUCTION']
});
function versionCollections(stage){return schema.STAGE_CONTRACTS[stage]?.primaryCollections||[];}
const VERSION_SCOPE_KEY_BY_STAGE=Object.freeze({2:'sourceSetVersion',4:'requirementsVersion',5:'requirementsVersion',6:'testSuiteVersion',8:'instructionVersion'});
function stampCurrentVersionMembership(project,stage,version){const key=VERSION_SCOPE_KEY_BY_STAGE[stage];if(!key||!version)return;const collections=stage===5?[...new Set(['requirements',...versionCollections(stage)])]:versionCollections(stage);for(const collection of collections)for(const record of records(project,collection)){record.scope={...(record.scope||{}),[key]:version};refreshRecordHashes(record,collection);}}
function registerStageVersion(project,stage,acceptedChangeId){
  ensureShape(project);
  const config=VERSION_BY_STAGE[stage];
  if(!config)return null;
  if(Number(stage)===2&&upper(project.stages[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE'){project.job.CURRENT_SOURCE_SET_VERSION='NOT APPLICABLE';return null;}
  const [jobField,prefix]=config;
  const payload={stage,collections:Object.fromEntries(versionCollections(stage).map(collection=>[collection,records(project,collection).map(record=>({id:recordId(record,collection),fields:recordFields(record),relationships:record.relationships||{},sha256:record.sha256||null}))])),acceptedData:project.stages[stage].acceptedData};
  const sha256=hash.sha256Value(payload);
  const latest=safe(project.projectData.artifactVersions).filter(item=>item.stage===stage&&item.kind===prefix).at(-1);
  if(latest?.sha256===sha256){project.job[jobField]=latest.version;stampCurrentVersionMembership(project,stage,latest.version);return latest;}
  const version=nextVersion(project.job[jobField],prefix);
  const record={versionId:allocateInfrastructureId(project,'VERSION','artifactVersions'),stage,kind:prefix,version,sha256,createdAt:now(),acceptedChangeId,payload};
  project.projectData.artifactVersions.push(record);
  project.job[jobField]=version;
  stampCurrentVersionMembership(project,stage,version);
  return record;
}

function unresolvedHumanRequests(project,stage){return safe(project?.projectData?.humanInputRequests).filter(request=>Number(request.stage)===Number(stage)&&upper(request.status||'OPEN')==='OPEN'&&request.blocking!==false);}
function openBlockers(project,stage){
  return records(project,'blockers').filter(blocker=>{
    const status=upper(recordValue(blocker,'STATUS')||blocker.status||'OPEN');
    if(!['OPEN','BLOCKED','IN PROGRESS','UNKNOWN'].includes(status))return false;
    if(stage===undefined)return true;
    const blockerStage=Number(blocker.stage||recordValue(blocker,'STAGE_DISCOVERED')||0);
    if(blockerStage===Number(stage))return true;
    const affected=String(recordValue(blocker,'AFFECTED_ARTIFACTS')||recordValue(blocker,'DOWNSTREAM_WORK_STOPPED')||'');
    return new RegExp(`(?:STAGE\\s*0?${stage}\\b|\\b${stage}\\b)`,`i`).test(affected);
  });
}
function acceptedChanges(project,stage){return safe(project?.projectData?.acceptedChanges).filter(change=>Number(change.stage)===Number(stage)&&change.status==='COMMITTED'&&change.responseType==='DATA_PROPOSAL'&&!change.invalidatedBy);}
function orderedAcceptedOperations(project,stage){
  return acceptedChanges(project,stage).map((change,index)=>({change,index,eventSequence:Number.isSafeInteger(Number(change.eventSequence))?Number(change.eventSequence):index,operation:upper(change.operation||'COMPLETE')})).sort((a,b)=>a.eventSequence-b.eventSequence||a.index-b.index);
}
function acceptedChangePrompt(project,change){const promptId=String(change?.promptId||change?.instructionId||'');return safe(project?.projectData?.generatedPrompts).find(item=>String(item?.instructionId||item?.promptId||'')===promptId)||null;}
function stageOneChallengeRequired(project,sequence){
  if(sequence.some(item=>item.operation==='SEMANTIC_CHALLENGE'))return true;
  if(truth(project?.stages?.[1]?.agentData?.SEMANTIC_CHALLENGE_REQUIRED)||truth(project?.stages?.[1]?.derivedData?.SEMANTIC_CHALLENGE_REQUIRED))return true;
  const relevantPrompts=new Set(sequence.filter(item=>item.operation==='COMPLETE').map(item=>acceptedChangePrompt(project,item.change)).filter(Boolean));
  for(const prompt of safe(project?.projectData?.generatedPrompts))if(Number(prompt?.stage)===1&&!prompt?.invalidatedBy&&upper(prompt?.operation||'COMPLETE')==='COMPLETE')relevantPrompts.add(prompt);
  return [...relevantPrompts].some(prompt=>prompt?.contextManifest?.semanticChallengePlan?.challengeRequired===true);
}
function stageFourChallengeRequirements(project){
  const dispositionIds=[],atomicityIds=[],accounting=evaluateObligationAccounting(project),stageData=project?.stages?.[4]?.agentData||{};
  for(const [obligationId,entries] of accounting.dispositions)if(entries.some(entry=>['inapplicable','retained nonnormative context'].includes(String(entry?.disposition||'').toLowerCase())))dispositionIds.push(obligationId);
  for(const requirement of records(project,'requirements',{stage:4}))if(obligationIdsFromRelationship(recordValue(requirement,'USER_INPUT_RELATIONSHIP')).length>1)atomicityIds.push(recordId(requirement,'requirements'));
  if(safe(stageData.OBLIGATION_DISPOSITION_CHALLENGE_RECORDS).length&&!dispositionIds.length)dispositionIds.push('DECLARED_DISPOSITION_CHALLENGE');
  if(safe(stageData.ATOMICITY_CHALLENGE_RECORDS).length&&!atomicityIds.length)atomicityIds.push('DECLARED_ATOMICITY_CHALLENGE');
  return {dispositionRequired:dispositionIds.length>0,atomicityRequired:atomicityIds.length>0,dispositionIds,atomicityIds};
}
function stageSuboperationClosure(project,stage){
  ensureShape(project);const number=Number(stage),sequence=orderedAcceptedOperations(project,number),reasons=[];
  if(number===1){
    const challenges=sequence.filter(item=>item.operation==='SEMANTIC_CHALLENGE'),challengeRequired=stageOneChallengeRequired(project,sequence),latestChallenge=challenges.at(-1)||null;
    if(challengeRequired&&!latestChallenge)reasons.push('Stage 01 requires a current accepted SEMANTIC_CHALLENGE before intake reconciliation.');
    if(challengeRequired){const controllingSequence=Math.max(latestChallenge?.eventSequence??-1,...sequence.filter(item=>item.operation==='COMPLETE').map(item=>item.eventSequence)),finalEntry=sequence.filter(item=>item.operation==='RECONCILE_INTAKE'&&item.eventSequence>controllingSequence).at(-1)||null;if(!finalEntry)reasons.push('Stage 01 semantic challenge cannot complete intake until a current RECONCILE_INTAKE response is accepted after every current COMPLETE extraction and SEMANTIC_CHALLENGE.');return {stage:number,challengeRequired,sequence,finalEntry,finalChange:finalEntry?.change||null,finalOperation:'RECONCILE_INTAKE',reasons};}
    const finalEntry=sequence.filter(item=>item.operation==='COMPLETE').at(-1)||null;if(!finalEntry)reasons.push('No validated Stage 01 COMPLETE response has been accepted.');return {stage:number,challengeRequired:false,sequence,finalEntry,finalChange:finalEntry?.change||null,finalOperation:'COMPLETE',reasons};
  }
  if(number===4){
    const required=stageFourChallengeRequirements(project),dispositionChallenges=sequence.filter(item=>item.operation==='DISPOSITION_CHALLENGE'),atomicityChallenges=sequence.filter(item=>item.operation==='ATOMICITY_CHALLENGE'),challengeAccepted=dispositionChallenges.length>0||atomicityChallenges.length>0,challengeRequired=required.dispositionRequired||required.atomicityRequired||challengeAccepted;
    if(required.dispositionRequired&&!dispositionChallenges.length)reasons.push('Stage 04 requires a current accepted DISPOSITION_CHALLENGE suboperation for every release-reducing obligation disposition.');
    if(required.atomicityRequired&&!atomicityChallenges.length)reasons.push('Stage 04 requires a current accepted ATOMICITY_CHALLENGE suboperation for every multi-obligation merge or declared atomicity challenge.');
    if(challengeRequired){const controllingSequence=Math.max(...sequence.filter(item=>['COMPLETE','DISPOSITION_CHALLENGE','ATOMICITY_CHALLENGE'].includes(item.operation)).map(item=>item.eventSequence),-1),finalEntry=sequence.filter(item=>item.operation==='RECONCILE_REQUIREMENTS'&&item.eventSequence>controllingSequence).at(-1)||null;if(!finalEntry)reasons.push('Stage 04 challenge work cannot complete requirement compilation until a current RECONCILE_REQUIREMENTS response is accepted after every current compilation and disposition/atomicity challenge.');return {stage:number,challengeRequired,sequence,finalEntry,finalChange:finalEntry?.change||null,finalOperation:'RECONCILE_REQUIREMENTS',requirements:required,reasons};}
    const finalEntry=sequence.filter(item=>item.operation==='COMPLETE').at(-1)||null;if(!finalEntry)reasons.push('No validated Stage 04 COMPLETE response has been accepted.');return {stage:number,challengeRequired:false,sequence,finalEntry,finalChange:finalEntry?.change||null,finalOperation:'COMPLETE',requirements:required,reasons};
  }
  throw new Error('Suboperation closure is defined only for Stages 01 and 04.');
}
function acceptedControlEvents(project,stage){return safe(project?.projectData?.responseDispositions).filter(item=>Number(item.stage)===Number(stage)&&['ACCEPTED_HUMAN_QUESTION_SET','ACCEPTED_BLOCKER_EVENT','ACCEPTED_EXECUTION_FAILURE'].includes(item.type)&&!item.invalidatedBy);}

function hasStageActivity(project,stage){
  if(acceptedChanges(project,stage).length)return true;
  if(safe(project?.projectData?.rawResponses).some(item=>Number(item.stage)===Number(stage)))return true;
  if(unresolvedHumanRequests(project,stage).length)return true;
  if((schema.STAGE_CONTRACTS[stage]?.allowedCollections||[]).some(collection=>records(project,collection,{stage}).length))return true;
  return Boolean(Object.keys(project?.stages?.[stage]?.acceptedData||{}).length);
}
function latestIteration(project,stages=[10,17,19]){
  const candidates=records(project,'iterations').filter(record=>stages.includes(Number(record.stage)));
  return candidates.at(-1)||null;
}
function mandatoryRequirements(project,scopeRule=null){
  const source=scopeRule?recordsForScope(project,'requirements',scopeRule):recordsForCurrentScope(project,'requirements');
  return source.filter(record=>{
    const value=upper(recordValue(record,'MANDATORY_OPTIONAL_STATUS')||recordValue(record,'MANDATORY')||'MANDATORY');
    return value!=='OPTIONAL'&&value!=='FALSE'&&value!=='NO';
  });
}
function confirmedDefects(project,{allScopes=false}={}){
  const source=allScopes?records(project,'defects'):records(project,'defects').filter(record=>!record?.scope||Object.keys(record.scope).length===0||scopeMatches(record,currentScope(project)));
  return source.filter(record=>{
    const status=upper(recordValue(record,'STATUS')||'CONFIRMED');
    return !['REJECTED','DUPLICATE','NOT A DEFECT','RETIRED'].includes(status);
  });
}
function defectResolvedByRegression(project,defect){const defectId=recordId(defect,'defects'),linked=records(project,'regressions').filter(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===defectId&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED');if(!linked.length)return false;const iterationId=recordId(latestIteration(project,[10,17,19]),'iterations')||String(project.job.CURRENT_ITERATION||'').trim(),source=iterationId?currentRegressionExecutions(project,iterationId):records(project,'regressionExecutions');return linked.every(reg=>{const id=recordId(reg,'regressions'),executions=source.filter(x=>String(recordValue(x,'REG_ID')||x.relationships?.REG_ID||'')===id&&upper(recordValue(x,'PHASE'))!=='PRE_CORRECTION');return executions.length===1&&effectiveRegressionDetermination(project,executions[0]).determination==='SATISFIED';});}
function unresolvedMaterialDefects(project){return confirmedDefects(project).filter(record=>{const severity=upper(recordValue(record,'SEVERITY')),status=upper(recordValue(record,'STATUS'));return ['CRITICAL','MAJOR'].includes(severity)&&!['CLOSED','CLOSED VERIFIED','CLOSED_VERIFIED','RESOLVED','CORRECTED AND VERIFIED','RETIRED'].includes(status)&&!defectResolvedByRegression(project,record);});}
function requirementId(record){return recordId(record,'requirements');}
function testRequirementId(record){return String(recordValue(record,'REQ_ID')||record.relationships?.REQ_ID||'');}
function resultRequirementId(project,record){const direct=String(recordValue(record,'REQ_ID')||record.relationships?.REQ_ID||'').trim();if(direct)return direct;const testId=String(recordValue(record,'TEST_ID')||record.relationships?.TEST_ID||'').trim();if(!testId)return '';const test=records(project,'tests').find(item=>recordId(item,'tests')===testId);return test?testRequirementId(test):'';}
function runIterationId(record){return String(recordValue(record,'ITERATION_ID')||record.relationships?.ITERATION_ID||'');}
function verificationKey(record,{includeProposition=false}={}){const values=[recordValue(record,'REQ_ID')||record.relationships?.REQ_ID,recordValue(record,'RUN_ID')||record.relationships?.RUN_ID,recordValue(record,'TEST_ID')||record.relationships?.TEST_ID];if(includeProposition)values.unshift(recordValue(record,'PROPOSITION_ID')||record.relationships?.PROPOSITION_ID);return values.join('|');}
function iterationCandidateId(project,iterationId){const iteration=records(project,'iterations').find(record=>recordId(record,'iterations')===String(iterationId||''));return String(recordValue(iteration,'CANDIDATE_ID')||iteration?.relationships?.CANDIDATE_ID||iteration?.scope?.candidateId||'').trim();}

const SCOPE_KEYS=Object.freeze(['inputVersion','sourceSetVersion','requirementsVersion','testSuiteVersion','instructionVersion','iterationId','candidateId','baselineId','productId']);
const scopeValue=value=>{const text=String(value??'').trim();return !text||['NONE','NOT APPLICABLE','UNKNOWN','PENDING','UNASSIGNED'].includes(text.toUpperCase())?null:value;};
function currentScope(project){return {inputVersion:scopeValue(project.job.CURRENT_INPUT_VERSION),sourceSetVersion:scopeValue(project.job.CURRENT_SOURCE_SET_VERSION),requirementsVersion:scopeValue(project.job.CURRENT_REQUIREMENTS_VERSION),testSuiteVersion:scopeValue(project.job.CURRENT_TEST_SUITE_VERSION),instructionVersion:scopeValue(project.job.CURRENT_INSTRUCTION_VERSION),iterationId:scopeValue(recordId(latestIteration(project,[10,17,19]),'iterations')||project.job.CURRENT_ITERATION),candidateId:scopeValue(recordId(records(project,'candidateFreezes').at(-1),'candidateFreezes')),baselineId:scopeValue(recordId(records(project,'baselines').at(-1),'baselines')||project.job.CURRENT_BASELINE_ID),productId:scopeValue(recordId(records(project,'products').at(-1),'products')||project.job.CURRENT_PRODUCT_ID)};}
function requiredVersionScopeKeys(collection){const stage=Number(schema.RECORD_SCHEMAS[collection]?.stage||0),keys=[];if(stage>=2)keys.push('inputVersion','sourceSetVersion');if(stage>=4)keys.push('requirementsVersion');if(stage>=6)keys.push('testSuiteVersion');if(stage>=8)keys.push('instructionVersion');return keys;}
function scopeMatches(record,scopeRule={},requiredKeys=[]){const rs=record?.scope||{};for(const key of requiredKeys){const expected=scopeRule[key];if(expected===undefined||expected===null)continue;if(!Object.prototype.hasOwnProperty.call(rs,key)||rs[key]===undefined||rs[key]===null||rs[key]!==expected)return false;}for(const [key,value] of Object.entries(rs)){if(value===undefined||value===null)continue;if(Object.prototype.hasOwnProperty.call(scopeRule,key)&&(scopeRule[key]??null)!==value)return false;}return true;}
function recordsForScope(project,collection,scopeRule={}){const applicable=Object.fromEntries(Object.entries(scopeRule||{}).filter(([,v])=>v!==undefined&&v!==null)),required=requiredVersionScopeKeys(collection).filter(key=>Object.prototype.hasOwnProperty.call(applicable,key));return records(project,collection).filter(record=>record.scope&&Object.keys(record.scope).length>0&&scopeMatches(record,applicable,required));}
function recordsForCurrentScope(project,collection,scopeRule={}){return recordsForScope(project,collection,{...currentScope(project),...scopeRule});}
function scopeForIteration(project,iterationId){const id=String(iterationId||''),iteration=records(project,'iterations').find(r=>recordId(r,'iterations')===id);if(!iteration)return {iterationId:id||null};const candidateId=String(recordValue(iteration,'CANDIDATE_ID')||iteration.relationships?.CANDIDATE_ID||iteration.scope?.candidateId||'');return {...clone(iteration.scope||{}),iterationId:id,candidateId:candidateId||null};}
function recordsForIteration(project,collection,iterationId){const id=String(iterationId??'').trim();if(!id)return [];return recordsForScope(project,collection,scopeForIteration(project,id));}
function applicableTests(project,requirement,scopeRule=null){const req=requirementId(requirement),source=scopeRule?recordsForScope(project,'tests',scopeRule):recordsForCurrentScope(project,'tests');return source.filter(test=>testRequirementId(test)===req&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY')));}
function relationshipIds(record,key){
  const values=[];const add=value=>{if(value===undefined||value===null)return;if(Array.isArray(value)){value.forEach(add);return;}if(typeof value==='object'){add(value.recordId??value.id??value.targetId??value.value);return;}const text=String(value).trim();if(text)values.push(text);};
  add(record?.relationships?.[key]);add(recordValue(record,key));return [...new Set(values)];
}
function propositionIdsFor(record,test=null){
  const ids=[];for(const key of ['PROPOSITION_ID','TARGET_PROPOSITION_ID','TARGET_PROPOSITION_IDS']){ids.push(...relationshipIds(record,key));if(test)ids.push(...relationshipIds(test,key));}return [...new Set(ids)];
}
function resultCollectionFor(project,result){for(const collection of ['verification','deterministicResults','meaningResults','adversarialResults','representationInspections','regressionExecutions'])if(records(project,collection,{active:false}).includes(result))return collection;return null;}
function semanticCoverage(test){const value=upper(recordValue(test,'ACCEPTED_SEMANTIC_COVERAGE')||recordValue(test,'SEMANTIC_COVERAGE_DISPOSITION')||recordValue(test,'SEMANTIC_COVERAGE')||'UNKNOWN');return ['EQUIVALENT','PARTIAL','UNKNOWN','NOT_EQUIVALENT'].includes(value)?value:'UNKNOWN';}
function testRole(test){const value=upper(recordValue(test,'TEST_ROLE')||'REQUIRED_PROOF');return ['REQUIRED_PROOF','SUPPORTING_PROOF','ADVISORY','NEGATIVE_ONLY','REGRESSION'].includes(value)?value:'REQUIRED_PROOF';}
function releaseBearingTest(test){const explicit=recordValue(test,'RELEASE_BEARING');if(explicit!==undefined&&explicit!==null&&String(explicit).trim()!=='')return truth(explicit);return ['REQUIRED_PROOF','SUPPORTING_PROOF','REGRESSION'].includes(testRole(test));}
function proofContractForTest(test){
  const evidenceClasses=scalarList(recordValue(test,'REQUIRED_EVIDENCE_CLASSES')).map(upper),epistemicBases=scalarList(recordValue(test,'REQUIRED_EPISTEMIC_BASES')).map(upper),freshness=recordValue(test,'FRESHNESS_REQUIREMENTS'),independence=recordValue(test,'INDEPENDENCE_REQUIREMENTS'),runDimensions=scalarList(recordValue(test,'REQUIRED_RUN_DIMENSIONS')).map(upper),reasons=[],closedObject=(value,keys)=>Boolean(value&&typeof value==='object'&&!Array.isArray(value)&&Object.keys(value).every(key=>keys.includes(key)));
  if(!evidenceClasses.length)reasons.push('REQUIRED_EVIDENCE_CLASSES is empty.');else if(evidenceClasses.some(value=>!REQUIRED_EVIDENCE_CLASSES.includes(value)))reasons.push('REQUIRED_EVIDENCE_CLASSES contains an unknown class.');
  if(!epistemicBases.length)reasons.push('REQUIRED_EPISTEMIC_BASES is empty.');else if(epistemicBases.some(value=>!EPISTEMIC_BASES.includes(value)||value==='NONE'))reasons.push('REQUIRED_EPISTEMIC_BASES contains an unknown or non-proving basis.');
  if(!closedObject(freshness,['status'])||!FRESHNESS_REQUIREMENT_STATES.includes(upper(freshness?.status)))reasons.push('FRESHNESS_REQUIREMENTS must be the closed {status} contract.');
  const independenceDimensions=closedObject(independence,['dimensions'])?safe(independence.dimensions).map(upper):[];if(!closedObject(independence,['dimensions'])||!independenceDimensions.length||independenceDimensions.some(value=>!INDEPENDENCE_DIMENSIONS.includes(value)))reasons.push('INDEPENDENCE_REQUIREMENTS must contain a nonempty closed dimensions list.');
  if(!runDimensions.length)reasons.push('REQUIRED_RUN_DIMENSIONS is empty.');else if(runDimensions.some(value=>!REQUIRED_RUN_DIMENSIONS.includes(value)))reasons.push('REQUIRED_RUN_DIMENSIONS contains an unknown dimension.');
  return {complete:reasons.length===0,reasons:[...new Set(reasons)],evidenceClasses:[...new Set(evidenceClasses)],epistemicBases:[...new Set(epistemicBases)],freshness:{status:upper(freshness?.status)},independence:{dimensions:[...new Set(independenceDimensions)]},runDimensions:[...new Set(runDimensions)]};
}
function freshnessEvaluation(record){
  const reasons=[];let status=controlledFreshness(recordValue(record,'FRESHNESS_STATUS'));
  const validUntil=String(recordValue(record,'VALID_UNTIL')||'').trim(),requires=upper(recordValue(record,'FRESHNESS_REQUIREMENT')||'');
  if(status==='UNKNOWN'&&validUntil){const end=Date.parse(validUntil);if(Number.isFinite(end))status=Date.now()<=end?'CURRENT':'EXPIRED';else reasons.push('validUntil is not a parseable declared time value.');}
  if(status==='UNKNOWN'&&['NONE','NOT_APPLICABLE','NOT APPLICABLE'].includes(requires))status='NOT_APPLICABLE';
  if(status==='EXPIRED')reasons.push('Required evidence or dependency is expired.');
  if(status==='UNKNOWN'&&requires&&!['NONE','NOT_APPLICABLE','NOT APPLICABLE'].includes(requires))reasons.push('Required freshness cannot be established.');
  return {status,reasons,observedAtBasis:upper(recordValue(record,'OBSERVED_AT_BASIS')||'DEVICE_REPORTED')||'DEVICE_REPORTED'};
}
function parsedEvidenceContent(record){
  const raw=recordValue(record,'CONTENT')??recordValue(record,'APPLICATION_EVIDENCE_CONTENT');if(raw&&typeof raw==='object'&&!Array.isArray(raw))return clone(raw);if(typeof raw!=='string'||!raw.trim())return null;try{const value=JSON.parse(raw);return value&&typeof value==='object'&&!Array.isArray(value)?value:null;}catch{return null;}
}
function acceptedOperationChange(project,stage,operation){return acceptedChanges(project,stage).filter(change=>upper(change.operation||'COMPLETE')===upper(operation)).at(-1)||null;}
function currentStageEvidence(project,stage,kind,operation=null){
  const latest=operation?acceptedOperationChange(project,stage,operation):acceptedChanges(project,stage).at(-1),proposalId=String(latest?.proposalId||''),rawResponseId=String(latest?.rawResponseId||'');if(!latest)return [];return records(project,'evidenceRecords').filter(record=>Number(record.stage)===Number(stage)).filter(record=>String(record.sourceProposalId||'')===proposalId||String(record.rawResponseId||'')===rawResponseId).filter(record=>upper(recordValue(record,'KIND')||recordValue(record,'APPLICATION_EVIDENCE_KIND'))===upper(kind));
}
const SOURCE_CANDIDATE_DISPOSITIONS=Object.freeze(['ADMITTED_SOURCE','NOT_APPLICABLE','DUPLICATE','OUT_OF_SCOPE','BLOCKED']);
const SOURCE_SEARCH_CONTRACT_FIELDS=Object.freeze(['SEARCH_UNIVERSE','SEARCH_PROCEDURE','SEARCH_LOCATIONS','SEARCH_QUERIES_OR_STRATEGIES','SEARCH_CUTOFF','SEARCH_LIMITATIONS','SEARCH_EXECUTION_EVIDENCE','DISCOVERY_RISK']);
function sourceSearchContractDefinition(project,data=project.stages?.[2]?.agentData||{}){
  const contractPayload=Object.fromEntries(SOURCE_SEARCH_CONTRACT_FIELDS.map(field=>[field,clone(data[field]??null)])),identityPayload={jobId:String(project.job?.JOB_ID||''),inputVersion:String(project.job?.CURRENT_INPUT_VERSION||''),sourceSetVersion:String(project.job?.CURRENT_SOURCE_SET_VERSION||''),contractPayload},contractId=`SOURCE-SEARCH-${hash.sha256Value(identityPayload).slice(0,20).toUpperCase()}`;return {contractId,contractPayload,identityPayload};
}
function stage2SearchContext(project,submittedIdentity){
  const value=String(submittedIdentity||'').trim();if(!value)return {record:null,matches:[]};const currentInput=String(project.job?.CURRENT_INPUT_VERSION||''),matches=records(project,'freshContexts',{active:false}).filter(isActiveRecord).filter(record=>Number(record.stage)===2).filter(record=>!currentInput||!record.scope?.inputVersion||String(record.scope.inputVersion)===currentInput).filter(record=>recordId(record,'freshContexts')===value||String(recordValue(record,'EXTERNAL_CONTEXT_IDENTIFIER')||'')===value);return {record:matches.length===1?matches[0]:null,matches};
}
function acceptedOperationReservationId(project,change){const proposal=safe(project?.projectData?.responseProposals).find(item=>String(item?.proposalId||'')===String(change?.proposalId||''));return String(proposal?.envelope?.operationBinding?.operationReservationId||'');}
function acceptedOperationReservation(project,change,operation){const proposal=safe(project?.projectData?.responseProposals).find(item=>String(item?.proposalId||'')===String(change?.proposalId||'')),reservationId=String(proposal?.envelope?.operationBinding?.operationReservationId||''),record=records(project,'operationReservations',{active:false}).find(item=>recordId(item,'operationReservations')===reservationId),boundScope=proposal?.envelope?.scope||change?.scope||{},valid=Boolean(record&&upper(recordValue(record,'STATUS')||record.status)==='ACCEPTED'&&Number(recordValue(record,'STAGE'))===2&&upper(recordValue(record,'OPERATION'))===upper(operation)&&String(recordValue(record,'SCOPE_HASH')||'')===hash.sha256Value(boundScope));return {reservationId,record,valid};}
function currentSourceSearchContract(project,contractId=null){
  const expected=String(contractId||sourceSearchContractDefinition(project).contractId),matches=safe(project.projectData?.sourceSearchContracts).filter(record=>record?.active!==false&&String(record?.contractId||'')===expected);return {record:matches.length===1?matches[0]:null,matches};
}
function synchronizeSourceSearchContract(project,{proposalId=null,rawResponseId=null,promptId=null,changeId=null,changes=[]}={}){
  ensureShape(project);const data=project.stages?.[2]?.agentData||{},definition=sourceSearchContractDefinition(project,data),searchChange=acceptedOperationChange(project,2,'SEARCH_EXECUTION'),reviewChange=acceptedOperationChange(project,2,'ADEQUACY_REVIEW'),executions=currentStageEvidence(project,2,'SOURCE_SEARCH_EXECUTION','SEARCH_EXECUTION'),reviews=currentStageEvidence(project,2,'SOURCE_SEARCH_ADEQUACY_REVIEW','ADEQUACY_REVIEW'),executionRecord=executions.length===1?executions[0]:null,reviewRecord=reviews.length===1?reviews[0]:null,execution=parsedEvidenceContent(executionRecord)||{},review=parsedEvidenceContent(reviewRecord)||{},performer=stage2SearchContext(project,searchChange?.scope?.contextId),reviewer=stage2SearchContext(project,reviewChange?.scope?.contextId),performerContextId=recordId(performer.record,'freshContexts')||null,reviewerContextId=recordId(reviewer.record,'freshContexts')||null,independence=performerContextId&&reviewerContextId?evaluateContextIndependence(project,{role:'SOURCE_SEARCH_ADEQUACY',reviewerContextId,generatorContextId:performerContextId,requiredDimensions:['APPLICATION_SESSION_DISTINCTNESS','APPLICATION_INPUT_ISOLATION','EXECUTOR_OR_REVIEWER_ROLE_SEPARATION']}):{determination:'UNKNOWN',reasons:['The source-search performer and adequacy reviewer do not both resolve to one current application context.'],dimensions:{}};
  const body={schema:'closed-loop-source-search-contract/1',contractId:definition.contractId,jobId:String(project.job?.JOB_ID||''),inputVersion:String(project.job?.CURRENT_INPUT_VERSION||''),sourceSetVersion:String(project.job?.CURRENT_SOURCE_SET_VERSION||''),contractPayload:definition.contractPayload,executionEvidenceId:recordId(executionRecord,'evidenceRecords')||null,adequacyReviewId:recordId(reviewRecord,'evidenceRecords')||null,searchExecutionChangeId:String(searchChange?.changeId||''),adequacyReviewChangeId:String(reviewChange?.changeId||''),searchExecutionReservationId:acceptedOperationReservationId(project,searchChange),adequacyReviewReservationId:acceptedOperationReservationId(project,reviewChange),searchPerformerContextId:performerContextId,reviewerContextId,searchPerformerExternalContextId:String(recordValue(performer.record,'EXTERNAL_CONTEXT_IDENTIFIER')||''),reviewerExternalContextId:String(recordValue(reviewer.record,'EXTERNAL_CONTEXT_IDENTIFIER')||''),independence:clone(independence),proposalId:String(proposalId||reviewRecord?.sourceProposalId||executionRecord?.sourceProposalId||''),rawResponseId:String(rawResponseId||reviewRecord?.rawResponseId||executionRecord?.rawResponseId||''),promptId:String(promptId||''),acceptedChangeId:String(changeId||'')},contractSha256=hash.sha256Value(body),existing=currentSourceSearchContract(project,definition.contractId).record;
  if(existing&&String(existing.contractSha256||'')===contractSha256)return existing;for(const prior of safe(project.projectData.sourceSearchContracts).filter(item=>item.active!==false)){prior.active=false;prior.status='SUPERSEDED';prior.supersededBy=definition.contractId;}const contract={contractId:definition.contractId,contractSha256,body,scope:currentScope(project),active:true,status:'CURRENT',createdAt:now(),source:'APPLICATION_DERIVATION'};project.projectData.sourceSearchContracts.push(contract);
  const bind=(record,role,contextId)=>{if(!record)return;applyApplicationConsequences(record,'evidenceRecords',{EPISTEMIC_BASIS:'SELF_ASSERTED',FRESHNESS_STATUS:'CURRENT',SOURCE_SEARCH_CONTRACT_ID:definition.contractId,SOURCE_SEARCH_CONTRACT_SHA256:contractSha256,SOURCE_SEARCH_CONTEXT_ID:contextId,SOURCE_SEARCH_ROLE:role},contextId?{SOURCE_SEARCH_CONTEXT_ID:contextId}:null,changes,'stage02.sourceSearchEvidence');};bind(executionRecord,'SEARCH_EXECUTION',performerContextId);bind(reviewRecord,'ADEQUACY_REVIEW',reviewerContextId);return contract;
}
function sourceSearchAssessment(project){
  ensureShape(project);
  const data=project.stages?.[2]?.agentData||{},reasons=[],definition=sourceSearchContractDefinition(project,data),searchChange=acceptedOperationChange(project,2,'SEARCH_EXECUTION'),reviewChange=acceptedOperationChange(project,2,'ADEQUACY_REVIEW');
  for(const field of SOURCE_SEARCH_CONTRACT_FIELDS)if(!explicitlyRecorded(data[field]))reasons.push(`Stage 02 bounded source-search contract is missing ${field}.`);
  if(!searchChange)reasons.push('A current accepted SEARCH_EXECUTION suboperation is required.');
  if(!reviewChange)reasons.push('A distinct current accepted ADEQUACY_REVIEW suboperation is required after SEARCH_EXECUTION.');
  if(searchChange&&reviewChange&&Number(reviewChange.eventSequence||0)<=Number(searchChange.eventSequence||0))reasons.push('ADEQUACY_REVIEW must be accepted after the current SEARCH_EXECUTION.');
  const executions=currentStageEvidence(project,2,'SOURCE_SEARCH_EXECUTION','SEARCH_EXECUTION'),executionRecord=executions.length===1?executions[0]:null,execution=executionRecord?parsedEvidenceContent(executionRecord):null;
  if(executions.length!==1)reasons.push(`Exactly one current SOURCE_SEARCH_EXECUTION evidence record is required; found ${executions.length}.`);
  if(!execution?.executed||upper(execution?.status||execution?.executionStatus)!=='COMPLETE')reasons.push('The bounded source search lacks affirmative COMPLETE execution evidence for the declared procedure.');
  const performer=stage2SearchContext(project,searchChange?.scope?.contextId),performerContextId=recordId(performer.record,'freshContexts')||null,performerExternalId=String(recordValue(performer.record,'EXTERNAL_CONTEXT_IDENTIFIER')||'');
  if(performer.matches.length!==1)reasons.push('SEARCH_EXECUTION is not bound through its accepted operation scope to exactly one current application-recorded search context.');
  if(execution&&String(execution.searchPerformerContextId||'')!==performerExternalId)reasons.push('SOURCE_SEARCH_EXECUTION reported a performer context different from its application-reserved accepted context.');
  const expectedUserSourcesHash=hash.sha256Value(String(project.job?.KNOWN_AUTHORITATIVE_SOURCES||''));if(String(execution?.userIdentifiedSourcesInputSha256||'')!==expectedUserSourcesHash)reasons.push('SOURCE_SEARCH_EXECUTION is not bound to the exact current user-identified source input.');
  const executionEvidenceId=recordId(executionRecord,'evidenceRecords'),declaredExecutionEvidence=scalarList(data.SEARCH_EXECUTION_EVIDENCE),permittedExecutionRefs=new Set([executionEvidenceId,String(executionRecord?.temporaryKey||'')].filter(Boolean));if(executionEvidenceId&&!declaredExecutionEvidence.some(value=>permittedExecutionRefs.has(value)))reasons.push('SEARCH_EXECUTION_EVIDENCE must reference the current search-execution evidence temporary key or its application-allocated canonical identity.');
  const candidates=Array.isArray(execution?.discoveredCandidates)?execution.discoveredCandidates:[],candidateKeys=new Set(),admitted=[];for(let index=0;index<candidates.length;index++){const candidate=candidates[index]&&typeof candidates[index]==='object'&&!Array.isArray(candidates[index])?candidates[index]:{},key=String(candidate.candidateKey||candidate.sourceKey||'').trim(),title=String(candidate.title||'').trim(),location=String(candidate.retrievalLocation||candidate.url||'').trim(),disposition=upper(candidate.disposition),reason=String(candidate.reason||'').trim();if(!key||candidateKeys.has(key))reasons.push(`Discovered source candidate ${index+1} lacks a unique candidateKey.`);if(key)candidateKeys.add(key);if(!title||!location)reasons.push(`Discovered source candidate ${key||index+1} lacks title or retrieval location.`);if(!SOURCE_CANDIDATE_DISPOSITIONS.includes(disposition)||!reason)reasons.push(`Discovered source candidate ${key||index+1} lacks a controlled disposition and substantive reason.`);if(disposition==='ADMITTED_SOURCE')admitted.push(candidate);if(disposition==='BLOCKED')reasons.push(`Discovered source candidate ${key||index+1} remains blocked.`);}
  const sources=recordsForCurrentScope(project,'sources').filter(record=>Number(record.stage)===2),sourceMatches=candidate=>sources.some(source=>String(recordValue(source,'TITLE')||'').trim()===String(candidate.title||'').trim()&&String(recordValue(source,'URL_REFERENCE')||'').trim()===String(candidate.retrievalLocation||candidate.url||'').trim());for(const candidate of admitted)if(!sourceMatches(candidate))reasons.push(`Admitted discovered source ${candidate.candidateKey||candidate.title||'UNKNOWN'} has no matching current canonical source record.`);if(sources.length!==admitted.length)reasons.push(`Current source records (${sources.length}) do not reconcile one-to-one with admitted discovered candidates (${admitted.length}).`);
  const reviews=currentStageEvidence(project,2,'SOURCE_SEARCH_ADEQUACY_REVIEW','ADEQUACY_REVIEW'),reviewRecord=reviews.length===1?reviews[0]:null,review=reviewRecord?parsedEvidenceContent(reviewRecord):null;if(reviews.length!==1)reasons.push(`Exactly one current SOURCE_SEARCH_ADEQUACY_REVIEW evidence record is required; found ${reviews.length}.`);if(upper(review?.determination)!=='ADEQUATE')reasons.push('The bounded source-search procedure lacks an affirmative independent adequacy determination.');
  const reviewer=stage2SearchContext(project,reviewChange?.scope?.contextId),reviewerContextId=recordId(reviewer.record,'freshContexts')||null,reviewerExternalId=String(recordValue(reviewer.record,'EXTERNAL_CONTEXT_IDENTIFIER')||''),searchReservation=acceptedOperationReservation(project,searchChange,'SEARCH_EXECUTION'),reviewReservation=acceptedOperationReservation(project,reviewChange,'ADEQUACY_REVIEW'),searchReservationId=searchReservation.reservationId,reviewReservationId=reviewReservation.reservationId;
  if(reviewer.matches.length!==1||!performerContextId||!reviewerContextId||reviewerContextId===performerContextId||!searchReservation.valid||!reviewReservation.valid||searchReservationId===reviewReservationId)reasons.push('Source-search adequacy review is not independently bound to two distinct accepted application reservations and distinct current application-recorded contexts.');
  if(review&&(String(review.reviewerContextId||'')!==reviewerExternalId||String(review.searchPerformerContextId||'')!==performerExternalId))reasons.push('The adequacy review reported context identities that differ from the two accepted application reservations.');
  const independence=performerContextId&&reviewerContextId?evaluateContextIndependence(project,{role:'SOURCE_SEARCH_ADEQUACY',reviewerContextId,generatorContextId:performerContextId,requiredDimensions:['APPLICATION_SESSION_DISTINCTNESS','APPLICATION_INPUT_ISOLATION','EXECUTOR_OR_REVIEWER_ROLE_SEPARATION']}):{determination:'UNKNOWN',dimensions:{}};if(independence.determination!=='APPLICATION_ESTABLISHED')reasons.push('Source-search adequacy review application-controlled separation is not established.');if(Number(review?.discoveredCandidateCount)!==candidates.length||review?.reviewedCandidateDispositions!==true)reasons.push('The adequacy review does not account for every discovered candidate disposition.');
  const materiality=value=>{const normalized=upper(value&&typeof value==='object'?value.materiality:value);return ['MATERIAL','NONMATERIAL','UNKNOWN'].includes(normalized)?normalized:'UNKNOWN';},contractRiskMateriality=materiality(data.DISCOVERY_RISK),reviewRiskMateriality=materiality(review?.discoveryRiskMateriality),riskContradicted=contractRiskMateriality!==reviewRiskMateriality,riskMateriality=[contractRiskMateriality,reviewRiskMateriality].includes('MATERIAL')?'MATERIAL':[contractRiskMateriality,reviewRiskMateriality].includes('UNKNOWN')?'UNKNOWN':'NONMATERIAL';
  if(riskContradicted)reasons.push(`Source-discovery risk materiality is contradicted between the search contract (${contractRiskMateriality}) and adequacy review (${reviewRiskMateriality}); the application applies the worse current consequence.`);if(riskMateriality!=='NONMATERIAL')reasons.push('Unresolved source-discovery risk is MATERIAL or UNKNOWN; Stage 02 must fail closed.');if(!String(review?.reason||'').trim())reasons.push('Source-search adequacy review lacks substantive reasoning.');
  const contractMatches=currentSourceSearchContract(project,definition.contractId),contract=contractMatches.record;if(contractMatches.matches.length!==1)reasons.push(`Exactly one current application-owned SOURCE_SEARCH_CONTRACT is required; found ${contractMatches.matches.length}.`);if(contract){if(String(contract.contractSha256||'')!==hash.sha256Value(contract.body||{}))reasons.push('The current SOURCE_SEARCH_CONTRACT integrity hash is invalid.');if(String(contract.body?.inputVersion||'')!==String(project.job?.CURRENT_INPUT_VERSION||'')||String(contract.body?.sourceSetVersion||'')!==String(project.job?.CURRENT_SOURCE_SET_VERSION||''))reasons.push('The current SOURCE_SEARCH_CONTRACT is stale.');if(String(contract.body?.executionEvidenceId||'')!==String(executionEvidenceId||'')||String(contract.body?.adequacyReviewId||'')!==String(recordId(reviewRecord,'evidenceRecords')||''))reasons.push('The current SOURCE_SEARCH_CONTRACT is not bound to the exact execution and adequacy evidence.');if(String(contract.body?.searchExecutionChangeId||'')!==String(searchChange?.changeId||'')||String(contract.body?.adequacyReviewChangeId||'')!==String(reviewChange?.changeId||'')||String(contract.body?.searchExecutionReservationId||'')!==searchReservationId||String(contract.body?.adequacyReviewReservationId||'')!==reviewReservationId)reasons.push('The current SOURCE_SEARCH_CONTRACT is not bound to the exact two accepted operations and reservations.');if(String(contract.body?.searchPerformerContextId||'')!==String(performerContextId||'')||String(contract.body?.reviewerContextId||'')!==String(reviewerContextId||''))reasons.push('The current SOURCE_SEARCH_CONTRACT is not bound to the exact application context identities.');for(const [record,role,contextId] of [[executionRecord,'SEARCH_EXECUTION',performerContextId],[reviewRecord,'ADEQUACY_REVIEW',reviewerContextId]])if(record&&(String(recordValue(record,'SOURCE_SEARCH_CONTRACT_ID')||'')!==definition.contractId||String(recordValue(record,'SOURCE_SEARCH_CONTRACT_SHA256')||'')!==String(contract.contractSha256||'')||upper(recordValue(record,'SOURCE_SEARCH_ROLE'))!==role||String(recordValue(record,'SOURCE_SEARCH_CONTEXT_ID')||'')!==String(contextId||'')))reasons.push(`The ${role} evidence lacks its exact application-owned contract and context binding.`);}
  const determination=upper(data.SOURCE_APPLICABILITY_DETERMINATION);if(determination==='NO_APPLICABLE_EXTERNAL_SOURCE'&&(sources.length||admitted.length||candidates.some(candidate=>upper(candidate?.disposition)==='BLOCKED')))reasons.push('NO_APPLICABLE_EXTERNAL_SOURCE is inconsistent with admitted, blocked, or unresolved discovered candidates.');return {complete:reasons.length===0,reasons:[...new Set(reasons)],contractId:definition.contractId,contractSha256:contract?.contractSha256||null,executionEvidenceId:executionEvidenceId||null,adequacyReviewId:recordId(reviewRecord,'evidenceRecords')||null,searchExecutionChangeId:searchChange?.changeId||null,adequacyReviewChangeId:reviewChange?.changeId||null,searchPerformerContextId:performerContextId,reviewerContextId,candidateCount:candidates.length,admittedCount:admitted.length,contractDiscoveryRiskMateriality:contractRiskMateriality,reviewDiscoveryRiskMateriality:reviewRiskMateriality,discoveryRiskContradicted:riskContradicted,discoveryRiskMateriality:riskMateriality,independenceBasis:independence.determination,independence};
}
function normalizeSourceConsequences(project){
  ensureShape(project);const allEvidence=new Map(recordsForCurrentScope(project,'evidenceRecords').map(record=>[recordId(record,'evidenceRecords'),record])),artifacts=recordsForCurrentScope(project,'artifacts'),summary=[];
  for(const source of recordsForCurrentScope(project,'sources')){
    const id=recordId(source,'sources'),linked=[...new Set([...(source.evidenceRefs||[]),...relationshipIds(source,'EVIDENCE_IDS')])].map(value=>allEvidence.get(String(value))).filter(Boolean),claims=linked.filter(record=>['SOURCE_AUTHENTICITY_EVIDENCE','SOURCE_FRESHNESS_EVIDENCE','SOURCE_AUTHENTICITY_AND_FRESHNESS'].includes(upper(recordValue(record,'KIND')||recordValue(record,'APPLICATION_EVIDENCE_KIND')))).map(record=>({record,payload:parsedEvidenceContent(record)})).filter(item=>item.payload).sort((left,right)=>String(recordId(left.record,'evidenceRecords')).localeCompare(String(recordId(right.record,'evidenceRecords')))),sourcePublisher=String(recordValue(source,'CLAIMED_PUBLISHER_IDENTITY')||recordValue(source,'ISSUING_ORGANIZATION_OR_AUTHOR')||'').trim(),publisherClaims=[...new Set(claims.map(item=>String(item.payload?.authenticity?.claimedPublisherIdentity||item.payload?.claimedPublisherIdentity||'').trim()).filter(Boolean).map(upper))],authenticityClaims=claims.map(item=>upper(item.payload?.authenticity?.status||item.payload?.authenticityStatus)).filter(Boolean),authenticityContradicted=authenticityClaims.includes('CONTRADICTED')||claims.some(item=>item.payload?.authenticity?.established===false)||publisherClaims.length>1||Boolean(sourcePublisher&&publisherClaims.length&&publisherClaims.some(value=>value!==upper(sourcePublisher))),hasAuthenticityClaim=claims.some(item=>item.payload?.authenticity||item.payload?.claimedPublisherIdentity||item.payload?.retrievalReceipt),localHash=String(recordValue(source,'LOCAL_COPY_SHA256')||'').toLowerCase(),matchingArtifacts=localHash?artifacts.filter(artifact=>upper(recordValue(artifact,'AVAILABILITY'))==='BYTES_PERSISTED_AND_VERIFIED'&&String(recordValue(artifact,'SHA256')||'').toLowerCase()===localHash):[];
    // No current adapter independently verifies publisher identity. Parsing an
    // agent response or finding locally matching bytes cannot elevate the
    // external-world authenticity observation above SELF_ASSERTED.
    let authenticityStatus='UNKNOWN',authenticityBasis='NONE';if(authenticityContradicted){authenticityStatus='CONTRADICTED';authenticityBasis='SELF_ASSERTED';}else if(hasAuthenticityClaim||sourcePublisher){authenticityStatus='SELF_ASSERTED';authenticityBasis='SELF_ASSERTED';}
    const freshnessValues=[];for(const item of claims){const value=item.payload?.freshness||item.payload,status=controlledFreshness(value?.status||value?.freshnessStatus);if(status!=='UNKNOWN'&&String(value?.observedAt||value?.validUntil||value?.reason||'').trim())freshnessValues.push(status);}const supersession=upper(recordValue(source,'SUPERSESSION_STATUS')),currency=upper(recordValue(source,'CURRENCY_STATUS'));if(['SUPERSEDED','EXPIRED','OBSOLETE','NO LONGER CURRENT','NOT CURRENT'].includes(supersession)||['SUPERSEDED','EXPIRED','OBSOLETE','NO LONGER CURRENT','NOT CURRENT'].includes(currency))freshnessValues.push('EXPIRED');if(['CURRENT','IN FORCE','ACTIVE'].includes(currency)||['NOT SUPERSEDED','CURRENT','ACTIVE'].includes(supersession))freshnessValues.push('CURRENT');const freshnessSet=new Set(freshnessValues),freshnessContradictionStatus=freshnessSet.size>1?'CONTRADICTED':'CLEAR';let freshnessStatus='UNKNOWN';if(freshnessContradictionStatus==='CLEAR'&&freshnessSet.size===1)freshnessStatus=[...freshnessSet][0];const freshnessBasis=freshnessSet.size?'SELF_ASSERTED':'NONE';
    const evidenceIds=claims.map(item=>recordId(item.record,'evidenceRecords')),authenticityReasons=authenticityContradicted?['Current source-authenticity claims conflict.']:authenticityStatus==='UNKNOWN'?['Current source authenticity has no admissible affirmative evidence.']:authenticityStatus==='SELF_ASSERTED'?['Publisher authenticity is externally asserted and is not application-verified.']:[],freshnessReasons=freshnessContradictionStatus==='CONTRADICTED'?['Current source-freshness claims conflict.']:freshnessStatus==='UNKNOWN'?['Current source freshness cannot be established.']:freshnessStatus==='EXPIRED'?['Current source evidence establishes that the source is expired or superseded.']:[],authenticityDerivationId='SOURCE-AUTH-'+hash.sha256Value({sourceId:id,evidenceIds,authenticityStatus,authenticityBasis,authenticityContradicted}).slice(0,20).toUpperCase(),freshnessDerivationId='SOURCE-FRESH-'+hash.sha256Value({sourceId:id,evidenceIds,freshnessStatus,freshnessBasis,freshnessContradictionStatus}).slice(0,20).toUpperCase();
    const consequence={BYTE_IDENTITY_STATUS:matchingArtifacts.length===1?'APPLICATION_VERIFIED_STORED_BYTES':localHash?'UNVERIFIED_CLAIM':'NOT_AVAILABLE',AUTHENTICITY_BASIS:authenticityBasis,AUTHENTICITY_STATUS:authenticityStatus,AUTHENTICITY_TRUTH_VALUE:authenticityStatus==='CONTRADICTED'||authenticityStatus==='UNKNOWN'?'UNKNOWN':'TRUE',CURRENT_SCOPE_STATUS:'CURRENT',AUTHENTICITY_CONTRADICTION_STATUS:authenticityContradicted?'CONTRADICTED':'CLEAR',AUTHENTICITY_REASONS:authenticityReasons,AUTHENTICITY_EVIDENCE_IDS:evidenceIds,AUTHENTICITY_DERIVATION_ID:authenticityDerivationId,FRESHNESS_STATUS:freshnessStatus,FRESHNESS_BASIS:freshnessBasis,FRESHNESS_CONTRADICTION_STATUS:freshnessContradictionStatus,FRESHNESS_REASONS:freshnessReasons,FRESHNESS_EVIDENCE_IDS:evidenceIds,FRESHNESS_DERIVATION_ID:freshnessDerivationId},prior=Object.fromEntries(Object.keys(consequence).map(key=>[key,recordValue(source,key)??null]));if(hash.sha256Value(prior)!==hash.sha256Value(consequence)){source.fields={...recordFields(source),...consequence};Object.assign(source,consequence);refreshRecordHashes(source,'sources');}summary.push({sourceId:id,...consequence,freshnessBasis,freshnessContradictionStatus,evidenceIds});
  }
  return summary;
}
function sourceIdsForRequirement(project,requirement,proposition=null){
  const ids=new Set(),add=value=>{for(const id of safe(value).flatMap(item=>Array.isArray(item)?item:[item]).map(item=>String(item??'').trim()).filter(Boolean))ids.add(id);};
  add(relationshipIds(requirement,'SOURCE_ID'));add(relationshipIds(requirement,'SOURCE_IDS'));add(relationshipIds(proposition,'SOURCE_ID'));add(relationshipIds(proposition,'SOURCE_IDS'));
  const obligationIds=new Set(obligationIdsFromRelationship(recordValue(requirement,'USER_INPUT_RELATIONSHIP'))),manifest=obligationManifest(project);for(const item of manifest.items)if(obligationIds.has(item.obligationId)&&item.sourceIdentity)add([item.sourceIdentity]);
  return [...ids].sort();
}
function evaluateSourceDependencies(project,{sourceIds=[],requiredEpistemicBases=[]}={}){
  ensureShape(project);const ids=[...new Set(safe(sourceIds).map(String).filter(Boolean))].sort(),requiredBases=[...new Set(safe(requiredEpistemicBases).map(upper).filter(value=>EPISTEMIC_BASES.includes(value)&&value!=='NONE'))],normalized=new Map(normalizeSourceConsequences(project).map(item=>[item.sourceId,item])),requirements=[],reasons=[],evidenceIds=[];
  for(const sourceId of ids){const source=normalized.get(sourceId);if(!source){requirements.push({sourceId,exists:false,authenticityStatus:'UNKNOWN',authenticityBasis:'NONE',authenticityContradictionStatus:'CLEAR',freshnessStatus:'UNKNOWN',freshnessBasis:'NONE',freshnessContradictionStatus:'CLEAR',basisMeetsContract:false,evidenceIds:[]});reasons.push(`Required source ${sourceId} is missing or stale.`);continue;}
    const authenticityStatus=upper(source.AUTHENTICITY_STATUS)||'UNKNOWN',authenticityBasis=controlledBasis(source.AUTHENTICITY_BASIS),authenticityContradictionStatus=upper(source.AUTHENTICITY_CONTRADICTION_STATUS)||'CLEAR',freshnessStatus=controlledFreshness(source.FRESHNESS_STATUS),freshnessBasis=controlledBasis(source.FRESHNESS_BASIS||source.freshnessBasis),freshnessContradictionStatus=upper(source.FRESHNESS_CONTRADICTION_STATUS||source.freshnessContradictionStatus)||'CLEAR',sourceEvidenceIds=[...new Set([...(source.AUTHENTICITY_EVIDENCE_IDS||[]),...(source.FRESHNESS_EVIDENCE_IDS||[]),...(source.evidenceIds||[])].map(String).filter(Boolean))].sort(),basisMeetsContract=!requiredBases.length||requiredBases.includes(authenticityBasis)&&(freshnessStatus==='NOT_APPLICABLE'||requiredBases.includes(freshnessBasis));
    evidenceIds.push(...sourceEvidenceIds);requirements.push({sourceId,exists:true,authenticityStatus,authenticityBasis,authenticityContradictionStatus,freshnessStatus,freshnessBasis,freshnessContradictionStatus,basisMeetsContract,evidenceIds:sourceEvidenceIds});
    if(authenticityStatus==='CONTRADICTED'||authenticityContradictionStatus==='CONTRADICTED')reasons.push(`Required source ${sourceId} has contradictory authenticity evidence.`);else if(authenticityStatus==='UNKNOWN')reasons.push(`Required source ${sourceId} authenticity is UNKNOWN.`);
    if(freshnessContradictionStatus==='CONTRADICTED')reasons.push(`Required source ${sourceId} has contradictory freshness evidence.`);else if(freshnessStatus==='EXPIRED')reasons.push(`Required source ${sourceId} is EXPIRED.`);else if(freshnessStatus==='UNKNOWN')reasons.push(`Required source ${sourceId} freshness is UNKNOWN.`);
    if(!basisMeetsContract)reasons.push(`Required source ${sourceId} authenticity or freshness basis is below the proof obligation contract (${requiredBases.join(', ')||'NO PROVING BASIS'}).`);
  }
  const contradicted=requirements.some(item=>item.authenticityStatus==='CONTRADICTED'||item.authenticityContradictionStatus==='CONTRADICTED'||item.freshnessContradictionStatus==='CONTRADICTED'),freshnessStatus=requirements.some(item=>item.freshnessStatus==='EXPIRED')?'EXPIRED':requirements.some(item=>item.freshnessStatus==='UNKNOWN')?'UNKNOWN':requirements.length&&requirements.every(item=>item.freshnessStatus==='NOT_APPLICABLE')?'NOT_APPLICABLE':requirements.length?'CURRENT':'NOT_APPLICABLE',basisValues=requirements.flatMap(item=>[item.authenticityBasis,...(item.freshnessStatus==='NOT_APPLICABLE'?[]:[item.freshnessBasis])]).filter(value=>value&&value!=='NONE'),epistemicBasis=!ids.length?'APPLICATION_OBSERVED':basisValues.includes('SELF_ASSERTED')?'SELF_ASSERTED':basisValues.includes('EXTERNALLY_SUPPORTED')?'EXTERNALLY_SUPPORTED':basisValues.includes('VERIFIED_EXTERNAL')?'VERIFIED_EXTERNAL':basisValues.includes('APPLICATION_OBSERVED')?'APPLICATION_OBSERVED':'NONE',sufficient=reasons.length===0;
  return {sourceIds:ids,requiredEpistemicBases:requiredBases,requirements,sufficient,truthValue:sufficient?'TRUE':'UNKNOWN',epistemicBasis,currentScopeStatus:'CURRENT',freshnessStatus,contradictionStatus:contradicted?'CONTRADICTED':'CLEAR',reasons:[...new Set(reasons)],evidenceIds:[...new Set(evidenceIds)].sort(),stateSha256:hash.sha256Value(requirements)};
}
function controlledNormativeClass(value){const normalized=upper(value);if(['MANDATORY','REQUIRED','TRUE','YES'].includes(normalized))return 'MANDATORY';if(normalized==='CONDITIONAL')return 'CONDITIONAL';if(['OPTIONAL','FALSE','NO'].includes(normalized))return 'OPTIONAL';return 'UNKNOWN';}
function proposedNormativeClassification(requirement){for(const candidate of [recordValue(requirement,'MANDATORY_OPTIONAL_STATUS'),recordValue(requirement,'MANDATORY')]){const value=controlledNormativeClass(candidate);if(value!=='UNKNOWN')return value;}return 'UNKNOWN';}
function normativeClassification(requirement){const fields=recordFields(requirement),normalizedPresent=Object.prototype.hasOwnProperty.call(fields,'NORMATIVE_CLASSIFICATION')||Object.prototype.hasOwnProperty.call(requirement||{},'NORMATIVE_CLASSIFICATION');return normalizedPresent?controlledNormativeClass(recordValue(requirement,'NORMATIVE_CLASSIFICATION')):proposedNormativeClassification(requirement);}
function evaluateApplicability(project,{requirement=null,proposition=null,seen=new Set()}={}){
  const requirementPropositions=requirement?propositionsForRequirement(project,requirement):[],primaryIds=requirement?relationshipIds(requirement,'PRIMARY_PROPOSITION_ID'):[],resolvedProposition=proposition||requirementPropositions.find(item=>primaryIds.includes(recordId(item,'propositions')))||(requirementPropositions.length===1?requirementPropositions[0]:null),reqId=requirement?requirementId(requirement):relationshipIds(resolvedProposition,'REQUIREMENT_ID')[0]||relationshipIds(resolvedProposition,'REQ_ID')[0]||'',propId=resolvedProposition?recordId(resolvedProposition,'propositions'):'',classification=normativeClassification(requirement||{}),reasons=[],supportingEvidenceIds=[];
  const candidates=recordsForCurrentScope(project,'applicabilityRecords').filter(record=>{const subjectType=upper(recordValue(record,'SUBJECT_TYPE')),subjectId=String(recordValue(record,'SUBJECT_ID')||''),reqs=relationshipIds(record,'REQUIREMENT_ID').concat(relationshipIds(record,'REQ_ID')),props=relationshipIds(record,'PROPOSITION_ID');return (propId&&(props.includes(propId)||(subjectId===propId&&subjectType==='PROPOSITION')))||(!propId&&reqId&&(reqs.includes(reqId)||(subjectId===reqId&&['REQUIREMENT','REQ'].includes(subjectType))));});
  let state='UNKNOWN',basis='NONE',freshnessStatus='UNKNOWN',contradictionStatus='CLEAR',record=null;
  if(candidates.length){const selected=candidates.filter(item=>recordValue(item,'CURRENT_SELECTION')===undefined||truth(recordValue(item,'CURRENT_SELECTION')));record=(selected.length?selected:candidates).at(-1);const considered=selected.length?selected:candidates,values=[...new Set(considered.map(item=>controlledApplicability(recordValue(item,'APPLICABILITY')||recordValue(item,'APPLICABILITY_RESULT')||recordValue(item,'STATE'))))];if(selected.length>1||values.length>1){contradictionStatus='CONTRADICTED';reasons.push('Current applicability records contain multiple selected or incompatible determinations.');}state=values.length===1?values[0]:'UNKNOWN';basis=controlledBasis(recordValue(record,'EPISTEMIC_BASIS'));freshnessStatus=freshnessEvaluation(record).status;supportingEvidenceIds.push(...relationshipIds(record,'EVIDENCE_ID'),...relationshipIds(record,'EVIDENCE_IDS'),...relationshipIds(record,'SUPPORTING_EVIDENCE_IDS'),...safe(record.evidenceRefs).map(String));
    if(state==='NOT_APPLICABLE'){const before=reasons.length,review=applicabilityReviewAssessment(project,record);if(!String(recordValue(record,'REASONING')||'').trim())reasons.push('NOT_APPLICABLE lacks affirmative semantic reasoning.');const currentEvidenceById=new Map(recordsForCurrentScope(project,'evidenceRecords').map(item=>[recordId(item,'evidenceRecords'),item])),missingEvidence=supportingEvidenceIds.filter(id=>!currentEvidenceById.has(id)),evidenceFreshness=supportingEvidenceIds.map(id=>currentEvidenceById.get(id)).filter(Boolean).map(item=>freshnessEvaluation(item).status);if(!supportingEvidenceIds.length)reasons.push('NOT_APPLICABLE lacks affirmative current evidence.');else if(missingEvidence.length)reasons.push('NOT_APPLICABLE evidence is missing or stale: '+missingEvidence.join(', ')+'.');if(evidenceFreshness.some(status=>status==='EXPIRED'||status==='UNKNOWN'))reasons.push('NOT_APPLICABLE evidence is expired or has unknown freshness.');if(evidenceFreshness.length)freshnessStatus=evidenceFreshness.includes('EXPIRED')?'EXPIRED':evidenceFreshness.includes('UNKNOWN')?'UNKNOWN':evidenceFreshness.every(status=>status==='NOT_APPLICABLE')?'NOT_APPLICABLE':'CURRENT';if(!review.ready)reasons.push(...review.reasons);if(reasons.length>before)state='UNKNOWN';}
  }else{
    const direct=controlledApplicability(recordValue(requirement,'APPLICABILITY')||recordValue(proposition,'APPLICABILITY'));
    if(resolvedProposition)reasons.push('No current first-class applicability determination exists for the primary proposition.');
    else if(direct==='APPLICABLE'){state='APPLICABLE';basis='EXTERNALLY_SUPPORTED';freshnessStatus='NOT_APPLICABLE';reasons.push('Applicability is inherited from the accepted legacy requirement because no proposition model or first-class applicability record exists.');}
    else if(direct==='NOT_APPLICABLE'){state='UNKNOWN';reasons.push('A bare NOT_APPLICABLE field cannot remove a release obligation without affirmative evidence and review.');}
    else reasons.push('No current applicability determination exists.');
  }
  if(classification==='CONDITIONAL'){
    const activationId=relationshipIds(record,'ACTIVATION_PROPOSITION_ID')[0];
    if(!activationId)reasons.push('Conditional requirement has no activation proposition.');
    else if(!seen.has(activationId)){const activation=recordsForCurrentScope(project,'propositions').find(item=>recordId(item,'propositions')===activationId);if(!activation)reasons.push('Conditional activation proposition is not current.');else{const nextSeen=new Set(seen);nextSeen.add(activationId);const activationState=evaluatePropositionState(project,{proposition:activation,seen:nextSeen});if(activationState.truthValue==='TRUE')state='APPLICABLE';else if(activationState.truthValue==='FALSE'&&activationState.contradictionStatus==='CLEAR')state='NOT_APPLICABLE';else state='UNKNOWN';}}
    else{state='UNKNOWN';contradictionStatus='CONTRADICTED';reasons.push('Conditional activation proposition cycle detected.');}
  }
  if(freshnessStatus==='EXPIRED'||(freshnessStatus==='UNKNOWN'&&recordValue(record,'FRESHNESS_REQUIREMENT')))state='UNKNOWN';
  if(contradictionStatus==='CONTRADICTED')state='UNKNOWN';
  return {applicability:state,truthValue:state==='APPLICABLE'?'TRUE':state==='NOT_APPLICABLE'?'FALSE':'UNKNOWN',epistemicBasis:basis,currentScopeStatus:'CURRENT',freshnessStatus,contradictionStatus,reasons:[...new Set(reasons)],supportingEvidenceIds:[...new Set(supportingEvidenceIds)],recordId:record?recordId(record,'applicabilityRecords'):null,activationPropositionId:record?relationshipIds(record,'ACTIVATION_PROPOSITION_ID')[0]||null:null};
}
function observationEntailments(project,observation,propositionId=null,proofObligationId=null,scopeRule=null){
  const oid=recordId(observation,'observationRecords'),source=scopeRule?recordsForScope(project,'entailmentReviews',scopeRule):recordsForCurrentScope(project,'entailmentReviews');return source.filter(review=>{const observationIds=relationshipIds(review,'OBSERVATION_ID'),propositionIds=relationshipIds(review,'PROPOSITION_ID'),obligationIds=relationshipIds(review,'PROOF_OBLIGATION_ID');return observationIds.includes(oid)&&(!propositionId||propositionIds.includes(String(propositionId)))&&(!proofObligationId||!obligationIds.length||obligationIds.includes(String(proofObligationId)));});
}
function evaluateObservationEntailment(project,{observation,propositionId=null,proofObligation=null,allowedEpistemicBases=null,scopeRule=null}={}){
  const scopedEvidence=scopeRule?recordsForScope(project,'evidenceRecords',scopeRule):recordsForCurrentScope(project,'evidenceRecords'),reasons=[],oid=recordId(observation,'observationRecords'),origin=upper(recordValue(observation,'ORIGIN_CLASS')||recordValue(observation,'OBSERVATION_ORIGIN')||recordValue(observation,'ORIGIN')),basis=controlledBasis(recordValue(observation,'EPISTEMIC_BASIS')),freshness=freshnessEvaluation(observation),evidenceIds=[...new Set([...safe(observation?.evidenceRefs).map(String),...relationshipIds(observation,'SOURCE_EVIDENCE_IDS'),...relationshipIds(observation,'EVIDENCE_IDS'),...relationshipIds(observation,'EVIDENCE_ID')])],currentEvidence=evidenceIds.map(id=>scopedEvidence.find(e=>recordId(e,'evidenceRecords')===id)).filter(Boolean),obligationId=proofObligation?recordId(proofObligation,'proofObligations'):null,reviews=observationEntailments(project,observation,propositionId,obligationId,scopeRule),relations=[...new Set(reviews.map(r=>upper(recordValue(r,'ACCEPTED_RELATION')||recordValue(r,'ENTAILMENT_FINDING')||recordValue(r,'RELATION')||recordValue(r,'ENTAILMENT_RELATION'))).filter(v=>ENTAILMENT_RELATIONS.includes(v)))];
  const attestationBinding={resultId:String(recordValue(observation,'RESULT_ID')||''),observationId:oid,testId:relationshipIds(observation,'TEST_ID')[0]||'',propositionId:String(propositionId||''),inputIdentitySha256:hash.sha256Value(safe(recordValue(observation,'INPUT_IDENTITIES_AND_HASHES')))};
  if(!oid)reasons.push('Observation identity is missing.');if(!origin)reasons.push('Observation origin is missing.');if(basis==='NONE')reasons.push('Observation epistemic basis is missing.');if(basis==='VERIFIED_EXTERNAL'&&!currentEvidence.some(item=>registeredAttestationEvidence(item,attestationBinding)))reasons.push('VERIFIED_EXTERNAL basis requires a registered application-verified attestation bound to this exact result, observation, test, proposition, and input identity; normalization cannot elevate an unrelated external claim.');if(!evidenceIds.length||currentEvidence.length!==evidenceIds.length)reasons.push('Observation lacks complete current canonical evidence.');if(freshness.status==='EXPIRED'||freshness.status==='UNKNOWN')reasons.push(...freshness.reasons.length?freshness.reasons:['Observation freshness is unknown.']);if(!reviews.length)reasons.push('No accepted entailment review links the observation to the proposition or proof leaf.');if(relations.length!==1)reasons.push('Observation entailment relation is missing, unknown, or contradictory.');const allowed=scalarList(allowedEpistemicBases||recordValue(proofObligation,'REQUIRED_EPISTEMIC_BASES'));if(!allowed.length)reasons.push('The proof obligation has no required epistemic-basis contract.');else if(!allowed.map(upper).includes(basis))reasons.push('Observation epistemic basis is below or outside the proof obligation contract.');const relation=relations.length===1?relations[0]:'UNKNOWN';return {observationId:oid,relation,truthValue:relation==='ESTABLISHES'?'TRUE':relation==='REFUTES'?'FALSE':'UNKNOWN',epistemicBasis:basis,origin,freshnessStatus:freshness.status,sufficient:reasons.length===0&&(relation==='ESTABLISHES'||relation==='REFUTES'),reasons:[...new Set(reasons)],evidenceIds};
}
function expressionPayload(expression){let value=recordValue(expression,'NORMALIZED_EXPRESSION')??recordValue(expression,'EXPRESSION')??expression;if(typeof value==='string'){try{value=JSON.parse(value);}catch{return null;}}return value&&typeof value==='object'&&!Array.isArray(value)?value:null;}
const PRODUCT_RESULT_COLLECTION_BY_TEST_TYPE=Object.freeze({DETERMINISTIC:'deterministicResults',MEANING:'meaningResults',ADVERSARIAL:'adversarialResults'});
function proofResultObservationStates(project,result,{propositionId,proofObligation,scopeRule}={}){
  const source=scopeRule?recordsForScope(project,'observationRecords',scopeRule):recordsForCurrentScope(project,'observationRecords'),ids=relationshipIds(result,'OBSERVATION_ID'),resultId=recordId(result,resultCollectionFor(project,result)||'');
  const observations=source.filter(item=>ids.includes(recordId(item,'observationRecords'))||relationshipIds(item,'RESULT_ID').includes(resultId));
  return observations.map(observation=>evaluateObservationEntailment(project,{observation,propositionId,proofObligation,scopeRule}));
}
function aggregateProofResultStates(states){
  const conclusive=states.filter(item=>item.conclusive),satisfied=conclusive.filter(item=>item.determination==='SATISFIED'),violated=conclusive.filter(item=>item.determination==='VIOLATED'),unknown=states.filter(item=>!item.conclusive||item.determination==='UNDETERMINED'),reasons=states.flatMap(item=>safe(item.reasons)),contradicted=Boolean(satisfied.length&&violated.length);
  let truthValue='UNKNOWN';if(violated.length)truthValue='FALSE';else if(states.length&&satisfied.length===states.length)truthValue='TRUE';
  if(contradicted)reasons.push('The bound current result set contains sufficient results that both establish and refute the proposition.');
  if(unknown.length)reasons.push('One or more exact required result-set members are missing, duplicate, stale, unsupported, or insufficiently evidenced.');
  const bases=conclusive.flatMap(item=>safe(item.observations).filter(observation=>observation.sufficient).map(observation=>observation.epistemicBasis)),basisOrder=['NONE','SELF_ASSERTED','EXTERNALLY_SUPPORTED','VERIFIED_EXTERNAL','APPLICATION_OBSERVED'],epistemicBasis=bases.length?bases.reduce((weakest,value)=>basisOrder.indexOf(value)<basisOrder.indexOf(weakest)?value:weakest,bases[0]):'NONE',freshnesses=conclusive.flatMap(item=>safe(item.observations).map(observation=>observation.freshnessStatus)),freshnessStatus=freshnesses.includes('EXPIRED')?'EXPIRED':freshnesses.includes('UNKNOWN')?'UNKNOWN':freshnesses.length?'CURRENT':'UNKNOWN';
  return {truthValue,valid:true,contradictionStatus:contradicted?'CONTRADICTED':'CLEAR',epistemicBasis,freshnessStatus,supportingEvidenceIds:[...new Set(conclusive.flatMap(item=>safe(item.observations).flatMap(observation=>safe(observation.evidenceIds))))],resultIds:conclusive.map(item=>item.resultId),reasons:[...new Set(reasons)]};
}
function proofTestResultSetState(project,testId,{proofObligation=null,propositionId=null,scopeRule=null}={}){
  const scope=scopeRule||currentScope(project),tests=recordsForScope(project,'tests',scope),test=tests.find(item=>recordId(item,'tests')===testId),reasons=[];
  if(!test)return {truthValue:'UNKNOWN',valid:true,reasons:['The TEST_RESULT_SET references a test that is not current in the evaluated scope.']};
  const obligationTestIds=relationshipIds(proofObligation,'REQUIRED_TEST_IDS');if(!proofObligation||!obligationTestIds.includes(testId))reasons.push('The TEST_RESULT_SET test is not frozen in the evaluated proof obligation.');
  if(!releaseBearingTest(test)||['ADVISORY','NEGATIVE_ONLY'].includes(testRole(test)))reasons.push('An optional, advisory, or negative-only test cannot bind a release-bearing proof leaf.');
  const semantic=semanticReviewAssessment(project,{subjectKind:'TEST',subject:test,scopeRule});if(!semantic.ready)reasons.push(...semantic.reasons.map(reason=>'The TEST_RESULT_SET test '+reason));
  if(propositionId&&!propositionIdsFor(test).includes(String(propositionId)))reasons.push('The TEST_RESULT_SET test does not target the evaluated proposition.');
  if(reasons.length)return {truthValue:'UNKNOWN',valid:false,reasons};
  let expected=[],rows=[],collection='';
  if(scope.productId){
    collection=PRODUCT_RESULT_COLLECTION_BY_TEST_TYPE[upper(recordValue(test,'TEST_TYPE'))]||'';
    if(!collection)return {truthValue:'UNKNOWN',valid:true,reasons:['The current test type has no registered product-result collection for TEST_RESULT_SET evaluation.']};
    expected=[{key:[scope.productId,testId].join('|')}];rows=recordsForScope(project,collection,scope).filter(item=>String(recordValue(item,'TEST_ID')||item.relationships?.TEST_ID||'')===testId&&String(recordValue(item,'PRODUCT_ID')||item.relationships?.PRODUCT_ID||item.scope?.productId||'')===String(scope.productId));
  }else if(scope.iterationId){
    collection='verification';const relation=requiredVerificationRelationSet(project,scope.iterationId),requirementIdValue=String(recordValue(proofObligation,'REQUIREMENT_ID')||proofObligation?.relationships?.REQUIREMENT_ID||'');expected=relation.tuples.filter(item=>item.testId===testId&&item.requirementId===requirementIdValue&&(!propositionId||item.propositionId===String(propositionId)));rows=recordsForIteration(project,'verification',scope.iterationId).filter(item=>String(recordValue(item,'TEST_ID')||item.relationships?.TEST_ID||'')===testId&&String(recordValue(item,'REQ_ID')||item.relationships?.REQ_ID||'')===requirementIdValue&&(!propositionId||String(recordValue(item,'PROPOSITION_ID')||item.relationships?.PROPOSITION_ID||'')===String(propositionId)));
  }else return {truthValue:'UNKNOWN',valid:true,reasons:['TEST_RESULT_SET evaluation requires an exact current iteration or product scope.']};
  if(!expected.length)return {truthValue:'UNKNOWN',valid:true,reasons:['The frozen TEST_RESULT_SET has no required result slots in the evaluated scope.']};
  const keyFor=record=>collection==='verification'?verificationKey(record,{includeProposition:Boolean(propositionId)}):[String(recordValue(record,'PRODUCT_ID')||record.relationships?.PRODUCT_ID||record.scope?.productId||''),String(recordValue(record,'TEST_ID')||record.relationships?.TEST_ID||'')].join('|'),expectedKeys=new Set(expected.map(item=>item.key)),grouped=new Map();for(const row of rows){const key=keyFor(row);if(!grouped.has(key))grouped.set(key,[]);grouped.get(key).push(row);}const states=[];
  for(const expectedItem of expected){const matches=grouped.get(expectedItem.key)||[];if(matches.length!==1){states.push({determination:'UNDETERMINED',conclusive:false,reasons:[matches.length?'Duplicate current results exist for required result-set slot '+expectedItem.key+'.':'Required result-set slot '+expectedItem.key+' is missing.'],observations:[]});continue;}const result=matches[0],boundPropositions=propositionIdsFor(result,test),boundObligations=relationshipIds(result,'PROOF_OBLIGATION_ID'),obligationId=recordId(proofObligation,'proofObligations'),bindingReasons=[];if(propositionId&&!boundPropositions.includes(String(propositionId)))bindingReasons.push('Result is not application-bound to the evaluated proposition.');if(boundObligations.length&&!boundObligations.includes(obligationId))bindingReasons.push('Result is bound to a different proof obligation.');const evaluated=evaluateResultConsistency(collection,result,test,project),observations=proofResultObservationStates(project,result,{propositionId,proofObligation,scopeRule:scope}),requiredRelation=evaluated.determination==='SATISFIED'?'TRUE':evaluated.determination==='VIOLATED'?'FALSE':'UNKNOWN',entailmentReady=requiredRelation==='UNKNOWN'||observations.some(item=>item.sufficient&&item.truthValue===requiredRelation),allReasons=[...bindingReasons,...evaluated.reasons,...(entailmentReady?[]:['No sufficient current observation and entailment establish the normalized result determination.'])];states.push({resultId:recordId(result,collection),determination:evaluated.determination,conclusive:allReasons.length===0&&['SATISFIED','VIOLATED'].includes(evaluated.determination),reasons:allReasons,observations});}
  for(const [key,matches] of grouped)if(!expectedKeys.has(key))states.push({determination:'UNDETERMINED',conclusive:false,reasons:['Unexpected result exists outside the frozen result-set slots: '+key+'.'],observations:matches.flatMap(result=>proofResultObservationStates(project,result,{propositionId,proofObligation,scopeRule:scope}))});
  return {...aggregateProofResultStates(states),testId,scope:clone(scope)};
}
function proofExpressionBindingReadiness(project,expression,{propositionId=null,requiredTestIds=[]}={}){
  const reasons=[],seen=new Set(),allowed=new Set(safe(requiredTestIds).map(String)),visit=(node,path='$')=>{if(!node||typeof node!=='object'||Array.isArray(node))return;if(upper(node.op)!=='LEAF'){safe(node.children).forEach((child,index)=>visit(child,`${path}.children[${index}]`));return;}const type=upper(node.leafType||node.referenceType),id=String(node.referenceId||'');if(type==='TEST_RESULT_SET'){const test=recordsForCurrentScope(project,'tests').find(item=>recordId(item,'tests')===id);if(!allowed.has(id))reasons.push(`${path} TEST_RESULT_SET is not frozen in the proof obligation.`);if(!test)reasons.push(`${path} TEST_RESULT_SET does not reference a current test.`);else{if(!releaseBearingTest(test)||['ADVISORY','NEGATIVE_ONLY'].includes(testRole(test)))reasons.push(`${path} cannot bind an optional, advisory, or negative-only test.`);if(propositionId&&!propositionIdsFor(test).includes(String(propositionId)))reasons.push(`${path} test targets a different proposition.`);const semantic=semanticReviewAssessment(project,{subjectKind:'TEST',subject:test});if(!semantic.ready)reasons.push(...semantic.reasons.map(reason=>`${path} ${reason}`));}}else if(['TEST_RESULT','RESULT','VERIFICATION_RESULT'].includes(type)){let found=false;for(const collection of ['verification','deterministicResults','meaningResults','adversarialResults','representationInspections','regressionExecutions'])if(recordsForCurrentScope(project,collection).some(item=>recordId(item,collection)===id)){found=true;break;}if(!found)reasons.push(`${path} exact TEST_RESULT reference is not current; use TEST_RESULT_SET for a frozen future test slot.`);}else if(type==='PROOF_EXPRESSION'){if(seen.has(id)){reasons.push(`${path} creates a proof-expression cycle.`);return;}const nested=recordsForCurrentScope(project,'proofExpressions').find(item=>recordId(item,'proofExpressions')===id);if(!nested)reasons.push(`${path} references no current proof expression.`);else{seen.add(id);visit(expressionPayload(nested),`${path}.proofExpression`);seen.delete(id);}}};const payload=expressionPayload(expression),validation=schema.validateProofExpression?schema.validateProofExpression(payload):{valid:Boolean(payload),issues:[]};if(!validation.valid)reasons.push(...safe(validation.issues));else visit(payload);return {ready:reasons.length===0,reasons:[...new Set(reasons)]};
}
function proofLeafState(project,leaf,{proofObligation=null,propositionId=null,seen=new Set(),scopeRule=null}={}){
  const type=upper(leaf.referenceType||leaf.leafType||leaf.kind||leaf.type||''),id=String(leaf.referenceId||leaf.targetId||leaf.id||leaf.value||'').trim(),reasons=[];
  const scoped=collection=>scopeRule?recordsForScope(project,collection,scopeRule):recordsForCurrentScope(project,collection);
  if(type==='PROOF_EXPRESSION'){const expression=scoped('proofExpressions').find(record=>recordId(record,'proofExpressions')===id);if(!expression)return {truthValue:'UNKNOWN',valid:true,reasons:['Referenced proof expression is not current.']};return evaluateProofExpression(project,expression,{proofObligation,propositionId,seen,scopeRule});}
  if(type==='OBSERVATION'||type==='OBSERVATION_OBLIGATION'){const observation=scoped('observationRecords').find(record=>recordId(record,'observationRecords')===id);if(!observation)return {truthValue:'UNKNOWN',valid:true,reasons:['Referenced observation is not current.']};const evaluation=evaluateObservationEntailment(project,{observation,propositionId,proofObligation,scopeRule});return {...evaluation,truthValue:evaluation.sufficient?evaluation.truthValue:'UNKNOWN',valid:true};}
  if(type==='ARTIFACT_IDENTITY'||type==='ARTIFACT_IDENTITY_OBLIGATION'){const identity=scoped('artifactIdentities').find(record=>recordId(record,'artifactIdentities')===id);const ok=identity&&truth(recordValue(identity,'EXACT_HASH_MATCH'))&&truth(recordValue(identity,'EXACT_SIZE_MATCH'))&&truth(recordValue(identity,'DELIVERY_ARTIFACT_IDENTITY_VERIFIED')??true);return {truthValue:ok?'TRUE':'UNKNOWN',valid:true,reasons:ok?[]:['Referenced artifact identity is not current and exact.']};}
  if(type==='ENVIRONMENT'||type==='ENVIRONMENT_DEPENDENCY'){const dependency=scoped('environmentDependencies').find(record=>recordId(record,'environmentDependencies')===id);return {...evaluateEnvironmentDependency(project,dependency),valid:true};}
  if(type==='TEST_RESULT_SET')return proofTestResultSetState(project,id,{proofObligation,propositionId,scopeRule});
  if(['TEST_RESULT','RESULT','VERIFICATION_RESULT'].includes(type)){
    let hit=null;for(const collection of ['verification','deterministicResults','meaningResults','adversarialResults','representationInspections','regressionExecutions']){const record=scoped(collection).find(item=>recordId(item,collection)===id);if(record){hit={collection,record};break;}}
    if(!hit)return {truthValue:'UNKNOWN',valid:true,reasons:['Referenced test result is not current.']};const test=testForResult(project,hit.record),semantic=releaseBearingTest(test)?semanticReviewAssessment(project,{subjectKind:'TEST',subject:test,scopeRule}):{ready:true,reasons:[]};if(!semantic.ready)reasons.push(...semantic.reasons);const evaluated=evaluateResultConsistency(hit.collection,hit.record,test,project),observations=proofResultObservationStates(project,hit.record,{propositionId,proofObligation,scopeRule}),requiredTruth=evaluated.determination==='SATISFIED'?'TRUE':evaluated.determination==='VIOLATED'?'FALSE':'UNKNOWN';if(requiredTruth!=='UNKNOWN'&&!observations.some(item=>item.sufficient&&item.truthValue===requiredTruth))reasons.push('Referenced result lacks a sufficient matching observation and entailment.');const truthValue=evaluated.determination==='SATISFIED'?'TRUE':evaluated.determination==='VIOLATED'?'FALSE':'UNKNOWN',aggregate=aggregateProofResultStates([{resultId:id,determination:evaluated.determination,conclusive:reasons.length===0&&evaluated.reasons.length===0&&['SATISFIED','VIOLATED'].includes(evaluated.determination),reasons:[...reasons,...evaluated.reasons],observations}]);return {...aggregate,resultId:id};
  }
  return {truthValue:'UNKNOWN',valid:false,reasons:['Unknown or unsupported proof-expression leaf type.']};
}
function evaluateProofExpression(project,expression,{proofObligation=null,propositionId=null,seen=new Set(),scopeRule=null}={}){
  const expressionId=recordId(expression,'proofExpressions')||String(expression?.PROOF_EXPRESSION_ID||''),reasons=[];if(expressionId&&seen.has(expressionId))return {truthValue:'UNKNOWN',valid:false,reasons:['Proof-expression cycle detected.'],expressionId};const nextSeen=new Set(seen);if(expressionId)nextSeen.add(expressionId);const node=expressionPayload(expression);if(!node)return {truthValue:'UNKNOWN',valid:false,reasons:['Proof expression is missing or malformed.'],expressionId};
  const operator=upper(node.op||node.operator||node.type||'');if(!PROOF_EXPRESSION_OPERATORS.includes(operator))return {truthValue:'UNKNOWN',valid:false,reasons:['Unknown proof-expression operator.'],expressionId};
  if(operator==='LEAF'){const state=proofLeafState(project,node,{proofObligation,propositionId,seen:nextSeen,scopeRule});return {...state,valid:state.valid!==false,expressionId,operator};}
  const children=safe(node.children||node.operands);if(!children.length)return {truthValue:'UNKNOWN',valid:false,reasons:[operator+' proof expression cannot be empty.'],expressionId,operator};const states=children.map(child=>evaluateProofExpression(project,child,{proofObligation,propositionId,seen:nextSeen,scopeRule})),trueCount=states.filter(x=>x.truthValue==='TRUE').length,falseCount=states.filter(x=>x.truthValue==='FALSE').length,unknownCount=states.length-trueCount-falseCount;let truthValue='UNKNOWN';
  if(operator==='ALL_OF')truthValue=falseCount?'FALSE':unknownCount?'UNKNOWN':'TRUE';else if(operator==='ANY_OF')truthValue=trueCount?'TRUE':unknownCount?'UNKNOWN':'FALSE';else{const k=Number(node.k??node.minimum??node.value);if(!Number.isInteger(k)||k<1||k>states.length)reasons.push('AT_LEAST_K requires an integer 1 <= K <= child count.');else truthValue=trueCount>=k?'TRUE':trueCount+unknownCount<k?'FALSE':'UNKNOWN';}
  for(const state of states)reasons.push(...safe(state.reasons));const bases=states.map(item=>item.epistemicBasis).filter(Boolean),freshnesses=states.map(item=>item.freshnessStatus).filter(Boolean),contradicted=states.some(item=>item.contradictionStatus==='CONTRADICTED');return {truthValue,valid:states.every(x=>x.valid!==false)&&!reasons.some(reason=>/AT_LEAST_K requires/i.test(reason)),reasons:[...new Set(reasons)],expressionId,operator,children:states,epistemicBasis:bases.includes('NONE')?'NONE':bases.includes('SELF_ASSERTED')?'SELF_ASSERTED':bases.includes('EXTERNALLY_SUPPORTED')?'EXTERNALLY_SUPPORTED':bases.includes('VERIFIED_EXTERNAL')?'VERIFIED_EXTERNAL':bases.includes('APPLICATION_OBSERVED')?'APPLICATION_OBSERVED':'NONE',freshnessStatus:freshnesses.includes('EXPIRED')?'EXPIRED':freshnesses.includes('UNKNOWN')?'UNKNOWN':freshnesses.length?'CURRENT':'UNKNOWN',contradictionStatus:contradicted?'CONTRADICTED':'CLEAR',supportingEvidenceIds:[...new Set(states.flatMap(item=>safe(item.supportingEvidenceIds)))]};
}
function evaluateEnvironmentDependency(project,dependency){if(!dependency)return {truthValue:'UNKNOWN',epistemicBasis:'NONE',freshnessStatus:'UNKNOWN',reasons:['Required environment dependency is missing.']};const truthValue=controlledTruth(recordValue(dependency,'TRUTH_VALUE')||recordValue(dependency,'SATISFIED')),basis=controlledBasis(recordValue(dependency,'EPISTEMIC_BASIS')),freshness=freshnessEvaluation(dependency),reasons=[];if(truthValue==='UNKNOWN')reasons.push('Environment dependency truth is unknown.');if(basis==='NONE')reasons.push('Environment dependency epistemic basis is missing.');if(['EXPIRED','UNKNOWN'].includes(freshness.status))reasons.push(...freshness.reasons.length?freshness.reasons:['Environment dependency freshness is unknown.']);return {dependencyId:recordId(dependency,'environmentDependencies'),truthValue:reasons.length&&truthValue==='TRUE'?'UNKNOWN':truthValue,epistemicBasis:basis,freshnessStatus:freshness.status,reasons:[...new Set(reasons)]};}
function propositionsForRequirement(project,requirement){const reqId=requirementId(requirement);return recordsForCurrentScope(project,'propositions').filter(record=>relationshipIds(record,'REQUIREMENT_ID').concat(relationshipIds(record,'REQ_ID')).includes(reqId));}
function propositionRole(proposition){const value=upper(recordValue(proposition,'PROPOSITION_ROLE'));return ['PRIMARY_REQUIREMENT','CONDITIONAL_ACTIVATION'].includes(value)?value:'UNKNOWN';}
function primaryPropositionsForRequirement(project,requirement){const propositions=propositionsForRequirement(project,requirement),declared=relationshipIds(requirement,'PRIMARY_PROPOSITION_ID');if(declared.length)return propositions.filter(item=>declared.includes(recordId(item,'propositions'))&&propositionRole(item)!=='CONDITIONAL_ACTIVATION');const explicit=propositions.filter(item=>propositionRole(item)==='PRIMARY_REQUIREMENT');if(explicit.length)return explicit;return propositions.length===1&&propositionRole(propositions[0])!=='CONDITIONAL_ACTIVATION'?propositions:[];}
function activationPropositionsForRequirement(project,requirement){return propositionsForRequirement(project,requirement).filter(item=>propositionRole(item)==='CONDITIONAL_ACTIVATION');}
function sameIdentitySet(left,right){const normalize=value=>[...new Set(safe(value).map(String))].sort();return hash.sha256Value(normalize(left))===hash.sha256Value(normalize(right));}
function semanticReviewAssessment(project,{subjectKind,subject,scopeRule=null}={}){
  const kind=upper(subjectKind),collection=kind==='TEST'?'tests':'proofExpressions',subjectId=recordId(subject,collection),reasons=[];if(!subjectId)return {ready:false,disposition:'UNKNOWN',reviewIds:[],reasons:['Semantic-review subject identity is missing.']};
  const declaredReviewIds=relationshipIds(subject,'SEMANTIC_REVIEW_IDS'),all=acceptedTestSemanticReviews(project,{subjectKind:kind,subjectId,scopeRule}),payloadHash=hash.sha256Value(testSemanticSubjectPayload(kind,subject)),reviews=all.filter(review=>String(recordValue(review,'SUBJECT_SEMANTIC_SHA256'))===payloadHash),reviewIds=reviews.map(review=>recordId(review,'testSemanticReviews'));
  if(reviews.length!==1)reasons.push('Exactly one accepted independent semantic review must bind the exact current '+kind.toLowerCase().replace('_',' ')+' payload; found '+reviews.length+'.');
  if(!sameIdentitySet(declaredReviewIds,reviewIds))reasons.push('The subject semantic-review relationship is missing, stale, or contains an unrelated review.');
  const targetIds=kind==='TEST'?propositionIdsFor(subject):relationshipIds(subject,'TARGET_PROPOSITION_ID');
  for(const review of reviews){
    if(!sameIdentitySet(relationshipIds(review,'TARGET_PROPOSITION_IDS'),targetIds))reasons.push('The semantic review does not bind the exact target proposition set.');
    const authorChangeId=String(recordValue(review,'AUTHOR_ACCEPTED_CHANGE_ID')||''),reviewChangeId=String(recordValue(review,'REVIEW_ACCEPTED_CHANGE_ID')||''),authorPrompt=String(recordValue(review,'AUTHOR_PROMPT_IDENTITY')||''),reviewPrompt=String(recordValue(review,'REVIEW_PROMPT_IDENTITY')||''),author=acceptedChanges(project,6).find(change=>change.changeId===authorChangeId),reviewChange=acceptedChanges(project,6).find(change=>change.changeId===reviewChangeId),authorContext=String(author?.scope?.contextId||''),reviewContext=String(reviewChange?.scope?.contextId||'');
    if(!author||!reviewChange||authorChangeId===reviewChangeId)reasons.push('The semantic review does not bind distinct accepted author and reviewer changes.');
    if(!authorPrompt||!reviewPrompt||authorPrompt===reviewPrompt||authorPrompt!==String(author?.promptId||'')||reviewPrompt!==String(reviewChange?.promptId||''))reasons.push('The semantic review does not bind distinct controlling prompt identities.');
    if(!authorContext||!reviewContext||authorContext===reviewContext)reasons.push('The semantic review does not bind distinct application-recorded author and reviewer contexts.');
    reasons.push(...acceptedReviewIndependence(project,{stage:6,author,review:reviewChange}).reasons);
    if(upper(recordValue(review,'INDEPENDENCE_STATUS'))!=='APPLICATION_ESTABLISHED')reasons.push('The semantic review independence consequence is not application-established.');
  }
  const findings=[...new Set(reviews.map(review=>upper(recordValue(review,'EQUIVALENCE_FINDING'))))],disposition=findings.length===1?findings[0]:'UNKNOWN',acceptedDisposition=kind==='TEST'?upper(recordValue(subject,'ACCEPTED_SEMANTIC_COVERAGE')):upper(recordValue(subject,'SEMANTIC_EQUIVALENCE_DISPOSITION'));if(disposition!=='EQUIVALENT')reasons.push('The exact current semantic review does not establish EQUIVALENT coverage.');if(acceptedDisposition!=='EQUIVALENT')reasons.push('The application-normalized author/reviewer semantic disposition is not EQUIVALENT.');
  return {ready:reasons.length===0,disposition,reviewIds,reviews,reasons:[...new Set(reasons)],payloadHash};
}
function applicabilityReviewAssessment(project,applicability,scopeRule=null){
  const applicabilityId=recordId(applicability,'applicabilityRecords'),reasons=[];if(!applicabilityId)return {ready:false,reviewIds:[],reviews:[],reasons:['Applicability identity is missing.']};
  const declaredReviewIds=relationshipIds(applicability,'INDEPENDENT_REVIEW_ID'),payloadHash=hash.sha256Value(applicabilitySubjectPayload(project,applicability)),reviews=acceptedApplicabilityReviews(project,applicabilityId,scopeRule).filter(review=>String(recordValue(review,'SUBJECT_SEMANTIC_SHA256'))===payloadHash),reviewIds=reviews.map(review=>recordId(review,'applicabilityReviews')),subjectId=relationshipIds(applicability,'SUBJECT_ID')[0]||String(recordValue(applicability,'SUBJECT_ID')||'');
  if(reviews.length!==1)reasons.push('Exactly one accepted independent applicability review must bind the exact current proposal, reasoning, evidence, and subject; found '+reviews.length+'.');
  if(!sameIdentitySet(declaredReviewIds,reviewIds))reasons.push('The applicability independent-review relationship is missing, stale, or contains an unrelated review.');
  for(const review of reviews){
    if(!sameIdentitySet(relationshipIds(review,'SUBJECT_PROPOSITION_ID'),[subjectId]))reasons.push('The applicability review does not bind the exact subject proposition.');
    const authorChangeId=String(recordValue(review,'AUTHOR_ACCEPTED_CHANGE_ID')||''),reviewChangeId=String(recordValue(review,'REVIEW_ACCEPTED_CHANGE_ID')||''),authorPrompt=String(recordValue(review,'AUTHOR_PROMPT_IDENTITY')||''),reviewPrompt=String(recordValue(review,'REVIEW_PROMPT_IDENTITY')||''),author=acceptedChanges(project,5).find(change=>change.changeId===authorChangeId),reviewChange=acceptedChanges(project,5).find(change=>change.changeId===reviewChangeId),authorContext=String(author?.scope?.contextId||''),reviewContext=String(reviewChange?.scope?.contextId||'');
    if(!author||!reviewChange||authorChangeId===reviewChangeId)reasons.push('The applicability review does not bind distinct accepted author and reviewer changes.');
    if(!authorPrompt||!reviewPrompt||authorPrompt===reviewPrompt||authorPrompt!==String(author?.promptId||'')||reviewPrompt!==String(reviewChange?.promptId||''))reasons.push('The applicability review does not bind distinct controlling prompt identities.');
    if(!authorContext||!reviewContext||authorContext===reviewContext)reasons.push('The applicability review does not bind distinct application-recorded author and reviewer contexts.');
    reasons.push(...acceptedReviewIndependence(project,{stage:5,author,review:reviewChange}).reasons);
    if(upper(recordValue(review,'INDEPENDENCE_STATUS'))!=='APPLICATION_ESTABLISHED')reasons.push('The applicability review independence consequence is not application-established.');
  }
  return {ready:reasons.length===0,reviewIds,reviews,reasons:[...new Set(reasons)],payloadHash};
}
function proofExpressionSemanticReadiness(project,expression,scopeRule=null){return semanticReviewAssessment(project,{subjectKind:'PROOF_EXPRESSION',subject:expression,scopeRule});}
function aggregateTestProofContract(project,tests,scopeRule=null){
  const contracts=[],reasons=[];for(const test of tests){const id=recordId(test,'tests'),semantic=semanticReviewAssessment(project,{subjectKind:'TEST',subject:test,scopeRule}),contract=proofContractForTest(test);contracts.push({test,id,semantic,contract});reasons.push(...semantic.reasons.map(reason=>id+': '+reason),...contract.reasons.map(reason=>id+': '+reason));}
  if(!contracts.length)reasons.push('No current release-bearing test is frozen for the proposition.');
  const freshnessStatuses=contracts.map(item=>item.contract.freshness.status),freshnessStatus=freshnessStatuses.includes('CURRENT')?'CURRENT':freshnessStatuses.includes('CURRENT_OR_NOT_APPLICABLE')?'CURRENT_OR_NOT_APPLICABLE':freshnessStatuses.length?'NOT_APPLICABLE':'UNKNOWN';
  const value={REQUIRED_EVIDENCE_CLASSES:[...new Set(contracts.flatMap(item=>item.contract.evidenceClasses))],REQUIRED_EPISTEMIC_BASES:[...new Set(contracts.flatMap(item=>item.contract.epistemicBases))],FRESHNESS_REQUIREMENTS:{status:freshnessStatus},INDEPENDENCE_REQUIREMENTS:{dimensions:[...new Set(contracts.flatMap(item=>item.contract.independence.dimensions))]},REQUIRED_RUN_DIMENSIONS:[...new Set(contracts.flatMap(item=>item.contract.runDimensions))]};
  return {complete:contracts.length>0&&reasons.length===0,reasons:[...new Set(reasons)],contracts,value};
}
function deriveProofObligationRegistry(project,{commit=false}={}){
  ensureShape(project);
  const reasons=[],obligations=[],excludedPropositionIds=[],requirements=recordsForCurrentScope(project,'requirements').filter(req=>['MANDATORY','CONDITIONAL','UNKNOWN'].includes(normativeClassification(req))),scope=currentScope(project),scopeHash=hash.sha256Value(scope),canonical=recordsForCurrentScope(project,'proofObligations'),required=[];
  for(const requirement of requirements){
    const allPropositions=propositionsForRequirement(project,requirement),primary=primaryPropositionsForRequirement(project,requirement);
    if(primary.length!==1){reasons.push('Requirement '+requirementId(requirement)+' must have exactly one current primary proposition.');continue;}
    if(allPropositions.length>1&&!relationshipIds(requirement,'PRIMARY_PROPOSITION_ID').length)reasons.push('Requirement '+requirementId(requirement)+' has multiple propositions without an application-selected primary proposition.');
    const proposition=primary[0],applicability=evaluateApplicability(project,{requirement,proposition});
    if(applicability.applicability==='NOT_APPLICABLE'&&!applicability.reasons.length){excludedPropositionIds.push(recordId(proposition,'propositions'));continue;}
    required.push({requirement,proposition,applicability,obligationRole:'RELEASE_PROPOSITION'});
    if(normativeClassification(requirement)==='CONDITIONAL'&&applicability.activationPropositionId){const activation=allPropositions.find(item=>recordId(item,'propositions')===applicability.activationPropositionId);if(!activation||propositionRole(activation)!=='CONDITIONAL_ACTIVATION')reasons.push('Conditional requirement '+requirementId(requirement)+' does not resolve to its current activation proposition.');else required.push({requirement,proposition:activation,applicability:{applicability:'APPLICABLE',truthValue:'TRUE',epistemicBasis:'APPLICATION_OBSERVED',currentScopeStatus:'CURRENT',freshnessStatus:'NOT_APPLICABLE',contradictionStatus:'CLEAR',reasons:[],supportingEvidenceIds:[],recordId:applicability.recordId,activationPropositionId:null},obligationRole:'CONDITIONAL_ACTIVATION'});}
  }
  if(canonical.length){
    for(const {requirement,proposition,applicability,obligationRole} of required){
      const pid=recordId(proposition,'propositions'),hits=canonical.filter(item=>relationshipIds(item,'PROPOSITION_ID').includes(pid));
      if(hits.length!==1){reasons.push('Release-bearing proposition '+pid+' must have exactly one current proof obligation.');continue;}
      const obligation=hits[0],expressionId=relationshipIds(obligation,'PROOF_EXPRESSION_ID')[0],expression=recordsForCurrentScope(project,'proofExpressions').find(item=>recordId(item,'proofExpressions')===expressionId),requiredTestIds=relationshipIds(obligation,'REQUIRED_TEST_IDS'),tests=requiredTestIds.map(id=>recordsForCurrentScope(project,'tests').find(item=>recordId(item,'tests')===id)).filter(Boolean),aggregate=aggregateTestProofContract(project,tests),sourceIds=sourceIdsForRequirement(project,requirement,proposition),sourceState=evaluateSourceDependencies(project,{sourceIds,requiredEpistemicBases:aggregate.value.REQUIRED_EPISTEMIC_BASES});
      if(!expression)reasons.push('Proof obligation for '+pid+' references no current proof expression.');else{const semantic=proofExpressionSemanticReadiness(project,expression);reasons.push(...semantic.reasons.map(reason=>pid+': '+reason));const binding=proofExpressionBindingReadiness(project,expression,{propositionId:pid,requiredTestIds});reasons.push(...binding.reasons.map(reason=>pid+': '+reason));}
      if(!requiredTestIds.length||tests.length!==requiredTestIds.length)reasons.push('Proof obligation for '+pid+' lacks its exact current release-bearing test set.');
      reasons.push(...aggregate.reasons,...sourceState.reasons.map(reason=>pid+': '+reason));
      for(const field of Object.keys(aggregate.value))if(hash.sha256Value(recordValue(obligation,field))!==hash.sha256Value(aggregate.value[field]))reasons.push('Proof obligation for '+pid+' does not exactly preserve its current '+field+' contract.');
      if(!sameIdentitySet(relationshipIds(requirement,'SOURCE_IDS'),sourceIds)||!sameIdentitySet(relationshipIds(proposition,'SOURCE_IDS'),sourceIds))reasons.push('Requirement and proposition '+pid+' do not preserve the same complete current source dependency set.');
      if(!sameIdentitySet(relationshipIds(obligation,'REQUIRED_SOURCE_IDS'),sourceIds))reasons.push('Proof obligation for '+pid+' does not preserve its complete current source dependency set.');
      if(hash.sha256Value(recordValue(obligation,'SOURCE_STATE_REQUIREMENTS'))!==hash.sha256Value(sourceState.requirements)||String(recordValue(obligation,'SOURCE_STATE_SHA256')||'')!==sourceState.stateSha256)reasons.push('Proof obligation for '+pid+' is stale relative to current source authenticity, freshness, contradiction, or epistemic-basis state.');
      if(String(recordValue(obligation,'ACTIVATION_PROPOSITION_ID')||'')!==String(applicability.activationPropositionId||''))reasons.push('Proof obligation for '+pid+' does not preserve the current applicability activation proposition.');
      if(String(recordValue(obligation,'CURRENT_SCOPE_HASH')||'')!==scopeHash)reasons.push('Proof obligation for '+pid+' is not bound to the exact current scope.');
      if(String(recordValue(obligation,'REQUIREMENT_ID')||obligation.relationships?.REQUIREMENT_ID||'')!==requirementId(requirement))reasons.push('Proof obligation for '+pid+' is bound to the wrong requirement.');
      if(upper(recordValue(obligation,'OBLIGATION_ROLE'))!==obligationRole)reasons.push('Proof obligation for '+pid+' has the wrong release/activation role.');
      obligations.push(obligation);
    }
    const requiredIds=new Set(required.map(({proposition})=>recordId(proposition,'propositions'))),unexpected=canonical.filter(item=>!requiredIds.has(relationshipIds(item,'PROPOSITION_ID')[0]));if(unexpected.length)reasons.push('Current proof-obligation registry contains obligations outside the current applicable release-bearing proposition set.');
    return {complete:reasons.length===0&&obligations.length===required.length&&(required.length>0||excludedPropositionIds.length>0),obligations,excludedPropositionIds,reasons:[...new Set(reasons)],source:'CANONICAL',scopeHash};
  }
  for(const {requirement,proposition,applicability,obligationRole} of required){
    const pid=recordId(proposition,'propositions'),expressions=recordsForCurrentScope(project,'proofExpressions').filter(item=>relationshipIds(item,'TARGET_PROPOSITION_ID').concat(relationshipIds(item,'PROPOSITION_ID')).includes(pid));if(expressions.length!==1){reasons.push('Proposition '+pid+' must have exactly one current accepted proof expression.');continue;}
    const expression=expressions[0],semantic=proofExpressionSemanticReadiness(project,expression);if(!semantic.ready)reasons.push(...semantic.reasons.map(reason=>pid+': '+reason));
    const tests=recordsForCurrentScope(project,'tests').filter(test=>propositionIdsFor(test).includes(pid)&&releaseBearingTest(test)),requiredTestIds=tests.map(test=>recordId(test,'tests')),aggregate=aggregateTestProofContract(project,tests),binding=proofExpressionBindingReadiness(project,expression,{propositionId:pid,requiredTestIds}),sourceIds=sourceIdsForRequirement(project,requirement,proposition),sourceState=evaluateSourceDependencies(project,{sourceIds,requiredEpistemicBases:aggregate.value.REQUIRED_EPISTEMIC_BASES});
    reasons.push(...aggregate.reasons,...binding.reasons.map(reason=>pid+': '+reason),...sourceState.reasons.map(reason=>pid+': '+reason));
    const dependencies=recordsForCurrentScope(project,'environmentDependencies').filter(item=>relationshipIds(item,'PROPOSITION_ID').includes(pid)||relationshipIds(item,'PROOF_OBLIGATION_ID').length===0),expressionId=recordId(expression,'proofExpressions'),blocked=applicability.applicability!=='APPLICABLE'||!semantic.ready||!aggregate.complete||!binding.ready||!sourceState.sufficient,fields={OBLIGATION_ROLE:obligationRole,PROPOSITION_ID:pid,REQUIREMENT_ID:requirementId(requirement),NORMATIVE_CLASS:normativeClassification(requirement),APPLICABILITY:applicability.applicability,ACTIVATION_PROPOSITION_ID:applicability.activationPropositionId||null,PROOF_EXPRESSION_ID:expressionId,REQUIRED_TEST_IDS:requiredTestIds,REQUIRED_OBSERVATION_LEAF_IDS:[],...aggregate.value,REQUIRED_SOURCE_IDS:sourceIds,SOURCE_STATE_REQUIREMENTS:sourceState.requirements,SOURCE_STATE_SHA256:sourceState.stateSha256,REQUIRED_ARTIFACT_IDS:[...new Set(tests.flatMap(test=>relationshipIds(test,'ARTIFACT_ID')))],REQUIRED_DEPENDENCY_IDS:dependencies.map(item=>recordId(item,'environmentDependencies')),SATISFACTION_STATE:'UNKNOWN',BLOCKING_REASONS:[...applicability.reasons,...semantic.reasons,...aggregate.reasons,...binding.reasons,...sourceState.reasons],CURRENT_SCOPE_HASH:scopeHash,STATUS:blocked?'BLOCKED':'CURRENT'},id='PROOF-OBLIGATION-'+hash.sha256Value({pid,scope}).slice(0,20).toUpperCase();
    let obligation={id,stage:6,active:true,scope:clone(scope),fields:{...fields,PROOF_OBLIGATION_ID:id},...fields,PROOF_OBLIGATION_ID:id,relationships:{PROPOSITION_ID:pid,REQUIREMENT_ID:requirementId(requirement),ACTIVATION_PROPOSITION_ID:fields.ACTIVATION_PROPOSITION_ID,PROOF_EXPRESSION_ID:expressionId,REQUIRED_TEST_IDS:fields.REQUIRED_TEST_IDS,REQUIRED_SOURCE_IDS:fields.REQUIRED_SOURCE_IDS,REQUIRED_ARTIFACT_IDS:fields.REQUIRED_ARTIFACT_IDS,REQUIRED_DEPENDENCY_IDS:fields.REQUIRED_DEPENDENCY_IDS},source:'APPLICATION_DERIVATION'};
    if(commit&&collectionAvailable('proofObligations')){const existing=records(project,'proofObligations',{active:false}).find(item=>recordId(item,'proofObligations')===id);if(existing)obligation=Object.assign(existing,obligation);else project.projectData.proofObligations.push(obligation);refreshRecordHashes(obligation,'proofObligations');}
    obligations.push(obligation);
  }
  return {complete:reasons.length===0&&obligations.length===required.length&&(required.length>0||excludedPropositionIds.length>0),obligations,excludedPropositionIds,reasons:[...new Set(reasons)],source:commit?'APPLICATION_DERIVED_COMMITTED':'APPLICATION_DERIVED_PREVIEW',scopeHash};
}
function evaluatePropositionState(project,{proposition,proofObligation=null,seen=new Set(),scopeRule=null}={}){
  if(!proposition)return {truthValue:'UNKNOWN',epistemicBasis:'NONE',currentScopeStatus:'STALE',freshnessStatus:'UNKNOWN',contradictionStatus:'CLEAR',reasons:['Current proposition is missing.'],supportingEvidenceIds:[]};const scoped=collection=>scopeRule?recordsForScope(project,collection,scopeRule):recordsForCurrentScope(project,collection),pid=recordId(proposition,'propositions'),reqId=relationshipIds(proposition,'REQUIREMENT_ID')[0]||relationshipIds(proposition,'REQ_ID')[0],requirement=scoped('requirements').find(req=>requirementId(req)===reqId),activationRole=propositionRole(proposition)==='CONDITIONAL_ACTIVATION',applicability=activationRole?{applicability:'APPLICABLE',truthValue:'TRUE',epistemicBasis:'APPLICATION_OBSERVED',currentScopeStatus:'CURRENT',freshnessStatus:'NOT_APPLICABLE',contradictionStatus:'CLEAR',reasons:[],supportingEvidenceIds:[],recordId:null,activationPropositionId:null}:evaluateApplicability(project,{requirement,proposition,seen}),obligation=proofObligation||scoped('proofObligations').find(item=>relationshipIds(item,'PROPOSITION_ID').includes(pid)),sourceIds=relationshipIds(obligation,'REQUIRED_SOURCE_IDS').length?relationshipIds(obligation,'REQUIRED_SOURCE_IDS'):sourceIdsForRequirement(project,requirement,proposition),sourceState=evaluateSourceDependencies(project,{sourceIds,requiredEpistemicBases:scalarList(recordValue(obligation,'REQUIRED_EPISTEMIC_BASES'))}),expressionId=relationshipIds(obligation,'PROOF_EXPRESSION_ID')[0],expression=expressionId?scoped('proofExpressions').find(item=>recordId(item,'proofExpressions')===expressionId):null,observations=scoped('observationRecords').filter(item=>relationshipIds(item,'PROPOSITION_ID').includes(pid)),evaluations=observations.map(item=>evaluateObservationEntailment(project,{observation:item,propositionId:pid,proofObligation:obligation,scopeRule})),establishes=evaluations.filter(item=>item.sufficient&&item.truthValue==='TRUE'),refutes=evaluations.filter(item=>item.sufficient&&item.truthValue==='FALSE'),reasons=[...applicability.reasons,...sourceState.reasons];let truthValue='UNKNOWN',contradictionStatus=applicability.contradictionStatus,expressionState=null;
  if(applicability.applicability==='NOT_APPLICABLE')truthValue='TRUE';else if(applicability.applicability!=='APPLICABLE')reasons.push('Proposition applicability is not affirmatively established.');else if(establishes.length&&refutes.length){contradictionStatus='CONTRADICTED';reasons.push('Current sufficient observations both establish and refute the proposition.');}else if(refutes.length)truthValue='FALSE';else if(expression){expressionState=evaluateProofExpression(project,expression,{proofObligation:obligation,propositionId:pid,seen,scopeRule});truthValue=expressionState.valid?expressionState.truthValue:'UNKNOWN';reasons.push(...expressionState.reasons);if(expressionState.contradictionStatus==='CONTRADICTED')contradictionStatus='CONTRADICTED';if(!expressionState.valid)reasons.push('The current proof expression is structurally invalid.');}else if(establishes.length)truthValue='TRUE';else reasons.push('No current accepted proof expression or sufficient establishing observation exists.');
  if(sourceState.contradictionStatus==='CONTRADICTED')contradictionStatus='CONTRADICTED';if(!sourceState.sufficient&&truthValue==='TRUE')truthValue='UNKNOWN';
  const bases=[...establishes,...refutes].map(item=>item.epistemicBasis).concat(expressionState?.epistemicBasis?[expressionState.epistemicBasis]:[],sourceIds.length?[sourceState.epistemicBasis]:[]),basis=applicability.applicability==='NOT_APPLICABLE'?applicability.epistemicBasis:bases.includes('NONE')?'NONE':bases.includes('SELF_ASSERTED')?'SELF_ASSERTED':bases.includes('EXTERNALLY_SUPPORTED')?'EXTERNALLY_SUPPORTED':bases.includes('VERIFIED_EXTERNAL')?'VERIFIED_EXTERNAL':bases.includes('APPLICATION_OBSERVED')?'APPLICATION_OBSERVED':'NONE',freshnessStatuses=[...establishes,...refutes].map(item=>item.freshnessStatus).concat(expressionState?.freshnessStatus?[expressionState.freshnessStatus]:[],sourceIds.length?[sourceState.freshnessStatus]:[]),freshnessStatus=applicability.applicability==='NOT_APPLICABLE'?'NOT_APPLICABLE':freshnessStatuses.includes('EXPIRED')?'EXPIRED':freshnessStatuses.includes('UNKNOWN')?'UNKNOWN':freshnessStatuses.length?'CURRENT':'UNKNOWN';if(contradictionStatus==='CONTRADICTED'&&truthValue==='TRUE')truthValue='UNKNOWN';return {propositionId:pid,truthValue,epistemicBasis:basis,currentScopeStatus:'CURRENT',freshnessStatus,contradictionStatus,reasons:[...new Set(reasons)],supportingEvidenceIds:[...new Set([...evaluations.flatMap(item=>item.evidenceIds||[]),...safe(expressionState?.supportingEvidenceIds),...sourceState.evidenceIds])],derivationId:expressionId||null,applicability,sourceState};
}
const adjudication_NONE=Object.freeze(new Set(['','NONE','NOT APPLICABLE','N/A','NO','FALSE','0','EMPTY','NO FINDINGS','NO DEFECTS','NO FAILURES','NO DEVIATIONS','NO BLOCKERS','NOT PRESENT']));
const adjudication_SUCCESS=Object.freeze(new Set(['SATISFIED','SUCCESS','SUCCEEDED','PASS','PASSED','ACCEPTED','AUTHORIZED','CONFIRMED','COMPLETE','COMPLETED','VALID','MATCH','MATCHED','IDENTICAL','SAME','COMPLIANT','TRUE','YES','REJECTED_INVALID']));
const adjudication_FAILURE=Object.freeze(new Set(['VIOLATED','FAIL','FAILED','FAILURE','REJECTED','INVALID','MISMATCH','MISMATCHED','NONCOMPLIANT','FALSE','NO','ACCEPTED_INVALID']));
const adjudication_UNKNOWN=Object.freeze(new Set(['UNDETERMINED','UNKNOWN','BLOCKED','NOT RUN','NOT_RUN','UNAVAILABLE','UNVERIFIED','NOT ESTABLISHED']));
function adjudicationEmpty(value){if(value===null||value===undefined)return true;if(Array.isArray(value))return value.length===0||value.every(adjudicationEmpty);if(typeof value==='object')return Object.keys(value).length===0||Object.values(value).every(adjudicationEmpty);return adjudication_NONE.has(upper(value));}
function explicitlyRecorded(value){if(value===null||value===undefined)return false;if(typeof value==='string')return Boolean(value.trim());if(Array.isArray(value))return value.length>0;if(typeof value==='object')return Object.keys(value).length>0;return true;}
function adjudicationAdverse(value){if(value===null||value===undefined)return false;if(typeof value==='boolean')return value;if(typeof value==='number')return value>0;if(Array.isArray(value))return value.some(v=>!adjudicationEmpty(v));if(typeof value==='object')return Object.values(value).some(adjudicationAdverse);return !adjudicationEmpty(value);}
function controlledOutcome(value){const v=upper(value);if(adjudication_UNKNOWN.has(v))return 'UNDETERMINED';if(adjudication_FAILURE.has(v))return 'VIOLATED';if(adjudication_SUCCESS.has(v))return 'SATISFIED';return null;}
function evidenceRecordsFor(project,result,canonicalEvidence=null){if(Array.isArray(canonicalEvidence))return canonicalEvidence;const ids=[...new Set(safe(result?.evidenceRefs).map(String).filter(Boolean))];return ids.map(id=>records(project,'evidenceRecords').find(e=>recordId(e,'evidenceRecords')===id)).filter(Boolean);}
function resultExecutionIdentity(project,result){return String(recordValue(result,'EXECUTION_ID')||recordValue(result,'REG_EXEC_ID')||recordValue(result,'RUN_ID')||result?.relationships?.RUN_ID||recordValue(result,'PRODUCT_ID')||result?.relationships?.PRODUCT_ID||'').trim();}
function resultContextIdentity(project,result){
  const normalize=value=>{const text=String(value??'').trim();return !text||['NONE','UNKNOWN','NOT APPLICABLE','PENDING','UNASSIGNED'].includes(upper(text))?'':text;};
  const direct=normalize(recordValue(result,'CONTEXT_ID')||recordValue(result,'VERIFIER_CONTEXT_ID')||recordValue(result,'PRODUCTION_CONTEXT_ID')||result?.relationships?.CONTEXT_ID);if(direct)return direct;
  const runId=normalize(recordValue(result,'RUN_ID')||result?.relationships?.RUN_ID);if(runId){const run=records(project,'runs').find(r=>recordId(r,'runs')===runId);const runContext=normalize(recordValue(run,'CONTEXT_ID')||run?.relationships?.CONTEXT_ID);if(runContext)return runContext;}
  const rawId=normalize(result?.rawResponseId),proposalId=normalize(result?.sourceProposalId);if(rawId)return 'RAW_RESPONSE:'+rawId;if(proposalId)return 'PROPOSAL:'+proposalId;return '';
}
function resultIndependenceAssessment(project,result,test,requiredDimensions){
  const required=independenceDimensionList(requiredDimensions),unknown=reason=>({determination:'UNKNOWN',reasons:[reason],requiredDimensions:required,dimensions:{}});if(!required.length)return unknown('The release-bearing proof has no independence-dimension contract.');
  if(result?.source==='APPLICATION_TEST_RUNTIME'){
    const dimensions={},reasons=[];for(const dimension of required){let truthValue='UNKNOWN',epistemicBasis='NONE',dimensionReasons=[];if(dimension==='APPLICATION_INPUT_ISOLATION'){const exactInputs=String(recordValue(result,'INPUT_ARTIFACT_IDENTITIES')||'').trim(),worker=String(recordValue(result,'TEST_WORKER_SHA256')||'').trim(),build=String(recordValue(result,'RUNTIME_BUILD_IDENTITY')||'').trim();truthValue=exactInputs&&/^[a-f0-9]{64}$/i.test(worker)&&build?'TRUE':'UNKNOWN';epistemicBasis=truthValue==='TRUE'?'APPLICATION_OBSERVED':'NONE';if(truthValue!=='TRUE')dimensionReasons.push('Native result lacks exact application-bound input, worker-byte, or runtime-build identity.');}else dimensionReasons.push('The native application route cannot establish the required '+dimension+' dimension.');dimensions[dimension]={dimension,truthValue,epistemicBasis,reasons:dimensionReasons,evidenceIds:[]};reasons.push(...dimensionReasons);}return {determination:reasons.length?'UNKNOWN':'APPLICATION_ESTABLISHED',reasons:[...new Set(reasons)],requiredDimensions:required,dimensions};
  }
  const stage=Number(result?.stage||0),change=safe(project?.projectData?.acceptedChanges).filter(item=>!item.invalidatedBy&&String(item.status||'COMMITTED')==='COMMITTED'&&Number(item.stage)===stage&&String(item.proposalId||'')===String(result?.sourceProposalId||'')).at(-1),reviewerContextId=String(recordValue(result,'VERIFIER_CONTEXT_ID')||result?.relationships?.VERIFIER_CONTEXT_ID||change?.scope?.contextId||'').trim(),runId=String(recordValue(result,'RUN_ID')||result?.relationships?.RUN_ID||result?.scope?.runId||'').trim(),iterationId=String(result?.scope?.iterationId||'').trim(),productId=String(recordValue(result,'PRODUCT_ID')||result?.relationships?.PRODUCT_ID||result?.scope?.productId||'').trim(),product=productId?records(project,'products').find(item=>recordId(item,'products')===productId&&isActiveRecord(item)):null,productionContextId=String(recordValue(product,'PRODUCTION_CONTEXT_ID')||product?.relationships?.PRODUCTION_CONTEXT_ID||'').trim(),role=stage===23?'MEANING_REVIEW':stage===24?'ADVERSARIAL_REVIEW':stage===12||([17,19].includes(stage)&&upper(change?.operation)==='VERIFY')?'VERIFICATION':'EXTERNAL_RESULT';
  if(!reviewerContextId)return unknown('The release-bearing result is not bound through its accepted operation to one current application-recorded executor or reviewer context.');
  return evaluateContextIndependence(project,{role,iterationId,runId,verifierContextId:reviewerContextId,reviewerContextId,productionContextId,requiredDimensions:required});
}
function observedValue(result){for(const key of ['OBSERVED_RESULT','ACTUAL_RESULT','OBSERVED_MEANING','OBSERVATIONS','RESULT','VALIDATOR_RESULTS','MEANING_VERIFICATION_RESULTS','PROCESS_EVIDENCE','PRODUCT_EVIDENCE']){const value=recordValue(result,key);if(!adjudicationEmpty(value))return value;}return null;}
function evaluateEvidenceContract(test,result,canonicalEvidence,project){
  const evidence=evidenceRecordsFor(project,result,canonicalEvidence),reasons=[],mode=upper(recordValue(test,'EXECUTION_MODE')),artifactReq=upper(recordValue(test,'ARTIFACT_REQUIREMENTS')),requiresAttachment=Boolean(test&&artifactReq&&!adjudication_NONE.has(artifactReq));
  if(!evidence.length)reasons.push('No canonical evidence record is linked to the result; narrative alone is non-controlling.');
  for(const item of evidence){const native=item?.source==='APPLICATION_TEST_RUNTIME',authority=native?'APPLICATION':upper(recordValue(item,'AUTHORITY_TYPE')),attachmentId=String(recordValue(item,'ATTACHMENT_ID')||item?.relationships?.ATTACHMENT_ID||'').trim();if(adjudicationEmpty(native?recordValue(item,'APPLICATION_EVIDENCE_KIND'):recordValue(item,'KIND')))reasons.push('Canonical evidence kind is missing.');if(!authority||authority==='UNKNOWN')reasons.push('Canonical evidence authority type is missing or unknown.');if(adjudicationEmpty(native?recordValue(item,'APPLICATION_EVIDENCE_CONTENT'):recordValue(item,'CONTENT'))&&adjudicationEmpty(native?recordValue(item,'APPLICATION_EVIDENCE_DESCRIPTION'):recordValue(item,'DESCRIPTION'))&&adjudicationEmpty(recordValue(item,'LOCATION')))reasons.push('Canonical evidence contains no preserved observation payload.');if(attachmentId&&attachmentId!=='UNKNOWN'){const a=records(project,'artifacts').find(x=>recordId(x,'artifacts')===attachmentId);if(!a||upper(recordValue(a,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED')reasons.push('Referenced evidence attachment bytes are not application-verified.');}}
  if(requiresAttachment&&!evidence.some(item=>{const id=String(recordValue(item,'ATTACHMENT_ID')||item?.relationships?.ATTACHMENT_ID||'').trim();return id&&id!=='UNKNOWN';}))reasons.push('The controlling test requires an attachment-backed evidence record.');
  const executionId=resultExecutionIdentity(project,result),contextId=resultContextIdentity(project,result);
  if(test&&mode!=='APPLICATION_DETERMINISTIC'&&!executionId)reasons.push('Execution identity is not established.');
  if(test&&['INDEPENDENT_AGENT_REVIEW','EXTERNAL_AGENT_TOOL','HUMAN_INSPECTION','EXTERNAL_SYSTEM'].includes(mode)&&!contextId)reasons.push('Execution/reviewer context identity is not established.');
  return {sufficient:reasons.length===0,reasons,evidenceIds:evidence.map(e=>recordId(e,'evidenceRecords')),executionId,contextId};
}
function claimedDetermination(collection,record){if(collection==='deterministicResults'&&record?.source==='APPLICATION_TEST_RUNTIME')return upper(recordValue(record,'APPLICATION_DETERMINATION'));if(collection==='processAudits')return upper(recordValue(record,'PROCESS_DETERMINATION'));if(collection==='productAudits')return upper(recordValue(record,'PRODUCT_DETERMINATION'));if(collection==='regressionExecutions')return upper(recordValue(record,'RESULT'));if(collection==='products')return upper(recordValue(record,'STATUS'));return upper(recordValue(record,'DETERMINATION'));}
function testForResult(project,record){const id=String(recordValue(record,'TEST_ID')||record?.relationships?.TEST_ID||'').trim();return id?records(project,'tests').find(t=>recordId(t,'tests')===id):null;}
function effectiveRegressionDetermination(project,record){const evidence=evaluateEvidenceContract(null,record,null,project);if(!evidence.sufficient)return {determination:'UNDETERMINED',reasons:evidence.reasons};const phase=upper(recordValue(record,'PHASE')),out=controlledOutcome(recordValue(record,'RESULT'));if(!out)return {determination:'UNDETERMINED',reasons:['Regression RESULT is not a controlled application-evaluable outcome.']};if(phase==='PRE_CORRECTION')return {determination:out==='VIOLATED'?'SATISFIED':'VIOLATED',reasons:out==='VIOLATED'?[]:['Pre-correction regression did not demonstrate the expected failure.']};return {determination:out,reasons:out==='SATISFIED'?[]:['Post-correction regression did not establish success.']};}
function comparisonFacts(project,reqId,iterationId){const rows=(iterationId?recordsForIteration(project,'verification',iterationId):recordsForCurrentScope(project,'verification')).filter(r=>String(recordValue(r,'REQ_ID')||r.relationships?.REQ_ID||'')===reqId),determinations=rows.map(r=>effectiveDetermination('verification',r,testForResult(project,r),project)),ds=determinations;return {rows,determinations,runDeterminations:rows.map((r,i)=>({runId:String(recordValue(r,'RUN_ID')||r.relationships?.RUN_ID||''),testId:String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||''),determination:ds[i]})),allSatisfied:rows.length>0&&ds.every(d=>d==='SATISFIED'),anyViolation:ds.includes('VIOLATED'),anyUndetermined:ds.some(d=>d!=='SATISFIED'&&d!=='VIOLATED')};}
function validateRootCauseRecord(record,project){const reasons=[],defectId=String(recordValue(record,'DEFECT_ID')||record?.relationships?.DEFECT_ID||'').trim(),defect=records(project,'defects').find(d=>recordId(d,'defects')===defectId);if(!defect)reasons.push('RCA defect identity is missing or does not resolve.');for(const key of ['LAYER_TRACE','EARLIEST_DEFECTIVE_LAYER','ROOT_CAUSE','DOWNSTREAM_INVALIDATION'])if(adjudicationEmpty(recordValue(record,key)))reasons.push('RCA '+key+' is missing.');if(adjudicationEmpty(recordValue(record,'EVIDENCE'))&&!safe(record?.evidenceRefs).length)reasons.push('RCA evidence linkage is missing.');return {valid:reasons.length===0,reasons,defectId,responsibleLayer:String(recordValue(record,'EARLIEST_DEFECTIVE_LAYER')||'').trim()};}
function validateChangeTrace(record,project){const reasons=[],triggerRaw=recordValue(record,'TRIGGERING_DEFECT_IDS'),triggerIds=Array.isArray(triggerRaw)?triggerRaw.map(String):String(triggerRaw||'').split(/[;,\s]+/).filter(Boolean),layer=String(recordValue(record,'RESPONSIBLE_LAYER')||'').trim();if(!triggerIds.length)reasons.push('Changeset has no triggering defect identity.');if(!layer)reasons.push('Changeset responsible layer is missing.');if(adjudicationEmpty(recordValue(record,'EXACT_MODIFICATION')))reasons.push('Changeset exact modification is missing.');if(adjudicationEmpty(recordValue(record,'NEW_ARTIFACT_VERSION')))reasons.push('Changeset changed artifact/version identity is missing.');if(adjudicationEmpty(recordValue(record,'DOWNSTREAM_INVALIDATION')))reasons.push('Changeset downstream invalidation statement is missing.');for(const id of triggerIds){const rca=records(project,'rootCauses').find(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===id);if(!rca){reasons.push('No RCA is linked to '+id+'.');continue;}const rv=validateRootCauseRecord(rca,project);if(!rv.valid)reasons.push(...rv.reasons.map(x=>id+': '+x));if(rv.responsibleLayer&&layer&&upper(rv.responsibleLayer)!==upper(layer)&&upper(recordValue(record,'AUTHORIZATION'))!=='CONTROLLED OVERRIDE')reasons.push('Changeset responsible layer does not match RCA for '+id+' and no controlled override exists.');}return {valid:reasons.length===0,reasons,triggerIds};}
function confirmationDetermination(project,record){const reasons=[],iterationId=String(recordValue(record,'CONFIRMATION_ITERATION_ID')||record?.relationships?.CONFIRMATION_ITERATION_ID||record?.scope?.iterationId||'').trim(),iteration=records(project,'iterations').find(r=>recordId(r,'iterations')===iterationId);if(!iteration){reasons.push('Confirmation iteration identity is missing.');return {determination:'UNDETERMINED',reasons};}const ev=evaluateIteration(project,iterationId,'UNCHANGED_CONFIRMATION');if(!ev.complete)reasons.push(...ev.reasons);const matrix=verificationMatrix(project,iterationId);if(matrix.expected.length===0||matrix.missing.length||matrix.duplicates.length||matrix.invalid.length)reasons.push('Confirmation verification matrix is incomplete or invalid.');const bad=matrix.verification.map(r=>effectiveDetermination('verification',r,testForResult(project,r),project)).filter(x=>x!=='SATISFIED');if(bad.length)reasons.push('Confirmation contains a non-satisfied effective verification result.');const regs=records(project,'regressions').filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED');for(const reg of regs){const id=recordId(reg,'regressions'),exec=currentRegressionExecutions(project,iterationId).filter(x=>String(recordValue(x,'REG_ID')||x.relationships?.REG_ID||'')===id&&upper(recordValue(x,'PHASE'))!=='PRE_CORRECTION');if(exec.length!==1||effectiveRegressionDetermination(project,exec[0]).determination!=='SATISFIED')reasons.push('Active regression '+id+' is not successfully evidenced in confirmation.');}const defects=confirmedDefects(project).filter(d=>String(d.scope?.iterationId||'')===iterationId);if(defects.length)reasons.push('Confirmation iteration has new confirmed defects.');for(const req of mandatoryRequirements(project,scopeForIteration(project,iterationId))){const f=comparisonFacts(project,requirementId(req),iterationId);if(!f.allSatisfied)reasons.push('Confirmation comparison is not all-satisfied for '+requirementId(req)+'.');}return {determination:reasons.length?'UNDETERMINED':'SATISFIED',reasons};}
function auditDetermination(collection,record,project){const reasons=[],e=evaluateEvidenceContract(null,record,null,project);if(!e.sufficient)reasons.push(...e.reasons);if(collection==='processAudits'){for(const key of ['UNAUTHORIZED_MODIFICATION','PROCESS_DEFECTS','BLOCKERS'])if(adjudicationAdverse(recordValue(record,key)))reasons.push(key+' is materially adverse.');for(const key of ['APPROVED_INPUTS_VS_ACTUAL','APPROVED_INSTRUCTION_VS_ACTUAL','APPROVED_TOOLS_VS_ACTUAL','REQUIRED_TESTS_VS_EXECUTED','CHAIN_OF_CUSTODY']){const v=recordValue(record,key),out=controlledOutcome(v);if(out!=='SATISFIED'&& !['MATCH','MATCHED','IDENTICAL','SAME','COMPLIANT','COMPLETE','COMPLETED','INTACT'].includes(upper(v)))reasons.push(key+' is not application-establishable as satisfied.');}}else{for(const key of ['CRITICAL_DEFECTS','MAJOR_DEFECTS','MANDATORY_UNKNOWNS'])if(numeric(recordValue(record,key))>0)reasons.push(key+' is nonzero.');for(const key of ['PRODUCT_DEFECTS','BLOCKERS'])if(adjudicationAdverse(recordValue(record,key)))reasons.push(key+' is materially adverse.');for(const key of ['VALIDATOR_RESULTS','MEANING_VERIFICATION_RESULTS'])if(controlledOutcome(recordValue(record,key))!=='SATISFIED')reasons.push(key+' does not establish success.');const mandatory=numeric(recordValue(record,'MANDATORY_REQUIREMENT_COUNT')),affirmative=numeric(recordValue(record,'AFFIRMATIVE_SATISFACTION_COUNT'));if(mandatory>0&&affirmative!==mandatory)reasons.push('Affirmative satisfaction count does not equal mandatory requirement count.');}return {determination:reasons.length?'UNDETERMINED':'SATISFIED',reasons};}
function parseRepresentationInspectionObservation(record){
  const reasons=[],normalizeList=value=>[...new Set(safe(value).map(x=>String(x??'').trim()).filter(Boolean))].sort(),sameSet=(a,b)=>hash.sha256Value(normalizeList(a))===hash.sha256Value(normalizeList(b));
  let payload=null;try{payload=JSON.parse(String(recordValue(record,'OBSERVATIONS')||''));}catch{reasons.push('OBSERVATIONS must be the contracted strict JSON coverage object.');return {valid:false,reasons,payload:null};}
  if(!payload||typeof payload!=='object'||Array.isArray(payload)){reasons.push('OBSERVATIONS coverage payload is not an object.');return {valid:false,reasons,payload:null};}
  const requiredPageOrViewIds=normalizeList(payload.requiredPageOrViewIds),inspectedPageOrViewIds=normalizeList(payload.inspectedPageOrViewIds),requiredPackagedFileIds=normalizeList(payload.requiredPackagedFileIds),openedOrTestedPackagedFileIds=normalizeList(payload.openedOrTestedPackagedFileIds),requiredTransformationIds=normalizeList(payload.requiredTransformationIds),inspectedTransformationIds=normalizeList(payload.inspectedTransformationIds),observation=String(payload.observation||'').trim();
  if(!sameSet(requiredPageOrViewIds,inspectedPageOrViewIds))reasons.push('required page/view identities do not exactly match inspected page/view identities.');
  if(!sameSet(requiredPackagedFileIds,openedOrTestedPackagedFileIds))reasons.push('required packaged-file identities do not exactly match opened/tested packaged-file identities.');
  if(!sameSet(requiredTransformationIds,inspectedTransformationIds))reasons.push('required transformation identities do not exactly match inspected transformation identities.');
  if(!observation)reasons.push('representation observation is empty.');
  return {valid:reasons.length===0,reasons,payload,requiredPageOrViewIds,inspectedPageOrViewIds,requiredPackagedFileIds,openedOrTestedPackagedFileIds,requiredTransformationIds,inspectedTransformationIds,observation};
}

function evaluateResultConsistency(collection,record,test,project){
  const reasons=[],claimed=claimedDetermination(collection,record),evidence=evaluateEvidenceContract(test,record,null,project);let determination='UNDETERMINED';
  if(collection==='deterministicResults'&&record?.source==='APPLICATION_TEST_RUNTIME'){const app=upper(recordValue(record,'APPLICATION_DETERMINATION')||'UNDETERMINED'),epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});const nativeReasons=[...evidence.reasons,...epistemic.reasons];return {determination:nativeReasons.length&&app==='SATISFIED'?'UNDETERMINED':app,reasons:[...new Set(nativeReasons)],claimedDetermination:app,evidence};}
  if(collection==='regressionExecutions')return {...effectiveRegressionDetermination(project,record),claimedDetermination:claimed};
  if(collection==='preflightRecords'){const adverseTokens=new Set(['TRUE','YES','PRESENT','FOUND','UNRESOLVED','VIOLATED','FAILED','BLOCKED']);for(const key of ['MULTIPLE_INTERPRETATIONS','UNDEFINED_OBJECTS','UNSUPPLIED_DEPENDENCIES','INTERNAL_CONFLICTS','UNAVAILABLE_CAPABILITIES']){const value=recordValue(record,key);if(typeof value==='boolean'?value:adverseTokens.has(upper(value)))reasons.push(key+' establishes an unresolved preflight defect.');}const findings=upper(recordValue(record,'FINDINGS'));if(['DEFECT FOUND','MATERIAL DEFECT','UNRESOLVED DEFECT','MATERIAL AMBIGUITY','MATERIAL CONFLICT'].includes(findings))reasons.push('FINDINGS establishes an unresolved preflight defect.');if(!evidence.sufficient)reasons.push(...evidence.reasons);determination=reasons.length?'UNDETERMINED':'SATISFIED';}
  else if(collection==='confirmationRecords')return {...confirmationDetermination(project,record),claimedDetermination:claimed};
  else if(collection==='processAudits'||collection==='productAudits')return {...auditDetermination(collection,record,project),claimedDetermination:claimed};
  else if(collection==='products'){if(adjudicationAdverse(recordValue(record,'FAILURES')))reasons.push('Production execution reports failures.');if(adjudicationAdverse(recordValue(record,'DEVIATIONS')))reasons.push('Production execution reports deviations.');if(reasons.length)determination='VIOLATED';else determination=record?.completionState==='COMPLETED'?'SATISFIED':'UNDETERMINED';}
  else if(collection==='verification'){if(!evidence.sufficient)reasons.push(...evidence.reasons);const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const runId=String(recordValue(record,'RUN_ID')||record.relationships?.RUN_ID||''),iterationId=String(record.scope?.iterationId||runIterationId(record)||''),ind=evaluateContextIndependence(project,{role:'VERIFICATION',iterationId,runId,verifierContextId:String(recordValue(record,'VERIFIER_CONTEXT_ID')||'')});if(!['APPLICATION_ESTABLISHED','EXTERNALLY_SUPPORTED'].includes(ind.determination))reasons.push('Verification independence is neither application-established nor externally supported.');const observed=recordValue(record,'OBSERVED_RESULT'),expected=recordValue(record,'EXPECTED_RESULT')||recordValue(test,'EXPECTED_RESULT'),out=controlledOutcome(observed);if(out)determination=out;else if(!adjudicationEmpty(observed)&&!adjudicationEmpty(expected)&&upper(observed)===upper(expected))determination='SATISFIED';else determination='UNDETERMINED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}
  else if(collection==='deterministicResults'){if(!evidence.sufficient)reasons.push(...evidence.reasons);const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const actual=recordValue(record,'ACTUAL_RESULT'),expected=recordValue(record,'EXPECTED_RESULT')||recordValue(test,'EXPECTED_RESULT'),out=controlledOutcome(actual);if(out)determination=out;else if(!adjudicationEmpty(actual)&&!adjudicationEmpty(expected)&&upper(actual)===upper(expected))determination='SATISFIED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}
  else if(collection==='meaningResults'){if(!evidence.sufficient)reasons.push(...evidence.reasons);const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const comparison=controlledOutcome(recordValue(record,'EVIDENCE_BASED_COMPARISON')),observed=recordValue(record,'OBSERVED_MEANING'),required=recordValue(record,'REQUIRED_MEANING');if(comparison)determination=comparison;else if(!adjudicationEmpty(observed)&&!adjudicationEmpty(required)&&upper(observed)===upper(required))determination='SATISFIED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}
  else if(collection==='adversarialResults'){if(!evidence.sufficient)reasons.push(...evidence.reasons);const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const defectId=String(recordValue(record,'DEFECT_ID')||record.relationships?.DEFECT_ID||'').trim(),actual=upper(recordValue(record,'ACTUAL_RESULT')),testId=String(recordValue(record,'TEST_ID')||record.relationships?.TEST_ID||'').trim(),regId=String(recordValue(record,'REG_ID')||record.relationships?.REG_ID||'').trim();if(defectId)determination='VIOLATED';else if(regId){const executions=records(project,'regressionExecutions').filter(x=>String(recordValue(x,'REG_ID')||x.relationships?.REG_ID||'')===regId&&upper(recordValue(x,'PHASE'))!=='PRE_CORRECTION'),latest=executions.at(-1);determination=latest&&effectiveRegressionDetermination(project,latest).determination==='SATISFIED'?'SATISFIED':'UNDETERMINED';}else{const expected=upper(recordValue(test,'EXPECTED_RESULT')),failure=upper(recordValue(test,'FAILURE_CONDITION')),out=controlledOutcome(actual);if(out)determination=out;else if(expected&&actual===expected)determination='SATISFIED';else if(failure&&actual===failure)determination='VIOLATED';else if(['NO MATERIAL ADVERSARIAL DEFECT FOUND'].includes(actual))determination='SATISFIED';else determination='UNDETERMINED';}if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}
  else if(collection==='representationInspections'){if(!evidence.sufficient)reasons.push(...evidence.reasons);const requirement=records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,epistemic=evaluateEvidenceSufficiency(project,{requirement,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const defectId=String(recordValue(record,'DEFECT_ID')||record.relationships?.DEFECT_ID||'').trim(),artifactId=String(recordValue(record,'ARTIFACT_ID')||record.relationships?.ARTIFACT_ID||''),artifact=recordsForCurrentScope(project,'artifacts').find(a=>recordId(a,'artifacts')===artifactId),rendered=!adjudicationEmpty(recordValue(record,'RENDERING_OPENING_EVIDENCE')),coverage=parseRepresentationInspectionObservation(record);if(!coverage.valid)reasons.push(...coverage.reasons);if(defectId)determination='VIOLATED';else if(!artifact||!rendered||!coverage.valid)determination='UNDETERMINED';else determination='SATISFIED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}
  else determination=controlledOutcome(claimed)||'UNDETERMINED';
  const normalized=upper(recordValue(record,'APPLICATION_DETERMINATION'));if(['VIOLATED','UNDETERMINED'].includes(normalized))determination=normalized;else if(normalized==='SATISFIED'&&determination!=='SATISFIED')reasons.push('Application-normalized determination is favorable but the current effective evidence evaluation is not.');
  if(claimed==='SATISFIED'&&determination!=='SATISFIED')reasons.push('Claimed favorable determination conflicts with application-derived effective determination.');
  return {determination,reasons,claimedDetermination:claimed,evidence};
}
function effectiveDetermination(collection,record,test,project){return evaluateResultConsistency(collection,record,test,project).determination;}
function adjudicatedClone(project){
  const collections=['verification','deterministicResults','meaningResults','adversarialResults','representationInspections','preflightRecords','confirmationRecords','processAudits','productAudits','regressionExecutions','products'];
  const copy={...project,job:{...(project?.job||{})},release:{...(project?.release||{})},stages:{...(project?.stages||{})},projectData:{...(project?.projectData||{})}};
  for(const collection of collections)copy.projectData[collection]=safe(project?.projectData?.[collection]).map(record=>clone(record));
  for(const collection of collections)for(const r of records(copy,collection)){const test=testForResult(copy,r),d=effectiveDetermination(collection,r,test,copy);if(collection==='processAudits')recordFields(r).PROCESS_DETERMINATION=d;else if(collection==='productAudits')recordFields(r).PRODUCT_DETERMINATION=d;else if(collection==='regressionExecutions'){const phase=upper(recordValue(r,'PHASE'));recordFields(r).RESULT=phase==='PRE_CORRECTION'?(d==='SATISFIED'?'VIOLATED':d==='VIOLATED'?'SATISFIED':'UNDETERMINED'):d;}else if(collection==='products')recordFields(r).STATUS=d==='SATISFIED'?'COMPLETED':d==='VIOLATED'?'FAILED':'BLOCKED';else recordFields(r).DETERMINATION=d;}
  return copy;
}
function validateTraceIntegrity(kind,record,project){if(kind==='RCA')return validateRootCauseRecord(record,project);if(kind==='CHANGESET')return validateChangeTrace(record,project);return {valid:false,reasons:['Unknown trace-integrity kind.']};}
function requiredVerificationRelationSet(project,iterationId){
  const scope=scopeForIteration(project,iterationId),requirements=mandatoryRequirements(project,scope),runs=recordsForIteration(project,'runs',iterationId).filter(r=>runIterationId(r)===String(iterationId||'')),tuples=[],hasPropositionModel=recordsForScope(project,'propositions',scope).length>0||recordsForScope(project,'proofObligations',scope).length>0;
  for(const requirement of requirements){const reqId=requirementId(requirement),applicability=evaluateApplicability(project,{requirement});if(applicability.applicability==='NOT_APPLICABLE')continue;const propositions=propositionsForRequirement(project,requirement),tests=applicableTests(project,requirement,scope);for(const test of tests){const role=testRole(test),explicitPerRun=recordValue(test,'PER_RUN_VERIFICATION'),dimensions=scalarList(recordValue(test,'REQUIRED_RUN_DIMENSIONS')).map(upper),perRun=explicitPerRun===undefined||explicitPerRun===null||String(explicitPerRun).trim()===''?role==='REQUIRED_PROOF':truth(explicitPerRun)||dimensions.includes('RUN_ID');if(!perRun||['ADVISORY','NEGATIVE_ONLY'].includes(role))continue;let propositionIds=propositionIdsFor(test);if(hasPropositionModel){if(!propositionIds.length&&propositions.length===1)propositionIds=[recordId(propositions[0],'propositions')];if(!propositionIds.length)continue;}else propositionIds=[''];for(const propositionId of propositionIds)for(const run of runs)tuples.push({propositionId:propositionId||null,requirementId:reqId,runId:recordId(run,'runs'),testId:recordId(test,'tests')});}}
  const key=tuple=>[...(tuple.propositionId?[tuple.propositionId]:[]),tuple.requirementId,tuple.runId,tuple.testId].join('|'),deduped=[],seen=new Set();for(const tuple of tuples){const value=key(tuple);if(seen.has(value))continue;seen.add(value);deduped.push({...tuple,key:value});}return {relationSetId:'VERIFICATION-RELATION-SET-'+hash.sha256Value({scope,tuples:deduped}).slice(0,20).toUpperCase(),scope,requirements,runs,tuples:deduped,keys:deduped.map(item=>item.key),usesPropositions:hasPropositionModel};
}
function verificationMatrix(project,iterationId){const relationSet=requiredVerificationRelationSet(project,iterationId),{scope,requirements,runs}=relationSet,verification=recordsForIteration(project,'verification',iterationId),expected=relationSet.keys,expectedSet=new Set(expected),counts=new Map();for(const r of verification){const key=verificationKey(r,{includeProposition:relationSet.usesPropositions});counts.set(key,(counts.get(key)||0)+1);}const missing=expected.filter(k=>!counts.has(k)),duplicates=[...counts].filter(([k,c])=>expectedSet.has(k)&&c!==1).map(([key,count])=>({key,count}));const invalid=verification.filter(r=>{const key=verificationKey(r,{includeProposition:relationSet.usesPropositions}),test=testForResult(project,r),evaluation=evaluateResultConsistency('verification',r,test,project),ind=evaluateContextIndependence(project,{role:'VERIFICATION',iterationId,runId:String(recordValue(r,'RUN_ID')||r.relationships?.RUN_ID||''),verifierContextId:String(recordValue(r,'VERIFIER_CONTEXT_ID')||'')});return !expectedSet.has(key)||!['SATISFIED','VIOLATED','UNDETERMINED'].includes(evaluation.determination)||!evaluation.evidence?.sufficient||['UNKNOWN','VIOLATED','SELF_ASSERTED'].includes(ind.determination);});return {scope,requirements,runs,verification,expected,counts,missing,duplicates,invalid,relationSet,coverage:expected.length?expected.filter(k=>counts.get(k)===1).length/expected.length:0};}
function currentRegressionExecutions(project,iterationId){return recordsForIteration(project,'regressionExecutions',iterationId).filter(r=>!iterationId||String(recordValue(r,'ITERATION_ID')||r.relationships?.ITERATION_ID||r.scope?.iterationId||'')===String(iterationId));}
function activeRegressions(project){return records(project,'regressions').filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED');}
function acceptedOperationSet(project,stage,scopeRule={}){
  const proposals=safe(project.projectData.responseProposals),out=new Set(),keys=['iterationId','candidateId','baselineId','productId'];
  for(const c of acceptedChanges(project,stage)){
    const proposal=proposals.find(p=>p.proposalId===c.proposalId),op=c.operation||proposal?.envelope?.operation,scope={...(proposal?.envelope?.scope||{}),...(c.scope||{}),...(c.applicationScopeBinding||{})};
    if(keys.some(key=>scopeRule[key]!==undefined&&scopeRule[key]!==null&&String(scope[key]??'')!==String(scopeRule[key])))continue;
    if(op)out.add(String(op));
  }
  if(Number(stage)===17){
    const appIteration=records(project,'iterations').find(r=>Number(r.stage)===17&&isActiveRecord(r)&&(!scopeRule.iterationId||recordId(r,'iterations')===String(scopeRule.iterationId))&&(!scopeRule.candidateId||String(recordValue(r,'CANDIDATE_ID')||r.scope?.candidateId||'')===String(scopeRule.candidateId)));
    if(appIteration)out.add('FREEZE');
  }
  if(Number(stage)===19){const appIteration=records(project,'iterations').find(r=>Number(r.stage)===19&&isActiveRecord(r)&&upper(recordValue(r,'PURPOSE'))==='UNCHANGED_CONFIRMATION'&&(!scopeRule.iterationId||recordId(r,'iterations')===String(scopeRule.iterationId))&&(!scopeRule.candidateId||String(recordValue(r,'CANDIDATE_ID')||r.scope?.candidateId||'')===String(scopeRule.candidateId)));if(appIteration)out.add('CONFIRM_FREEZE');}
  return out;
}
function candidateComponentIdentity(project,candidateId){const c=records(project,'candidateFreezes').find(r=>recordId(r,'candidateFreezes')===String(candidateId||''));return c?hash.sha256Value(recordValue(c,'COMPONENT_HASHES')||{}):'';}
function evaluateIteration(project,iterationId,mode='INITIAL'){
  const matrix=verificationMatrix(project,iterationId),runs=matrix.runs,scope=matrix.scope,iteration=records(project,'iterations').find(r=>recordId(r,'iterations')===String(iterationId||'')),stage=Number(runs[0]?.stage||iteration?.stage||0),expectedCandidate=String(scope.candidateId||''),candidateIdentity=candidateComponentIdentity(project,expectedCandidate),requiredRunOperation=mode==='INITIAL'?'COMPLETE':'EXECUTE_RUN',runChanges=acceptedChanges(project,stage).filter(c=>String(c.operation||'')===requiredRunOperation&&String(c.scope?.iterationId||'')===String(iterationId)),raw=safe(project.projectData.rawResponses),receipts=safe(project.projectData.outputReceipts),runFailures=[];
  for(const run of runs){const runId=recordId(run,'runs'),contextId=String(recordValue(run,'CONTEXT_ID')||run.relationships?.CONTEXT_ID||'');const links=runChanges.filter(c=>String(c.scope?.runId||'')===runId&&String(c.scope?.contextId||'')===contextId);if(links.length!==1){runFailures.push(runId);continue;}const rr=raw.find(x=>x.rawResponseId===links[0].rawResponseId&&!x.invalidatedBy),rc=receipts.find(x=>x.rawResponseId===links[0].rawResponseId);if(!rr||!rc||String(rc.runId||'')!==runId||String(rc.contextId||'')!==contextId)runFailures.push(runId);}
  const independence=evaluateContextIndependence(project,{role:'RUN_BATCH',iterationId}),comparisons=recordsForIteration(project,'comparisons',iterationId),defects=recordsForIteration(project,'defects',iterationId),rca=recordsForIteration(project,'rootCauses',iterationId),regExec=currentRegressionExecutions(project,iterationId),regFailures=[];
  if(mode!=='INITIAL')for(const reg of activeRegressions(project)){const id=recordId(reg,'regressions'),xs=regExec.filter(x=>String(recordValue(x,'REG_ID')||x.relationships?.REG_ID||'')===id),latest=xs.at(-1);if(xs.length!==1||!latest||effectiveRegressionDetermination(project,latest).determination!=='SATISFIED')regFailures.push(id);}
  const unexplained=comparisons.filter(r=>truth(recordValue(r,'CORRECTNESS_AFFECTING_VARIANCE'))&&!truth(recordValue(r,'AUTHORIZED_VARIANCE'))),ops=acceptedOperationSet(project,stage,{iterationId}),reasons=[];
  const requiredOps=stage===19?['CONFIRM_FREEZE','EXECUTE_RUN','VERIFY','COMPARE','REGRESSION_VERIFY','CONFIRM']:stage===17?['FREEZE','EXECUTE_RUN','VERIFY','COMPARE']:[];
  if(stage===17){
    const comparison={rows:comparisons},signals=iterationDefectSignals(project,iterationId,matrix,comparison),confirmed=defects.filter(item=>!['REJECTED','DUPLICATE','NOT A DEFECT','RETIRED'].includes(upper(recordValue(item,'STATUS')||'CONFIRMED')));
    if(signals.required)requiredOps.push('ROOT_CAUSE');
    if(activeRegressions(project).length)requiredOps.push('REGRESSION');
    if(confirmed.length)requiredOps.push('CORRECT');
  }
  if(runs.length!==10)reasons.push('Exactly ten current runs are required.');if(!['APPLICATION_ESTABLISHED','EXTERNALLY_SUPPORTED'].includes(independence.determination))reasons.push(...(independence.reasons.length?independence.reasons:['Required run-batch independence dimensions are not sufficiently established or externally supported.']));if(!candidateIdentity)reasons.push('Every run must resolve to one exact frozen candidate identity and component hash set.');if(runFailures.length)reasons.push('Every current run requires exactly one accepted run-bound raw response and receipt: '+runFailures.join(', ')+'.');if(mode!=='INITIAL'&&(matrix.expected.length===0||matrix.missing.length||matrix.duplicates.length||matrix.invalid.length))reasons.push('The current REQ × RUN × TEST matrix is incomplete, non-independent, or evidentially insufficient.');if(regFailures.length)reasons.push('Every active permanent regression requires exactly one sufficiently evidenced successful execution in the current iteration: '+regFailures.join(', ')+'.');const missingOps=requiredOps.filter(op=>!ops.has(op));if(missingOps.length)reasons.push('Required stage operations are missing: '+missingOps.join(', ')+'.');if(unexplained.length)reasons.push('Unexplained correctness-affecting variance remains.');
  const stability=executionStability(project,iterationId);
  return {mode,iterationId,runs:runs.map(r=>recordId(r,'runs')),contextCount:independence.contextCount||0,candidateIds:independence.candidateIds||[],candidateIdentity,matrix,independence,stability,comparisonCount:comparisons.length,defectCount:defects.length,rootCauseCount:rca.length,regressionExecutionCount:regExec.length,unexplainedVarianceCount:unexplained.length,complete:reasons.length===0,reasons};
}
function deriveMandatoryTestCoverage(project){const requirements=mandatoryRequirements(project);const covered=requirements.filter(r=>applicableTests(project,r).length).length;return {value:requirements.length?covered/requirements.length:0,inputReferences:requirements.map(requirementId),calculationVersion:'1'};}
function deriveVerificationMatrix(project){const iteration=latestIteration(project,[10,17,19]);const id=recordId(iteration,'iterations');const m=verificationMatrix(project,id);return {value:{expected:m.expected.length,complete:m.expected.length-m.missing.length,missing:m.missing,duplicates:m.duplicates,coverage:m.coverage},inputReferences:[...m.expected],calculationVersion:'1'};}
function deriveConvergence(project){return {value:convergenceMetrics(project),inputReferences:[recordId(latestIteration(project,[17]),'iterations')],calculationVersion:'1'};}
function deriveRelease(project){const value=releaseMetrics(project);return {value,inputReferences:value.inputReferences||[],calculationVersion:'1'};}
function deriveArtifactIdentity(project){const current=records(project,'artifactIdentities');return {value:{authorized:current.length>0&&current.every(r=>upper(recordValue(r,'AUTHORIZATION'))==='AUTHORIZED'),count:current.length},inputReferences:current.map(r=>recordId(r,'artifactIdentities')),calculationVersion:'1'};}
function deriveEvidenceChains(project){const current=records(project,'evidenceChains');return {value:{complete:current.length>0&&current.every(r=>upper(recordValue(r,'STATUS'))==='COMPLETE'),count:current.length},inputReferences:current.map(r=>recordId(r,'evidenceChains')),calculationVersion:'1'};}
const DERIVATIONS=Object.freeze({'stage06.mandatoryTestCoverage':deriveMandatoryTestCoverage,'stage12.verificationMatrix':deriveVerificationMatrix,'stage18.convergence':deriveConvergence,'stage27.release':deriveRelease,'stage28.artifactIdentity':deriveArtifactIdentity,'stage29.evidenceChains':deriveEvidenceChains});

function coverageMetrics(project,iterationIdOverride=null){const iterationId=iterationIdOverride||recordId(latestIteration(project,[10,17,19]),'iterations')||project.job.CURRENT_ITERATION||'',scope=iterationId?scopeForIteration(project,iterationId):currentScope(project),requirements=mandatoryRequirements(project,scope),tests=iterationId?recordsForScope(project,'tests',scope):recordsForCurrentScope(project,'tests'),covered=new Set(tests.map(testRequirementId).filter(Boolean)),requirementCoverage=requirements.length?requirements.filter(req=>covered.has(requirementId(req))).length/requirements.length:0,matrix=verificationMatrix(project,iterationId),regressions=records(project,'regressions').filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),executions=currentRegressionExecutions(project,iterationId),successful=new Set(executions.filter(r=>upper(recordValue(r,'PHASE'))!=='PRE_CORRECTION'&&effectiveRegressionDetermination(project,r).determination==='SATISFIED').map(r=>String(recordValue(r,'REG_ID')||r.relationships?.REG_ID||'')));return {mandatoryRequirementCount:requirements.length,requirementsWithTests:requirements.filter(req=>covered.has(requirementId(req))).length,requirementCoverage,iterationRunCount:matrix.runs.length,expectedVerificationCount:matrix.expected.length,actualVerificationPairCount:matrix.expected.filter(key=>matrix.counts.get(key)===1).length,actualVerificationTripleCount:matrix.expected.filter(key=>matrix.counts.get(key)===1).length,verificationCoverage:matrix.coverage,missingVerificationTriples:matrix.missing,duplicateVerificationTriples:matrix.duplicates,activeRegressionCount:regressions.length,successfulRegressionCount:regressions.filter(r=>successful.has(recordId(r,'regressions'))).length,regressionSuccess:regressions.length?regressions.filter(r=>successful.has(recordId(r,'regressions'))).length/regressions.length:1};}
function convergenceMetrics(project){const latest=latestIteration(project,[17]),iterationId=recordId(latest,'iterations'),coverage=coverageMetrics(project,iterationId),material=unresolvedMaterialDefects(project).filter(r=>!iterationId||!r.scope?.iterationId||r.scope.iterationId===iterationId),critical=material.filter(r=>upper(recordValue(r,'SEVERITY'))==='CRITICAL').length,major=material.filter(r=>upper(recordValue(r,'SEVERITY'))==='MAJOR').length,verification=iterationId?recordsForIteration(project,'verification',iterationId):recordsForCurrentScope(project,'verification'),mandatoryUnknowns=verification.filter(r=>effectiveDetermination('verification',r,testForResult(project,r),project)!=='SATISFIED').length+openBlockers(project).length,comparisons=iterationId?recordsForIteration(project,'comparisons',iterationId):recordsForCurrentScope(project,'comparisons'),contradictions=detectCurrentContradictions(project).filter(x=>x.severity==='RELEASE_MATERIAL').length,ambiguities=comparisons.filter(r=>adjudicationAdverse(recordValue(r,'INTERPRETATION_VARIANCE'))).length,unexplainedVariance=comparisons.filter(r=>adjudicationAdverse(recordValue(r,'CORRECTNESS_AFFECTING_VARIANCE'))&&!truth(recordValue(r,'AUTHORIZED_VARIANCE'))).length,result={iterationId,requirementCoverage:coverage.requirementCoverage,verificationCoverage:coverage.verificationCoverage,regressionSuccess:coverage.regressionSuccess,criticalDefects:critical,majorDefects:major,mandatoryUnresolvedUnknowns:mandatoryUnknowns,contradictions,ambiguities,unexplainedVariance};result.converged=Boolean(iterationId)&&result.requirementCoverage===1&&result.verificationCoverage===1&&result.regressionSuccess===1&&critical===0&&major===0&&mandatoryUnknowns===0&&contradictions===0&&ambiguities===0&&unexplainedVariance===0;return result;}
function releaseVerificationTrust(project,record){
  const runId=String(recordValue(record,'RUN_ID')||record?.relationships?.RUN_ID||'').trim(),iterationId=String(record?.scope?.iterationId||'').trim(),verifierContextId=String(recordValue(record,'VERIFIER_CONTEXT_ID')||'').trim(),ind=evaluateContextIndependence(project,{role:'VERIFICATION',iterationId,runId,verifierContextId});
  if(ind.determination==='APPLICATION_ESTABLISHED')return {determination:'APPLICATION_ESTABLISHED',reasons:[],basis:'REGISTERED_CONTEXT'};
  const rawId=String(record?.rawResponseId||'').trim(),proposalId=String(record?.sourceProposalId||'').trim(),stage=Number(record?.stage||0),accepted=acceptedChanges(project,stage).find(change=>change.rawResponseId===rawId&&(!proposalId||change.proposalId===proposalId)),receipt=safe(project.projectData.outputReceipts).find(item=>item.rawResponseId===rawId),proposal=safe(project.projectData.responseProposals).find(item=>item.proposalId===(proposalId||accepted?.proposalId)),run=records(project,'runs').find(item=>recordId(item,'runs')===runId),runContext=String(recordValue(run,'CONTEXT_ID')||run?.relationships?.CONTEXT_ID||'').trim(),generatorChanges=acceptedChanges(project,Number(run?.stage||0)).filter(change=>String(change?.scope?.runId||'')===runId),generatorRawIds=new Set(generatorChanges.map(change=>String(change.rawResponseId||'')).filter(Boolean)),scope=record?.scope||{},reasons=[];
  if(!rawId||!proposalId||!accepted||!receipt||!proposal)reasons.push('No canonical accepted external verification execution receipt resolves to this verification record.');
  if(generatorRawIds.has(rawId))reasons.push('Verifier execution receipt is the same accepted response as the generating run.');
  if(!verifierContextId||['UNKNOWN','NONE','NOT APPLICABLE','UNASSIGNED','PENDING'].includes(upper(verifierContextId)))reasons.push('Verifier context identity is not supplied.');
  if(runContext&&upper(verifierContextId)===upper(runContext))reasons.push('Verifier context identity matches the generating run context.');
  if(receipt){if(Number(receipt.stage)!==stage)reasons.push('Verifier receipt stage does not match the verification record.');if(scope.inputVersion&&safe(receipt.inputIdentities).length&&!safe(receipt.inputIdentities).includes(scope.inputVersion))reasons.push('Verifier receipt is not bound to the authorized input version.');for(const [key,receiptKey] of [['requirementsVersion','requirementsVersion'],['testSuiteVersion','testSuiteVersion'],['instructionVersion','instructionVersion']])if(scope[key]&&receipt[receiptKey]&&String(scope[key])!==String(receipt[receiptKey]))reasons.push('Verifier receipt '+receiptKey+' does not match the authorized verification scope.');}
  const op=String(accepted?.operation||proposal?.envelope?.operation||'COMPLETE');if(stage===12&&op!=='COMPLETE')reasons.push('Stage 12 verifier receipt is not the canonical COMPLETE verification response.');if([17,19].includes(stage)&&op!=='VERIFY')reasons.push('Iterative verifier receipt is not the canonical VERIFY response.');
  return {determination:reasons.length?'UNDETERMINED':'APPLICATION_ESTABLISHED',reasons,basis:reasons.length?'NONE':'CANONICAL_EXTERNAL_EXECUTION_RECEIPT',rawResponseId:rawId||null};
}
function legacyReleaseMetrics(project){const requirements=mandatoryRequirements(project,currentScope(project)),deterministic=recordsForCurrentScope(project,'deterministicResults'),meaning=recordsForCurrentScope(project,'meaningResults'),adversarial=recordsForCurrentScope(project,'adversarialResults'),inspections=recordsForCurrentScope(project,'representationInspections'),processAudits=recordsForCurrentScope(project,'processAudits'),productAudits=recordsForCurrentScope(project,'productAudits'),confirmations=recordsForCurrentScope(project,'confirmationRecords'),regressions=records(project,'regressions').filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),regExec=recordsForCurrentScope(project,'regressionExecutions'),baseline=recordsForCurrentScope(project,'baselines').at(-1),product=recordsForCurrentScope(project,'products').at(-1),blockers=openBlockers(project),defects=unresolvedMaterialDefects(project),resultCollections=[['deterministicResults',deterministic],['meaningResults',meaning],['adversarialResults',adversarial],['representationInspections',inspections]],evaluated=resultCollections.flatMap(([collection,rows])=>rows.map(r=>({collection,record:r,e:evaluateResultConsistency(collection,r,testForResult(project,r),project)}))),failed=evaluated.filter(x=>x.e.determination==='VIOLATED'),unknown=evaluated.filter(x=>x.e.determination!=='SATISFIED'&&x.e.determination!=='VIOLATED');let satisfied=0,violated=0,undetermined=0;const blockingRequirements=[],violatedRequirements=[];for(const req of requirements){const id=requirementId(req),expectedTests=recordsForCurrentScope(project,'tests').filter(test=>testRequirementId(test)===id&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY'))),results=[...deterministic,...meaning,...adversarial].filter(r=>resultRequirementId(project,r)===id&&String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'')),byTest=new Map();for(const result of results){const testId=String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||'');if(!byTest.has(testId))byTest.set(testId,[]);byTest.get(testId).push(result);}const groups=expectedTests.map(test=>(byTest.get(recordId(test,'tests'))||[]).map(r=>effectiveDetermination(resultCollections.find(([,rows])=>rows.includes(r))?.[0]||'verification',r,test,project))),hasViolation=groups.some(group=>group.includes('VIOLATED')),complete=expectedTests.length>0&&groups.every(group=>group.length===1&&group[0]==='SATISFIED');if(hasViolation){violated++;violatedRequirements.push(id);}else if(complete)satisfied++;else{undetermined++;blockingRequirements.push(id);}}const activeRegIds=new Set(regressions.map(r=>recordId(r,'regressions'))),passedReg=new Set(regExec.filter(r=>upper(recordValue(r,'PHASE'))!=='PRE_CORRECTION'&&effectiveRegressionDetermination(project,r).determination==='SATISFIED').map(r=>String(recordValue(r,'REG_ID')||r.relationships?.REG_ID||''))),missingReg=[...activeRegIds].filter(id=>!passedReg.has(id)),auditsOk=processAudits.length===1&&productAudits.length===1&&effectiveDetermination('processAudits',processAudits[0],null,project)==='SATISFIED'&&effectiveDetermination('productAudits',productAudits[0],null,project)==='SATISFIED',confirmationOk=confirmations.length===1&&effectiveDetermination('confirmationRecords',confirmations[0],null,project)==='SATISFIED',productOk=product&&effectiveDetermination('products',product,null,project)==='SATISFIED',contradictions=detectCurrentContradictions(project),insufficient=evaluated.filter(x=>!x.e.evidence?.sufficient),releaseIteration=latestIteration(project,[19,17,10]),releaseIterationId=recordId(releaseIteration,'iterations'),releaseVerificationMatrix=releaseIterationId?verificationMatrix(project,releaseIterationId):{expected:[],missing:['NO_ITERATION'],duplicates:[],verification:[]},releaseVerificationTrustChecks=releaseVerificationMatrix.verification.map(record=>({record,trust:releaseVerificationTrust(project,record)})),releaseVerificationTrustFailures=releaseVerificationTrustChecks.filter(item=>item.trust.determination!=='APPLICATION_ESTABLISHED');let determination='ACCEPTED';if(violated||failed.length||defects.some(r=>['CRITICAL','MAJOR'].includes(upper(recordValue(r,'SEVERITY')))))determination='REJECTED';else if(!requirements.length||undetermined||unknown.length||blockers.length||!baseline||!productOk||!auditsOk||!confirmationOk||missingReg.length||contradictions.length||insufficient.length||!releaseVerificationMatrix.expected.length||releaseVerificationMatrix.missing.length||releaseVerificationMatrix.duplicates.length||releaseVerificationTrustFailures.length)determination='BLOCKED';const inputReferences=[...requirements.map(requirementId),...evaluated.map(x=>recordId(x.record,x.collection)),...processAudits.map(r=>recordId(r,'processAudits')),...productAudits.map(r=>recordId(r,'productAudits')),...confirmations.map(r=>recordId(r,'confirmationRecords')),...regExec.map(r=>recordId(r,'regressionExecutions')),recordId(baseline,'baselines'),recordId(product,'products')].filter(Boolean);return {determination,mandatoryRequirementCount:requirements.length,satisfied,violated,undetermined,validatorCount:evaluated.length,failedValidatorIds:failed.map(x=>recordId(x.record,x.collection)),unknownValidatorIds:unknown.map(x=>recordId(x.record,x.collection)),criticalDefects:defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='CRITICAL').length,majorDefects:defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='MAJOR').length,blockingRequirements,violatedRequirements,blockerIds:blockers.map(r=>recordId(r,'blockers')),missingRegressionIds:missingReg,baselineId:recordId(baseline,'baselines')||null,productId:recordId(product,'products')||null,auditsOk,confirmationOk,productOk,contradictions,insufficientEvidenceResultIds:insufficient.map(x=>recordId(x.record,x.collection)).filter(Boolean),releaseVerificationTrustFailures:releaseVerificationTrustFailures.map(item=>({verificationId:recordId(item.record,'verification'),reasons:item.trust.reasons,basis:item.trust.basis})),inputReferences};}
function releaseMetrics(project){const propositions=recordsForCurrentScope(project,'propositions');if(!propositions.length)return legacyReleaseMetrics(project);const requirements=recordsForCurrentScope(project,'requirements').filter(item=>normativeClassification(item)!=='OPTIONAL'),registry=deriveProofObligationRegistry(project),states=propositions.filter(proposition=>{const requirementIdValue=relationshipIds(proposition,'REQUIREMENT_ID')[0]||relationshipIds(proposition,'REQ_ID')[0];return requirements.some(requirement=>requirementId(requirement)===requirementIdValue);}).map(proposition=>evaluatePropositionState(project,{proposition})),satisfiedStates=states.filter(state=>state.truthValue==='TRUE'&&state.contradictionStatus==='CLEAR'),violatedStates=states.filter(state=>state.truthValue==='FALSE'),undeterminedStates=states.filter(state=>state.truthValue==='UNKNOWN'||state.contradictionStatus==='CONTRADICTED'),blockers=openBlockers(project),defects=unresolvedMaterialDefects(project),baseline=recordsForCurrentScope(project,'baselines').at(-1),product=recordsForCurrentScope(project,'products').at(-1),processAudits=recordsForCurrentScope(project,'processAudits'),productAudits=recordsForCurrentScope(project,'productAudits'),confirmations=recordsForCurrentScope(project,'confirmationRecords'),results=[...recordsForCurrentScope(project,'deterministicResults').map(record=>['deterministicResults',record]),...recordsForCurrentScope(project,'meaningResults').map(record=>['meaningResults',record]),...recordsForCurrentScope(project,'adversarialResults').map(record=>['adversarialResults',record]),...recordsForCurrentScope(project,'representationInspections').map(record=>['representationInspections',record])],effective=results.map(([collection,record])=>({collection,record,state:evaluateResultConsistency(collection,record,testForResult(project,record),project)})),failed=effective.filter(item=>item.state.determination==='VIOLATED'),unknown=effective.filter(item=>item.state.determination==='UNDETERMINED'),registryIntegrity=evaluateRegistryIntegrity(project),contradictions=detectCurrentContradictions(project),dependencies=recordsForCurrentScope(project,'environmentDependencies').map(item=>evaluateEnvironmentDependency(project,item)),classificationUnknown=requirements.filter(item=>normativeClassification(item)==='UNKNOWN'),applicabilityUnknown=states.filter(item=>item.applicability?.applicability==='UNKNOWN'),expiredEvidence=states.filter(item=>['EXPIRED','UNKNOWN'].includes(item.freshnessStatus)),dueDeferred=deferredFailureTestsDue(project,27),auditsOk=processAudits.length===1&&productAudits.length===1&&effectiveDetermination('processAudits',processAudits[0],null,project)==='SATISFIED'&&effectiveDetermination('productAudits',productAudits[0],null,project)==='SATISFIED',confirmationOk=confirmations.length===1&&effectiveDetermination('confirmationRecords',confirmations[0],null,project)==='SATISFIED',productOk=Boolean(product&&effectiveDetermination('products',product,null,project)==='SATISFIED');let determination='ACCEPTED';if(violatedStates.length||failed.length||defects.some(item=>['CRITICAL','MAJOR','UNKNOWN'].includes(upper(recordValue(item,'ACCEPTED_SEVERITY')||recordValue(item,'SEVERITY')))))determination='REJECTED';else if(!states.length||undeterminedStates.length||!registry.complete||blockers.length||!baseline||!productOk||!auditsOk||!confirmationOk||!registryIntegrity.complete||contradictions.length||dependencies.some(item=>item.truthValue!=='TRUE')||classificationUnknown.length||applicabilityUnknown.length||expiredEvidence.length||dueDeferred.length||unknown.length)determination='BLOCKED';const inputReferences=[...requirements.map(requirementId),...states.map(item=>item.propositionId),...registry.obligations.map(item=>recordId(item,'proofObligations')||item.id),...effective.map(item=>recordId(item.record,item.collection)),recordId(baseline,'baselines'),recordId(product,'products')].filter(Boolean);return {determination,productReleaseEligibility:determination==='ACCEPTED'?'PRODUCT_RELEASE_ELIGIBLE':'NOT_ELIGIBLE',mandatoryRequirementCount:requirements.length,mandatoryPropositionCount:states.length,satisfied:satisfiedStates.length,violated:violatedStates.length,undetermined:undeterminedStates.length,validatorCount:effective.length,failedValidatorIds:failed.map(item=>recordId(item.record,item.collection)),unknownValidatorIds:unknown.map(item=>recordId(item.record,item.collection)),criticalDefects:defects.filter(item=>upper(recordValue(item,'ACCEPTED_SEVERITY')||recordValue(item,'SEVERITY'))==='CRITICAL').length,majorDefects:defects.filter(item=>upper(recordValue(item,'ACCEPTED_SEVERITY')||recordValue(item,'SEVERITY'))==='MAJOR').length,blockingRequirements:[...new Set(undeterminedStates.map(item=>item.propositionId))],violatedRequirements:[...new Set(violatedStates.map(item=>item.propositionId))],blockerIds:blockers.map(item=>recordId(item,'blockers')),missingRegressionIds:registryIntegrity.reasons,baselineId:recordId(baseline,'baselines')||null,productId:recordId(product,'products')||null,auditsOk,confirmationOk,productOk,contradictions,insufficientEvidenceResultIds:unknown.map(item=>recordId(item.record,item.collection)),releaseVerificationTrustFailures:[],unknownApplicabilityIds:applicabilityUnknown.map(item=>item.propositionId),insufficientEpistemicBasisIds:states.filter(item=>item.epistemicBasis==='NONE'||item.epistemicBasis==='SELF_ASSERTED').map(item=>item.propositionId),expiredEvidenceIds:expiredEvidence.map(item=>item.propositionId),dueDeferredTestIds:dueDeferred.map(item=>item.mutationId),dependencyBlockerIds:dependencies.filter(item=>item.truthValue!=='TRUE').map(item=>item.dependencyId),releaseEvidenceGraphAcyclic:!detectEvidenceJustificationCycles(project).length,inputReferences};}
function gate(stage,project){
  ensureShape(project);
  const reasons=[];
  if(stage>1&&project.stages[stage-1].status!=='COMPLETE')reasons.push(`Stage ${String(stage-1).padStart(2,'0')} is not complete.`);
  const questions=unresolvedHumanRequests(project,stage);
  if(questions.length)reasons.push(`${questions.length} blocking human-input request${questions.length===1?' is':'s are'} unresolved.`);
  const blockers=openBlockers(project,stage);
  if(blockers.length)reasons.push(`${blockers.length} open mandatory blocker${blockers.length===1?' affects':'s affect'} this stage.`);
  const executionFailures=safe(project.projectData.executionFailures).filter(x=>Number(x.stage)===Number(stage)&&!x.invalidatedBy&&!x.resolvedBy&&upper(x.status||'OPEN')!=='RESOLVED');
  if(executionFailures.length)reasons.push(`${executionFailures.length} accepted execution failure${executionFailures.length===1?' blocks':'s block'} this stage until a successful replacement response is accepted.`);
  const changes=acceptedChanges(project,stage);
  const hasAccepted=changes.length>0;
  const acceptedOperation=operation=>changes.filter(change=>upper(change.operation||'COMPLETE')===upper(operation));
  const collection=name=>records(project,name,{stage});
  const all=name=>records(project,name);
  const requireAccepted=()=>{if(!hasAccepted)reasons.push('No validated agent response has been accepted for this stage.');};
  const requireCount=(name,min,message)=>{if(collection(name).length<min)reasons.push(message||`${min} ${name} record${min===1?' is':'s are'} required.`);};
  const previousReasons=reasons.length;

  switch(stage){
    case 1:{
      if(!String(project.job.EXACT_USER_OBJECTIVE_VERBATIM||'').trim())reasons.push('Verbatim User Job Input is required.');
      const closure=stageSuboperationClosure(project,1),latest=closure.finalChange;reasons.push(...closure.reasons);
      const confirmed=safe(project.projectData.stageConfirmations).some(item=>Number(item.stage)===1&&item.confirmed===true&&!item.invalidatedBy&&item.acceptedChangeId===latest?.changeId&&item.inputVersion===project.job.CURRENT_INPUT_VERSION&&upper(item.operation||closure.finalOperation)===closure.finalOperation);
      if(!confirmed)reasons.push('Human confirmation bound to the current accepted change and input version is required.');
      break;
    }
    case 2:{
      requireAccepted();const determination=upper(project.stages[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION),sources=recordsForCurrentScope(project,'sources').filter(record=>Number(record.stage)===2),search=sourceSearchAssessment(project);reasons.push(...search.reasons);
      if(!sources.length){if(determination!=='NO_APPLICABLE_EXTERNAL_SOURCE')reasons.push('Either at least one legitimate independent external source or an explicit NO_APPLICABLE_EXTERNAL_SOURCE determination is required.');}
      else if(determination!=='APPLICABLE_SOURCES_ESTABLISHED')reasons.push('Established source records require SOURCE_APPLICABILITY_DETERMINATION = APPLICABLE_SOURCES_ESTABLISHED.');
      for(const source of sources)reasons.push(...schema.sourceClassificationIssues(recordFields(source)).map(issue=>`${recordId(source,'sources')}: ${issue}`));
      if(collection('sourceConflicts').some(record=>['UNRESOLVED','BLOCKED','UNKNOWN','OPEN'].includes(upper(recordValue(record,'RESOLUTION_STATUS')))))reasons.push('An external-source conflict remains unresolved or blocked.');
      break;
    }
    case 3:{
      requireAccepted();const sourceIds=recordsForCurrentScope(project,'sources').map(record=>recordId(record,'sources')),noSource=upper(project.stages[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE';
      if(!sourceIds.length){if(!noSource)reasons.push('Stage 03 cannot proceed without a current Stage 02 source set or valid no-source determination.');break;}
      requireCount('research',1);const researched=new Set(recordsForCurrentScope(project,'research').map(record=>String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||''))),missing=sourceIds.filter(id=>!researched.has(id));if(missing.length)reasons.push(`Research is missing for source(s): ${missing.join(', ')}.`);
      const researchData=project.stages[3]?.agentData||{};
      const allKnownControllingSourcesExamined=sourceIds.length>0&&missing.length===0;if(!allKnownControllingSourcesExamined)reasons.push('Stage 03 has not established current research coverage for every controlling source.');
      if(!truth(researchData.SECOND_CONFLICT_AND_EXCEPTION_PASS_COMPLETED))reasons.push('Stage 03 second conflict and exception pass is incomplete.');
      if(numeric(researchData.LATEST_PASS_NUMBER)<2)reasons.push('Stage 03 requires a completed second conflict and exception pass.');
      if(!falsey(researchData.NEW_MATERIAL_CATEGORY_FOUND_IN_LATEST_PASS))reasons.push('Stage 03 is not exhausted because the latest pass still found new material.');
      for(const field of ['SOURCE_AUTHENTICITY_REFERENCES','SOURCE_FRESHNESS_REFERENCES','TIME_SENSITIVE_FINDINGS','SUPERSEDED_MATERIAL','RESEARCH_EPISTEMIC_LIMITATIONS'])if(!explicitlyRecorded(researchData[field]))reasons.push(`Stage 03 must retain ${field} from the current source authenticity/freshness review.`);
      const authenticityRefs=new Set(scalarList(researchData.SOURCE_AUTHENTICITY_REFERENCES)),freshnessRefs=new Set(scalarList(researchData.SOURCE_FRESHNESS_REFERENCES)),limitations=String(typeof researchData.RESEARCH_EPISTEMIC_LIMITATIONS==='string'?researchData.RESEARCH_EPISTEMIC_LIMITATIONS:JSON.stringify(researchData.RESEARCH_EPISTEMIC_LIMITATIONS||'')),superseded=String(typeof researchData.SUPERSEDED_MATERIAL==='string'?researchData.SUPERSEDED_MATERIAL:JSON.stringify(researchData.SUPERSEDED_MATERIAL||''));for(const source of recordsForCurrentScope(project,'sources')){const id=recordId(source,'sources'),auth=upper(recordValue(source,'AUTHENTICITY_STATUS'))||'UNKNOWN',fresh=controlledFreshness(recordValue(source,'FRESHNESS_STATUS'));if(!authenticityRefs.has(id))reasons.push(`Stage 03 authenticity references omit current source ${id}.`);if(!freshnessRefs.has(id))reasons.push(`Stage 03 freshness references omit current source ${id}.`);if((auth!=='VERIFIED'||['UNKNOWN','EXPIRED'].includes(fresh))&&!limitations.includes(id))reasons.push(`Stage 03 epistemic limitations do not identify source ${id} (${auth}/${fresh}).`);if(fresh==='EXPIRED'&&!superseded.includes(id))reasons.push(`Stage 03 superseded-material accounting omits expired source ${id}.`);}
      const sourceById=new Map(recordsForCurrentScope(project,'sources').map(source=>[recordId(source,'sources'),source]));for(const collectionName of ['research','candidateRequirements'])for(const finding of recordsForCurrentScope(project,collectionName)){const sourceId=relationshipIds(finding,'SOURCE_ID')[0]||String(recordValue(finding,'SOURCE_ID')||''),source=sourceById.get(sourceId);if(!source)continue;const expected={authenticityStatus:upper(recordValue(source,'AUTHENTICITY_STATUS'))||'UNKNOWN',authenticityBasis:controlledBasis(recordValue(source,'AUTHENTICITY_BASIS')),freshnessStatus:controlledFreshness(recordValue(source,'FRESHNESS_STATUS'))};if(upper(recordValue(finding,'SOURCE_AUTHENTICITY_STATUS'))!==expected.authenticityStatus||controlledBasis(recordValue(finding,'SOURCE_AUTHENTICITY_BASIS'))!==expected.authenticityBasis||controlledFreshness(recordValue(finding,'SOURCE_FRESHNESS_STATUS'))!==expected.freshnessStatus)reasons.push(`Stage 03 ${collectionName} finding ${recordId(finding,collectionName)} is not bound to the current application-normalized source authenticity and freshness consequence.`);if(expected.authenticityStatus!=='VERIFIED'||expected.freshnessStatus!=='CURRENT'){if(!truth(recordValue(finding,'SOURCE_EPISTEMIC_LIMITATION_REQUIRED')))reasons.push(`Stage 03 ${collectionName} finding ${recordId(finding,collectionName)} omitted its application-derived epistemic limitation.`);if(!limitations.includes(sourceId))reasons.push(`Stage 03 epistemic limitations do not cover finding ${recordId(finding,collectionName)} from source ${sourceId}.`);}}
      break;
    }
    case 4:{
      if(project.stages?.[1]?.status!=='COMPLETE')reasons.push('Stage 04 requires exhausted Stage 01 human-authority intake.');
      if(project.stages?.[3]?.status!=='COMPLETE')reasons.push('Stage 04 requires exhausted Stage 03 source research.');
      const closure=stageSuboperationClosure(project,4);reasons.push(...closure.reasons);requireCount('requirements',1);const accounting=evaluateObligationAccounting(project),stageData=project.stages?.[4]?.agentData||{},dispositionChallenges=safe(stageData.OBLIGATION_DISPOSITION_CHALLENGE_RECORDS),atomicityChallenges=safe(stageData.ATOMICITY_CHALLENGE_RECORDS),challengeText=JSON.stringify([...dispositionChallenges,...atomicityChallenges]);if(!accounting.complete)reasons.push(...accounting.reasons);if(adjudicationAdverse(stageData.UNRESOLVED_SEMANTIC_CHALLENGES))reasons.push('Stage 04 has unresolved disposition or atomicity semantic challenges.');
      for(const req of collection('requirements')){
        for(const name of schema.RECORD_SCHEMAS.requirements.required)if(!String(recordValue(req,name)||'').trim())reasons.push(`${recordId(req,'requirements')}: ${name} is missing.`);
        const sourceId=String(recordValue(req,'SOURCE_ID')||req.relationships?.SOURCE_ID||'').trim(),userRelationship=String(recordValue(req,'USER_INPUT_RELATIONSHIP')||'').trim();
        if(!sourceId&&!userRelationship)reasons.push(`${recordId(req,'requirements')}: requirement lacks source provenance or an explicit User Job Input relationship.`);
        const primary=primaryPropositionsForRequirement(project,req),allPropositions=propositionsForRequirement(project,req),activation=activationPropositionsForRequirement(project,req),classification=proposedNormativeClassification(req);if(primary.length!==1)reasons.push(`${recordId(req,'requirements')}: exactly one current primary proposition is required.`);if(allPropositions.length>1&&!relationshipIds(req,'PRIMARY_PROPOSITION_ID').length)reasons.push(`${recordId(req,'requirements')}: multiple proposition records require one application-selected PRIMARY_PROPOSITION_ID.`);if(allPropositions.length>1&&allPropositions.some(item=>propositionRole(item)==='UNKNOWN'))reasons.push(`${recordId(req,'requirements')}: every proposition in a multi-proposition requirement requires a controlled proposition role.`);if(classification==='CONDITIONAL'&&activation.length!==1)reasons.push(`${recordId(req,'requirements')}: conditional classification requires exactly one distinct activation proposition.`);if(classification!=='CONDITIONAL'&&activation.length)reasons.push(`${recordId(req,'requirements')}: a nonconditional requirement cannot contain an activation proposition.`);const obligationIds=obligationIdsFromRelationship(userRelationship);if(obligationIds.length>1&&!challengeText.includes(recordId(req,'requirements')))reasons.push(`${recordId(req,'requirements')}: a multi-obligation merge lacks the required independent atomicity challenge.`);
      }
      for(const [obligationId,entries] of accounting.dispositions)if(entries.some(entry=>['inapplicable','retained nonnormative context'].includes(entry.disposition))&&!challengeText.includes(obligationId))reasons.push(`Stage 04 disposition ${obligationId} lacks the required independent challenge.`);
      break;
    }
    case 5:{
      requireAccepted();const stageData=project.stages?.[5]?.agentData||{},requirements=recordsForCurrentScope(project,'requirements'),applicability=recordsForCurrentScope(project,'applicabilityRecords');
      if(collection('requirementResolutions').some(record=>['OPEN','UNRESOLVED','BLOCKED','UNKNOWN'].includes(upper(recordValue(record,'STATUS')))))reasons.push('A requirement-set defect remains unresolved.');
      for(const requirement of requirements){const classification=normativeClassification(requirement),primaryPropositionIds=primaryPropositionsForRequirement(project,requirement).map(item=>recordId(item,'propositions')),applicabilityRecord=applicability.find(item=>relationshipIds(item,'SUBJECT_ID').some(id=>primaryPropositionIds.includes(id)));if(classification==='UNKNOWN')reasons.push(requirementId(requirement)+': normative classification remains UNKNOWN.');const proposedClass=controlledNormativeClass(recordValue(applicabilityRecord,'NORMATIVE_CLASSIFICATION_PROPOSAL')),reductionReviewRequired=['CONDITIONAL','OPTIONAL'].includes(proposedClass);if(reductionReviewRequired){const review=applicabilityRecord?applicabilityReviewAssessment(project,applicabilityRecord):{ready:false,reasons:['No exact applicability/classification proposal exists.']};if(!review.ready)reasons.push(requirementId(requirement)+': normative-classification reduction lacks an accepted independent exact review. '+review.reasons.join(' '));if(!relationshipIds(requirement,'NORMATIVE_CLASSIFICATION_REVIEW_ID').length)reasons.push(requirementId(requirement)+': normalized classification lacks its canonical review relationship.');}if(['MANDATORY','CONDITIONAL','UNKNOWN'].includes(classification)){const state=evaluateApplicability(project,{requirement}),hasFirstClassApplicability=Boolean(applicabilityRecord);if(state.applicability==='UNKNOWN'||state.contradictionStatus==='CONTRADICTED')reasons.push(requirementId(requirement)+': applicability is unknown or contradicted.');if(!hasFirstClassApplicability)reasons.push(requirementId(requirement)+': no first-class current applicability record exists for its primary proposition.');if(classification==='CONDITIONAL'&&!state.activationPropositionId)reasons.push(requirementId(requirement)+': conditional classification lacks an activation proposition on its current applicability record.');}}
      if(adjudicationAdverse(stageData.PROOF_STRUCTURE_GAPS))reasons.push('Stage 05 proof-structure gaps remain unresolved.');
      break;
    }
    case 6:{
      requireAccepted();
      const metrics=coverageMetrics(project),mandatoryIds=new Set(mandatoryRequirements(project).map(requirementId)),mandatoryTests=collection('tests').filter(test=>mandatoryIds.has(testRequirementId(test)));
      if(metrics.mandatoryRequirementCount===0)reasons.push('No active mandatory requirements exist to cover.');
      if(metrics.requirementCoverage!==1)reasons.push(`Mandatory requirement-to-test coverage is ${(metrics.requirementCoverage*100).toFixed(2)}%, not 100%.`);
      const incomplete=mandatoryTests.filter(test=>!TEST_EXECUTION_ACTIONS[upper(recordValue(test,'EXECUTION_MODE'))]||!String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim()||!String(recordValue(test,'ARTIFACT_REQUIREMENTS')||'').trim());
      if(incomplete.length)reasons.push(`${incomplete.length} mandatory test definition(s) lack complete execution responsibility, capability, or artifact requirements.`);
      const unavailable=mandatoryTests.filter(test=>upper(recordValue(test,'EXECUTION_MODE'))==='UNAVAILABLE');
      if(unavailable.length)reasons.push(`${unavailable.length} mandatory test definition(s) have unavailable execution capability and remain blocked.`);
      const unsupportedApplication=mandatoryTests.filter(test=>upper(recordValue(test,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&!applicationTestSupported(test));
      if(unsupportedApplication.length)reasons.push(`${unsupportedApplication.length} mandatory test definition(s) claim APPLICATION_DETERMINISTIC without a registered application-native executor.`);
      const executionPlan=testExecutionPlan(project),missingCurrentArtifacts=executionPlan.items.filter(item=>mandatoryIds.has(item.requirementId)&&item.artifactRequired&&item.artifactIds.length>0&&!item.artifactReady);
      if(missingCurrentArtifacts.length)reasons.push(`${missingCurrentArtifacts.length} mandatory test definition(s) depend on exact artifact bytes that are missing or no longer application-verified.`);
      const registry=deriveProofObligationRegistry(project);if(recordsForCurrentScope(project,'propositions').length&&!registry.complete)reasons.push(...registry.reasons);for(const test of mandatoryTests){if(!String(recordValue(test,'TEST_PROPOSITION_TEXT')||'').trim())reasons.push(recordId(test,'tests')+': test proposition text is missing.');if(!propositionIdsFor(test).length)reasons.push(recordId(test,'tests')+': target proposition identity is missing.');if(!String(recordValue(test,'POSITIVE_RESULT_MEANING')||'').trim()||!String(recordValue(test,'NEGATIVE_RESULT_MEANING')||'').trim())reasons.push(recordId(test,'tests')+': positive and negative result meanings are required.');if(releaseBearingTest(test)){const semantic=semanticReviewAssessment(project,{subjectKind:'TEST',subject:test});if(!semantic.ready)reasons.push(...semantic.reasons.map(reason=>recordId(test,'tests')+': '+reason));const contract=proofContractForTest(test);reasons.push(...contract.reasons.map(reason=>recordId(test,'tests')+': '+reason));}const runDimensions=scalarList(recordValue(test,'REQUIRED_RUN_DIMENSIONS'));if(runDimensions.length&&!recordValue(test,'EXPECTED_VARIANCE_CONTRACT'))reasons.push(recordId(test,'tests')+': repeated-execution test lacks a frozen expected-variance contract.');}for(const expression of recordsForCurrentScope(project,'proofExpressions')){const validation=schema.validateProofExpression?schema.validateProofExpression(expressionPayload(expression)): {valid:Boolean(expressionPayload(expression)),issues:[]},semantic=proofExpressionSemanticReadiness(project,expression);if(!validation.valid)reasons.push(recordId(expression,'proofExpressions')+': '+validation.issues.join(' '));if(!semantic.ready)reasons.push(...semantic.reasons.map(reason=>recordId(expression,'proofExpressions')+': '+reason));}for(const dependency of recordsForCurrentScope(project,'environmentDependencies')){const state=evaluateEnvironmentDependency(project,dependency);if(state.truthValue!=='TRUE')reasons.push('Required environment dependency '+state.dependencyId+' is not current and established.');}
      // Stage 06 does not require future product outputs that cannot yet exist, but an exact canonical artifact dependency must remain currently verified.
      break;
    }
    case 7:{
      requireAccepted();
      const reqs=mandatoryRequirements(project),covered=new Set(all('failureTests').map(testRequirementId));
      const missing=reqs.filter(req=>!covered.has(requirementId(req))).map(requirementId);
      if(missing.length)reasons.push(`Failure tests are missing for: ${missing.join(', ')}.`);
      for(const mutation of all('failureTests')){const id=recordId(mutation,'failureTests'),availability=upper(recordValue(mutation,'FAILURE_TEST_AVAILABILITY_CLASS')||'EXECUTABLE_NOW');if(!String(recordValue(mutation,'FIXTURE')||'').trim())reasons.push(id+': failure fixture is not identifiable.');if(!String(recordValue(mutation,'EXPECTED_REJECTION')||'').trim())reasons.push(id+': expected rejection is missing.');if(!String(recordValue(mutation,'EVIDENCE')||'').trim()&&!safe(mutation.evidenceRefs).length)reasons.push(id+': fixture-design or execution evidence is missing.');if(availability==='DEFERRED_TARGET_DEPENDENT'){const contract=deferredFailureTestContract(project,mutation);reasons.push(...contract.issues.map(issue=>id+': '+issue));continue;}if(availability==='NOT_APPLICABLE')continue;if(availability==='BLOCKED'){reasons.push(id+': failure test execution is blocked.');continue;}if(availability!=='EXECUTABLE_NOW'){reasons.push(id+': failure-test availability class is invalid.');continue;}if(!String(recordValue(mutation,'ACTUAL_RESULT')||'').trim())reasons.push(id+': actual execution result is missing.');const actual=upper(recordValue(mutation,'ACTUAL_RESULT')),expected=upper(recordValue(mutation,'EXPECTED_REJECTION')),acceptedInvalid=/\b(?:ACCEPTED|ALLOWED|PASSED|NOT REJECTED|VALID)\b/.test(actual)&&!/\b(?:REJECTED|BLOCKED|FAILED|INVALID)\b/.test(actual);if(expected.includes('REJECT')&&acceptedInvalid){const defectId=String(recordValue(mutation,'VALIDATOR_DEFECT_ID')||mutation.relationships?.VALIDATOR_DEFECT_ID||'').trim();if(!defectId)reasons.push('A known-invalid fixture was accepted without a linked validator defect.');reasons.push(id+': known-invalid fixture was accepted; Stage 07 remains blocked until the validator is corrected and the preserved fixture is actually rejected.');}}
      break;
    }
    case 8:{requireAccepted();requireCount('instructions',1);const reqs=mandatoryRequirements(project);const traced=new Set(recordsForCurrentScope(project,'instructionTraces').map(r=>String(recordValue(r,'REQ_ID')||r.relationships?.REQ_ID||'')));const missing=reqs.filter(r=>!traced.has(requirementId(r))).map(requirementId);if(missing.length)reasons.push('Instruction traces are missing for: '+missing.join(', ')+'.');break;}
    case 9:{
      requireAccepted();requireCount('preflightRecords',1);
      if(collection('preflightRecords').some(record=>['VIOLATED','UNDETERMINED','BLOCKED','REJECTED'].includes(upper(recordValue(record,'DETERMINATION')))))reasons.push('Instruction preflight contains an unresolved material finding.');
      const preflightChange=changes.at(-1),preflightReviewer=String(preflightChange?.scope?.contextId||''),preflightIndependence=evaluateContextIndependence(project,{role:'PREFLIGHT_REVIEW',reviewerContextId:preflightReviewer});
      if(!['APPLICATION_ESTABLISHED','EXTERNALLY_SUPPORTED'].includes(preflightIndependence.determination))reasons.push('Preflight reviewer independence is not established: '+preflightIndependence.reasons.join(' '));
      break;
    }
    case 10:requireCount('iterations',1);requireCount('candidateFreezes',1);break;
    case 11:{requireAccepted();const iteration=latestIteration(project,[10,11]);const ev=evaluateIteration(project,recordId(iteration,'iterations'),'INITIAL');if(!ev.complete)reasons.push(...ev.reasons);break;}
    case 12:{requireAccepted();const iteration=latestIteration(project,[10]);const m=verificationMatrix(project,recordId(iteration,'iterations'));if(m.runs.length!==10)reasons.push('The current verification batch does not contain exactly ten runs.');if(m.expected.length===0||m.coverage!==1)reasons.push('REQ × RUN × TEST verification coverage is not 100%.');if(m.duplicates.length)reasons.push('Duplicate verification triples exist.');if(m.invalid.length)reasons.push('A verification triple lacks independently established/supported verification or sufficient evidence.');break;}
    case 13:{
      requireAccepted();const iteration=latestIteration(project,[10]),iterationId=recordId(iteration,'iterations'),scope=scopeForIteration(project,iterationId),reqs=mandatoryRequirements(project,scope),compared=new Set(recordsForIteration(project,'comparisons',iterationId).map(record=>String(recordValue(record,'REQ_ID')||record.relationships?.REQ_ID||'')));
      const missing=reqs.filter(req=>!compared.has(requirementId(req))).map(requirementId);
      if(missing.length)reasons.push(`Cross-run comparison is missing for: ${missing.join(', ')}.`);
      break;
    }
    case 14:{
      requireAccepted();const defects=confirmedDefects(project),analysed=new Set(all('rootCauses').map(record=>String(recordValue(record,'DEFECT_ID')||record.relationships?.DEFECT_ID||'')));
      const missing=defects.filter(defect=>!analysed.has(recordId(defect,'defects'))).map(defect=>recordId(defect,'defects'));
      if(missing.length)reasons.push(`Root-cause analysis is missing for: ${missing.join(', ')}.`);
      break;
    }
    case 15:{requireAccepted();const defects=confirmedDefects(project),regs=records(project,'regressions'),covered=new Map(regs.map(r=>[String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||''),r]));const missing=defects.filter(d=>!covered.has(recordId(d,'defects'))).map(d=>recordId(d,'defects'));if(missing.length)reasons.push('Permanent regression definitions are missing for: '+missing.join(', ')+'.');const executions=records(project,'regressionExecutions');for(const reg of regs){const id=recordId(reg,'regressions');if(!executions.some(e=>String(recordValue(e,'REG_ID')||e.relationships?.REG_ID||'')===id&&upper(recordValue(e,'PHASE'))==='PRE_CORRECTION'&&effectiveRegressionDetermination(project,e).determination==='SATISFIED'))reasons.push('Regression '+id+' lacks an actual sufficiently evidenced pre-correction failing execution.');}break;}
    case 16:requireAccepted();if(confirmedDefects(project).length&&!collection('changes').length)reasons.push('A responsible-layer changeset or blocker is required for confirmed defects.');break;
    case 17:{requireAccepted();const iteration=latestIteration(project,[17]);const ev=evaluateIteration(project,recordId(iteration,'iterations'),'CORRECTED');if(!ev.complete)reasons.push(...ev.reasons);break;}
    case 18:{
      requireAccepted();const metrics=convergenceMetrics(project);
      if(!metrics.converged)reasons.push('All convergence conditions are not simultaneously satisfied.');
      {const convergenceIteration=latestIteration(project,[17]),convergenceScope=scopeForIteration(project,recordId(convergenceIteration,'iterations')),propositions=recordsForScope(project,'propositions',convergenceScope);if(propositions.length){const registry=deriveProofObligationRegistry(project),states=propositions.map(proposition=>evaluatePropositionState(project,{proposition,scopeRule:convergenceScope})),dueDeferred=deferredFailureTestsDue(project,18,convergenceScope),unknownVariance=recordsForScope(project,'comparisons',convergenceScope).filter(item=>adjudicationAdverse(recordValue(item,'UNKNOWN_VARIANCES'))||upper(recordValue(item,'VARIANCE_CLASSIFICATION'))==='UNKNOWN');if(!registry.complete)reasons.push(...registry.reasons);for(const state of states)if(state.truthValue!=='TRUE'||state.contradictionStatus!=='CLEAR'||['EXPIRED','UNKNOWN'].includes(state.freshnessStatus)||state.applicability?.applicability==='UNKNOWN')reasons.push('Mandatory proposition '+state.propositionId+' is not simultaneously current, applicable, uncontradicted, fresh, and satisfied.');if(dueDeferred.length)reasons.push('Due deferred failure tests remain incomplete: '+dueDeferred.map(item=>item.mutationId).join(', ')+'.');if(detectEvidenceJustificationCycles(project).length)reasons.push('Current release-bearing evidence contains a justification cycle.');if(unknownVariance.length)reasons.push('Expected-variance classification remains UNKNOWN.');if(adjudicationAdverse(project.stages?.[18]?.agentData?.UNRESOLVED_SEMANTIC_CHALLENGES))reasons.push('A required semantic challenge remains unresolved.');}}
      break;
    }
    case 19:{requireAccepted();const iteration=latestIteration(project,[19]);const ev=evaluateIteration(project,recordId(iteration,'iterations'),'UNCHANGED_CONFIRMATION');if(!ev.complete)reasons.push(...ev.reasons);requireCount('confirmationRecords',1);if(collection('confirmationRecords').some(r=>upper(recordValue(r,'DETERMINATION'))!=='SATISFIED'))reasons.push('Unchanged confirmation is not affirmatively satisfied.');break;}
    case 20:{requireAccepted();const confirmations=recordsForCurrentScope(project,'confirmationRecords').filter(record=>upper(recordValue(record,'DETERMINATION'))==='SATISFIED'),baselines=recordsForCurrentScope(project,'baselines'),iteration=latestIteration(project,[19]),iterationId=recordId(iteration,'iterations'),candidateId=iterationCandidateId(project,iterationId);if(confirmations.length!==1)reasons.push('Exactly one current successful unchanged confirmation is required.');if(baselines.length!==1)reasons.push('Exactly one current production baseline is required.');for(const baseline of baselines){const approved=recordValue(baseline,'APPROVED_VERSIONS')||{};if(upper(recordValue(baseline,'HUMAN_AUTHORIZATION'))!=='AUTHORIZED'||String(recordValue(baseline,'SUPPORTING_CONFIRMATION_ID')||baseline.relationships?.SUPPORTING_CONFIRMATION_ID||'')!==recordId(confirmations[0],'confirmationRecords'))reasons.push('The current baseline is not bound to the current authorized unchanged confirmation.');if(String(approved.iterationId||'')!==iterationId||String(approved.candidateId||'')!==candidateId||!candidateComponentIdentity(project,candidateId))reasons.push('The current baseline does not preserve the exact unchanged-confirmation iteration and frozen candidate identity.');}break;}
    case 21:{requireAccepted();const products=recordsForCurrentScope(project,'products'),product=products.at(-1),productId=recordId(product,'products'),baselines=recordsForCurrentScope(project,'baselines'),baseline=baselines.at(-1),artifacts=recordsForCurrentScope(project,'artifacts').filter(a=>productId&&String(a.scope?.productId||'')===productId),verified=artifacts.filter(a=>upper(recordValue(a,'AVAILABILITY'))==='BYTES_PERSISTED_AND_VERIFIED'&&/^[a-f0-9]{64}$/i.test(String(recordValue(a,'SHA256')||''))&&Number.isInteger(Number(recordValue(a,'BYTE_SIZE')))&&Number(recordValue(a,'BYTE_SIZE'))>=0&&String(recordValue(a,'STORAGE_REFERENCE')||'').startsWith('indexeddb:')),inventory=safe(recordValue(product,'GENERATED_ARTIFACT_INVENTORY')).map(String).sort(),verifiedIds=verified.map(a=>recordId(a,'artifacts')).sort(),contextId=String(recordValue(product,'PRODUCTION_CONTEXT_ID')||product?.relationships?.PRODUCTION_CONTEXT_ID||''),context=records(project,'freshContexts').find(r=>recordId(r,'freshContexts')===contextId&&isActiveRecord(r));if(products.length!==1)reasons.push('Exactly one current product execution identity is required.');if(baselines.length!==1||String(recordValue(product,'BASELINE_ID')||product?.relationships?.BASELINE_ID||'')!==recordId(baseline,'baselines'))reasons.push('The current product execution is not bound to exactly one current approved baseline.');if(!context||Number(context.stage)!==21)reasons.push('The product execution does not use a dedicated current Stage 21 production context.');if(upper(recordValue(product,'STATUS'))!=='COMPLETED'||product?.completionState!=='COMPLETED')reasons.push('The reserved product execution has not been completed by an accepted Stage 21 response.');if(!verified.length||verified.length!==artifacts.length)reasons.push('Every current finished-product artifact must have actual IndexedDB bytes, byte size, and application-computed SHA-256 before Stage 21 can complete.');if(hash.sha256Value(inventory)!==hash.sha256Value(verifiedIds))reasons.push('The application-generated product artifact inventory does not exactly match the verified current product bytes.');break;}
    case 22:{
      const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),expected=recordsForCurrentScope(project,'tests').filter(test=>mandatoryIds.has(testRequirementId(test))&&upper(recordValue(test,'TEST_TYPE'))==='DETERMINISTIC'&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY')));
      if(expected.some(test=>upper(recordValue(test,'EXECUTION_MODE'))!=='APPLICATION_DETERMINISTIC'))requireAccepted();
      const results=recordsForCurrentScope(project,'deterministicResults'),counts=new Map();for(const result of results){const id=String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||'');counts.set(id,(counts.get(id)||0)+1);}const expectedIds=new Set(expected.map(test=>recordId(test,'tests'))),missing=[...expectedIds].filter(id=>counts.get(id)!==1),unexpected=results.filter(result=>!expectedIds.has(String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||'')));if(missing.length)reasons.push('Exactly one deterministic result is required for each applicable deterministic test: '+missing.join(', ')+'.');if(unexpected.length)reasons.push('Unexpected deterministic results exist outside the current applicable deterministic-test set.');if(results.some(record=>effectiveDetermination('deterministicResults',record,expected.find(t=>recordId(t,'tests')===String(recordValue(record,'TEST_ID')||record.relationships?.TEST_ID||'')),project)!=='SATISFIED'))reasons.push('A deterministic finished-product verification is violated or undetermined.');for(const result of results){const test=expected.find(t=>recordId(t,'tests')===String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''));if(test&&!evaluateEvidenceSufficiency(project,{test,result}).sufficient)reasons.push('Deterministic result '+recordId(result,'deterministicResults')+' has evidence insufficient for its proposition.');}break;
    }
    case 23:{
      requireAccepted();const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),expected=recordsForCurrentScope(project,'tests').filter(test=>mandatoryIds.has(testRequirementId(test))&&upper(recordValue(test,'TEST_TYPE'))==='MEANING'&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY'))),expectedById=new Map(expected.map(test=>[recordId(test,'tests'),test])),expectedIds=new Set(expectedById.keys()),results=recordsForCurrentScope(project,'meaningResults'),counts=new Map();for(const result of results){const id=String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||'');counts.set(id,(counts.get(id)||0)+1);}const missing=[...expectedIds].filter(id=>counts.get(id)!==1),unexpected=results.filter(result=>!expectedIds.has(String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''))),mismatched=results.filter(result=>{const test=expectedById.get(String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''));return test&&String(recordValue(result,'REQ_ID')||result.relationships?.REQ_ID||'')!==testRequirementId(test);});if(missing.length)reasons.push('Exactly one meaning review is required for every current applicable meaning test: '+missing.join(', ')+'.');if(unexpected.length)reasons.push('Unexpected meaning-review records exist outside the current meaning-test set.');if(mismatched.length)reasons.push('A meaning-review requirement identity does not match its controlling Stage 6 test.');if(results.some(record=>evaluateResultConsistency('meaningResults',record,expectedById.get(String(recordValue(record,'TEST_ID')||record.relationships?.TEST_ID||'')),project).determination!=='SATISFIED'))reasons.push('A meaning verification is violated or undetermined by the application-normalized observation and entailment outcome.');for(const result of results){const test=expectedById.get(String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''));if(test&&!evaluateEvidenceSufficiency(project,{test,result}).sufficient)reasons.push('Meaning review '+recordId(result,'meaningResults')+' has evidence insufficient for the proposition.');}const meaningChange=changes.at(-1),meaningReviewer=String(meaningChange?.scope?.contextId||''),meaningProduct=recordsForCurrentScope(project,'products').at(-1),meaningProducer=String(recordValue(meaningProduct,'PRODUCTION_CONTEXT_ID')||meaningProduct?.relationships?.PRODUCTION_CONTEXT_ID||'');const meaningIndependence=evaluateContextIndependence(project,{role:'MEANING_REVIEW',reviewerContextId:meaningReviewer,productionContextId:meaningProducer});if(!['APPLICATION_ESTABLISHED','EXTERNALLY_SUPPORTED'].includes(meaningIndependence.determination))reasons.push('Meaning reviewer independence is not established: '+meaningIndependence.reasons.join(' '));break;
    }
    case 24:{
      requireAccepted();const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),expectedTests=recordsForCurrentScope(project,'tests').filter(test=>mandatoryIds.has(testRequirementId(test))&&upper(recordValue(test,'TEST_TYPE'))==='ADVERSARIAL'&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY'))),expectedTestIds=new Set(expectedTests.map(test=>recordId(test,'tests'))),expectedRegs=records(project,'regressions').filter(reg=>upper(recordValue(reg,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'&&upper(recordValue(reg,'APPLICABILITY')||'APPLICABLE')!=='NOT APPLICABLE'),expectedRegIds=new Set(expectedRegs.map(reg=>recordId(reg,'regressions'))),results=recordsForCurrentScope(project,'adversarialResults'),testCounts=new Map(),regCounts=new Map();for(const result of results){const testId=String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''),regId=String(recordValue(result,'REG_ID')||result.relationships?.REG_ID||'');if(testId)testCounts.set(testId,(testCounts.get(testId)||0)+1);if(regId)regCounts.set(regId,(regCounts.get(regId)||0)+1);}const missingTests=[...expectedTestIds].filter(id=>testCounts.get(id)!==1),missingRegs=[...expectedRegIds].filter(id=>regCounts.get(id)!==1),orphan=results.filter(result=>!String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||'')&&!String(recordValue(result,'REG_ID')||result.relationships?.REG_ID||'')),unexpected=results.filter(result=>{const testId=String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''),regId=String(recordValue(result,'REG_ID')||result.relationships?.REG_ID||'');return (testId&&!expectedTestIds.has(testId))||(regId&&!expectedRegIds.has(regId));});if(!expectedTestIds.size)reasons.push('At least one current mandatory adversarial verification test must be defined at Stage 6.');if(missingTests.length)reasons.push('Exactly one adversarial result is required for every current applicable adversarial test: '+missingTests.join(', ')+'.');if(missingRegs.length)reasons.push('Exactly one adversarial regression challenge is required for every active applicable permanent regression: '+missingRegs.join(', ')+'.');if(orphan.length)reasons.push('Every adversarial result must identify the Stage 6 adversarial test or permanent regression it challenges.');if(unexpected.length)reasons.push('Unexpected adversarial results exist outside the current required adversarial/regression set.');if(results.some(record=>evaluateResultConsistency('adversarialResults',record,expectedTests.find(test=>recordId(test,'tests')===String(recordValue(record,'TEST_ID')||record.relationships?.TEST_ID||'')),project).determination!=='SATISFIED'))reasons.push('Adversarial verification found a violated, failed, or undetermined application-normalized observation and entailment outcome.');for(const result of results){const testId=String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''),test=expectedTests.find(t=>recordId(t,'tests')===testId);if(!evaluateEvidenceSufficiency(project,{test,result}).sufficient)reasons.push('Adversarial result '+recordId(result,'adversarialResults')+' has insufficient evidence.');}const adversarialChange=changes.at(-1),adversarialReviewer=String(adversarialChange?.scope?.contextId||''),adversarialProduct=recordsForCurrentScope(project,'products').at(-1),adversarialProducer=String(recordValue(adversarialProduct,'PRODUCTION_CONTEXT_ID')||adversarialProduct?.relationships?.PRODUCTION_CONTEXT_ID||'');const adversarialIndependence=evaluateContextIndependence(project,{role:'ADVERSARIAL_REVIEW',reviewerContextId:adversarialReviewer,productionContextId:adversarialProducer});if(!['APPLICATION_ESTABLISHED','EXTERNALLY_SUPPORTED'].includes(adversarialIndependence.determination))reasons.push('Adversarial reviewer independence is not established: '+adversarialIndependence.reasons.join(' '));break;
    }
    case 25:{requireAccepted();const productId=currentScope(project).productId,artifacts=recordsForCurrentScope(project,'artifacts').filter(a=>productId&&String(a.scope?.productId||'')===String(productId)),inspections=recordsForCurrentScope(project,'representationInspections'),counts=new Map();for(const inspection of inspections){const id=String(recordValue(inspection,'ARTIFACT_ID')||inspection.relationships?.ARTIFACT_ID||'');counts.set(id,(counts.get(id)||0)+1);}const expectedIds=new Set(artifacts.map(a=>recordId(a,'artifacts'))),missing=[...expectedIds].filter(id=>counts.get(id)!==1),unexpected=inspections.filter(i=>!expectedIds.has(String(recordValue(i,'ARTIFACT_ID')||i.relationships?.ARTIFACT_ID||'')));if(!expectedIds.size)reasons.push('No current product or delivery artifacts exist for representation inspection.');if(missing.length)reasons.push('Exactly one current representation inspection is required for every current product artifact: '+missing.join(', ')+'.');if(unexpected.length)reasons.push('Unexpected representation inspections exist outside the current product artifact set.');if(inspections.some(record=>evaluateResultConsistency('representationInspections',record,null,project).determination!=='SATISFIED'))reasons.push('A final representation inspection is violated or undetermined by the application-normalized artifact, coverage, defect, and evidence checks.');for(const inspection of inspections)if(!evaluateEvidenceSufficiency(project,{result:inspection}).sufficient)reasons.push('Representation inspection '+recordId(inspection,'representationInspections')+' has insufficient evidence.');break;
    }
    case 26:
      requireAccepted();requireCount('processAudits',1);requireCount('productAudits',1);
      if(collection('processAudits').some(record=>upper(recordValue(record,'PROCESS_DETERMINATION'))!=='SATISFIED'))reasons.push('Process audit is not SATISFIED.');
      if(collection('productAudits').some(record=>upper(recordValue(record,'PRODUCT_DETERMINATION'))!=='SATISFIED'))reasons.push('Product audit is not SATISFIED.');for(const audit of [...collection('processAudits'),...collection('productAudits')])if(!evaluateEvidenceSufficiency(project,{result:audit}).sufficient)reasons.push('Stage 26 audit '+String(audit.id||audit.recordId||'UNKNOWN')+' has insufficient evidence.');const stage26Contradictions=detectCurrentContradictions(project);if(stage26Contradictions.length)reasons.push('Current process/product evidence contains unresolved contradictions: '+stage26Contradictions.map(x=>x.type+':'+x.key).join(', ')+'.');
      break;
    case 27:{requireAccepted();const releases=recordsForCurrentScope(project,'releaseRecords'),r=releases.at(-1),metrics=releaseMetrics(project);if(releases.length!==1)reasons.push('Exactly one current application-derived product-release-eligibility determination is required.');if(!r)reasons.push('The application has not recorded a current release determination.');else{const determination=upper(recordValue(r,'DETERMINATION'));if(!['ACCEPTED','REJECTED','BLOCKED'].includes(determination))reasons.push('Current release determination is invalid.');if(determination!==metrics.determination)reasons.push('Stored release eligibility is stale relative to the current proof obligations and evidence.');if(truth(recordValue(r,'PRODUCT_RELEASE_ELIGIBILITY'))!==(determination==='ACCEPTED'))reasons.push('PRODUCT_RELEASE_ELIGIBILITY does not match the controlling determination.');if(project.release?.deliveryAuthorizationEffective||upper(project.release?.authorization)==='AUTHORIZED')reasons.push('Stage 27 product eligibility is incorrectly represented as final delivery authorization.');}break;}
    case 28:{
      const release=all('releaseRecords').at(-1);
      if(upper(recordValue(release,'DETERMINATION'))!=='ACCEPTED')reasons.push('Stage 27 must be ACCEPTED before artifact identity verification.');
      const identities=recordsForCurrentScope(project,'artifactIdentities');if(!identities.length)reasons.push('No audited-versus-delivery artifact identity comparison exists.');
      if(identities.some(record=>!truth(recordValue(record,'EXACT_HASH_MATCH'))||!truth(recordValue(record,'EXACT_SIZE_MATCH'))||!truth(recordValue(record,'AUTHORIZED_FILENAME_MATCH'))||!truth(recordValue(record,'DELIVERY_ARTIFACT_IDENTITY_VERIFIED'))))reasons.push('At least one delivery artifact does not exactly match the audited canonical artifact identity.');if(!project.release?.humanDeliveryIntent)reasons.push('Current human delivery-selection intent is missing.');if(project.release?.deliveryAuthorizationEffective||upper(project.release?.authorization)==='AUTHORIZED')reasons.push('Stage 28 identity verification cannot make final delivery authorization effective.');
      break;
    }
    case 29:{
      const obligations=deriveProofObligationRegistry(project),chains=recordsForCurrentScope(project,'evidenceChains'),byObligation=new Map(chains.map(record=>[String(recordValue(record,'PROOF_OBLIGATION_ID')||record.relationships?.PROOF_OBLIGATION_ID||''),record])),semantic=recordsForCurrentScope(project,'propositions').length>0;if(semantic){const incomplete=obligations.obligations.filter(obligation=>upper(recordValue(byObligation.get(recordId(obligation,'proofObligations')),'STATUS'))!=='COMPLETE').map(obligation=>recordId(obligation,'proofObligations'));if(!obligations.complete)reasons.push(...obligations.reasons);if(incomplete.length)reasons.push(`Complete current evidence chains are missing for proof obligations: ${incomplete.join(', ')}.`);}else{const reqs=mandatoryRequirements(project),byReq=new Map(chains.map(record=>[String(recordValue(record,'REQ_ID')||record.relationships?.REQ_ID||''),record])),incomplete=reqs.filter(req=>upper(recordValue(byReq.get(requirementId(req)),'STATUS'))!=='COMPLETE').map(requirementId);if(incomplete.length)reasons.push(`Complete evidence chains are missing for: ${incomplete.join(', ')}.`);}if(detectEvidenceJustificationCycles(project).length)reasons.push('Current evidence-chain justification contains a cycle.');if(project.release?.deliveryAuthorizationEffective||upper(project.release?.authorization)==='AUTHORIZED')reasons.push('Stage 29 evidence-chain closure cannot make final delivery authorization effective.');
      break;
    }
    case 30:{requireAccepted();const integrity=evaluateRegistryIntegrity(project),delivery=terminalDeliveryState(project);if(!integrity.complete)reasons.push(...integrity.reasons);if(!delivery.authorized)reasons.push(...(delivery.reasons.length?delivery.reasons:['The terminal application-owned DELIVERY_RECORD has not been created.']));break;}
  }
  const blocked=questions.length>0||blockers.length>0||executionFailures.length>0;
  return {stage,complete:reasons.length===0,blocked,reasons,acceptedResponseCount:changes.length,checkedAt:now(),priorReasonCount:previousReasons};
}


const INTAKE_ACCOUNTING_DISPOSITIONS=Object.freeze(['EXTRACTED_RELEVANT_INFORMATION','RETAINED_AS_CONTEXT','NO_PROJECT_RELEVANT_INFORMATION','UNRESOLVED_HUMAN_AUTHORITY','LATER_RESOLVABLE','INACCESSIBLE_OR_BLOCKED']);
const INTAKE_STATEMENT_CLASSES=Object.freeze(['FACT','REQUIREMENT','CONSTRAINT','DECISION','PROHIBITION','REQUESTED_OUTPUT','ACCEPTANCE_CONDITION','MATERIAL_REFERENCE','UNRESOLVED_HUMAN_ONLY','CONTEXT']);
const OBLIGATION_DISPOSITIONS=Object.freeze(['retained nonnormative context','inapplicable','blocked']);
function enumerateInputLeaves(value,sourceLocation,kind,label,units,{inputVersion,extra={}}={}){
  if(value===undefined||value===null)return;
  if(typeof value==='string'){
    const normalized=value.replace(/\r\n/g,'\n').replace(/\r/g,'\n'),lines=normalized.split('\n');let emitted=0;
    lines.forEach((rawLine,index)=>{if(!rawLine.trim())return;const location=lines.length>1?`${sourceLocation}#line=${index+1}`:sourceLocation,rawValueSha256=hash.sha256Value(rawLine),unitId=`INPUT-UNIT-${hash.sha256Value({inputVersion,kind,sourceLocation:location,rawValueSha256}).slice(0,20).toUpperCase()}`;units.push({unitId,kind,sourceLocation:location,label:lines.length>1?`${label} line ${index+1}`:label,rawValueSha256,rawValueText:rawLine,...clone(extra)});emitted++;});
    if(!emitted&&value.length){const rawValueSha256=hash.sha256Value(value),unitId=`INPUT-UNIT-${hash.sha256Value({inputVersion,kind,sourceLocation,rawValueSha256}).slice(0,20).toUpperCase()}`;units.push({unitId,kind,sourceLocation,label,rawValueSha256,rawValueText:value,...clone(extra)});}
    return;
  }
  if(Array.isArray(value)){value.forEach((item,index)=>enumerateInputLeaves(item,`${sourceLocation}[${index}]`,kind,`${label}[${index}]`,units,{inputVersion,extra}));return;}
  if(typeof value==='object'){for(const key of Object.keys(value).sort())enumerateInputLeaves(value[key],`${sourceLocation}.${key}`,kind,`${label}.${key}`,units,{inputVersion,extra});return;}
  enumerateInputLeaves(String(value),sourceLocation,kind,label,units,{inputVersion,extra});
}
function intakeCoverageManifest(project){
  ensureShape(project);const inputVersion=project.job.CURRENT_INPUT_VERSION||'UNKNOWN',units=[];
  for(const [name,definition] of Object.entries(schema.JOB_FIELDS||{})){if(!['HUMAN','HUMAN_DECISION'].includes(definition?.producer))continue;enumerateInputLeaves(project.job[name],`job.${name}`,definition.producer==='HUMAN'?'JOB_FIELD':'HUMAN_DECISION',name,units,{inputVersion});}
  const mirroredUserEnteredKeys=new Set([
    'objective','suppliedMaterials','requiredOutputFormat','deadlineOrTemporalScope','desiredSourceCount','knownAuthorities','availableTools','prohibitedActions','explicitRequirements','suppliedArtifactFiles','suppliedArtifactText',
    ...Object.entries(schema.JOB_FIELDS||{}).filter(([,definition])=>['HUMAN','HUMAN_DECISION'].includes(definition?.producer)).map(([name])=>name)
  ]);
  const nonMirroredUserEntered=Object.fromEntries(Object.entries(project.projectData.userEntered||{}).filter(([key])=>!mirroredUserEnteredKeys.has(key)));
  if(Object.keys(nonMirroredUserEntered).length)enumerateInputLeaves(nonMirroredUserEntered,'projectData.userEntered','ORIGINAL_USER_INPUT','Original user input',units,{inputVersion});
  for(const answer of safe(project.projectData.humanInputAnswers).filter(item=>!item.invalidatedBy)){const answerId=String(answer.answerId||answer.requestId||'UNKNOWN');enumerateInputLeaves(answer.answer,`projectData.humanInputAnswers.${answerId}.answer`,'HUMAN_ANSWER',`Human answer ${answerId}`,units,{inputVersion,extra:{answerId}});}
  const stageOneSelected=new Set(safe(project.stages?.[1]?.authorizedFiles).map(item=>String(item?.artifactId||item?.id||'')).filter(Boolean));
  const stageOneArtifacts=recordsForCurrentScope(project,'artifacts').filter(record=>Number(record?.stage||recordValue(record,'STAGE')||record?.lineage?.stage||0)===1||stageOneSelected.has(recordId(record,'artifacts')));
  for(const artifact of stageOneArtifacts){const artifactId=recordId(artifact,'artifacts'),filename=String(recordValue(artifact,'FILENAME')||artifactId),sha256=String(recordValue(artifact,'SHA256')||''),byteSize=Number(recordValue(artifact,'BYTE_SIZE')||0),availability=String(recordValue(artifact,'AVAILABILITY')||'UNKNOWN'),sourceLocation=`artifact.${artifactId}`,rawValueText=JSON.stringify({artifactId,filename,sha256,byteSize,availability}),rawValueSha256=hash.sha256Value(rawValueText),unitId=`INPUT-UNIT-${hash.sha256Value({inputVersion,kind:'SUPPLIED_MATERIAL',sourceLocation,rawValueSha256}).slice(0,20).toUpperCase()}`;units.push({unitId,kind:'SUPPLIED_MATERIAL',sourceLocation,label:`Supplied material ${filename}`,rawValueSha256,rawValueText,artifactId,filename,artifactSha256:sha256,byteSize,availability});}
  const seen=new Set(),deduped=[];for(const unit of units){const key=`${unit.sourceLocation}|${unit.rawValueSha256}`;if(seen.has(key))continue;seen.add(key);deduped.push(unit);}const base={schema:'closed-loop-intake-manifest/1',inputVersion,units:deduped,unitCount:deduped.length};return {...base,manifestSha256:hash.sha256Value(base)};
}
function parseCapturedInputSet(stageOrProject){const stageOne=stageOrProject?.stages?.[1]||stageOrProject||{},raw=stageOne?.agentData?.INPUT_SET_CONTENTS??stageOne?.acceptedData?.INPUT_SET_CONTENTS??'';if(!String(raw||'').trim())return {format:'EMPTY',units:[],raw:''};try{const parsed=JSON.parse(String(raw));if(parsed&&typeof parsed==='object'&&Array.isArray(parsed.units))return {format:'STRUCTURED',...parsed,raw:String(raw)};}catch{}return {format:'LEGACY_TEXT',units:[],raw:String(raw)};}
function evaluateIntakeAccounting(project,{capture}={}){
  ensureShape(project);const manifest=intakeCoverageManifest(project),parsed=capture===undefined?parseCapturedInputSet(project):(()=>{try{const value=typeof capture==='string'?JSON.parse(capture):capture;return value&&typeof value==='object'&&Array.isArray(value.units)?{format:'STRUCTURED',...value}:{format:'INVALID',units:[]};}catch{return {format:'INVALID',units:[]};}})(),reasons=[];
  if(parsed.format!=='STRUCTURED')reasons.push('Stage 01 INPUT_SET_CONTENTS must be the structured current intake accounting capture.');
  if(String(parsed.inputVersion||'')!==String(manifest.inputVersion))reasons.push('Stage 01 intake accounting is not bound to the current input version.');
  if(String(parsed.manifestSha256||'')!==String(manifest.manifestSha256))reasons.push('Stage 01 intake accounting is not bound to the current application intake manifest.');
  const expected=new Map(manifest.units.map(unit=>[unit.unitId,unit])),counts=new Map(),accounted=new Set();
  for(const [index,unit] of safe(parsed.units).entries()){
    const id=String(unit?.sourceUnitId||'').trim();if(!id){reasons.push(`Stage 01 intake unit ${index+1} has no sourceUnitId.`);continue;}counts.set(id,(counts.get(id)||0)+1);const source=expected.get(id);if(!source){reasons.push(`Stage 01 intake accounting contains unknown unit ${id}.`);continue;}
    const hashMatches=String(unit?.sourceRawValueSha256||'')===String(source.rawValueSha256);if(!hashMatches)reasons.push(`Stage 01 intake unit ${id} is not bound to the exact source raw-value hash.`);
    const disposition=upper(unit?.disposition);if(!INTAKE_ACCOUNTING_DISPOSITIONS.includes(disposition))reasons.push(`Stage 01 intake unit ${id} has an invalid disposition.`);const permitsNoStatement=['NO_PROJECT_RELEVANT_INFORMATION','INACCESSIBLE_OR_BLOCKED'].includes(disposition);if(permitsNoStatement&&!String(unit?.reason||'').trim())reasons.push(`Stage 01 intake unit ${id} requires a reason for ${disposition}.`);
    const statements=safe(unit?.extractedStatements);if(!permitsNoStatement&&!statements.length)reasons.push(`Stage 01 intake unit ${id} contains no extracted human-authority statement.`);const statementKeys=new Set();let statementsValid=true;
    for(const statement of statements){const key=String(statement?.statementKey||'').trim(),text=String(statement?.text||'').trim(),classification=String(statement?.statementClass||'').trim().toUpperCase();if(!key||statementKeys.has(key)){reasons.push(`Stage 01 intake unit ${id} has a missing or duplicate statementKey.`);statementsValid=false;}statementKeys.add(key);if(!text){reasons.push(`Stage 01 intake unit ${id} contains an empty extracted statement.`);statementsValid=false;}if(!INTAKE_STATEMENT_CLASSES.includes(classification)){reasons.push(`Stage 01 intake unit ${id} has an invalid statementClass.`);statementsValid=false;}}
    if(hashMatches&&INTAKE_ACCOUNTING_DISPOSITIONS.includes(disposition)&&statementsValid&&(permitsNoStatement?Boolean(String(unit?.reason||'').trim()):statements.length>0))accounted.add(id);
  }
  for(const [id,count] of counts)if(count!==1)reasons.push(`Stage 01 intake unit ${id} is accounted ${count} times; exactly one accounting entry is required.`);for(const id of expected.keys())if(!counts.has(id))reasons.push(`Stage 01 intake accounting omitted controlled input unit ${id}.`);const coverage=manifest.unitCount?accounted.size/manifest.unitCount:1;return {complete:reasons.length===0,coverage,reasons,manifest,parsed,expectedUnitCount:manifest.unitCount,accountedUnitCount:accounted.size};
}
function semanticLeaves(value,emit,path='value'){if(value===undefined||value===null)return;if(typeof value==='string'){const lines=value.replace(/\r\n/g,'\n').replace(/\r/g,'\n').split('\n');for(const [index,line] of lines.entries())if(line.trim())emit(line,lines.length>1?`${path}#line=${index+1}`:path);return;}if(Array.isArray(value)){value.forEach((item,index)=>semanticLeaves(item,emit,`${path}[${index}]`));return;}if(typeof value==='object'){for(const key of Object.keys(value).sort())semanticLeaves(value[key],emit,`${path}.${key}`);return;}emit(String(value),path);}
function obligationManifest(project){
  ensureShape(project);const inputVersion=project.job.CURRENT_INPUT_VERSION||'UNKNOWN',sourceSetVersion=project.job.CURRENT_SOURCE_SET_VERSION||null,items=[];const add=(text,origin,provenance,sourceIdentity=null,evidenceRefs=[],classification='UNCLASSIFIED')=>{const value=String(text??'');if(!value.trim())return;const obligationId=`OBLIGATION-${hash.sha256Value({origin,provenance,text:value}).slice(0,20).toUpperCase()}`;items.push({obligationId,text:value,origin,classification,provenance:clone(provenance),sourceIdentity:sourceIdentity||null,evidenceRefs:[...new Set(safe(evidenceRefs).map(String))]});};
  const directHumanIntake=intakeCoverageManifest(project);for(const unit of safe(directHumanIntake.units)){if(String(unit?.kind||'').toUpperCase()==='SUPPLIED_MATERIAL')continue;add(unit?.rawValueText,'CURRENT_USER_JOB_INPUT',{sourceUnitId:String(unit?.unitId||'UNKNOWN'),sourceLocation:String(unit?.sourceLocation||'UNKNOWN'),rawValueSha256:String(unit?.rawValueSha256||''),inputVersion},null,[],'HUMAN_AUTHORITY_RAW');}
  const capture=parseCapturedInputSet(project);for(const unit of safe(capture.units))for(const [index,statement] of safe(unit?.extractedStatements).entries())add(statement?.text,'STAGE01_CAPTURED_HUMAN_AUTHORITY',{sourceUnitId:String(unit?.sourceUnitId||'UNKNOWN'),statementKey:String(statement?.statementKey||index+1),inputVersion},null,[],String(statement?.statementClass||'UNCLASSIFIED').toUpperCase());
  const stageOne=project.stages?.[1]?.agentData||project.stages?.[1]?.acceptedData||{};for(const field of ['EXACT_DELIVERABLE_REQUESTED','ASSUMPTIONS','UNKNOWN_INFORMATION'])semanticLeaves(stageOne[field],(text,location)=>add(text,'STAGE01_ACCEPTED_JOB_DEFINITION',{field,location,inputVersion}),null,[],'STAGE01_JOB_DEFINITION');
  const stageThree=project.stages?.[3]?.agentData||project.stages?.[3]?.acceptedData||{};for(const field of ['EXCEPTIONS_AND_EDGE_CONDITIONS','CONFLICTING_OR_INVALIDATING_MATERIAL','RESEARCH_GAPS_AND_BLOCKERS'])semanticLeaves(stageThree[field],(text,location)=>add(text,'STAGE03_ACCEPTED_RESEARCH_SUMMARY',{field,location,sourceSetVersion}),null,[],'STAGE03_RESEARCH_SUMMARY');
  const researchFields=['MANDATORY_STATEMENTS','RECOMMENDATIONS','OPTIONAL_PRACTICES','EXAMPLES','EXPLANATORY_MATERIAL','PROHIBITIONS','EXCEPTIONS','DEPENDENCIES','APPLICABILITY_FACTS','RESTRICTIONS','INVALIDATING_MATERIAL'];for(const record of recordsForCurrentScope(project,'research')){const researchId=recordId(record,'research'),sourceId=String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||'')||null;for(const field of researchFields)semanticLeaves(recordValue(record,field),(text,location)=>add(text,'STAGE03_SOURCE_RESEARCH',{researchId,field,location,sourceId,sourceSetVersion},sourceId,record.evidenceRefs,field));}
  for(const record of recordsForCurrentScope(project,'candidateRequirements')){const candidateRequirementId=recordId(record,'candidateRequirements'),sourceId=String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||'')||null;semanticLeaves(recordValue(record,'CANDIDATE_OBLIGATION'),(text,location)=>add(text,'STAGE03_CANDIDATE_REQUIREMENT',{candidateRequirementId,location,sourceId,sourceSetVersion},sourceId,record.evidenceRefs,'CANDIDATE_OBLIGATION'));}
  const unique=[],seen=new Set();for(const item of items){if(seen.has(item.obligationId))continue;seen.add(item.obligationId);unique.push(item);}const sourceContext=recordsForCurrentScope(project,'sources').map(record=>({sourceId:recordId(record,'sources'),contentSha256:record.contentSha256||record.sha256||hash.sha256Value(recordFields(record)),fields:recordFields(record)})),evidenceContext=recordsForCurrentScope(project,'evidenceRecords').filter(record=>[2,3].includes(Number(record.stage))).map(record=>({evidenceId:recordId(record,'evidenceRecords'),sourceId:String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||''),sha256:record.sha256||recordValue(record,'SHA256')||hash.sha256Value(recordFields(record))}));const base={schema:'closed-loop-obligation-manifest/1',inputVersion,sourceSetVersion,items:unique,obligationCount:unique.length,sourceContext,evidenceContext};return {...base,manifestSha256:hash.sha256Value(base)};
}
function obligationIdsFromRelationship(value){return [...new Set(String(value??'').match(/OBLIGATION-[A-F0-9]{20}/g)||[])];}
function evaluateObligationAccounting(project,{envelope}={}){
  ensureShape(project);const manifest=obligationManifest(project),expected=new Map(manifest.items.map(item=>[item.obligationId,item])),mapped=new Map(),dispositions=new Map(),reasons=[];const requirements=envelope?safe(envelope.records?.requirements):recordsForCurrentScope(project,'requirements'),evidence=envelope?safe(envelope.evidence):recordsForCurrentScope(project,'evidenceRecords').filter(record=>Number(record.stage)===4);
  for(const requirement of requirements){const fields=envelope?(requirement?.fields||{}):recordFields(requirement),refs=obligationIdsFromRelationship(fields.USER_INPUT_RELATIONSHIP);for(const id of refs){if(!expected.has(id))reasons.push(`Stage 04 requirement accounting references unknown obligation ${id}.`);else{const list=mapped.get(id)||[];list.push(envelope?(requirement.tempKey||requirement.targetId||'response-requirement'):recordId(requirement,'requirements'));mapped.set(id,list);}}}
  for(const item of evidence){const kind=String(envelope?item?.kind:recordValue(item,'KIND')||'').trim().toUpperCase();if(kind!=='OBLIGATION_DISPOSITION')continue;const raw=String(envelope?item?.content:recordValue(item,'CONTENT')||'');let parsed;try{parsed=JSON.parse(raw);}catch{reasons.push('Stage 04 OBLIGATION_DISPOSITION evidence contains invalid JSON.');continue;}const id=String(parsed?.obligationId||'').trim(),disposition=String(parsed?.disposition||'').trim().toLowerCase(),reason=String(parsed?.reason||'').trim();if(!expected.has(id)){reasons.push(`Stage 04 disposition references unknown obligation ${id||'MISSING'}.`);continue;}if(!OBLIGATION_DISPOSITIONS.includes(disposition))reasons.push(`Stage 04 obligation ${id} has an invalid non-requirement disposition.`);if(!reason)reasons.push(`Stage 04 obligation ${id} non-requirement disposition requires a reason.`);const list=dispositions.get(id)||[];list.push({disposition,reason});dispositions.set(id,list);}
  let accounted=0;for(const id of expected.keys()){const mappings=mapped.get(id)||[],choices=dispositions.get(id)||[];if(!mappings.length&&!choices.length)reasons.push(`Stage 04 obligation accounting omitted ${id}.`);if(mappings.length&&choices.length)reasons.push(`Stage 04 obligation ${id} is both mapped to requirement(s) and given a non-requirement disposition.`);if(choices.length>1)reasons.push(`Stage 04 obligation ${id} has duplicate non-requirement dispositions.`);const validChoice=choices.length===1&&OBLIGATION_DISPOSITIONS.includes(choices[0].disposition)&&Boolean(choices[0].reason);if((mappings.length>0&&!choices.length)||(!mappings.length&&validChoice))accounted++;}const coverage=manifest.obligationCount?accounted/manifest.obligationCount:1;return {complete:reasons.length===0,coverage,reasons,manifest,expectedObligationCount:manifest.obligationCount,accountedObligationCount:accounted,mapped,dispositions};
}

function representationInspectionCoverage(project){
  ensureShape(project);
  const scope=currentScope(project),productId=String(scope.productId||''),artifacts=recordsForCurrentScope(project,'artifacts').filter(a=>productId&&String(a.scope?.productId||'')===productId),inspections=recordsForCurrentScope(project,'representationInspections'),reasons=[],details=[];
  let totalPagesOrViewsRequired=0,totalPagesOrViewsInspected=0,totalPackagedFilesRequired=0,totalPackagedFilesOpenedOrTested=0,totalTransformationsRequired=0,totalTransformationsInspected=0;
  for(const artifact of artifacts){
    const artifactId=recordId(artifact,'artifacts'),rows=inspections.filter(i=>String(recordValue(i,'ARTIFACT_ID')||i.relationships?.ARTIFACT_ID||'')===artifactId);
    if(rows.length!==1){reasons.push(`Exactly one current aggregate representation inspection is required for ${artifactId}; found ${rows.length}.`);continue;}
    const inspection=rows[0],coverage=parseRepresentationInspectionObservation(inspection);if(!coverage.valid){for(const reason of coverage.reasons)reasons.push(`${recordId(inspection,'representationInspections')}: ${reason}`);if(!coverage.payload)continue;}
    const {requiredPageOrViewIds,inspectedPageOrViewIds,requiredPackagedFileIds,openedOrTestedPackagedFileIds,requiredTransformationIds,inspectedTransformationIds,observation}=coverage;
    totalPagesOrViewsRequired+=requiredPageOrViewIds.length;totalPagesOrViewsInspected+=inspectedPageOrViewIds.length;totalPackagedFilesRequired+=requiredPackagedFileIds.length;totalPackagedFilesOpenedOrTested+=openedOrTestedPackagedFileIds.length;totalTransformationsRequired+=requiredTransformationIds.length;totalTransformationsInspected+=inspectedTransformationIds.length;
    details.push({artifactId,requiredPageOrViewIds,inspectedPageOrViewIds,requiredPackagedFileIds,openedOrTestedPackagedFileIds,requiredTransformationIds,inspectedTransformationIds,observation});
  }
  const inspectionArtifactIds=new Set(inspections.map(i=>String(recordValue(i,'ARTIFACT_ID')||i.relationships?.ARTIFACT_ID||''))),expectedArtifactIds=new Set(artifacts.map(a=>recordId(a,'artifacts')));
  for(const id of inspectionArtifactIds)if(id&&!expectedArtifactIds.has(id))reasons.push(`Representation inspection references non-current product artifact ${id}.`);
  return {complete:artifacts.length>0&&reasons.length===0,reasons,productId,totalDeliveryArtifacts:artifacts.length,totalPagesOrViewsRequired,totalPagesOrViewsInspected,totalPackagedFilesRequired,totalPackagedFilesOpenedOrTested,totalTransformationsRequired,totalTransformationsInspected,details};
}

const applicationadjudicationGate=gate;
gate=function adjudicationGate(stage,project){
  const adjudicated=adjudicatedClone(project),base=applicationadjudicationGate(stage,adjudicated),reasons=[...base.reasons];
  const add=x=>{for(const r of safe(x))if(r&&!reasons.includes(r))reasons.push(r);};
  if(stage===1){const accounting=evaluateIntakeAccounting(project);if(!accounting.complete)add(accounting.reasons);}
  if(stage===4){const accounting=evaluateObligationAccounting(project);if(!accounting.complete)add(accounting.reasons);}
  if(stage===7){for(const mutation of recordsForCurrentScope(project,'failureTests')){const id=recordId(mutation,'failureTests'),availability=upper(recordValue(mutation,'FAILURE_TEST_AVAILABILITY_CLASS')||'EXECUTABLE_NOW');if(availability==='DEFERRED_TARGET_DEPENDENT'){const contract=deferredFailureTestContract(project,mutation);if(!contract.valid)add(contract.issues.map(reason=>id+': '+reason));continue;}if(availability==='NOT_APPLICABLE')continue;if(availability==='BLOCKED'){add([id+': failure test execution is blocked.']);continue;}const explicitOutcome=upper(recordValue(mutation,'EXECUTION_OUTCOME')),legacyActual=upper(recordValue(mutation,'ACTUAL_RESULT')),expected=upper(recordValue(mutation,'EXPECTED_REJECTION')),legacyOutcome=!explicitOutcome&&expected.includes('REJECT')&&['REJECTED','REJECT','DENIED','BLOCKED'].includes(legacyActual)?'REJECTED_INVALID':!explicitOutcome&&expected.includes('REJECT')&&['ACCEPTED','PASS','PASSED','SUCCESS','SUCCEEDED'].includes(legacyActual)?'ACCEPTED_INVALID':'',outcome=explicitOutcome||legacyOutcome;if(!['REJECTED_INVALID','ACCEPTED_INVALID','UNDETERMINED','NOT_RUN'].includes(outcome))add([id+': controlled EXECUTION_OUTCOME is required (REJECTED_INVALID/ACCEPTED_INVALID/UNDETERMINED/NOT_RUN); only the finite legacy rejection/acceptance vocabulary is migrated.']);if(outcome==='ACCEPTED_INVALID'&&!String(recordValue(mutation,'VALIDATOR_DEFECT_ID')||mutation.relationships?.VALIDATOR_DEFECT_ID||'').trim())add(['A known-invalid fixture was accepted without a linked validator defect.']);if(['UNDETERMINED','NOT_RUN'].includes(outcome))add([id+': failure test is not conclusively executed.']);}}
  if(stage===7){for(const mutation of recordsForCurrentScope(project,'failureTests')){const id=recordId(mutation,'failureTests'),availability=upper(recordValue(mutation,'FAILURE_TEST_AVAILABILITY_CLASS')||'EXECUTABLE_NOW');if(availability==='DEFERRED_TARGET_DEPENDENT'||availability==='NOT_APPLICABLE')continue;const reqId=String(recordValue(mutation,'REQ_ID')||mutation.relationships?.REQ_ID||''),evidence=evaluateEvidenceContract(null,mutation,null,project),plans=testExecutionPlan(project).items.filter(item=>String(item.requirementId||'')===reqId),capable=plans.some(item=>item.operatorAction!=='BLOCKED'&&item.executionRoute!=='BLOCKED'&&item.capabilityReady!==false);if(!evidence.sufficient)add(evidence.reasons.map(reason=>id+': '+reason));if(!capable)add([id+': no capable current execution route is established for the mutated requirement.']);if(adjudicationEmpty(recordValue(mutation,'FIXTURE')))add([id+': actual failure fixture is not identifiable.']);if(adjudicationEmpty(recordValue(mutation,'EXPECTED_REJECTION')))add([id+': expected rejection is missing.']);if(adjudicationEmpty(recordValue(mutation,'ACTUAL_RESULT')))add([id+': actual execution result is missing.']);}}
  if(stage===9)for(const r of recordsForCurrentScope(project,'preflightRecords')){const e=evaluateResultConsistency('preflightRecords',r,null,project);if(e.determination!=='SATISFIED')add(e.reasons.length?e.reasons:['Preflight is not application-derived SATISFIED.']);}
  if(stage===13){const iteration=latestIteration(project,[10]),iterationId=recordId(iteration,'iterations');for(const req of mandatoryRequirements(project,scopeForIteration(project,iterationId))){const facts=comparisonFacts(project,requirementId(req),iterationId),rows=recordsForIteration(project,'comparisons',iterationId).filter(r=>String(recordValue(r,'REQ_ID')||r.relationships?.REQ_ID||'')===requirementId(req));if(rows.length!==1)add(['Exactly one comparison record is required for '+requirementId(req)+'.']);if(!facts.allSatisfied&&facts.anyViolation)add(['Derived comparison contains a violation for '+requirementId(req)+'.']);if(facts.anyUndetermined)add(['Derived comparison contains an undetermined result for '+requirementId(req)+'.']);}}
  if(stage===14)for(const defect of confirmedDefects(project)){const id=recordId(defect,'defects'),rcas=records(project,'rootCauses').filter(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===id);if(rcas.length!==1)add(['Exactly one complete RCA is required for '+id+'.']);else{const v=validateRootCauseRecord(rcas[0],project);if(!v.valid)add(v.reasons.map(x=>id+': '+x));}}
  if(stage===16){for(const defect of confirmedDefects(project)){const id=recordId(defect,'defects'),changes=records(project,'changes',{stage:16}).filter(c=>{const raw=recordValue(c,'TRIGGERING_DEFECT_IDS'),ids=Array.isArray(raw)?raw.map(String):String(raw||'').split(/[;,\s]+/).filter(Boolean);return ids.includes(id);});if(changes.length!==1)add(['Exactly one responsible-layer changeset trace is required for '+id+'.']);else{const v=validateChangeTrace(changes[0],project);if(!v.valid)add(v.reasons.map(x=>id+': '+x));}}}
  if(stage===19)for(const r of recordsForCurrentScope(project,'confirmationRecords')){const e=confirmationDetermination(project,r);if(e.determination!=='SATISFIED')add(e.reasons.length?e.reasons:['Unchanged confirmation is not application-derived SATISFIED.']);}
  if(stage===20){const ok=recordsForCurrentScope(project,'confirmationRecords').filter(r=>effectiveDetermination('confirmationRecords',r,null,project)==='SATISFIED'),backup=backupCheckpointStatus(project,'BASELINE_APPROVAL');if(ok.length!==1)add(['Exactly one application-derived successful unchanged confirmation is required.']);if(!backup.complete)add(backup.reasons);}
  if(stage===21)for(const r of recordsForCurrentScope(project,'products')){const e=evaluateResultConsistency('products',r,null,project);if(e.determination!=='SATISFIED')add(e.reasons.length?e.reasons:['Production execution is not application-derived SATISFIED.']);}
  const stageCollection={22:'deterministicResults',23:'meaningResults',24:'adversarialResults',25:'representationInspections'}[stage];if(stageCollection)for(const r of recordsForCurrentScope(project,stageCollection)){const e=evaluateResultConsistency(stageCollection,r,testForResult(project,r),project);if(e.determination!=='SATISFIED')add(e.reasons.length?e.reasons:[stageCollection+' contains a non-satisfied effective result.']);}
  if(stage===25){const coverage=representationInspectionCoverage(project);if(!coverage.complete)add(coverage.reasons.length?coverage.reasons:['Stage 25 representation coverage is incomplete.']);}
  if(stage===26){for(const r of recordsForCurrentScope(project,'processAudits')){const e=auditDetermination('processAudits',r,project);if(e.determination!=='SATISFIED')add(e.reasons);}for(const r of recordsForCurrentScope(project,'productAudits')){const e=auditDetermination('productAudits',r,project);if(e.determination!=='SATISFIED')add(e.reasons);}}
  if(stage===26){const process=recordsForCurrentScope(project,'processAudits'),product=recordsForCurrentScope(project,'productAudits'),contradictions=detectCurrentContradictions(project);if(process.length!==1)add([`Exactly one current process audit is required; found ${process.length}.`]);if(product.length!==1)add([`Exactly one current product audit is required; found ${product.length}.`]);if(openBlockers(project,26).length)add(['Stage 26 has unresolved reconciliation blockers.']);if(contradictions.length)add(['Stage 26 cannot reconcile while current contradictions remain.']);}
  if(stage===30){const baseline=recordsForCurrentScope(project,'baselines').at(-1),baselineTime=Date.parse(String(baseline?.createdAt||baseline?.deviceTimestamp||'')),active=activeRegressions(project);if(!baseline||!Number.isFinite(baselineTime))add(['Stage 30 requires the current approved baseline identity and approval time.']);for(const reg of active){const id=recordId(reg,'regressions'),allExec=records(project,'regressionExecutions').filter(e=>String(recordValue(e,'REG_ID')||e.relationships?.REG_ID||'')===id),pre=allExec.filter(e=>upper(recordValue(e,'PHASE'))==='PRE_CORRECTION'&&effectiveRegressionDetermination(project,e).determination==='SATISFIED'),post=allExec.filter(e=>upper(recordValue(e,'PHASE'))!=='PRE_CORRECTION').filter(e=>{const t=Date.parse(String(e.deviceTimestamp||e.createdAt||recordValue(e,'ENDED_AT_DEVICE_TIME')||''));return Number.isFinite(t)&&Number.isFinite(baselineTime)&&t<=baselineTime;}).sort((a,b)=>Date.parse(String(a.deviceTimestamp||a.createdAt||recordValue(a,'ENDED_AT_DEVICE_TIME')||''))-Date.parse(String(b.deviceTimestamp||b.createdAt||recordValue(b,'ENDED_AT_DEVICE_TIME')||'')));if(!pre.length)add([`Permanent regression ${id} lacks preserved pre-correction failure reproduction.`]);const latest=post.at(-1);if(!latest||effectiveRegressionDetermination(project,latest).determination!=='SATISFIED')add([`Permanent regression ${id} lacks a successful latest applicable execution before baseline approval.`]);}}
  if(stage>=27&&releaseMetrics(project).determination==='ACCEPTED'&&detectCurrentContradictions(project).length)add(['Release cannot be ACCEPTED while a adjudication contradiction exists.']);
  return {...base,complete:reasons.length===0,reasons,blocked:base.blocked||reasons.some(r=>/UNDETERMINED|BLOCKED|missing|unavailable|not established|not conclusively/i.test(r))};
};
function deriveStageData(project,stage){ensureShape(project);const derived={};const ids=collection=>recordsForCurrentScope(project,collection).filter(r=>Number(r.stage)===Number(stage)).map(r=>recordId(r,collection));const metrics=[4,6,12].includes(Number(stage))?coverageMetrics(project):null;const convergence=Number(stage)===18?convergenceMetrics(project):null;const release=Number(stage)===27?releaseMetrics(project):null;switch(stage){case 1:{const intake=evaluateIntakeAccounting(project);Object.assign(derived,{JOB_ID:project.job.JOB_ID,DATE_OPENED:project.job.DATE_OPENED,INPUT_SET_VERSION:project.job.CURRENT_INPUT_VERSION,INPUT_SET_HASH_OR_MANIFEST:project.job.INPUT_SET_HASH_OR_MANIFEST||intake.manifest?.manifestSha256||'UNKNOWN',JOB_RECORD_STATUS:intake.complete&&acceptedChanges(project,1).length?'READY':'NOT READY',STATUS_EVIDENCE:intake.complete?`Application accounted ${intake.accountedUnitCount}/${intake.expectedUnitCount} controlled input units.`:intake.reasons.join('; ')});break;}case 2:{const conflicts=recordsForCurrentScope(project,'sourceConflicts'),unresolved=conflicts.filter(r=>['UNRESOLVED','BLOCKED','UNKNOWN','OPEN'].includes(upper(recordValue(r,'RESOLUTION_STATUS'))));Object.assign(derived,{SOURCE_SET_VERSION:project.job.CURRENT_SOURCE_SET_VERSION||'NOT APPLICABLE',SOURCE_RECORDS:ids('sources'),SOURCE_CONFLICT_RECORDS:ids('sourceConflicts'),UNRESOLVED_CONTROLLING_CONFLICTS:unresolved.map(r=>recordId(r,'sourceConflicts'))});break;}case 3:{const sourceIds=recordsForCurrentScope(project,'sources').map(r=>recordId(r,'sources')),research=recordsForCurrentScope(project,'research'),researched=new Set(research.map(r=>String(recordValue(r,'SOURCE_ID')||r.relationships?.SOURCE_ID||''))),candidate=recordsForCurrentScope(project,'candidateRequirements');Object.assign(derived,{RESEARCH_VERSION:'RESEARCH-'+hash.sha256Value(research.map(r=>r.recordSha256||r.sha256||recordId(r,'research'))).slice(0,16).toUpperCase(),SOURCE_RESEARCH_RECORDS:research.map(r=>recordId(r,'research')),CANDIDATE_REQUIREMENT_RECORDS:candidate.map(r=>recordId(r,'candidateRequirements')),ALL_KNOWN_CONTROLLING_SOURCES_EXAMINED:sourceIds.length===0?upper(project.stages[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE':sourceIds.every(id=>researched.has(id))});break;}case 4:{const reqs=recordsForCurrentScope(project,'requirements'),accounting=evaluateObligationAccounting(project),atomicityIssues=reqs.filter(req=>{const obligation=String(recordValue(req,'OBLIGATION')||'').trim(),success=String(recordValue(req,'OBSERVABLE_SATISFACTION_CONDITION')||'').trim(),failure=String(recordValue(req,'FAILURE_CONDITION')||'').trim();return !obligation||!success||!failure;}).map(req=>recordId(req,'requirements'));Object.assign(derived,{REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NOT APPLICABLE',REQUIREMENT_RECORDS:reqs.map(r=>recordId(r,'requirements')),ATOMICITY_REVIEW_RESULTS:{reviewed:reqs.length,structurallyIncomplete:atomicityIssues,obligationAccountingComplete:accounting.complete,obligationAccountingCoverage:exactRatio(accounting.accountedObligationCount,accounting.expectedObligationCount||1)},TOTAL_REQUIREMENTS:reqs.length,MANDATORY_REQUIREMENTS:metrics.mandatoryRequirementCount});break;}case 5:{const resolutions=recordsForCurrentScope(project,'requirementResolutions'),unresolved=resolutions.filter(r=>['OPEN','UNRESOLVED','BLOCKED','UNKNOWN'].includes(upper(recordValue(r,'STATUS')))),blockers=openBlockers(project,5);Object.assign(derived,{INPUT_REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NONE',OUTPUT_REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NONE',REQUIREMENT_DEFECT_RECORDS:resolutions.map(r=>recordId(r,'requirementResolutions')),UNRESOLVED_CONFLICTS:unresolved.map(r=>recordId(r,'requirementResolutions')),MISSING_PREREQUISITES:blockers.filter(b=>/prerequisite/i.test(String(recordValue(b,'MISSING_ITEM_TYPE')||recordValue(b,'WHY_WORK_CANNOT_CONTINUE')||''))).map(b=>recordId(b,'blockers')),MANDATORY_BLOCKERS:blockers.map(b=>recordId(b,'blockers'))});break;}case 6:{const tests=recordsForCurrentScope(project,'tests'),mandatoryIds=new Set(mandatoryRequirements(project).map(requirementId)),mandatoryTests=tests.filter(t=>mandatoryIds.has(testRequirementId(t))),executionPlan=testExecutionPlan(project),currentArtifactBlockedTestIds=new Set(executionPlan.items.filter(item=>mandatoryIds.has(item.requirementId)&&item.artifactRequired&&item.artifactIds.length>0&&!item.artifactReady).map(item=>item.testId)),blocked=mandatoryTests.filter(t=>upper(recordValue(t,'EXECUTION_MODE'))==='UNAVAILABLE'||upper(recordValue(t,'STATUS'))==='BLOCKED'||(upper(recordValue(t,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&!applicationTestSupported(t))||currentArtifactBlockedTestIds.has(recordId(t,'tests')));Object.assign(derived,{TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NOT APPLICABLE',TEST_RECORDS:tests.map(r=>recordId(r,'tests')),COVERAGE_RECORDS:mandatoryRequirements(project).map(req=>({requirementId:requirementId(req),testIds:tests.filter(t=>testRequirementId(t)===requirementId(req)).map(t=>recordId(t,'tests'))})),TOTAL_ACTIVE_MANDATORY_REQUIREMENTS:metrics.mandatoryRequirementCount,ACTIVE_MANDATORY_REQUIREMENTS_WITH_AT_LEAST_ONE_READY_TEST:metrics.requirementsWithTests,MANDATORY_TEST_COVERAGE:exactRatio(metrics.requirementsWithTests,metrics.mandatoryRequirementCount||1),BLOCKED_MANDATORY_REQUIREMENTS:[...new Set(blocked.map(t=>testRequirementId(t)))]});break;}case 7:{const reqs=mandatoryRequirements(project),mutations=recordsForCurrentScope(project,'failureTests'),covered=new Set(mutations.map(testRequirementId)),acceptedInvalid=[],defective=[];for(const m of mutations){const actual=upper(recordValue(m,'ACTUAL_RESULT')),expected=upper(recordValue(m,'EXPECTED_REJECTION')),bad=expected.includes('REJECT')&&/\b(?:ACCEPTED|ALLOWED|PASSED|NOT REJECTED|VALID)\b/.test(actual)&&!/\b(?:REJECTED|BLOCKED|FAILED|INVALID)\b/.test(actual);if(bad){acceptedInvalid.push(recordId(m,'failureTests'));const defect=String(recordValue(m,'VALIDATOR_DEFECT_ID')||m.relationships?.VALIDATOR_DEFECT_ID||'');if(defect)defective.push(defect);}}Object.assign(derived,{MUTATION_SUITE_VERSION:'MUTATION-'+hash.sha256Value(mutations.map(r=>r.recordSha256||r.sha256||recordId(r,'failureTests'))).slice(0,16).toUpperCase(),FAILURE_TEST_RECORDS:mutations.map(r=>recordId(r,'failureTests')),ACTIVE_REQUIREMENTS:reqs.map(requirementId),REQUIREMENTS_WITH_AT_LEAST_ONE_FAILURE_TEST:reqs.filter(r=>covered.has(requirementId(r))).map(requirementId),FAILURE_TEST_COVERAGE:exactRatio(reqs.filter(r=>covered.has(requirementId(r))).length,reqs.length||1),INVALID_FIXTURES_ACCEPTED:acceptedInvalid,DEFECTIVE_VALIDATORS:[...new Set(defective)]});break;}case 8:{const traces=recordsForCurrentScope(project,'instructionTraces');Object.assign(derived,{DRAFT_INSTRUCTION_VERSION:project.job.CURRENT_INSTRUCTION_VERSION||'NONE',INSTRUCTION_TRACE_RECORDS:traces.map(r=>recordId(r,'instructionTraces'))});break;}case 9:{const reviews=recordsForCurrentScope(project,'preflightRecords'),change=acceptedChanges(project,9).at(-1),contextId=String(change?.scope?.contextId||''),ind=evaluateContextIndependence(project,{role:'PREFLIGHT_REVIEW',reviewerContextId:contextId});Object.assign(derived,{INPUT_INSTRUCTION_VERSION:project.job.CURRENT_INSTRUCTION_VERSION||'NONE',OUTPUT_INSTRUCTION_VERSION:project.job.CURRENT_INSTRUCTION_VERSION||'NONE',PREFLIGHT_REVIEWER_ID:contextId||'UNKNOWN',REVIEW_CONTEXT_INDEPENDENT_FROM_AUTHOR:ind.determination,SENTENCE_REVIEW_RECORDS:reviews.map(r=>recordId(r,'preflightRecords')),PREFLIGHT_ITERATION_RECORDS:safe(project.projectData.acceptedChanges).filter(c=>Number(c.stage)===9&&!c.invalidatedBy).map(c=>c.changeId)});break;}case 10:{const iteration=latestIteration(project,[10]),iterationId=recordId(iteration,'iterations'),candidateId=iterationCandidateId(project,iterationId),candidate=records(project,'candidateFreezes').find(r=>recordId(r,'candidateFreezes')===candidateId),manifest=safe(recordValue(candidate,'COMPONENT_MANIFEST')),hashes=recordValue(candidate,'COMPONENT_HASHES')||{};Object.assign(derived,{CANDIDATE_ID:candidateId||'NONE',ITERATION_ID:iterationId||'NONE',FREEZE_DATE:candidate?.createdAt||'NONE',FROZEN_COMPONENT_RECORDS:manifest.length,TOOL_CONFIGURATION_RECORDS:adjudicationEmpty(recordValue(candidate,'TOOL_CONFIGURATION'))?0:1,ALL_REQUIRED_COMPONENTS_PRESENT:manifest.length>0&&manifest.every(x=>x.artifactId&&x.filename&&x.sha256),ALL_RUNS_WILL_RECEIVE_IDENTICAL_FROZEN_MATERIALS:Boolean(candidateId&&candidateComponentIdentity(project,candidateId)),HASHES_RECORDED_WHERE_PRACTICAL:manifest.length>0&&Object.keys(hashes).length===manifest.length,CHANGES_ALLOWED_DURING_BATCH:'NO IN-PLACE MATERIAL CHANGES; ANY MATERIAL CHANGE REQUIRES A NEW CANDIDATE'});break;}case 11:case 19:{const it=latestIteration(project,[stage]),iterationId=recordId(it,'iterations'),ev=evaluateIteration(project,iterationId,stage===19?'UNCHANGED_CONFIRMATION':'INITIAL');Object.assign(derived,{ITERATION_ID:ev.iterationId,RUN_COUNT:ev.runs.length,FRESH_CONTEXT_COUNT:ev.contextCount,ITERATION_COMPLETE:ev.complete,ITERATION_REASONS:ev.reasons,STABILITY_SUMMARY:stage===19?ev.stability:null});if(stage===11){const runs=ev.runs,contexts=runs.map(r=>String(recordValue(r,'CONTEXT_ID')||r.relationships?.CONTEXT_ID||''));Object.assign(derived,{CANDIDATE_ID:iterationCandidateId(project,iterationId)||'NONE',RUN_RECORDS:runs.map(r=>recordId(r,'runs')),FRESH_CONTEXTS_CREATED:new Set(contexts.filter(Boolean)).size,RUNS_RECEIVING_EXACT_PACKAGE:ev.reasons.some(r=>/candidate|package/i.test(r))?0:runs.length,CONTAMINATED_RUNS:runs.filter(r=>{const c=records(project,'freshContexts').find(x=>recordId(x,'freshContexts')===String(recordValue(r,'CONTEXT_ID')||r.relationships?.CONTEXT_ID||''));return ['YES','TRUE','CONTAMINATED'].includes(upper(recordValue(c,'CONTAMINATION_STATUS')||recordValue(r,'CONTAMINATION_CHECK')));}).map(r=>recordId(r,'runs')),OUTPUTS_SAVED_SEPARATELY:runs.filter(r=>!adjudicationEmpty(recordValue(r,'COMPLETE_OUTPUT'))).length});}if(stage===19){const confirmation=recordsForCurrentScope(project,'confirmationRecords').at(-1),sourceId=String(recordValue(confirmation,'SOURCE_ITERATION_ID')||confirmation?.relationships?.SOURCE_ITERATION_ID||''),matrix=verificationMatrix(project,iterationId),regs=records(project,'regressions').filter(r=>isActiveRecord(r)&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),regExec=currentRegressionExecutions(project,iterationId),defects=recordsForIteration(project,'defects',iterationId),comparisonCount=recordsForIteration(project,'comparisons',iterationId).length,reqCount=mandatoryRequirements(project,scopeForIteration(project,iterationId)).length,determination=confirmationDetermination(project,confirmation);Object.assign(derived,{SOURCE_CONVERGED_ITERATION:sourceId||'NONE',CONFIRMATION_ITERATION_ID:iterationId||'NONE',ZERO_CHANGE_AUDIT:!ev.reasons.some(r=>/change|candidate|hash/i.test(r)),TEN_NEW_CONTEXTS_CREATED:ev.contextCount,SAME_FROZEN_PACKAGE_USED:!ev.reasons.some(r=>/package|candidate/i.test(r)),RUNS_COMPLETED:ev.runs.length,COMPLETE_TEST_SUITE_RUN:matrix.expected.length>0&&!matrix.missing.length&&!matrix.duplicates.length,ALL_REGRESSION_TESTS_RUN:regs.every(r=>regExec.some(e=>String(recordValue(e,'REG_ID')||e.relationships?.REG_ID||'')===recordId(r,'regressions'))),CROSS_RUN_COMPARISON_COMPLETED:comparisonCount===reqCount,NEW_CRITICAL_DEFECTS:defects.filter(d=>upper(recordValue(d,'SEVERITY'))==='CRITICAL').length,NEW_MAJOR_DEFECTS:defects.filter(d=>upper(recordValue(d,'SEVERITY'))==='MAJOR').length,NEW_REQUIREMENTS_DISCOVERED:0,INJECTED_DEFECTS_NOT_DETECTED:0,NEW_CORRECTNESS_AFFECTING_VARIANCE:ev.stability?.unexplainedVarianceCount||0,CONFIRMATION_ACCEPTANCE_CRITERIA_SATISFIED:determination.determination==='SATISFIED',REQUIRED_RETURN_STAGE:determination.determination==='SATISFIED'?'NONE':'STAGE 14'});}break;}case 12:{const iteration=latestIteration(project,[10]),iterationId=recordId(iteration,'iterations'),matrix=verificationMatrix(project,iterationId),rows=recordsForIteration(project,'verification',iterationId),ds=rows.map(r=>effectiveDetermination('verification',r,testForResult(project,r),project)),selfValidated=rows.filter(r=>{const runId=String(recordValue(r,'RUN_ID')||r.relationships?.RUN_ID||''),run=records(project,'runs').find(x=>recordId(x,'runs')===runId),generator=String(recordValue(run,'CONTEXT_ID')||run?.relationships?.CONTEXT_ID||''),reviewer=resultContextIdentity(project,r);return generator&&reviewer&&generator===reviewer;});Object.assign(derived,{ITERATION_ID:iterationId||'NONE',REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NONE',TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NONE',VERIFICATION_RECORDS:rows.map(r=>recordId(r,'verification')),ACTIVE_MANDATORY_REQUIREMENTS:mandatoryRequirements(project,scopeForIteration(project,iterationId)).map(requirementId),RUNS:matrix.runs.map(r=>recordId(r,'runs')),EXPECTED_MANDATORY_RECORDS:matrix.expected.length,ACTUAL_MANDATORY_RECORDS:matrix.verification.length,MISSING_RECORDS:matrix.missing.length,SATISFIED_RECORDS:ds.filter(d=>d==='SATISFIED').length,VIOLATED_RECORDS:ds.filter(d=>d==='VIOLATED').length,UNDETERMINED_RECORDS:ds.filter(d=>d==='UNDETERMINED').length,SELF_VALIDATED_RECORDS:selfValidated.length,VERIFICATION_COVERAGE:exactRatio(matrix.expected.length-matrix.missing.length,matrix.expected.length||1)});break;}case 13:{const it=latestIteration(project,[10]),iterationId=recordId(it,'iterations'),scope=iterationId?scopeForIteration(project,iterationId):currentScope(project),facts={},reqs=mandatoryRequirements(project,scope);for(const req of reqs){const f=comparisonFacts(project,requirementId(req),iterationId);facts[requirementId(req)]={RUN_DETERMINATIONS:f.runDeterminations,ALL_TEN_SATISFIED:f.allSatisfied,ANY_VIOLATION:f.anyViolation,ANY_UNDETERMINED:f.anyUndetermined,SATISFIED_COUNT:f.determinations.filter(x=>x==='SATISFIED').length,VIOLATED_COUNT:f.determinations.filter(x=>x==='VIOLATED').length,UNDETERMINED_COUNT:f.determinations.filter(x=>!['SATISFIED','VIOLATED'].includes(x)).length};}const stability=iterationId?executionStability(project,iterationId):{repeatedDefectCount:0,uniqueDefectCount:0},comparisons=iterationId?recordsForIteration(project,'comparisons',iterationId):[];Object.assign(derived,{ITERATION_ID:iterationId||'NONE',COMPARISON_VERSION:'COMPARISON-'+hash.sha256Value(facts).slice(0,16).toUpperCase(),REQUIREMENT_COMPARISON_RECORDS:comparisons.length,REQUIREMENTS_SATISFIED_BY_ALL_TEN:reqs.filter(r=>facts[requirementId(r)]?.ALL_TEN_SATISFIED).map(requirementId),REQUIREMENTS_WITH_AT_LEAST_ONE_VIOLATION:reqs.filter(r=>facts[requirementId(r)]?.ANY_VIOLATION).map(requirementId),REQUIREMENTS_WITH_AT_LEAST_ONE_UNDETERMINED:reqs.filter(r=>facts[requirementId(r)]?.ANY_UNDETERMINED).map(requirementId),CORRECTNESS_AFFECTING_DISAGREEMENTS:comparisons.filter(r=>truth(recordValue(r,'CORRECTNESS_AFFECTING_VARIANCE'))).map(r=>recordId(r,'comparisons')),PROHIBITED_OUTPUT_VARIANCES:comparisons.filter(r=>adjudicationAdverse(recordValue(r,'PROHIBITED_VARIANCE'))).map(r=>recordId(r,'comparisons')),INCONCLUSIVE_TESTS:reqs.filter(r=>facts[requirementId(r)]?.ANY_UNDETERMINED).map(requirementId),REPEATED_FAILURE_GROUPS:stability.repeatedDefectCount,UNIQUE_FAILURES:stability.uniqueDefectCount,APPLICATION_DERIVED_COMPARISON_FACTS:facts,STABILITY_SUMMARY:stability});break;}case 14:{const defects=confirmedDefects(project),rcas=recordsForCurrentScope(project,'rootCauses'),byDefect=new Map();for(const r of rcas){const id=String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'');if(!byDefect.has(id))byDefect.set(id,[]);byDefect.get(id).push(r);}const confirmed=[],unknown=[],blocked=[];for(const d of defects){const id=recordId(d,'defects'),rows=byDefect.get(id)||[];if(rows.length===1){const v=validateRootCauseRecord(rows[0],project);if(v.valid)confirmed.push(id);else unknown.push(id);}else if(rows.length===0)unknown.push(id);else blocked.push(id);}Object.assign(derived,{ROOT_CAUSE_ANALYSIS_VERSION:'RCA-'+hash.sha256Value(rcas.map(r=>r.sha256||r.recordSha256||recordId(r,'rootCauses'))).slice(0,16).toUpperCase(),DEFECT_ROOT_CAUSE_RECORDS:rcas.length,TOTAL_MATERIAL_DEFECTS:defects.length,CONFIRMED_ROOT_CAUSES:confirmed,UNDETERMINED_ROOT_CAUSES:unknown,BLOCKED_ANALYSES:blocked});break;}case 15:{const defects=confirmedDefects(project),regs=records(project,'regressions').filter(r=>isActiveRecord(r)),execs=records(project,'regressionExecutions'),covered=[],preProven=[],missing=[];for(const d of defects){const id=recordId(d,'defects'),rs=regs.filter(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===id);if(rs.length)covered.push(id);else missing.push(id);if(rs.some(r=>execs.some(e=>String(recordValue(e,'REG_ID')||e.relationships?.REG_ID||'')===recordId(r,'regressions')&&upper(recordValue(e,'PHASE'))==='PRE_CORRECTION'&&effectiveRegressionDetermination(project,e).determination==='SATISFIED')))preProven.push(id);}Object.assign(derived,{INPUT_TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NONE',OUTPUT_TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NONE',REGRESSION_FIXTURE_VERSION:'REGRESSION-'+hash.sha256Value(regs.map(r=>r.sha256||recordId(r,'regressions'))).slice(0,16).toUpperCase(),REGRESSION_RECORDS:regs.length,CONFIRMED_DEFECTS:defects.length,CONFIRMED_DEFECTS_WITH_REGRESSION_TEST:covered,PRE_CORRECTION_FAILURES_PROVEN:preProven,UNCONVERTED_CONFIRMED_DEFECTS:missing});break;}case 16:{const changes=recordsForCurrentScope(project,'changes').filter(r=>Number(r.stage)===16),defects=confirmedDefects(project),triggered=[...new Set(changes.flatMap(c=>{const raw=recordValue(c,'TRIGGERING_DEFECT_IDS');return Array.isArray(raw)?raw.map(String):String(raw||'').split(/[;,\s]+/).filter(Boolean);} ))],changedInstructions=changes.some(c=>/INSTRUCTION/i.test(String(recordValue(c,'RESPONSIBLE_LAYER')||''))),changedArtifacts=changes.filter(c=>String(recordValue(c,'NEW_ARTIFACT_VERSION')||'').trim()),inPlace=changes.filter(c=>truth(recordValue(c,'IN_PLACE_MODIFICATION')));Object.assign(derived,{CHANGE_SET_ID:changes.map(c=>recordId(c,'changes')),TRIGGERING_DEFECT_IDS:triggered,RCA_VERSION:'RCA-'+hash.sha256Value(recordsForCurrentScope(project,'rootCauses').map(r=>r.sha256||recordId(r,'rootCauses'))).slice(0,16).toUpperCase(),DATE:changes.at(-1)?.createdAt||now(),ARTIFACT_CHANGE_RECORDS:changedArtifacts.length,INSTRUCTION_CHANGED:changedInstructions,IF_EXECUTION_ONLY_DEFECT_WAS_INSTRUCTION_PRESERVED:!changedInstructions,PREFLIGHT_REPEATED_IF_CHANGED:changedInstructions?recordsForCurrentScope(project,'preflightRecords').length>0:true,ARTIFACTS_CHANGED:changedArtifacts.length>0,NEW_VERSIONS_CREATED:changedArtifacts.length,IN_PLACE_MODIFICATIONS:inPlace.length,DOWNSTREAM_VERIFICATIONS_INVALIDATED:safe(project.projectData.history).filter(h=>h.type==='DOWNSTREAM_INVALIDATED'&&h.stage===16).at(-1)?.invalidatedStages||[]});break;}case 17:{const iterations=records(project,'iterations').filter(r=>Number(r.stage)===17&&isActiveRecord(r)),currentIt=iterations.at(-1),previous=records(project,'iterations').filter(r=>isActiveRecord(r)&&recordId(r,'iterations')!==recordId(currentIt,'iterations')).at(-1),ev=evaluateIteration(project,recordId(currentIt,'iterations'),'CORRECTED'),matrix=recordId(currentIt,'iterations')?verificationMatrix(project,recordId(currentIt,'iterations')):{coverage:0},comparisons=recordId(currentIt,'iterations')?recordsForIteration(project,'comparisons',recordId(currentIt,'iterations')):[],defects=recordId(currentIt,'iterations')?recordsForIteration(project,'defects',recordId(currentIt,'iterations')):[],rcas=recordsForCurrentScope(project,'rootCauses'),regs=records(project,'regressions').filter(r=>isActiveRecord(r)),changes=recordsForCurrentScope(project,'changes');Object.assign(derived,{PREVIOUS_ITERATION_ID:recordId(previous,'iterations')||'NONE',NEW_ITERATION_ID:recordId(currentIt,'iterations')||'NONE',PREVIOUS_CANDIDATE_ID:previous?iterationCandidateId(project,recordId(previous,'iterations')):'NONE',NEW_CANDIDATE_ID:currentIt?iterationCandidateId(project,recordId(currentIt,'iterations')):'NONE',CHANGESET_ID:recordId(changes.at(-1),'changes')||'NONE',NEW_FROZEN_VERSIONS:recordValue(currentIt,'FROZEN_VERSIONS')||{},OLD_CONVERSATIONS_CONTINUED:false,TEN_NEW_CONTEXTS_CREATED:ev.contextCount,RUN_NAMESPACE:ev.runs.map(r=>recordId(r,'runs')),IDENTICAL_PACKAGE_CONFIRMED_FOR_ALL_RUNS:!ev.reasons.some(r=>/candidate|package/i.test(r)),PRIOR_OUTPUTS_WITHHELD:!ev.reasons.some(r=>/prior|contamin/i.test(r)),EXECUTE_COMPLETED:ev.runs.length===10,VERIFY_COMPLETED:matrix.coverage===1,COMPARE_COMPLETED:comparisons.length===mandatoryRequirements(project,scopeForIteration(project,recordId(currentIt,'iterations'))).length,ROOT_CAUSE_COMPLETED:defects.every(d=>rcas.some(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===recordId(d,'defects'))),REGRESSION_TESTS_ADDED:defects.every(d=>regs.some(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===recordId(d,'defects'))),CORRECTIONS_COMPLETED:defects.every(d=>changes.some(c=>String(recordValue(c,'TRIGGERING_DEFECT_IDS')||'').includes(recordId(d,'defects'))))});break;}case 18:{const reqs=mandatoryRequirements(project),regs=records(project,'regressions').filter(r=>isActiveRecord(r)&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),completeReqs=reqs.filter(r=>!adjudicationEmpty(recordValue(r,'OBLIGATION'))&&!adjudicationEmpty(recordValue(r,'APPLICABILITY'))&&!adjudicationEmpty(recordValue(r,'OBSERVABLE_SATISFACTION_CONDITION'))),failed=[];if(convergence.requirementCoverage!==1)failed.push('MANDATORY_REQUIREMENT_COVERAGE');if(convergence.verificationCoverage!==1)failed.push('MANDATORY_VERIFICATION_COVERAGE');if(convergence.regressionSuccess!==1)failed.push('REGRESSION_TEST_SUCCESS');if(convergence.criticalDefects)failed.push('CRITICAL_DEFECTS');if(convergence.majorDefects)failed.push('MAJOR_DEFECTS');if(convergence.mandatoryUnresolvedUnknowns)failed.push('MANDATORY_UNRESOLVED_UNKNOWNS');if(convergence.contradictions)failed.push('KNOWN_CORRECTNESS_AFFECTING_CONTRADICTIONS');if(convergence.ambiguities)failed.push('KNOWN_CORRECTNESS_AFFECTING_AMBIGUITIES');if(convergence.unexplainedVariance)failed.push('UNEXPLAINED_CORRECTNESS_AFFECTING_EXECUTION_VARIANCE');Object.assign(derived,{ITERATION_ID:convergence.iterationId||project.job.CURRENT_ITERATION||'NONE',METRICS_VERSION:'METRICS-'+hash.sha256Value(convergence).slice(0,16).toUpperCase(),TOTAL_MANDATORY_REQUIREMENTS:reqs.length,MANDATORY_REQUIREMENTS_WITH_COMPLETE_SPECIFICATION_AND_APPLICABILITY:completeReqs.length,MANDATORY_REQUIREMENT_COVERAGE:exactRatio(completeReqs.length,reqs.length||1),MANDATORY_REQUIREMENTS_WITH_AFFIRMATIVE_APPLICABLE_VERIFICATION:Math.round(reqs.length*convergence.verificationCoverage),MANDATORY_VERIFICATION_COVERAGE:exactRatio(Math.round(reqs.length*convergence.verificationCoverage),reqs.length||1),TOTAL_STILL_APPLICABLE_REGRESSION_TESTS:regs.length,SUCCESSFUL_REGRESSION_TESTS:Math.round(regs.length*convergence.regressionSuccess),REGRESSION_TEST_SUCCESS:exactRatio(Math.round(regs.length*convergence.regressionSuccess),regs.length||1),CRITICAL_DEFECTS:convergence.criticalDefects,MAJOR_DEFECTS:convergence.majorDefects,MANDATORY_UNRESOLVED_UNKNOWNS:convergence.mandatoryUnresolvedUnknowns,KNOWN_CORRECTNESS_AFFECTING_CONTRADICTIONS:convergence.contradictions,KNOWN_CORRECTNESS_AFFECTING_AMBIGUITIES:convergence.ambiguities,UNEXPLAINED_CORRECTNESS_AFFECTING_EXECUTION_VARIANCE:convergence.unexplainedVariance,ALL_CONDITIONS_SIMULTANEOUSLY_TRUE:convergence.converged,FAILED_CONDITIONS:failed,STABILITY_SUMMARY:convergence.iterationId?executionStability(project,convergence.iterationId):null});break;}case 20:{const confirmation=recordsForCurrentScope(project,'confirmationRecords').filter(r=>effectiveDetermination('confirmationRecords',r,null,project)==='SATISFIED').at(-1),baseline=recordsForCurrentScope(project,'baselines').at(-1),approved=recordValue(baseline,'APPROVED_VERSIONS')||{},candidateId=String(approved.candidateId||''),candidate=records(project,'candidateFreezes').find(r=>recordId(r,'candidateFreezes')===candidateId),manifest=safe(recordValue(candidate,'COMPONENT_MANIFEST'));Object.assign(derived,{BASELINE_ID:recordId(baseline,'baselines')||'NONE',BASELINE_APPROVAL_DATE:baseline?.createdAt||'NONE',SUPPORTING_CONFIRMATION_ITERATION:String(recordValue(confirmation,'CONFIRMATION_ITERATION_ID')||recordValue(confirmation,'ITERATION_ID')||confirmation?.scope?.iterationId||'NONE'),APPROVED_VERSIONS:approved,BASELINE_FILE_RECORDS:manifest,UNCHANGED_CONFIRMATION_SUCCEEDED:Boolean(confirmation),ALL_APPROVED_COMPONENTS_PRESENT:manifest.length>0&&manifest.every(x=>x.artifactId&&x.filename&&x.sha256),ALL_IMMUTABLE_FILES_HASHED:manifest.length>0&&manifest.every(x=>/^[a-f0-9]{64}$/i.test(String(x.sha256||''))),ANY_CHANGED_COMPONENT_RETAINS_BASELINE_STATUS:false});break;}case 21:{const product=recordsForCurrentScope(project,'products').at(-1),productId=recordId(product,'products'),baselineId=String(recordValue(product,'BASELINE_ID')||product?.relationships?.BASELINE_ID||project.job.CURRENT_BASELINE_ID||''),artifacts=recordsForCurrentScope(project,'artifacts').filter(a=>productId&&String(a.scope?.productId||'')===productId);Object.assign(derived,{PRODUCT_ID:productId||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',BASELINE_ID:baselineId||'NONE',EXECUTION_ID:recordValue(product,'EXECUTION_ID')||'NONE',OUTPUT_FILE_RECORDS:artifacts.map(a=>recordId(a,'artifacts'))});break;}case 22:{const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),tests=recordsForCurrentScope(project,'tests').filter(t=>mandatoryIds.has(testRequirementId(t))&&upper(recordValue(t,'TEST_TYPE'))==='DETERMINISTIC'&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(t,'STATUS')||'READY'))),results=recordsForCurrentScope(project,'deterministicResults'),byTest=new Map();for(const r of results){const id=String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'');if(!byTest.has(id))byTest.set(id,[]);byTest.get(id).push(r);}const determinations=tests.map(t=>{const rows=byTest.get(recordId(t,'tests'))||[];return rows.length===1?effectiveDetermination('deterministicResults',rows[0],t,project):'UNDETERMINED';}),product=recordsForCurrentScope(project,'products').at(-1),productArtifacts=recordsForCurrentScope(project,'artifacts').filter(a=>String(a.scope?.productId||'')===String(currentScope(project).productId||''));Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',PRODUCT_HASHES_BEFORE_TEST:Object.fromEntries(productArtifacts.map(a=>[recordId(a,'artifacts'),recordValue(a,'SHA256')||'UNKNOWN'])),TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NONE',VALIDATOR_VERSION:globalThis.closedLoopTestRuntime?.version||'EXTERNAL',DETERMINISTIC_TEST_RESULTS:results.length,APPLICABLE_MANDATORY_DETERMINISTIC_TESTS:tests.map(t=>recordId(t,'tests')),EXECUTED:determinations.filter(d=>d!=='UNDETERMINED').length,SATISFIED:determinations.filter(d=>d==='SATISFIED').length,VIOLATED:determinations.filter(d=>d==='VIOLATED').length,UNDETERMINED:determinations.filter(d=>d==='UNDETERMINED').length,MISSING_TEST_RESULTS:tests.filter(t=>(byTest.get(recordId(t,'tests'))||[]).length!==1).map(t=>recordId(t,'tests')),PRODUCT_REJECTED_BY_MANDATORY_FAILURE:determinations.includes('VIOLATED')});break;}case 23:{const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),tests=recordsForCurrentScope(project,'tests').filter(t=>mandatoryIds.has(testRequirementId(t))&&upper(recordValue(t,'TEST_TYPE'))==='MEANING'),results=recordsForCurrentScope(project,'meaningResults'),ds=results.map(r=>effectiveDetermination('meaningResults',r,testForResult(project,r),project)),change=acceptedChanges(project,23).at(-1),reviewer=String(change?.scope?.contextId||''),product=recordsForCurrentScope(project,'products').at(-1),producer=String(recordValue(product,'PRODUCTION_CONTEXT_ID')||product?.relationships?.PRODUCTION_CONTEXT_ID||''),ind=evaluateContextIndependence(project,{role:'MEANING_REVIEW',reviewerContextId:reviewer,productionContextId:producer});Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NONE',MEANING_RUBRIC_VERSION:'MEANING-'+hash.sha256Value(tests.map(t=>t.sha256||recordId(t,'tests'))).slice(0,16).toUpperCase(),EVALUATOR_ID:reviewer||'UNKNOWN',EVALUATOR_INDEPENDENT_FROM_GENERATOR:ind.determination,MEANING_REQUIREMENT_RECORDS:results.length,ACTIVE_MEANING_REQUIREMENTS:tests.length,MEANING_RECORDS_COMPLETED:results.length,SATISFIED:ds.filter(d=>d==='SATISFIED').length,VIOLATED:ds.filter(d=>d==='VIOLATED').length,UNDETERMINED:ds.filter(d=>d==='UNDETERMINED').length,UNSUPPORTED_BARE_CONCLUSIONS:results.filter(r=>!evaluateEvidenceSufficiency(project,{test:testForResult(project,r),result:r}).sufficient).length});break;}case 24:{const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),tests=recordsForCurrentScope(project,'tests').filter(t=>mandatoryIds.has(testRequirementId(t))&&upper(recordValue(t,'TEST_TYPE'))==='ADVERSARIAL'),regs=records(project,'regressions').filter(r=>isActiveRecord(r)&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),results=recordsForCurrentScope(project,'adversarialResults'),ds=results.map(r=>effectiveDetermination('adversarialResults',r,testForResult(project,r),project)),change=acceptedChanges(project,24).at(-1),reviewer=String(change?.scope?.contextId||''),product=recordsForCurrentScope(project,'products').at(-1),producer=String(recordValue(product,'PRODUCTION_CONTEXT_ID')||product?.relationships?.PRODUCTION_CONTEXT_ID||''),ind=evaluateContextIndependence(project,{role:'ADVERSARIAL_REVIEW',reviewerContextId:reviewer,productionContextId:producer}),linkedDefects=results.map(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')).filter(Boolean),defectRows=records(project,'defects').filter(d=>linkedDefects.includes(recordId(d,'defects')));Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',ADVERSARIAL_REVIEW_VERSION:'ADVERSARIAL-'+hash.sha256Value(results.map(r=>r.sha256||recordId(r,'adversarialResults'))).slice(0,16).toUpperCase(),REVIEWER_ID:reviewer||'UNKNOWN',REVIEWER_INDEPENDENT:ind.determination,ADVERSARIAL_CHECK_RECORDS:results.length,ATTACKS_EXECUTED:results.length,MANDATORY_DEFECTS_FOUND:ds.filter(d=>d==='VIOLATED').length,CRITICAL_DEFECTS_FOUND:defectRows.filter(d=>upper(recordValue(d,'SEVERITY'))==='CRITICAL').length,MAJOR_DEFECTS_FOUND:defectRows.filter(d=>upper(recordValue(d,'SEVERITY'))==='MAJOR').length,UNDETERMINED_ATTACKS:ds.filter(d=>d==='UNDETERMINED').length,REGRESSIONS_FOUND:results.filter(r=>String(recordValue(r,'REG_ID')||r.relationships?.REG_ID||'')).length,RETURN_TO_ROOT_CAUSE_REQUIRED:ds.some(d=>d!=='SATISFIED')});break;}case 25:{const coverage=representationInspectionCoverage(project),baseline=recordsForCurrentScope(project,'baselines').at(-1),product=recordsForCurrentScope(project,'products').at(-1),defects=unresolvedMaterialDefects(project);Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||project.job.CURRENT_PRODUCT_ID||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',REPRESENTATION_REVIEW_VERSION:'REPRESENTATION-'+hash.sha256Value(coverage.details).slice(0,16).toUpperCase(),APPROVED_BASELINE_ID:recordId(baseline,'baselines')||project.job.CURRENT_BASELINE_ID||'NONE',DELIVERY_ARTIFACT_INVENTORY:coverage.details.map(x=>x.artifactId),TRANSFORMATION_CHAIN_RECORDS:coverage.totalTransformationsInspected,PAGE_OR_VIEW_INSPECTION_RECORDS:coverage.totalPagesOrViewsInspected,PACKAGE_INSPECTION_RECORDS:coverage.totalPackagedFilesOpenedOrTested,TOTAL_DELIVERY_ARTIFACTS:coverage.totalDeliveryArtifacts,TOTAL_PAGES_OR_VIEWS_REQUIRED:coverage.totalPagesOrViewsRequired,TOTAL_PAGES_OR_VIEWS_INSPECTED:coverage.totalPagesOrViewsInspected,TOTAL_PACKAGED_FILES_REQUIRED:coverage.totalPackagedFilesRequired,TOTAL_PACKAGED_FILES_OPENED_OR_TESTED:coverage.totalPackagedFilesOpenedOrTested,UNRESOLVED_CRITICAL_DEFECTS:defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='CRITICAL').length,UNRESOLVED_MAJOR_DEFECTS:defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='MAJOR').length,UNRESOLVED_REPRESENTATION_UNKNOWNS:coverage.reasons.length,FINAL_REPRESENTATION_DETERMINATION:coverage.complete?'SATISFIED':'UNDETERMINED'});break;}case 26:{const process=recordsForCurrentScope(project,'processAudits'),productAudits=recordsForCurrentScope(project,'productAudits'),product=recordsForCurrentScope(project,'products').at(-1),baseline=recordsForCurrentScope(project,'baselines').at(-1),allAudits=[...process,...productAudits],missing=allAudits.filter(a=>!evaluateEvidenceSufficiency(project,{result:a}).sufficient).map(a=>recordId(a,process.includes(a)?'processAudits':'productAudits')),defects=[...new Set(allAudits.map(a=>String(recordValue(a,'DEFECT_ID')||a.relationships?.DEFECT_ID||'')).filter(Boolean))],blockers=openBlockers(project,26).map(b=>recordId(b,'blockers'));Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',BASELINE_ID:recordId(baseline,'baselines')||project.job.CURRENT_BASELINE_ID||'NONE',REVIEW_VERSION:'RECONCILIATION-'+hash.sha256Value(allAudits.map(a=>a.recordSha256||a.sha256||a.id)).slice(0,16).toUpperCase(),MISSING_EVIDENCE_LINKS:missing,RECONCILIATION_DEFECT_IDS:defects,RECONCILIATION_BLOCKER_IDS:blockers});break;}case 27:{const rr=recordsForCurrentScope(project,'releaseRecords').at(-1),allResults=[...recordsForCurrentScope(project,'deterministicResults'),...recordsForCurrentScope(project,'meaningResults'),...recordsForCurrentScope(project,'adversarialResults')];Object.assign(derived,{RELEASE_GATE_ID:recordId(rr,'releaseRecords')||'PENDING',DATE_AND_TIME:rr?.createdAt||now(),PRODUCT_ID:release.productId||'NONE',PRODUCT_VERSION:recordValue(recordsForCurrentScope(project,'products').at(-1),'PRODUCT_VERSION')||'NONE',BASELINE_ID:release.baselineId||'NONE',RECONCILED_REVIEW_VERSION:'RECONCILED-'+hash.sha256Value([...recordsForCurrentScope(project,'processAudits'),...recordsForCurrentScope(project,'productAudits')].map(r=>r.sha256||r.recordSha256||r.id)).slice(0,16).toUpperCase(),TOTAL_MANDATORY_REQUIREMENTS:release.mandatoryRequirementCount,MANDATORY_REQUIREMENTS_WITH_AFFIRMATIVE_SUPPORTING_EVIDENCE:release.satisfied,MANDATORY_REQUIREMENTS_DEMONSTRABLY_VIOLATED:release.violated,MANDATORY_REQUIREMENTS_NOT_ESTABLISHED:release.undetermined,TOTAL_MANDATORY_VALIDATORS:release.validatorCount,MANDATORY_VALIDATORS_SUCCEEDED:release.validatorCount-release.failedValidatorIds.length-release.unknownValidatorIds.length,MANDATORY_VALIDATORS_FAILED:release.failedValidatorIds.length,MANDATORY_VALIDATORS_UNDETERMINED_OR_NOT_RUN:release.unknownValidatorIds.length,UNRESOLVED_CRITICAL_DEFECTS:release.criticalDefects,UNRESOLVED_MAJOR_DEFECTS:release.majorDefects,BLOCKING_REQUIREMENT_IDS:release.blockingRequirements,VIOLATED_REQUIREMENT_IDS:release.violatedRequirements,FAILED_TEST_IDS:release.failedValidatorIds,UNDETERMINED_OR_MISSING_TEST_IDS:release.unknownValidatorIds,UNRESOLVED_DEFECT_IDS:unresolvedMaterialDefects(project).map(d=>recordId(d,'defects')),BLOCKER_IDS:release.blockerIds,SELECTED_RELEASE_STATE:release.determination,CONTROLLING_DECISION_RULE:release.determination==='REJECTED'?'AFFIRMATIVE_CURRENT_NONCONFORMANCE_REJECTS':release.determination==='BLOCKED'?'INSUFFICIENT_OR_CONTRADICTORY_CURRENT_EVIDENCE_BLOCKS':'ALL_CURRENT_RELEASE_CONDITIONS_SATISFIED',CONTROLLING_REASON:release.determination==='ACCEPTED'?'Every mandatory requirement is currently established with sufficient noncontradictory evidence and release-critical identity conditions are satisfied.':release.determination==='REJECTED'?'Current evidence establishes mandatory nonconformance.':'Current evidence is incomplete, insufficient, stale, blocked, or contradictory.',AFFIRMATIVE_EVIDENCE:release.inputReferences});break;}case 28:Object.assign(derived,DERIVATIONS['stage28.artifactIdentity'](project).value);break;case 29:Object.assign(derived,DERIVATIONS['stage29.evidenceChains'](project).value);break;case 30:{const defects=records(project,'defects',{active:false}),confirmed=confirmedDefects(project,{allScopes:true}),regs=records(project,'regressions',{active:false}),activeRegs=regs.filter(r=>isActiveRecord(r)&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),retiredRegs=regs.filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE'))==='RETIRED'),execs=records(project,'regressionExecutions',{active:false}),missingFields=defects.filter(d=>['OBSERVED_FAILURE','EXPECTED_CONDITION','SEVERITY','STATUS'].some(k=>adjudicationEmpty(recordValue(d,k)))).map(d=>recordId(d,'defects')),covered=new Set(regs.map(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||''))),missingRegs=confirmed.filter(d=>!covered.has(recordId(d,'defects'))).map(d=>recordId(d,'defects'));Object.assign(derived,{DEFECT_REGISTRY_VERSION:'DEFECT-REGISTRY-'+hash.sha256Value(defects.map(d=>d.recordSha256||d.sha256||recordId(d,'defects'))).slice(0,16).toUpperCase(),REGRESSION_REGISTRY_VERSION:'REGRESSION-REGISTRY-'+hash.sha256Value(regs.map(r=>r.recordSha256||r.sha256||recordId(r,'regressions'))).slice(0,16).toUpperCase(),REGISTRY_IS_APPEND_ONLY:true,DEFECT_RECORDS:defects.length,REGRESSION_RECORDS:regs.length,FUTURE_BASELINE_REGRESSION_EXECUTION_RECORDS:execs.length,TOTAL_DEFECT_RECORDS:defects.length,TOTAL_CONFIRMED_DEFECTS_WITH_REGRESSION_TESTS:confirmed.length-missingRegs.length,TOTAL_ACTIVE_REGRESSION_TESTS:activeRegs.length,TOTAL_RETIRED_REGRESSION_TESTS:retiredRegs.length,DEFECT_RECORDS_MISSING_REQUIRED_FIELDS:missingFields,CONFIRMED_DEFECTS_MISSING_REGRESSION_TESTS:missingRegs,FINAL_REGISTRY_DETERMINATION:missingFields.length||missingRegs.length?'UNDETERMINED':'SATISFIED'});break;}}derived.STAGE_DECISION=project.stages[stage].status==='COMPLETE'?'READY TO PROCEED':project.stages[stage].status==='BLOCKED'?'BLOCKED':'NOT READY - CORRECTION REQUIRED';derived.DECISION_EVIDENCE=project.stages[stage].gate?.reasons?.length?project.stages[stage].gate.reasons.join('; '):'Canonical current-scope records and deterministic calculations satisfy the stage gate.';applyAmendmentDerivedData(project,stage,derived);return derived;}

function applyAmendmentDerivedData(project,stage,derived){
  const number=Number(stage),stageAgent=project.stages?.[number]?.agentData||{};
  if(number===4){const requirements=recordsForCurrentScope(project,'requirements'),propositions=recordsForCurrentScope(project,'propositions');Object.assign(derived,{PROPOSITION_RECORDS:propositions.map(item=>recordId(item,'propositions')),PROPOSITION_MAPPING_RECORDS:requirements.map(requirement=>({requirementId:requirementId(requirement),primaryPropositionIds:primaryPropositionsForRequirement(project,requirement).map(item=>recordId(item,'propositions'))})),PROPOSED_NORMATIVE_CLASSIFICATIONS:requirements.map(requirement=>({requirementId:requirementId(requirement),classification:normativeClassification(requirement)})),CONDITIONAL_ACTIVATION_PROPOSITIONS:requirements.filter(requirement=>normativeClassification(requirement)==='CONDITIONAL').map(requirement=>({requirementId:requirementId(requirement),activationPropositionId:relationshipIds(requirement,'ACTIVATION_PROPOSITION_ID')[0]||null})),PROPOSED_APPLICABILITY_RECORDS:recordsForCurrentScope(project,'applicabilityRecords').map(item=>recordId(item,'applicabilityRecords')),OBLIGATION_DISPOSITION_CHALLENGE_RECORDS:safe(stageAgent.OBLIGATION_DISPOSITION_CHALLENGE_RECORDS),ATOMICITY_CHALLENGE_RECORDS:safe(stageAgent.ATOMICITY_CHALLENGE_RECORDS),UNRESOLVED_SEMANTIC_CHALLENGES:safe(stageAgent.UNRESOLVED_SEMANTIC_CHALLENGES)});}
  if(number===5){const requirements=recordsForCurrentScope(project,'requirements'),applicabilityReviews=recordsForCurrentScope(project,'applicabilityReviews'),classificationReviews=applicabilityReviews.filter(item=>['CONDITIONAL','OPTIONAL'].includes(controlledNormativeClass(recordValue(item,'REVIEWED_NORMATIVE_CLASSIFICATION'))));Object.assign(derived,{PROPOSITION_EQUIVALENCE_REVIEWS:recordsForCurrentScope(project,'propositionEquivalenceReviews').map(item=>recordId(item,'propositionEquivalenceReviews')),NORMATIVE_CLASSIFICATION_REVIEWS:classificationReviews.map(item=>recordId(item,'applicabilityReviews')),APPLICABILITY_DETERMINATIONS:requirements.map(requirement=>({requirementId:requirementId(requirement),...evaluateApplicability(project,{requirement})})),CONDITIONAL_ACTIVATION_REVIEWS:classificationReviews.filter(item=>controlledNormativeClass(recordValue(item,'REVIEWED_NORMATIVE_CLASSIFICATION'))==='CONDITIONAL').map(item=>recordId(item,'applicabilityReviews')),ATOMICITY_RESOLUTION_RECORDS:safe(stageAgent.ATOMICITY_RESOLUTION_RECORDS),PROOF_STRUCTURE_GAPS:safe(stageAgent.PROOF_STRUCTURE_GAPS),RELEASE_OBLIGATION_REDUCTION_REVIEWS:classificationReviews.map(item=>({reviewId:recordId(item,'applicabilityReviews'),classification:controlledNormativeClass(recordValue(item,'REVIEWED_NORMATIVE_CLASSIFICATION')),status:upper(recordValue(item,'ACCEPTED_STATUS'))}))});}
  if(number===6){const registry=deriveProofObligationRegistry(project),tests=recordsForCurrentScope(project,'tests'),expressions=recordsForCurrentScope(project,'proofExpressions');Object.assign(derived,{PROOF_OBLIGATION_SET_ID:hash.sha256Value(registry.obligations.map(item=>item.recordSha256||item.sha256||recordId(item,'proofObligations')).sort()),PROOF_OBLIGATION_RECORDS:registry.obligations.map(item=>recordId(item,'proofObligations')),PROOF_EXPRESSION_RECORDS:expressions.map(item=>recordId(item,'proofExpressions')),TEST_PROPOSITION_RECORDS:tests.map(item=>({testId:recordId(item,'tests'),text:String(recordValue(item,'TEST_PROPOSITION_TEXT')||''),targetPropositionIds:propositionIdsFor(item)})),TEST_SEMANTIC_COVERAGE_RECORDS:tests.map(item=>({testId:recordId(item,'tests'),coverage:semanticCoverage(item),semanticReviewIds:relationshipIds(item,'SEMANTIC_REVIEW_IDS')})),TEST_ROLE_RECORDS:tests.map(item=>({testId:recordId(item,'tests'),role:testRole(item)})),RELEASE_BEARING_TESTS:tests.filter(releaseBearingTest).map(item=>recordId(item,'tests')),EXPECTED_VARIANCE_CONTRACTS:tests.filter(item=>recordValue(item,'EXPECTED_VARIANCE_CONTRACT')).map(item=>({testId:recordId(item,'tests'),contract:recordValue(item,'EXPECTED_VARIANCE_CONTRACT')})),REQUIRED_EPISTEMIC_BASES:registry.obligations.map(item=>({proofObligationId:recordId(item,'proofObligations'),required:scalarList(recordValue(item,'REQUIRED_EPISTEMIC_BASES'))})),REQUIRED_ENVIRONMENT_DEPENDENCIES:recordsForCurrentScope(project,'environmentDependencies').map(item=>recordId(item,'environmentDependencies')),SEMANTIC_EQUIVALENCE_BLOCKERS:registry.reasons});}
  if([11,17,19].includes(number)){const iteration=latestIteration(project,[number]),iterationId=recordId(iteration,'iterations'),independence=evaluateContextIndependence(project,{role:'RUN_BATCH',iterationId}),contexts=recordsForIteration(project,'freshContexts',iterationId),reservations=recordsForIteration(project,'operationReservations',iterationId);Object.assign(derived,{APPLICATION_SESSION_DISTINCTNESS:independence.dimensions.APPLICATION_SESSION_DISTINCTNESS?.truthValue||'UNKNOWN',APPLICATION_INPUT_ISOLATION:independence.dimensions.APPLICATION_INPUT_ISOLATION?.truthValue||'UNKNOWN',USER_TRANSFER_CONFORMITY:independence.dimensions.USER_TRANSFER_CONFORMITY?.truthValue||'UNKNOWN',PROVIDER_CONTEXT_INDEPENDENCE:independence.dimensions.PROVIDER_CONTEXT_INDEPENDENCE?.truthValue||'UNKNOWN',INDEPENDENCE_LIMITATIONS:independence.reasons,ENVIRONMENT_MANIFESTS:contexts.map(item=>recordValue(item,'ENVIRONMENT_MANIFEST_ID')||recordValue(item,'ENVIRONMENT_MANIFEST')||null).filter(Boolean),EXPECTED_VARIANCE_CONTRACT_ID:hash.sha256Value(recordsForCurrentScope(project,'tests').map(item=>recordValue(item,'EXPECTED_VARIANCE_CONTRACT')||null)),OPERATION_RESERVATION_IDS:reservations.map(item=>recordId(item,'operationReservations')),PACKAGE_IDS:reservations.map(item=>String(recordValue(item,'PACKAGE_ID')||'')).filter(Boolean),CHALLENGE_NONCES:reservations.map(item=>String(recordValue(item,'CHALLENGE_NONCE')||'')).filter(Boolean),INPUT_BINDING_BASES:contexts.map(item=>recordValue(item,'INPUT_BINDING_BASIS')||'UNKNOWN')});}
  if(number===2){const search=sourceSearchAssessment(project),sourceSummary=normalizeSourceConsequences(project);Object.assign(derived,{SOURCE_SEARCH_CONTRACT_ID:search.contractId,SEARCH_ADEQUACY_REVIEW_ID:search.adequacyReviewId,SOURCE_AUTHENTICITY_SUMMARY:sourceSummary.map(item=>({sourceId:item.sourceId,status:item.AUTHENTICITY_STATUS,basis:item.AUTHENTICITY_BASIS,evidenceIds:item.evidenceIds})),SOURCE_FRESHNESS_SUMMARY:sourceSummary.map(item=>({sourceId:item.sourceId,status:item.FRESHNESS_STATUS,basis:item.freshnessBasis,contradictionStatus:item.freshnessContradictionStatus,evidenceIds:item.evidenceIds}))});}
  if(number===12){const iteration=latestIteration(project,[10,17,19]),relation=requiredVerificationRelationSet(project,recordId(iteration,'iterations')),verification=recordsForIteration(project,'verification',recordId(iteration,'iterations'));Object.assign(derived,{REQUIRED_VERIFICATION_RELATION_SET_ID:hash.sha256Value(relation.keys),REQUIRED_RELATION_TUPLES:relation.tuples,PROPOSITION_IDS:[...new Set(relation.tuples.map(item=>item.propositionId).filter(Boolean))],OBSERVATION_IDS:[...new Set(verification.flatMap(item=>relationshipIds(item,'OBSERVATION_ID')))],ENTAILMENT_RELATION_IDS:[...new Set(verification.flatMap(item=>relationshipIds(item,'ENTAILMENT_ID')))],EPISTEMIC_BASES:[...new Set(verification.map(item=>upper(recordValue(item,'EPISTEMIC_BASIS'))).filter(Boolean))],FRESHNESS_STATUSES:[...new Set(verification.map(item=>freshnessEvaluation(item).status))],INDEPENDENCE_DIMENSION_RECORDS:verification.map(item=>evaluateContextIndependence(project,{role:'VERIFICATION',iterationId:recordId(iteration,'iterations'),runId:String(recordValue(item,'RUN_ID')||item.relationships?.RUN_ID||''),verifierContextId:String(recordValue(item,'VERIFIER_CONTEXT_ID')||'')})),INPUT_BINDING_BASES:verification.map(item=>recordValue(item,'INPUT_BINDING_BASIS')||'UNKNOWN')});}
  if(number===13){const iteration=latestIteration(project,[10,17,19]),stability=executionStability(project,recordId(iteration,'iterations'));Object.assign(derived,{EXPECTED_VARIANCE_CONTRACT_IDS:recordsForCurrentScope(project,'tests').filter(item=>recordValue(item,'EXPECTED_VARIANCE_CONTRACT')).map(item=>recordId(item,'tests')),ALLOWED_VARIANCES:recordsForCurrentScope(project,'comparisons').flatMap(item=>safe(recordValue(item,'ALLOWED_VARIANCES'))),PROHIBITED_VARIANCES:recordsForCurrentScope(project,'comparisons').flatMap(item=>safe(recordValue(item,'PROHIBITED_VARIANCES'))),UNKNOWN_VARIANCES:recordsForCurrentScope(project,'comparisons').flatMap(item=>safe(recordValue(item,'UNKNOWN_VARIANCES'))),ENVIRONMENT_DIFFERENCE_RECORDS:recordsForCurrentScope(project,'comparisons').flatMap(item=>safe(recordValue(item,'ENVIRONMENT_DIFFERENCE_RECORDS'))),PROPOSITION_STABILITY:stability.propositionStability||{}});}
  if(number===18){const iteration=latestIteration(project,[17]),scope=scopeForIteration(project,recordId(iteration,'iterations')),registry=deriveProofObligationRegistry(project),states=recordsForScope(project,'propositions',scope).map(item=>evaluatePropositionState(project,{proposition:item,scopeRule:scope})),dueDeferred=deferredFailureTestsDue(project,18,scope);Object.assign(derived,{TOTAL_MANDATORY_PROPOSITIONS:states.length,SATISFIED_MANDATORY_PROPOSITIONS:states.filter(item=>item.truthValue==='TRUE'&&item.contradictionStatus==='CLEAR').length,PROOF_OBLIGATION_COVERAGE:exactRatio(registry.obligations.length,states.length||1),UNKNOWN_APPLICABILITY_COUNT:states.filter(item=>item.applicability?.applicability==='UNKNOWN').length,UNKNOWN_EQUIVALENCE_COUNT:registry.reasons.filter(reason=>/EQUIVALENT|semantic/i.test(reason)).length,UNKNOWN_ENTAILMENT_COUNT:states.filter(item=>item.reasons.some(reason=>/entailment|observation|proof expression/i.test(reason))).length,EXPIRED_OR_UNKNOWN_FRESHNESS_COUNT:states.filter(item=>['EXPIRED','UNKNOWN'].includes(item.freshnessStatus)).length,DUE_DEFERRED_TESTS_INCOMPLETE:dueDeferred.length,EVIDENCE_CYCLE_COUNT:detectEvidenceJustificationCycles(project).length,UNRESOLVED_SEMANTIC_CHALLENGES:safe(stageAgent.UNRESOLVED_SEMANTIC_CHALLENGES)});}
  if(number===19)Object.assign(derived,{CONFIRMATION_REQUIREMENT_CHALLENGE_COMPLETED:truth(stageAgent.CONFIRMATION_REQUIREMENT_CHALLENGE_COMPLETED),NEW_REQUIREMENT_CANDIDATES_FOUND:Number(stageAgent.NEW_REQUIREMENT_CANDIDATES_FOUND||safe(stageAgent.NEW_REQUIREMENT_CANDIDATE_IDS).length),NEW_REQUIREMENT_CANDIDATE_IDS:safe(stageAgent.NEW_REQUIREMENT_CANDIDATE_IDS),CHALLENGE_SCOPE:stageAgent.CHALLENGE_SCOPE||'UNKNOWN',CHALLENGE_EPISTEMIC_BASIS:controlledBasis(stageAgent.CHALLENGE_EPISTEMIC_BASIS),PROVIDER_CONTEXT_INDEPENDENCE_SUMMARY:derived.PROVIDER_CONTEXT_INDEPENDENCE||'UNKNOWN',ENVIRONMENT_COMPARABILITY_SUMMARY:stageAgent.ENVIRONMENT_COMPARABILITY_SUMMARY||'UNKNOWN'});
  if(number===20){const backup=backupCheckpointStatus(project,'BASELINE_APPROVAL');Object.assign(derived,{BACKUP_CHECKPOINT_ID:backup.checkpointId,BASELINE_INTEGRITY_BASIS:backup.complete?'APPLICATION_IMMUTABLE_CONTENT_ADDRESSED_WITH_VERIFIED_RESTORE':'UNKNOWN',BASELINE_EXTERNAL_AUTHENTICATION_STATUS:'UNKNOWN'});}
  if(number===22){const results=recordsForCurrentScope(project,'deterministicResults');Object.assign(derived,{PROPOSITION_IDS:[...new Set(results.flatMap(item=>propositionIdsFor(item,testForResult(project,item))))],PROOF_OBLIGATION_IDS:[...new Set(results.flatMap(item=>relationshipIds(item,'PROOF_OBLIGATION_ID')))],OBSERVATION_ORIGINS:results.map(item=>upper(recordValue(item,'OBSERVATION_ORIGIN')||'UNKNOWN')),OBSERVATION_IDS:[...new Set(results.flatMap(item=>relationshipIds(item,'OBSERVATION_ID')))],ENTAILMENT_RELATION_IDS:[...new Set(results.flatMap(item=>relationshipIds(item,'ENTAILMENT_ID')))],EPISTEMIC_BASES:[...new Set(results.map(item=>controlledBasis(recordValue(item,'EPISTEMIC_BASIS'))))],INPUT_BINDING_BASES:results.map(item=>recordValue(item,'INPUT_BINDING_BASIS')||'NATIVE_APPLICATION_BOUND'),RUNTIME_BUILD_IDENTITY:[...new Set(results.map(item=>String(recordValue(item,'RUNTIME_BUILD_IDENTITY')||'')).filter(Boolean))],TEST_WORKER_SHA256:[...new Set(results.map(item=>String(recordValue(item,'TEST_WORKER_SHA256')||'')).filter(Boolean))],PARSER_OR_ADAPTER_IDENTITIES:[...new Set(results.flatMap(item=>scalarList(recordValue(item,'PARSER_OR_ADAPTER_IDENTITIES'))))],ENVIRONMENT_MANIFEST_IDS:[...new Set(results.flatMap(item=>relationshipIds(item,'ENVIRONMENT_MANIFEST_ID')))],FRESHNESS_STATUSES:[...new Set(results.map(item=>freshnessEvaluation(item).status))]});}
  if(number===27){const release=releaseMetrics(project);Object.assign(derived,{PRODUCT_RELEASE_ELIGIBILITY:release.determination==='ACCEPTED',PROOF_OBLIGATION_SET_ID:hash.sha256Value(recordsForCurrentScope(project,'proofObligations').map(item=>item.recordSha256||item.sha256||recordId(item,'proofObligations')).sort()),TOTAL_MANDATORY_PROPOSITIONS:release.mandatoryPropositionCount??release.mandatoryRequirementCount,SATISFIED_MANDATORY_PROPOSITIONS:release.satisfied,VIOLATED_MANDATORY_PROPOSITIONS:release.violated,UNDETERMINED_MANDATORY_PROPOSITIONS:release.undetermined,UNKNOWN_APPLICABILITY_IDS:release.unknownApplicabilityIds||[],INSUFFICIENT_EPISTEMIC_BASIS_IDS:release.insufficientEpistemicBasisIds||[],EXPIRED_EVIDENCE_IDS:release.expiredEvidenceIds||[],DUE_DEFERRED_TEST_IDS:release.dueDeferredTestIds||[],DEPENDENCY_BLOCKER_IDS:release.dependencyBlockerIds||[],RELEASE_EVIDENCE_GRAPH_ACYCLIC:release.releaseEvidenceGraphAcyclic!==false});}
  if(number===28){const identities=recordsForCurrentScope(project,'artifactIdentities'),identityArtifactId=item=>String(recordValue(item,'ARTIFACT_ID')||item?.relationships?.ARTIFACT_ID||'').trim()||null;Object.assign(derived,{DELIVERY_ARTIFACT_IDENTITY_VERIFIED:identities.length>0&&identities.every(item=>truth(recordValue(item,'DELIVERY_ARTIFACT_IDENTITY_VERIFIED'))),HUMAN_DELIVERY_INTENT:Boolean(project.release?.humanDeliveryIntent),HUMAN_DELIVERY_AUTHORIZATION_ID:project.release?.humanDeliveryAuthorizationId||null,DELIVERY_AUTHORIZATION_EFFECTIVE:false,FILENAME_NORMALIZATION_VERSION:schema.FILENAME_NORMALIZATION_VERSION||'closed-loop-filename/1',FORMAT_INTERPRETATION_RECORDS:identities.map(item=>({artifactId:identityArtifactId(item),filename:recordValue(item,'RELEASE_FILENAME')??null})),CURRENT_BYTE_REVERIFICATION_RECEIPT:hash.sha256Value(identities.map(item=>({artifactId:identityArtifactId(item),sha256:recordValue(item,'PRE_DELIVERY_SHA256')??null,size:recordValue(item,'RELEASE_BYTE_SIZE')??null})))});}
  if(number===29){const chains=recordsForCurrentScope(project,'evidenceChains'),cycles=detectEvidenceJustificationCycles(project);Object.assign(derived,{PROOF_OBLIGATION_SET_ID:hash.sha256Value(recordsForCurrentScope(project,'proofObligations').map(item=>item.recordSha256||item.sha256||recordId(item,'proofObligations')).sort()),PROPOSITION_CHAIN_RECORDS:chains.map(item=>({chainId:recordId(item,'evidenceChains'),propositionId:recordValue(item,'PROPOSITION_ID'),status:recordValue(item,'STATUS')})),OBSERVATION_AND_ENTAILMENT_LINKS:chains.map(item=>({chainId:recordId(item,'evidenceChains'),observationIds:safe(recordValue(item,'OBSERVATION_IDS')),entailmentIds:safe(recordValue(item,'ENTAILMENT_IDS'))})),EPISTEMIC_BASIS_COVERAGE:exactRatio(chains.filter(item=>safe(recordValue(item,'EPISTEMIC_BASES')).length).length,chains.length||1),FRESHNESS_COVERAGE:exactRatio(chains.filter(item=>safe(recordValue(item,'FRESHNESS_STATUSES')).every(value=>['CURRENT','NOT_APPLICABLE'].includes(upper(value)))).length,chains.length||1),ENVIRONMENT_DEPENDENCY_COVERAGE:exactRatio(chains.filter(item=>!safe(recordValue(item,'MISSING_LINKS')).some(value=>String(value).startsWith('ENVIRONMENT_DEPENDENCY'))).length,chains.length||1),EVIDENCE_CHAIN_SET_SHA256:hash.sha256Value(chains.map(item=>item.recordSha256||item.sha256||recordId(item,'evidenceChains')).sort()),JUSTIFICATION_CYCLE_COUNT:cycles.length,DELIVERY_AUTHORIZATION_EFFECTIVE:false});}
  if(number===30){const integrity=evaluateRegistryIntegrity(project),terminal=terminalDeliveryState(project);Object.assign(derived,{REGRESSION_RETIREMENT_RECORDS:records(project,'regressions',{active:false}).filter(item=>upper(recordValue(item,'RETIREMENT_STATUS')||recordValue(item,'ACTIVE_RETIRED_STATE'))==='RETIRED').map(item=>recordId(item,'regressions')),REGISTRY_INTEGRITY_STATUS:integrity.complete?'COMPLETE':'BLOCKED',REGISTRY_INTEGRITY_HASH:integrity.registryIntegrityHash,TERMINAL_PROOF_OBLIGATION_SET_HASH:terminal.terminalProofObligationSetHash,DELIVERY_ID:project.release?.deliveryId||null,DELIVERY_STATE:terminal.deliveryState,DELIVERY_RECORD_HASH:recordValue(recordsForCurrentScope(project,'deliveryRecords').at(-1),'DELIVERY_RECORD_HASH')||null,DELIVERY_AUTHORIZATION_EFFECTIVE:terminal.authorized,SUPERSEDED_OR_WITHDRAWN_DELIVERY_IDS:records(project,'deliveryRecords',{active:false}).filter(item=>['SUPERSEDED','WITHDRAWN_FOR_FUTURE_USE'].includes(upper(recordValue(item,'DELIVERY_STATE')))).map(item=>recordId(item,'deliveryRecords')),POST_RELEASE_REENTRY_STATE:project.release?.postReleaseReentryState||'NONE'});}
  if([13,18,19].includes(number)&&derived.STABILITY_SUMMARY)derived.STABILITY_SUMMARY=canonicalStabilitySummary(derived.STABILITY_SUMMARY);
  return derived;
}

function recalculate(project){
  ensureShape(project);
  project.job.INPUT_SET_HASH_OR_MANIFEST=intakeCoverageManifest(project).manifestSha256;
  normalizeSourceConsequences(project);
  if(acceptedChanges(project,6).length&&recordsForCurrentScope(project,'propositions').length&&recordsForCurrentScope(project,'proofExpressions').length&&!recordsForCurrentScope(project,'proofObligations').length)deriveProofObligationRegistry(project,{commit:true});
  let previousComplete=true;
  for(let stage=1;stage<=30;stage++){
    let state=project.stages[stage];
    if(!previousComplete){
      const prerequisite=`Stage ${String(stage-1).padStart(2,'0')} is not complete.`;
      state.status='NOT STARTED';
      state.gate={complete:false,blocked:false,reasons:[prerequisite]};
      state.decision='';
      state.decisionEvidence=prerequisite;
      state.derivedData={STAGE_DECISION:'NOT READY - CORRECTION REQUIRED',DECISION_EVIDENCE:prerequisite};
      continue;
    }
    const wasComplete=state.status==='COMPLETE',result=gate(stage,project);
    // gate() normalizes project shape and may replace stage objects; always mutate the current canonical object.
    state=project.stages[stage];
    state.gate=result;
    if(result.blocked){state.status='BLOCKED';}
    else if(result.complete){state.status='COMPLETE';}
    else if(hasStageActivity(project,stage)){state.status='IN PROGRESS';}
    else state.status='READY';
    state.decision=state.status==='COMPLETE'?'READY TO PROCEED':state.status==='BLOCKED'?'BLOCKED':'';
    state.decisionEvidence=result.reasons.length?result.reasons.join('; '):'Derived canonical stage gate satisfied.';
    state.derivedData=deriveStageData(project,stage);
    previousComplete=state.status==='COMPLETE';
  }
  const completed=Object.values(project.stages).filter(state=>state.status==='COMPLETE').length;
  const currentStage=completed===30?30:Math.max(1,Object.values(project.stages).find(state=>state.status!=='COMPLETE')?.number||30);
  const current=project.stages[currentStage];
  project.activeStage=Math.max(1,Math.min(30,Number(project.activeStage||currentStage)));
  project.job.CURRENT_STAGE=`STAGE ${String(currentStage).padStart(2,'0')}`;
  const projectOpenBlockers=openBlockers(project);project.job.CURRENT_STATE=projectOpenBlockers.length?'BLOCKED':completed===30?'COMPLETE':current.status==='BLOCKED'?'BLOCKED':current.status==='IN PROGRESS'?'IN PROGRESS':'READY';
  project.job.CURRENT_BLOCKERS=projectOpenBlockers.length?projectOpenBlockers.map(record=>recordId(record,'blockers')).join(', '):'NONE';
  project.job.NEXT_REQUIRED_ACTION=completed===30?actionEnvelope(project,currentStage,{actionType:'COMPLETE',heading:'Workflow complete',explanation:'Preserve the completed workflow and exact release evidence.',primaryButton:null}):operationalNextAction(project,currentStage);
  project.job.LATEST_EVIDENCE_REFERENCE=safe(project.projectData.acceptedChanges).at(-1)?.changeId||project.job.LATEST_EVIDENCE_REFERENCE||'NONE';
  project.job.JOB_RECORD_STATUS=project.stages[1].status==='COMPLETE'?'READY':'NOT READY';
  project.job.STATUS_EVIDENCE=project.stages[1].gate?.reasons?.join('; ')||'Stage 01 canonical evidence is complete.';
  return project;
}

const RESPONSIBLE_STAGE_BY_COLLECTION=Object.freeze({sources:2,sourceConflicts:2,research:3,candidateRequirements:3,requirements:4,propositions:4,propositionEquivalenceReviews:5,applicabilityRecords:5,applicabilityReviews:5,requirementResolutions:5,tests:6,proofExpressions:6,testSemanticReviews:6,proofObligations:6,failureTests:7,instructions:8,instructionTraces:8,preflightRecords:9,iterations:10,candidateFreezes:10,runs:11,verification:12,comparisons:13,defects:14,rootCauses:14,regressions:15,regressionExecutions:15,changes:16,convergenceRecords:18,confirmationRecords:19,baselines:20,products:21,deterministicResults:22,meaningResults:23,adversarialResults:24,representationInspections:25,processAudits:26,productAudits:26,releaseGateReviews:27,releaseRecords:27,artifactIdentities:28,evidenceInvestigations:29,evidenceChains:29,deliveryRecords:30,observationRecords:1,entailmentReviews:5,environmentDependencies:6,operationReservations:1,evidenceRecords:1,artifacts:1,blockers:1,freshContexts:9,deploymentManifests:30});
function responsibleStageForMutation(collection,mutationClass='CONTENT'){const name=String(collection||''),stage=RESPONSIBLE_STAGE_BY_COLLECTION[name];if(!stage)throw new Error('No responsible-stage mapping exists for '+name+'.');const kind=upper(mutationClass);if(name==='observationRecords'&&kind.includes('NATIVE'))return 22;if(name==='observationRecords'&&kind.includes('MEANING'))return 23;if(name==='observationRecords'&&kind.includes('ADVERSARIAL'))return 24;if(name==='observationRecords'&&kind.includes('INSPECTION'))return 25;if(name==='entailmentReviews'&&kind.includes('RESULT'))return 22;return stage;}
function invalidateForMutation(project,mutations,{changeId=null,reason='Canonical mutation changed release-bearing state.'}={}){ensureShape(project);const list=safe(Array.isArray(mutations)?mutations:[mutations]),stages=list.map(item=>responsibleStageForMutation(typeof item==='string'?item:item?.collection,item?.mutationClass)).filter(Boolean),earliestResponsibleStage=Math.min(...stages);if(!Number.isFinite(earliestResponsibleStage))throw new Error('At least one mapped mutation is required.');const id=changeId||allocateInfrastructureId(project,'INVALIDATION','history'),invalidatedStages=invalidateDownstream(project,earliestResponsibleStage,id,reason);return {earliestResponsibleStage,responsibleStageMapVersion:'closed-loop-responsible-stage-map/1',invalidatedStages,changeId:id};}
function invalidateDownstream(project,stage,changeId,reason='Material upstream change'){ensureShape(project);const invalidatedStages=[];for(let number=Number(stage)+1;number<=30;number++){const state=project.stages[number];if(state.status!=='NOT STARTED'||state.acceptedDataChangeIds.length||state.acceptedControlEventIds.length)invalidatedStages.push(number);state.status='NOT STARTED';state.invalidatedBy=changeId;state.gate={complete:false,blocked:false,reasons:['Invalidated by '+changeId+': '+reason+'.']};for(const collection of Object.keys(schema.RECORD_SCHEMAS)){for(const record of records(project,collection,{stage:number,active:false}))if(!record.invalidatedBy){record.invalidatedBy=changeId;record.active=false;record.validity='INVALIDATED';refreshRecordHashes(record,collection);}}for(const change of safe(project.projectData.acceptedChanges).filter(x=>Number(x.stage)===number&&!x.invalidatedBy))change.invalidatedBy=changeId;for(const prompt of safe(project.projectData.generatedPrompts).filter(x=>Number(x.stage)===number&&!x.invalidatedBy))prompt.invalidatedBy=changeId;for(const proposal of safe(project.projectData.responseProposals).filter(x=>Number(x.stage)===number&&!x.invalidatedBy&&x.status==='PENDING_OPERATOR_REVIEW')){proposal.invalidatedBy=changeId;proposal.status='STALE';}for(const confirmation of safe(project.projectData.stageConfirmations).filter(x=>Number(x.stage)===number&&!x.invalidatedBy))confirmation.invalidatedBy=changeId;}for(const collection of ['releaseRecords','artifactIdentities','evidenceChains','convergenceRecords','deliveryRecords'])for(const record of records(project,collection,{active:false}))if(!record.invalidatedBy){record.invalidatedBy=changeId;record.active=false;record.validity='INVALIDATED';}for(const reservation of records(project,'operationReservations',{active:false}))if(ACTIVE_RESERVATION_STATES.includes(upper(recordValue(reservation,'STATUS')||reservation.status))){reservation.fields={...recordFields(reservation),STATUS:'SUPERSEDED'};reservation.status='SUPERSEDED';reservation.invalidatedBy=changeId;reservation.active=false;if(collectionAvailable('operationReservations'))refreshRecordHashes(reservation,'operationReservations');}project.release.gateState='';project.release.authorization='NOT AUTHORIZED';project.release.authorizedArtifactIds=[];project.release.deliveryState='SUPERSEDED';addHistory(project,'DOWNSTREAM_INVALIDATED',{stage,changeId,reason,invalidatedStages,responsibleStageMapVersion:'closed-loop-responsible-stage-map/1'});recalculate(project);return invalidatedStages;}

function invalidateStageForAuthorityChange(project,{stage=project.activeStage,reason='Authoritative human input changed.',operatorLabel='HUMAN_OPERATOR'}={}){ensureShape(project);const number=Number(stage),event=addHistory(project,'STAGE_AUTHORITY_CHANGED',{stage:number,reason,operatorLabel,identityAssurance:'SELF_ASSERTED'}),id=event.eventId,changes=acceptedChanges(project,number),proposalIds=new Set(changes.map(c=>c.proposalId));for(const change of changes)change.invalidatedBy=id;for(const prompt of safe(project.projectData.generatedPrompts).filter(x=>Number(x.stage)===number&&!x.invalidatedBy))prompt.invalidatedBy=id;for(const proposal of safe(project.projectData.responseProposals).filter(x=>Number(x.stage)===number&&!x.invalidatedBy&&x.status==='PENDING_OPERATOR_REVIEW')){proposal.invalidatedBy=id;proposal.status='STALE';}for(const confirmation of safe(project.projectData.stageConfirmations).filter(x=>Number(x.stage)===number&&!x.invalidatedBy))confirmation.invalidatedBy=id;for(const collection of Object.keys(schema.RECORD_SCHEMAS))for(const record of records(project,collection,{stage:number,active:false}))if(!record.invalidatedBy&&(proposalIds.has(record.sourceProposalId)||changes.some(c=>c.rawResponseId&&c.rawResponseId===record.rawResponseId))){record.invalidatedBy=id;record.active=false;record.validity='INVALIDATED';refreshRecordHashes(record,collection);}const state=project.stages[number];state.agentData={};state.acceptedData={};state.acceptedDataChangeIds=[];state.acceptedResponseIds=[];state.currentPromptId=null;if(number<30)invalidateDownstream(project,number,id,reason);else recalculate(project);return event;}

function recordHumanInputVersion(project,changedFields,operator='HUMAN_OPERATOR'){
  ensureShape(project);
  const payload={...Object.fromEntries(schema.HUMAN_INTAKE_FIELDS.map(name=>[name,project.job[name]??''])),clarifications:clone(project.projectData.userEntered?.clarifications||[]),suppliedArtifactFiles:clone(project.projectData.userEntered?.suppliedArtifactFiles||{}),suppliedArtifactText:clone(project.projectData.userEntered?.suppliedArtifactText||{})};
  const sha256=hash.sha256Value(payload);
  const latest=safe(project.projectData.inputVersions).at(-1);
  if(latest?.sha256===sha256)return latest;
  const version=nextVersion(project.job.CURRENT_INPUT_VERSION,'INPUT');
  const record={inputVersionId:allocateInfrastructureId(project,'INPUT-VERSION','inputVersions'),version,sha256,createdAt:now(),operator,changedFields:[...changedFields],payload};
  project.projectData.inputVersions.push(record);
  project.job.CURRENT_INPUT_VERSION=version;
  project.job.INPUT_SET_HASH_OR_MANIFEST=sha256;
  project.projectData.userEntered={...project.projectData.userEntered,...payload};
  addHistory(project,'USER_JOB_INPUT_VERSIONED',{recordId:record.inputVersionId,version,changedFields:[...changedFields],sha256});
  return record;
}

function recordStageConfirmation(project,stage,confirmed,statement,operatorLabel='HUMAN_OPERATOR',options={}){
  ensureShape(project);const number=Number(stage),closure=number===1?stageSuboperationClosure(project,1):null,latest=number===1?closure.finalChange:acceptedChanges(project,number).at(-1);if(number===1&&!latest)throw new Error('Stage 01 confirmation requires the current final accepted '+closure.finalOperation+' DATA_PROPOSAL. '+closure.reasons.join(' '));
  if(number===1&&options.acceptedChangeId&&String(options.acceptedChangeId)!==String(latest.changeId))throw new Error('Stage 01 confirmation must bind the current final accepted '+closure.finalOperation+' change.');
  if(number===1&&options.inputVersion&&String(options.inputVersion)!==String(project.job.CURRENT_INPUT_VERSION||''))throw new Error('Stage 01 confirmation must bind the current User Job Input version.');
  const acceptedPrompt=number===1?acceptedChangePrompt(project,latest):null,finalInstructionId=String(latest?.promptId||latest?.instructionId||acceptedPrompt?.instructionId||acceptedPrompt?.promptId||''),finalContextSignature=String(latest?.contextSignature||acceptedPrompt?.contextSignature||'');
  if(number===1&&options.instructionId&&String(options.instructionId)!==finalInstructionId)throw new Error('Stage 01 confirmation instruction identity does not match the current final accepted operation.');
  if(number===1&&options.contextSignature&&String(options.contextSignature)!==finalContextSignature)throw new Error('Stage 01 confirmation context signature does not match the current final accepted operation.');
  for(const prior of safe(project.projectData.stageConfirmations).filter(x=>Number(x.stage)===number&&!x.invalidatedBy))prior.invalidatedBy=options.invalidatedBy||'SUPERSEDED_CONFIRMATION';
  const history=addHistory(project,'HUMAN_STAGE_CONFIRMATION_RECORDED',{stage:number,confirmed:Boolean(confirmed)});
  const prompt=safe(project.projectData.generatedPrompts).filter(x=>Number(x.stage)===number&&!x.invalidatedBy).at(-1);
  const record={confirmationId:allocateInfrastructureId(project,'STAGE-CONFIRMATION','stageConfirmations'),stage:number,confirmed:Boolean(confirmed),statement:String(statement||''),acceptedChangeId:options.acceptedChangeId||latest?.changeId||null,inputVersion:options.inputVersion||project.job.CURRENT_INPUT_VERSION||null,instructionId:options.instructionId||(number===1?finalInstructionId:null)||prompt?.instructionId||prompt?.promptId||null,contextSignature:options.contextSignature||(number===1?finalContextSignature:null)||prompt?.contextSignature||null,operation:number===1?closure.finalOperation:upper(latest?.operation||'COMPLETE'),operatorLabel:String(options.operatorLabel||operatorLabel||'UNSPECIFIED'),identityAssurance:'SELF_ASSERTED',eventSequence:history.eventSequence,deviceTimestamp:history.deviceTimestamp,createdAt:history.deviceTimestamp};
  project.projectData.stageConfirmations.push(record);recalculate(project);return record;
}

function recordReleaseDetermination(project){ensureShape(project);const metrics=releaseMetrics(project);const releaseEvidenceSha256=hash.sha256Value({metrics,inputReferences:metrics.inputReferences});const existing=records(project,'releaseRecords').find(r=>r.releaseEvidenceSha256===releaseEvidenceSha256);if(existing)return existing;for(const prior of records(project,'releaseRecords',{active:false}))if(!prior.invalidatedBy){prior.active=false;prior.validity='SUPERSEDED';prior.invalidatedBy='RELEASE-EVIDENCE-CHANGED';refreshRecordHashes(prior,'releaseRecords');}for(const delivery of recordsForCurrentScope(project,'deliveryRecords')){delivery.active=false;delivery.validity='SUPERSEDED';delivery.fields={...recordFields(delivery),DELIVERY_STATE:'SUPERSEDED'};delivery.DELIVERY_STATE='SUPERSEDED';refreshRecordHashes(delivery,'deliveryRecords');}project.release.authorization='NOT AUTHORIZED';project.release.deliveryAuthorizationEffective=false;project.release.deliveryState='SUPERSEDED';project.release.authorizedArtifactIds=[];const id=allocateId(project,'releaseRecords');const fields={RELEASE_ID:id,PRODUCT_ID:metrics.productId||'UNKNOWN',BASELINE_ID:metrics.baselineId||'UNKNOWN',DETERMINATION:metrics.determination,PRODUCT_RELEASE_ELIGIBILITY:metrics.determination==='ACCEPTED',PROOF_OBLIGATION_SET_ID:hash.sha256Value(recordsForCurrentScope(project,'proofObligations').map(item=>item.recordSha256||item.sha256||recordId(item,'proofObligations'))),TOTAL_MANDATORY_PROPOSITIONS:metrics.mandatoryPropositionCount??metrics.mandatoryRequirementCount,SATISFIED_MANDATORY_PROPOSITIONS:metrics.satisfied,VIOLATED_MANDATORY_PROPOSITIONS:metrics.violated,UNDETERMINED_MANDATORY_PROPOSITIONS:metrics.undetermined,MANDATORY_REQUIREMENT_COUNTS:metrics.mandatoryRequirementCount,AFFIRMATIVE_EVIDENCE_COUNTS:metrics.satisfied,VIOLATED_COUNTS:metrics.violated,UNDETERMINED_COUNTS:metrics.undetermined,VALIDATOR_COUNTS:metrics.validatorCount,FAILED_VALIDATORS:metrics.failedValidatorIds,UNKNOWN_VALIDATORS:metrics.unknownValidatorIds,CRITICAL_DEFECTS:metrics.criticalDefects,MAJOR_DEFECTS:metrics.majorDefects,BLOCKING_REQUIREMENTS:metrics.blockingRequirements,VIOLATIONS:metrics.violatedRequirements,BLOCKERS:metrics.blockerIds,UNKNOWN_APPLICABILITY_IDS:metrics.unknownApplicabilityIds||[],INSUFFICIENT_EPISTEMIC_BASIS_IDS:metrics.insufficientEpistemicBasisIds||[],EXPIRED_EVIDENCE_IDS:metrics.expiredEvidenceIds||[],DUE_DEFERRED_TEST_IDS:metrics.dueDeferredTestIds||[],DEPENDENCY_BLOCKER_IDS:metrics.dependencyBlockerIds||[],RELEASE_EVIDENCE_GRAPH_ACYCLIC:metrics.releaseEvidenceGraphAcyclic!==false,CONTROLLING_EVIDENCE:releaseEvidenceSha256};const record={id,stage:27,createdAt:now(),active:true,fields,...fields,releaseEvidenceSha256,scope:currentScope(project),source:'APPLICATION_DERIVATION',derivationKey:'stage27.release'};refreshRecordHashes(record,'releaseRecords');project.projectData.releaseRecords.push(record);project.release.gateState=metrics.determination;project.release.productEligibility=metrics.determination==='ACCEPTED'?'PRODUCT_RELEASE_ELIGIBLE':'NOT_ELIGIBLE';addHistory(project,'RELEASE_DETERMINATION_CALCULATED',{recordId:id,determination:metrics.determination,productReleaseEligibility:project.release.productEligibility,releaseEvidenceSha256});recalculate(project);return record;}

function legacyConstructEvidenceChains(project){ensureShape(project);const requirements=mandatoryRequirements(project,currentScope(project)),instructions=recordsForCurrentScope(project,'instructions'),product=recordsForCurrentScope(project,'products').at(-1),productId=recordId(product,'products'),executionId=String(recordValue(product,'EXECUTION_ID')||'').trim(),release=recordsForCurrentScope(project,'releaseRecords').at(-1),identities=recordsForCurrentScope(project,'artifactIdentities'),traces=recordsForCurrentScope(project,'instructionTraces'),created=[];
  for(const requirement of requirements){const reqId=requirementId(requirement),sourceId=String(recordValue(requirement,'SOURCE_ID')||requirement.relationships?.SOURCE_ID||recordValue(requirement,'USER_INPUT_RELATIONSHIP')||''),trace=traces.find(r=>String(recordValue(r,'REQ_ID')||r.relationships?.REQ_ID||'')===reqId),instructionId=String(recordValue(trace,'INSTRUCTION_ID')||trace?.relationships?.INSTRUCTION_ID||'').trim(),instruction=instructionId?instructions.find(r=>recordId(r,'instructions')===instructionId):null,tests=applicableTests(project,requirement),results=[...recordsForCurrentScope(project,'verification'),...recordsForCurrentScope(project,'deterministicResults'),...recordsForCurrentScope(project,'meaningResults'),...recordsForCurrentScope(project,'adversarialResults')].filter(r=>resultRequirementId(project,r)===reqId),evidenceIds=new Set(),missing=[];
    for(const r of results)for(const id of evidenceReferences(r))evidenceIds.add(id);if(!sourceId)missing.push('AUTHORITY');if(!trace)missing.push('INSTRUCTION_TRACE');if(!instruction)missing.push('INSTRUCTION');if(!productId)missing.push('PRODUCT');if(productId&&!executionId)missing.push('EXECUTION');for(const test of tests){const tid=recordId(test,'tests'),testResults=results.filter(r=>String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'')===tid);if(!testResults.length)missing.push('TEST_RESULT:'+tid);else for(const result of testResults){const collection=['verification','deterministicResults','meaningResults','adversarialResults'].find(name=>recordsForCurrentScope(project,name).includes(result))||'verification',effective=effectiveDetermination(collection,result,test,project),contract=evaluateEvidenceContract(test,result,null,project),sufficiency=evaluateEvidenceSufficiency(project,{requirement,test,result});if(effective!=='SATISFIED')missing.push('NON_SATISFIED_EFFECTIVE_RESULT:'+tid);if(!contract.sufficient||!sufficiency.sufficient)missing.push('INSUFFICIENT_EVIDENCE:'+tid);}}if(!tests.length)missing.push('TEST');if(!evidenceIds.size)missing.push('CANONICAL_EVIDENCE');if(!release)missing.push('RELEASE_DECISION');else if(upper(recordValue(release,'DETERMINATION'))!=='ACCEPTED')missing.push('RELEASE_NOT_ACCEPTED');const authorizedIdentities=identities.filter(r=>upper(recordValue(r,'AUTHORIZATION'))==='AUTHORIZED'&&truth(recordValue(r,'EXACT_HASH_MATCH'))&&truth(recordValue(r,'EXACT_SIZE_MATCH')));if(!identities.length||authorizedIdentities.length!==identities.length)missing.push('DELIVERY_ARTIFACT_IDENTITY');
    const prior=records(project,'evidenceChains',{active:false}).find(r=>String(recordValue(r,'REQ_ID')||r.relationships?.REQ_ID||'')===reqId&&!r.invalidatedBy),id=prior?recordId(prior,'evidenceChains'):allocateId(project,'evidenceChains'),fields={CHAIN_ID:id,REQ_ID:reqId,AUTHORITY_ID:sourceId||'UNKNOWN',INSTRUCTION_ID:instruction?instructionId:'UNKNOWN',EXECUTION_ID:executionId||'UNKNOWN',PRODUCT_ELEMENT:productId||'UNKNOWN',TEST_ID:tests.map(t=>recordId(t,'tests')),TEST_RESULT_ID:results.map(r=>r.id||r.recordId).filter(Boolean),EVIDENCE_ID:[...evidenceIds],RELEASE_DECISION_ID:recordId(release,'releaseRecords')||'UNKNOWN',ARTIFACT_HASH_IDENTITY:identities.map(r=>recordId(r,'artifactIdentities')),STATUS:missing.length?'INCOMPLETE':'COMPLETE',MISSING_LINKS:[...new Set(missing)]},record={id,stage:29,createdAt:prior?.createdAt||now(),updatedAt:now(),active:true,fields,...fields,scope:currentScope(project),source:'APPLICATION_DERIVATION',derivationKey:'stage29.evidenceChains'};refreshRecordHashes(record,'evidenceChains');if(prior)Object.assign(prior,record);else project.projectData.evidenceChains.push(record);created.push(record);}
  addHistory(project,'EVIDENCE_CHAINS_CONSTRUCTED',{count:created.length,complete:created.filter(r=>r.STATUS==='COMPLETE').length});recalculate(project);return created;
}
function constructEvidenceChains(project){
  ensureShape(project);if(!recordsForCurrentScope(project,'propositions').length)return legacyConstructEvidenceChains(project);const registry=deriveProofObligationRegistry(project),release=recordsForCurrentScope(project,'releaseRecords').at(-1),releaseAccepted=upper(recordValue(release,'DETERMINATION'))==='ACCEPTED',identities=recordsForCurrentScope(project,'artifactIdentities'),identityByArtifact=new Map(identities.map(item=>[String(recordValue(item,'ARTIFACT_ID')||item.relationships?.ARTIFACT_ID||''),item])),cycles=detectEvidenceJustificationCycles(project),created=[];
  for(const obligation of registry.obligations){const obligationId=recordId(obligation,'proofObligations'),propositionId=relationshipIds(obligation,'PROPOSITION_ID')[0],requirementIdValue=relationshipIds(obligation,'REQUIREMENT_ID')[0]||String(recordValue(obligation,'REQUIREMENT_ID')||''),proposition=recordsForCurrentScope(project,'propositions').find(item=>recordId(item,'propositions')===propositionId),state=evaluatePropositionState(project,{proposition,proofObligation:obligation}),applicability=state.applicability||evaluateApplicability(project,{proposition}),sourceState=state.sourceState||evaluateSourceDependencies(project,{sourceIds:relationshipIds(obligation,'REQUIRED_SOURCE_IDS'),requiredEpistemicBases:scalarList(recordValue(obligation,'REQUIRED_EPISTEMIC_BASES'))}),requiredSourceIds=sourceState.sourceIds,sourceEvidenceIds=sourceState.evidenceIds,expressionId=relationshipIds(obligation,'PROOF_EXPRESSION_ID')[0],expression=recordsForCurrentScope(project,'proofExpressions').find(item=>recordId(item,'proofExpressions')===expressionId),expressionState=evaluateProofExpression(project,expression,{proofObligation:obligation,propositionId}),observations=recordsForCurrentScope(project,'observationRecords').filter(item=>relationshipIds(item,'PROPOSITION_ID').includes(propositionId)||relationshipIds(item,'PROOF_OBLIGATION_ID').includes(obligationId)),entailments=recordsForCurrentScope(project,'entailmentReviews').filter(item=>relationshipIds(item,'PROPOSITION_ID').includes(propositionId)&&relationshipIds(item,'OBSERVATION_ID').some(id=>observations.some(observation=>recordId(observation,'observationRecords')===id))),entailmentStates=observations.map(observation=>evaluateObservationEntailment(project,{observation,propositionId,proofObligation:obligation})),requiredTestIds=relationshipIds(obligation,'REQUIRED_TEST_IDS'),resultCollections=['verification','deterministicResults','meaningResults','adversarialResults','representationInspections','regressionExecutions'],results=resultCollections.flatMap(collection=>recordsForCurrentScope(project,collection).filter(item=>requiredTestIds.includes(String(recordValue(item,'TEST_ID')||item.relationships?.TEST_ID||'')))),evidenceIds=[...new Set([...state.supportingEvidenceIds,...sourceEvidenceIds,...observations.flatMap(item=>[...safe(item.evidenceRefs).map(String),...relationshipIds(item,'SOURCE_EVIDENCE_IDS'),...relationshipIds(item,'EVIDENCE_IDS')])])],requiredArtifactIds=relationshipIds(obligation,'REQUIRED_ARTIFACT_IDS'),requiredDependencyIds=relationshipIds(obligation,'REQUIRED_DEPENDENCY_IDS'),dependencies=requiredDependencyIds.map(id=>recordsForCurrentScope(project,'environmentDependencies').find(item=>recordId(item,'environmentDependencies')===id)),missing=[];
    if(!proposition)missing.push('PROPOSITION');if(applicability.applicability!=='APPLICABLE'&&applicability.applicability!=='NOT_APPLICABLE')missing.push('APPLICABILITY');if(applicability.contradictionStatus==='CONTRADICTED')missing.push('APPLICABILITY_CONTRADICTION');if(!sourceState.sufficient)missing.push('SOURCE_AUTHENTICITY_FRESHNESS_OR_BASIS');if(sourceState.contradictionStatus==='CONTRADICTED')missing.push('SOURCE_CONTRADICTION');if(!expression||!expressionState.valid||expressionState.truthValue!=='TRUE')missing.push('PROOF_EXPRESSION');if(state.truthValue!=='TRUE')missing.push('PROPOSITION_NOT_SATISFIED');if(state.contradictionStatus!=='CLEAR')missing.push('PROPOSITION_CONTRADICTION');if(['EXPIRED','UNKNOWN'].includes(state.freshnessStatus))missing.push('FRESHNESS');if(!observations.length)missing.push('OBSERVATION');if(!entailments.length||!entailmentStates.some(item=>item.sufficient&&item.truthValue==='TRUE'))missing.push('ENTAILMENT');if(!evidenceIds.length)missing.push('CANONICAL_EVIDENCE');if(requiredSourceIds.length&&!sourceEvidenceIds.length)missing.push('SOURCE_EVIDENCE');if(requiredTestIds.some(id=>!results.some(result=>String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||'')===id)))missing.push('REQUIRED_TEST_RESULT');if(!releaseAccepted)missing.push('PRODUCT_RELEASE_ELIGIBILITY');for(const artifactId of requiredArtifactIds){const identity=identityByArtifact.get(artifactId);if(!identity||!truth(recordValue(identity,'DELIVERY_ARTIFACT_IDENTITY_VERIFIED'))||!truth(recordValue(identity,'EXACT_HASH_MATCH'))||!truth(recordValue(identity,'EXACT_SIZE_MATCH')))missing.push('ARTIFACT_IDENTITY:'+artifactId);}for(const dependency of dependencies){const dependencyState=evaluateEnvironmentDependency(project,dependency);if(dependencyState.truthValue!=='TRUE')missing.push('ENVIRONMENT_DEPENDENCY:'+dependencyState.dependencyId);}if(dependencies.some(item=>!item))missing.push('ENVIRONMENT_DEPENDENCY');if(cycles.length)missing.push('JUSTIFICATION_CYCLE');
    const prior=records(project,'evidenceChains',{active:false}).find(item=>String(recordValue(item,'PROOF_OBLIGATION_ID')||item.relationships?.PROOF_OBLIGATION_ID||'')===obligationId&&!item.invalidatedBy),id=prior?recordId(prior,'evidenceChains'):allocateId(project,'evidenceChains'),fields={CHAIN_ID:id,REQ_ID:requirementIdValue,PROPOSITION_ID:propositionId,PROOF_OBLIGATION_ID:obligationId,APPLICABILITY_ID:applicability.recordId||null,APPLICABILITY_STATE:applicability.applicability,PROOF_EXPRESSION_ID:expressionId||null,OBSERVATION_IDS:observations.map(item=>recordId(item,'observationRecords')),ENTAILMENT_IDS:entailments.map(item=>recordId(item,'entailmentReviews')),EPISTEMIC_BASES:[...new Set([...entailmentStates.map(item=>item.epistemicBasis),...(requiredSourceIds.length?[sourceState.epistemicBasis]:[])])],FRESHNESS_STATUSES:[...new Set([...entailmentStates.map(item=>item.freshnessStatus),...(requiredSourceIds.length?[sourceState.freshnessStatus]:[])])],ENVIRONMENT_DEPENDENCY_IDS:requiredDependencyIds,SOURCE_IDS:requiredSourceIds,SOURCE_EVIDENCE_IDS:sourceEvidenceIds,AUTHORITY_ID:requirementIdValue,INSTRUCTION_ID:recordsForCurrentScope(project,'instructionTraces').filter(item=>String(recordValue(item,'REQ_ID')||item.relationships?.REQ_ID||'')===requirementIdValue).map(item=>String(recordValue(item,'INSTRUCTION_ID')||item.relationships?.INSTRUCTION_ID||'')).filter(Boolean),EXECUTION_ID:recordsForCurrentScope(project,'products').map(item=>String(recordValue(item,'EXECUTION_ID')||'')).filter(Boolean),PRODUCT_ELEMENT:recordId(recordsForCurrentScope(project,'products').at(-1),'products')||'UNKNOWN',TEST_ID:requiredTestIds,TEST_RESULT_ID:results.map(item=>String(item.id||item.recordId||'')).filter(Boolean),EVIDENCE_ID:evidenceIds,RELEASE_DECISION_ID:recordId(release,'releaseRecords')||'UNKNOWN',ARTIFACT_HASH_IDENTITY:requiredArtifactIds.map(id=>recordId(identityByArtifact.get(id),'artifactIdentities')).filter(Boolean),JUSTIFICATION_CYCLE_COUNT:cycles.length,STATUS:missing.length?'INCOMPLETE':'COMPLETE',MISSING_LINKS:[...new Set(missing)]},record={id,stage:29,createdAt:prior?.createdAt||now(),updatedAt:now(),active:true,fields,...fields,relationships:{REQ_ID:requirementIdValue,PROPOSITION_ID:propositionId,PROOF_OBLIGATION_ID:obligationId,PROOF_EXPRESSION_ID:expressionId||null,OBSERVATION_IDS:fields.OBSERVATION_IDS,ENTAILMENT_IDS:fields.ENTAILMENT_IDS,EVIDENCE_ID:evidenceIds,SOURCE_IDS:requiredSourceIds,SOURCE_EVIDENCE_IDS:sourceEvidenceIds,RELEASE_DECISION_ID:recordId(release,'releaseRecords')||null,ARTIFACT_HASH_IDENTITY:fields.ARTIFACT_HASH_IDENTITY},scope:currentScope(project),source:'APPLICATION_DERIVATION',derivationKey:'stage29.evidenceChains'};refreshRecordHashes(record,'evidenceChains');if(prior)Object.assign(prior,record);else project.projectData.evidenceChains.push(record);created.push(record);
  }
  const chainSetSha256=hash.sha256Value(created.map(item=>item.recordSha256||item.sha256||recordId(item,'evidenceChains')).sort());for(const record of created){record.fields.EVIDENCE_CHAIN_SET_SHA256=chainSetSha256;record.EVIDENCE_CHAIN_SET_SHA256=chainSetSha256;refreshRecordHashes(record,'evidenceChains');}addHistory(project,'EVIDENCE_CHAINS_CONSTRUCTED',{count:created.length,complete:created.filter(item=>upper(recordValue(item,'STATUS'))==='COMPLETE').length,proofObligationCount:registry.obligations.length,evidenceChainSetSha256:chainSetSha256,justificationCycleCount:cycles.length});recalculate(project);return created;
}
function verifyArtifactIdentity(project,audited,delivery){
  ensureShape(project);const release=recordsForCurrentScope(project,'releaseRecords').at(-1);if(upper(recordValue(release,'DETERMINATION'))!=='ACCEPTED')throw new Error('Artifact identity verification is prohibited until Stage 27 PRODUCT_RELEASE_ELIGIBLE is current.');const a=safe(audited),d=safe(delivery),canonicalArtifacts=recordsForCurrentScope(project,'artifacts'),canonicalById=new Map(canonicalArtifacts.map(item=>[recordId(item,'artifacts'),item])),duplicate=(list,key)=>{const seen=new Set();for(const item of list){const value=String(item[key]??'');if(!value||seen.has(value))return true;seen.add(value);}return false;};if(!a.length||!d.length)throw new Error('Current audited and delivery artifact bytes are required.');if(duplicate(a,'artifactId')||duplicate(d,'artifactId')||duplicate(a,'name')||duplicate(d,'name'))throw new Error('Duplicate artifact identity or filename is prohibited.');if(a.length!==d.length)throw new Error('Audited and delivery artifact counts differ.');const normalized=list=>list.map(item=>({artifactId:String(item.artifactId||''),name:String(item.name||''),version:String(item.version||'UNKNOWN'),storageReference:String(item.storageReference||''),size:Number(item.size),sha256:String(item.sha256||'').toLowerCase()})).sort((left,right)=>left.artifactId.localeCompare(right.artifactId)||left.name.localeCompare(right.name)),auditedNormalized=normalized(a),deliveryNormalized=normalized(d),product=recordsForCurrentScope(project,'products').at(-1),productId=recordId(product,'products'),declaredInventory=safe(recordValue(product,'GENERATED_ARTIFACT_INVENTORY')).map(item=>typeof item==='string'?item:String(item?.artifactId||item?.ARTIFACT_ID||'')).filter(Boolean),productScopedIds=canonicalArtifacts.filter(item=>productId&&String(item.scope?.productId||recordValue(item,'PRODUCT_ID')||item.relationships?.PRODUCT_ID||'')===productId).map(item=>recordId(item,'artifacts')),requiredArtifactIds=[...new Set(declaredInventory.length?declaredInventory:productScopedIds)].sort(),auditedIds=auditedNormalized.map(item=>item.artifactId).sort();if(!productId||!requiredArtifactIds.length)throw new Error('Current finished-product artifact inventory is unavailable.');if(hash.sha256Value(requiredArtifactIds)!==hash.sha256Value(auditedIds))throw new Error('Audited artifact selection does not exactly match the current finished-product artifact inventory.');for(const item of auditedNormalized){const canonical=canonicalById.get(item.artifactId);if(!canonical)throw new Error('Audited artifact '+item.artifactId+' is not a current canonical artifact.');if(upper(recordValue(canonical,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED')throw new Error('Audited artifact '+item.artifactId+' does not have verified canonical byte custody.');if(!/^[a-f0-9]{64}$/i.test(item.sha256)||item.sha256!==String(recordValue(canonical,'SHA256')||'').toLowerCase()||item.size!==Number(recordValue(canonical,'BYTE_SIZE'))||item.name!==String(recordValue(canonical,'FILENAME')||''))throw new Error('Audited artifact '+item.artifactId+' does not match canonical filename, byte size, and application-computed SHA-256.');}for(const item of deliveryNormalized){if(!canonicalById.has(item.artifactId))throw new Error('Delivery artifact '+item.artifactId+' does not resolve to a current canonical artifact.');if(!/^[a-f0-9]{64}$/i.test(item.sha256)||!Number.isSafeInteger(item.size)||item.size<0)throw new Error('Delivery artifact '+item.artifactId+' lacks application-computed byte identity.');if(item.storageReference!=='OPERATOR_SELECTED_DELIVERY_BYTES')throw new Error('Delivery artifact '+item.artifactId+' is not bound to the application-hashed operator-selected delivery bytes.');}
  const identityEvidenceSha256=hash.sha256Value({releaseId:recordId(release,'releaseRecords'),audited:auditedNormalized,delivery:deliveryNormalized}),existing=recordsForCurrentScope(project,'artifactIdentities').filter(item=>item.identityEvidenceSha256===identityEvidenceSha256);if(existing.length){const complete=existing.every(item=>truth(recordValue(item,'DELIVERY_ARTIFACT_IDENTITY_VERIFIED')));project.release.artifactIdentityVerified=complete;project.release.humanDeliveryIntent=complete&&existing.every(item=>truth(recordValue(item,'HUMAN_DELIVERY_INTENT')));project.release.humanDeliveryAuthorizationId=project.release.humanDeliveryAuthorizationId||safe(project.projectData.humanDecisions).filter(item=>Number(item.stage)===28&&item.field==='HUMAN_DELIVERY_INTENT'&&truth(item.value)).at(-1)?.decisionId||null;project.release.authorization='NOT AUTHORIZED';project.release.deliveryAuthorizationEffective=false;project.release.authorizedArtifactIds=existing.map(item=>String(recordValue(item,'ARTIFACT_ID')||item.relationships?.ARTIFACT_ID||''));return existing;}
  const supersessionId=`ARTIFACT-IDENTITY-EVIDENCE-${identityEvidenceSha256}`;for(const prior of records(project,'artifactIdentities',{active:false}))if(isActiveRecord(prior)){prior.active=false;prior.validity='SUPERSEDED';prior.invalidatedBy=supersessionId;refreshRecordHashes(prior,'artifactIdentities');}const byId=new Map(deliveryNormalized.map(item=>[item.artifactId,item])),created=[];for(const left of auditedNormalized){const right=byId.get(left.artifactId);if(!right)throw new Error('Missing delivery artifact '+left.artifactId+'.');const exactHash=left.sha256===right.sha256,exactSize=left.size===right.size,exactName=left.name===right.name,id=allocateId(project,'artifactIdentities'),fields={IDENTITY_ID:id,ARTIFACT_ID:left.artifactId,AUDITED_FILENAME:left.name,AUDITED_VERSION:left.version,AUDITED_STORAGE_REFERENCE:left.storageReference||String(recordValue(canonicalById.get(left.artifactId),'STORAGE_REFERENCE')||'INDEXEDDB'),AUDITED_BYTE_SIZE:left.size,AUDITED_SHA256:left.sha256,RELEASE_FILENAME:right.name,RELEASE_VERSION:right.version,RELEASE_STORAGE_REFERENCE:right.storageReference,RELEASE_BYTE_SIZE:right.size,PRE_DELIVERY_SHA256:right.sha256,EXACT_HASH_MATCH:exactHash,EXACT_SIZE_MATCH:exactSize,AUTHORIZED_FILENAME_MATCH:exactName,POST_AUDIT_MODIFICATION_EVIDENCE:exactHash?'NONE':'MISMATCH',DELIVERY_ARTIFACT_IDENTITY_VERIFIED:exactHash&&exactSize&&exactName,HUMAN_DELIVERY_INTENT:true,DELIVERY_AUTHORIZATION_EFFECTIVE:false,AUTHORIZATION:exactHash&&exactSize&&exactName?'IDENTITY VERIFIED':'NOT AUTHORIZED'};const record={id,stage:28,createdAt:now(),active:true,fields,...fields,relationships:{ARTIFACT_ID:left.artifactId,RELEASE_ID:recordId(release,'releaseRecords')},scope:currentScope(project),identityEvidenceSha256,source:'APPLICATION_DERIVATION',derivationKey:'stage28.artifactIdentity'};refreshRecordHashes(record,'artifactIdentities');project.projectData.artifactIdentities.push(record);created.push(record);}const complete=created.length===a.length&&created.every(item=>truth(recordValue(item,'DELIVERY_ARTIFACT_IDENTITY_VERIFIED'))),decisionId=allocateInfrastructureId(project,'HUMAN-DELIVERY-INTENT','humanDecisions');project.projectData.humanDecisions.push({decisionId,stage:28,field:'HUMAN_DELIVERY_INTENT',value:true,artifactIds:created.map(item=>recordValue(item,'ARTIFACT_ID')),identityAssurance:'SELF_ASSERTED',createdAt:now(),source:'HUMAN_DECISION'});project.release.artifactIdentityVerified=complete;project.release.humanDeliveryIntent=complete;project.release.humanDeliveryAuthorizationId=decisionId;project.release.authorization='NOT AUTHORIZED';project.release.deliveryAuthorizationEffective=false;project.release.authorizedArtifactIds=created.map(item=>String(recordValue(item,'ARTIFACT_ID')));addHistory(project,'ARTIFACT_IDENTITY_VERIFIED',{count:created.length,artifactIdentityVerified:complete,humanDeliveryIntent:complete,deliveryAuthorizationEffective:false,identityEvidenceSha256});recalculate(project);return created;
}
const SENSITIVE_BACKUP_CLASSIFICATIONS=Object.freeze(['CONFIDENTIAL','RESTRICTED','CREDENTIAL_SECRET']);
function backupArtifactMetadataManifest(project){return records(project,'artifacts',{active:false}).map(item=>({artifactId:recordId(item,'artifacts'),filename:String(recordValue(item,'FILENAME')||''),byteSize:Number(recordValue(item,'BYTE_SIZE')),sha256:String(recordValue(item,'SHA256')||'').toLowerCase(),availability:upper(recordValue(item,'AVAILABILITY')||'UNKNOWN'),disclosureClassification:upper(recordValue(item,'DISCLOSURE_CLASSIFICATION')||'UNKNOWN'),active:isActiveRecord(item)})).sort((a,b)=>a.artifactId.localeCompare(b.artifactId));}
function backupArtifactManifest(project){return backupArtifactMetadataManifest(project).filter(item=>item.availability==='BYTES_PERSISTED_AND_VERIFIED');}
function requiredBackupArtifactIds(project,milestone){const value=upper(milestone);if(value==='BASELINE_APPROVAL'){const candidate=records(project,'candidateFreezes').filter(isActiveRecord).at(-1);return [...new Set(safe(recordValue(candidate,'COMPONENT_MANIFEST')).map(item=>String(item?.artifactId||'')).filter(Boolean))].sort();}if(value==='FINAL_DELIVERY'){const identities=recordsForCurrentScope(project,'artifactIdentities'),fromIdentity=identities.map(item=>String(recordValue(item,'ARTIFACT_ID')||item.relationships?.ARTIFACT_ID||'')).filter(Boolean);if(fromIdentity.length)return [...new Set(fromIdentity)].sort();const product=recordsForCurrentScope(project,'products').at(-1);return [...new Set(safe(recordValue(product,'GENERATED_ARTIFACT_INVENTORY')).map(item=>typeof item==='string'?item:String(item?.artifactId||item?.ARTIFACT_ID||'')).filter(Boolean))].sort();}return backupArtifactManifest(project).map(item=>item.artifactId);}
function backupCheckpointSubject(project,milestone){const value=upper(milestone),allArtifacts=backupArtifactManifest(project),requiredArtifactIds=requiredBackupArtifactIds(project,value),requiredSet=new Set(requiredArtifactIds),baselineArtifactIds=new Set(records(project,'artifacts',{active:false}).filter(item=>Number(item.stage||0)<=20||requiredSet.has(recordId(item,'artifacts'))).map(item=>recordId(item,'artifacts'))),artifactManifest=value==='BASELINE_APPROVAL'?allArtifacts.filter(item=>baselineArtifactIds.has(item.artifactId)):allArtifacts,isFinal=value==='FINAL_DELIVERY',payload={milestone:value,jobId:String(project.job?.JOB_ID||''),inputVersion:project.job?.CURRENT_INPUT_VERSION||null,sourceSetVersion:project.job?.CURRENT_SOURCE_SET_VERSION||null,requirementsVersion:project.job?.CURRENT_REQUIREMENTS_VERSION||null,testSuiteVersion:project.job?.CURRENT_TEST_SUITE_VERSION||null,instructionVersion:project.job?.CURRENT_INSTRUCTION_VERSION||null,iterationId:project.job?.CURRENT_ITERATION||null,baselineId:project.job?.CURRENT_BASELINE_ID||null,productId:isFinal?project.job?.CURRENT_PRODUCT_ID||null:null,requiredArtifactIds,artifacts:artifactManifest.filter(item=>requiredSet.has(item.artifactId)),confirmationIds:recordsForCurrentScope(project,'confirmationRecords').map(item=>recordId(item,'confirmationRecords')),releaseIds:isFinal?recordsForCurrentScope(project,'releaseRecords').map(item=>recordId(item,'releaseRecords')):[],identityIds:isFinal?recordsForCurrentScope(project,'artifactIdentities').map(item=>recordId(item,'artifactIdentities')):[],evidenceChainHashes:isFinal?recordsForCurrentScope(project,'evidenceChains').map(item=>item.recordSha256||item.sha256||recordId(item,'evidenceChains')).sort():[],registryIntegrityHash:isFinal?evaluateRegistryIntegrity(project).registryIntegrityHash:null,humanDeliveryAuthorizationId:isFinal?project.release?.humanDeliveryAuthorizationId||null:null,deliveryRecordHashes:isFinal?recordsForCurrentScope(project,'deliveryRecords').map(item=>item.recordSha256||item.sha256||recordId(item,'deliveryRecords')).sort():[]};return {milestone:value,subjectSha256:hash.sha256Value(payload),payload,requiredArtifactIds,artifactManifest};}
function backupDisclosureProjectSubject(project){const value=clone(project);delete value.projectSha256;delete value.revision;if(value.projectData){for(const key of ['disclosureDecisions','humanDecisions','externalCheckpoints','backupPolicies','commandReceipts','history'])delete value.projectData[key];}return value;}
function projectBackupSecretFindings(project){const text=JSON.stringify(backupDisclosureProjectSubject(project)),findings=[];for(const [code,pattern] of [['PRIVATE_KEY',/-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----/],['AWS_ACCESS_KEY',/\bAKIA[0-9A-Z]{16}\b/],['GENERIC_SECRET',/\b(?:api[_-]?key|client[_-]?secret|password)\s*[:=]\s*[^\s,;]{8,}/i]])if(pattern.test(text))findings.push(code);return findings;}
function sensitiveBackupSubjects(project){const metadata=backupArtifactMetadataManifest(project),subjects=metadata.filter(item=>SENSITIVE_BACKUP_CLASSIFICATIONS.includes(item.disclosureClassification)).map(item=>({subjectId:item.artifactId,kind:'ARTIFACT_OR_METADATA',classification:item.disclosureClassification,sha256:item.sha256||hash.sha256Value(item)})),summary=upper(project.stages?.[1]?.agentData?.DISCLOSURE_CLASSIFICATION_SUMMARY||project.stages?.[1]?.acceptedData?.DISCLOSURE_CLASSIFICATION_SUMMARY),projectClassification=SENSITIVE_BACKUP_CLASSIFICATIONS.find(value=>summary.includes(value)),secretFindings=projectBackupSecretFindings(project);if(projectClassification||secretFindings.length)subjects.push({subjectId:'PROJECT_CANONICAL_BYTES',kind:'CANONICAL_PROJECT',classification:projectClassification||'CREDENTIAL_SECRET',sha256:hash.sha256Value(backupDisclosureProjectSubject(project)),secretFindings});return subjects.sort((a,b)=>a.subjectId.localeCompare(b.subjectId));}
function sensitiveBackupArtifacts(project){return sensitiveBackupSubjects(project);}
function evaluateBackupDisclosure(project,{encryptionContractId=null,humanAuthorizationId=null}={}){
  ensureShape(project);
  const sensitive=sensitiveBackupSubjects(project),sensitiveIds=sensitive.map(item=>item.subjectId),manifestSha256=hash.sha256Value(sensitive.map(item=>({subjectId:item.subjectId,kind:item.kind,sha256:item.sha256,classification:item.classification})));
  if(!sensitive.length)return {allowed:true,handling:'NO_SENSITIVE_MATERIAL',sensitiveArtifactIds:[],sensitiveSubjectIds:[],manifestSha256,humanAuthorizationId:null,encryptionContractId:null,encryptedExportAvailable:false,reasons:[]};
  // This static build has no application-owned encrypted-export adapter. A
  // declarative contract record is not proof that the gzip package was
  // encrypted, so it can never authorize this plaintext export path.
  const encryptedExportAvailable=false,decision=safe(project.projectData.disclosureDecisions).find(item=>String(item.decisionId||'')===String(humanAuthorizationId||'')&&item.active!==false&&item.acknowledged===true&&String(item.sensitiveArtifactManifestSha256||'')===manifestSha256&&hash.sha256Value([...safe(item.artifactIds).map(String)].sort())===hash.sha256Value([...sensitiveIds].sort()));
  if(decision)return {allowed:true,handling:'EXPLICIT_UNENCRYPTED_ACKNOWLEDGMENT',sensitiveArtifactIds:sensitiveIds.filter(id=>id!=='PROJECT_CANONICAL_BYTES'),sensitiveSubjectIds:sensitiveIds,manifestSha256,humanAuthorizationId:String(decision.decisionId),encryptionContractId:null,encryptedExportAvailable,reasons:[]};
  const requestedContract=String(encryptionContractId||'').trim(),contractReason=requestedContract?' The requested encryption contract cannot be used because this build has no application-owned encrypted-export adapter.':'';
  return {allowed:false,handling:'BLOCKED',sensitiveArtifactIds:sensitiveIds.filter(id=>id!=='PROJECT_CANONICAL_BYTES'),sensitiveSubjectIds:sensitiveIds,manifestSha256,humanAuthorizationId:null,encryptionContractId:null,encryptedExportAvailable,reasons:[`Complete export contains sensitive project or artifact subject(s) ${sensitiveIds.join(', ')} and requires an explicit human acknowledgment of unencrypted handling.${contractReason}`]};
}
function recordBackupDisclosureDecision(project,{operatorLabel='HUMAN_OPERATOR',acknowledged=false}={}){ensureShape(project);if(acknowledged!==true)throw new Error('Explicit acknowledgment of unencrypted sensitive backup handling is required.');const sensitive=sensitiveBackupSubjects(project);if(!sensitive.length)throw new Error('No sensitive project or artifact subject requires an unencrypted-export decision.');const artifactIds=sensitive.map(item=>item.subjectId).sort(),sensitiveArtifactManifestSha256=hash.sha256Value(sensitive.map(item=>({subjectId:item.subjectId,kind:item.kind,sha256:item.sha256,classification:item.classification}))),decisionId=`BACKUP-DISCLOSURE-${hash.sha256Value({jobId:project.job?.JOB_ID,artifactIds,sensitiveArtifactManifestSha256}).slice(0,20).toUpperCase()}`,existing=safe(project.projectData.disclosureDecisions).find(item=>item.decisionId===decisionId&&item.active!==false);if(existing)return existing;const decision={decisionId,type:'UNENCRYPTED_SENSITIVE_BACKUP_ACKNOWLEDGMENT',artifactIds,sensitiveArtifactManifestSha256,acknowledged:true,operatorLabel:String(operatorLabel||'HUMAN_OPERATOR'),identityAssurance:'SELF_ASSERTED',active:true,createdAt:now(),source:'HUMAN_DECISION'};project.projectData.disclosureDecisions.push(decision);project.projectData.humanDecisions.push({...clone(decision),field:'UNENCRYPTED_SENSITIVE_BACKUP_ACKNOWLEDGMENT',value:true,stage:Number(project.activeStage||0)});addHistory(project,'SENSITIVE_BACKUP_EXPORT_ACKNOWLEDGED',{decisionId,subjectIds:artifactIds,operatorLabel:decision.operatorLabel,identityAssurance:'SELF_ASSERTED'});return decision;}
function projectBackupStateSha256(project){const value=clone(project);delete value.projectSha256;return hash.sha256Value(value);}
function normalizeBackupVerificationManifest(items){return safe(items).map(item=>({artifactId:String(item?.artifactId||''),filename:String(item?.filename||''),byteSize:Number(item?.byteSize),sha256:String(item?.sha256||'').toLowerCase()})).sort((a,b)=>a.artifactId.localeCompare(b.artifactId));}
function validateBackupRestoreProof(project,{milestone,verifiedArtifactManifest=[],restoreProof,checkSourceProject=true}={}){const subject=backupCheckpointSubject(project,milestone),manifest=normalizeBackupVerificationManifest(verifiedArtifactManifest),expectedManifest=normalizeBackupVerificationManifest(subject.artifactManifest),proof=restoreProof&&typeof restoreProof==='object'&&!Array.isArray(restoreProof)?clone(restoreProof):null,reasons=[];if(!proof)reasons.push('A complete application restore-proof object is required.');if(hash.sha256Value(manifest)!==hash.sha256Value(expectedManifest))reasons.push('The restore proof does not cover the exact complete canonical artifact-byte allowlist.');if(proof){const evidenceDigest=String(proof.restoreTestEvidenceSha256||''),evidencePayload={...proof};delete evidencePayload.restoreTestEvidenceSha256;if(!/^[a-f0-9]{64}$/i.test(evidenceDigest)||hash.sha256Value(evidencePayload)!==evidenceDigest)reasons.push('The restore-test evidence digest is missing or does not match its complete proof.');const binding={contract:'closed-loop-backup-restore-binding/1',milestone:subject.milestone,jobId:String(project.job?.JOB_ID||''),projectSubjectSha256:subject.subjectSha256,artifactManifestSha256:hash.sha256Value(manifest),artifactCount:manifest.length,artifactByteIdentities:manifest,status:'SUCCEEDED'};if(String(proof.contract||'')!=='closed-loop-backup-restore-test/1'||upper(proof.status)!=='SUCCEEDED'||upper(proof.artifactRehydration)!=='DECODED_REHASHED_AND_READ_BACK')reasons.push('The restore proof does not establish application byte decoding, rehashing, and read-back.');if(String(proof.importValidationContract||'')!=='SHARED_COMPLETE_IMPORT_VALIDATION'||String(proof.activationPath||'')!=='SHARED_IMPORT_ACTIVATION_TRANSACTION')reasons.push('The restore test did not use the complete production import validation and activation path.');if(!['ISOLATED_INDEXEDDB','ISOLATED_MEMORY_TEST_ADAPTER'].includes(String(proof.activationStore||''))||proof.isolatedDatabaseDistinctFromActive!==true||Number(proof.activeDatabaseWriteCount)!==0)reasons.push('The restore test was not isolated from the active project store.');if(Number(proof.restoredProjectRowCount)!==1||Number(proof.restoredArtifactRowCount)!==manifest.length||Number(proof.stagingRowCount)!==0)reasons.push('The restore test did not verify the exact restored project, artifact, and staging rows.');if(proof.temporaryLineageRemoved!==true||proof.isolatedDatabaseDeleted!==true)reasons.push('The restore test did not remove its isolated temporary lineage completely.');if(!/^[a-f0-9]{64}$/i.test(String(proof.activatedProjectSha256||'')))reasons.push('The activated isolated project row has no verified canonical digest.');if(String(proof.restoreBindingSha256||'')!==hash.sha256Value(binding)||String(proof.projectSubjectSha256||'')!==subject.subjectSha256||String(proof.artifactManifestSha256||'')!==binding.artifactManifestSha256||Number(proof.artifactCount)!==manifest.length||hash.sha256Value(normalizeBackupVerificationManifest(proof.artifactByteIdentities))!==hash.sha256Value(manifest))reasons.push('The restore proof is not bound to the current project subject and exact artifact identities.');if(String(proof.sourceProjectSha256||'')!==String(proof.restoredProjectSha256||''))reasons.push('The restored project digest does not match the exact source project digest.');if(checkSourceProject&&String(proof.sourceProjectSha256||'')!==projectBackupStateSha256(project))reasons.push('The restore proof was not generated from the exact current pre-checkpoint project bytes.');}return {valid:reasons.length===0,reasons,subject,manifest,proof};}
function normalizeBackupPackageBinding(value){const binding=value&&typeof value==='object'&&!Array.isArray(value)?value:{};return {schema:String(binding.schema||''),packageSha256:String(binding.packageSha256||'').toLowerCase(),packageByteSha256:String(binding.packageByteSha256||'').toLowerCase(),packageByteLength:Number(binding.packageByteLength),expandedByteLength:Number(binding.expandedByteLength)};}
function validateBackupPackageBinding(value){const binding=normalizeBackupPackageBinding(value),reasons=[];if(binding.schema!=='closed-loop-backup-package-binding/1')reasons.push('The backup package binding schema is not current.');if(!/^[a-f0-9]{64}$/.test(binding.packageSha256))reasons.push('The canonical package digest is missing or invalid.');if(!/^[a-f0-9]{64}$/.test(binding.packageByteSha256))reasons.push('The actual exported package-byte digest is missing or invalid.');if(!Number.isSafeInteger(binding.packageByteLength)||binding.packageByteLength<1)reasons.push('The actual exported package byte length is missing or invalid.');if(!Number.isSafeInteger(binding.expandedByteLength)||binding.expandedByteLength<1)reasons.push('The expanded package byte length is missing or invalid.');return {valid:reasons.length===0,reasons,binding,bindingSha256:reasons.length?null:hash.sha256Value(binding)};}
function recordPendingBackupExport(project,{milestone,verifiedArtifactManifest=[],restoreProof,packageBinding,disclosureHandling,humanAuthorizationId=null,encryptionContractId=null,createdAt=null}={}){ensureShape(project);const validated=validateBackupRestoreProof(project,{milestone,verifiedArtifactManifest,restoreProof,checkSourceProject:true}),bindingValidation=validateBackupPackageBinding(packageBinding),{subject,manifest,proof}=validated;if(!['BASELINE_APPROVAL','FINAL_DELIVERY','MANUAL'].includes(subject.milestone))throw new Error('Unknown backup checkpoint milestone.');if(!validated.valid)throw new Error('A successful exact application restore test is required before export: '+validated.reasons.join(' '));if(!bindingValidation.valid)throw new Error('The exact exported package bytes are not bound: '+bindingValidation.reasons.join(' '));const disclosure=evaluateBackupDisclosure(project,{humanAuthorizationId,encryptionContractId});if(!disclosure.allowed||upper(disclosureHandling)!==disclosure.handling)throw new Error(disclosure.reasons.join(' ')||'Backup disclosure handling does not match the current project.');const artifactManifestSha256=hash.sha256Value(manifest),pendingExportId=`BACKUP-EXPORT-${hash.sha256Value({milestone:subject.milestone,subjectSha256:subject.subjectSha256,packageBindingSha256:bindingValidation.bindingSha256,restoreTestEvidenceSha256:proof.restoreTestEvidenceSha256}).slice(0,24).toUpperCase()}`,existing=safe(project.projectData.pendingBackupExports).find(item=>item.pendingExportId===pendingExportId&&item.active!==false);if(existing)return existing;for(const prior of safe(project.projectData.pendingBackupExports).filter(item=>upper(item.milestone)===subject.milestone&&item.active!==false)){prior.active=false;prior.status='SUPERSEDED';prior.supersededBy=pendingExportId;}const timestamp=createdAt||now(),record={pendingExportId,milestone:subject.milestone,subjectSha256:subject.subjectSha256,artifactManifestSha256,artifactManifest:manifest,includedArtifactIds:manifest.map(item=>item.artifactId),requiredArtifactIds:subject.requiredArtifactIds,includesArtifactBytes:true,restoreTestStatus:'SUCCEEDED',restoreTestEvidenceSha256:String(proof.restoreTestEvidenceSha256),restoreBindingSha256:String(proof.restoreBindingSha256),restoreProof:proof,sourceProjectSha256:String(proof.sourceProjectSha256),packageBinding:bindingValidation.binding,packageBindingSha256:bindingValidation.bindingSha256,disclosureHandling:disclosure.handling,humanAuthorizationId:disclosure.humanAuthorizationId,encryptionContractId:null,externalCustodyAcknowledged:false,externalCustodyBasis:'NONE',active:true,status:'AWAITING_EXTERNAL_CUSTODY_ACKNOWLEDGMENT',createdAt:timestamp,source:'APPLICATION_EXACT_PACKAGE_EXPORT'};project.projectData.pendingBackupExports.push(record);addHistory(project,'BACKUP_PACKAGE_EXPORTED_AWAITING_CUSTODY',{pendingExportId,milestone:subject.milestone,subjectSha256:subject.subjectSha256,packageByteSha256:bindingValidation.binding.packageByteSha256,packageByteLength:bindingValidation.binding.packageByteLength,externalCustodyAcknowledged:false});return record;}
function recordBackupCheckpoint(project,{milestone,verifiedArtifactManifest=[],restoreProof,packageBinding,externalCustodyAcknowledgment,pendingExportId,disclosureHandling,humanAuthorizationId=null,encryptionContractId=null,createdAt=null}={}){ensureShape(project);const subject=backupCheckpointSubject(project,milestone),pending=safe(project.projectData.pendingBackupExports).find(item=>item.active!==false&&String(item.pendingExportId||'')===String(pendingExportId||'')),validated=validateBackupRestoreProof(project,{milestone,verifiedArtifactManifest,restoreProof,checkSourceProject:false}),bindingValidation=validateBackupPackageBinding(packageBinding),manifest=validated.manifest,proof=validated.proof,ack=externalCustodyAcknowledgment&&typeof externalCustodyAcknowledgment==='object'&&!Array.isArray(externalCustodyAcknowledgment)?externalCustodyAcknowledgment:null;if(!['BASELINE_APPROVAL','FINAL_DELIVERY','MANUAL'].includes(subject.milestone))throw new Error('Unknown backup checkpoint milestone.');if(!pending||upper(pending.milestone)!==subject.milestone||String(pending.subjectSha256||'')!==subject.subjectSha256)throw new Error('A current exact exported package is required before backup custody can be acknowledged.');if(!validated.valid||String(proof?.sourceProjectSha256||'')!==String(pending.sourceProjectSha256||''))throw new Error('A successful restore test for the exact exported package is required: '+[...validated.reasons,'The restore proof source project does not match the exported package.'].filter((reason,index,array)=>index<validated.reasons.length||String(proof?.sourceProjectSha256||'')!==String(pending.sourceProjectSha256||'')).join(' '));if(!bindingValidation.valid||bindingValidation.bindingSha256!==String(pending.packageBindingSha256||'')||hash.sha256Value(bindingValidation.binding)!==hash.sha256Value(pending.packageBinding||{}))throw new Error('The custody acknowledgment does not bind the exact exported package bytes.');if(!ack||ack.acknowledged!==true||String(ack.pendingExportId||'')!==String(pending.pendingExportId)||String(ack.packageByteSha256||'').toLowerCase()!==bindingValidation.binding.packageByteSha256||String(ack.identityAssurance||'')!=='SELF_ASSERTED'||!String(ack.humanDecisionId||''))throw new Error('Explicit operator acknowledgment that the exact package was stored outside this browser is required. Download initiation is not proof of external custody.');const disclosure=evaluateBackupDisclosure(project,{humanAuthorizationId,encryptionContractId});if(!disclosure.allowed||upper(disclosureHandling)!==disclosure.handling)throw new Error(disclosure.reasons.join(' ')||'Backup disclosure handling does not match the current project.');const artifactManifestSha256=hash.sha256Value(manifest),restoreTestEvidenceSha256=String(proof.restoreTestEvidenceSha256),checkpointId=`BACKUP-CHECKPOINT-${hash.sha256Value({milestone:subject.milestone,subjectSha256:subject.subjectSha256,artifactManifestSha256,restoreTestEvidenceSha256,packageBindingSha256:bindingValidation.bindingSha256,humanDecisionId:ack.humanDecisionId}).slice(0,24).toUpperCase()}`,existing=safe(project.projectData.externalCheckpoints).find(item=>item.checkpointId===checkpointId&&item.active!==false);if(existing)return existing;for(const prior of safe(project.projectData.externalCheckpoints).filter(item=>upper(item.milestone)===subject.milestone&&item.active!==false)){prior.active=false;prior.status='SUPERSEDED';prior.supersededBy=checkpointId;}const timestamp=createdAt||now(),record={checkpointId,milestone:subject.milestone,subjectSha256:subject.subjectSha256,artifactManifestSha256,artifactManifest:manifest,includedArtifactIds:manifest.map(item=>item.artifactId),requiredArtifactIds:subject.requiredArtifactIds,includesArtifactBytes:true,restoreTestStatus:'SUCCEEDED',restoreTestEvidenceSha256,restoreBindingSha256:String(proof.restoreBindingSha256),restoreProof:proof,sourceProjectSha256:String(proof.sourceProjectSha256),packageBinding:bindingValidation.binding,packageBindingSha256:bindingValidation.bindingSha256,pendingExportId:String(pending.pendingExportId),externalCustodyAcknowledged:true,externalCustodyBasis:'SELF_ASSERTED_OPERATOR_ACKNOWLEDGMENT',externalCustodyHumanDecisionId:String(ack.humanDecisionId),externalCustodyAcknowledgedAt:String(ack.acknowledgedAt||timestamp),disclosureHandling:disclosure.handling,humanAuthorizationId:disclosure.humanAuthorizationId,encryptionContractId:null,active:true,status:'CURRENT',createdAt:timestamp,source:'APPLICATION_BACKUP_RESTORE_AND_CUSTODY_CLOSURE'};project.projectData.externalCheckpoints.push(record);pending.active=false;pending.status='CUSTODY_ACKNOWLEDGED';pending.checkpointId=checkpointId;const policy={policyId:'BACKUP-POLICY-'+hash.sha256Value(String(project.job?.JOB_ID||'')).slice(0,20).toUpperCase(),requiredCheckpointEvents:['BASELINE_APPROVAL','FINAL_DELIVERY'],artifactBytesRequired:true,authoritativeRestoreRule:'RESTORE_SAME_LINEAGE_OR_CLONE_WITH_NEW_IDENTITIES',mostRecentSuccessfulBackupIdentity:checkpointId,restoreTestStatus:'SUCCEEDED',status:'CURRENT'};project.projectData.backupPolicies=[policy];addHistory(project,'BACKUP_CHECKPOINT_VERIFIED',{checkpointId,milestone:subject.milestone,subjectSha256:subject.subjectSha256,artifactManifestSha256,restoreTestEvidenceSha256,restoreBindingSha256:String(proof.restoreBindingSha256),packageByteSha256:bindingValidation.binding.packageByteSha256,packageByteLength:bindingValidation.binding.packageByteLength,externalCustodyAcknowledged:true,externalCustodyBasis:record.externalCustodyBasis,humanDecisionId:ack.humanDecisionId,disclosureHandling:disclosure.handling});return record;}
function confirmBackupExternalCustody(project,{pendingExportId,operatorLabel='HUMAN_OPERATOR',acknowledged=false,createdAt=null}={}){ensureShape(project);if(acknowledged!==true)throw new Error('Confirm that the exact exported package was stored outside this browser.');const pending=safe(project.projectData.pendingBackupExports).find(item=>item.active!==false&&String(item.pendingExportId||'')===String(pendingExportId||''));if(!pending)throw new Error('The pending exact backup export is missing or stale.');const timestamp=createdAt||now(),humanDecisionId=`BACKUP-CUSTODY-${hash.sha256Value({pendingExportId:pending.pendingExportId,packageByteSha256:pending.packageBinding?.packageByteSha256,operatorLabel:String(operatorLabel||'HUMAN_OPERATOR')}).slice(0,24).toUpperCase()}`,existingDecision=safe(project.projectData.humanDecisions).find(item=>String(item.decisionId||'')===humanDecisionId),decision=existingDecision||{decisionId:humanDecisionId,type:'BACKUP_EXTERNAL_CUSTODY_ACKNOWLEDGMENT',field:'BACKUP_EXTERNAL_CUSTODY_ACKNOWLEDGMENT',value:true,acknowledged:true,pendingExportId:pending.pendingExportId,packageByteSha256:pending.packageBinding.packageByteSha256,packageByteLength:pending.packageBinding.packageByteLength,operatorLabel:String(operatorLabel||'HUMAN_OPERATOR'),identityAssurance:'SELF_ASSERTED',stage:Number(project.activeStage||0),createdAt:timestamp,source:'HUMAN_DECISION'};if(!existingDecision)project.projectData.humanDecisions.push(decision);const acknowledgment={acknowledged:true,pendingExportId:pending.pendingExportId,packageByteSha256:pending.packageBinding.packageByteSha256,packageByteLength:pending.packageBinding.packageByteLength,humanDecisionId,operatorLabel:decision.operatorLabel,identityAssurance:'SELF_ASSERTED',acknowledgedAt:timestamp};return recordBackupCheckpoint(project,{milestone:pending.milestone,verifiedArtifactManifest:pending.artifactManifest,restoreProof:pending.restoreProof,packageBinding:pending.packageBinding,externalCustodyAcknowledgment:acknowledgment,pendingExportId:pending.pendingExportId,disclosureHandling:pending.disclosureHandling,humanAuthorizationId:pending.humanAuthorizationId,encryptionContractId:null,createdAt:timestamp});}
function backupCheckpointStatus(project,milestone){ensureShape(project);const subject=backupCheckpointSubject(project,milestone),checkpoint=safe(project.projectData.externalCheckpoints).filter(item=>item.active!==false&&upper(item.milestone)===subject.milestone).at(-1),pending=safe(project.projectData.pendingBackupExports).filter(item=>item.active!==false&&upper(item.milestone)===subject.milestone&&String(item.subjectSha256||'')===subject.subjectSha256).at(-1),reasons=[];if(!checkpoint)reasons.push(pending?`${subject.milestone} exact package was generated, but external custody has not been acknowledged.`:`${subject.milestone} requires a current complete backup checkpoint.`);else{if(String(checkpoint.subjectSha256||'')!==subject.subjectSha256)reasons.push(`${subject.milestone} backup checkpoint is stale for current release-bearing state.`);const proof=validateBackupRestoreProof(project,{milestone:subject.milestone,verifiedArtifactManifest:checkpoint.artifactManifest,restoreProof:checkpoint.restoreProof,checkSourceProject:false}),binding=validateBackupPackageBinding(checkpoint.packageBinding);if(!proof.valid)reasons.push(...proof.reasons.map(reason=>`${subject.milestone} backup restore receipt invalid: ${reason}`));if(!binding.valid||binding.bindingSha256!==String(checkpoint.packageBindingSha256||''))reasons.push(`${subject.milestone} backup checkpoint does not bind the exact exported package bytes.`);if(upper(checkpoint.restoreTestStatus)!=='SUCCEEDED'||String(checkpoint.restoreTestEvidenceSha256||'')!==String(checkpoint.restoreProof?.restoreTestEvidenceSha256||''))reasons.push(`${subject.milestone} backup checkpoint lacks its exact successful restore receipt.`);if(checkpoint.includesArtifactBytes!==true)reasons.push(`${subject.milestone} backup checkpoint does not include artifact bytes.`);if(checkpoint.externalCustodyAcknowledged!==true||upper(checkpoint.externalCustodyBasis)!=='SELF_ASSERTED_OPERATOR_ACKNOWLEDGMENT'||!String(checkpoint.externalCustodyHumanDecisionId||''))reasons.push(`${subject.milestone} backup has no explicit operator acknowledgment of external custody; download initiation is not proof of persistence.`);const included=[...new Set(safe(checkpoint.includedArtifactIds).map(String))].sort(),expected=subject.artifactManifest.map(item=>item.artifactId).sort();if(hash.sha256Value(included)!==hash.sha256Value(expected))reasons.push(`${subject.milestone} backup checkpoint does not cover the exact complete canonical artifact allowlist.`);for(const id of subject.requiredArtifactIds)if(!included.includes(id))reasons.push(`${subject.milestone} backup checkpoint omits required artifact ${id}.`);const disclosure=evaluateBackupDisclosure(project,{humanAuthorizationId:checkpoint.humanAuthorizationId,encryptionContractId:checkpoint.encryptionContractId});if(!disclosure.allowed||upper(checkpoint.disclosureHandling)!==disclosure.handling)reasons.push(`${subject.milestone} backup checkpoint disclosure authorization is missing or stale.`);}return {complete:reasons.length===0,reasons,checkpoint:reasons.length?null:checkpoint,checkpointId:reasons.length?null:String(checkpoint?.checkpointId||''),subjectSha256:subject.subjectSha256,requiredArtifactIds:subject.requiredArtifactIds,custodyPending:Boolean(pending&&!checkpoint),pendingExportId:pending?.pendingExportId||null,pendingExport:pending||null};}
function evaluateRegistryIntegrity(project){ensureShape(project);const defects=confirmedDefects(project,{allScopes:true}),regressions=records(project,'regressions',{active:false}),executions=records(project,'regressionExecutions',{active:false}),reasons=[],covered=new Set();for(const regression of regressions){const defectId=String(recordValue(regression,'DEFECT_ID')||regression.relationships?.DEFECT_ID||'');if(defectId)covered.add(defectId);const lifecycle=upper(recordValue(regression,'RETIREMENT_STATUS')||recordValue(regression,'ACTIVE_RETIRED_STATE')||'ACTIVE');if(lifecycle==='RETIRED'){const authority=String(recordValue(regression,'RETIREMENT_AUTHORITY_AND_EVIDENCE')||recordValue(regression,'RETIREMENT_EVIDENCE')||'').trim(),review=String(recordValue(regression,'RETIREMENT_REVIEW_ID')||regression.relationships?.RETIREMENT_REVIEW_ID||'').trim();if(!authority||!review)reasons.push('Retired regression '+recordId(regression,'regressions')+' lacks retirement authority, evidence, or independent review.');continue;}if(!isActiveRecord(regression))continue;const id=recordId(regression,'regressions'),current=executions.filter(item=>isActiveRecord(item)&&String(recordValue(item,'REG_ID')||item.relationships?.REG_ID||'')===id&&upper(recordValue(item,'PHASE'))!=='PRE_CORRECTION');if(current.length!==1||effectiveRegressionDetermination(project,current[0]).determination!=='SATISFIED')reasons.push('Active applicable regression '+id+' lacks exactly one current sufficient successful execution.');}for(const defect of defects)if(!covered.has(recordId(defect,'defects')))reasons.push('Confirmed defect '+recordId(defect,'defects')+' has no permanent regression record.');const registryIntegrityHash=hash.sha256Value({defects:records(project,'defects',{active:false}).map(item=>item.recordSha256||item.sha256||recordId(item,'defects')),regressions:regressions.map(item=>item.recordSha256||item.sha256||recordId(item,'regressions')),executions:executions.map(item=>item.recordSha256||item.sha256||recordId(item,'regressionExecutions'))});return {complete:reasons.length===0,reasons,registryIntegrityHash,defectRegistryVersion:'DEFECT-REGISTRY-'+registryIntegrityHash.slice(0,16).toUpperCase(),regressionRegistryVersion:'REGRESSION-REGISTRY-'+hash.sha256Value(regressions.map(item=>item.recordSha256||item.sha256||recordId(item,'regressions'))).slice(0,16).toUpperCase()};}
function terminalDeliveryState(project,{forCreation=false,skipContradictions=false}={}){ensureShape(project);const reasons=[],release=recordsForCurrentScope(project,'releaseRecords').at(-1),releaseAccepted=upper(recordValue(release,'DETERMINATION'))==='ACCEPTED',product=recordsForCurrentScope(project,'products').at(-1),baseline=recordsForCurrentScope(project,'baselines').at(-1),identities=recordsForCurrentScope(project,'artifactIdentities'),chains=recordsForCurrentScope(project,'evidenceChains'),registry=evaluateRegistryIntegrity(project),obligations=deriveProofObligationRegistry(project),propositionStates=obligations.obligations.map(obligation=>{const propositionId=relationshipIds(obligation,'PROPOSITION_ID')[0],proposition=recordsForCurrentScope(project,'propositions').find(item=>recordId(item,'propositions')===propositionId);return evaluatePropositionState(project,{proposition,proofObligation:obligation});}),canonicalArtifacts=new Map(recordsForCurrentScope(project,'artifacts').map(item=>[recordId(item,'artifacts'),item])),humanIntent=Boolean(project.release?.humanDeliveryIntent||truth(project.stages?.[28]?.humanData?.HUMAN_DELIVERY_INTENT)||truth(project.stages?.[28]?.humanData?.DELIVERY_AUTHORIZATION)),activeReservations=records(project,'operationReservations',{active:false}).filter(item=>ACTIVE_RESERVATION_STATES.includes(upper(recordValue(item,'STATUS')||item.status))),cycles=detectEvidenceJustificationCycles(project);if(!releaseAccepted)reasons.push('Stage 27 PRODUCT_RELEASE_ELIGIBLE determination is not current and ACCEPTED.');if(!product||!baseline)reasons.push('Current product and baseline identities are required.');if(!identities.length||identities.some(item=>!truth(recordValue(item,'DELIVERY_ARTIFACT_IDENTITY_VERIFIED'))||!truth(recordValue(item,'EXACT_HASH_MATCH'))||!truth(recordValue(item,'EXACT_SIZE_MATCH'))))reasons.push('Stage 28 delivery artifact identity is incomplete or inexact.');for(const identity of identities){const artifactId=String(recordValue(identity,'ARTIFACT_ID')||identity.relationships?.ARTIFACT_ID||''),artifact=canonicalArtifacts.get(artifactId);if(!artifact||upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||String(recordValue(artifact,'SHA256')||'').toLowerCase()!==String(recordValue(identity,'AUDITED_SHA256')||'').toLowerCase())reasons.push('Authorized artifact '+(artifactId||'UNKNOWN')+' bytes are unavailable, stale, or not reverified.');}const expectedChains=obligations.obligations.length||mandatoryRequirements(project).length;if(!chains.length||chains.length<expectedChains||chains.some(item=>upper(recordValue(item,'STATUS'))!=='COMPLETE'||safe(recordValue(item,'MISSING_LINKS')).length))reasons.push('Stage 29 mandatory evidence-chain closure is incomplete.');if(cycles.length)reasons.push('Current evidence justification graph contains a cycle.');if(recordsForCurrentScope(project,'propositions').length){if(!obligations.complete)reasons.push(...obligations.reasons);for(const state of propositionStates)if(state.truthValue!=='TRUE'||state.contradictionStatus!=='CLEAR'||['EXPIRED','UNKNOWN'].includes(state.freshnessStatus))reasons.push('Mandatory proposition '+state.propositionId+' is not currently proven.');}for(const dependency of recordsForCurrentScope(project,'environmentDependencies')){const state=evaluateEnvironmentDependency(project,dependency);if(state.truthValue!=='TRUE')reasons.push('Environment dependency '+state.dependencyId+' is not current and established.');}if(!registry.complete)reasons.push(...registry.reasons);const registryStageReady=truth(project.stages?.[30]?.derivedData?.REGISTRY_INTEGRITY_STATUS)||upper(project.stages?.[30]?.derivedData?.REGISTRY_INTEGRITY_STATUS)==='COMPLETE'||acceptedChanges(project,30).length>0;if(!registryStageReady)reasons.push('Stage 30 registry integrity has not been completed through the current workflow operation.');if(!humanIntent)reasons.push('Current human delivery-selection intent is missing.');if(openBlockers(project).length)reasons.push('A current blocker prevents terminal delivery.');if(activeReservations.length)reasons.push('An active or orphaned operation reservation remains unresolved.');if(!skipContradictions){const contradictions=detectCurrentContradictions(project).filter(item=>item.type!=='STALE_DELIVERY_AUTHORIZATION');if(contradictions.length)reasons.push('Current release-material contradictions remain unresolved.');}const terminalEvidenceHash=hash.sha256Value({releaseId:recordId(release,'releaseRecords'),productId:recordId(product,'products'),baselineId:recordId(baseline,'baselines'),identityIds:identities.map(item=>recordId(item,'artifactIdentities')).sort(),chainHashes:chains.map(item=>item.recordSha256||item.sha256||recordId(item,'evidenceChains')).sort(),registryIntegrityHash:registry.registryIntegrityHash,proofObligationSetHash:hash.sha256Value(obligations.obligations.map(item=>item.recordSha256||item.sha256||recordId(item,'proofObligations')).sort()),humanDeliveryAuthorizationId:project.release?.humanDeliveryAuthorizationId||null}),currentDelivery=recordsForCurrentScope(project,'deliveryRecords').find(item=>upper(recordValue(item,'DELIVERY_STATE'))==='AUTHORIZED'&&String(recordValue(item,'TERMINAL_EVIDENCE_HASH')||'')===terminalEvidenceHash),prerequisitesSatisfied=reasons.length===0,authorized=Boolean(currentDelivery&&prerequisitesSatisfied);project.release.deliveryAuthorizationEffective=authorized;project.release.authorization=authorized?'AUTHORIZED':'NOT AUTHORIZED';project.release.deliveryState=authorized?'AUTHORIZED':currentDelivery?'WITHDRAWN_FOR_FUTURE_USE':'BLOCKED';return {deliveryState:authorized?'AUTHORIZED':'BLOCKED',authorized,eligibleToCreate:forCreation&&prerequisitesSatisfied,prerequisitesSatisfied,reasons:[...new Set(reasons)],releaseId:recordId(release,'releaseRecords')||null,productId:recordId(product,'products')||null,baselineId:recordId(baseline,'baselines')||null,artifactIdentityIds:identities.map(item=>recordId(item,'artifactIdentities')),artifactIds:identities.map(item=>String(recordValue(item,'ARTIFACT_ID')||item.relationships?.ARTIFACT_ID||'')),evidenceChainSetSha256:hash.sha256Value(chains.map(item=>item.recordSha256||item.sha256||recordId(item,'evidenceChains')).sort()),terminalProofObligationSetHash:hash.sha256Value(obligations.obligations.map(item=>item.recordSha256||item.sha256||recordId(item,'proofObligations')).sort()),terminalEvidenceHash,registry};}
const terminalDeliveryStateBeforeBackupRequirement=terminalDeliveryState;
terminalDeliveryState=function terminalDeliveryStateWithBackupRequirement(project,options={}){const base=terminalDeliveryStateBeforeBackupRequirement(project,options),currentDelivery=recordsForCurrentScope(project,'deliveryRecords').find(item=>upper(recordValue(item,'DELIVERY_STATE'))==='AUTHORIZED'&&String(recordValue(item,'TERMINAL_EVIDENCE_HASH')||'')===base.terminalEvidenceHash),backup=currentDelivery?backupCheckpointStatus(project,'FINAL_DELIVERY'):{complete:false,reasons:['The final backup is created only after the current DELIVERY_RECORD exists.'],checkpointId:null,subjectSha256:null,custodyPending:false,pendingExportId:null},reasons=[...new Set([...base.reasons,...(currentDelivery?backup.reasons:[])])],prerequisitesSatisfied=base.prerequisitesSatisfied&&Boolean(currentDelivery)&&backup.complete,authorized=Boolean(currentDelivery&&prerequisitesSatisfied),terminalClosureSha256=authorized?hash.sha256Value({terminalEvidenceHash:base.terminalEvidenceHash,deliveryRecordHash:currentDelivery.recordSha256||currentDelivery.sha256||recordId(currentDelivery,'deliveryRecords'),backupCheckpointId:backup.checkpointId,backupSubjectSha256:backup.subjectSha256,packageBindingSha256:backup.checkpoint?.packageBindingSha256||null}):null;project.release.deliveryAuthorizationEffective=authorized;project.release.authorization=authorized?'AUTHORIZED':'NOT AUTHORIZED';project.release.deliveryState=authorized?'AUTHORIZED':currentDelivery?'BLOCKED_PENDING_FINAL_BACKUP_CLOSURE':'BLOCKED';return {...base,currentDelivery,deliveryState:authorized?'AUTHORIZED':'BLOCKED',authorized,eligibleToCreate:Boolean(options?.forCreation&&!currentDelivery&&base.prerequisitesSatisfied),prerequisitesSatisfied,reasons,terminalEvidenceHash:base.terminalEvidenceHash,terminalClosureSha256,backupCheckpointId:backup.checkpointId,backupCheckpointStatus:backup};};
function recordDelivery(project,{expectedRevision=projectRevision(project),committedRevision=Number(expectedRevision)+1,artifactReadBackProof=null,commandId=null,idempotencyKey=null}={}){
  ensureShape(project);
  if(!collectionAvailable('deliveryRecords'))throw new Error('The DELIVERY_RECORD schema is unavailable.');
  const state=terminalDeliveryState(project,{forCreation:true});
  if(!state.eligibleToCreate)throw new Error('Final delivery is blocked: '+state.reasons.join(' '));
  const existing=recordsForCurrentScope(project,'deliveryRecords').find(item=>String(recordValue(item,'TERMINAL_EVIDENCE_HASH')||'')===state.terminalEvidenceHash&&upper(recordValue(item,'DELIVERY_STATE'))==='AUTHORIZED');if(existing&&Number(expectedRevision)===projectRevision(project))return existing;
  const artifacts=new Map(recordsForCurrentScope(project,'artifacts').map(item=>[recordId(item,'artifacts'),item])),payload={terminalEvidenceHash:state.terminalEvidenceHash,expectedRevision:Number(expectedRevision),artifactIds:[...state.artifactIds].sort(),artifactReadBackProof:artifactReadBackProof?clone(artifactReadBackProof):null};
  const outcome=executeIdempotentCommand(project,{commandType:'CREATE_DELIVERY_RECORD',target:String(project.job?.JOB_ID||''),expectedRevision,committedRevision,scope:currentScope(project),payload,commandId,idempotencyKey,mutate:()=>{
    for(const prior of recordsForCurrentScope(project,'deliveryRecords')){prior.active=false;prior.validity='SUPERSEDED';prior.fields={...recordFields(prior),DELIVERY_STATE:'SUPERSEDED'};prior.DELIVERY_STATE='SUPERSEDED';refreshRecordHashes(prior,'deliveryRecords');}
    const fields={JOB_ID:String(project.job?.JOB_ID||''),PRODUCT_ID:state.productId,PRODUCT_VERSION:String(recordValue(recordsForCurrentScope(project,'products').at(-1),'PRODUCT_VERSION')||''),BASELINE_ID:state.baselineId,RELEASE_ID:state.releaseId,HASH_REVIEW_ID:state.artifactIdentityIds.join(','),EVIDENCE_CHAIN_VERSION:state.evidenceChainSetSha256,EVIDENCE_CHAIN_SET_SHA256:state.evidenceChainSetSha256,DEFECT_REGISTRY_VERSION:state.registry.defectRegistryVersion,REGRESSION_REGISTRY_VERSION:state.registry.regressionRegistryVersion,REGISTRY_INTEGRITY_HASH:state.registry.registryIntegrityHash,HUMAN_DELIVERY_AUTHORIZATION_ID:String(project.release?.humanDeliveryAuthorizationId||''),HUMAN_IDENTITY_ASSURANCE:'SELF_ASSERTED',AUTHORIZED_ARTIFACT_IDS:state.artifactIds,AUTHORIZED_FILENAMES:state.artifactIds.map(id=>String(recordValue(artifacts.get(id),'FILENAME')||'')),BYTE_SIZES:Object.fromEntries(state.artifactIds.map(id=>[id,Number(recordValue(artifacts.get(id),'BYTE_SIZE'))])),HASH_ALGORITHMS:state.artifactIds.map(()=>'SHA-256'),DIGESTS:state.artifactIds.map(id=>String(recordValue(artifacts.get(id),'SHA256')||'')),EXPECTED_PRECONDITION_REVISION:Number(expectedRevision),COMMITTED_PROJECT_REVISION:projectRevision(project),TERMINAL_PROOF_OBLIGATION_SET_HASH:state.terminalProofObligationSetHash,TERMINAL_EVIDENCE_HASH:state.terminalEvidenceHash,DELIVERY_STATE:'AUTHORIZED',CONTROLLING_REASON:'Every current terminal prerequisite is simultaneously satisfied and bound to the exact authorized bytes.',RECEIPT_ID:'PENDING',EVENT_SEQUENCE:0};
    return commandRecord(project,'deliveryRecords',fields,{stage:30,source:'APPLICATION_DERIVATION'});
  }});
  if(artifactReadBackProof){outcome.receipt.artifactReadBackProof=clone(artifactReadBackProof);outcome.receipt.artifactReadBackProofSha256=hash.sha256Value(artifactReadBackProof);}
  const resultId=String(outcome.result?.id||outcome.result?.recordId||''),record=records(project,'deliveryRecords',{active:false}).find(item=>recordId(item,'deliveryRecords')===resultId)||outcome.result;
  record.fields={...recordFields(record),RECEIPT_ID:outcome.receipt.receiptId,EVENT_SEQUENCE:Number(project.projectData.history.at(-1)?.eventSequence||0),EXPECTED_PRECONDITION_REVISION:Number(outcome.receipt.expectedRevision),COMMITTED_PROJECT_REVISION:Number(outcome.receipt.committedRevision)};
  Object.assign(record,record.fields);
  const digestPayload={...record.fields};delete digestPayload.DELIVERY_RECORD_HASH;const digest=hash.sha256Value(digestPayload);record.fields.DELIVERY_RECORD_HASH=digest;record.DELIVERY_RECORD_HASH=digest;refreshRecordHashes(record,'deliveryRecords');
  project.release.deliveryAuthorizationEffective=true;project.release.authorization='AUTHORIZED';project.release.deliveryState='AUTHORIZED';project.release.deliveryId=recordId(record,'deliveryRecords');
  recalculate(project);return record;
}

function refreshRecordHashes(record,collection){
  if(!record)return record;const definition=schema.RECORD_SCHEMAS[collection];if(!definition)return record;record.contentSha256=hash.contentRecordSha256(record,definition.idField);record.recordSha256=hash.recordSha256(record);record.sha256=record.recordSha256;return record;
}
function commandRecord(project,collection,fields,{stage=null,source='APPLICATION_COMMAND',scope=null}={}){
  ensureShape(project);const definition=schema.RECORD_SCHEMAS[collection];if(!definition)throw new Error(`Unknown canonical collection: ${collection}.`);
  const id=allocateId(project,collection),deviceTimestamp=now(),full={...clone(fields),[definition.idField]:id};
  const record={id,stage:(stage??Number(project.activeStage||0))||null,createdAt:deviceTimestamp,updatedAt:deviceTimestamp,active:true,scope:scope||currentScope(project),fields:full,...full,source};
  refreshRecordHashes(record,collection);
  project.projectData[collection].push(record);return record;
}
function projectRevision(project){return Number(project?.revision??project?.projectRevision??0);}
function cryptographicNonce(byteLength=16){if(!globalThis.crypto?.getRandomValues)throw new Error('A cryptographically secure random generator is required for operation reservations.');const bytes=new Uint8Array(byteLength);globalThis.crypto.getRandomValues(bytes);return [...bytes].map(value=>value.toString(16).padStart(2,'0')).join('');}
function executeIdempotentCommand(project,{commandType,target='',expectedRevision=projectRevision(project),committedRevision=Number(expectedRevision)+1,scope=currentScope(project),payload=null,idempotencyKey=null,commandId=null,mutate=null}={},mutationFunction=null){
  ensureShape(project);const execute=mutate||mutationFunction;if(typeof execute!=='function')throw new Error('An idempotent command requires a mutation function.');const normalizedType=String(commandType||''),normalizedTarget=String(target||''),normalizedExpectedRevision=Number(expectedRevision),normalizedCommittedRevision=Number(committedRevision),normalizedPayloadHash=hash.sha256Value(payload??null),deterministicKey=hash.sha256Value({commandType:normalizedType,target:normalizedTarget,expectedRevision:normalizedExpectedRevision,scope:clone(scope||{}),normalizedPayloadHash}),key=String(idempotencyKey||deterministicKey),requestedCommandId=commandId===null||commandId===undefined?'':String(commandId),receipts=safe(project.projectData.commandReceipts),byKey=receipts.filter(receipt=>String(receipt.idempotencyKey||'')===key),byCommand=requestedCommandId?receipts.filter(receipt=>String(receipt.commandId||'')===requestedCommandId):[];
  if(byKey.length>1||byCommand.length>1)throw new Error('Canonical command receipts contain duplicate command or idempotency identities.');const existing=byKey[0]||byCommand[0]||null;if(existing){if(String(existing.idempotencyKey||'')!==key||(requestedCommandId&&String(existing.commandId||'')!==requestedCommandId)||String(existing.normalizedPayloadHash||'')!==normalizedPayloadHash||String(existing.commandType||'')!==normalizedType||String(existing.target||'')!==normalizedTarget||Number(existing.expectedRevision)!==normalizedExpectedRevision||Number(existing.committedRevision)!==normalizedExpectedRevision+1||String(existing.scopeHash||'')!==hash.sha256Value(scope||{}))throw new Error('Command or idempotency identity was reused with different command content or an invalid committed revision.');return {result:clone(existing.resultSnapshot),receipt:existing,replayed:true};}
  if(normalizedExpectedRevision!==projectRevision(project))throw new Error('Command expectedRevision is stale.');if(!Number.isSafeInteger(normalizedCommittedRevision)||normalizedCommittedRevision!==normalizedExpectedRevision+1)throw new Error('Command committedRevision must equal the exact next canonical CAS revision.');const id=String(commandId||allocateInfrastructureId(project,'COMMAND','commandReceipts')),result=execute(),receipt={receiptId:allocateInfrastructureId(project,'COMMAND-RECEIPT','commandReceipts'),commandId:id,commandType:normalizedType,target:normalizedTarget,expectedRevision:normalizedExpectedRevision,committedRevision:normalizedCommittedRevision,scope:clone(scope||{}),scopeHash:hash.sha256Value(scope||{}),normalizedPayloadHash,idempotencyKey:key,resultIdentity:String(result?.id||result?.recordId||result?.reservationId||''),resultSnapshot:clone(result),deviceTimestamp:now(),status:'COMMITTED'};project.projectData.commandReceipts.push(receipt);addHistory(project,'IDEMPOTENT_COMMAND_COMMITTED',{commandId:id,commandType:receipt.commandType,target:receipt.target,idempotencyKey:key,expectedRevision:normalizedExpectedRevision,committedRevision:normalizedCommittedRevision,resultIdentity:receipt.resultIdentity});return {result,receipt,replayed:false};
}
function reservationTargetKey({stage,operation,targetSlot,scope}={}){const stableScope=clone(scope||{});delete stableScope.projectRevision;return hash.sha256Value({stage:Number(stage),operation:upper(operation||'COMPLETE'),targetSlot:String(targetSlot||'DEFAULT'),scope:stableScope});}
const EXECUTION_PACKAGE_DESCRIPTOR_KEYS=Object.freeze(['schema','jobId','stage','operation','targetSlot','scopeSha256','testIds','artifacts','filesToWithhold','expectedReturnFiles','disclosureAuthorization','runId','productId']);
function normalizeFrozenArtifactInputs(items){
  if(!Array.isArray(items))throw new Error('A frozen execution-package descriptor requires an artifacts array.');
  const normalized=items.map((item,index)=>{if(!item||typeof item!=='object'||Array.isArray(item))throw new Error(`Frozen package artifact input ${index} must be an object.`);const artifactId=String(item.artifactId||'').trim(),filename=String(item.filename||''),byteSize=Number(item.byteSize),sha256=String(item.sha256||'').toLowerCase(),availability=upper(item.availability||''),role=String(item.role||''),disclosureClassification=upper(item.disclosureClassification||'UNKNOWN');if(!artifactId||!filename||!Number.isSafeInteger(byteSize)||byteSize<0||!/^[a-f0-9]{64}$/.test(sha256))throw new Error(`Frozen package artifact input ${artifactId||index} lacks an exact identity, filename, safe byte size, or SHA-256.`);if(availability!=='BYTES_PERSISTED_AND_VERIFIED')throw new Error(`Frozen package artifact input ${artifactId} is not application-verified and available.`);return {artifactId,filename,byteSize,sha256,role,availability,disclosureClassification};}).sort((a,b)=>a.artifactId<b.artifactId?-1:a.artifactId>b.artifactId?1:0);
  if(new Set(normalized.map(item=>item.artifactId)).size!==normalized.length)throw new Error('A frozen execution-package descriptor contains duplicate artifact identities.');
  return normalized;
}
function validateReservationPackageDescriptor(project,{descriptor,packageId,packageManifestHash,stage,operation,targetSlot,scope}={}){
  if(descriptor===null||descriptor===undefined)return {descriptor:null,artifactInputs:[],artifactInputSetSha256:hash.sha256Value([])};
  if(!descriptor||typeof descriptor!=='object'||Array.isArray(descriptor))throw new Error('An operation reservation package descriptor must be an object.');
  const unknown=Object.keys(descriptor).filter(key=>!EXECUTION_PACKAGE_DESCRIPTOR_KEYS.includes(key));if(unknown.length)throw new Error('Operation reservation package descriptor contains unknown fields: '+unknown.join(', ')+'.');
  const copy=clone(descriptor),manifestHash=String(packageManifestHash||'').toLowerCase(),expectedPackageId=`PACKAGE-${manifestHash.slice(0,24).toUpperCase()}`;
  if(copy.schema!=='closed-loop-execution-package-descriptor/1'||String(copy.jobId||'')!==String(project.job?.JOB_ID||'')||Number(copy.stage)!==Number(stage)||upper(copy.operation||'COMPLETE')!==upper(operation||'COMPLETE')||String(copy.targetSlot||'')!==String(targetSlot||'')||String(copy.scopeSha256||'')!==hash.sha256Value(scope||{}))throw new Error('The frozen package descriptor does not match the exact job, stage, operation, target slot, or scope.');
  if(!Array.isArray(copy.testIds)||new Set(copy.testIds.map(String)).size!==copy.testIds.length||!Array.isArray(copy.filesToWithhold)||!Array.isArray(copy.expectedReturnFiles))throw new Error('The frozen package descriptor has an invalid test, withheld-file, or expected-return set.');
  const descriptorInputs=normalizeFrozenArtifactInputs(copy.artifacts),currentArtifacts=new Map(records(project,'artifacts').filter(isActiveRecord).map(item=>[recordId(item,'artifacts'),item])),artifactInputs=descriptorInputs.map(input=>{const artifact=currentArtifacts.get(input.artifactId);if(!artifact||upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||String(recordValue(artifact,'FILENAME')||'')!==input.filename||Number(recordValue(artifact,'BYTE_SIZE'))!==input.byteSize||String(recordValue(artifact,'SHA256')||'').toLowerCase()!==input.sha256)throw new Error(`Frozen package artifact input ${input.artifactId} does not match current application-verified canonical bytes.`);return {...input,version:String(recordValue(artifact,'VERSION')||''),hashAlgorithm:String(recordValue(artifact,'HASH_ALGORITHM')||'SHA-256'),productId:String(artifact.scope?.productId||recordValue(artifact,'PRODUCT_ID')||artifact.relationships?.PRODUCT_ID||''),runId:String(artifact.scope?.runId||recordValue(artifact,'RUN_ID')||artifact.relationships?.RUN_ID||'')};});if(hash.sha256Value(copy)!==manifestHash||!/^[a-f0-9]{64}$/.test(manifestHash)||String(packageId||'')!==expectedPackageId)throw new Error('The operation reservation package identity does not match the exact frozen descriptor bytes.');
  return {descriptor:copy,artifactInputs,artifactInputSetSha256:hash.sha256Value(artifactInputs)};
}
function reserveOperation(project,{stage=project.activeStage,operation='COMPLETE',targetSlot='DEFAULT',packageId=null,promptIdentity=null,contextSignature=null,packageManifestHash=null,packageDescriptor=null,expectedRevision=projectRevision(project),promptProjectRevision=expectedRevision,owningBrowserTabInstance='UNKNOWN',scope=currentScope(project),disclosureClassification='UNKNOWN',authorizationBasis='NONE'}={}){
  ensureShape(project);if(!collectionAvailable('operationReservations'))throw new Error('The operation-reservation schema is unavailable.');const number=Number(stage),op=upper(operation||'COMPLETE'),slot=String(targetSlot||'DEFAULT'),instructionId=String(promptIdentity||'').trim(),commandRevision=Number(expectedRevision),boundPromptRevision=Number(promptProjectRevision);if(!Number.isInteger(number)||number<1||number>Number(schema.STAGE_COUNT||30)||!schema.operationContract(number,op))throw new Error('The operation reservation does not identify a registered stage operation.');if(!instructionId)throw new Error('An operation reservation requires the exact nonblank deterministic PROMPT_IDENTITY.');if(!Number.isSafeInteger(commandRevision)||commandRevision!==projectRevision(project))throw new Error('Operation-reservation expectedRevision is stale.');if(!Number.isSafeInteger(boundPromptRevision)||boundPromptRevision<commandRevision)throw new Error('Operation-reservation promptProjectRevision is invalid.');const scopeValueCopy={...clone(scope||{}),projectRevision:boundPromptRevision},descriptorBinding=validateReservationPackageDescriptor(project,{descriptor:packageDescriptor,packageId,packageManifestHash,stage:number,operation:op,targetSlot:slot,scope:scopeValueCopy}),targetKey=reservationTargetKey({stage:number,operation:op,targetSlot:slot,scope:scopeValueCopy}),active=records(project,'operationReservations',{active:false}).filter(item=>ACTIVE_RESERVATION_STATES.includes(upper(recordValue(item,'STATUS')||item.status))&&String(recordValue(item,'TARGET_KEY')||'')===targetKey);if(active.length>1)throw new Error('Multiple authoritative reservations exist for the same operation target.');if(active.length===1){const existing=active[0],same=String(recordValue(existing,'PROMPT_IDENTITY')||'')===instructionId&&String(recordValue(existing,'PACKAGE_ID')||'')===String(packageId||'')&&Number(recordValue(existing,'EXPECTED_REVISION'))===boundPromptRevision&&String(recordValue(existing,'REQUIRED_ARTIFACT_INPUT_SET_SHA256')||hash.sha256Value([]))===descriptorBinding.artifactInputSetSha256;if(same)return existing;throw new Error('An authoritative reservation already controls this operation target. Resume, cancel, or supersede it before creating another.');}
  const payload={stage:number,operation:op,targetSlot:slot,targetKey,packageId:String(packageId||''),promptIdentity:instructionId,contextSignature:String(contextSignature||''),packageManifestHash:String(packageManifestHash||''),packageDescriptor:descriptorBinding.descriptor,requiredArtifactInputs:descriptorBinding.artifactInputs,requiredArtifactInputSetSha256:descriptorBinding.artifactInputSetSha256,expectedRevision:commandRevision,promptProjectRevision:boundPromptRevision,scope:scopeValueCopy,owningBrowserTabInstance:String(owningBrowserTabInstance||'UNKNOWN'),disclosureClassification:String(disclosureClassification||'UNKNOWN'),authorizationBasis:String(authorizationBasis||'NONE')};const outcome=executeIdempotentCommand(project,{commandType:'RESERVE_OPERATION',target:targetKey,expectedRevision:commandRevision,scope:scopeValueCopy,payload,mutate:()=>{const challengeNonce=cryptographicNonce(16),record=commandRecord(project,'operationReservations',{JOB_ID:String(project.job?.JOB_ID||''),STAGE:number,OPERATION:op,TARGET_SLOT:slot,TARGET_KEY:targetKey,PACKAGE_ID:String(packageId||''),PROMPT_IDENTITY:instructionId,CONTEXT_SIGNATURE:String(contextSignature||''),PACKAGE_MANIFEST_HASH:String(packageManifestHash||''),PACKAGE_DESCRIPTOR:descriptorBinding.descriptor,REQUIRED_ARTIFACT_INPUTS:descriptorBinding.artifactInputs,REQUIRED_ARTIFACT_INPUT_SET_SHA256:descriptorBinding.artifactInputSetSha256,CHALLENGE_NONCE:challengeNonce,EXPECTED_REVISION:boundPromptRevision,SCOPE:clone(scopeValueCopy),SCOPE_HASH:hash.sha256Value(scopeValueCopy),STATUS:'ACTIVE',OWNING_BROWSER_TAB_INSTANCE:String(owningBrowserTabInstance||'UNKNOWN'),DISCLOSURE_CLASSIFICATION:String(disclosureClassification||'UNKNOWN'),AUTHORIZATION_BASIS:String(authorizationBasis||'NONE')},{stage:number,source:'APPLICATION_RESERVATION',scope:scopeValueCopy});record.reservationId=recordId(record,'operationReservations');addHistory(project,'OPERATION_RESERVED',{stage:number,operation:op,reservationId:record.reservationId,targetKey,challengeNonce,packageManifestHash:String(packageManifestHash||''),requiredArtifactInputSetSha256:descriptorBinding.artifactInputSetSha256,requiredArtifactIds:descriptorBinding.artifactInputs.map(item=>item.artifactId),commandExpectedRevision:commandRevision,promptProjectRevision:boundPromptRevision,promptIdentity:instructionId});return record;}});return records(project,'operationReservations',{active:false}).find(item=>recordId(item,'operationReservations')===outcome.receipt.resultIdentity)||outcome.result;
}
function transitionOperationReservation(project,reservationId,toStatus,{expectedRevision=projectRevision(project),owningBrowserTabInstance=null,reason=''}={}){ensureShape(project);const id=String(reservationId||''),record=records(project,'operationReservations',{active:false}).find(item=>recordId(item,'operationReservations')===id);if(!record)throw new Error('Operation reservation does not exist.');const from=upper(recordValue(record,'STATUS')||record.status),allowed={ACTIVE:['ORPHANED','CANCELLED','ACCEPTED','SUPERSEDED'],RESERVED:['ACTIVE','ORPHANED','CANCELLED','SUPERSEDED'],ORPHANED:['ACTIVE','CANCELLED','SUPERSEDED']}[from]||[];if(!allowed.includes(toStatus))throw new Error('Illegal operation-reservation transition '+from+' -> '+toStatus+'.');if(Number(expectedRevision)!==projectRevision(project))throw new Error('Operation-reservation transition is stale.');if(toStatus==='ACTIVE'&&owningBrowserTabInstance===null)throw new Error('Resuming an orphaned reservation requires the current browser-tab instance.');const payload={reservationId:id,from,toStatus,owningBrowserTabInstance,reason};const outcome=executeIdempotentCommand(project,{commandType:'TRANSITION_OPERATION_RESERVATION',target:id,expectedRevision,scope:record.scope||currentScope(project),payload,mutate:()=>{record.fields={...recordFields(record),STATUS:toStatus};record.STATUS=toStatus;record.status=toStatus;record.updatedAt=now();if(owningBrowserTabInstance!==null){record.fields.OWNING_BROWSER_TAB_INSTANCE=String(owningBrowserTabInstance);record.OWNING_BROWSER_TAB_INSTANCE=String(owningBrowserTabInstance);}if(reason){record.fields.TRANSITION_REASON=String(reason);record.TRANSITION_REASON=String(reason);}if(['CANCELLED','ACCEPTED','SUPERSEDED'].includes(toStatus))record.active=false;refreshRecordHashes(record,'operationReservations');addHistory(project,'OPERATION_RESERVATION_'+toStatus,{reservationId:id,from,toStatus,reason:String(reason||'')});return record;}});return records(project,'operationReservations',{active:false}).find(item=>recordId(item,'operationReservations')===outcome.receipt.resultIdentity)||outcome.result;}
function orphanOperationReservation(project,reservationId,options={}){return transitionOperationReservation(project,reservationId,'ORPHANED',options);}
function resumeOperationReservation(project,reservationId,{expectedRevision=projectRevision(project),promptProjectRevision=null,owningBrowserTabInstance=null,reason='',stage=null,operation=null,targetSlot=null,promptIdentity=null,contextSignature='',scope=null,packageId=null,packageManifestHash='',packageDescriptor=null,commandId=null,idempotencyKey=null}={}){
  ensureShape(project);
  const id=String(reservationId||''),record=records(project,'operationReservations',{active:false}).find(item=>recordId(item,'operationReservations')===id);
  if(!record)throw new Error('Operation reservation does not exist.');
  if(upper(recordValue(record,'STATUS')||record.status)!=='ORPHANED')throw new Error('Only an orphaned operation reservation can be resumed.');
  const commandRevision=Number(expectedRevision),number=Number(stage),op=upper(operation||''),slot=String(targetSlot||''),owner=String(owningBrowserTabInstance||'').trim();
  if(commandRevision!==projectRevision(project))throw new Error('Operation-reservation resume is stale.');
  if(!owner)throw new Error('Resuming an orphaned reservation requires the current browser-tab instance.');
  if(number!==Number(recordValue(record,'STAGE'))||op!==upper(recordValue(record,'OPERATION'))||slot!==String(recordValue(record,'TARGET_SLOT')||''))throw new Error('The resumed operation target does not match the orphaned reservation.');

  const bindingFor=proposal=>proposal?.envelope?.operationBinding||proposal?.operationBinding||null,pending=safe(project.projectData.responseProposals).filter(item=>!item.invalidatedBy&&upper(item.status||item.state)==='PENDING_OPERATOR_REVIEW'&&String(bindingFor(item)?.operationReservationId||'')===id);
  if(pending.length>1)throw new Error('Multiple pending proposals claim the same orphaned operation reservation.');
  const proposal=pending[0]||null,oldInstructionId=String(recordValue(record,'PROMPT_IDENTITY')||''),oldPrompt=safe(project.projectData.generatedPrompts).find(item=>String(item.instructionId||item.promptId||'')===oldInstructionId&&!item.invalidatedBy),binding=bindingFor(proposal),bindingMatches=Boolean(proposal&&oldPrompt&&String(proposal.promptId||'')===oldInstructionId&&String(binding?.packageId||'')===String(recordValue(record,'PACKAGE_ID')||'')&&String(binding?.challengeNonce||'')===String(recordValue(record,'CHALLENGE_NONCE')||'')&&Number(binding?.projectRevision)===Number(recordValue(record,'EXPECTED_REVISION'))&&String(binding?.scopeSha256||'')===String(recordValue(record,'SCOPE_HASH')||'')),proposalRevision=Number(proposal?.preconditions?.projectRevision),preservePending=Boolean(bindingMatches&&Number.isSafeInteger(proposalRevision)&&proposalRevision+1===commandRevision);
  if(preservePending){
    const futureCommittedRevision=commandRevision+1,payload={reservationId:id,mode:'PENDING_PROPOSAL_PRESERVED',proposalId:proposal.proposalId,owningBrowserTabInstance:owner,reason:String(reason||'')},outcome=executeIdempotentCommand(project,{commandType:'RESUME_OPERATION_RESERVATION',target:id,expectedRevision:commandRevision,scope:record.scope||currentScope(project),payload,commandId,idempotencyKey,mutate:()=>{const updates={STATUS:'ACTIVE',OWNING_BROWSER_TAB_INSTANCE:owner,TRANSITION_REASON:String(reason||'Operator explicitly resumed the orphaned reservation with its already validated pending proposal.')};record.fields={...recordFields(record),...updates};Object.assign(record,updates);record.status='ACTIVE';record.active=true;record.updatedAt=now();proposal.preconditions={...(proposal.preconditions||{}),projectRevision:futureCommittedRevision};proposal.resumedReservationId=id;proposal.resumedAt=now();proposal.resumeExpectedCommittedRevision=futureCommittedRevision;refreshRecordHashes(record,'operationReservations');addHistory(project,'OPERATION_RESERVATION_RESUMED',{reservationId:id,stage:number,operation:op,targetSlot:slot,mode:'PENDING_PROPOSAL_PRESERVED',proposalId:proposal.proposalId,commandExpectedRevision:commandRevision,proposalExpectedRevision:futureCommittedRevision});return record;}});
    return records(project,'operationReservations',{active:false}).find(item=>recordId(item,'operationReservations')===outcome.receipt.resultIdentity)||outcome.result;
  }

  const boundPromptRevision=Number(promptProjectRevision),instructionId=String(promptIdentity||'').trim();
  if(!Number.isSafeInteger(boundPromptRevision)||boundPromptRevision<commandRevision+1)throw new Error('Resuming an operation reservation requires the exact future prompt-bound project revision.');
  if(!instructionId)throw new Error('Resuming an operation reservation requires the exact nonblank replacement PROMPT_IDENTITY.');
  if(oldPrompt&&instructionId===oldInstructionId)throw new Error('A resumed reservation with a previously saved prompt requires a new deterministic PROMPT_IDENTITY.');
  const scopeValueCopy={...clone(scope||{}),projectRevision:boundPromptRevision},descriptorBinding=validateReservationPackageDescriptor(project,{descriptor:packageDescriptor,packageId,packageManifestHash,stage:number,operation:op,targetSlot:slot,scope:scopeValueCopy}),targetKey=reservationTargetKey({stage:number,operation:op,targetSlot:slot,scope:scopeValueCopy}),payload={reservationId:id,mode:'REBOUND_PROMPT',stage:number,operation:op,targetSlot:slot,promptIdentity:instructionId,contextSignature:String(contextSignature||''),scope:scopeValueCopy,packageId:String(packageId||''),packageManifestHash:String(packageManifestHash||''),packageDescriptor:descriptorBinding.descriptor,requiredArtifactInputs:descriptorBinding.artifactInputs,requiredArtifactInputSetSha256:descriptorBinding.artifactInputSetSha256,owningBrowserTabInstance:owner,reason:String(reason||'')};
  const outcome=executeIdempotentCommand(project,{commandType:'RESUME_OPERATION_RESERVATION',target:id,expectedRevision:commandRevision,scope:scopeValueCopy,payload,commandId,idempotencyKey,mutate:()=>{const invalidationId=`RESERVATION-RESUMED-${id}-${boundPromptRevision}`;for(const prior of safe(project.projectData.generatedPrompts).filter(item=>!item.invalidatedBy&&(String(item.operationReservationId||item.OPERATION_RESERVATION_ID||item.reservation?.operationReservationId||'')===id||String(item.instructionId||item.promptId||'')===oldInstructionId))){prior.invalidatedBy=invalidationId;for(const map of safe(project.projectData.blindAliasMaps).filter(item=>!item.invalidatedBy&&item.instructionId===prior.instructionId))map.invalidatedBy=invalidationId;}for(const stale of safe(project.projectData.responseProposals).filter(item=>!item.invalidatedBy&&String(bindingFor(item)?.operationReservationId||'')===id)){stale.invalidatedBy=invalidationId;stale.status='STALE';}if(project.stages?.[number]?.currentPromptId===oldInstructionId)project.stages[number].currentPromptId=null;const challengeNonce=cryptographicNonce(16),updates={STATUS:'ACTIVE',OWNING_BROWSER_TAB_INSTANCE:owner,TRANSITION_REASON:String(reason||'Operator explicitly resumed the orphaned reservation.'),TARGET_KEY:targetKey,PACKAGE_ID:String(packageId||''),PACKAGE_MANIFEST_HASH:String(packageManifestHash||''),PACKAGE_DESCRIPTOR:descriptorBinding.descriptor,REQUIRED_ARTIFACT_INPUTS:descriptorBinding.artifactInputs,REQUIRED_ARTIFACT_INPUT_SET_SHA256:descriptorBinding.artifactInputSetSha256,PROMPT_IDENTITY:instructionId,CONTEXT_SIGNATURE:String(contextSignature||''),EXPECTED_REVISION:boundPromptRevision,SCOPE:clone(scopeValueCopy),SCOPE_HASH:hash.sha256Value(scopeValueCopy),CHALLENGE_NONCE:challengeNonce};record.fields={...recordFields(record),...updates};Object.assign(record,updates);record.scope=scopeValueCopy;record.status='ACTIVE';record.active=true;record.updatedAt=now();refreshRecordHashes(record,'operationReservations');addHistory(project,'OPERATION_RESERVATION_RESUMED',{reservationId:id,stage:number,operation:op,targetSlot:slot,mode:'REBOUND_PROMPT',oldPromptIdentity:oldInstructionId,promptIdentity:instructionId,commandExpectedRevision:commandRevision,promptProjectRevision:boundPromptRevision,challengeNonce,packageManifestHash:String(packageManifestHash||''),requiredArtifactInputSetSha256:descriptorBinding.artifactInputSetSha256,requiredArtifactIds:descriptorBinding.artifactInputs.map(item=>item.artifactId),invalidationId});return record;}});
  return records(project,'operationReservations',{active:false}).find(item=>recordId(item,'operationReservations')===outcome.receipt.resultIdentity)||outcome.result;
}
function cancelOperationReservation(project,reservationId,options={}){return transitionOperationReservation(project,reservationId,'CANCELLED',options);}
function acceptOperationReservation(project,reservationId,options={}){return transitionOperationReservation(project,reservationId,'ACCEPTED',options);}
function orphanActiveOperationReservations(project,{owningBrowserTabInstance=null,expectedRevision=projectRevision(project)}={}){ensureShape(project);const selected=records(project,'operationReservations',{active:false}).filter(item=>upper(recordValue(item,'STATUS')||item.status)==='ACTIVE'&&(!owningBrowserTabInstance||String(recordValue(item,'OWNING_BROWSER_TAB_INSTANCE')||'')===String(owningBrowserTabInstance))),out=[];for(const reservation of selected)out.push(orphanOperationReservation(project,recordId(reservation,'operationReservations'),{expectedRevision,reason:'Browser session ended before the reserved operation completed.'}));return out;}
function createHumanBlocker(project,{stage=project.activeStage,reason,operatorLabel='HUMAN_OPERATOR'}={}){
  if(!String(reason||'').trim())throw new Error('A blocker reason is required.');
  const record=commandRecord(project,'blockers',{MISSING_ITEM_TYPE:'UNRESOLVED',MISSING_FACT_INPUT_AUTHORITY_EVIDENCE_CAPABILITY_DECISION_RULE:String(reason).trim(),AFFECTED_REQUIREMENTS:'UNKNOWN',AFFECTED_TESTS:'UNKNOWN',AFFECTED_ARTIFACTS:'UNKNOWN',WHY_WORK_CANNOT_CONTINUE:String(reason).trim(),ATTEMPTED_RESOLUTIONS:'NONE',DOWNSTREAM_WORK_STOPPED:`STAGE ${String(stage).padStart(2,'0')} AND DEPENDENT WORK`,OWNER:operatorLabel,STATUS:'OPEN',RESOLUTION_EVIDENCE:'NONE',CLOSURE:'OPEN',REEVALUATION:'REQUIRED',REQUIRED_REVALIDATION:'REQUIRED'},{stage,source:'HUMAN_DECISION'});
  addHistory(project,'HUMAN_BLOCKER_CREATED',{stage,recordId:record.id,operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return record;
}
function reconcileArtifactCustodyVerification(project,{report,stage=project.activeStage}={}){
  ensureShape(project);if(!report||typeof report!=='object')throw new Error('An application artifact-verification report is required.');const open=records(project,'blockers',{active:false}).filter(record=>record.source==='APPLICATION'&&record.applicationBlockerKind==='ARTIFACT_CUSTODY'&&isActiveRecord(record)&&['OPEN','BLOCKED','IN PROGRESS','UNKNOWN'].includes(upper(recordValue(record,'STATUS')||'OPEN')));
  if(report.verified){for(const blocker of open){const updates={STATUS:'RESOLVED',RESOLUTION_EVIDENCE:`Application re-read ${report.artifactCount} stored Blob(s); all required canonical byte identities reconciled at ${report.at||now()}.`,CLOSURE:'CLOSED',REEVALUATION:'COMPLETED',REQUIRED_REVALIDATION:'RECALCULATED'};blocker.fields=blocker.fields&&typeof blocker.fields==='object'?blocker.fields:{};for(const [key,value] of Object.entries(updates)){blocker.fields[key]=value;blocker[key]=value;}blocker.updatedAt=now();blocker.resolvedBy='APPLICATION';refreshRecordHashes(blocker,'blockers');addHistory(project,'APPLICATION_ARTIFACT_CUSTODY_RESTORED',{recordId:recordId(blocker,'blockers'),verificationAt:report.at||now()});}if(open.length)recalculate(project);return {verified:true,resolvedBlockerIds:open.map(record=>recordId(record,'blockers'))};}
  const failures=safe(report.artifacts).filter(item=>!item.verified),artifactIds=failures.map(item=>item.artifactId).filter(Boolean),reason=`Stored artifact custody verification failed: ${failures.map(item=>`${item.artifactId||'UNKNOWN'} (${item.issue||'byte/hash/metadata mismatch'})`).join(', ')||'required canonical bytes are missing or mismatched'}.`;let blocker=open[0];if(!blocker){blocker=commandRecord(project,'blockers',{MISSING_ITEM_TYPE:'ARTIFACT_CUSTODY',MISSING_FACT_INPUT_AUTHORITY_EVIDENCE_CAPABILITY_DECISION_RULE:reason,AFFECTED_REQUIREMENTS:'UNKNOWN',AFFECTED_TESTS:'UNKNOWN',AFFECTED_ARTIFACTS:artifactIds.length?artifactIds:'UNKNOWN',WHY_WORK_CANNOT_CONTINUE:reason,ATTEMPTED_RESOLUTIONS:'APPLICATION BYTE RE-READ AND SHA-256 VERIFICATION',DOWNSTREAM_WORK_STOPPED:'RELEASE AND DEPENDENT WORK',OWNER:'APPLICATION',STATUS:'OPEN',RESOLUTION_EVIDENCE:'NONE',CLOSURE:'OPEN',REEVALUATION:'REQUIRED',REQUIRED_REVALIDATION:'REQUIRED'},{stage,source:'APPLICATION'});blocker.applicationBlockerKind='ARTIFACT_CUSTODY';refreshRecordHashes(blocker,'blockers');addHistory(project,'APPLICATION_ARTIFACT_CUSTODY_BLOCKED',{recordId:recordId(blocker,'blockers'),artifactIds,verificationAt:report.at||now()});}
  project.release.authorization='NOT AUTHORIZED';project.release.authorizedArtifactIds=[];for(const collection of ['artifactIdentities','evidenceChains','releaseRecords'])for(const record of records(project,collection,{active:false}))if(isActiveRecord(record)){record.active=false;record.validity='INVALIDATED';record.invalidatedBy=recordId(blocker,'blockers');refreshRecordHashes(record,collection);}recalculate(project);return {verified:false,blockerId:recordId(blocker,'blockers')};
}
function resolveHumanBlocker(project,{blockerId,resolutionEvidence,operatorLabel='HUMAN_OPERATOR'}={}){
  ensureShape(project);const id=String(blockerId||'').trim(),evidence=String(resolutionEvidence||'').trim();if(!id)throw new Error('A blocker identity is required.');if(!evidence)throw new Error('Resolution evidence is required.');const blocker=records(project,'blockers',{active:false}).find(record=>recordId(record,'blockers')===id);if(!blocker)throw new Error(`Blocker ${id} does not exist.`);if(blocker.source!=='HUMAN_DECISION')throw new Error('Only independently human-created blockers may be resolved by this human authority control.');if(!isActiveRecord(blocker)||!['OPEN','BLOCKED','IN PROGRESS','UNKNOWN'].includes(upper(recordValue(blocker,'STATUS')||'OPEN')))throw new Error(`Blocker ${id} is not open.`);
  const updates={STATUS:'RESOLVED',RESOLUTION_EVIDENCE:evidence,CLOSURE:'CLOSED',REEVALUATION:'COMPLETED',REQUIRED_REVALIDATION:'RECALCULATED'};blocker.fields=blocker.fields&&typeof blocker.fields==='object'?blocker.fields:{};for(const [key,value] of Object.entries(updates)){blocker.fields[key]=value;blocker[key]=value;}blocker.updatedAt=now();blocker.resolvedBy=operatorLabel;blocker.identityAssurance='SELF_ASSERTED';blocker.contentSha256=hash.contentRecordSha256?hash.contentRecordSha256(blocker,'BLOCKER_ID'):hash.sha256Value(blocker.fields);blocker.recordSha256=hash.recordSha256?hash.recordSha256(blocker):hash.sha256Value(blocker);blocker.sha256=blocker.recordSha256;addHistory(project,'HUMAN_BLOCKER_RESOLVED',{stage:Number(blocker.stage||0),recordId:id,operatorLabel,identityAssurance:'SELF_ASSERTED',resolutionEvidence:evidence});recalculate(project);return blocker;
}
function registerFreshContext(project,{stage=project.activeStage,externalContextIdentifier,operatorLabel='HUMAN_OPERATOR',purpose='GENERAL'}={}){
  ensureShape(project);const identifier=String(externalContextIdentifier||'').trim();const placeholder=new Set(['','UNASSIGNED','UNKNOWN','PENDING','NOT APPLICABLE','NONE']);if(placeholder.has(upper(identifier)))throw new Error('A real external fresh-context identifier is required.');
  const iterationId=String(project.job.CURRENT_ITERATION||'NOT APPLICABLE');const current=records(project,'freshContexts',{stage}).filter(record=>String(recordValue(record,'ITERATION_ID')||record.scope?.iterationId||'')===iterationId);if(current.some(record=>upper(recordValue(record,'EXTERNAL_CONTEXT_IDENTIFIER'))===upper(identifier)))throw new Error('This external context identifier is already registered for the current iteration.');
  const reviewer=upper(purpose)==='REVIEWER';let record=reviewer?null:current.find(record=>placeholder.has(upper(recordValue(record,'EXTERNAL_CONTEXT_IDENTIFIER'))));
  if(record){record.fields=record.fields&&typeof record.fields==='object'?record.fields:{};record.fields.EXTERNAL_CONTEXT_IDENTIFIER=identifier;record.EXTERNAL_CONTEXT_IDENTIFIER=identifier;record.fulfilledAt=now();record.fulfilledBy=operatorLabel;record.identityAssurance='SELF_ASSERTED';refreshRecordHashes(record,'freshContexts');}
  else record=commandRecord(project,'freshContexts',{EXTERNAL_CONTEXT_IDENTIFIER:identifier,ROLE:reviewer?`${core.STAGES[Number(stage)-1]?.role||'UNKNOWN'} — INDEPENDENT REVIEWER`:core.STAGES[Number(stage)-1]?.role||'UNKNOWN',ITERATION_ID:iterationId,RUN_ID:'NOT APPLICABLE',AUTHORIZED_PROJECT_INPUTS:project.job.CURRENT_INPUT_VERSION||'UNKNOWN',AUTHORIZED_EXTERNAL_SOURCE_MATERIAL:project.job.CURRENT_SOURCE_SET_VERSION||'NOT APPLICABLE',FROZEN_ARTIFACT_VERSIONS:'CURRENT AUTHORIZED VERSIONS',TOOL_AVAILABILITY:'UNKNOWN',CONTAMINATION_STATUS:'UNKNOWN',OUTPUT_IDENTITY:'UNKNOWN',DEVIATIONS:'NONE RECORDED',EVIDENCE:reviewer?'Dedicated independent reviewer context identifier supplied by operator.':'Context identifier supplied by operator.',USABILITY_DETERMINATION:'UNKNOWN'},{stage,source:reviewer?'HUMAN_REVIEWER_CONTEXT':'HUMAN_INPUT'});
  addHistory(project,'FRESH_CONTEXT_REGISTERED',{stage,recordId:record.id,externalContextIdentifier:identifier,operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return record;
}
function recordHumanDecision(project,{stage,field,value,operatorLabel='HUMAN_OPERATOR',reason='Explicit human authority input.'}={}){
  ensureShape(project);const number=Number(stage),definition=schema.STAGE_FIELDS?.[number]?.[field];if(!definition)throw new Error(`Unknown Stage ${number} field ${field}.`);
  const actor=definition.producer;const authorization=schema.authorizeMutation({fieldDefinition:definition,actor,mutationType:'SET'});if(authorization===false||authorization?.authorized===false||authorization?.allowed===false)throw new Error(authorization?.reason||`Mutation of ${field} is not authorized for ${actor}.`);
  if(!['HUMAN','HUMAN_DECISION'].includes(String(actor)))throw new Error(`${field} is not human authority.`);project.stages[number].humanData[field]=clone(value);
  const history=addHistory(project,'HUMAN_DECISION_RECORDED',{stage:number,field,value:clone(value),operatorLabel,identityAssurance:'SELF_ASSERTED',reason});const record={decisionId:allocateInfrastructureId(project,'HUMAN-DECISION','humanDecisions'),stage:number,field,value:clone(value),operatorLabel,identityAssurance:'SELF_ASSERTED',reason,eventSequence:history.eventSequence,deviceTimestamp:history.deviceTimestamp,createdAt:history.deviceTimestamp};project.projectData.humanDecisions=safe(project.projectData.humanDecisions);project.projectData.humanDecisions.push(record);return record;
}
function invalidateAcceptedResponse(project,{stage=project.activeStage,rawResponseId,reason='Corrected response required.',operatorLabel='HUMAN_OPERATOR'}={}){
  ensureShape(project);const number=Number(stage),current=acceptedChanges(project,number),selected=current.find(item=>!rawResponseId||item.rawResponseId===rawResponseId);if(!selected)throw new Error('No current accepted data change exists for invalidation.');
  const proposal=safe(project.projectData.responseProposals).find(item=>item.proposalId===selected.proposalId),operation=selected.operation||proposal?.envelope?.operation||'COMPLETE',scope=clone(selected.scope||proposal?.scope||proposal?.envelope?.scope||{}),laneKeys=['iterationId','candidateId','runId','contextId','baselineId','productId'],scalar=value=>String(value??''),sameLane=item=>String(item?.operation||item?.envelope?.operation||'COMPLETE')===String(operation)&&laneKeys.every(key=>scalar((item?.scope||item?.envelope?.scope||{})[key])===scalar(scope?.[key]));
  const affected=current.filter(sameLane);if(!affected.length)throw new Error('The selected accepted response no longer belongs to a current refinement lane.');
  const event=addHistory(project,'ACCEPTED_RESPONSE_INVALIDATED',{stage:number,rawResponseId:selected.rawResponseId,promptId:selected.promptId||proposal?.promptId||null,operation,scope,reason,operatorLabel,identityAssurance:'SELF_ASSERTED'}),id=event.eventId,proposalIds=new Set(affected.map(change=>change.proposalId)),rawIds=new Set(affected.map(change=>change.rawResponseId)),canonicalIds=new Set(affected.flatMap(change=>safe(change.canonicalRecordIds))),snapshotMap=new Map();
  for(const change of affected){change.invalidatedBy=id;for(const snapshot of safe(change.reservedTargetSnapshots))snapshotMap.set(`${snapshot.collection}:${snapshot.id}`,clone(snapshot.record));}
  for(const prompt of safe(project.projectData.generatedPrompts).filter(item=>Number(item.stage)===number&&!item.invalidatedBy&&sameLane(item)))prompt.invalidatedBy=id;
  for(const pending of safe(project.projectData.responseProposals).filter(item=>Number(item.stage)===number&&!item.invalidatedBy&&sameLane(item))){if(proposalIds.has(pending.proposalId)||pending.status==='PENDING_OPERATOR_REVIEW'||pending.status==='ACCEPTED'){pending.invalidatedBy=id;pending.status='INVALIDATED_FOR_REFINEMENT';}}
  for(const confirmation of safe(project.projectData.stageConfirmations).filter(item=>Number(item.stage)===number&&!item.invalidatedBy))confirmation.invalidatedBy=id;
  for(const collection of Object.keys(schema.RECORD_SCHEMAS)){const list=safe(project.projectData[collection]);for(let index=0;index<list.length;index++){const record=list[index],recordIdentity=recordId(record,collection),key=`${collection}:${recordIdentity}`;if(snapshotMap.has(key)){list[index]=clone(snapshotMap.get(key));continue;}if(!record.invalidatedBy&&(proposalIds.has(record.sourceProposalId)||rawIds.has(record.rawResponseId)||canonicalIds.has(recordIdentity))){record.invalidatedBy=id;record.active=false;record.validity='INVALIDATED';refreshRecordHashes(record,collection);}}}
  const remaining=acceptedChanges(project,number).sort((a,b)=>Number(a.eventSequence||0)-Number(b.eventSequence||0)),rebuilt={};for(const change of remaining){const acceptedProposal=safe(project.projectData.responseProposals).find(item=>item.proposalId===change.proposalId);Object.assign(rebuilt,clone(acceptedProposal?.proposedStageData||{}));}
  const state=project.stages[number];state.agentData=rebuilt;state.acceptedData=clone(rebuilt);state.acceptedDataChangeIds=remaining.map(change=>change.changeId);state.acceptedResponseIds=remaining.map(change=>change.rawResponseId);state.currentPromptId=null;state.status='NOT STARTED';state.invalidatedBy=null;
  if(number<30)invalidateDownstream(project,number,id,reason);else recalculate(project);return event;
}
function reserveRunBatch(project,{stage=project.activeStage,iterationId=null,candidateId=null,count=10}={}){
  ensureShape(project);if(count!==10)throw new Error('Run batches must contain exactly ten slots.');const resolvedIteration=String(iterationId||project.job.CURRENT_ITERATION||'');if(!resolvedIteration)throw new Error('A current iteration is required before reserving runs.');const stageNumber=Number(stage),iteration=records(project,'iterations').find(r=>recordId(r,'iterations')===resolvedIteration&&isActiveRecord(r));if(!iteration)throw new Error('The requested current iteration does not exist or is inactive.');const iterationCandidate=iterationCandidateId(project,resolvedIteration),resolvedCandidate=String(candidateId||iterationCandidate||'');if(!resolvedCandidate)throw new Error('A frozen candidate is required before reserving runs.');if(iterationCandidate&&resolvedCandidate!==iterationCandidate)throw new Error('The requested candidate does not match the current iteration candidate.');const candidate=records(project,'candidateFreezes').find(r=>recordId(r,'candidateFreezes')===resolvedCandidate&&isActiveRecord(r));if(!candidate||!candidateComponentIdentity(project,resolvedCandidate))throw new Error('The current iteration must resolve to one active frozen candidate with application-computed component hashes.');const iterationScope=scopeForIteration(project,resolvedIteration),existingRuns=records(project,'runs').filter(run=>Number(run.stage)===stageNumber&&String(recordValue(run,'ITERATION_ID')||run.scope?.iterationId||'')===resolvedIteration);
  if(existingRuns.length){if(existingRuns.length!==count)throw new Error(`The current Stage ${String(stageNumber).padStart(2,'0')} iteration already contains ${existingRuns.length} active run slots; expected exactly ${count}. Resolve the partial or contradictory batch instead of allocating more.`);const existing=existingRuns.map(run=>{const runId=recordId(run,'runs'),contextId=String(recordValue(run,'CONTEXT_ID')||run.relationships?.CONTEXT_ID||''),runCandidate=String(recordValue(run,'CANDIDATE_ID')||run.scope?.candidateId||'');if(runCandidate!==resolvedCandidate)throw new Error(`Existing run ${runId} is bound to candidate ${runCandidate||'UNKNOWN'}, not current candidate ${resolvedCandidate}.`);const context=records(project,'freshContexts').find(item=>recordId(item,'freshContexts')===contextId&&String(recordValue(item,'ITERATION_ID')||item.scope?.iterationId||'')===resolvedIteration);if(!context)throw new Error(`Existing run ${runId} does not resolve to its active fresh-context slot.`);return {runId,contextId};});if(new Set(existing.map(item=>item.contextId)).size!==count)throw new Error('The existing run batch does not contain ten distinct active context slots.');return existing;}
  const created=[];for(let i=0;i<count;i++){const context=commandRecord(project,'freshContexts',{EXTERNAL_CONTEXT_IDENTIFIER:'UNASSIGNED',ROLE:core.STAGES[stageNumber-1]?.role||'UNKNOWN',ITERATION_ID:resolvedIteration,RUN_ID:'PENDING',AUTHORIZED_PROJECT_INPUTS:project.job.CURRENT_INPUT_VERSION||'UNKNOWN',AUTHORIZED_EXTERNAL_SOURCE_MATERIAL:project.job.CURRENT_SOURCE_SET_VERSION||'NOT APPLICABLE',FROZEN_ARTIFACT_VERSIONS:'CURRENT AUTHORIZED VERSIONS',TOOL_AVAILABILITY:'UNKNOWN',CONTAMINATION_STATUS:'UNKNOWN',OUTPUT_IDENTITY:'UNKNOWN',DEVIATIONS:'NONE',EVIDENCE:'Application-reserved context slot.',USABILITY_DETERMINATION:'PENDING'},{stage:stageNumber,source:'APPLICATION_RESERVATION',scope:iterationScope});const run=commandRecord(project,'runs',{ITERATION_ID:resolvedIteration,CANDIDATE_ID:resolvedCandidate,CONTEXT_ID:context.id,FRESH_CONTEXT_RECORD:context.id,CONTAMINATION_CHECK:'PENDING',TOOL_CONFIGURATION:'PENDING',EXECUTION_STATUS:'RESERVED',COMPLETE_OUTPUT:''},{stage:stageNumber,source:'APPLICATION_RESERVATION',scope:{...iterationScope,contextId:context.id}});context.fields.RUN_ID=context.RUN_ID=run.id;context.scope={...iterationScope,runId:run.id,contextId:context.id};run.scope={...iterationScope,runId:run.id,contextId:context.id};refreshRecordHashes(context,'freshContexts');refreshRecordHashes(run,'runs');created.push({runId:run.id,contextId:context.id});}
  addHistory(project,'RUN_BATCH_RESERVED',{stage:stageNumber,iterationId:resolvedIteration,candidateId:resolvedCandidate,count,slots:created});recalculate(project);return created;
}
function registerArtifactBytes(project,{stage=project.activeStage,artifactId,filename,mediaType,byteSize,sha256,lineage={},role='STAGE_ARTIFACT'}={}){
  ensureShape(project);if(!artifactId||!filename||!Number.isInteger(Number(byteSize))||Number(byteSize)<0||!/^[a-f0-9]{64}$/i.test(String(sha256||'')))throw new Error('Verified artifact identity is incomplete.');
  if(records(project,'artifacts',{active:false}).some(r=>recordId(r,'artifacts')===String(artifactId)))return records(project,'artifacts',{active:false}).find(r=>recordId(r,'artifacts')===String(artifactId));
  const fields={FILENAME:String(filename),TYPE:String(mediaType||'application/octet-stream'),VERSION:'APPLICATION-CONTROLLED',BYTE_SIZE:Number(byteSize),SHA256:String(sha256).toLowerCase(),ROLE:role,STORAGE_REFERENCE:`indexeddb:${artifactId}`,AVAILABILITY:'BYTES_PERSISTED_AND_VERIFIED',NOTES:JSON.stringify(lineage)};
  const definition=schema.RECORD_SCHEMAS.artifacts,id=String(artifactId),deviceTimestamp=now(),full={...fields,[definition.idField]:id};const record={id,stage,createdAt:deviceTimestamp,updatedAt:deviceTimestamp,active:true,scope:currentScope(project),fields:full,...full,source:'APPLICATION_BYTES_VERIFIED'};refreshRecordHashes(record,'artifacts');project.projectData.artifacts.push(record);const productId=String(lineage?.productId||'');if(productId){const product=records(project,'products').find(r=>recordId(r,'products')===productId&&isActiveRecord(r));if(!product)throw new Error('Artifact lineage references a missing or inactive product.');const inventory=[...new Set([...safe(recordValue(product,'GENERATED_ARTIFACT_INVENTORY')),id])];product.fields={...(product.fields||{}),GENERATED_ARTIFACT_INVENTORY:inventory};product.GENERATED_ARTIFACT_INVENTORY=inventory;product.updatedAt=now();refreshRecordHashes(product,'products');}addHistory(project,'ARTIFACT_BYTES_REGISTERED',{stage,artifactId:id,byteSize:Number(byteSize),sha256:String(sha256).toLowerCase(),productId:productId||null});recalculate(project);return record;
}
function selectedArtifacts(project,artifactIds){const ids=[...new Set(safe(artifactIds).map(String))],items=records(project,'artifacts',{active:true}).filter(a=>ids.includes(recordId(a,'artifacts')));if(items.length!==ids.length)throw new Error('Every selected artifact must resolve to one active canonical artifact.');for(const a of items)if(upper(recordValue(a,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||!recordValue(a,'SHA256')||!Number.isFinite(Number(recordValue(a,'BYTE_SIZE'))))throw new Error(`Artifact ${recordId(a,'artifacts')} does not have verified stored bytes.`);return items;}
function freezeCandidate(project,{stage=project.activeStage||10,artifactIds=[],operatorLabel='HUMAN_OPERATOR',purpose='TEST_CANDIDATE',toolConfiguration='CURRENT AUTHORIZED CONFIGURATION',settings='CURRENT AUTHORIZED SETTINGS',permissions='CURRENT AUTHORIZED PERMISSIONS',limitations='RECORDED LIMITATIONS',roleDistribution='WORKFLOW ROLE MAP',batchChangeRule='ANY MATERIAL CHANGE REQUIRES A NEW CANDIDATE'}={}){
  ensureShape(project);const freezeStage=Number(stage);if(![10,17,19].includes(freezeStage))throw new Error('Candidate freezes are allowed only at Stages 10, 17, and 19.');const artifacts=selectedArtifacts(project,artifactIds);if(!artifacts.length)throw new Error('At least one verified artifact is required to freeze a candidate.');const manifest=artifacts.map(a=>({artifactId:recordId(a,'artifacts'),filename:recordValue(a,'FILENAME'),byteSize:Number(recordValue(a,'BYTE_SIZE')),sha256:recordValue(a,'SHA256'),storageReference:recordValue(a,'STORAGE_REFERENCE')})),componentHashes=Object.fromEntries(manifest.map(a=>[a.artifactId,a.sha256])),createdAt=now(),previousIteration=latestIteration(project,[10,17,19]),previousCandidate=records(project,'candidateFreezes').at(-1);let candidateId,candidate;if(freezeStage===19){if(!previousCandidate)throw new Error('Stage 19 unchanged confirmation requires an existing candidate.');const priorManifest=recordValue(previousCandidate,'COMPONENT_MANIFEST')||[];if(hash.sha256Value(priorManifest)!==hash.sha256Value(manifest))throw new Error('Stage 19 requires the exact unchanged candidate artifact identity.');candidateId=recordId(previousCandidate,'candidateFreezes');candidate=previousCandidate;}else candidateId=allocateId(project,'candidateFreezes');const iterationId=allocateId(project,'iterations');
  const frozenScope={...currentScope(project),iterationId,candidateId};const iterationFields={ITERATION_ID:iterationId,CANDIDATE_ID:candidateId,PREVIOUS_ITERATION_ID:recordId(previousIteration,'iterations')||null,CHANGESET_ID:recordId(records(project,'changes').at(-1),'changes')||null,PURPOSE:purpose,STATUS:'FROZEN',LINEAGE:hash.sha256Value(manifest),EVIDENCE:`Application freeze from ${manifest.length} verified artifact(s).`};const iteration={id:iterationId,stage:freezeStage,createdAt,active:true,scope:frozenScope,fields:iterationFields,...iterationFields,source:'APPLICATION_DERIVATION'};iteration.contentSha256=hash.contentRecordSha256(iteration,'ITERATION_ID');iteration.recordSha256=hash.recordSha256(iteration);iteration.sha256=iteration.recordSha256;project.projectData.iterations.push(iteration);
  if(freezeStage!==19){const candidateFields={CANDIDATE_ID:candidateId,ITERATION_ID:iterationId,COMPONENT_MANIFEST:manifest,COMPONENT_VERSIONS:Object.fromEntries(manifest.map(a=>[a.artifactId,'APPLICATION-CONTROLLED'])),COMPONENT_HASHES:componentHashes,ROLE_DISTRIBUTION:roleDistribution,IMMUTABLE_LOCATIONS:manifest.map(a=>a.storageReference),TOOL_CONFIGURATION:toolConfiguration,SETTINGS:settings,PERMISSIONS:permissions,LIMITATIONS:limitations,BATCH_CHANGE_RULE:batchChangeRule,STATUS:'FROZEN',EVIDENCE:hash.sha256Value({manifest,operatorLabel})};candidate={id:candidateId,stage:freezeStage,createdAt,active:true,scope:frozenScope,fields:candidateFields,...candidateFields,source:'APPLICATION_DERIVATION'};candidate.contentSha256=hash.contentRecordSha256(candidate,'CANDIDATE_ID');candidate.recordSha256=hash.recordSha256(candidate);candidate.sha256=candidate.recordSha256;project.projectData.candidateFreezes.push(candidate);}
  project.job.CURRENT_ITERATION=iterationId;addHistory(project,'CANDIDATE_FROZEN',{stage:freezeStage,iterationId,candidateId,artifactIds:manifest.map(a=>a.artifactId),unchangedConfirmation:freezeStage===19,operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return {iteration,candidate};
}
function beginUnchangedConfirmationIteration(project,{candidateId,operatorLabel='HUMAN_OPERATOR'}={}){
  ensureShape(project);if(project.stages?.[18]?.status!=='COMPLETE')throw new Error('Stage 18 must be complete before unchanged confirmation can begin.');const candidate=records(project,'candidateFreezes').find(record=>recordId(record,'candidateFreezes')===String(candidateId||'')&&Number(record.stage)===17&&isActiveRecord(record));if(!candidate)throw new Error('A current Stage 17 frozen candidate is required for unchanged confirmation.');const componentHashes=recordValue(candidate,'COMPONENT_HASHES')||{};if(!componentHashes||typeof componentHashes!=='object'||Array.isArray(componentHashes)||!Object.keys(componentHashes).length)throw new Error('The unchanged-confirmation candidate must have application-computed component hashes.');const previousIterationId=String(recordValue(candidate,'ITERATION_ID')||candidate.scope?.iterationId||'');const previousIteration=records(project,'iterations').find(record=>recordId(record,'iterations')===previousIterationId);if(!previousIteration)throw new Error('The candidate source iteration does not exist.');const iterationId=allocateId(project,'iterations'),createdAt=now(),baseScope=clone(previousIteration.scope||candidate.scope||currentScope(project));const fields={ITERATION_ID:iterationId,CANDIDATE_ID:String(candidateId),PREVIOUS_ITERATION_ID:previousIterationId,CHANGESET_ID:null,PURPOSE:'UNCHANGED_CONFIRMATION',STATUS:'FROZEN',LINEAGE:hash.sha256Value({candidateId:String(candidateId),componentHashes}),EVIDENCE:'Application allocated a new unchanged-confirmation iteration using the exact existing frozen candidate identity.'};const iteration={id:iterationId,stage:19,createdAt,active:true,scope:{...baseScope,iterationId,candidateId:String(candidateId)},fields,...fields,source:'APPLICATION_DERIVATION'};iteration.contentSha256=hash.contentRecordSha256(iteration,'ITERATION_ID');iteration.recordSha256=hash.recordSha256(iteration);iteration.sha256=iteration.recordSha256;project.projectData.iterations.push(iteration);project.job.CURRENT_ITERATION=iterationId;addHistory(project,'UNCHANGED_CONFIRMATION_ITERATION_STARTED',{stage:19,iterationId,candidateId:String(candidateId),previousIterationId,operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return iteration;
}
function freezeBaseline(project,{artifactIds=[],operatorLabel='HUMAN_OPERATOR',authorization='AUTHORIZED'}={}){
  ensureShape(project);
  const confirmation=recordsForCurrentScope(project,'confirmationRecords').filter(record=>upper(recordValue(record,'DETERMINATION'))==='SATISFIED').at(-1);
  if(!confirmation)throw new Error('A current successful unchanged confirmation is required before baseline freeze.');
  const currentIteration=records(project,'iterations').find(record=>recordId(record,'iterations')===String(project.job.CURRENT_ITERATION||'')&&isActiveRecord(record));
  if(!currentIteration||Number(currentIteration.stage)!==19)throw new Error('The current unchanged-confirmation iteration is required before baseline freeze.');
  const currentCandidateId=iterationCandidateId(project,recordId(currentIteration,'iterations'));
  const currentCandidate=records(project,'candidateFreezes').find(record=>recordId(record,'candidateFreezes')===currentCandidateId&&isActiveRecord(record));
  if(!currentCandidate||!candidateComponentIdentity(project,currentCandidateId))throw new Error('The unchanged-confirmation iteration does not resolve to an active frozen candidate.');
  const manifest=safe(recordValue(currentCandidate,'COMPONENT_MANIFEST'));
  const candidateArtifactIds=manifest.map(item=>String(item?.artifactId||'')).filter(Boolean);
  if(!candidateArtifactIds.length||candidateArtifactIds.length!==manifest.length||new Set(candidateArtifactIds).size!==candidateArtifactIds.length)throw new Error('The confirmed candidate manifest does not contain one unique canonical artifact identity for every component.');
  const requested=[...new Set(safe(artifactIds).map(String))];
  if(requested.length&&hash.sha256Value([...requested].sort())!==hash.sha256Value([...candidateArtifactIds].sort()))throw new Error('Baseline authorization must use the exact artifact set from the unchanged-confirmed frozen candidate.');
  const artifacts=selectedArtifacts(project,candidateArtifactIds),byId=new Map(artifacts.map(artifact=>[recordId(artifact,'artifacts'),artifact]));
  for(const item of manifest){const artifact=byId.get(String(item.artifactId));if(!artifact||String(recordValue(artifact,'FILENAME'))!==String(item.filename)||Number(recordValue(artifact,'BYTE_SIZE'))!==Number(item.byteSize)||String(recordValue(artifact,'SHA256')).toLowerCase()!==String(item.sha256||'').toLowerCase())throw new Error(`Confirmed candidate artifact ${item.artifactId||'UNKNOWN'} no longer matches its frozen filename, byte size, and SHA-256.`);}
  const baselineId=allocateId(project,'baselines'),createdAt=now(),approvedVersions={inputVersion:project.job.CURRENT_INPUT_VERSION,sourceSetVersion:project.job.CURRENT_SOURCE_SET_VERSION,requirementsVersion:project.job.CURRENT_REQUIREMENTS_VERSION,testSuiteVersion:project.job.CURRENT_TEST_SUITE_VERSION,instructionVersion:project.job.CURRENT_INSTRUCTION_VERSION,iterationId:recordId(currentIteration,'iterations'),candidateId:currentCandidateId,hashes:Object.fromEntries(artifacts.map(artifact=>[recordId(artifact,'artifacts'),recordValue(artifact,'SHA256')]))};
  const fields={BASELINE_ID:baselineId,SUPPORTING_CONFIRMATION_ID:recordId(confirmation,'confirmationRecords'),APPROVED_VERSIONS:approvedVersions,IMMUTABLE_ARTIFACT_RECORDS:artifacts.map(artifact=>recordId(artifact,'artifacts')),HASHES:approvedVersions.hashes,HUMAN_AUTHORIZATION:authorization,AUTHORIZED_RECIPIENT_ROLES:'CURRENT AUTHORIZED ROLES',CONTROLLED_STORAGE:'INDEXEDDB VERIFIED BYTES',STATUS:'FROZEN',EVIDENCE:hash.sha256Value({approvedVersions,operatorLabel,authorization})};
  const record={id:baselineId,stage:20,createdAt,active:true,scope:{...currentScope(project),baselineId},fields,...fields,source:'APPLICATION_DERIVATION'};
  record.contentSha256=hash.contentRecordSha256(record,'BASELINE_ID');record.recordSha256=hash.recordSha256(record);record.sha256=record.recordSha256;project.projectData.baselines.push(record);project.job.CURRENT_BASELINE_ID=baselineId;addHistory(project,'BASELINE_FROZEN',{stage:20,baselineId,artifactIds:fields.IMMUTABLE_ARTIFACT_RECORDS,iterationId:approvedVersions.iterationId,candidateId:approvedVersions.candidateId,operatorLabel,identityAssurance:'SELF_ASSERTED',backupCheckpointRequired:true});recalculate(project);return record;
}
function reserveProductExecution(project,{operatorLabel='HUMAN_OPERATOR'}={}){ensureShape(project);const baseline=recordsForCurrentScope(project,'baselines').at(-1);if(!baseline)throw new Error('A current frozen baseline is required.');const noRun=value=>['','NONE','NOT APPLICABLE','UNASSIGNED','PENDING'].includes(upper(value));const usedContextIds=new Set(records(project,'products').filter(isActiveRecord).map(r=>String(recordValue(r,'PRODUCTION_CONTEXT_ID')||''))),contexts=recordsForCurrentScope(project,'freshContexts').filter(r=>Number(r.stage)===21&&noRun(recordValue(r,'RUN_ID'))&&!usedContextIds.has(recordId(r,'freshContexts'))),context=contexts.at(-1);if(!context)throw new Error('A current unused fresh production context is required.');const productId=allocateId(project,'products'),executionId=allocateInfrastructureId(project,'EXECUTION','products'),createdAt=now(),fields={PRODUCT_ID:productId,PRODUCT_VERSION:'PRODUCT-v001',BASELINE_ID:recordId(baseline,'baselines'),EXECUTION_ID:executionId,PRODUCTION_CONTEXT_ID:recordId(context,'freshContexts'),INSTRUCTION_VERSION:project.job.CURRENT_INSTRUCTION_VERSION,BASELINE_MATERIALS:recordValue(baseline,'IMMUTABLE_ARTIFACT_RECORDS')||[],EXECUTION_TIMESTAMPS:{reservedAt:createdAt},TOOL_CONFIGURATION:'PENDING AGENT COMPLETION',DEVIATIONS:'NONE RECORDED',FAILURES:'NONE RECORDED',GENERATED_ARTIFACT_INVENTORY:[],STATUS:'RESERVED'};const record={id:productId,stage:21,createdAt,active:true,reserved:true,completionState:'AWAITING_AGENT_COMPLETION',scope:{...currentScope(project),baselineId:fields.BASELINE_ID,productId},fields,...fields,source:'APPLICATION_RESERVED'};record.contentSha256=hash.contentRecordSha256(record,'PRODUCT_ID');record.recordSha256=hash.recordSha256(record);record.sha256=record.recordSha256;project.projectData.products.push(record);project.job.CURRENT_PRODUCT_ID=productId;addHistory(project,'PRODUCT_EXECUTION_RESERVED',{stage:21,productId,executionId,contextId:fields.PRODUCTION_CONTEXT_ID,operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return record;}
function createNewJobReset(project){ensureShape(project);const id=allocateInfrastructureId(project,'NEW-JOB-RESET','newJobResets'),createdAt=now(),fields={NEW_JOB_ID:project.job.JOB_ID,OLD_JOB_MATERIAL_REUSED:'FALSE',AUTHORIZED_REUSED_ARTIFACTS:'NONE',NEW_JOB_START_STAGE:'STAGE 01',RESET_COMPLETED_BY:'APPLICATION',RESET_DATE_AND_TIME:createdAt,RESET_EVIDENCE:'A clean 30-stage project was created without carrying active state from another job.'},record={id,stage:1,createdAt,active:true,fields,...fields,source:'APPLICATION_DERIVATION'};record.sha256=hash.sha256Value(fields);project.projectData.newJobResets.push(record);addHistory(project,'NEW_JOB_RESET_RECORDED',{stage:1,recordId:id});return record;}

function recordMigratedAcceptedChange(project,record){ensureShape(project);if(!record?.changeId)throw new Error('A migrated accepted change requires an identity.');const existing=safe(project.projectData.acceptedChanges).find(x=>x.changeId===record.changeId);if(existing)return existing;const value={...clone(record),source:'DETERMINISTIC_MIGRATION'};project.projectData.acceptedChanges.push(value);addHistory(project,'MIGRATED_ACCEPTED_CHANGE_RECORDED',{stage:Number(value.stage),recordId:value.changeId,migrationSchema:value.migrationSchema||'UNKNOWN'});return value;}

function samePromptTarget(a,b){return Number(a?.stage)===Number(b?.stage)&&String(a?.operation||'COMPLETE')===String(b?.operation||'COMPLETE')&&['iterationId','candidateId','runId','contextId','baselineId','productId'].every(key=>String(a?.scope?.[key]??'')===String(b?.scope?.[key]??''));}
function registerGeneratedPrompt(project,promptRecord){
  ensureShape(project);if(!promptRecord?.instructionId||!Number.isInteger(Number(promptRecord?.stage)))throw new Error('A complete generated prompt record is required.');
  const expectedRevision=Number(project.revision||0)+1;if(Number(promptRecord.scope?.projectRevision)!==expectedRevision)throw new Error(`Generated prompt revision ${promptRecord.scope?.projectRevision} does not match next committed revision ${expectedRevision}.`);
  if(collectionAvailable('operationReservations')){const reservationId=String(promptRecord.operationReservationId||promptRecord.OPERATION_RESERVATION_ID||promptRecord.reservation?.operationReservationId||''),challengeNonce=String(promptRecord.challengeNonce||promptRecord.CHALLENGE_NONCE||promptRecord.reservation?.challengeNonce||'');if(Boolean(reservationId)!==Boolean(challengeNonce))throw new Error('A reserved prompt must include both its operation reservation identity and challenge nonce.');if(reservationId){const reservation=records(project,'operationReservations',{active:false}).find(item=>recordId(item,'operationReservations')===reservationId&&ACTIVE_RESERVATION_STATES.includes(upper(recordValue(item,'STATUS')||item.status)));if(!reservation)throw new Error('The prompt operation reservation is missing, stale, or inactive.');const reservedPromptIdentity=String(recordValue(reservation,'PROMPT_IDENTITY')||'').trim(),instructionId=String(promptRecord.instructionId||promptRecord.promptId||'').trim();if(!reservedPromptIdentity)throw new Error('The operation reservation has a blank PROMPT_IDENTITY and cannot control a prompt.');if(reservedPromptIdentity!==instructionId)throw new Error('The generated prompt instruction identity does not match the exact PROMPT_IDENTITY frozen in its operation reservation.');if(String(recordValue(reservation,'CHALLENGE_NONCE')||'')!==challengeNonce)throw new Error('The prompt challenge nonce does not match its operation reservation.');if(Number(recordValue(reservation,'STAGE'))!==Number(promptRecord.stage)||upper(recordValue(reservation,'OPERATION'))!==upper(promptRecord.operation||'COMPLETE'))throw new Error('The prompt does not match its reserved stage and operation.');if(Number(recordValue(reservation,'EXPECTED_REVISION'))!==Number(promptRecord.scope?.projectRevision))throw new Error('The generated prompt project revision does not match the prompt-bound revision frozen in its operation reservation.');if(String(recordValue(reservation,'SCOPE_HASH')||'')!==String(hash.sha256Value(promptRecord.scope||{})))throw new Error('The generated prompt scope does not match the exact scope frozen in its operation reservation.');if(String(recordValue(reservation,'CONTEXT_SIGNATURE')||'')&&String(recordValue(reservation,'CONTEXT_SIGNATURE'))!==String(promptRecord.contextSignature||''))throw new Error('The prompt context signature does not match its operation reservation.');}else{const activeReservation=records(project,'operationReservations',{active:false}).find(item=>ACTIVE_RESERVATION_STATES.includes(upper(recordValue(item,'STATUS')||item.status))&&Number(recordValue(item,'STAGE'))===Number(promptRecord.stage)&&upper(recordValue(item,'OPERATION'))===upper(promptRecord.operation||'COMPLETE'));if(activeReservation)throw new Error('An unbound prompt cannot replace the current authoritative reserved operation.');}}
  const existing=safe(project.projectData.generatedPrompts).find(x=>x.instructionId===promptRecord.instructionId);if(existing)return existing;
  const invalidationId=`PROMPT-SUPERSEDED-${promptRecord.instructionId}`;for(const prior of safe(project.projectData.generatedPrompts).filter(x=>!x.invalidatedBy&&samePromptTarget(x,promptRecord))){prior.invalidatedBy=invalidationId;for(const map of safe(project.projectData.blindAliasMaps).filter(x=>!x.invalidatedBy&&x.instructionId===prior.instructionId))map.invalidatedBy=invalidationId;}
  const record={...clone(promptRecord),source:'APPLICATION_PROMPT_REGISTRATION'};project.projectData.generatedPrompts.push(record);const aliases=safe(record.contextManifest?.blindAliasMap);if(aliases.length&&!project.projectData.blindAliasMaps.some(x=>x.instructionId===record.instructionId&&!x.invalidatedBy)){project.projectData.blindAliasMaps.push({mapId:`BLIND-MAP-${hash.sha256Value({instructionId:record.instructionId,contextSignature:record.contextSignature}).slice(0,20).toUpperCase()}`,instructionId:record.instructionId,stage:Number(record.stage),operation:record.operation,contextSignature:record.contextSignature,entries:clone(aliases),active:true,createdAt:now(),source:'APPLICATION_PROMPT_REGISTRATION'});}project.stages[Number(record.stage)].currentPromptId=record.instructionId;
  addHistory(project,'INSTRUCTION_SAVED',{recordId:record.instructionId,stage:Number(record.stage),sha256:record.bodySha256||record.sha256,projectRevision:expectedRevision});return record;
}


function recordHumanInspectionEvidence(project,{testId,observation,evidenceDescription='',operatorLabel='HUMAN_OPERATOR'}={}){
  ensureShape(project);const tid=String(testId||'').trim(),text=String(observation||'').trim(),description=String(evidenceDescription||'').trim(),stage=Number(project.activeStage||0);if(!tid)throw new Error('A current HUMAN_INSPECTION test is required.');if(!text)throw new Error('Record the actual human observation before continuing.');const test=recordsForCurrentScope(project,'tests').find(record=>recordId(record,'tests')===tid);if(!test||upper(recordValue(test,'EXECUTION_MODE'))!=='HUMAN_INSPECTION')throw new Error('The selected test is not a current HUMAN_INSPECTION test.');const plan=testExecutionPlan(project).items.find(item=>item.testId===tid);if(!plan?.executableNow)throw new Error(plan?.blockingReason||'The human inspection is not currently executable.');const currentArtifacts=new Map(recordsForCurrentScope(project,'artifacts').map(item=>[recordId(item,'artifacts'),item])),artifactIds=[...new Set(plan.requiredArtifactIds.map(String))];if(!artifactIds.length)throw new Error('Human inspection requires exact application-verified artifact identities; a filename-only or unbound observation is not admissible.');const artifactIdentities=artifactIds.map(artifactId=>{const artifact=currentArtifacts.get(artifactId);if(!artifact)throw new Error('Human inspection artifact '+artifactId+' is not current canonical state.');const filename=String(recordValue(artifact,'FILENAME')||''),version=String(recordValue(artifact,'VERSION')||''),byteSize=Number(recordValue(artifact,'BYTE_SIZE')),sha256=String(recordValue(artifact,'SHA256')||'').toLowerCase();if(!filename||!version||!Number.isSafeInteger(byteSize)||byteSize<0||!/^[a-f0-9]{64}$/.test(sha256)||upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED')throw new Error('Human inspection artifact '+artifactId+' lacks exact verified version, filename, size, or SHA-256.');return {artifactId,productId:String(artifact.scope?.productId||recordValue(artifact,'PRODUCT_ID')||artifact.relationships?.PRODUCT_ID||''),filename,version,byteSize,sha256};}),artifactIdentitySha256=hash.sha256Value(artifactIdentities),payloadBase={testId:tid,requirementId:testRequirementId(test),observation:text,evidenceDescription:description||'Human-owned observation recorded directly by the operator.',operatorLabel:String(operatorLabel||'HUMAN_OPERATOR'),identityAssurance:'SELF_ASSERTED',stage,scope:currentScope(project),artifactIdentities,artifactIdentitySha256},preexisting=recordsForCurrentScope(project,'evidenceRecords').find(record=>record?.source==='HUMAN_OBSERVATION'&&String(record?.humanInspectionTestId||'')===tid&&String(record?.humanInspectionArtifactIdentitySha256||'')===artifactIdentitySha256&&String(record?.humanInspectionObservationSha256||'')===hash.sha256Value(text));if(preexisting)return preexisting;
  const operationRoute=currentStageOperation(project,stage),operation=schema.operationContract(stage,operationRoute?.operation)?operationRoute.operation:'COMPLETE',promptIdentity='HUMAN-INSPECTION-INSTRUCTION-'+hash.sha256Value({jobId:project.job?.JOB_ID,stage,operation,testId:tid,artifactIdentitySha256}).slice(0,24).toUpperCase(),packageId='HUMAN-INSPECTION-PACKAGE-'+artifactIdentitySha256.slice(0,24).toUpperCase(),reservation=reserveOperation(project,{stage,operation,targetSlot:'HUMAN_INSPECTION:'+tid,packageId,promptIdentity,packageManifestHash:artifactIdentitySha256,expectedRevision:projectRevision(project),promptProjectRevision:projectRevision(project),owningBrowserTabInstance:'APPLICATION_HUMAN_INSPECTION',scope:currentScope(project),authorizationBasis:'SELF_ASSERTED_HUMAN_OBSERVATION'}),reservationId=recordId(reservation,'operationReservations'),payload={...payloadBase,inspectionReservationId:reservationId,challengeNonce:String(recordValue(reservation,'CHALLENGE_NONCE')||'')},identity=hash.sha256Value(payload),evidence=commandRecord(project,'evidenceRecords',{APPLICATION_EVIDENCE_KIND:'HUMAN_OBSERVATION',APPLICATION_EVIDENCE_DESCRIPTION:payload.evidenceDescription,APPLICATION_EVIDENCE_CONTENT:JSON.stringify(payload),SHA256:identity,INSPECTION_RESERVATION_ID:reservationId,STATUS:'CURRENT'},{stage,source:'HUMAN_OBSERVATION'});evidence.humanInspectionTestId=tid;evidence.humanInspectionArtifactIdentitySha256=artifactIdentitySha256;evidence.humanInspectionObservationSha256=hash.sha256Value(text);evidence.humanAuthority={operatorLabel:payload.operatorLabel,identityAssurance:'SELF_ASSERTED'};evidence.relationships={...(evidence.relationships||{}),INSPECTION_RESERVATION_ID:reservationId};refreshRecordHashes(evidence,'evidenceRecords');
  if(collectionAvailable('observationRecords')){const targetPropositions=propositionIdsFor(test),fields={ORIGIN_CLASS:'HUMAN_OBSERVATION',SUBMITTING_ACTOR:payload.operatorLabel,SUBJECT_DESCRIPTION:'Human inspection for '+tid,ARTIFACT_ID:artifactIds[0],RUN_ID:null,PRODUCT_ID:project.job?.CURRENT_PRODUCT_ID||null,RESULT_ID:null,TEST_ID:tid,INSPECTION_RESERVATION_ID:reservationId,HUMAN_OBSERVED_VALUE:{observation:text},HUMAN_OBSERVED_LOCATION:'HUMAN_INSPECTION',HUMAN_METHOD_OR_TOOL:'DIRECT_HUMAN_OBSERVATION',INPUT_IDENTITIES_AND_HASHES:artifactIdentities,OUTPUT_IDENTITIES_AND_HASHES:[],EPISTEMIC_BASIS:'SELF_ASSERTED',FRESHNESS_STATUS:'CURRENT',SOURCE_EVIDENCE_IDS:[recordId(evidence,'evidenceRecords')],CURRENT_SCOPE:currentScope(project),RAW_OR_EXECUTION_PROVENANCE:{evidenceId:recordId(evidence,'evidenceRecords'),inspectionReservationId:reservationId,challengeNonce:payload.challengeNonce,artifactIdentitySha256,deviceTimestamp:now(),identityAssurance:'SELF_ASSERTED'},STATUS:'CURRENT'},observed=commandRecord(project,'observationRecords',fields,{stage,source:'HUMAN_OBSERVATION'});observed.relationships={...(observed.relationships||{}),TEST_ID:tid,ARTIFACT_ID:artifactIds,INSPECTION_RESERVATION_ID:reservationId,SOURCE_EVIDENCE_IDS:[recordId(evidence,'evidenceRecords')],...(targetPropositions.length===1?{PROPOSITION_ID:targetPropositions[0]}:{PROPOSITION_ID:targetPropositions})};observed.evidenceRefs=[recordId(evidence,'evidenceRecords')];refreshRecordHashes(observed,'observationRecords');evidence.observationId=recordId(observed,'observationRecords');evidence.fields.OBSERVATION_ID=evidence.observationId;evidence.OBSERVATION_ID=evidence.observationId;evidence.relationships.OBSERVATION_ID=evidence.observationId;refreshRecordHashes(evidence,'evidenceRecords');}
  acceptOperationReservation(project,reservationId,{expectedRevision:projectRevision(project),reason:'Exact artifact-bound human observation was recorded canonically.'});addHistory(project,'HUMAN_INSPECTION_OBSERVATION_RECORDED',{stage,testId:tid,evidenceId:recordId(evidence,'evidenceRecords'),observationId:evidence.observationId||null,inspectionReservationId:reservationId,artifactIds,artifactIdentitySha256,operatorLabel:payload.operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return evidence;
}

function evidenceReferences(result){const ids=new Set();for(const id of safe(result?.evidenceRefs||result?.evidenceIds))if(String(id||'').trim())ids.add(String(id));for(const key of ['EVIDENCE_ID','EXACT_EVIDENCE','EVIDENCE','PROCESS_EVIDENCE','PRODUCT_EVIDENCE','CONTROLLING_EVIDENCE','PRE_CORRECTION_EVIDENCE','POST_CORRECTION_EVIDENCE']){const value=recordValue(result,key);if(Array.isArray(value))for(const id of value)if(/^EVIDENCE-/i.test(String(id)))ids.add(String(id));else if(/^EVIDENCE-/i.test(String(value||'')))ids.add(String(value));}return [...ids];}
function evaluateExternalArtifactInputBinding(project,{observation,test=null}={}){
  const reasons=[],origin=upper(recordValue(observation,'ORIGIN_CLASS')||recordValue(observation,'OBSERVATION_ORIGIN'));if(!['EXTERNAL_CLAIM','VERIFIED_EXTERNAL_OBSERVATION','AGENT_SEMANTIC_OBSERVATION'].includes(origin))return {required:false,sufficient:true,reasons,expectedArtifactInputs:[],observedArtifactInputs:[],reservationId:null};
  const provenance=recordValue(observation,'RAW_OR_EXECUTION_PROVENANCE')||{},reservationId=String(provenance.operationReservationId||''),reservation=records(project,'operationReservations',{active:false}).find(item=>recordId(item,'operationReservations')===reservationId);if(!reservation){reasons.push('External observation provenance does not resolve its exact operation reservation.');return {required:true,sufficient:false,reasons,expectedArtifactInputs:[],observedArtifactInputs:[],reservationId};}
  try{
    const status=upper(recordValue(reservation,'STATUS')||reservation.status),packageId=String(recordValue(reservation,'PACKAGE_ID')||''),manifestHash=String(recordValue(reservation,'PACKAGE_MANIFEST_HASH')||'').toLowerCase(),nonce=String(recordValue(reservation,'CHALLENGE_NONCE')||''),targetSlot=String(recordValue(reservation,'TARGET_SLOT')||''),scope=recordValue(reservation,'SCOPE')||{},descriptor=recordValue(reservation,'PACKAGE_DESCRIPTOR');if(!descriptor||typeof descriptor!=='object'||Array.isArray(descriptor))throw new Error('External observation reservation lacks its frozen canonical package descriptor.');const validated=validateReservationPackageDescriptor(project,{descriptor,packageId,packageManifestHash:manifestHash,stage:Number(recordValue(reservation,'STAGE')),operation:recordValue(reservation,'OPERATION'),targetSlot,scope}),expected=validated.artifactInputs,expectedSha=validated.artifactInputSetSha256,rawObserved=safe(recordValue(observation,'INPUT_IDENTITIES_AND_HASHES')),observed=rawObserved.filter(item=>item&&item.artifactId).map(item=>{const {kind,...identity}=item;return identity;}).sort((a,b)=>String(a.artifactId)<String(b.artifactId)?-1:String(a.artifactId)>String(b.artifactId)?1:0);
    if(status!=='ACCEPTED')reasons.push('External observation reservation has not been accepted for this canonical result.');if(String(provenance.packageId||'')!==packageId||String(provenance.challengeNonce||'')!==nonce||String(provenance.packageManifestHash||'').toLowerCase()!==manifestHash||String(provenance.targetSlot||'')!==targetSlot||String(provenance.scopeSha256||'')!==String(recordValue(reservation,'SCOPE_HASH')||'')||String(provenance.requiredArtifactInputSetSha256||'')!==expectedSha)reasons.push('External observation package, reservation, nonce, manifest, target, scope, or input-set provenance is mismatched.');
    if(new Set(observed.map(item=>String(item.artifactId||''))).size!==observed.length||hash.sha256Value(observed)!==hash.sha256Value(expected))reasons.push('External observation does not contain the exact frozen required artifact input identity and hash set.');
    const stored=safe(recordValue(reservation,'REQUIRED_ARTIFACT_INPUTS')),storedSha=String(recordValue(reservation,'REQUIRED_ARTIFACT_INPUT_SET_SHA256')||'');if(hash.sha256Value(stored)!==hash.sha256Value(expected)||storedSha!==expectedSha)reasons.push('External reservation required-artifact identities do not match its frozen package descriptor.');
    if(test){const testId=recordId(test,'tests'),descriptorTestIds=safe(validated.descriptor?.testIds).map(String),required=[...new Set(safe(testExecutionPlan(project).items.find(item=>item.testId===testId)?.requiredArtifactIds).map(String))],expectedIds=new Set(expected.map(item=>item.artifactId));if(!descriptorTestIds.includes(testId))reasons.push('External observation package is not frozen for the result test.');if(required.some(artifactId=>!expectedIds.has(artifactId)))reasons.push('External observation package omits at least one current required test artifact.');}
    return {required:true,sufficient:reasons.length===0,reasons:[...new Set(reasons)],expectedArtifactInputs:expected,observedArtifactInputs:observed,reservationId,packageId,artifactInputSetSha256:expectedSha};
  }catch(error){reasons.push(String(error?.message||error));return {required:true,sufficient:false,reasons:[...new Set(reasons)],expectedArtifactInputs:[],observedArtifactInputs:[],reservationId};}
}
function evaluateEvidenceSufficiency(project,{requirement=null,test=null,result=null}={}){
  const evaluationScope=result?.scope&&Object.keys(result.scope).length?clone(result.scope):null,scopedRecords=collection=>evaluationScope?recordsForScope(project,collection,evaluationScope):recordsForCurrentScope(project,collection),ids=evidenceReferences(result),referencedEvidence=ids.map(id=>records(project,'evidenceRecords').find(x=>recordId(x,'evidenceRecords')===id)).filter(Boolean),mode=upper(recordValue(test,'EXECUTION_MODE')||''),testId=String(recordId(test,'tests')||''),humanEvidence=mode==='HUMAN_INSPECTION'?scopedRecords('evidenceRecords').filter(x=>x?.source==='HUMAN_OBSERVATION'&&String(x?.humanInspectionTestId||'')===testId):[],evidence=[...new Map([...referencedEvidence,...humanEvidence].map(x=>[recordId(x,'evidenceRecords'),x])).values()],nativeEvidence=evidence.filter(x=>x?.source==='APPLICATION_TEST_RUNTIME'),narrative=nativeEvidence.map(x=>String(recordValue(x,'APPLICATION_EVIDENCE_CONTENT')||recordValue(x,'APPLICATION_EVIDENCE_DESCRIPTION')||'').trim()).find(Boolean)||['EXACT_EVIDENCE','EVIDENCE','PROCESS_EVIDENCE','PRODUCT_EVIDENCE','CONTROLLING_EVIDENCE'].map(k=>String(recordValue(result,k)||'').trim()).find(Boolean)||'',type=upper(recordValue(test,'TEST_TYPE')||''),artifactRequirements=String(recordValue(test,'ARTIFACT_REQUIREMENTS')||''),artifactRequired=Boolean(artifactRequirements&&!['NONE','NOT APPLICABLE','N/A'].includes(upper(artifactRequirements))),testText=[type,artifactRequirements,recordValue(test,'PROCEDURE'),recordValue(test,'EXPECTED_RESULT'),recordValue(test,'EVIDENCE_TO_PRESERVE')].join(' '),byteTest=artifactRequired||/\b(?:SHA-?256|HASH|BYTE(?:S|[- ]IDENTITY)?|CRYPTOGRAPHIC IDENTITY)\b/i.test(testText),reasons=[],requiredEvidenceClasses=[];
  const attached=evidence.map(e=>String(recordValue(e,'ATTACHMENT_ID')||e.relationships?.ATTACHMENT_ID||'')).filter(Boolean),verifiedArtifacts=records(project,'artifacts').filter(a=>attached.includes(recordId(a,'artifacts'))&&upper(recordValue(a,'AVAILABILITY'))==='BYTES_PERSISTED_AND_VERIFIED'&&/^[a-f0-9]{64}$/i.test(String(recordValue(a,'SHA256')||''))),productSha=String(recordValue(result,'PRODUCT_SHA256')||''),matchingProductArtifact=productSha&&scopedRecords('artifacts').some(a=>upper(recordValue(a,'AVAILABILITY'))==='BYTES_PERSISTED_AND_VERIFIED'&&String(recordValue(a,'SHA256')||'')===productSha),hasEvidence=Boolean(ids.length||narrative||evidence.length),semanticModelActive=Boolean(propositionIdsFor(result).length||propositionIdsFor(test).length||scopedRecords('propositions').length||scopedRecords('proofObligations').length),releaseProof=Boolean(semanticModelActive&&test&&releaseBearingTest(test)&&!['ADVISORY','NEGATIVE_ONLY'].includes(testRole(test))),proofContract=releaseProof?proofContractForTest(test):null;
  if(proofContract&&!proofContract.complete)reasons.push(...proofContract.reasons.map(reason=>'Release-bearing evidence contract: '+reason));if(proofContract)requiredEvidenceClasses.push(...proofContract.evidenceClasses);
  if(mode==='UNAVAILABLE'){requiredEvidenceClasses.push('CAPABLE_EXECUTION');reasons.push('UNAVAILABLE execution cannot establish satisfaction.');}
  if(!hasEvidence){requiredEvidenceClasses.push('CANONICAL_OR_EXACT_EVIDENCE');reasons.push('No substantive evidence is attached to the result.');}
  if(byteTest){requiredEvidenceClasses.push('APPLICATION_VERIFIED_BYTES');if(!verifiedArtifacts.length&&!matchingProductArtifact)reasons.push('Byte-identity evidence requires application-verified artifact bytes and SHA-256; prose or a claimed hash is insufficient.');}
  if(mode==='HUMAN_INSPECTION'){requiredEvidenceClasses.push('HUMAN_OBSERVATION');const human=evidence.some(e=>{const authority=upper(e?.source||recordValue(e,'AUTHORITY_TYPE')||recordValue(e,'APPLICATION_EVIDENCE_KIND')||recordValue(e,'KIND')),content=String(recordValue(e,'APPLICATION_EVIDENCE_CONTENT')||recordValue(e,'CONTENT')||recordValue(e,'APPLICATION_EVIDENCE_DESCRIPTION')||recordValue(e,'DESCRIPTION')||'').trim(),applicationRecorded=e?.source==='HUMAN_OBSERVATION'&&String(e?.humanAuthority?.identityAssurance||'')==='SELF_ASSERTED',canonicalHuman=authority==='HUMAN_OBSERVATION';return Boolean(content&&(applicationRecorded||canonicalHuman));});if(!human)reasons.push('Human inspection requires an explicit human-owned observation recorded through the application; an agent-labelled HUMAN evidence claim is insufficient.');}
  const targetPropositionIds=propositionIdsFor(result,test),resultId=result?recordId(result,resultCollectionFor(project,result)||'')||String(result.id||result.recordId||''):'';
  const observations=scopedRecords('observationRecords').filter(observation=>{const resultIds=relationshipIds(observation,'RESULT_ID'),testIds=relationshipIds(observation,'TEST_ID'),propositionIds=relationshipIds(observation,'PROPOSITION_ID'),observationEvidenceIds=[...safe(observation.evidenceRefs).map(String),...relationshipIds(observation,'SOURCE_EVIDENCE_IDS'),...relationshipIds(observation,'EVIDENCE_IDS'),...relationshipIds(observation,'EVIDENCE_ID')];return (resultId&&resultIds.includes(resultId))||(testId&&testIds.includes(testId))||targetPropositionIds.some(id=>propositionIds.includes(id))||ids.some(id=>observationEvidenceIds.includes(id));});
  if(mode==='HUMAN_INSPECTION'){
    requiredEvidenceClasses.push('EXACT_ARTIFACT_BOUND_INSPECTION_RESERVATION');const currentArtifacts=new Map(scopedRecords('artifacts').map(item=>[recordId(item,'artifacts'),item])),plan=testExecutionPlan(project).items.find(item=>item.testId===testId),requiredArtifactIds=[...new Set(safe(plan?.requiredArtifactIds).map(String))],validBinding=observations.some(observation=>{if(upper(recordValue(observation,'ORIGIN_CLASS')||recordValue(observation,'OBSERVATION_ORIGIN'))!=='HUMAN_OBSERVATION')return false;const reservationId=String(recordValue(observation,'INSPECTION_RESERVATION_ID')||relationshipIds(observation,'INSPECTION_RESERVATION_ID')[0]||recordValue(observation,'RAW_OR_EXECUTION_PROVENANCE')?.inspectionReservationId||'').trim(),reservation=records(project,'operationReservations',{active:false}).find(item=>recordId(item,'operationReservations')===reservationId),inputIdentities=safe(recordValue(observation,'INPUT_IDENTITIES_AND_HASHES')),byId=new Map(inputIdentities.map(item=>[String(item?.artifactId||''),item]));if(!reservation||!reservationId||upper(recordValue(reservation,'STATUS'))!=='ACCEPTED'||String(recordValue(reservation,'TARGET_SLOT')||'')!=='HUMAN_INSPECTION:'+testId||!requiredArtifactIds.length)return false;const expected=[];for(const artifactId of requiredArtifactIds){const artifact=currentArtifacts.get(artifactId);if(!artifact)return false;const identity={artifactId,productId:String(artifact.scope?.productId||recordValue(artifact,'PRODUCT_ID')||artifact.relationships?.PRODUCT_ID||''),filename:String(recordValue(artifact,'FILENAME')||''),version:String(recordValue(artifact,'VERSION')||''),byteSize:Number(recordValue(artifact,'BYTE_SIZE')),sha256:String(recordValue(artifact,'SHA256')||'').toLowerCase()},submitted=byId.get(artifactId);if(!submitted||identity.filename!==String(submitted.filename||'')||identity.version!==String(submitted.version||'')||identity.byteSize!==Number(submitted.byteSize)||identity.sha256!==String(submitted.sha256||'').toLowerCase())return false;expected.push(identity);}const identitySha256=hash.sha256Value(expected),provenance=recordValue(observation,'RAW_OR_EXECUTION_PROVENANCE')||{};return String(recordValue(reservation,'PACKAGE_MANIFEST_HASH')||'')===identitySha256&&String(provenance.artifactIdentitySha256||'')===identitySha256;});if(!validBinding)reasons.push('Human inspection evidence is not bound through an accepted current reservation to the exact artifact IDs, versions, filenames, byte sizes, and SHA-256 values; a filename-only or unbound observation is insufficient.');
  }
  const obligation=scopedRecords('proofObligations').find(item=>targetPropositionIds.some(id=>relationshipIds(item,'PROPOSITION_ID').includes(id)))||null,allowedBases=scalarList(releaseProof?proofContract?.epistemicBases:recordValue(test,'REQUIRED_EPISTEMIC_BASES')||recordValue(obligation,'REQUIRED_EPISTEMIC_BASES')).map(upper),observationEvaluations=[];
  for(const observation of observations){const propositions=targetPropositionIds.length?targetPropositionIds:[null];for(const propositionId of propositions)observationEvaluations.push(evaluateObservationEntailment(project,{observation,propositionId,proofObligation:obligation,allowedEpistemicBases:allowedBases,scopeRule:evaluationScope}));}
  const resultStage=Number(result?.stage||0),normalizedExternalGateRequired=[23,24,25].includes(resultStage),boundObservationIds=relationshipIds(result,'OBSERVATION_ID'),boundEntailmentIds=relationshipIds(result,'ENTAILMENT_ID'),applicationDetermination=upper(recordValue(result,'APPLICATION_DETERMINATION')),semanticModelRequired=Boolean(normalizedExternalGateRequired||targetPropositionIds.length||obligation||observations.length||collectionAvailable('observationRecords')&&recordValue(test,'TEST_PROPOSITION_TEXT'));
  const boundExternalObservations=boundObservationIds.map(id=>scopedRecords('observationRecords').find(item=>recordId(item,'observationRecords')===id)).filter(item=>['EXTERNAL_CLAIM','VERIFIED_EXTERNAL_OBSERVATION','AGENT_SEMANTIC_OBSERVATION'].includes(upper(recordValue(item,'ORIGIN_CLASS')||recordValue(item,'OBSERVATION_ORIGIN')))),externalArtifactInputBindings=boundExternalObservations.map(observation=>evaluateExternalArtifactInputBinding(project,{observation,test})),externalArtifactInputBindingRequired=externalArtifactInputBindings.some(binding=>binding.required),externalArtifactInputBindingSatisfied=externalArtifactInputBindingRequired&&externalArtifactInputBindings.length===1&&externalArtifactInputBindings[0].sufficient;if(externalArtifactInputBindingRequired){requiredEvidenceClasses.push('EXACT_RESERVATION_PACKAGE_ARTIFACT_INPUT_SET');if(!externalArtifactInputBindingSatisfied)reasons.push(...externalArtifactInputBindings.flatMap(binding=>binding.reasons));}
  if(normalizedExternalGateRequired){if(boundObservationIds.length!==1||boundEntailmentIds.length!==1)reasons.push('The external gate result is not application-normalized to exactly one observation and one entailment.');if(!['SATISFIED','VIOLATED','UNDETERMINED'].includes(applicationDetermination))reasons.push('The external gate result lacks an application-normalized determination.');const boundObservation=boundObservationIds.length===1?scopedRecords('observationRecords').find(item=>recordId(item,'observationRecords')===boundObservationIds[0]):null,boundEntailment=boundEntailmentIds.length===1?scopedRecords('entailmentReviews').find(item=>recordId(item,'entailmentReviews')===boundEntailmentIds[0]):null;if(!boundObservation||!boundEntailment||!relationshipIds(boundEntailment,'OBSERVATION_ID').includes(boundObservationIds[0])||targetPropositionIds.length&&!targetPropositionIds.some(id=>relationshipIds(boundEntailment,'PROPOSITION_ID').includes(id)))reasons.push('The application-normalized observation/entailment binding is missing, stale, or targets the wrong proposition.');}
  const semantic=releaseProof?semanticReviewAssessment(project,{subjectKind:'TEST',subject:test,scopeRule:evaluationScope}):{ready:true,reasons:[]};if(!semantic.ready){requiredEvidenceClasses.push('EQUIVALENT_SEMANTIC_COVERAGE');reasons.push(...semantic.reasons.map(reason=>'A release-bearing result lacks exact accepted semantic equivalence: '+reason));}
  if(semanticModelRequired){requiredEvidenceClasses.push('OBSERVATION_AND_ENTAILMENT');if(!observations.length)reasons.push('Release-bearing evidence has no current observation record.');if(observations.length&&!observationEvaluations.some(item=>item.sufficient)){const detail=observationEvaluations.flatMap(item=>item.reasons);reasons.push(...(detail.length?detail:['Current observation entailment is SUPPORTS_ONLY, CONTEXT_ONLY, DOES_NOT_ADDRESS, or UNKNOWN and does not establish or refute the proposition.']));}}
  if(mode==='EXTERNAL_SYSTEM'){
    requiredEvidenceClasses.push('EXTERNAL_SYSTEM_OUTPUT');const verified=observationEvaluations.some(item=>item.sufficient&&item.origin==='VERIFIED_EXTERNAL_OBSERVATION'&&item.epistemicBasis==='VERIFIED_EXTERNAL');
    if(!verified)reasons.push('External-system verification requires a current attributable external observation and independently verifiable receipt; a tool name, copied digest, or generic prose is insufficient.');
  }
  if(Number(result?.stage)===23||result&&Object.prototype.hasOwnProperty.call(recordFields(result),'OBSERVED_MEANING')){requiredEvidenceClasses.push('MEANING_COMPARISON');for(const key of ['PRODUCT_LOCATION','REQUIRED_MEANING','OBSERVED_MEANING','EVIDENCE_BASED_COMPARISON'])if(!String(recordValue(result,key)||'').trim())reasons.push('Meaning evidence is missing '+key+'.');}
  if(mode==='EXTERNAL_AGENT_TOOL'){requiredEvidenceClasses.push('CAPABILITY_EXECUTION');if(!String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim())reasons.push('External tool execution lacks a declared capability identity.');if(!String(recordValue(result,'OBSERVED_RESULT')||recordValue(result,'ACTUAL_RESULT')||recordValue(result,'DETERMINATION')||'').trim())reasons.push('External tool execution lacks an observed result.');}
  if(mode==='EXTERNAL_AGENT_TOOL'&&allowedBases.length&&!observationEvaluations.some(item=>allowedBases.includes(item.epistemicBasis)&&item.sufficient))reasons.push('External tool evidence does not meet the proof obligation epistemic-basis contract.');
  if(observationEvaluations.some(item=>item.freshnessStatus==='EXPIRED'||item.freshnessStatus==='UNKNOWN'))reasons.push('Required observation evidence is expired or has unknown freshness.');
  if(proofContract){const dimensions=missingRequiredResultDimensions(proofContract,{scopeRule:evaluationScope||currentScope(project),runId:String(recordValue(result,'RUN_ID')||result?.relationships?.RUN_ID||result?.scope?.runId||''),productId:String(recordValue(result,'PRODUCT_ID')||result?.relationships?.PRODUCT_ID||result?.scope?.productId||''),result});if(dimensions.length)reasons.push('Release-bearing result is missing required dimensions: '+dimensions.join(', ')+'.');const independence=resultIndependenceAssessment(project,result,test,proofContract.independence);if(!['APPLICATION_ESTABLISHED','EXTERNALLY_SUPPORTED'].includes(independence.determination))reasons.push('Release-bearing result does not satisfy its independence contract: '+(independence.reasons.join(' ')||independence.determination)+'.');const sufficientObservations=observationEvaluations.filter(item=>item.sufficient),freshnessAllowed=proofContract.freshness.status==='CURRENT'?sufficientObservations.some(item=>item.freshnessStatus==='CURRENT'):proofContract.freshness.status==='NOT_APPLICABLE'?sufficientObservations.some(item=>item.freshnessStatus==='NOT_APPLICABLE'):sufficientObservations.some(item=>['CURRENT','NOT_APPLICABLE'].includes(item.freshnessStatus));if(!freshnessAllowed)reasons.push('No sufficient observation satisfies the release-bearing freshness contract '+proofContract.freshness.status+'.');const hasHuman=evidence.some(item=>item?.source==='HUMAN_OBSERVATION'||upper(recordValue(item,'AUTHORITY_TYPE')||recordValue(item,'APPLICATION_EVIDENCE_KIND'))==='HUMAN_OBSERVATION'),hasVerifiedExternal=sufficientObservations.some(item=>item.epistemicBasis==='VERIFIED_EXTERNAL'),meaningComplete=['PRODUCT_LOCATION','REQUIRED_MEANING','OBSERVED_MEANING','EVIDENCE_BASED_COMPARISON'].every(key=>String(recordValue(result,key)||'').trim()),capabilityComplete=mode!=='UNAVAILABLE'&&Boolean(String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim()),rejectionEstablished=['REJECTED_INVALID','VIOLATED','REJECTED'].includes(upper(recordValue(result,'APPLICATION_DETERMINATION')||recordValue(result,'ACTUAL_RESULT')||recordValue(result,'OBSERVED_RESULT'))),classSatisfied={CANONICAL_EVIDENCE:evidence.length>0,CANONICAL_OR_EXACT_EVIDENCE:hasEvidence,OBSERVATION_AND_ENTAILMENT:sufficientObservations.length>0,APPLICATION_VERIFIED_BYTES:verifiedArtifacts.length>0||Boolean(matchingProductArtifact)||externalArtifactInputBindingSatisfied,HUMAN_OBSERVATION:hasHuman,EXACT_ARTIFACT_BOUND_INSPECTION_RESERVATION:mode==='HUMAN_INSPECTION'&&!reasons.some(reason=>/inspection evidence is not bound through an accepted current reservation/i.test(reason)),EXACT_RESERVATION_PACKAGE_ARTIFACT_INPUT_SET:externalArtifactInputBindingSatisfied,EXTERNAL_SYSTEM_ATTESTATION:hasVerifiedExternal,EXTERNAL_SYSTEM_OUTPUT:mode==='EXTERNAL_SYSTEM'&&sufficientObservations.length>0,MEANING_COMPARISON:meaningComplete,CAPABILITY_EXECUTION:capabilityComplete&&sufficientObservations.length>0,CAPABLE_EXECUTION:mode!=='UNAVAILABLE',NATIVE_APPLICATION_RESULT:result?.source==='APPLICATION_TEST_RUNTIME',EQUIVALENT_SEMANTIC_COVERAGE:semantic.ready,REJECTION_EVIDENCE:rejectionEstablished&&sufficientObservations.length>0};for(const evidenceClass of proofContract.evidenceClasses)if(!classSatisfied[evidenceClass])reasons.push('Required evidence class '+evidenceClass+' is not established by the current result.');}
  return {sufficient:reasons.length===0,reasons:[...new Set(reasons)],requiredEvidenceClasses:[...new Set(requiredEvidenceClasses)],presentEvidenceIds:[...new Set(evidence.map(e=>recordId(e,'evidenceRecords')).filter(Boolean))],verifiedArtifactIds:verifiedArtifacts.map(a=>recordId(a,'artifacts')),observationIds:[...new Set(observations.map(item=>recordId(item,'observationRecords')).filter(Boolean))],entailmentEvaluations:observationEvaluations,externalArtifactInputBindings,allowedEpistemicBases:allowedBases};
}

function applicationConsequenceChange(collection,record,field,value,derivationKey){return {origin:'APPLICATION_DERIVATION',jsonPointer:null,rawValueHash:null,normalizerUsed:'ACCEPTED_AGENT_CONSEQUENCE_NORMALIZATION',canonicalCollection:collection,canonicalRecordType:collection,canonicalRecordId:recordId(record,collection),canonicalField:field,relationshipTargetId:schema.RECORD_SCHEMAS[collection]?.relationships?.[field]?clone(value):null,evidenceIds:[...new Set(safe(record?.evidenceRefs).map(String))],normalizedValue:clone(value),temporaryResponseKey:record?.temporaryKey||null,derivationKey};}
function applyApplicationConsequences(record,collection,fields,relationships,changes,derivationPrefix){
  const definition=schema.RECORD_SCHEMAS[collection];if(!definition)throw new Error('Cannot normalize an unknown canonical collection: '+collection+'.');record.fields=record.fields&&typeof record.fields==='object'?record.fields:{};record.relationships=record.relationships&&typeof record.relationships==='object'?record.relationships:{};
  for(const [field,value] of Object.entries(fields||{})){const fieldDefinition=definition.fieldDefinitions?.[field];if(!fieldDefinition||fieldDefinition.producer!==schema.PRODUCER.APPLICATION)throw new Error(collection+'.'+field+' is not exclusively APPLICATION owned.');record.fields[field]=clone(value);record[field]=clone(value);changes.push(applicationConsequenceChange(collection,record,field,value,derivationPrefix+'.'+field));}
  for(const [field,value] of Object.entries(relationships||{})){const expected=definition.relationships?.[field],fieldDefinition=definition.fieldDefinitions?.[field];if(!expected||!fieldDefinition||fieldDefinition.producer!==schema.PRODUCER.APPLICATION)throw new Error(collection+'.'+field+' is not an application-owned canonical relationship.');record.relationships[field]=clone(value);record.fields[field]=clone(value);record[field]=clone(value);changes.push(applicationConsequenceChange(collection,record,field,value,derivationPrefix+'.relationship.'+field));}
  refreshRecordHashes(record,collection);return record;
}
function currentAcceptedEquivalenceReviews(project,propositionIds,scopeRule){
  const targets=new Set(safe(propositionIds).map(String)),source=scopeRule?recordsForScope(project,'propositionEquivalenceReviews',scopeRule):recordsForCurrentScope(project,'propositionEquivalenceReviews');return source.filter(review=>{if(upper(recordValue(review,'ACCEPTED_STATUS'))!=='ACCEPTED')return false;const ids=[...new Set([...relationshipIds(review,'PROPOSITION_A_ID'),...relationshipIds(review,'PROPOSITION_B_ID')])];return ids.length&&ids.every(id=>targets.has(id)||targets.size===1&&id===[...targets][0])&&ids.some(id=>targets.has(id));});
}
function normalizedSemanticDisposition(proposed,reviews){
  const requested=upper(proposed),values=[...new Set(safe(reviews).map(review=>upper(recordValue(review,'EQUIVALENCE_FINDING'))).filter(value=>['EQUIVALENT','PARTIAL','UNKNOWN','NOT_EQUIVALENT'].includes(value)))];if(values.includes('NOT_EQUIVALENT'))return 'NOT_EQUIVALENT';if(values.length!==1)return 'UNKNOWN';return requested&&requested!==values[0]?'UNKNOWN':values[0];
}
function acceptedChangeForCanonicalRecord(project,stage,canonicalRecordId,{excludeChangeId=null}={}){const matches=acceptedChanges(project,stage).filter(change=>change.changeId!==excludeChangeId&&safe(change.canonicalRecordIds).map(String).includes(String(canonicalRecordId)));return matches.at(-1)||null;}
function acceptedReviewIndependence(project,{stage,author,review}={}){
  const number=Number(stage),expectedReviewOperation=number===5?'APPLICABILITY_REVIEW':number===6?'SEMANTIC_REVIEW':'',authorContextId=String(author?.scope?.contextId||''),reviewContextId=String(review?.scope?.contextId||''),contexts=records(project,'freshContexts',{active:false}).filter(item=>isActiveRecord(item)&&Number(item.stage)===number),authorContext=contexts.find(item=>recordId(item,'freshContexts')===authorContextId),reviewContext=contexts.find(item=>recordId(item,'freshContexts')===reviewContextId),authorExternal=String(recordValue(authorContext,'EXTERNAL_CONTEXT_IDENTIFIER')||'').trim(),reviewExternal=String(recordValue(reviewContext,'EXTERNAL_CONTEXT_IDENTIFIER')||'').trim(),placeholder=value=>['','NONE','UNKNOWN','UNASSIGNED','PENDING','NOT APPLICABLE'].includes(upper(value)),reasons=[];
  if(upper(author?.operation||'')!=='COMPLETE')reasons.push('The semantic subject is not controlled by an accepted COMPLETE author/compiler operation.');if(!expectedReviewOperation||upper(review?.operation||'')!==expectedReviewOperation)reasons.push('The independent review is not controlled by the required '+(expectedReviewOperation||'review')+' operation.');if(!authorContext||authorContext?.source==='HUMAN_REVIEWER_CONTEXT')reasons.push('The accepted author/compiler change is not bound to one current non-reviewer context.');if(!reviewContext||reviewContext?.source!=='HUMAN_REVIEWER_CONTEXT')reasons.push('The accepted independent-review change is not bound to one current reviewer context.');if(placeholder(authorExternal)||placeholder(reviewExternal))reasons.push('The author and reviewer external context identifiers are not both recorded.');else if(upper(authorExternal)===upper(reviewExternal))reasons.push('The reviewer reuses the author external context identifier.');return {complete:reasons.length===0,reasons,authorContext,reviewContext,authorExternal,reviewExternal};
}
function independentReviewBinding(project,{stage,subjectId,reviewChangeId,reviewProposalId,reviewPromptId}){
  const author=acceptedChangeForCanonicalRecord(project,stage,subjectId,{excludeChangeId:reviewChangeId}),review=acceptedChanges(project,stage).find(change=>change.changeId===reviewChangeId&&change.proposalId===reviewProposalId),authorContext=String(author?.scope?.contextId||''),reviewContext=String(review?.scope?.contextId||''),reasons=[];
  if(!author||!review)reasons.push('The semantic review does not bind distinct accepted author and reviewer changes.');
  if(author?.proposalId===reviewProposalId)reasons.push('A response cannot review semantic content proposed in the same response.');
  if(!String(author?.promptId||'')||String(author?.promptId||'')===String(reviewPromptId||''))reasons.push('The semantic review reuses the author prompt identity.');
  if(!authorContext||!reviewContext||authorContext===reviewContext)reasons.push('The semantic review lacks distinct application-recorded author and reviewer contexts.');reasons.push(...acceptedReviewIndependence(project,{stage,author,review}).reasons);
  return {complete:reasons.length===0,reasons,author,review,authorContext,reviewContext};
}
function testSemanticSubjectPayload(kind,record){
  if(kind==='TEST')return {targetPropositionIds:propositionIdsFor(record),testPropositionText:recordValue(record,'TEST_PROPOSITION_TEXT')??null,testedScope:recordValue(record,'TESTED_SCOPE')??null,positiveResultMeaning:recordValue(record,'POSITIVE_RESULT_MEANING')??null,negativeResultMeaning:recordValue(record,'NEGATIVE_RESULT_MEANING')??null,semanticCoverageDisposition:recordValue(record,'SEMANTIC_COVERAGE_DISPOSITION')??null};
  return {targetPropositionIds:relationshipIds(record,'TARGET_PROPOSITION_ID'),expression:expressionPayload(record),semanticRationale:recordValue(record,'SEMANTIC_RATIONALE')??null};
}
function applicabilitySubjectPayload(project,record){const subjectId=relationshipIds(record,'SUBJECT_ID')[0]||recordValue(record,'SUBJECT_ID')||null,activationPropositionId=relationshipIds(record,'ACTIVATION_PROPOSITION_ID')[0]||null,propositions=recordsForCurrentScope(project,'propositions'),subject=propositions.find(item=>recordId(item,'propositions')===String(subjectId||'')),activation=propositions.find(item=>recordId(item,'propositions')===String(activationPropositionId||''));return {subjectId,subjectSemanticSha256:String(recordValue(subject,'CONTENT_SHA256')||subject?.contentSha256||subject?.recordSha256||''),requirementId:relationshipIds(subject,'REQUIREMENT_ID')[0]||null,activationPropositionId,activationSemanticSha256:String(recordValue(activation,'CONTENT_SHA256')||activation?.contentSha256||activation?.recordSha256||''),proposal:recordValue(record,'APPLICABILITY_PROPOSAL')??null,normativeClassificationProposal:recordValue(record,'NORMATIVE_CLASSIFICATION_PROPOSAL')??null,reasoning:recordValue(record,'REASONING')??null,evidenceIds:[...new Set([...relationshipIds(record,'EVIDENCE_IDS'),...safe(record?.evidenceRefs).map(String)])].sort()};}
function evidenceFreshnessForIds(project,evidenceIds,scopeRule=null){const source=scopeRule?recordsForScope(project,'evidenceRecords',scopeRule):recordsForCurrentScope(project,'evidenceRecords'),byId=new Map(source.map(item=>[recordId(item,'evidenceRecords'),item])),selected=safe(evidenceIds).map(String).map(id=>byId.get(id)).filter(Boolean),statuses=selected.map(item=>freshnessEvaluation(item).status);if(selected.length!==safe(evidenceIds).length||!statuses.length||statuses.includes('UNKNOWN'))return 'UNKNOWN';if(statuses.includes('EXPIRED'))return 'EXPIRED';if(statuses.every(status=>status==='NOT_APPLICABLE'))return 'NOT_APPLICABLE';return statuses.every(status=>['CURRENT','NOT_APPLICABLE'].includes(status))?'CURRENT':'UNKNOWN';}
function independentReviewSubjectScope(scopeRule){if(!scopeRule)return null;const bounded=clone(scopeRule);delete bounded.contextId;delete bounded.runId;delete bounded.projectRevision;return bounded;}
function acceptedTestSemanticReviews(project,{subjectKind,subjectId,scopeRule=null}={}){const bounded=independentReviewSubjectScope(scopeRule),source=bounded?recordsForScope(project,'testSemanticReviews',bounded):recordsForCurrentScope(project,'testSemanticReviews');return source.filter(review=>upper(recordValue(review,'ACCEPTED_STATUS'))==='ACCEPTED'&&upper(recordValue(review,'SUBJECT_KIND'))===upper(subjectKind)&&relationshipIds(review,subjectKind==='TEST'?'TEST_ID':'PROOF_EXPRESSION_ID').includes(String(subjectId)));}
function acceptedApplicabilityReviews(project,applicabilityId,scopeRule=null){const bounded=independentReviewSubjectScope(scopeRule),source=bounded?recordsForScope(project,'applicabilityReviews',bounded):recordsForCurrentScope(project,'applicabilityReviews');return source.filter(review=>upper(recordValue(review,'ACCEPTED_STATUS'))==='ACCEPTED'&&upper(recordValue(review,'REVIEW_FINDING'))==='ACCEPTED'&&relationshipIds(review,'APPLICABILITY_ID').includes(String(applicabilityId)));}
function propositionsForResultNormalization(project,result,test,scopeRule){
  const direct=propositionIdsFor(result),available=scopeRule?recordsForScope(project,'propositions',scopeRule):recordsForCurrentScope(project,'propositions'),availableIds=new Set(available.map(item=>recordId(item,'propositions')));if(direct.length){const invalid=direct.filter(id=>!availableIds.has(id));if(invalid.length)throw new Error('External result targets a missing or stale proposition: '+invalid.join(', ')+'.');return direct;}
  const fromTest=propositionIdsFor(test);if(fromTest.length)return fromTest;
  let requirementIdValue=resultRequirementId(project,result);if(!requirementIdValue){const regressionId=relationshipIds(result,'REG_ID')[0],regression=regressionId?records(project,'regressions').find(item=>recordId(item,'regressions')===regressionId&&isActiveRecord(item)):null;requirementIdValue=regression?String(recordValue(regression,'REQ_ID')||regression.relationships?.REQ_ID||''):'';}
  return available.filter(item=>relationshipIds(item,'REQUIREMENT_ID').concat(relationshipIds(item,'REQ_ID')).includes(requirementIdValue)).map(item=>recordId(item,'propositions'));
}
function externalResponseOperationBinding(project,{proposalId,stage,operation,promptId,scopeRule}={}){
  const proposal=safe(project.projectData?.responseProposals).find(item=>String(item?.proposalId||'')===String(proposalId||'')&&!item.invalidatedBy),binding=proposal?.envelope?.operationBinding||proposal?.operationBinding||null;if(!proposal||!binding||typeof binding!=='object'||Array.isArray(binding))throw new Error('External result normalization requires the exact current response proposal and application-issued operation binding.');
  const reservationId=String(binding.operationReservationId||''),reservation=records(project,'operationReservations',{active:false}).find(item=>recordId(item,'operationReservations')===reservationId);if(!reservation)throw new Error('External result operation binding references an unknown reservation.');
  const status=upper(recordValue(reservation,'STATUS')||reservation.status),packageId=String(recordValue(reservation,'PACKAGE_ID')||''),manifestHash=String(recordValue(reservation,'PACKAGE_MANIFEST_HASH')||'').toLowerCase(),nonce=String(recordValue(reservation,'CHALLENGE_NONCE')||''),scopeHash=String(recordValue(reservation,'SCOPE_HASH')||''),slot=String(recordValue(reservation,'TARGET_SLOT')||''),reservedPrompt=String(recordValue(reservation,'PROMPT_IDENTITY')||''),proposalScope=proposal.envelope?.scope||scopeRule||{};
  if(!['RESERVED','ACTIVE','RESUMED','ACCEPTED'].includes(status))throw new Error('External result operation reservation is not current or accepted.');if(String(recordValue(reservation,'JOB_ID')||'')!==String(project.job?.JOB_ID||'')||Number(recordValue(reservation,'STAGE'))!==Number(stage)||upper(recordValue(reservation,'OPERATION'))!==upper(operation||'COMPLETE'))throw new Error('External result reservation targets the wrong job, stage, or operation.');if(String(binding.packageId||'')!==packageId||String(binding.challengeNonce||'')!==nonce||String(binding.targetSlot||'')!==slot||Number(binding.projectRevision)!==Number(recordValue(reservation,'EXPECTED_REVISION'))||String(binding.scopeSha256||'')!==scopeHash||scopeHash!==hash.sha256Value(proposalScope))throw new Error('External result package, reservation nonce, revision, target slot, or scope binding is stale or mismatched.');if(!reservedPrompt||String(proposal.promptId||'')!==reservedPrompt||promptId&&String(promptId)!==reservedPrompt)throw new Error('External result controlling prompt does not match its frozen reservation.');
  const descriptor=recordValue(reservation,'PACKAGE_DESCRIPTOR');if(!descriptor||typeof descriptor!=='object'||Array.isArray(descriptor)||!/^[a-f0-9]{64}$/.test(manifestHash)||packageId!==`PACKAGE-${manifestHash.slice(0,24).toUpperCase()}`)throw new Error('External result reservation does not contain one exact frozen canonical package descriptor.');const validated=validateReservationPackageDescriptor(project,{descriptor,packageId,packageManifestHash:manifestHash,stage,operation,targetSlot:slot,scope:recordValue(reservation,'SCOPE')||proposalScope}),storedInputs=safe(recordValue(reservation,'REQUIRED_ARTIFACT_INPUTS')),storedSetSha=String(recordValue(reservation,'REQUIRED_ARTIFACT_INPUT_SET_SHA256')||'');if(hash.sha256Value(storedInputs)!==hash.sha256Value(validated.artifactInputs)||storedSetSha!==validated.artifactInputSetSha256)throw new Error('External result reservation artifact inputs no longer match the frozen package descriptor.');
  return {proposal,binding:clone(binding),reservation,reservationId,packageId,packageManifestHash:manifestHash,challengeNonce:nonce,targetSlot:slot,scopeSha256:scopeHash,artifactInputs:validated.artifactInputs,artifactInputSetSha256:validated.artifactInputSetSha256,descriptor:validated.descriptor};
}
function externalResultInputIdentities(project,{result,collection,test,runId,productId,evidenceIds,proposalId,stage,operation,promptId}){
  const scopeRule=clone(result.scope||{}),operationBinding=externalResponseOperationBinding(project,{proposalId,stage,operation,promptId,scopeRule}),identities=[],allowedArtifactIds=new Set(operationBinding.artifactInputs.map(item=>item.artifactId)),currentArtifacts=new Map(records(project,'artifacts').filter(isActiveRecord).map(item=>[recordId(item,'artifacts'),item])),run=runId?records(project,'runs').find(item=>recordId(item,'runs')===runId&&isActiveRecord(item)):null;if(run){const output=String(recordValue(run,'COMPLETE_OUTPUT')||'');identities.push({kind:'RUN_OUTPUT',runId,sha256:hash.sha256Text(output),canonicalByteLength:new TextEncoder().encode(output).byteLength});}
  for(const expected of operationBinding.artifactInputs){const artifact=currentArtifacts.get(expected.artifactId);if(!artifact)throw new Error('External result frozen package references missing or stale artifact '+expected.artifactId+'.');const actual={artifactId:expected.artifactId,filename:String(recordValue(artifact,'FILENAME')||''),version:String(recordValue(artifact,'VERSION')||''),byteSize:Number(recordValue(artifact,'BYTE_SIZE')),hashAlgorithm:String(recordValue(artifact,'HASH_ALGORITHM')||'SHA-256'),sha256:String(recordValue(artifact,'SHA256')||'').toLowerCase(),role:String(recordValue(artifact,'ROLE')||''),availability:upper(recordValue(artifact,'AVAILABILITY')||''),disclosureClassification:upper(recordValue(artifact,'DISCLOSURE_CLASSIFICATION')||'UNKNOWN'),productId:String(artifact.scope?.productId||recordValue(artifact,'PRODUCT_ID')||artifact.relationships?.PRODUCT_ID||''),runId:String(artifact.scope?.runId||recordValue(artifact,'RUN_ID')||artifact.relationships?.RUN_ID||'')};if(hash.sha256Value(actual)!==hash.sha256Value(expected)||actual.availability!=='BYTES_PERSISTED_AND_VERIFIED')throw new Error('External result frozen artifact '+expected.artifactId+' is no longer the exact current application-verified identity and bytes.');identities.push({kind:'PACKAGE_ARTIFACT',...actual});}
  if(test){const plan=testExecutionPlan(project).items.find(item=>item.testId===recordId(test,'tests')),required=[...new Set(safe(plan?.requiredArtifactIds).map(String))],missing=required.filter(artifactId=>!allowedArtifactIds.has(artifactId));if(missing.length)throw new Error('External result package omits required test artifact inputs: '+missing.join(', ')+'.');const descriptorTestIds=safe(operationBinding.descriptor?.testIds).map(String);if(!descriptorTestIds.includes(recordId(test,'tests')))throw new Error('External result package is not frozen for target test '+recordId(test,'tests')+'.');}
  const evidence=safe(evidenceIds).map(id=>records(project,'evidenceRecords').find(item=>recordId(item,'evidenceRecords')===id)).filter(Boolean),attached=evidence.map(item=>String(recordValue(item,'ATTACHMENT_ID')||item.relationships?.ATTACHMENT_ID||'')).filter(value=>value&&value!=='UNKNOWN');for(const artifactId of attached){const artifact=currentArtifacts.get(artifactId);if(!artifact)throw new Error('External result evidence references a missing or stale artifact '+artifactId+'.');if(allowedArtifactIds.size&&!allowedArtifactIds.has(artifactId))throw new Error('External result evidence references wrong artifact '+artifactId+'.');if(upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED')throw new Error('External result evidence artifact '+artifactId+' does not have verified bytes.');}
  return {...operationBinding,identities,allowedArtifactIds:[...allowedArtifactIds].sort(),attachedArtifactIds:attached};
}
function proofObligationScopeProjection(scopeRule={}){
  const projected={};for(const key of SCOPE_KEYS){const value=scopeValue(scopeRule?.[key]);if(value!==null)projected[key]=value;}return projected;
}
function normalizedAttestationBinding(value){if(!value||typeof value!=='object'||Array.isArray(value))return null;const allowed=['schema','resultId','observationId','testId','propositionId','inputIdentitySha256'];if(Object.keys(value).some(key=>!allowed.includes(key)))return null;return {schema:String(value.schema||''),resultId:String(value.resultId||''),observationId:String(value.observationId||''),testId:String(value.testId||''),propositionId:String(value.propositionId||''),inputIdentitySha256:String(value.inputIdentitySha256||'').toLowerCase()};}
function registeredAttestationEvidence(record,expectedBinding=null){
  if(!record||!String(recordValue(record,'ATTESTATION_ID')||'').trim()||!String(recordValue(record,'ATTESTATION_VERIFIER_ID')||'').trim()||upper(recordValue(record,'ATTESTATION_VERIFICATION_STATUS'))!=='VERIFIED'||record?.source!=='APPLICATION_ATTESTATION_VERIFICATION')return false;
  if(!expectedBinding)return false;
  const actual=normalizedAttestationBinding(recordValue(record,'ATTESTATION_BINDING')),expected=normalizedAttestationBinding({schema:'closed-loop-external-attestation-binding/1',...expectedBinding});return Boolean(actual&&expected&&hash.sha256Value(actual)===hash.sha256Value(expected));
}
function externalEvidenceFreshness(evidence,contract,{proposalId,scopeRule}={}){
  const required=contract?.freshness?.status||'UNKNOWN',statuses=[],reasons=[];
  for(const item of evidence){const fields=recordFields(item),declared=Object.prototype.hasOwnProperty.call(fields,'FRESHNESS_STATUS')||Object.prototype.hasOwnProperty.call(item||{},'FRESHNESS_STATUS'),status=controlledFreshness(recordValue(item,'FRESHNESS_STATUS')),sameAcceptedOperation=Boolean(proposalId&&String(item?.sourceProposalId||'')===String(proposalId)&&isActiveRecord(item)&&scopeMatches(item,scopeRule||{},requiredVersionScopeKeys('evidenceRecords').filter(key=>(scopeRule||{})[key]!==undefined&&(scopeRule||{})[key]!==null)));if(status==='EXPIRED'||status==='CURRENT'||status==='NOT_APPLICABLE')statuses.push(status);else if(!declared&&sameAcceptedOperation){statuses.push('CURRENT');reasons.push('Freshness is current only relative to the exact accepted operation and current application scope; no trusted external-time claim is made.');}else{statuses.push('UNKNOWN');reasons.push('Freshness could not be established from the declared policy and available application-controlled scope basis.');}}
  let freshnessStatus='UNKNOWN';if(required==='NOT_APPLICABLE')freshnessStatus='NOT_APPLICABLE';else if(required==='CURRENT'&&statuses.length&&statuses.every(status=>status==='CURRENT'))freshnessStatus='CURRENT';else if(required==='CURRENT_OR_NOT_APPLICABLE'&&statuses.length&&statuses.every(status=>['CURRENT','NOT_APPLICABLE'].includes(status)))freshnessStatus=statuses.includes('CURRENT')?'CURRENT':'NOT_APPLICABLE';if(statuses.includes('EXPIRED'))freshnessStatus='EXPIRED';return {freshnessStatus,statuses,reasons:[...new Set(reasons)],policy:{status:required}};
}
function externalEvidenceTrust(contractSource,evidence,{semanticObservation=false,attestationBinding=null,proposalId=null,scopeRule=null}={}){
  const contract=proofContractForTest(contractSource),attestation=semanticObservation?null:evidence.find(item=>registeredAttestationEvidence(item,attestationBinding))||null,basis=semanticObservation?'SELF_ASSERTED':attestation?'VERIFIED_EXTERNAL':'SELF_ASSERTED',origin=semanticObservation?'AGENT_SEMANTIC_OBSERVATION':attestation?'VERIFIED_EXTERNAL_OBSERVATION':'EXTERNAL_CLAIM',freshness=externalEvidenceFreshness(evidence,contract,{proposalId,scopeRule});return {contract,attestation,basis,origin,...freshness};
}
function missingRequiredResultDimensions(contract,{scopeRule,runId,productId,result}){const values={RUN_ID:runId,ITERATION_ID:scopeRule?.iterationId,CANDIDATE_ID:scopeRule?.candidateId,PRODUCT_ID:productId,ENVIRONMENT_MANIFEST_ID:recordValue(result,'ENVIRONMENT_MANIFEST_ID')||scopeRule?.environmentManifestId};return contract.runDimensions.filter(name=>scopeValue(values[name])===null);}
function normalizeExternalResultConsequences(project,{stage,operation,proposalId,rawResponseId,promptId,changeId,changes}){
  const routes=[];if(Number(stage)===12||([17,19].includes(Number(stage))&&upper(operation)==='VERIFY'))routes.push('verification');if(Number(stage)===22)routes.push('deterministicResults');if(Number(stage)===23)routes.push('meaningResults');if(Number(stage)===24)routes.push('adversarialResults');if(Number(stage)===25)routes.push('representationInspections');if(!routes.length)return;
  const observations=records(project,'observationRecords').filter(item=>item.sourceProposalId===proposalId&&isActiveRecord(item)),entailments=records(project,'entailmentReviews').filter(item=>item.sourceProposalId===proposalId&&isActiveRecord(item)),linkedEvidence=new Map();
  for(const collection of routes)for(const result of records(project,collection).filter(item=>item.sourceProposalId===proposalId&&isActiveRecord(item))){
    const resultId=recordId(result,collection),scopeRule=clone(result.scope||{}),proofScope=proofObligationScopeProjection(scopeRule),semanticSubjectScope=proofScope,testId=relationshipIds(result,'TEST_ID')[0]||null,explicitResultPropositionIds=propositionIdsFor(result),semanticModelActive=explicitResultPropositionIds.length>0||recordsForScope(project,'propositions',semanticSubjectScope).length>0||recordsForScope(project,'proofObligations',proofScope).length>0;
    if(!semanticModelActive){if(collection==='representationInspections')throw new Error('Representation inspection '+resultId+' lacks the current proposition and proof model required for normalized evidence.');continue;}
    if(collection==='representationInspections'&&!testId)throw new Error('Representation inspection '+resultId+' must be application-bound to one current representation test before it can affect the Stage 25 gate.');
    const test=testId?recordsForScope(project,'tests',semanticSubjectScope).find(item=>recordId(item,'tests')===testId):null;if(testId&&!test)throw new Error('External result '+resultId+' targets a missing or stale test '+testId+'.');const explicitPropositionIds=propositionIdsFor(result,test),propositionIds=propositionsForResultNormalization(project,result,test,semanticSubjectScope);if(!propositionIds.length&&!explicitPropositionIds.length){if(collection==='representationInspections')throw new Error('Representation inspection '+resultId+' cannot resolve one current target proposition.');continue;}if(propositionIds.length!==1)throw new Error('External result '+resultId+' must resolve to exactly one current proposition; resolved '+propositionIds.length+'.');
    const propositionId=propositionIds[0],releaseProofTest=Boolean(test&&releaseBearingTest(test)&&!['ADVISORY','NEGATIVE_ONLY'].includes(testRole(test))),adversarialRegressionProof=Boolean(collection==='adversarialResults'&&!test&&relationshipIds(result,'REG_ID').length),proofBindingRequired=releaseProofTest||adversarialRegressionProof,obligations=recordsForScope(project,'proofObligations',proofScope).filter(item=>relationshipIds(item,'PROPOSITION_ID').includes(propositionId)),obligation=proofBindingRequired?(obligations.length===1?obligations[0]:null):null;
    if(proofBindingRequired&&obligations.length!==1)throw new Error('External result '+resultId+' must resolve to exactly one current proof obligation; resolved '+obligations.length+'.');if(releaseProofTest&&!relationshipIds(obligation,'REQUIRED_TEST_IDS').includes(testId))throw new Error('External result '+resultId+' targets test '+testId+', which is not frozen in the current proof obligation.');if(releaseProofTest){const semantic=semanticReviewAssessment(project,{subjectKind:'TEST',subject:test,scopeRule:semanticSubjectScope}),obligationContract=proofContractForTest(obligation);if(!semantic.ready)throw new Error('External result '+resultId+' cannot bind release proof because its exact test semantics lack an accepted independent EQUIVALENT review: '+semantic.reasons.join(' '));if(!obligationContract.complete)throw new Error('External result '+resultId+' resolves to an incomplete proof-obligation contract: '+obligationContract.reasons.join(' '));}
    const observationIds=relationshipIds(result,'OBSERVATION_ID'),entailmentIds=relationshipIds(result,'ENTAILMENT_ID');if(observationIds.length!==1||entailmentIds.length!==1)throw new Error('External result '+resultId+' requires exactly one response-local observation and one response-local entailment binding.');const observation=observations.find(item=>recordId(item,'observationRecords')===observationIds[0]),entailment=entailments.find(item=>recordId(item,'entailmentReviews')===entailmentIds[0]);if(!observation||!entailment)throw new Error('External result '+resultId+' observation or entailment binding is missing, stale, or from another response.');if(!relationshipIds(entailment,'OBSERVATION_ID').includes(recordId(observation,'observationRecords')))throw new Error('External result '+resultId+' entailment targets the wrong observation.');if(!relationshipIds(entailment,'PROPOSITION_ID').includes(propositionId))throw new Error('External result '+resultId+' entailment targets the wrong proposition.');
    const runId=relationshipIds(result,'RUN_ID')[0]||null,productId=relationshipIds(result,'PRODUCT_ID')[0]||scopeRule.productId||null;if(collection==='verification'&&!runId)throw new Error('External verification '+resultId+' is not bound to an exact run.');if(runId&&scopeRule.runId&&runId!==String(scopeRule.runId))throw new Error('External verification '+resultId+' is bound to the wrong run.');if(['deterministicResults','meaningResults','adversarialResults','representationInspections'].includes(collection)){const expectedProduct=String(scopeRule.productId||project.job?.CURRENT_PRODUCT_ID||'');if(!productId||expectedProduct&&productId!==expectedProduct)throw new Error('External result '+resultId+' is bound to the wrong or missing product.');}
    const externalObserved=recordValue(observation,'EXTERNAL_OBSERVED_VALUE'),externalLocation=String(recordValue(observation,'EXTERNAL_OBSERVED_LOCATION')||'').trim(),externalMethod=String(recordValue(observation,'EXTERNAL_METHOD_OR_TOOL')||'').trim();if(!externalObserved||typeof externalObserved!=='object'||Array.isArray(externalObserved)||!externalLocation||!externalMethod)throw new Error('External result '+resultId+' requires a complete response-local external observation value, observed location, and method or tool identity.');
    const evidenceIds=[...new Set([...safe(result.evidenceRefs).map(String),...safe(observation.evidenceRefs).map(String),...safe(entailment.evidenceRefs).map(String)])];if(!evidenceIds.length)throw new Error('External result '+resultId+' has no canonical evidence binding.');const evidence=evidenceIds.map(id=>records(project,'evidenceRecords').find(item=>recordId(item,'evidenceRecords')===id&&isActiveRecord(item)));if(evidence.some(item=>!item))throw new Error('External result '+resultId+' references missing or stale canonical evidence.');const input=externalResultInputIdentities(project,{result,collection,test,runId,productId,evidenceIds,proposalId,stage,operation,promptId}),observationId=recordId(observation,'observationRecords'),attestationBinding={resultId,observationId,testId:testId||'',propositionId,inputIdentitySha256:hash.sha256Value(input.identities)},contractSource=releaseProofTest?test:adversarialRegressionProof?obligation:test,trust=externalEvidenceTrust(contractSource,evidence,{semanticObservation:collection==='meaningResults',attestationBinding,proposalId,scopeRule});if(proofBindingRequired&&!trust.contract.complete)throw new Error('External result '+resultId+' has an incomplete proof contract: '+trust.contract.reasons.join(' '));const missingDimensions=missingRequiredResultDimensions(trust.contract,{scopeRule,runId,productId,result});if(proofBindingRequired&&missingDimensions.length)throw new Error('External result '+resultId+' is missing required result dimensions: '+missingDimensions.join(', ')+'.');const origin=trust.origin,basis=trust.basis,freshnessStatus=trust.freshnessStatus,relation=upper(recordValue(entailment,'ENTAILMENT_FINDING'));if(!ENTAILMENT_RELATIONS.includes(relation))throw new Error('External result '+resultId+' has an invalid entailment finding.');const claimed=upper(recordValue(result,'DETERMINATION')||recordValue(result,'RESULT')||recordValue(result,'ACTUAL_RESULT')||externalObserved?.determination||externalObserved?.status||externalObserved?.result),applicationDetermination=relation==='REFUTES'?'VIOLATED':relation==='ESTABLISHES'?(claimed==='VIOLATED'?'UNDETERMINED':claimed==='SATISFIED'?'SATISFIED':'UNDETERMINED'):'UNDETERMINED',entailmentId=recordId(entailment,'entailmentReviews'),actor=String(recordValue(result,'VERIFIER')||recordValue(entailment,'REVIEWER_ID')||externalMethod||'EXTERNAL_SUBMITTER'),deferred=deferredFailureTestForCurrentTest(project,testId,Number(stage),scopeRule),deferredMutationId=deferred?.contract?.mutationId||null,proofObligationId=deferred?.contract?.proofObligationId||recordId(obligation,'proofObligations')||null,inspectionArtifactId=collection==='representationInspections'?relationshipIds(result,'ARTIFACT_ID')[0]||null:null;if(collection==='representationInspections'&&(!inspectionArtifactId||!input.allowedArtifactIds.includes(inspectionArtifactId)))throw new Error('Representation inspection '+resultId+' is not bound to one exact current product artifact.');
    applyApplicationConsequences(observation,'observationRecords',{ORIGIN_CLASS:origin,SUBMITTING_ACTOR:actor,SUBJECT_DESCRIPTION:'External observation for '+resultId+' and proposition '+propositionId+'.',ARTIFACT_ID:inspectionArtifactId||(input.allowedArtifactIds.length===1?input.allowedArtifactIds[0]:null),RUN_ID:runId,PRODUCT_ID:productId,RESULT_ID:resultId,TEST_ID:testId||null,MUTATION_ID:deferredMutationId,NATIVE_OBSERVED_VALUE:null,NATIVE_OBSERVED_LOCATION:null,NATIVE_METHOD_OR_TOOL:null,INPUT_IDENTITIES_AND_HASHES:input.identities,OUTPUT_IDENTITIES_AND_HASHES:input.attachedArtifactIds.map(artifactId=>{const artifact=records(project,'artifacts').find(item=>recordId(item,'artifacts')===artifactId);return {artifactId,byteSize:Number(recordValue(artifact,'BYTE_SIZE')),sha256:String(recordValue(artifact,'SHA256')||'').toLowerCase()};}),EPISTEMIC_BASIS:basis,FRESHNESS_STATUS:freshnessStatus,SOURCE_EVIDENCE_IDS:evidenceIds,CURRENT_SCOPE:scopeRule,RAW_OR_EXECUTION_PROVENANCE:{sourceProposalId:proposalId,rawResponseId,promptId,acceptedChangeId:changeId,packageId:input.packageId,operationReservationId:input.reservationId,challengeNonce:input.challengeNonce,packageManifestHash:input.packageManifestHash,targetSlot:input.targetSlot,scopeSha256:input.scopeSha256,requiredArtifactInputSetSha256:input.artifactInputSetSha256,requiredArtifactIds:input.allowedArtifactIds,originPreserved:true,externalAttributionUnverified:!trust.attestation,registeredAttestationId:recordValue(trust.attestation,'ATTESTATION_ID')||null,freshnessPolicy:trust.policy,freshnessEvaluationReasons:trust.reasons},STATUS:['UNKNOWN','EXPIRED'].includes(freshnessStatus)?'BLOCKED':'CURRENT'},{...(inspectionArtifactId?{ARTIFACT_ID:inspectionArtifactId}:{}),...(runId?{RUN_ID:runId}:{}),...(productId?{PRODUCT_ID:productId}:{}),...(testId?{TEST_ID:testId}:{}),...(deferredMutationId?{MUTATION_ID:deferredMutationId}:{}),SOURCE_EVIDENCE_IDS:evidenceIds},changes,'externalEvidence.observation');
    applyApplicationConsequences(entailment,'entailmentReviews',{OBSERVATION_ID:observationId,PROPOSITION_ID:propositionId,PROOF_OBLIGATION_ID:proofObligationId,PROOF_LEAF_KEY:testId||resultId,EVIDENCE_IDS:evidenceIds,ACCEPTED_RELATION:relation,CURRENT_SCOPE:scopeRule,STATUS:['UNKNOWN','EXPIRED'].includes(freshnessStatus)?'BLOCKED':'CURRENT',TRUTH_VALUE:relation==='ESTABLISHES'?'TRUE':relation==='REFUTES'?'FALSE':'UNKNOWN',EPISTEMIC_BASIS:basis,CURRENT_SCOPE_STATUS:'CURRENT',FRESHNESS_STATUS:freshnessStatus,CONTRADICTION_STATUS:'CLEAR',REASONS:[...(relation==='ESTABLISHES'||relation==='REFUTES'?[]:['The accepted semantic relation does not establish or refute the proposition.']),...(['UNKNOWN','EXPIRED'].includes(freshnessStatus)?['The evidence contract freshness requirement is not satisfied by the available application-controlled basis.']:[])],SUPPORTING_EVIDENCE_IDS:evidenceIds,DERIVATION_ID:changeId},{OBSERVATION_ID:observationId,PROPOSITION_ID:propositionId,PROOF_OBLIGATION_ID:proofObligationId,EVIDENCE_IDS:evidenceIds,SUPPORTING_EVIDENCE_IDS:evidenceIds},changes,'externalEvidence.entailment');
    const resultFields=collection==='deterministicResults'?{PRODUCT_ID:productId,PRODUCT_SHA256:hash.sha256Value(input.identities),TEST_ID:testId,APPLICATION_DETERMINATION:applicationDetermination,RUNTIME_VERSION:(trust.attestation?'EXTERNAL_ATTESTED:':'EXTERNAL_UNVERIFIED:')+String(recordValue(result,'TOOL_AND_VERSION')||recordValue(observation,'EXTERNAL_METHOD_OR_TOOL')||'UNKNOWN'),TEST_SPEC_SHA256:String(recordValue(test,'EXECUTABLE_SPEC_SHA256')||test?.recordSha256||test?.sha256||'UNKNOWN'),INPUT_ARTIFACT_IDENTITIES:JSON.stringify(input.identities),RUNTIME_OBSERVATIONS:JSON.stringify(externalObserved??null),EXPECTED_RESULT:String(recordValue(test,'EXPECTED_RESULT')||'UNKNOWN'),ACTUAL_RESULT:typeof externalObserved==='string'?externalObserved:JSON.stringify(externalObserved??null),DETERMINATION:applicationDetermination,EVIDENCE:evidenceIds.join(', '),PROPOSITION_ID:propositionId,PROOF_OBLIGATION_ID:proofObligationId,OBSERVATION_ID:observationId,ENTAILMENT_ID:entailmentId,OBSERVATION_ORIGIN:origin,EPISTEMIC_BASIS:basis,INPUT_BINDING_BASIS:trust.attestation?'VERIFIED_TOOL_ATTESTED':'SELF_ASSERTED',RUNTIME_BUILD_IDENTITY:trust.attestation?'EXTERNAL_ATTESTED':'EXTERNAL_UNVERIFIED',TEST_WORKER_SHA256:'NOT_APPLICABLE_EXTERNAL_EXECUTOR',WORKER_PROTOCOL_VERSION:'NOT_APPLICABLE_EXTERNAL_EXECUTOR',PARSER_OR_ADAPTER_IDENTITIES:[],ENVIRONMENT_MANIFEST_ID:'UNKNOWN',FRESHNESS_STATUS:freshnessStatus}:{...(collection==='representationInspections'?{TEST_ID:testId||null}:{}),PROPOSITION_ID:propositionId,PROOF_OBLIGATION_ID:proofObligationId,OBSERVATION_ID:observationId,ENTAILMENT_ID:entailmentId,APPLICATION_DETERMINATION:applicationDetermination,EPISTEMIC_BASIS:basis,FRESHNESS_STATUS:freshnessStatus},resultRelationships={PROPOSITION_ID:propositionId,PROOF_OBLIGATION_ID:proofObligationId,OBSERVATION_ID:observationId,ENTAILMENT_ID:entailmentId,...(collection==='deterministicResults'?{PRODUCT_ID:productId,TEST_ID:testId}:collection==='representationInspections'?{TEST_ID:testId}:{})};applyApplicationConsequences(result,collection,resultFields,resultRelationships,changes,'externalEvidence.result');
    for(const evidenceRecord of evidence){const evidenceId=recordId(evidenceRecord,'evidenceRecords');if(!linkedEvidence.has(evidenceId))linkedEvidence.set(evidenceId,[]);linkedEvidence.get(evidenceId).push({observationId,basis,origin,freshnessStatus,policy:trust.policy,reasons:trust.reasons});}
  }
  for(const [evidenceId,bindings] of linkedEvidence){const evidence=records(project,'evidenceRecords').find(item=>recordId(item,'evidenceRecords')===evidenceId),unique=[...new Set(bindings.map(item=>item.observationId))],basis=bindings.length&&bindings.every(item=>item.basis==='VERIFIED_EXTERNAL')?'VERIFIED_EXTERNAL':'SELF_ASSERTED',origin=bindings.some(item=>item.origin==='AGENT_SEMANTIC_OBSERVATION')?'AGENT_SEMANTIC_OBSERVATION':basis==='VERIFIED_EXTERNAL'?'VERIFIED_EXTERNAL_OBSERVATION':'EXTERNAL_CLAIM',freshnessStatuses=bindings.map(item=>item.freshnessStatus),freshnessStatus=freshnessStatuses.includes('EXPIRED')?'EXPIRED':freshnessStatuses.includes('UNKNOWN')?'UNKNOWN':freshnessStatuses.includes('CURRENT')?'CURRENT':freshnessStatuses.length?'NOT_APPLICABLE':'UNKNOWN',policies=[...new Map(bindings.map(item=>[hash.sha256Value(item.policy||{}),item.policy||{}])).values()],reasons=[...new Set(bindings.flatMap(item=>safe(item.reasons)))];applyApplicationConsequences(evidence,'evidenceRecords',{OBSERVATION_ORIGIN:origin,EPISTEMIC_BASIS:basis,FRESHNESS_STATUS:freshnessStatus,FRESHNESS_POLICY:policies.length===1?policies[0]:{status:'UNKNOWN'},FRESHNESS_EVALUATION_REASON:reasons.join(' ')||'Freshness was evaluated from the exact current evidence contract and application-controlled operation scope.',OBSERVATION_ID:unique.length===1?unique[0]:null},unique.length===1?{OBSERVATION_ID:unique[0]}:{},changes,'externalEvidence.evidence');}
}
function normalizeAcceptedAgentConsequences(project,{stage,operation='COMPLETE',proposalId,rawResponseId=null,promptId=null,changeId=null,stageData=null}={}){
  ensureShape(project);const number=Number(stage),changes=[],scope=currentScope(project),proposed=collection=>records(project,collection).filter(item=>item.sourceProposalId===String(proposalId||'')&&isActiveRecord(item));
  if(number===2)synchronizeSourceSearchContract(project,{proposalId,rawResponseId,promptId,changeId,changes});
  if(number===3){const sourceSummary=new Map(normalizeSourceConsequences(project).map(item=>[item.sourceId,item]));for(const collection of ['research','candidateRequirements'])for(const finding of proposed(collection)){const sourceId=relationshipIds(finding,'SOURCE_ID')[0]||String(recordValue(finding,'SOURCE_ID')||''),source=sourceSummary.get(sourceId);if(!source)throw new Error(`Accepted Stage 03 ${collection} record does not resolve to one current normalized source consequence.`);const limitationRequired=source.AUTHENTICITY_STATUS!=='VERIFIED'||source.FRESHNESS_STATUS!=='CURRENT'||source.freshnessContradictionStatus==='CONTRADICTED';applyApplicationConsequences(finding,collection,{SOURCE_AUTHENTICITY_STATUS:source.AUTHENTICITY_STATUS,SOURCE_AUTHENTICITY_BASIS:source.AUTHENTICITY_BASIS,SOURCE_FRESHNESS_STATUS:source.FRESHNESS_STATUS,SOURCE_FRESHNESS_BASIS:source.freshnessBasis,SOURCE_EPISTEMIC_LIMITATION_REQUIRED:limitationRequired},null,changes,'stage03.sourceConsequence');}}
  if(number===4){
    const requirements=proposed('requirements'),propositions=proposed('propositions');
    for(const proposition of propositions){
      const requirementIds=relationshipIds(proposition,'REQUIREMENT_ID');if(requirementIds.length!==1||!requirements.some(item=>recordId(item,'requirements')===requirementIds[0]))throw new Error('Each accepted Stage 04 proposition must bind exactly one response-local requirement.');
      const role=propositionRole(proposition);if(role==='UNKNOWN'&&propositions.filter(item=>relationshipIds(item,'REQUIREMENT_ID').includes(requirementIds[0])).length>1)throw new Error('Every proposition in a multi-proposition conditional requirement must declare PRIMARY_REQUIREMENT or CONDITIONAL_ACTIVATION.');
      const requirement=requirements.find(item=>recordId(item,'requirements')===requirementIds[0]),sourceIds=sourceIdsForRequirement(project,requirement,proposition),sourceState=evaluateSourceDependencies(project,{sourceIds});applyApplicationConsequences(proposition,'propositions',{REQUIREMENT_ID:requirementIds[0],SOURCE_IDS:sourceIds,SOURCE_STATE_SUMMARY:sourceState.requirements,CURRENT_SCOPE:clone(proposition.scope||scope),CONTENT_SHA256:hash.sha256Value({role:role==='UNKNOWN'?'PRIMARY_REQUIREMENT':role,text:recordValue(proposition,'PROPOSITION_TEXT'),subjectAndScope:recordValue(proposition,'SUBJECT_AND_SCOPE_DESCRIPTION'),satisfactionMeaning:recordValue(proposition,'SATISFACTION_MEANING'),failureMeaning:recordValue(proposition,'FAILURE_MEANING')}),STATUS:'CURRENT',TRUTH_VALUE:'UNKNOWN',EPISTEMIC_BASIS:'SELF_ASSERTED',CURRENT_SCOPE_STATUS:'CURRENT',FRESHNESS_STATUS:'NOT_APPLICABLE',CONTRADICTION_STATUS:'CLEAR',REASONS:[],SUPPORTING_EVIDENCE_IDS:safe(proposition.evidenceRefs).map(String),DERIVATION_ID:changeId},{REQUIREMENT_ID:requirementIds[0],SOURCE_IDS:sourceIds,SUPPORTING_EVIDENCE_IDS:safe(proposition.evidenceRefs).map(String)},changes,'stage04.proposition');
    }
    for(const requirement of requirements){
      const linked=propositions.filter(item=>relationshipIds(item,'REQUIREMENT_ID').includes(recordId(requirement,'requirements'))),primary=linked.filter(item=>propositionRole(item)==='PRIMARY_REQUIREMENT'||linked.length===1&&propositionRole(item)==='UNKNOWN'),activation=linked.filter(item=>propositionRole(item)==='CONDITIONAL_ACTIVATION'),classification=proposedNormativeClassification(requirement);if(primary.length!==1)throw new Error('Each accepted Stage 04 atomic requirement must have exactly one PRIMARY_REQUIREMENT proposition.');if(classification==='CONDITIONAL'&&activation.length!==1)throw new Error('A conditional requirement must contain exactly one distinct CONDITIONAL_ACTIVATION proposition.');if(classification!=='CONDITIONAL'&&activation.length)throw new Error('A nonconditional requirement cannot contain a CONDITIONAL_ACTIVATION proposition.');
      const sourceIds=sourceIdsForRequirement(project,requirement,primary[0]);applyApplicationConsequences(requirement,'requirements',{PRIMARY_PROPOSITION_ID:recordId(primary[0],'propositions'),SOURCE_IDS:sourceIds,NORMATIVE_CLASSIFICATION:'UNKNOWN',NORMATIVE_CLASSIFICATION_REVIEW_ID:null,CURRENT_APPLICABILITY:'UNKNOWN',ACCEPTED_SEVERITY:'UNKNOWN'},{PRIMARY_PROPOSITION_ID:recordId(primary[0],'propositions'),SOURCE_IDS:sourceIds,NORMATIVE_CLASSIFICATION_REVIEW_ID:null},changes,'stage04.requirement');
    }
  }
  if(number===5){
    const currentPropositions=recordsForCurrentScope(project,'propositions'),newApplicability=proposed('applicabilityRecords'),newEquivalenceReviews=proposed('propositionEquivalenceReviews'),newApplicabilityReviews=proposed('applicabilityReviews');
    for(const review of newEquivalenceReviews){const a=relationshipIds(review,'PROPOSITION_A_ID'),b=relationshipIds(review,'PROPOSITION_B_ID');if(a.length!==1||b.length!==1||![a[0],b[0]].every(id=>currentPropositions.some(item=>recordId(item,'propositions')===id)))throw new Error('A proposition-equivalence review must bind exactly two current proposition identities.');const finding=upper(recordValue(review,'EQUIVALENCE_FINDING'));if(!['EQUIVALENT','PARTIAL','UNKNOWN','NOT_EQUIVALENT'].includes(finding))throw new Error('Proposition-equivalence finding is not controlled.');const evidenceIds=safe(review.evidenceRefs).map(String);applyApplicationConsequences(review,'propositionEquivalenceReviews',{PROPOSITION_A_ID:a[0],PROPOSITION_B_ID:b[0],EVIDENCE_ID:evidenceIds[0]||null,CURRENT_SCOPE:clone(review.scope||scope),ACCEPTED_STATUS:'ACCEPTED',EQUIVALENCE_CLASS_ID:finding==='EQUIVALENT'?'PROP-EQ-'+hash.sha256Value([a[0],b[0]].sort()).slice(0,20).toUpperCase():'NONE'},{PROPOSITION_A_ID:a[0],PROPOSITION_B_ID:b[0],...(evidenceIds[0]?{EVIDENCE_ID:evidenceIds[0]}:{})},changes,'stage05.equivalence');}
    const reviewedApplicabilityIds=new Set();for(const review of newApplicabilityReviews){const targetIds=relationshipIds(review,'APPLICABILITY_ID'),propositionIds=relationshipIds(review,'SUBJECT_PROPOSITION_ID');if(targetIds.length!==1)throw new Error('An applicability review must bind exactly one existing applicability record.');const target=recordsForCurrentScope(project,'applicabilityRecords').find(item=>recordId(item,'applicabilityRecords')===targetIds[0]);if(!target)throw new Error('An applicability review targets a missing, stale, or response-local applicability proposal.');const subjectId=relationshipIds(target,'SUBJECT_ID')[0];if(propositionIds.length!==1||propositionIds[0]!==subjectId)throw new Error('An applicability review must bind the exact subject proposition of its applicability proposal.');const binding=independentReviewBinding(project,{stage:5,subjectId:targetIds[0],reviewChangeId:changeId,reviewProposalId:proposalId,reviewPromptId:promptId});if(!binding.complete)throw new Error(binding.reasons.join(' '));const finding=upper(recordValue(review,'REVIEW_FINDING')),evidenceIds=safe(review.evidenceRefs).map(String),reviewedClassification=controlledNormativeClass(recordValue(target,'NORMATIVE_CLASSIFICATION_PROPOSAL'));applyApplicationConsequences(review,'applicabilityReviews',{APPLICABILITY_ID:targetIds[0],SUBJECT_PROPOSITION_ID:subjectId,EVIDENCE_IDS:evidenceIds,REVIEWED_NORMATIVE_CLASSIFICATION:reviewedClassification,SUBJECT_SEMANTIC_SHA256:hash.sha256Value(applicabilitySubjectPayload(project,target)),AUTHOR_ACCEPTED_CHANGE_ID:binding.author.changeId,REVIEW_ACCEPTED_CHANGE_ID:changeId,AUTHOR_PROMPT_IDENTITY:binding.author.promptId,REVIEW_PROMPT_IDENTITY:promptId,INDEPENDENCE_STATUS:'APPLICATION_ESTABLISHED',CURRENT_SCOPE:clone(review.scope||scope),ACCEPTED_STATUS:finding==='ACCEPTED'?'ACCEPTED':'BLOCKED'},{APPLICABILITY_ID:targetIds[0],SUBJECT_PROPOSITION_ID:subjectId,EVIDENCE_IDS:evidenceIds},changes,'stage05.applicabilityReview');reviewedApplicabilityIds.add(targetIds[0]);}
    const normalizeApplicability=[...new Map([...newApplicability,...recordsForCurrentScope(project,'applicabilityRecords').filter(item=>reviewedApplicabilityIds.has(recordId(item,'applicabilityRecords')))].map(item=>[recordId(item,'applicabilityRecords'),item])).values()];
    for(const applicability of normalizeApplicability){
      let subjectIds=relationshipIds(applicability,'SUBJECT_ID');if(!subjectIds.length&&currentPropositions.length===1)subjectIds=[recordId(currentPropositions[0],'propositions')];if(subjectIds.length!==1||!currentPropositions.some(item=>recordId(item,'propositions')===subjectIds[0]))throw new Error('An applicability proposal must resolve to exactly one current proposition.');if(newApplicability.filter(item=>relationshipIds(item,'SUBJECT_ID').includes(subjectIds[0])||(!relationshipIds(item,'SUBJECT_ID').length&&currentPropositions.length===1)).length>1)throw new Error('Multiple current applicability proposals target the same proposition.');
      const subject=currentPropositions.find(item=>recordId(item,'propositions')===subjectIds[0]),requirementIdValue=relationshipIds(subject,'REQUIREMENT_ID')[0],requirement=recordsForCurrentScope(project,'requirements').find(item=>requirementId(item)===requirementIdValue),value=controlledApplicability(recordValue(applicability,'APPLICABILITY_PROPOSAL')),classification=controlledNormativeClass(recordValue(applicability,'NORMATIVE_CLASSIFICATION_PROPOSAL')),classificationReviewRequired=['CONDITIONAL','OPTIONAL'].includes(classification),evidenceIds=[...new Set([...safe(applicability.evidenceRefs).map(String),...relationshipIds(applicability,'EVIDENCE_IDS')])],activationIds=relationshipIds(applicability,'ACTIVATION_PROPOSITION_ID'),reviews=acceptedApplicabilityReviews(project,recordId(applicability,'applicabilityRecords'),applicability.scope).filter(review=>String(recordValue(review,'SUBJECT_SEMANTIC_SHA256'))===hash.sha256Value(applicabilitySubjectPayload(project,applicability))),review=reviews.length===1?reviews[0]:null,evidenceFreshness=evidenceFreshnessForIds(project,evidenceIds,applicability.scope),reviewRequired=value==='NOT_APPLICABLE'||classificationReviewRequired;
      if(classification==='UNKNOWN')throw new Error('Each Stage 05 applicability proposal must declare a controlled normative classification.');if(classification==='CONDITIONAL'&&activationIds.length!==1)throw new Error('A conditional applicability proposal must bind exactly one activation proposition.');if(classification!=='CONDITIONAL'&&activationIds.length)throw new Error('Only a conditional applicability proposal may bind an activation proposition.');if(activationIds.some(id=>{const target=currentPropositions.find(item=>recordId(item,'propositions')===id);return id===subjectIds[0]||!target||propositionRole(target)!=='CONDITIONAL_ACTIVATION'||relationshipIds(target,'REQUIREMENT_ID')[0]!==requirementIdValue;}))throw new Error('Conditional activation must bind a distinct current CONDITIONAL_ACTIVATION proposition of the same requirement.');if(value==='NOT_APPLICABLE'&&!evidenceIds.length)throw new Error('NOT_APPLICABLE requires affirmative current evidence.');
      const validNotApplicableEvidence=value!=='NOT_APPLICABLE'||['CURRENT','NOT_APPLICABLE'].includes(evidenceFreshness),effective=value==='NOT_APPLICABLE'&&(!review||!validNotApplicableEvidence)?'UNKNOWN':value,effectiveClassification=classificationReviewRequired&&!review?'UNKNOWN':classification,normalizationReasons=[];if(reviewRequired&&!review)normalizationReasons.push('This applicability or release-obligation reduction requires an accepted independent review of the exact classification, proposal, reasoning, evidence, subject, and activation binding.');if(value==='NOT_APPLICABLE'&&!validNotApplicableEvidence)normalizationReasons.push('NOT_APPLICABLE requires current or expressly time-invariant supporting evidence; expired or unknown evidence cannot remove the release obligation.');if(value==='UNKNOWN')normalizationReasons.push('Applicability remains semantically unresolved.');if(effectiveClassification==='UNKNOWN')normalizationReasons.push('Normative classification remains blocked pending exact independent review.');
      applyApplicationConsequences(applicability,'applicabilityRecords',{SUBJECT_TYPE:'PROPOSITION',SUBJECT_ID:subjectIds[0],ACTIVATION_PROPOSITION_ID:activationIds[0]||null,NORMATIVE_CLASSIFICATION:effectiveClassification,CLASSIFICATION_REVIEW_REQUIRED:classificationReviewRequired,EVIDENCE_IDS:evidenceIds,INDEPENDENT_REVIEW_ID:recordId(review,'applicabilityReviews')||null,APPLICABILITY:effective,TRUTH_VALUE:effective==='APPLICABLE'?'TRUE':effective==='NOT_APPLICABLE'?'FALSE':'UNKNOWN',EPISTEMIC_BASIS:review?'EXTERNALLY_SUPPORTED':'SELF_ASSERTED',FRESHNESS_STATUS:value==='NOT_APPLICABLE'?evidenceFreshness:effective==='UNKNOWN'?'UNKNOWN':'CURRENT',CONTRADICTION_STATUS:'CLEAR',CURRENT_SELECTION:true,CURRENT_SCOPE:clone(applicability.scope||scope),STATUS:effective==='UNKNOWN'||effectiveClassification==='UNKNOWN'?'BLOCKED':'CURRENT',CURRENT_SCOPE_STATUS:'CURRENT',REASONS:normalizationReasons,SUPPORTING_EVIDENCE_IDS:evidenceIds,DERIVATION_ID:changeId},{SUBJECT_ID:subjectIds[0],...(activationIds[0]?{ACTIVATION_PROPOSITION_ID:activationIds[0]}:{}),EVIDENCE_IDS:evidenceIds,...(review?{INDEPENDENT_REVIEW_ID:recordId(review,'applicabilityReviews')}:{}),SUPPORTING_EVIDENCE_IDS:evidenceIds},changes,'stage05.applicability');
    }
    for(const requirement of recordsForCurrentScope(project,'requirements')){const primary=primaryPropositionsForRequirement(project,requirement),applicabilityRecord=primary.length===1?recordsForCurrentScope(project,'applicabilityRecords').find(item=>relationshipIds(item,'SUBJECT_ID').includes(recordId(primary[0],'propositions'))):null,accepted=controlledNormativeClass(recordValue(applicabilityRecord,'NORMATIVE_CLASSIFICATION')),reviewId=relationshipIds(applicabilityRecord,'INDEPENDENT_REVIEW_ID')[0]||null,applicability=evaluateApplicability(project,{requirement});applyApplicationConsequences(requirement,'requirements',{NORMATIVE_CLASSIFICATION:accepted,NORMATIVE_CLASSIFICATION_REVIEW_ID:reviewId,CURRENT_APPLICABILITY:applicability.applicability,ACCEPTED_SEVERITY:upper(recordValue(requirement,'SEVERITY'))||'UNKNOWN'},{NORMATIVE_CLASSIFICATION_REVIEW_ID:reviewId},changes,'stage05.requirementConsequence');}
  }
  if(number===6){
    const currentPropositions=recordsForCurrentScope(project,'propositions'),newSemanticReviews=proposed('testSemanticReviews'),reviewedTests=new Set(),reviewedExpressions=new Set();
    for(const review of newSemanticReviews){const testIds=relationshipIds(review,'TEST_ID'),expressionIds=relationshipIds(review,'PROOF_EXPRESSION_ID');if((testIds.length===1?1:0)+(expressionIds.length===1?1:0)!==1)throw new Error('A test semantic review must bind exactly one existing test or proof expression.');const kind=testIds.length?'TEST':'PROOF_EXPRESSION',subjectId=(testIds[0]||expressionIds[0]),collection=kind==='TEST'?'tests':'proofExpressions',subject=recordsForCurrentScope(project,collection).find(item=>recordId(item,collection)===subjectId);if(!subject)throw new Error('A test semantic review targets missing, stale, or response-local semantic content.');const binding=independentReviewBinding(project,{stage:6,subjectId,reviewChangeId:changeId,reviewProposalId:proposalId,reviewPromptId:promptId});if(!binding.complete)throw new Error(binding.reasons.join(' '));const targets=kind==='TEST'?propositionIdsFor(subject):relationshipIds(subject,'TARGET_PROPOSITION_ID');if(!targets.length||targets.some(id=>!currentPropositions.some(item=>recordId(item,'propositions')===id)))throw new Error('A semantic review subject does not bind current target propositions.');const finding=upper(recordValue(review,'EQUIVALENCE_FINDING')),evidenceIds=safe(review.evidenceRefs).map(String);applyApplicationConsequences(review,'testSemanticReviews',{SUBJECT_KIND:kind,TEST_ID:kind==='TEST'?subjectId:null,PROOF_EXPRESSION_ID:kind==='PROOF_EXPRESSION'?subjectId:null,TARGET_PROPOSITION_IDS:targets,EVIDENCE_IDS:evidenceIds,SUBJECT_SEMANTIC_SHA256:hash.sha256Value(testSemanticSubjectPayload(kind,subject)),AUTHOR_ACCEPTED_CHANGE_ID:binding.author.changeId,REVIEW_ACCEPTED_CHANGE_ID:changeId,AUTHOR_PROMPT_IDENTITY:binding.author.promptId,REVIEW_PROMPT_IDENTITY:promptId,INDEPENDENCE_STATUS:'APPLICATION_ESTABLISHED',CURRENT_SCOPE:clone(review.scope||scope),ACCEPTED_STATUS:['EQUIVALENT','PARTIAL','NOT_EQUIVALENT'].includes(finding)?'ACCEPTED':'BLOCKED'},{...(kind==='TEST'?{TEST_ID:subjectId}:{PROOF_EXPRESSION_ID:subjectId}),TARGET_PROPOSITION_IDS:targets,EVIDENCE_IDS:evidenceIds},changes,'stage06.semanticReview');(kind==='TEST'?reviewedTests:reviewedExpressions).add(subjectId);}
    const normalizeTests=[...new Map([...proposed('tests'),...recordsForCurrentScope(project,'tests').filter(item=>reviewedTests.has(recordId(item,'tests')))].map(item=>[recordId(item,'tests'),item])).values()];for(const test of normalizeTests){const requirementIdValue=testRequirementId(test),targets=relationshipIds(test,'TARGET_PROPOSITION_IDS').length?relationshipIds(test,'TARGET_PROPOSITION_IDS'):currentPropositions.filter(item=>relationshipIds(item,'REQUIREMENT_ID').includes(requirementIdValue)).map(item=>recordId(item,'propositions'));if(!targets.length||targets.some(id=>!currentPropositions.some(item=>recordId(item,'propositions')===id&&relationshipIds(item,'REQUIREMENT_ID').includes(requirementIdValue))))throw new Error('A Stage 06 test must resolve only to current propositions of its requirement.');const allReviews=acceptedTestSemanticReviews(project,{subjectKind:'TEST',subjectId:recordId(test,'tests'),scopeRule:test.scope}),payloadHash=hash.sha256Value(testSemanticSubjectPayload('TEST',test)),reviews=allReviews.filter(review=>String(recordValue(review,'SUBJECT_SEMANTIC_SHA256'))===payloadHash),coverage=normalizedSemanticDisposition(recordValue(test,'SEMANTIC_COVERAGE_DISPOSITION'),reviews),role=testRole(test),releaseBearing=['REQUIRED_PROOF','SUPPORTING_PROOF','REGRESSION'].includes(role),contract=proofContractForTest(test);if(releaseBearing&&!contract.complete)throw new Error(recordId(test,'tests')+': '+contract.reasons.join(' '));applyApplicationConsequences(test,'tests',{TARGET_PROPOSITION_IDS:targets,ACCEPTED_SEMANTIC_COVERAGE:coverage,SEMANTIC_REVIEW_IDS:reviews.map(item=>recordId(item,'testSemanticReviews')),RELEASE_BEARING:releaseBearing},{TARGET_PROPOSITION_IDS:targets,SEMANTIC_REVIEW_IDS:reviews.map(item=>recordId(item,'testSemanticReviews'))},changes,'stage06.test');}
    const normalizeExpressions=[...new Map([...proposed('proofExpressions'),...recordsForCurrentScope(project,'proofExpressions').filter(item=>reviewedExpressions.has(recordId(item,'proofExpressions')))].map(item=>[recordId(item,'proofExpressions'),item])).values()];for(const expression of normalizeExpressions){const targets=relationshipIds(expression,'TARGET_PROPOSITION_ID');if(targets.length!==1||!currentPropositions.some(item=>recordId(item,'propositions')===targets[0]))throw new Error('A proof expression must target exactly one current proposition.');const allReviews=acceptedTestSemanticReviews(project,{subjectKind:'PROOF_EXPRESSION',subjectId:recordId(expression,'proofExpressions'),scopeRule:expression.scope}),payloadHash=hash.sha256Value(testSemanticSubjectPayload('PROOF_EXPRESSION',expression)),reviews=allReviews.filter(review=>String(recordValue(review,'SUBJECT_SEMANTIC_SHA256'))===payloadHash),disposition=normalizedSemanticDisposition('EQUIVALENT',reviews),normalized=recordValue(expression,'NORMALIZED_EXPRESSION');applyApplicationConsequences(expression,'proofExpressions',{TARGET_PROPOSITION_ID:targets[0],NORMALIZED_EXPRESSION:normalized,SEMANTIC_EQUIVALENCE_DISPOSITION:disposition,SEMANTIC_REVIEW_IDS:reviews.map(item=>recordId(item,'testSemanticReviews')),CURRENT_SCOPE_HASH:hash.sha256Value(clone(expression.scope||scope)),EXPRESSION_SHA256:hash.sha256Value(normalized),TRUTH_VALUE:'UNKNOWN',STATUS:disposition==='EQUIVALENT'?'CURRENT':'BLOCKED'},{TARGET_PROPOSITION_ID:targets[0],SEMANTIC_REVIEW_IDS:reviews.map(item=>recordId(item,'testSemanticReviews'))},changes,'stage06.proofExpression');}
  }
  normalizeExternalResultConsequences(project,{stage:number,operation,proposalId,rawResponseId,promptId,changeId,changes});return {changes,normalizedRecordIds:[...new Set(changes.map(item=>item.canonicalRecordId).filter(Boolean))]};
}
function preflightAuthorContextId(project){
  const instructionIds=new Set(recordsForCurrentScope(project,'instructions').map(item=>recordId(item,'instructions')).filter(Boolean));
  if(!instructionIds.size)return '';
  const change=acceptedChanges(project,8).filter(item=>String(item?.scope?.contextId||'').trim()&&safe(item?.canonicalRecordIds).some(id=>instructionIds.has(String(id)))).at(-1);
  return String(change?.scope?.contextId||'').trim();
}
function evaluateContextIndependence(project,{role='RUN_BATCH',iterationId=null,runId=null,verifierContextId=null,generatorContextId=null,reviewerContextId=null,productionContextId=null,promptContextManifest=null,requiredDimensions=null}={}){
  const forbidden=/prior\s*(?:run|output|verification|verifier|review)|reviewer\s*feedback|root\s*cause|\bRCA\b|proposed\s*correction|generator\s*self[- ]evaluation/i,dimensions={},evidence=[],candidateIds=new Set();
  const set=(name,truthValue,basis,reasons=[],ids=[])=>{dimensions[name]={dimension:name,truthValue:controlledTruth(truthValue),epistemicBasis:controlledBasis(basis),reasons:[...new Set(safe(reasons))],evidenceIds:[...new Set(safe(ids).filter(Boolean).map(String))]};evidence.push(...dimensions[name].evidenceIds);};
  const explicitRequired=independenceDimensionList(requiredDimensions),obligationRequired=recordsForCurrentScope(project,'proofObligations').flatMap(item=>independenceDimensionList(recordValue(item,'INDEPENDENCE_REQUIREMENTS'))),required=new Set([...explicitRequired,...obligationRequired]);
  const providerState=contexts=>{const values=contexts.map(ctx=>upper(recordValue(ctx,'PROVIDER_CONTEXT_INDEPENDENCE')||recordValue(ctx,'EXTERNAL_PROVIDER_CONTEXT_SEPARATION'))).filter(Boolean),bases=contexts.map(ctx=>controlledBasis(recordValue(ctx,'PROVIDER_CONTEXT_EPISTEMIC_BASIS')||recordValue(ctx,'EPISTEMIC_BASIS')));if(values.some(value=>['VIOLATED','FALSE','REUSED','CONTAMINATED'].includes(value)))return ['FALSE',bases.find(x=>x!=='NONE')||'SELF_ASSERTED',['Provider context reuse or contamination is affirmatively recorded.']];if(values.length===contexts.length&&values.every(value=>['TRUE','INDEPENDENT','VERIFIED'].includes(value))){const strongest=bases.includes('VERIFIED_EXTERNAL')?'VERIFIED_EXTERNAL':bases.includes('EXTERNALLY_SUPPORTED')?'EXTERNALLY_SUPPORTED':'SELF_ASSERTED';return ['TRUE',strongest,strongest==='SELF_ASSERTED'?['Provider separation is only self-asserted.']:[]];}return ['UNKNOWN','NONE',['Hidden provider memory, retrieval, account, and service context are not observable by the application.']];};
  if(role==='RUN_BATCH'){
    const runs=recordsForIteration(project,'runs',iterationId),runContextIds=new Set(runs.map(run=>String(recordValue(run,'CONTEXT_ID')||run.relationships?.CONTEXT_ID||'')).filter(Boolean)),contexts=recordsForIteration(project,'freshContexts',iterationId).filter(ctx=>runContextIds.has(recordId(ctx,'freshContexts'))),byId=new Map(contexts.map(ctx=>[recordId(ctx,'freshContexts'),ctx])),internal=[],external=[],isolationReasons=[],sessionReasons=[],transferValues=[],environmentValues=[];
    for(const run of runs){const rid=recordId(run,'runs'),cid=String(recordValue(run,'CONTEXT_ID')||run.relationships?.CONTEXT_ID||''),ctx=byId.get(cid),ext=String(recordValue(ctx,'EXTERNAL_CONTEXT_IDENTIFIER')||'').trim(),candidate=String(recordValue(run,'CANDIDATE_ID')||run.relationships?.CANDIDATE_ID||run.scope?.candidateId||'');internal.push(cid);external.push(ext);if(candidate)candidateIds.add(candidate);if(!ctx){sessionReasons.push(rid+' has no current fresh-context record.');continue;}if(String(recordValue(ctx,'RUN_ID')||ctx.relationships?.RUN_ID||'')&&String(recordValue(ctx,'RUN_ID')||ctx.relationships?.RUN_ID)!==rid)sessionReasons.push(cid+' is linked to another run.');if(forbidden.test(JSON.stringify(recordValue(ctx,'AUTHORIZED_PROJECT_INPUTS')||promptContextManifest||[])))isolationReasons.push(rid+' contains prohibited prior-output, reviewer, root-cause, or correction context.');if(['TRUE','YES','CONTAMINATED','DETECTED','POSITIVE'].includes(upper(recordValue(ctx,'CONTAMINATION_STATUS')))||['TRUE','YES','CONTAMINATED','DETECTED','POSITIVE'].includes(upper(recordValue(run,'CONTAMINATION_CHECK'))))isolationReasons.push(rid+' has affirmative contamination.');transferValues.push(upper(recordValue(ctx,'USER_TRANSFER_CONFORMITY')));environmentValues.push(upper(recordValue(ctx,'ENVIRONMENT_INDEPENDENCE')));}
    if(runs.length!==10)sessionReasons.push('Exactly ten current runs are required.');if(new Set(internal.filter(Boolean)).size!==10)sessionReasons.push('Ten distinct application context reservations are not present.');set('APPLICATION_SESSION_DISTINCTNESS',sessionReasons.length?'FALSE':'TRUE','APPLICATION_OBSERVED',sessionReasons,internal);set('APPLICATION_INPUT_ISOLATION',isolationReasons.length?'FALSE':contexts.length===10?'TRUE':'UNKNOWN','APPLICATION_OBSERVED',isolationReasons,internal);const externalReasons=[];if(external.some(x=>!x))externalReasons.push('One or more observed external context identifiers are missing.');if(new Set(external.map(upper).filter(Boolean)).size!==10)externalReasons.push('Observed external context identifiers are reused or are not ten distinct values.');set('EXTERNAL_CONTEXT_IDENTIFIER_DISTINCTNESS',externalReasons.length?'FALSE':'TRUE',externalReasons.length?'APPLICATION_OBSERVED':'EXTERNALLY_SUPPORTED',externalReasons,external);const provider=providerState(contexts);set('PROVIDER_CONTEXT_INDEPENDENCE',...provider);set('USER_TRANSFER_CONFORMITY',transferValues.includes('VIOLATED')||transferValues.includes('FALSE')?'FALSE':transferValues.length===10&&transferValues.every(value=>['TRUE','CONFORMING','CONFIRMED'].includes(value))?'TRUE':'UNKNOWN','SELF_ASSERTED',transferValues.length===10?[]:['User transfer conformity is not completely recorded.']);set('EXECUTOR_OR_REVIEWER_ROLE_SEPARATION','TRUE','APPLICATION_OBSERVED',[],internal);set('ENVIRONMENT_INDEPENDENCE',environmentValues.includes('VIOLATED')||environmentValues.includes('FALSE')?'FALSE':environmentValues.length===10&&environmentValues.every(value=>['TRUE','INDEPENDENT','VERIFIED'].includes(value))?'TRUE':'UNKNOWN','EXTERNALLY_SUPPORTED',environmentValues.length===10?[]:['Environment independence is not completely evidenced.']);if(candidateIds.size!==1)set('APPLICATION_INPUT_ISOLATION','FALSE','APPLICATION_OBSERVED',['Runs do not use one current candidate identity.'],internal);
  }else{
    const contexts=records(project,'freshContexts').filter(isActiveRecord),roleContexts=role==='PREFLIGHT_REVIEW'?contexts.filter(ctx=>Number(ctx.stage)===9):contexts,reviewer=String(reviewerContextId||verifierContextId||'').trim(),inferredProducer=role==='VERIFICATION'?recordValue(recordsForIteration(project,'runs',iterationId).find(item=>recordId(item,'runs')===String(runId||'')),'CONTEXT_ID'):role==='PREFLIGHT_REVIEW'?preflightAuthorContextId(project):'',producer=String(productionContextId||generatorContextId||inferredProducer||'').trim(),reviewerRecord=roleContexts.find(ctx=>recordId(ctx,'freshContexts')===reviewer||String(recordValue(ctx,'EXTERNAL_CONTEXT_IDENTIFIER')||'')===reviewer),producerRecord=contexts.find(ctx=>recordId(ctx,'freshContexts')===producer),reviewerExternal=String(recordValue(reviewerRecord,'EXTERNAL_CONTEXT_IDENTIFIER')||'').trim(),producerExternal=String(recordValue(producerRecord,'EXTERNAL_CONTEXT_IDENTIFIER')||'').trim(),same=Boolean(reviewer&&producer&&(reviewer===producer||reviewerExternal&&producerExternal&&upper(reviewerExternal)===upper(producerExternal))),isolationViolation=Boolean(reviewerRecord&&forbidden.test(JSON.stringify(recordValue(reviewerRecord,'AUTHORIZED_PROJECT_INPUTS')||promptContextManifest||[]))),contaminated=Boolean(reviewerRecord&&['TRUE','YES','CONTAMINATED','DETECTED','POSITIVE'].includes(upper(recordValue(reviewerRecord,'CONTAMINATION_STATUS'))));
    set('APPLICATION_SESSION_DISTINCTNESS',!reviewerRecord||!producer?'UNKNOWN':same?'FALSE':'TRUE','APPLICATION_OBSERVED',!reviewerRecord?['Current reviewer context reservation is missing.']:!producer?['Producer context identity is unavailable.']:same?['Reviewer reuses the producer context.']:[],[reviewer,producer]);set('APPLICATION_INPUT_ISOLATION',!reviewerRecord?'UNKNOWN':isolationViolation||contaminated?'FALSE':'TRUE','APPLICATION_OBSERVED',isolationViolation?['Reviewer input contains prohibited prior conclusions, root-cause, or correction material.']:contaminated?['Reviewer context contamination is affirmative.']:[],[reviewer]);set('EXTERNAL_CONTEXT_IDENTIFIER_DISTINCTNESS',!reviewerExternal||!producerExternal?'UNKNOWN':upper(reviewerExternal)===upper(producerExternal)?'FALSE':'TRUE','EXTERNALLY_SUPPORTED',!reviewerExternal||!producerExternal?['External context identifiers are not both available.']:upper(reviewerExternal)===upper(producerExternal)?['External context identifier is reused.']:[],[reviewerExternal,producerExternal]);const provider=providerState([reviewerRecord,producerRecord].filter(Boolean));set('PROVIDER_CONTEXT_INDEPENDENCE',...provider);set('EXECUTOR_OR_REVIEWER_ROLE_SEPARATION',!reviewerRecord||!producer?'UNKNOWN':same?'FALSE':'TRUE','APPLICATION_OBSERVED',same?['Executor/reviewer role separation is violated.']:[],[reviewer,producer]);const transfer=upper(recordValue(reviewerRecord,'USER_TRANSFER_CONFORMITY'));set('USER_TRANSFER_CONFORMITY',['VIOLATED','FALSE'].includes(transfer)?'FALSE':['TRUE','CONFORMING','CONFIRMED'].includes(transfer)?'TRUE':'UNKNOWN','SELF_ASSERTED',transfer?[]:['User transfer conformity is not established.'],[reviewer]);const environment=upper(recordValue(reviewerRecord,'ENVIRONMENT_INDEPENDENCE'));set('ENVIRONMENT_INDEPENDENCE',['VIOLATED','FALSE'].includes(environment)?'FALSE':['TRUE','INDEPENDENT','VERIFIED'].includes(environment)?'TRUE':'UNKNOWN','EXTERNALLY_SUPPORTED',environment?[]:['Environment independence is not established.'],[reviewer]);
  }
  const defaults=role==='RUN_BATCH'?['APPLICATION_SESSION_DISTINCTNESS','APPLICATION_INPUT_ISOLATION','EXTERNAL_CONTEXT_IDENTIFIER_DISTINCTNESS']:['APPLICATION_SESSION_DISTINCTNESS','APPLICATION_INPUT_ISOLATION','EXECUTOR_OR_REVIEWER_ROLE_SEPARATION'],requiredSet=new Set(required.size?[...required]:defaults),requiredStates=[...requiredSet].map(name=>dimensions[name]||{dimension:name,truthValue:'UNKNOWN',epistemicBasis:'NONE',reasons:['Required independence dimension is not evaluated.'],evidenceIds:[]}),falseStates=requiredStates.filter(item=>item.truthValue==='FALSE'),unknownStates=requiredStates.filter(item=>item.truthValue==='UNKNOWN'),weakStates=requiredStates.filter(item=>item.truthValue==='TRUE'&&!['APPLICATION_OBSERVED','VERIFIED_EXTERNAL'].includes(item.epistemicBasis));let determination=falseStates.length?'VIOLATED':unknownStates.length?'UNKNOWN':weakStates.some(item=>item.epistemicBasis==='SELF_ASSERTED')?'SELF_ASSERTED':weakStates.length?'EXTERNALLY_SUPPORTED':'APPLICATION_ESTABLISHED';const reasons=[...new Set(requiredStates.flatMap(item=>item.reasons||[]))];if(dimensions.PROVIDER_CONTEXT_INDEPENDENCE?.truthValue==='UNKNOWN')reasons.push('Provider context independence remains UNKNOWN; distinct application or external identifiers do not prove hidden provider-state separation.');return {determination,reasons:[...new Set(reasons)],evidence:[...new Set(evidence)],dimensions,requiredDimensions:[...requiredSet],contextCount:dimensions.EXTERNAL_CONTEXT_IDENTIFIER_DISTINCTNESS?.truthValue==='TRUE'?(role==='RUN_BATCH'?10:2):0,candidateIds:[...candidateIds],residualContextRisk:dimensions.PROVIDER_CONTEXT_INDEPENDENCE?.truthValue==='UNKNOWN'?'UNKNOWN':'RECORDED'};
}
function detectEvidenceJustificationCycles(project){const nodes=new Map(),edges=new Map();for(const collection of Object.keys(schema.RECORD_SCHEMAS)){for(const record of recordsForCurrentScope(project,collection)){const id=recordId(record,collection);if(id)nodes.set(id,{id,collection,record});}}for(const {id,record} of nodes.values()){const targets=[];for(const [key,value] of Object.entries(record.relationships||{}))if(/JUSTIF|DERIVES_FROM|EVIDENCE_DEPENDENCY|SUPPORTS_RECORD|PROOF_EXPRESSION/i.test(key))targets.push(...scalarList(value));for(const target of targets)if(nodes.has(target)){if(!edges.has(id))edges.set(id,new Set());edges.get(id).add(target);}}const visiting=new Set(),visited=new Set(),stack=[],cycles=[];const visit=id=>{if(visiting.has(id)){const index=stack.indexOf(id);cycles.push([...stack.slice(index),id]);return;}if(visited.has(id))return;visiting.add(id);stack.push(id);for(const next of edges.get(id)||[])visit(next);stack.pop();visiting.delete(id);visited.add(id);};for(const id of nodes.keys())visit(id);return cycles;}
function detectCurrentContradictions(project){
  const out=[],push=(type,key,details,severity='RELEASE_MATERIAL')=>out.push({type,key,details,severity}),adjudicationCollections=['verification','deterministicResults','meaningResults','adversarialResults','representationInspections','preflightRecords','confirmationRecords','processAudits','productAudits','regressionExecutions','products'],evaluation=(collection,record)=>evaluateResultConsistency(collection,record,testForResult(project,record),project);for(const [index,cycle] of detectEvidenceJustificationCycles(project).entries())push('EVIDENCE_JUSTIFICATION_CYCLE','CYCLE-'+String(index+1),cycle);
  for(const collection of adjudicationCollections)for(const record of recordsForCurrentScope(project,collection)){const assessed=evaluation(collection,record),id=recordId(record,collection)||String(record.id||record.recordId||'UNKNOWN');if(assessed.claimedDetermination==='SATISFIED'&&assessed.determination!=='SATISFIED')push('CLAIMED_FAVORABLE_EFFECTIVE_CONFLICT',collection+':'+id,[assessed.claimedDetermination,assessed.determination,...assessed.reasons]);}
  const verificationGroups=new Map();for(const record of recordsForCurrentScope(project,'verification')){const key=verificationKey(record,{includeProposition:true}),state=evaluation('verification',record).determination;if(!verificationGroups.has(key))verificationGroups.set(key,new Set());verificationGroups.get(key).add(state);}for(const [key,states] of verificationGroups)if(states.size>1)push('VERIFICATION_DETERMINATION_CONFLICT',key,[...states]);
  const propositions=recordsForCurrentScope(project,'propositions'),parent=new Map(propositions.map(item=>[recordId(item,'propositions'),recordId(item,'propositions')])),find=id=>{let root=parent.get(id);if(!root)return null;while(parent.get(root)!==root)root=parent.get(root);let current=id;while(parent.get(current)!==root){const next=parent.get(current);parent.set(current,root);current=next;}return root;},join=(a,b)=>{const ra=find(a),rb=find(b);if(ra&&rb&&ra!==rb)parent.set(rb,ra);};
  const equivalenceReviews=recordsForCurrentScope(project,'propositionEquivalenceReviews').filter(review=>['ACCEPTED','CURRENT','COMPLETE'].includes(upper(recordValue(review,'ACCEPTED_STATUS')||'ACCEPTED'))),reviewRelations=[];for(const review of equivalenceReviews){const ids=[...new Set([...relationshipIds(review,'PROPOSITION_ID'),...relationshipIds(review,'PROPOSITION_A_ID'),...relationshipIds(review,'PROPOSITION_B_ID')])].filter(id=>parent.has(id)),disposition=upper(recordValue(review,'EQUIVALENCE_FINDING')||recordValue(review,'EQUIVALENCE_DISPOSITION')||recordValue(review,'SEMANTIC_EQUIVALENCE_DISPOSITION')||recordValue(review,'DETERMINATION'));if(ids.length<2)continue;reviewRelations.push({ids,disposition,id:recordId(review,'propositionEquivalenceReviews')});if(disposition==='EQUIVALENT')for(let index=1;index<ids.length;index+=1)join(ids[0],ids[index]);}
  for(const relation of reviewRelations)if(['NOT_EQUIVALENT','UNKNOWN','PARTIAL'].includes(relation.disposition)){const roots=new Set(relation.ids.map(find));if(roots.size===1)push('PROPOSITION_EQUIVALENCE_CONTRADICTION',relation.id,[relation.disposition,...relation.ids]);}
  const propositionGroups=new Map();for(const proposition of propositions){const pid=recordId(proposition,'propositions'),root=find(pid)||pid;if(!propositionGroups.has(root))propositionGroups.set(root,{ids:new Set(),states:new Set(),records:[]});propositionGroups.get(root).ids.add(pid);}
  for(const collection of ['verification','deterministicResults','meaningResults','adversarialResults','representationInspections'])for(const record of recordsForCurrentScope(project,collection)){const test=testForResult(project,record),sufficiency=evaluateEvidenceSufficiency(project,{requirement:recordsForCurrentScope(project,'requirements').find(item=>requirementId(item)===resultRequirementId(project,record))||null,test,result:record}),state=evaluation(collection,record).determination;if(!sufficiency.sufficient||!['SATISFIED','VIOLATED'].includes(state))continue;for(const pid of propositionIdsFor(record,test)){const root=find(pid)||pid;if(!propositionGroups.has(root))propositionGroups.set(root,{ids:new Set([pid]),states:new Set(),records:[]});const group=propositionGroups.get(root);group.states.add(state);group.records.push(collection+':'+(recordId(record,collection)||'UNKNOWN')+':'+state);}}
  for(const [root,group] of propositionGroups)if(group.states.has('SATISFIED')&&group.states.has('VIOLATED'))push('SAME_PROPOSITION_CONTRADICTION',root,[...group.ids,...group.records]);
  const artifacts=new Map();for(const artifact of recordsForCurrentScope(project,'artifacts')){const id=recordId(artifact,'artifacts'),sig=[recordValue(artifact,'VERSION'),recordValue(artifact,'BYTE_SIZE'),recordValue(artifact,'SHA256'),recordValue(artifact,'LINEAGE_SHA256')].join('|');if(artifacts.has(id)&&artifacts.get(id)!==sig)push('ARTIFACT_IDENTITY_CONFLICT',id,[artifacts.get(id),sig]);else artifacts.set(id,sig);}
  const product=recordsForCurrentScope(project,'products').at(-1),baseline=recordsForCurrentScope(project,'baselines').at(-1);if(product&&baseline&&String(recordValue(product,'BASELINE_ID')||product.relationships?.BASELINE_ID||'')!==recordId(baseline,'baselines'))push('PRODUCT_BASELINE_CONFLICT',recordId(product,'products'),['Product baseline differs from current baseline.']);const release=recordsForCurrentScope(project,'releaseRecords').at(-1),releaseAccepted=upper(recordValue(release,'DETERMINATION'))==='ACCEPTED';if(releaseAccepted){const blockers=openBlockers(project);if(blockers.length)push('ACCEPTED_RELEASE_WITH_BLOCKER',recordId(release,'releaseRecords'),blockers.map(item=>recordId(item,'blockers')));const violated=propositions.map(item=>evaluatePropositionState(project,{proposition:item})).filter(item=>item.truthValue==='FALSE').map(item=>item.propositionId);if(violated.length)push('ACCEPTED_RELEASE_WITH_MANDATORY_VIOLATION',recordId(release,'releaseRecords'),violated);const material=unresolvedMaterialDefects(project);if(material.length)push('ACCEPTED_RELEASE_WITH_MATERIAL_DEFECT',recordId(release,'releaseRecords'),material.map(item=>recordId(item,'defects')));}
  const currentDelivery=recordsForCurrentScope(project,'deliveryRecords').find(item=>upper(recordValue(item,'DELIVERY_STATE'))==='AUTHORIZED');if(currentDelivery){const terminal=terminalDeliveryState(project,{create:false,skipContradictions:true});if(!terminal.authorized)push('STALE_DELIVERY_AUTHORIZATION',recordId(currentDelivery,'deliveryRecords'),terminal.reasons);}
  return out;
}
function executionStability(project,iterationId){
  const matrix=verificationMatrix(project,iterationId),byReq={},byTest={},byReqRun=new Map(),byTestValues=new Map();
  const combine=list=>list.some(x=>x==='VIOLATED')?'VIOLATED':list.some(x=>x==='UNDETERMINED')?'UNDETERMINED':list.length&&list.every(x=>x==='SATISFIED')?'SATISFIED':'UNDETERMINED';
  for(const v of matrix.verification){
    const determination=effectiveDetermination('verification',v,testForResult(project,v),project),reqId=String(recordValue(v,'REQ_ID')||v.relationships?.REQ_ID||''),runId=String(recordValue(v,'RUN_ID')||v.relationships?.RUN_ID||''),testId=String(recordValue(v,'TEST_ID')||v.relationships?.TEST_ID||''),reqRunKey=reqId+'|'+runId;
    if(!byReqRun.has(reqRunKey))byReqRun.set(reqRunKey,[]);byReqRun.get(reqRunKey).push(determination);
    if(!byTestValues.has(testId))byTestValues.set(testId,[]);byTestValues.get(testId).push(determination);
  }
  for(const req of matrix.requirements){const rid=requirementId(req),perRun=matrix.runs.map(run=>combine(byReqRun.get(rid+'|'+recordId(run,'runs'))||[])),counts={satisfied:perRun.filter(x=>x==='SATISFIED').length,violated:perRun.filter(x=>x==='VIOLATED').length,undetermined:perRun.filter(x=>x==='UNDETERMINED').length};byReq[rid]={...counts,agreementRate:matrix.runs.length?Math.max(...Object.values(counts))/matrix.runs.length:0};}
  for(const test of recordsForScope(project,'tests',matrix.scope)){const tid=recordId(test,'tests'),vals=byTestValues.get(tid)||[],counts={satisfied:vals.filter(x=>x==='SATISFIED').length,violated:vals.filter(x=>x==='VIOLATED').length,undetermined:vals.filter(x=>x==='UNDETERMINED').length};byTest[tid]={...counts,agreementRate:matrix.runs.length?Math.max(...Object.values(counts))/matrix.runs.length:0};}
  const defects=recordsForIteration(project,'defects',iterationId),patterns=new Map(),newDefectsByRun={};for(const d of defects){const key=hash.sha256Value([recordValue(d,'OBSERVED_FAILURE'),recordValue(d,'EXPECTED_CONDITION')]);patterns.set(key,(patterns.get(key)||0)+1);const run=String(recordValue(d,'RUN_ID')||d.relationships?.RUN_ID||'UNASSIGNED');newDefectsByRun[run]=(newDefectsByRun[run]||0)+1;}
  const comparisonRecords=recordsForIteration(project,'comparisons',iterationId),allowedVariances=[],prohibitedVariances=[],unknownVariances=[];for(const comparison of comparisonRecords){const id=recordId(comparison,'comparisons'),testId=String(recordValue(comparison,'TEST_ID')||comparison.relationships?.TEST_ID||''),test=recordsForScope(project,'tests',matrix.scope).find(item=>recordId(item,'tests')===testId),contract=upper(recordValue(comparison,'EXPECTED_VARIANCE_CLASS')||recordValue(test,'EXPECTED_VARIANCE_CLASS')||recordValue(test,'EXPECTED_VARIANCE_CONTRACT')),observed=truth(recordValue(comparison,'CORRECTNESS_AFFECTING_VARIANCE'))||truth(recordValue(comparison,'VARIANCE_OBSERVED'))||Boolean(String(recordValue(comparison,'OBSERVED_VARIANCE')||'').trim());if(!observed)continue;if(['ALLOWED_SET','NUMERIC_TOLERANCE','ORDER_INSENSITIVE','NONCORRECTNESS_VARIANCE_ALLOWED'].includes(contract)&&!truth(recordValue(comparison,'OUTSIDE_EXPECTED_VARIANCE')))allowedVariances.push(id);else if(contract==='INVARIANT'||truth(recordValue(comparison,'OUTSIDE_EXPECTED_VARIANCE')))prohibitedVariances.push(id);else unknownVariances.push(id);}
  const environmentManifests=recordsForIteration(project,'freshContexts',iterationId).map(item=>recordValue(item,'ENVIRONMENT_MANIFEST_ID')||recordValue(item,'ENVIRONMENT_MANIFEST_SHA256')).filter(Boolean),environmentDifferences=[...new Set(environmentManifests.map(String))];
  return {runCount:matrix.runs.length,requirementStability:byReq,testStability:byTest,totalDistinctDefects:defects.length,repeatedDefectCount:[...patterns.values()].filter(n=>n>1).reduce((a,n)=>a+n,0),uniqueDefectCount:[...patterns.values()].filter(n=>n===1).length,newDefectsByRun,allowedVariances,prohibitedVariances,unknownVariances,expectedVarianceComplete:prohibitedVariances.length===0&&unknownVariances.length===0,environmentManifestIdentities:environmentDifferences,environmentDifferenceCount:environmentDifferences.length>1?environmentDifferences.length:0,unexplainedVarianceCount:prohibitedVariances.length+unknownVariances.length};
}
function executionHandoff(project,{stage=Number(project.activeStage||0),operation=null,testIds=null,runIds=null}={}){
  const op=String(operation||'').toUpperCase(),testStages=[7,12,15,22,23,24].includes(stage)||(stage===17&&['VERIFY','REGRESSION'].includes(op))||(stage===19&&['VERIFY','REGRESSION_VERIFY'].includes(op)),ids=testIds?new Set(testIds.map(String)):null,allItems=testExecutionPlan(project).items,defectRequirementIds=stage===15?new Set(confirmedDefects(project).map(d=>String(recordValue(d,'REQ_ID')||d.relationships?.REQ_ID||'')).filter(Boolean)):null,stageItems=stage===22?allItems.filter(i=>i.testType==='DETERMINISTIC'):stage===23?allItems.filter(i=>i.testType==='MEANING'):stage===24?allItems.filter(i=>i.testType==='ADVERSARIAL'):stage===15&&defectRequirementIds?.size?allItems.filter(i=>defectRequirementIds.has(String(i.requirementId))):allItems,items=testStages?stageItems.filter(i=>!ids||ids.has(i.testId)):[],send=new Map(),withhold=new Map(),expectBack=new Map(),artifacts=recordsForCurrentScope(project,'artifacts'),artifactsById=new Map(artifacts.map(a=>[recordId(a,'artifacts'),a])),productResolution=[22,23,24,25].includes(stage)?currentProductArtifactResolution(project):null;
  const exactArtifact=a=>{if(!a)return null;const id=recordId(a,'artifacts'),available=upper(recordValue(a,'AVAILABILITY'))==='BYTES_PERSISTED_AND_VERIFIED';return {artifactId:id,filename:String(recordValue(a,'FILENAME')||id),byteSize:Number(recordValue(a,'BYTE_SIZE')||0),sha256:available?String(recordValue(a,'SHA256')||'UNKNOWN'):'UNKNOWN',role:String(recordValue(a,'ROLE')||'AUTHORIZED_INPUT'),availability:available?'BYTES_PERSISTED_AND_VERIFIED':'UNAVAILABLE'};};
  const addArtifact=a=>{const exact=exactArtifact(a);if(!exact)return false;send.set(exact.artifactId,exact);return exact.availability==='BYTES_PERSISTED_AND_VERIFIED';};
  const addReferenced=value=>{for(const id of new Set((JSON.stringify(value||'').match(/ARTIFACT-[A-Za-z0-9-]+/g)||[])))addArtifact(artifactsById.get(id));};
  for(const item of items){for(const entry of item.handoff.filesToSend||item.handoff.send){const canonical=artifactsById.get(entry.artifactId);if(canonical)addArtifact(canonical);else send.set(String(entry.artifactId),{...entry,availability:'UNAVAILABLE',sha256:'UNKNOWN'});}for(const x of item.handoff.filesToWithhold||item.handoff.withhold)withhold.set(x.artifactIdOrCategory,x);for(const x of item.handoff.expectedReturnFiles||item.handoff.expectBack)expectBack.set(x.kind+'|'+(x.filenameOrPattern||x.filename||''),x);}
  if(stage===1){const selectedIds=new Set(safe(project.stages?.[1]?.authorizedFiles).map(item=>String(item?.artifactId||item?.id||'')).filter(Boolean));for(const id of selectedIds)addArtifact(artifactsById.get(id));}
  const current=currentScope(project),runExecution=stage===11||(stage===17&&op==='EXECUTE_RUN')||(stage===19&&op==='EXECUTE_RUN');
  if(stage===1){for(const file of safe(project?.stages?.[1]?.authorizedFiles)){const id=String(file?.artifactId||file?.id||'').trim();if(id)addArtifact(artifactsById.get(id));}}
  if(runExecution){const candidate=recordsForCurrentScope(project,'candidateFreezes').find(c=>recordId(c,'candidateFreezes')===String(current.candidateId||''))||recordsForCurrentScope(project,'candidateFreezes').at(-1);addReferenced(recordValue(candidate,'COMPONENT_MANIFEST'));addReferenced(recordValue(candidate,'IMMUTABLE_LOCATIONS'));}
  if(stage===21){const baseline=recordsForCurrentScope(project,'baselines').find(b=>recordId(b,'baselines')===String(current.baselineId||''))||recordsForCurrentScope(project,'baselines').at(-1);addReferenced(recordValue(baseline,'IMMUTABLE_ARTIFACT_RECORDS'));addReferenced(recordValue(baseline,'APPROVED_VERSIONS'));}
  if(productResolution)for(const artifact of productResolution.artifacts)addArtifact(artifact);
  if(stage===12){const wanted=runIds?new Set(runIds.map(String)):null;for(const run of recordsForCurrentScope(project,'runs'))if(!wanted||wanted.has(recordId(run,'runs')))addReferenced(recordValue(run,'OUTPUT_ARTIFACT_IDENTITIES'));}
  const stageWithhold={11:['outputs from other runs','reviewer feedback','failure explanations','proposed corrections'],12:['other verifiers’ determinations','Stage 13 comparison findings','root-cause analysis','correction proposals'],23:['Stage 21 generator correctness claims','unneeded deterministic pass conclusions','adversarial findings'],24:['generator reasoning or self-evaluation','prior reviewer conclusions that tell the adversarial reviewer what to find']};for(const label of stageWithhold[stage]||[])withhold.set(label,{artifactIdOrCategory:label,reason:'Withheld to preserve information isolation and reduce verification bias.'});
  const externalWork=[1,7,11,12,15,17,19,21,23,24,25].includes(stage);if(externalWork)expectBack.set('STRUCTURED_RESPONSE|final strict JSON response',{kind:'STRUCTURED_RESPONSE',filenameOrPattern:'final strict JSON response',required:true});
  const filesToSend=[...send.values()],filesToWithhold=[...withhold.values()],expectedReturnFiles=[...expectBack.values()],missing=filesToSend.filter(item=>item.availability!=='BYTES_PERSISTED_AND_VERIFIED'),productIssues=productResolution?.issues||[],blockingReasons=[missing.length?'Required exact file bytes are unavailable: '+missing.map(item=>item.filename||item.artifactId).join(', '):'',...productIssues].filter(Boolean);return {filesToSend,recordsToSend:items.map(item=>({testId:item.testId,requirementId:item.requirementId,executionRoute:item.executionRoute,expectedResult:item.returnRequirements})),filesToWithhold,instruction:blockingReasons.length?'Required bytes are unavailable or not bound to the exact current product. Do not perform or claim this operation until the application verifies them.':'Transfer exactly the listed files and records to the declared executor; browser-local custody does not transfer them automatically.',expectedReturnFiles,expectedStructuredResponse:expectedReturnFiles.some(item=>item.kind==='STRUCTURED_RESPONSE'),requiredEvidenceDescription:[...new Set(items.map(item=>item.returnRequirements?.requiredEvidenceDescription).filter(Boolean))].join('\n'),blockingReason:blockingReasons.length?blockingReasons.join(' '):null,currentProductId:productResolution?.productId||null,currentProductArtifactIds:productResolution?.artifactIds||[],currentProductArtifactIdentitySha256:productResolution?.identitySha256||null,send:filesToSend,withhold:filesToWithhold,expectBack:expectedReturnFiles};
}
const DEFERRED_FAILURE_TARGET_STAGES=Object.freeze({RUN_OUTPUT:12,REGRESSION_TARGET:15,CURRENT_PRODUCT:22,MEANING_REVIEW_TARGET:23,ADVERSARIAL_TARGET:24,FINAL_REPRESENTATION:25});
const DEFERRED_FAILURE_ROUTES=Object.freeze(['APPLICATION','INDEPENDENT_AI','EXTERNAL_AGENT_TOOL','HUMAN','EXTERNAL_SYSTEM']);
const DEFERRED_FAILURE_PLAN_KEYS=Object.freeze(['targetKind','triggerStage','executionRoute','testId','requiredCapability','requiredArtifacts','proofObligationId','expectedRejection','requiredEvidenceDescription','completionCollection']);
const DEFERRED_FAILURE_ARTIFACT_KEYS=Object.freeze(['source','artifactRole','filenamePattern','mediaType','required']);
function deferredFailureTestContract(project,mutation,{scopeRule=null}={}){
  const id=recordId(mutation,'failureTests')||'UNKNOWN',availability=upper(recordValue(mutation,'FAILURE_TEST_AVAILABILITY_CLASS')||'EXECUTABLE_NOW'),issues=[];
  if(availability!=='DEFERRED_TARGET_DEPENDENT')return {mutationId:id,availability,valid:true,issues,triggerStage:null,targetKind:null,plan:null,proofObligation:null};
  const scopedRecords=collection=>scopeRule?recordsForScope(project,collection,scopeRule):recordsForCurrentScope(project,collection),plan=recordValue(mutation,'DEFERRED_EXECUTION_PLAN'),triggerStage=Number(recordValue(mutation,'DEFERRED_EXECUTION_TRIGGER_STAGE')),targetKind=upper(plan?.targetKind),expectedStage=DEFERRED_FAILURE_TARGET_STAGES[targetKind],route=upper(plan?.executionRoute),testId=String(plan?.testId||'').trim(),test=scopedRecords('tests').find(item=>recordId(item,'tests')===testId)||null,requiredArtifacts=safe(plan?.requiredArtifacts),proofObligationId=String(plan?.proofObligationId||'').trim(),proofObligation=scopedRecords('proofObligations').find(item=>recordId(item,'proofObligations')===proofObligationId)||null;
  if(!plan||typeof plan!=='object'||Array.isArray(plan))issues.push('DEFERRED_EXECUTION_PLAN must be one closed future execution contract object.');
  else{const unknown=Object.keys(plan).filter(key=>!DEFERRED_FAILURE_PLAN_KEYS.includes(key));if(unknown.length)issues.push('Deferred execution plan contains unknown properties: '+unknown.join(', ')+'.');}
  if(!expectedStage)issues.push('Deferred targetKind must be one of '+Object.keys(DEFERRED_FAILURE_TARGET_STAGES).join(', ')+'.');
  if(!Number.isSafeInteger(triggerStage)||triggerStage!==expectedStage||Number(plan?.triggerStage)!==expectedStage)issues.push('Deferred trigger stage must equal the application-owned earliest target-available stage for '+(targetKind||'UNKNOWN')+'.');
  if(!DEFERRED_FAILURE_ROUTES.includes(route))issues.push('Deferred executionRoute must be a capable non-blocked controlled route.');
  if(!testId||!test)issues.push('Deferred execution plan must identify one current canonical negative or regression test.');
  else{const routed=testExecutionPlan(project).items.find(item=>item.testId===testId);if(!['NEGATIVE_ONLY','REGRESSION'].includes(testRole(test)))issues.push('Deferred execution test role must be NEGATIVE_ONLY or REGRESSION.');if(!routed||routed.executionRoute!==route)issues.push('Deferred executionRoute does not match the application-derived route for its canonical test.');if(!routed?.capabilityReady)issues.push('Deferred execution capability is not affirmatively available; only the future target may be deferred.');if(upper(routed?.requiredCapability)!==upper(plan?.requiredCapability))issues.push('Deferred requiredCapability does not match its canonical test.');}
  if(!String(plan?.requiredCapability||'').trim())issues.push('Deferred execution plan lacks the exact required capability.');
  if(!requiredArtifacts.length||requiredArtifacts.some(item=>!item||typeof item!=='object'||Array.isArray(item)||!String(item.source||item.artifactRole||item.filenamePattern||'').trim()))issues.push('Deferred execution plan must deterministically describe every required future artifact.');
  for(const item of requiredArtifacts)if(item&&typeof item==='object'&&!Array.isArray(item)){const unknown=Object.keys(item).filter(key=>!DEFERRED_FAILURE_ARTIFACT_KEYS.includes(key));if(unknown.length)issues.push('Deferred artifact requirement contains unknown properties: '+unknown.join(', ')+'.');}
  if(['CURRENT_PRODUCT','MEANING_REVIEW_TARGET','ADVERSARIAL_TARGET','FINAL_REPRESENTATION'].includes(targetKind)&&!requiredArtifacts.some(item=>upper(item?.source)==='CURRENT_PRODUCT'))issues.push('The deferred product target must bind its future artifacts through source CURRENT_PRODUCT.');
  if(!proofObligationId||!proofObligation)issues.push('Deferred execution plan does not resolve to one current proof obligation.');
  else if(testId&&!scalarList(recordValue(proofObligation,'REQUIRED_TEST_IDS')).includes(testId))issues.push('Deferred canonical test is not frozen in the referenced proof obligation.');
  if(!String(plan?.requiredEvidenceDescription||'').trim())issues.push('Deferred execution plan lacks the required evidence description.');
  if(!String(plan?.expectedRejection||'').trim()||upper(plan?.expectedRejection)!==upper(recordValue(mutation,'EXPECTED_REJECTION')))issues.push('Deferred execution plan expectedRejection must match the preserved mutation expectation.');
  if(upper(plan?.completionCollection)!=='OBSERVATIONRECORDS')issues.push('Deferred completionCollection must be observationRecords so execution is preserved canonically rather than asserted on the Stage 07 definition.');
  const claimedActual=upper(recordValue(mutation,'ACTUAL_RESULT')),claimedOutcome=upper(recordValue(mutation,'EXECUTION_OUTCOME'));if(claimedActual&&!['NOT RUN','NOT_RUN','DEFERRED'].includes(claimedActual)||claimedOutcome&&!['NOT_RUN','DEFERRED'].includes(claimedOutcome))issues.push('A DEFERRED_TARGET_DEPENDENT failure definition cannot claim that execution already occurred.');
  return {mutationId:id,availability,valid:issues.length===0,issues,triggerStage:Number.isSafeInteger(triggerStage)?triggerStage:null,targetKind,route,test,testId,requiredArtifacts,plan,proofObligation,proofObligationId};
}
function deferredFailureTestForCurrentTest(project,testId,atStage=Number(project.activeStage||0),scopeRule=null){const source=scopeRule?recordsForScope(project,'failureTests',scopeRule):recordsForCurrentScope(project,'failureTests'),matches=source.map(mutation=>({mutation,contract:deferredFailureTestContract(project,mutation,{scopeRule})})).filter(item=>item.contract.valid&&item.contract.availability==='DEFERRED_TARGET_DEPENDENT'&&item.contract.testId===String(testId||'')&&Number(atStage)>=Number(item.contract.triggerStage));if(matches.length>1)throw new Error('Multiple current deferred failure tests target the same canonical execution slot '+String(testId||'UNKNOWN')+'.');return matches[0]||null;}
function deferredFailureTestExecutionState(project,mutation,{atStage=Number(project.activeStage||0),scopeRule=null}={}){
  const scopedRecords=collection=>scopeRule?recordsForScope(project,collection,scopeRule):recordsForCurrentScope(project,collection),contract=deferredFailureTestContract(project,mutation,{scopeRule});if(contract.availability!=='DEFERRED_TARGET_DEPENDENT')return {...contract,due:false,complete:true,observationIds:[],entailmentIds:[],reasons:contract.issues};
  const due=contract.valid&&Number(atStage)>=Number(contract.triggerStage),reasons=[...contract.issues];if(!contract.valid)return {...contract,due:false,complete:false,observationIds:[],entailmentIds:[],reasons};
  const mutationId=contract.mutationId,observations=scopedRecords('observationRecords').filter(item=>relationshipIds(item,'MUTATION_ID').includes(mutationId)&&relationshipIds(item,'TEST_ID').includes(contract.testId)),canonicalEvidenceIds=new Set(scopedRecords('evidenceRecords').map(item=>recordId(item,'evidenceRecords'))),currentArtifacts=new Map(scopedRecords('artifacts').map(item=>[recordId(item,'artifacts'),item])),productResolution=['CURRENT_PRODUCT','MEANING_REVIEW_TARGET','ADVERSARIAL_TARGET','FINAL_REPRESENTATION'].includes(contract.targetKind)?currentProductArtifactResolution(project):null,validObservations=[],rejectedObservationReasons=[];
  for(const observation of observations){const observed=recordValue(observation,'NATIVE_OBSERVED_VALUE')||recordValue(observation,'EXTERNAL_OBSERVED_VALUE')||recordValue(observation,'HUMAN_OBSERVED_VALUE')||{},outcome=upper(observed?.executionOutcome||observed?.outcome||''),expected=upper(observed?.expectedRejection||''),inputIdentities=safe(recordValue(observation,'INPUT_IDENTITIES_AND_HASHES')),evidenceIds=[...relationshipIds(observation,'SOURCE_EVIDENCE_IDS'),...safe(observation.evidenceRefs).map(String)],inputById=new Map(inputIdentities.map(item=>[String(item?.artifactId||''),item])),inputIssues=[];
    const native=upper(recordValue(observation,'ORIGIN_CLASS'))==='NATIVE_APPLICATION_OBSERVATION',nativeSatisfied=native&&upper(observed?.determination)==='SATISFIED',externalRejected=outcome==='REJECTED_INVALID'&&expected===upper(recordValue(mutation,'EXPECTED_REJECTION'));if(!nativeSatisfied&&!externalRejected)inputIssues.push('The canonical observation does not prove the preserved invalid fixture was rejected as expected.');
    if(!evidenceIds.length||evidenceIds.some(evidenceId=>!canonicalEvidenceIds.has(evidenceId)))inputIssues.push('The deferred execution observation lacks current canonical evidence.');
    if(productResolution){if(productResolution.issues.length)inputIssues.push(...productResolution.issues);for(const identity of productResolution.identities||[]){const submitted=inputById.get(identity.artifactId),artifact=currentArtifacts.get(identity.artifactId);if(!submitted||!artifact||String(submitted.filename||'')!==identity.filename||Number(submitted.byteSize)!==identity.byteSize||String(submitted.sha256||'').toLowerCase()!==identity.sha256)inputIssues.push('Deferred execution did not bind exact current product artifact '+identity.artifactId+'.');}}
    const entailments=scopedRecords('entailmentReviews').filter(item=>relationshipIds(item,'OBSERVATION_ID').includes(recordId(observation,'observationRecords'))&&relationshipIds(item,'PROOF_OBLIGATION_ID').includes(contract.proofObligationId)&&upper(recordValue(item,'ACCEPTED_RELATION')||recordValue(item,'ENTAILMENT_FINDING'))==='ESTABLISHES'),propositionId=relationshipIds(contract.proofObligation,'PROPOSITION_ID')[0]||null,entailmentState=evaluateObservationEntailment(project,{observation,propositionId,proofObligation:contract.proofObligation,allowedEpistemicBases:scalarList(recordValue(contract.proofObligation,'REQUIRED_EPISTEMIC_BASES')),scopeRule});if(!entailments.length||!entailmentState.sufficient)inputIssues.push(...(entailmentState.reasons.length?entailmentState.reasons:['The deferred execution observation lacks an accepted ESTABLISHES relation to its proof obligation.']));
    if(!inputIssues.length)validObservations.push({observation,entailments});else rejectedObservationReasons.push(...inputIssues.map(reason=>recordId(observation,'observationRecords')+': '+reason));
  }
  if(due&&validObservations.length!==1)reasons.push('Exactly one current sufficient canonical deferred failure-test execution is required when the target becomes available; found '+validObservations.length+'.',...rejectedObservationReasons);
  return {...contract,due,complete:validObservations.length===1,observationIds:validObservations.map(item=>recordId(item.observation,'observationRecords')),entailmentIds:validObservations.flatMap(item=>item.entailments.map(entailment=>recordId(entailment,'entailmentReviews'))),reasons:[...new Set(reasons)]};
}
function deferredFailureTestsDue(project,atStage=Number(project.activeStage||0),scopeRule=null){const source=scopeRule?recordsForScope(project,'failureTests',scopeRule):recordsForCurrentScope(project,'failureTests');return source.map(mutation=>deferredFailureTestExecutionState(project,mutation,{atStage,scopeRule})).filter(state=>state.availability==='DEFERRED_TARGET_DEPENDENT'&&state.due&&!state.complete);}
function canonicalTestBindingCatalog(project){
  ensureShape(project);
  const allowed=['JOB_ID','CURRENT_INPUT_VERSION','CURRENT_SOURCE_SET_VERSION','CURRENT_REQUIREMENTS_VERSION','CURRENT_TEST_SUITE_VERSION','CURRENT_INSTRUCTION_VERSION','CURRENT_ITERATION','CURRENT_BASELINE_ID','CURRENT_PRODUCT_ID'];
  const out={};
  for(const fieldName of allowed){const value=project?.job?.[fieldName];if(value===undefined||value===null||value==='')continue;const canonicalKey=`JOB.${fieldName}`;out[canonicalKey]={canonicalKey,value:clone(value),valueSha256:hash.sha256Value(value)};}
  return out;
}
function evidenceChainExplanation(project,chain){const req=String(recordValue(chain,'REQ_ID')||chain.relationships?.REQ_ID||''),requirement=recordsForCurrentScope(project,'requirements').find(r=>requirementId(r)===req)||records(project,'requirements').find(r=>requirementId(r)===req)||null,tests=safe(recordValue(chain,'TEST_ID')),results=safe(recordValue(chain,'TEST_RESULT_ID')),evidence=safe(recordValue(chain,'EVIDENCE_ID')),identities=safe(recordValue(chain,'ARTIFACT_HASH_IDENTITY')),support=[],unresolved=[...safe(recordValue(chain,'MISSING_LINKS'))],resultCollections=['verification','deterministicResults','meaningResults','adversarialResults'];for(const t of tests)support.push(t+' applies to '+req);for(const id of results){let hit=null;for(const collection of resultCollections){const record=recordsForCurrentScope(project,collection).find(r=>recordId(r,collection)===String(id));if(record){hit={collection,record};break;}}if(!hit){unresolved.push('CURRENT_RESULT:'+id);continue;}const test=testForResult(project,hit.record),effective=effectiveDetermination(hit.collection,hit.record,test,project),contract=evaluateEvidenceContract(test,hit.record,null,project),sufficiency=evaluateEvidenceSufficiency(project,{requirement,test,result:hit.record});support.push('Effective determination for '+id+' is '+effective);if(contract.sufficient&&sufficiency.sufficient)support.push((contract.evidenceIds||sufficiency.presentEvidenceIds||[]).join(', ')+' satisfies the structural evidence contract and is capable of proving the proposition for '+id);else unresolved.push('INSUFFICIENT_EVIDENCE:'+id);if(effective!=='SATISFIED')unresolved.push('NON_SATISFIED_EFFECTIVE_RESULT:'+id);}for(const e of evidence)support.push(e+' is canonical evidence linked to the current proposition');for(const id of identities){const record=recordsForCurrentScope(project,'artifactIdentities').find(x=>recordId(x,'artifactIdentities')===id);if(record&&upper(recordValue(record,'AUTHORIZATION'))==='AUTHORIZED'&&truth(recordValue(record,'EXACT_HASH_MATCH'))&&truth(recordValue(record,'EXACT_SIZE_MATCH')))support.push(id+' proves audited delivery bytes match: '+String(recordValue(record,'PRE_DELIVERY_SHA256')||'UNKNOWN'));else unresolved.push('UNAUTHORIZED_ARTIFACT_IDENTITY:'+id);}return {proposition:'Delivered artifact satisfies '+req,support,unresolved:[...new Set(unresolved)]};}

function stage16CorrectionPlan(project){
  ensureShape(project);
  const roots=recordsForCurrentScope(project,'rootCauses');
  if(!roots.length)return {actionType:'BLOCKED',heading:'Root cause required before correction',explanation:'Stage 16 cannot choose a correction point until Stage 14 has established the earliest defective layer with evidence. Return to root-cause analysis; do not patch the final output by guesswork.',items:[]};
  const items=roots.map(root=>{const defectId=String(recordValue(root,'DEFECT_ID')||root.relationships?.DEFECT_ID||''),earliestDefectiveLayer=String(recordValue(root,'EARLIEST_DEFECTIVE_LAYER')||'').trim(),rootCause=String(recordValue(root,'ROOT_CAUSE')||'').trim(),downstreamInvalidation=String(recordValue(root,'DOWNSTREAM_INVALIDATION')||'').trim(),layer=upper(earliestDefectiveLayer),cause=upper(rootCause),executionOnly=/\bEXECUTION\b|\bRUN\b/.test(layer)&&!/INSTRUCTION|REQUIREMENT|TEST|SOURCE|RESEARCH|CONFIGURATION|INPUT/.test(layer),humanAuthority=/HUMAN|USER INPUT|HUMAN AUTHORITY/.test(layer+' '+cause);if(executionOnly)return {defectId,earliestDefectiveLayer,rootCause,downstreamInvalidation,actionType:'NO_SPECIFICATION_CHANGE',requiredAction:'Preserve the governing requirement, test, and instruction; keep the regression active and rerun the required execution path. Do not invent a specification change.'};if(humanAuthority)return {defectId,earliestDefectiveLayer,rootCause,downstreamInvalidation,actionType:'HUMAN_AUTHORITY',requiredAction:'Confirm only the corrected human-owned fact or decision when requested. The application versions it and invalidates dependent downstream work.'};return {defectId,earliestDefectiveLayer,rootCause,downstreamInvalidation,actionType:'CONTROLLED_CORRECTION',requiredAction:'Correct this earliest defective controlled artifact through the current Stage 16 instruction. Do not patch downstream outputs in place.'};});
  const executionOnly=items.filter(x=>x.actionType==='NO_SPECIFICATION_CHANGE').length,human=items.filter(x=>x.actionType==='HUMAN_AUTHORITY').length,controlled=items.filter(x=>x.actionType==='CONTROLLED_CORRECTION').length,heading=controlled||human?'Correct the established root cause'+(items.length===1?'':'s'):'No specification correction required',explanation=(controlled?controlled+' controlled correction'+(controlled===1?'':'s')+' required. ':'')+(human?human+' human-authority correction'+(human===1?'':'s')+' required. ':'')+(executionOnly?executionOnly+' execution-only defect'+(executionOnly===1?' requires':'s require')+' no specification edit. ':'')+'The application preserves prior versions and determines downstream invalidation and rerun scope.';
  return {actionType:controlled?'CONTROLLED_CORRECTION':human?'HUMAN_AUTHORITY':'NO_SPECIFICATION_CHANGE',heading,explanation,items};
}
function latestOperationValidation(project,stage,operation){
  const prompts=safe(project?.projectData?.generatedPrompts),target=upper(operation),rows=safe(project?.projectData?.responseValidations).filter(item=>Number(item?.stage)===Number(stage)&&!item?.invalidatedBy).filter(item=>{const prompt=prompts.find(candidate=>String(candidate?.instructionId||candidate?.promptId||'')===String(item?.promptId||''));if(prompt?.invalidatedBy)return false;return upper(item?.operation||prompt?.operation||'COMPLETE')===target;});return rows.at(-1)||null;
}
function validationIssueCodes(validation){return new Set(safe(validation?.issues).map(item=>String(item?.code||'')));}
function semanticSuboperationNextOperation(project,stage){
  const number=Number(stage);if(![1,4].includes(number))return null;const closure=stageSuboperationClosure(project,number);if(closure.finalChange)return null;
  if(number===1){const acceptedChallenge=closure.sequence.some(item=>item.operation==='SEMANTIC_CHALLENGE'),codes=validationIssueCodes(latestOperationValidation(project,1,'COMPLETE')),validationRequiresChallenge=['REQUIRED_SEMANTIC_CHALLENGE_MISSING','STAGE01_BROAD_EXCLUSION_UNCHALLENGED','MATERIAL_INTAKE_UNCERTAINTY_UNCHALLENGED','SEMANTIC_PLACEHOLDER_COVERAGE'].some(code=>codes.has(code));if(acceptedChallenge)return 'RECONCILE_INTAKE';if(closure.challengeRequired||validationRequiresChallenge)return 'SEMANTIC_CHALLENGE';return null;}
  const sequence=closure.sequence,codes=validationIssueCodes(latestOperationValidation(project,4,'COMPLETE')),dispositionRequired=closure.requirements.dispositionRequired||codes.has('REQUIRED_STAGE04_DISPOSITION_CHALLENGE_MISSING')||codes.has('UNCHALLENGED_OBLIGATION_DISPOSITION'),atomicityRequired=closure.requirements.atomicityRequired||codes.has('REQUIRED_STAGE04_ATOMICITY_CHALLENGE_MISSING')||codes.has('UNCHALLENGED_OBLIGATION_MERGE'),dispositionAccepted=sequence.some(item=>item.operation==='DISPOSITION_CHALLENGE'),atomicityAccepted=sequence.some(item=>item.operation==='ATOMICITY_CHALLENGE');if(dispositionRequired&&!dispositionAccepted)return 'DISPOSITION_CHALLENGE';if(atomicityRequired&&!atomicityAccepted)return 'ATOMICITY_CHALLENGE';if(dispositionAccepted||atomicityAccepted)return 'RECONCILE_REQUIREMENTS';return null;
}
function acceptedChangeScope(project,change){const proposal=safe(project?.projectData?.responseProposals).find(item=>item.proposalId===change?.proposalId);return {...clone(proposal?.envelope?.scope||proposal?.scope||{}),...clone(change?.scope||{})};}
function iterativeAcceptedChanges(project,stage,iterationId,operation=null){return orderedAcceptedOperations(project,stage).filter(item=>{const scope=acceptedChangeScope(project,item.change);return String(scope.iterationId||'')===String(iterationId||'')&&(!operation||item.operation===upper(operation));});}
function reviewerContextForIteration(project,stage,iterationId,generatorContextId=''){
  const placeholders=new Set(['','NONE','NOT APPLICABLE','UNASSIGNED','PENDING','UNKNOWN']),generator=String(generatorContextId||'');return records(project,'freshContexts').filter(item=>isActiveRecord(item)&&Number(item.stage)===Number(stage)&&String(recordValue(item,'ITERATION_ID')||item.scope?.iterationId||'')===String(iterationId||'')).filter(item=>placeholders.has(upper(recordValue(item,'RUN_ID')||item.scope?.runId||''))&&!placeholders.has(upper(recordValue(item,'EXTERNAL_CONTEXT_IDENTIFIER')))).filter(item=>recordId(item,'freshContexts')!==generator&&String(recordValue(item,'EXTERNAL_CONTEXT_IDENTIFIER')||'')!==generator).at(-1)||null;
}
function comparisonClosure(project,iterationId){
  const scope=scopeForIteration(project,iterationId),requirements=mandatoryRequirements(project,scope),expectedIds=requirements.map(requirementId),rows=recordsForIteration(project,'comparisons',iterationId),counts=new Map();for(const row of rows){const id=String(recordValue(row,'REQ_ID')||row.relationships?.REQ_ID||'');counts.set(id,(counts.get(id)||0)+1);}const missing=expectedIds.filter(id=>!counts.has(id)),duplicates=[...counts].filter(([id,count])=>expectedIds.includes(id)&&count!==1).map(([requirementIdValue,count])=>({requirementId:requirementIdValue,count})),unexpected=rows.filter(row=>!expectedIds.includes(String(recordValue(row,'REQ_ID')||row.relationships?.REQ_ID||'')));return {scope,requirements,expectedIds,rows,missing,duplicates,unexpected,complete:expectedIds.length>0&&!missing.length&&!duplicates.length&&!unexpected.length};
}
function iterationDefectSignals(project,iterationId,matrix,comparison){
  const resultSignals=matrix.verification.filter(record=>effectiveDetermination('verification',record,testForResult(project,record),project)!=='SATISFIED').map(record=>recordId(record,'verification')),comparisonSignals=comparison.rows.filter(record=>truth(recordValue(record,'CORRECTNESS_AFFECTING_VARIANCE'))||adjudicationAdverse(recordValue(record,'PROHIBITED_VARIANCE'))||adjudicationAdverse(recordValue(record,'INCONCLUSIVE_TESTS'))||safe(recordValue(record,'DEFECT_IDS')).length).map(record=>recordId(record,'comparisons')),defectIds=recordsForIteration(project,'defects',iterationId).map(record=>recordId(record,'defects'));return {resultSignals,comparisonSignals,defectIds,required:resultSignals.length>0||comparisonSignals.length>0||defectIds.length>0};
}
function stageSemanticReviewOperation(project,stage){
  const number=Number(stage),scope=currentScope(project),contexts=records(project,'freshContexts',{active:false}).filter(item=>isActiveRecord(item)&&Number(item.stage)===number),authorChange=acceptedOperationChange(project,number,'COMPLETE'),authorContextId=String(authorChange?.scope?.contextId||''),authorContext=authorContextId?contexts.find(item=>recordId(item,'freshContexts')===authorContextId):contexts.find(item=>item.source!=='HUMAN_REVIEWER_CONTEXT')||null,reviewContexts=contexts.filter(item=>item.source==='HUMAN_REVIEWER_CONTEXT'&&recordId(item,'freshContexts')!==recordId(authorContext,'freshContexts')),reviewContext=reviewContexts.at(-1)||null,base=(operation,state,reason,{context=null,targetIds=[],counts={}}={})=>{const contextId=recordId(context,'freshContexts')||'',targetKey=targetIds.length?targetIds.slice().sort().join('|'):operation;return {stage:number,operation,scope:{...scope,...(contextId?{contextId}:{})},targetSlot:`STAGE-${String(number).padStart(2,'0')}-${operation}-${hash.sha256Value(targetKey).slice(0,16).toUpperCase()}`,state,reason,targetIds:targetIds.slice(),counts:{registeredContexts:contexts.length,...counts}};};
  if(!authorChange)return base('COMPLETE',authorContext?'ACTION_REQUIRED':'CONTEXT_REQUIRED',authorContext?'Complete the Stage '+String(number).padStart(2,'0')+' author/compiler operation in this application-selected context.':'Register the real external author/compiler context before generating the Stage '+String(number).padStart(2,'0')+' COMPLETE prompt.',{context:authorContext});
  if(!authorContext)return base('COMPLETE','BLOCKED','The accepted COMPLETE operation is not bound to one current application-recorded author/compiler context.');
  if(number===5){
    const targets=recordsForCurrentScope(project,'applicabilityRecords').filter(item=>controlledApplicability(recordValue(item,'APPLICABILITY_PROPOSAL'))==='NOT_APPLICABLE'||['CONDITIONAL','OPTIONAL'].includes(controlledNormativeClass(recordValue(item,'NORMATIVE_CLASSIFICATION_PROPOSAL')))),pending=[],rejected=[];for(const item of targets){const assessment=applicabilityReviewAssessment(project,item);if(assessment.ready)continue;if(assessment.reviews.length)rejected.push(recordId(item,'applicabilityRecords'));else pending.push(recordId(item,'applicabilityRecords'));}
    const counts={semanticReductionReviewCount:targets.length,notApplicableDispositionCount:targets.filter(item=>controlledApplicability(recordValue(item,'APPLICABILITY_PROPOSAL'))==='NOT_APPLICABLE').length,normativeReductionReviewCount:targets.filter(item=>['CONDITIONAL','OPTIONAL'].includes(controlledNormativeClass(recordValue(item,'NORMATIVE_CLASSIFICATION_PROPOSAL')))).length,pendingApplicabilityReviewCount:pending.length,rejectedApplicabilityReviewCount:rejected.length};if(rejected.length)return base('APPLICABILITY_REVIEW','BLOCKED','An accepted independent semantic-closure review did not accept the exact applicability or normative-classification proposal. Correct it through a new COMPLETE operation; the failed review cannot be bypassed.',{context:reviewContext,targetIds:rejected,counts});if(pending.length)return base('APPLICABILITY_REVIEW',reviewContext?'ACTION_REQUIRED':'CONTEXT_REQUIRED',reviewContext?'Independently review every application-selected applicability exclusion or release-obligation reduction, including its exact classification, subject, reasoning, evidence, and activation binding.':'Register a distinct external applicability and normative-classification reviewer context before generating the application-selected review prompt.',{context:reviewContext,targetIds:pending,counts});const stageResult=gate(5,project);return base('COMPLETE',stageResult.complete?'COMPLETE':'BLOCKED',stageResult.complete?'The accepted Stage 05 author operation and every required independent applicability and classification review satisfy the current semantic-closure gate.':stageResult.reasons.join(' '),{context:authorContext,counts});
  }
  const tests=recordsForCurrentScope(project,'tests').filter(releaseBearingTest),expressions=recordsForCurrentScope(project,'proofExpressions'),pending=[],rejected=[];for(const [kind,items] of [['TEST',tests],['PROOF_EXPRESSION',expressions]])for(const item of items){const assessment=semanticReviewAssessment(project,{subjectKind:kind,subject:item});const id=(kind==='TEST'?'TEST:':'PROOF_EXPRESSION:')+recordId(item,kind==='TEST'?'tests':'proofExpressions');if(assessment.ready)continue;if(assessment.reviews.length)rejected.push(id);else pending.push(id);}
  const counts={releaseBearingTestCount:tests.length,proofExpressionCount:expressions.length,pendingSemanticReviewCount:pending.length,rejectedSemanticReviewCount:rejected.length};if(rejected.length)return base('SEMANTIC_REVIEW','BLOCKED','An accepted independent semantic review did not establish exact EQUIVALENT coverage for the frozen test or proof expression. Correct the Stage 06 semantic content through a new COMPLETE operation; a weaker or adverse review cannot be bypassed.',{context:reviewContext,targetIds:rejected,counts});if(pending.length)return base('SEMANTIC_REVIEW',reviewContext?'ACTION_REQUIRED':'CONTEXT_REQUIRED',reviewContext?'Independently review the exact test proposition, tested scope, result meanings, target proposition, and proof-expression semantics selected by the application.':'Register a distinct external semantic-reviewer context before generating the application-selected review prompt.',{context:reviewContext,targetIds:pending,counts});const stageResult=gate(6,project);return base('SEMANTIC_REVIEW',stageResult.complete?'COMPLETE':'BLOCKED',stageResult.complete?'The accepted Stage 06 compiler operation and every exact independent semantic review satisfy the current proof-definition gate.':stageResult.reasons.join(' '),{context:reviewContext||authorContext,counts});
}
function currentStageOperation(project,stage){
  ensureShape(project);const number=Number(stage);
  if(number===2){
    const searchChange=acceptedOperationChange(project,2,'SEARCH_EXECUTION'),reviewChange=acceptedOperationChange(project,2,'ADEQUACY_REVIEW'),contexts=records(project,'freshContexts',{active:false}).filter(item=>isActiveRecord(item)&&Number(item.stage)===2),searchContext=searchChange?stage2SearchContext(project,searchChange.scope?.contextId).record:contexts.find(item=>item.source!=='HUMAN_REVIEWER_CONTEXT')||null,reviewContext=reviewChange?stage2SearchContext(project,reviewChange.scope?.contextId).record:contexts.find(item=>item.source==='HUMAN_REVIEWER_CONTEXT'&&recordId(item,'freshContexts')!==recordId(searchContext,'freshContexts'))||null,route=(operation,state,reason,context)=>({stage:2,operation,scope:{...currentScope(project),...(context?{contextId:recordId(context,'freshContexts')}:{})},targetSlot:`STAGE-02-${operation}-${hash.sha256Value(recordId(context,'freshContexts')||'UNRESERVED').slice(0,16).toUpperCase()}`,state,reason,counts:{acceptedSearchExecutions:searchChange?1:0,acceptedAdequacyReviews:reviewChange?1:0,registeredContexts:contexts.length}});
    if(!searchChange)return route('SEARCH_EXECUTION',searchContext?'ACTION_REQUIRED':'CONTEXT_REQUIRED',searchContext?'Execute the bounded source-search contract in this application-selected performer context.':'Register the real external source-search performer context before generating the bounded search instruction.',searchContext);
    if(!reviewChange)return route('ADEQUACY_REVIEW',reviewContext?'ACTION_REQUIRED':'CONTEXT_REQUIRED',reviewContext?'Independently review the exact accepted bounded-search result in this distinct application-selected reviewer context.':'Register a distinct external adequacy-reviewer context before generating the review instruction.',reviewContext);
    const assessment=sourceSearchAssessment(project);return route('ADEQUACY_REVIEW',assessment.complete?'COMPLETE':'BLOCKED',assessment.complete?'The distinct accepted search execution and adequacy review reconcile to one complete current Stage 02 search contract.':assessment.reasons.join(' '),reviewContext);
  }
  if([5,6].includes(number))return stageSemanticReviewOperation(project,number);
  if(![17,19].includes(number))return {stage:number,operation:schema.STAGE_CONTRACTS?.[number]?.operations?.[0]||'COMPLETE',scope:currentScope(project),targetSlot:`STAGE-${String(number).padStart(2,'0')}-DEFAULT`,state:'ACTION_REQUIRED',reason:'This stage does not use the repeated-iteration phase controller.',counts:{}};
  const iteration=latestIteration(project,[number]),iterationId=recordId(iteration,'iterations'),expectedPurpose=number===19?'UNCHANGED_CONFIRMATION':null,candidateId=iterationId?iterationCandidateId(project,iterationId):'',scope=iterationId?scopeForIteration(project,iterationId):currentScope(project),base=(operation,state,reason,extra={})=>{const selectedScope={...scope,...clone(extra.scope||{})},slotSuffix=String(extra.targetIdentity||selectedScope.runId||operation),targetSlot=`STAGE-${String(number).padStart(2,'0')}-${String(iterationId||'UNRESERVED')}-${operation}-${hash.sha256Value(slotSuffix).slice(0,16).toUpperCase()}`;return {stage:number,operation,scope:selectedScope,targetSlot,state,reason,counts:clone(extra.counts||{})};};
  if(!iterationId||(expectedPurpose&&upper(recordValue(iteration,'PURPOSE'))!==expectedPurpose)){const operation=number===17?'FREEZE':'CONFIRM_FREEZE';return base(operation,'APPLICATION_CONTROL',number===17?'The corrected candidate and new iteration must be frozen before any execution lane exists.':'The application must bind a new unchanged-confirmation iteration to the exact converged candidate before execution.');}
  const freeze=number===17?records(project,'candidateFreezes').find(item=>isActiveRecord(item)&&Number(item.stage)===17&&recordId(item,'candidateFreezes')===candidateId):records(project,'candidateFreezes').find(item=>isActiveRecord(item)&&recordId(item,'candidateFreezes')===candidateId);if(!freeze)return base(number===17?'FREEZE':'CONFIRM_FREEZE','BLOCKED','The current iteration does not resolve to its required active frozen candidate identity.');
  const runs=recordsForIteration(project,'runs',iterationId).filter(item=>Number(item.stage)===number&&runIterationId(item)===iterationId),runChanges=iterativeAcceptedChanges(project,number,iterationId,'EXECUTE_RUN'),runCounts=new Map();for(const item of runChanges){const runId=String(acceptedChangeScope(project,item.change).runId||'');if(runId)runCounts.set(runId,(runCounts.get(runId)||0)+1);}const duplicateRunIds=[...runCounts].filter(([,count])=>count!==1).map(([runId])=>runId),pendingRuns=runs.filter(run=>runCounts.get(recordId(run,'runs'))!==1||run.completionState!=='COMPLETED'),runSummary={runCount:runs.length,acceptedRunCount:runs.filter(run=>runCounts.get(recordId(run,'runs'))===1).length,pendingRunCount:pendingRuns.length};
  if(!runs.length)return base('EXECUTE_RUN','RESERVE_RUN_BATCH','Exactly ten application-owned run and context identities must be reserved before execution.',{counts:runSummary});if(runs.length!==10||duplicateRunIds.length)return base('EXECUTE_RUN','BLOCKED',runs.length!==10?`The current iteration has ${runs.length} run slots; exactly ten are required.`:'More than one accepted EXECUTE_RUN response controls a current run slot: '+duplicateRunIds.join(', ')+'.',{counts:runSummary});if(pendingRuns.length){const run=pendingRuns[0],runId=recordId(run,'runs'),contextId=String(recordValue(run,'CONTEXT_ID')||run.relationships?.CONTEXT_ID||run.scope?.contextId||''),context=records(project,'freshContexts').find(item=>recordId(item,'freshContexts')===contextId&&isActiveRecord(item)),externalContext=String(recordValue(context,'EXTERNAL_CONTEXT_IDENTIFIER')||''),contextReady=!['','NONE','NOT APPLICABLE','UNASSIGNED','PENDING','UNKNOWN'].includes(upper(externalContext));return base('EXECUTE_RUN',contextReady?'ACTION_REQUIRED':'CONTEXT_REQUIRED',contextReady?'Execute only this application-selected run using its exact frozen candidate and reserved context.':'Register the real fresh external execution-context identifier for this application-selected run before generating its prompt.',{scope:{runId,contextId},targetIdentity:runId,counts:runSummary});}
  const matrix=verificationMatrix(project,iterationId),matrixSummary={...runSummary,requiredRelationCount:matrix.expected.length,verifiedRelationCount:matrix.expected.length-matrix.missing.length,missingRelationCount:matrix.missing.length,duplicateRelationCount:matrix.duplicates.length,invalidRelationCount:matrix.invalid.length};if(!matrix.expected.length)return base('VERIFY','BLOCKED','The current required verification relation set is empty; Stage 06 proof obligations or per-run test relationships are incomplete.',{counts:matrixSummary});if(matrix.duplicates.length||matrix.invalid.length)return base('VERIFY','BLOCKED','The current verification relation set contains duplicate, out-of-scope, non-independent, or insufficiently evidenced records. Invalidate and replace the defective verification response rather than adding another result.',{counts:matrixSummary});if(matrix.missing.length){const tuple=matrix.relationSet.tuples.find(item=>item.key===matrix.missing[0]),run=matrix.runs.find(item=>recordId(item,'runs')===String(tuple?.runId||'')),generatorContextId=String(recordValue(run,'CONTEXT_ID')||run?.relationships?.CONTEXT_ID||run?.scope?.contextId||''),reviewer=reviewerContextForIteration(project,number,iterationId,generatorContextId),verifyScope={runId:String(tuple?.runId||'')};if(reviewer)verifyScope.contextId=recordId(reviewer,'freshContexts');return base('VERIFY',reviewer?'ACTION_REQUIRED':'CONTEXT_REQUIRED',reviewer?'Verify the application-selected missing proposition/requirement/run/test relation in the current independent reviewer context.':'Register a fresh independent verifier context before generating the application-selected verification prompt.',{scope:verifyScope,targetIdentity:tuple?.key||matrix.missing[0],counts:matrixSummary});}
  const comparison=comparisonClosure(project,iterationId),compareChanges=iterativeAcceptedChanges(project,number,iterationId,'COMPARE'),comparisonSummary={...matrixSummary,requiredComparisonCount:comparison.expectedIds.length,comparisonCount:comparison.rows.length,missingComparisonCount:comparison.missing.length};if(!compareChanges.length)return base('COMPARE','ACTION_REQUIRED','Compare the ten completely verified runs against the frozen expected-variance contracts.',{counts:comparisonSummary});if(!comparison.complete)return base('COMPARE','BLOCKED','The accepted COMPARE response does not contain exactly one current comparison for every applicable mandatory requirement.',{counts:comparisonSummary});
  if(number===17){
    const signals=iterationDefectSignals(project,iterationId,matrix,comparison),rootChanges=iterativeAcceptedChanges(project,17,iterationId,'ROOT_CAUSE'),defects=recordsForIteration(project,'defects',iterationId),rootCauses=recordsForIteration(project,'rootCauses',iterationId),rooted=new Set(rootCauses.map(item=>String(recordValue(item,'DEFECT_ID')||item.relationships?.DEFECT_ID||''))),confirmed=defects.filter(item=>!['REJECTED','DUPLICATE','NOT A DEFECT','RETIRED'].includes(upper(recordValue(item,'STATUS')||'CONFIRMED'))),missingRoot=confirmed.filter(item=>!rooted.has(recordId(item,'defects'))),regressions=activeRegressions(project),regExecutions=currentRegressionExecutions(project,iterationId),failedRegressionExecutionIds=regExecutions.filter(execution=>effectiveRegressionDetermination(project,execution).determination!=='SATISFIED').map(execution=>recordId(execution,'regressionExecutions')),defectSignalCount=signals.resultSignals.length+signals.comparisonSignals.length+failedRegressionExecutionIds.length,rootCauseRequired=signals.required||failedRegressionExecutionIds.length>0;
    if(rootCauseRequired&&(!rootChanges.length||!confirmed.length||missingRoot.length))return base('ROOT_CAUSE','ACTION_REQUIRED','Current verification, comparison, or regression evidence exposes a defect signal. Root-cause every resulting confirmed defect at its earliest responsible layer before regression or correction.',{counts:{...comparisonSummary,defectSignalCount,confirmedDefectCount:confirmed.length,missingRootCauseCount:missingRoot.length}});
    const linkedRegressions=new Map();for(const regression of regressions){const defectId=String(recordValue(regression,'DEFECT_ID')||regression.relationships?.DEFECT_ID||'');if(defectId&&!linkedRegressions.has(defectId))linkedRegressions.set(defectId,[]);if(defectId)linkedRegressions.get(defectId).push(regression);}
    const missingRegression=confirmed.filter(defect=>!linkedRegressions.get(recordId(defect,'defects'))?.length),incompleteExecutionIds=regressions.filter(regression=>{const id=recordId(regression,'regressions'),matches=regExecutions.filter(execution=>String(recordValue(execution,'REG_ID')||execution.relationships?.REG_ID||'')===id);return matches.length!==1||effectiveRegressionDetermination(project,matches[0]).determination!=='SATISFIED';}).map(regression=>recordId(regression,'regressions')),regressionRequired=regressions.length>0||confirmed.length>0;
    if(regressionRequired&&(!iterativeAcceptedChanges(project,17,iterationId,'REGRESSION').length||missingRegression.length||incompleteExecutionIds.length))return base('REGRESSION','ACTION_REQUIRED','Execute every active applicable permanent regression exactly once in this corrected iteration, and convert every newly confirmed defect into a permanent regression before progression.',{counts:{...comparisonSummary,confirmedDefectCount:confirmed.length,activeRegressionCount:regressions.length,missingRegressionCount:missingRegression.length,incompleteRegressionExecutionCount:incompleteExecutionIds.length}});
    if(!rootCauseRequired&&!confirmed.length)return base(regressionRequired?'REGRESSION':'COMPARE','COMPLETE','The corrected iteration has ten completed runs, a complete verification relation set, complete comparison coverage, every active regression succeeded, and no current defect signal.',{counts:{...comparisonSummary,activeRegressionCount:regressions.length}});
    const changes=recordsForIteration(project,'changes',iterationId),corrected=new Set(changes.flatMap(item=>scalarList(recordValue(item,'TRIGGERING_DEFECT_IDS')))),missingCorrection=confirmed.filter(defect=>!corrected.has(recordId(defect,'defects')));if(!iterativeAcceptedChanges(project,17,iterationId,'CORRECT').length||missingCorrection.length)return base('CORRECT','ACTION_REQUIRED','Apply the minimal complete correction set for every confirmed root cause and preserve all invalidation consequences.',{counts:{...comparisonSummary,confirmedDefectCount:confirmed.length,missingCorrectionCount:missingCorrection.length}});return base('CORRECT','COMPLETE','Every conditionally required Stage 17 operation is complete for the current corrected iteration.',{counts:{...comparisonSummary,confirmedDefectCount:confirmed.length,activeRegressionCount:regressions.length}});
  }
  const regressionChanges=iterativeAcceptedChanges(project,19,iterationId,'REGRESSION_VERIFY'),regressions=activeRegressions(project),regressionExecutions=currentRegressionExecutions(project,iterationId),regressionMissing=regressions.filter(regression=>{const id=recordId(regression,'regressions'),matches=regressionExecutions.filter(execution=>String(recordValue(execution,'REG_ID')||execution.relationships?.REG_ID||'')===id);return matches.length!==1||effectiveRegressionDetermination(project,matches[0]).determination!=='SATISFIED';});if(!regressionChanges.length||regressionMissing.length)return base('REGRESSION_VERIFY','ACTION_REQUIRED','Run every active applicable regression against the unchanged confirmation candidate and preserve one sufficient current result.',{counts:{...comparisonSummary,activeRegressionCount:regressions.length,incompleteRegressionCount:regressionMissing.length}});const confirmChanges=iterativeAcceptedChanges(project,19,iterationId,'CONFIRM'),confirmations=recordsForIteration(project,'confirmationRecords',iterationId);if(!confirmChanges.length||confirmations.length!==1)return base('CONFIRM','ACTION_REQUIRED','Complete the bounded unchanged-confirmation challenge and return one current confirmation record.',{counts:{...comparisonSummary,confirmationRecordCount:confirmations.length}});return base('CONFIRM','COMPLETE','Every Stage 19 confirmation operation is complete in the required order for the exact unchanged candidate.',{counts:{...comparisonSummary,activeRegressionCount:regressions.length,confirmationRecordCount:confirmations.length}});
}
const ACTION_TYPES=Object.freeze(['RUN_APP_TESTS','CALCULATE_RELEASE','BUILD_EVIDENCE_CHAINS','CREATE_BACKUP_CHECKPOINT','CONFIRM_BACKUP_CUSTODY','CREATE_DELIVERY_RECORD','FREEZE_CANDIDATE','RESERVE_RUN_BATCH','BEGIN_UNCHANGED_CONFIRMATION','FREEZE_BASELINE','REGISTER_PRODUCTION_CONTEXT','RESERVE_PRODUCT_EXECUTION','AI_REVIEW','EXTERNAL_AGENT_TOOL','HUMAN_INSPECTION','EXTERNAL_SYSTEM','ATTACH_REQUIRED_FILES','CONTINUE_AGENT_CONVERSATION','PASTE_FINAL_JSON','REVIEW_PROPOSAL','BLOCKED','COMPLETE']);
function actionReceiptState(project,stage){
  const latestReceipt=safe(project?.projectData?.outputReceipts).filter(item=>Number(item.stage)===Number(stage)).at(-1)||null;
  const latestAccepted=acceptedChanges(project,stage).at(-1)||null;
  return {
    canonicalStateChanged:latestReceipt?.canonicalStateChanged===true||Boolean(latestAccepted&&latestReceipt?.canonicalStateChanged!==false),
    acceptedChange:latestReceipt?.acceptedChange||latestReceipt?.changeId||latestAccepted?.changeId||null,
    downstreamInvalidated:safe(latestReceipt?.downstreamInvalidated||latestReceipt?.invalidatedStages),
    newPromptRequired:Boolean(latestReceipt?.newPromptRequired)
  };
}
function actionEnvelope(project,stage,values={}){
  const receipt=actionReceiptState(project,stage),stageDefinition=core.STAGES?.[Number(stage)-1],operatorChecks=safe(stageDefinition?.humanChecklist||stageDefinition?.completionGate);
  const action={
    actionType:'PASTE_FINAL_JSON',heading:'Complete the current stage',explanation:'Return the current final structured response and any required returned files.',primaryButton:null,secondaryAction:null,
    operation:null,filesToSend:[],filesToWithhold:[],expectedReturnFiles:[],operatorChecks,blockingReason:null,
    ...receipt,...values
  };
  if(!ACTION_TYPES.includes(action.actionType))throw new Error('Unknown operational action type: '+action.actionType+'.');
  action.filesToSend=safe(action.filesToSend);action.filesToWithhold=safe(action.filesToWithhold);action.expectedReturnFiles=safe(action.expectedReturnFiles);action.operatorChecks=safe(action.operatorChecks);action.downstreamInvalidated=safe(action.downstreamInvalidated);
  return action;
}
function iterativeOperationAction(project,route){
  const stage=Number(route.stage),operation=upper(route.operation),handoff=executionHandoff(project,{stage,operation,runIds:route.scope?.runId?[String(route.scope.runId)]:null}),common={operation,scope:clone(route.scope||{}),targetSlot:route.targetSlot,phaseState:route.state,phaseCounts:clone(route.counts||{}),filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack};
  if(route.state==='BLOCKED')return actionEnvelope(project,stage,{...common,actionType:'BLOCKED',heading:'The current iteration phase is blocked',explanation:route.reason,blockingReason:route.reason});
  if(route.state==='COMPLETE')return actionEnvelope(project,stage,{...common,actionType:'COMPLETE',heading:stage===17?'Corrected iteration complete':'Unchanged confirmation complete',explanation:route.reason,primaryButton:null});
  if(route.state==='APPLICATION_CONTROL')return actionEnvelope(project,stage,{...common,actionType:operation==='FREEZE'?'FREEZE_CANDIDATE':'BEGIN_UNCHANGED_CONFIRMATION',heading:operation==='FREEZE'?'Freeze the corrected candidate':'Begin the unchanged confirmation iteration',explanation:route.reason,primaryButton:operation==='FREEZE'?'Freeze corrected candidate':'Begin unchanged confirmation'});
  if(route.state==='RESERVE_RUN_BATCH')return actionEnvelope(project,stage,{...common,actionType:'RESERVE_RUN_BATCH',heading:'Reserve exactly ten current execution lanes',explanation:route.reason,primaryButton:'Reserve 10 runs'});
  if(route.state==='CONTEXT_REQUIRED')return actionEnvelope(project,stage,{...common,actionType:operation==='VERIFY'?'AI_REVIEW':'CONTINUE_AGENT_CONVERSATION',heading:operation==='VERIFY'?'Register the selected independent verifier context':'Register the selected fresh execution context',explanation:route.reason+' No prompt is current for this phase until that real external context identity is registered.',primaryButton:operation==='VERIFY'?'Register verifier context':'Register execution context',contextRegistrationRequired:true,promptAvailable:false,newPromptRequired:true});
  const verificationOperation=['VERIFY','REGRESSION_VERIFY'].includes(operation),heading={EXECUTE_RUN:'Execute the selected iteration run',VERIFY:'Verify the selected run relation independently',COMPARE:'Compare the ten current executions',ROOT_CAUSE:'Root-cause the current defect signals',REGRESSION:'Create and execute the required regressions',CORRECT:'Correct the confirmed root causes',REGRESSION_VERIFY:'Verify every active regression',CONFIRM:'Complete unchanged confirmation'}[operation]||'Complete the selected iteration phase';
  return actionEnvelope(project,stage,{...common,actionType:verificationOperation?'AI_REVIEW':'CONTINUE_AGENT_CONVERSATION',heading,explanation:route.reason,primaryButton:verificationOperation?'Prepare independent review':'Continue selected phase',promptAvailable:true,newPromptRequired:true});
}
function operationalNextAction(project,currentStage){
  const stage=Number(currentStage||1),requests=unresolvedHumanRequests(project,stage);
  const proposals=safe(project?.projectData?.responseProposals).filter(item=>Number(item.stage)===stage&&!item.invalidatedBy&&['PENDING','PENDING_OPERATOR_REVIEW'].includes(upper(item.status||item.state)));
  if(proposals.length){const proposal=proposals.at(-1);return actionEnvelope(project,stage,{operation:upper(proposal?.envelope?.operation||proposal?.operation||'COMPLETE'),actionType:'REVIEW_PROPOSAL',heading:'Review the proposed canonical change',explanation:'Validation succeeded. Review the current value beside the proposed value, provenance, and validation result before accepting or rejecting this proposal.',primaryButton:'Review proposal'});}
  if(requests.length){const request=requests.at(-1);return actionEnvelope(project,stage,{operation:upper(request?.operation||'COMPLETE'),actionType:'CONTINUE_AGENT_CONVERSATION',heading:'Answer the current human-only question',explanation:'Continue the human conversation. Saving an answer creates a new User Job Input version, invalidates the old prompt and proposal, and requires a replacement prompt for this same stage.',primaryButton:'Continue conversation',newPromptRequired:true});}
  const semanticOperation=semanticSuboperationNextOperation(project,stage);if(semanticOperation){const handoff=executionHandoff(project,{stage,operation:semanticOperation});if(semanticOperation==='SEMANTIC_CHALLENGE')return actionEnvelope(project,stage,{operation:semanticOperation,actionType:'AI_REVIEW',heading:'Run the independent Stage 01 omission challenge',explanation:'The latest complete intake response triggered a bounded independent semantic challenge. Open the application-reserved fresh challenge context and send the exact raw input and required files only. The first extraction is withheld; the user does not choose or configure this technical suboperation.',primaryButton:'Prepare semantic challenge',filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack});if(semanticOperation==='RECONCILE_INTAKE')return actionEnvelope(project,stage,{operation:semanticOperation,actionType:'CONTINUE_AGENT_CONVERSATION',heading:'Reconcile the Stage 01 intake',explanation:'The independent omission challenge is accepted. Use the application-generated reconciliation instruction to resolve every material difference against the first extraction. This reconciliation, not the challenge alone, becomes the final Stage 01 operation.',primaryButton:'Prepare intake reconciliation',filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack});if(semanticOperation==='DISPOSITION_CHALLENGE')return actionEnvelope(project,stage,{operation:semanticOperation,actionType:'AI_REVIEW',heading:'Challenge the Stage 04 dispositions independently',explanation:'The first requirement compilation contains a release-reducing disposition or classification. The application selected the independent disposition-challenge suboperation; the operator does not choose or self-approve it.',primaryButton:'Prepare disposition challenge'});if(semanticOperation==='ATOMICITY_CHALLENGE')return actionEnvelope(project,stage,{operation:semanticOperation,actionType:'AI_REVIEW',heading:'Challenge Stage 04 atomicity independently',explanation:'The first requirement compilation merges obligations or otherwise requires an atomicity challenge. The application selected the independent atomicity-challenge suboperation; the operator does not choose or self-approve it.',primaryButton:'Prepare atomicity challenge'});return actionEnvelope(project,stage,{operation:'RECONCILE_REQUIREMENTS',actionType:'CONTINUE_AGENT_CONVERSATION',heading:'Reconcile the Stage 04 requirement compilation',explanation:'Every required independent Stage 04 challenge is accepted. Use the application-generated reconciliation instruction to resolve all material differences. Challenge responses alone cannot complete Stage 04.',primaryButton:'Prepare requirement reconciliation'});}
  if([5,6].includes(stage)){const route=currentStageOperation(project,stage),review=route.operation==='APPLICABILITY_REVIEW'||route.operation==='SEMANTIC_REVIEW',common={operation:route.operation,scope:clone(route.scope||{}),targetSlot:route.targetSlot,targetIds:clone(route.targetIds||[]),phaseState:route.state,phaseCounts:clone(route.counts||{}),newPromptRequired:route.state!=='COMPLETE'};if(route.state==='CONTEXT_REQUIRED')return actionEnvelope(project,stage,{...common,actionType:review?'AI_REVIEW':'CONTINUE_AGENT_CONVERSATION',heading:review?(stage===5?'Register the independent applicability reviewer':'Register the independent semantic reviewer'):'Register the author/compiler context',explanation:route.reason+' No prompt is current until the real external context identity is registered.',primaryButton:review?'Register reviewer context':'Register author context',contextRegistrationRequired:true,promptAvailable:false});if(route.state==='BLOCKED')return actionEnvelope(project,stage,{...common,actionType:'BLOCKED',heading:review?'Semantic review is blocked':'Semantic closure is blocked',explanation:route.reason,blockingReason:route.reason});if(route.state==='COMPLETE')return actionEnvelope(project,stage,{...common,actionType:'COMPLETE',heading:stage===5?'Requirement set semantically closed':'Verification proof definitions semantically closed',explanation:route.reason,primaryButton:null});return actionEnvelope(project,stage,{...common,actionType:review?'AI_REVIEW':'CONTINUE_AGENT_CONVERSATION',heading:review?(stage===5?'Review applicability exclusions independently':'Review exact test and proof semantics independently'):(stage===5?'Resolve the requirement set':'Build the verification suite'),explanation:route.reason,primaryButton:review?'Prepare independent review':'Prepare author instruction',promptAvailable:true});}
  if(stage===2){const route=currentStageOperation(project,2),common={operation:route.operation,scope:clone(route.scope||{}),targetSlot:route.targetSlot,phaseState:route.state,phaseCounts:clone(route.counts||{}),newPromptRequired:route.state!=='COMPLETE'};if(route.state==='CONTEXT_REQUIRED')return actionEnvelope(project,stage,{...common,actionType:'BLOCKED',heading:route.operation==='SEARCH_EXECUTION'?'Register the source-search performer context':'Register the independent adequacy-reviewer context',explanation:route.reason+' Use the Stage 02 supporting-record control; no prompt is current until the real external context identity is registered.',blockingReason:route.reason,contextRegistrationRequired:true});if(route.state==='BLOCKED')return actionEnvelope(project,stage,{...common,actionType:'BLOCKED',heading:'Stage 02 reconciliation is blocked',explanation:route.reason,blockingReason:route.reason});if(route.state==='COMPLETE')return actionEnvelope(project,stage,{...common,actionType:'COMPLETE',heading:'Bounded source inventory complete',explanation:route.reason,primaryButton:null});return actionEnvelope(project,stage,{...common,actionType:route.operation==='ADEQUACY_REVIEW'?'AI_REVIEW':'CONTINUE_AGENT_CONVERSATION',heading:route.operation==='ADEQUACY_REVIEW'?'Review bounded-search adequacy independently':'Execute the bounded source search',explanation:route.reason,primaryButton:route.operation==='ADEQUACY_REVIEW'?'Prepare independent adequacy review':'Prepare bounded search instruction'});}
  if([17,19].includes(stage))return iterativeOperationAction(project,currentStageOperation(project,stage));
  if(stage===16){const correction=stage16CorrectionPlan(project);if(correction.actionType==='BLOCKED')return actionEnvelope(project,stage,{actionType:'BLOCKED',heading:correction.heading,explanation:correction.explanation,blockingReason:correction.explanation});if(correction.actionType==='HUMAN_AUTHORITY')return actionEnvelope(project,stage,{actionType:'CONTINUE_AGENT_CONVERSATION',heading:correction.heading,explanation:correction.explanation,primaryButton:'Provide human authority'});return actionEnvelope(project,stage,{actionType:'REVIEW_PROPOSAL',heading:correction.heading,explanation:correction.explanation,primaryButton:'Review correction'});}

  if(stage===10&&acceptedChanges(project,10).length){
    const freezes=recordsForCurrentScope(project,'candidateFreezes').filter(r=>Number(r.stage)===10&&isActiveRecord(r));
    if(!freezes.length)return actionEnvelope(project,stage,{actionType:'FREEZE_CANDIDATE',heading:'Freeze the exact test candidate',explanation:'Select the exact verified component files for this candidate. The application allocates iteration/candidate identities, hashes the actual bytes, freezes the component set, and prevents in-place mutation. No additional agent response belongs to this control step.',primaryButton:'Freeze candidate'});
  }
  if(stage===11){
    const iteration=latestIteration(project,[10]),iterationId=recordId(iteration,'iterations');
    if(iterationId){const runs=records(project,'runs').filter(r=>isActiveRecord(r)&&String(recordValue(r,'ITERATION_ID')||r.relationships?.ITERATION_ID||r.scope?.iterationId||'')===iterationId);if(!runs.length)return actionEnvelope(project,stage,{actionType:'RESERVE_RUN_BATCH',heading:'Reserve the ten independent execution lanes',explanation:'The application must create exactly ten current run identities and ten fresh context identities for the frozen candidate before any run prompt is sent. You do not allocate run IDs or context IDs.',primaryButton:'Reserve 10 runs'});if(runs.length!==10)return actionEnvelope(project,stage,{actionType:'BLOCKED',heading:'Run reservation is inconsistent',explanation:'The current iteration contains '+runs.length+' reserved run records instead of exactly ten. Do not create ad hoc runs or reuse contexts.',blockingReason:'Exactly ten application-reserved run lanes are required.'});}
  }
  if(stage===20&&acceptedChanges(project,20).length){
    const baselines=recordsForCurrentScope(project,'baselines').filter(isActiveRecord);
    if(!baselines.length)return actionEnvelope(project,stage,{actionType:'FREEZE_BASELINE',heading:'Authorize and freeze the production baseline',explanation:'Select the exact approved component files. The application binds them to the successful unchanged confirmation, computes byte identities, allocates the baseline identity, and freezes the application-immutable baseline. Stage 20 remains incomplete until the resulting baseline is included in a complete restore-tested backup.',primaryButton:'Freeze baseline'});
    const backup=backupCheckpointStatus(project,'BASELINE_APPROVAL');if(!backup.complete)return backup.custodyPending?actionEnvelope(project,stage,{actionType:'CONFIRM_BACKUP_CUSTODY',heading:'Confirm external custody of the exact baseline backup',explanation:'The application generated and restore-tested the exact package, but starting a browser download is not proof that it was saved. Confirm only after the identified package is stored outside this browser. The acknowledgment is recorded honestly as self-asserted human custody evidence.',primaryButton:'Confirm backup stored',pendingExportId:backup.pendingExportId}):actionEnvelope(project,stage,{actionType:'CREATE_BACKUP_CHECKPOINT',heading:'Create and restore-test the baseline backup',explanation:'The authorized baseline now exists. The application must generate a complete backup containing that baseline and its actual artifact bytes, bind the actual package SHA-256 and byte length, and verify the real restore path. Download initiation will not complete the checkpoint.',primaryButton:'Create baseline backup'});
  }
  if(stage===21){
    const productionContext=records(project,'freshContexts').filter(r=>isActiveRecord(r)&&Number(r.stage)===21).at(-1);
    if(!productionContext)return actionEnvelope(project,stage,{actionType:'REGISTER_PRODUCTION_CONTEXT',heading:'Register a fresh production context',explanation:'Open a fresh external production conversation/context. Enter its identifier in the Stage 21 supporting-record control; the application binds it before creating the product execution.',primaryButton:'Register production context'});
    const products=recordsForCurrentScope(project,'products').filter(isActiveRecord);
    if(!products.length)return actionEnvelope(project,stage,{actionType:'RESERVE_PRODUCT_EXECUTION',heading:'Reserve the finished-product execution',explanation:'The application must allocate product/execution identities and bind them to the approved baseline and fresh production context before the Stage 21 production instruction is sent.',primaryButton:'Reserve product execution'});
  }

  if([9,23,24].includes(stage)){const reviewer=records(project,'freshContexts').filter(r=>isActiveRecord(r)&&Number(r.stage)===stage).at(-1);if(!reviewer){const handoff=executionHandoff(project,{stage,operation:'COMPLETE'}),preflight=stage===9;return actionEnvelope(project,stage,{actionType:'AI_REVIEW',heading:'Open a fresh independent reviewer context',explanation:preflight?'Use a new reviewer context that did not author the Stage 08 production instruction. Register its external context identifier before sending the Stage 09 preflight instruction so the application can bind the reviewer identity into the current prompt.':'Use a reviewer context that did not generate the product. Register its external context identifier before sending any product or review material so the application can bind and evaluate independence.',primaryButton:'Register reviewer context',filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack});}}
  const plan=testExecutionPlan(project),allRelevant=stage===12?plan.items:stage===22?plan.items.filter(item=>item.testType==='DETERMINISTIC'):stage===23?plan.items.filter(item=>item.testType==='MEANING'):stage===24?plan.items.filter(item=>item.testType==='ADVERSARIAL'):[],resultCollection=stage===22?'deterministicResults':stage===23?'meaningResults':stage===24?'adversarialResults':null,completedTestIds=resultCollection?new Set(recordsForCurrentScope(project,resultCollection).map(record=>String(recordValue(record,'TEST_ID')||record.relationships?.TEST_ID||'')).filter(Boolean)):new Set(),relevant=resultCollection?allRelevant.filter(item=>!completedTestIds.has(String(item.testId))):allRelevant;
  if(resultCollection&&allRelevant.length&&!relevant.length){const currentResults=recordsForCurrentScope(project,resultCollection),problems=currentResults.map(result=>evaluateResultConsistency(resultCollection,result,testForResult(project,result),project)).filter(item=>item.determination!=='SATISFIED');if(problems.length)return actionEnvelope(project,stage,{actionType:'BLOCKED',heading:'Current verification found a failure',explanation:'Every applicable '+(stage===22?'deterministic':stage===23?'meaning-based':'adversarial')+' test has a current result, but at least one current result is violated, undetermined, contradictory, or insufficiently evidenced. Preserve that result as failure evidence and return through root-cause and controlled correction; do not rerun or overwrite it merely to make the failure disappear.',blockingReason:problems.flatMap(item=>item.reasons||[]).join('; ')||'Current verification is not fully satisfied.'});}
  const blocked=relevant.find(item=>!item.executableNow);
  if(blocked){const missingBytes=!blocked.artifactReady||/artifact|bytes?|file/i.test(String(blocked.blockingReason||'')),handoff=blocked.handoff||{send:[],withhold:[],expectBack:[]};return actionEnvelope(project,stage,{actionType:missingBytes?'ATTACH_REQUIRED_FILES':'BLOCKED',heading:missingBytes?'Attach the exact required files':'Verification is blocked',explanation:blocked.blockingReason||'The declared verification route is not executable with the current capability and artifact state.',primaryButton:missingBytes?'Attach required files':null,filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack,blockingReason:blocked.blockingReason||'Required execution evidence is not currently obtainable.'});}
  const native=relevant.find(item=>item.executionMode==='APPLICATION_DETERMINISTIC'&&item.executableNow&&(item.operatorAction==='RUN_IN_APP'||item.operatorAction==='NO_ACTION'));
  if(native){const handoff=native.handoff||{send:[],withhold:[],expectBack:[]};return actionEnvelope(project,stage,{actionType:'RUN_APP_TESTS',heading:'Run application verification',explanation:'No external action is required for the current supported deterministic test. The application will execute the registered Test IR against the exact verified inputs and record an application-owned result.',primaryButton:'Run tests',filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack});}
  const item=relevant.find(item=>item.operatorAction!=='NO_ACTION');
  if(item){const handoff=item.handoff||{send:[],withhold:[],expectBack:[]},common={filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack};if(item.operatorAction==='SEND_TO_INDEPENDENT_REVIEWER')return actionEnvelope(project,stage,{...common,actionType:'AI_REVIEW',heading:'Send to a fresh independent reviewer',explanation:'Prepare the exact bounded verification package, send only the listed material, withhold the listed context, and return the contracted structured result and evidence.',primaryButton:'Prepare verification package'});if(item.operatorAction==='SEND_TO_TOOL_AGENT')return actionEnvelope(project,stage,{...common,actionType:'EXTERNAL_AGENT_TOOL',heading:'Run verification with the required external tool capability',explanation:'Use an external tool-capable environment that actually provides '+(item.requiredCapability||'the declared capability')+'. Send the exact listed files and return the contracted evidence.',primaryButton:'Prepare verification package'});if(item.operatorAction==='HUMAN_INSPECTION'){const recorded=recordsForCurrentScope(project,'evidenceRecords').find(e=>e?.source==='HUMAN_OBSERVATION'&&String(e?.humanInspectionTestId||'')===item.testId);if(recorded)return actionEnvelope(project,stage,{...common,actionType:'PASTE_FINAL_JSON',heading:'Human observation recorded — return the structured result',explanation:'The application has preserved the human-owned observation for '+item.testId+'. The current stage executor may now use that exact evidence to return the contracted final JSON; it must not invent, replace, or relabel the human observation.',primaryButton:'Paste final JSON'});return actionEnvelope(project,stage,{...common,actionType:'HUMAN_INSPECTION',heading:'Human inspection is required',explanation:'Perform the exact inspection for '+item.testId+' and record the observation directly in the application. An AI assertion that a human inspected the product is insufficient.',primaryButton:'Record human observation',testId:item.testId,requirementId:item.requirementId,requiredEvidenceDescription:item.returnRequirements?.requiredEvidenceDescription||''});}if(item.operatorAction==='USE_EXTERNAL_SYSTEM')return actionEnvelope(project,stage,{...common,actionType:'EXTERNAL_SYSTEM',heading:'Use the required external system',explanation:'Perform the declared operation in '+(item.requiredCapability||'the required external system')+' and return evidence attributable to that system.',primaryButton:'Prepare external-system package'});}
  if(stage===1){const handoff=executionHandoff(project,{stage:1,operation:'COMPLETE'});return actionEnvelope(project,stage,{operation:'COMPLETE',actionType:'CONTINUE_AGENT_CONVERSATION',heading:'Complete the one-time Stage 01 intake',explanation:'Send the current Stage 01 instruction and every listed supplied project file to the same external conversation once. The agent must exhaustively capture the project intent and all human-authority content from those materials, ask only genuinely human-only questions, and return final JSON only when intake is complete. Later stages reuse the accepted Stage 01 capture and must not require these original project files again.',primaryButton:'Continue conversation',filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack});}
  if(stage===27&&acceptedChanges(project,27).length&&!recordsForCurrentScope(project,'releaseRecords').length)return actionEnvelope(project,stage,{actionType:'CALCULATE_RELEASE',heading:'Calculate product release eligibility',explanation:'The advisory Stage 27 review is accepted. No agent or human may select eligibility. The application must now evaluate the complete current proof-obligation registry, evidence, blockers, violations, unknowns, contradictions, dependencies, baseline identity, and product identity and record exactly one idempotent ACCEPTED, REJECTED, or BLOCKED eligibility determination. ACCEPTED means eligible for final delivery checks, not delivered.',primaryButton:'Calculate eligibility'});
  if(stage===28)return actionEnvelope(project,stage,{actionType:'ATTACH_REQUIRED_FILES',heading:'Verify the exact delivery bytes',explanation:'No external agent response belongs to Stage 28. Select the audited canonical files and the exact operator-selected delivery files, then let the application hash and compare them one-to-one. This verifies identity and records human delivery intent; delivery is not yet authorized.',primaryButton:null,blockingReason:null});
  if(stage===29){const obligations=deriveProofObligationRegistry(project).obligations,chains=recordsForCurrentScope(project,'evidenceChains'),chainObligationIds=new Set(chains.map(record=>String(recordValue(record,'PROOF_OBLIGATION_ID')||record.relationships?.PROOF_OBLIGATION_ID||''))),missing=obligations.filter(obligation=>!chainObligationIds.has(recordId(obligation,'proofObligations')));if(!chains.length||missing.length)return actionEnvelope(project,stage,{actionType:'BUILD_EVIDENCE_CHAINS',heading:'Construct the current evidence chains',explanation:'The application will build one current, acyclic, sufficient chain for every proof obligation from the exact proposition, applicability, proof expression, observations, entailments, evidence, eligibility, and artifact-identity records. Delivery is not authorized at Stage 29. If a chain remains incomplete, the Stage 29 agent may investigate only the missing evidence.',primaryButton:'Build evidence chains'});}
  if(stage===30&&acceptedChanges(project,30).length){const terminal=terminalDeliveryState(project,{forCreation:true});if(!terminal.currentDelivery)return terminal.eligibleToCreate?actionEnvelope(project,stage,{actionType:'CREATE_DELIVERY_RECORD',heading:'Create the terminal delivery record',explanation:'Every pre-delivery prerequisite is satisfied. The application will create the idempotent DELIVERY_RECORD first. Delivery remains ineffective until a later exact backup containing that record is restore-tested and its external custody is explicitly acknowledged.',primaryButton:'Create delivery record'}):actionEnvelope(project,stage,{actionType:'BLOCKED',heading:'Final delivery remains blocked',explanation:terminal.reasons.join(' '),blockingReason:terminal.reasons.join(' ')});if(!terminal.backupCheckpointStatus.complete){const backup=terminal.backupCheckpointStatus;if(backup.custodyPending)return actionEnvelope(project,stage,{actionType:'CONFIRM_BACKUP_CUSTODY',heading:'Confirm external custody of the exact post-delivery backup',explanation:'The generated final package contains the current DELIVERY_RECORD and passed the application restore path. Browser download initiation is not proof of persistence. Confirm only after the identified exact bytes are stored outside this browser; delivery remains ineffective until this acknowledgment commits.',primaryButton:'Confirm final backup stored',pendingExportId:backup.pendingExportId});return actionEnvelope(project,stage,{actionType:'CREATE_BACKUP_CHECKPOINT',heading:'Create the post-delivery backup',explanation:'The current DELIVERY_RECORD exists, but final delivery is not effective. Generate and restore-test the exact package now so it includes that record and every authorized artifact byte; the application will bind the actual compressed package digest and byte length.',primaryButton:'Create final backup'});}if(terminal.authorized)return actionEnvelope(project,stage,{actionType:'COMPLETE',heading:'Final delivery authorized',explanation:'The current DELIVERY_RECORD, exact post-delivery backup, successful restore proof, and self-asserted external-custody acknowledgment are all current. The authorization is bound to the exact authorized bytes.',primaryButton:null});return actionEnvelope(project,stage,{actionType:'BLOCKED',heading:'Final delivery remains blocked',explanation:terminal.reasons.join(' '),blockingReason:terminal.reasons.join(' ')});}
  if(stage===4)return actionEnvelope(project,stage,{operation:'COMPLETE',actionType:'PASTE_FINAL_JSON',heading:'Compile the complete requirement specification',explanation:'Use the current Stage 04 instruction. It already contains the exhausted Stage 01 human-authority capture, the complete Stage 03 research, and the application obligation manifest. Do not reattach or restate the original project-intent file. Return one final strict JSON response only after every obligation in the manifest has been accounted for.',primaryButton:'Paste final JSON'});
  return actionEnvelope(project,stage,{actionType:'PASTE_FINAL_JSON',heading:'Return the final structured response',explanation:'Use the current Stage '+String(stage).padStart(2,'0')+' instruction. Continue the external work or conversation until the executor is ready to return one final strict JSON response and any required files.',primaryButton:'Paste final JSON'});
}

function deterministicRuntime(){return globalThis.closedLoopTestRuntime||null;}
function applicationTestCapabilities(){const runtime=deterministicRuntime();return runtime?.capabilities?runtime.capabilities():Object.freeze([]);}
function applicationTestSupported(test){const runtime=deterministicRuntime();return Boolean(runtime?.supports&&runtime.supports(test));}
const TEST_EXECUTION_ACTIONS=Object.freeze({
  APPLICATION_DETERMINISTIC:'No operator execution is required only when the exact REQUIRED_CAPABILITY names a registered application-native test executor. Otherwise request a corrected test definition; the browser must not pretend it can run the test.',
  EXTERNAL_AGENT_TOOL:'Use the generated verification instruction in an external agent/tool environment that actually has the declared capability and exact required artifacts, then ingest its structured result and evidence.',
  INDEPENDENT_AGENT_REVIEW:'Use a fresh independent reviewer context with the declared inputs and evidence; do not reuse the producing context as its own verifier.',
  HUMAN_INSPECTION:'Perform only the irreducible human/domain inspection described by the test, preserve the observation/evidence, and make that evidence available to the verification step.',
  EXTERNAL_SYSTEM:'Obtain the result from the declared specialized system, lab, machine, or software; preserve exact report/output evidence and any returned files.',
  UNAVAILABLE:'The required capability is unavailable. Do not claim execution; this remains blocking until a valid capability or equivalent verification path exists.'
});
function capabilityAffirmativelyAvailable(project,requiredCapability,mode,test){
  const capability=upper(requiredCapability);if(!capability)return false;
  if(mode==='APPLICATION_DETERMINISTIC')return applicationTestSupported(test);
  if(['INDEPENDENT_AGENT_REVIEW','HUMAN_INSPECTION'].includes(mode))return true;
  if(mode==='UNAVAILABLE')return false;
  const declarations=[project?.job?.AVAILABLE_TOOLS,...recordsForCurrentScope(project,'freshContexts').map(record=>recordValue(record,'TOOL_AVAILABILITY'))],negative=/\b(?:UNAVAILABLE|NOT AVAILABLE|NO ACCESS|MISSING|ABSENT|UNKNOWN)\b/;
  return declarations.some(value=>{const text=upper(typeof value==='string'?value:JSON.stringify(value??''));return text.includes(capability)&&!negative.test(text);});
}
function testRequiresCurrentProduct(test){
  const testedScope=recordValue(test,'TESTED_SCOPE'),bindings=recordValue(test,'EXECUTABLE_INPUT_BINDINGS')||{},scopeText=upper(typeof testedScope==='string'?testedScope:JSON.stringify(testedScope||{})),bindingText=upper(JSON.stringify(bindings||{})),artifactText=upper(recordValue(test,'ARTIFACT_REQUIREMENTS')||''),inputText=upper(recordValue(test,'INPUTS')||'');
  return scopeText.includes('CURRENT_PRODUCT')||bindingText.includes('CURRENT_PRODUCT')||/\b(?:CURRENT|FINISHED|FINAL|DELIVERED)\s+PRODUCT\b/.test(artifactText+' '+inputText)||/\bPRODUCT\s+BYTES?\b/.test(artifactText+' '+inputText);
}
function currentProductArtifactResolution(project){
  ensureShape(project);const productId=String(project.job?.CURRENT_PRODUCT_ID||'').trim(),issues=[];
  if(!productId||['NONE','NOT APPLICABLE','UNKNOWN','PENDING','UNASSIGNED'].includes(upper(productId)))return {productId:null,product:null,artifactIds:[],artifacts:[],issues:['No current finished-product identity exists.'],identitySha256:null};
  const product=recordsForCurrentScope(project,'products').find(item=>recordId(item,'products')===productId&&isActiveRecord(item));
  if(!product)return {productId,product:null,artifactIds:[],artifacts:[],issues:['The current finished-product identity does not resolve to one active current-scope product.'],identitySha256:null};
  const inventory=[...new Set(safe(recordValue(product,'GENERATED_ARTIFACT_INVENTORY')).map(String).map(value=>value.trim()).filter(Boolean))];
  if(!inventory.length)issues.push('The current finished product has no application-controlled artifact inventory.');
  const currentArtifacts=recordsForCurrentScope(project,'artifacts'),byId=new Map(currentArtifacts.map(item=>[recordId(item,'artifacts'),item])),artifacts=[];
  for(const artifactId of inventory){const artifact=byId.get(artifactId);if(!artifact){issues.push('Current product artifact '+artifactId+' does not resolve to one active current-scope artifact.');continue;}const boundProductId=String(artifact.scope?.productId||recordValue(artifact,'PRODUCT_ID')||artifact.relationships?.PRODUCT_ID||'').trim();if(boundProductId!==productId){issues.push('Current product artifact '+artifactId+' is not bound to current product '+productId+'.');continue;}if(upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||!Number.isSafeInteger(Number(recordValue(artifact,'BYTE_SIZE')))||Number(recordValue(artifact,'BYTE_SIZE'))<0||!/^[a-f0-9]{64}$/i.test(String(recordValue(artifact,'SHA256')||'')))issues.push('Current product artifact '+artifactId+' does not have application-verified stored bytes, byte size, and SHA-256.');artifacts.push(artifact);}
  const identities=artifacts.map(item=>({artifactId:recordId(item,'artifacts'),productId,filename:String(recordValue(item,'FILENAME')||''),version:String(recordValue(item,'VERSION')||''),byteSize:Number(recordValue(item,'BYTE_SIZE')),sha256:String(recordValue(item,'SHA256')||'').toLowerCase()}));
  return {productId,product,artifactIds:identities.map(item=>item.artifactId),artifacts,identities,issues:[...new Set(issues)],identitySha256:identities.length&&!issues.length?hash.sha256Value(identities):null};
}
function testExecutionPlan(project){
  ensureShape(project);const evidenceById=new Map(records(project,'evidenceRecords').map(item=>[recordId(item,'evidenceRecords'),item])),artifactsById=new Map(records(project,'artifacts').map(item=>[recordId(item,'artifacts'),item])),currentArtifacts=recordsForCurrentScope(project,'artifacts'),currentArtifactById=new Map(currentArtifacts.map(a=>[recordId(a,'artifacts'),a])),activeStage=Number(project.activeStage||0),currentProductId=String(project.job?.CURRENT_PRODUCT_ID||''),productResolution=currentProductArtifactResolution(project);
  const executorMap={APPLICATION_DETERMINISTIC:'APPLICATION',EXTERNAL_AGENT_TOOL:'EXTERNAL_AGENT_TOOL',INDEPENDENT_AGENT_REVIEW:'INDEPENDENT_REVIEWER',HUMAN_INSPECTION:'HUMAN',EXTERNAL_SYSTEM:'EXTERNAL_SYSTEM',UNAVAILABLE:'UNAVAILABLE'},actionMap={APPLICATION_DETERMINISTIC:'RUN_IN_APP',EXTERNAL_AGENT_TOOL:'SEND_TO_TOOL_AGENT',INDEPENDENT_AGENT_REVIEW:'SEND_TO_INDEPENDENT_REVIEWER',HUMAN_INSPECTION:'HUMAN_INSPECTION',EXTERNAL_SYSTEM:'USE_EXTERNAL_SYSTEM',UNAVAILABLE:'BLOCKED'};
  const bindingArtifacts=test=>{const bindings=recordValue(test,'EXECUTABLE_INPUT_BINDINGS')||{},resolved=[],issues=[];for(const [name,binding] of Object.entries(bindings)){let matches=[];if(typeof binding==='string')matches=currentArtifacts.filter(a=>recordId(a,'artifacts')===binding);else if(binding&&typeof binding==='object'){matches=currentArtifacts.filter(a=>{if(binding.artifactId&&recordId(a,'artifacts')!==String(binding.artifactId))return false;if(binding.filename&&String(recordValue(a,'FILENAME')||'')!==String(binding.filename))return false;if(binding.artifactRole&&String(recordValue(a,'ROLE')||'')!==String(binding.artifactRole))return false;if(binding.source==='CURRENT_PRODUCT'&&String(a.scope?.productId||'')!==currentProductId)return false;return true;});if(binding.source==='CURRENT_PRODUCT'&&!currentProductId&&activeStage<21)continue;}if(matches.length===1)resolved.push(recordId(matches[0],'artifacts'));else if(activeStage>=22||typeof binding==='string')issues.push(`${name} resolves to ${matches.length} verified current artifacts`);}return {artifactIds:[...new Set(resolved)],issues};};
  const items=recordsForCurrentScope(project,'tests').map(test=>{const mode=upper(recordValue(test,'EXECUTION_MODE'))||'UNSPECIFIED',requiredCapability=String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim(),artifactRequirements=String(recordValue(test,'ARTIFACT_REQUIREMENTS')||'').trim(),declaredArtifactRequired=Boolean(artifactRequirements&&!['NONE','NOT APPLICABLE','N/A'].includes(upper(artifactRequirements))),productRequired=testRequiresCurrentProduct(test),artifactRequired=declaredArtifactRequired||productRequired,evidenceIds=safe(test.evidenceRefs).map(String),evidenceArtifactIds=evidenceIds.map(id=>evidenceById.get(id)).map(item=>canonicalReference(recordValue(item,'ATTACHMENT_ID')||item?.relationships?.ATTACHMENT_ID)).filter(Boolean),bindings=bindingArtifacts(test),futureProductBinding=productRequired&&activeStage<22&&!productResolution.productId,productArtifactIds=productRequired&&productResolution.productId?productResolution.artifactIds:[],artifactIds=[...new Set([...evidenceArtifactIds,...bindings.artifactIds,...productArtifactIds])],missingArtifactIds=artifactIds.filter(id=>!currentArtifactById.has(id)),unverifiedArtifactIds=artifactIds.filter(id=>{const artifact=currentArtifactById.get(id);return artifact&&upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED';}),productBindingIssues=productRequired&&activeStage>=22?productResolution.issues:[],bindingIssues=[...bindings.issues,...productBindingIssues],bindingReady=futureProductBinding||activeStage<22||(!bindingIssues.length&&(!productRequired||productArtifactIds.length>0)&&(!Object.keys(recordValue(test,'EXECUTABLE_INPUT_BINDINGS')||{}).length||bindings.artifactIds.length>0)&&!missingArtifactIds.length&&!unverifiedArtifactIds.length),artifactReady=(futureProductBinding||!artifactRequired||(artifactIds.length>0&&!missingArtifactIds.length&&!unverifiedArtifactIds.length))&&bindingReady,applicationExecutorSupported=mode!=='APPLICATION_DETERMINISTIC'||applicationTestSupported(test),validMode=Object.prototype.hasOwnProperty.call(TEST_EXECUTION_ACTIONS,mode),capabilityReady=capabilityAffirmativelyAvailable(project,requiredCapability,mode,test),executableNow=validMode&&capabilityReady&&artifactReady,blockingReason=!validMode?'Execution mode is not validly classified.':!requiredCapability?'Required capability is missing.':mode==='UNAVAILABLE'?'Required capability is explicitly unavailable.':mode==='APPLICATION_DETERMINISTIC'&&!applicationExecutorSupported?'No application-native executor is registered for '+requiredCapability+'.':!capabilityReady?'Required capability '+requiredCapability+' is not affirmatively available in current canonical state.':!bindingReady?'Executable input binding is unresolved or ambiguous: '+bindingIssues.join('; ')+'.':!artifactReady?'Required exact artifact bytes are missing or no longer verified.':null,requiredArtifactNames=artifactIds.map(id=>String(recordValue(currentArtifactById.get(id),'FILENAME')||id)),operatorAction=executableNow?actionMap[mode]||'BLOCKED':'BLOCKED',requiredEvidenceDescription=String(recordValue(test,'EVIDENCE_TO_PRESERVE')||'Preserve evidence capable of establishing the tested proposition.').trim(),native=mode==='APPLICATION_DETERMINISTIC',returnRequirements={structuredResponse:!native,requiredAttachmentKinds:native?[]:(artifactRequired?['CONTRACTED_VERIFICATION_ARTIFACT_OR_REPORT']:[]),requiredEvidenceDescription:native?'Application creates deterministic execution evidence from the exact verified input bytes.':requiredEvidenceDescription},send=native?[]:artifactIds.map(id=>{const a=currentArtifactById.get(id);return {artifactId:id,filename:a?String(recordValue(a,'FILENAME')||id):id,byteSize:a?Number(recordValue(a,'BYTE_SIZE')):null,sha256:a?String(recordValue(a,'SHA256')||'UNKNOWN'):'UNKNOWN',role:a?String(recordValue(a,'ROLE')||artifactRequirements||'TEST_INPUT'):(artifactRequirements||'TEST_INPUT'),availability:a?String(recordValue(a,'AVAILABILITY')||'UNKNOWN'):'UNAVAILABLE'};}),withhold=mode==='INDEPENDENT_AGENT_REVIEW'?[{artifactIdOrCategory:'prior verifier conclusions',reason:'Preserve reviewer independence.'},{artifactIdOrCategory:'proposed corrections',reason:'Prevent expectation bias.'}]:[],expectBack=native?[]:[{kind:'STRUCTURED_RESPONSE',filenameOrPattern:'final strict JSON response',required:true},{kind:'EVIDENCE',filenameOrPattern:requiredEvidenceDescription,required:true}],executionRoute=native?'APPLICATION':mode==='INDEPENDENT_AGENT_REVIEW'?'INDEPENDENT_AI':mode==='HUMAN_INSPECTION'?'HUMAN':mode==='EXTERNAL_SYSTEM'?'EXTERNAL_SYSTEM':mode==='EXTERNAL_AGENT_TOOL'?'EXTERNAL_AGENT_TOOL':'BLOCKED',userAction=!executableNow?'BLOCKED':native?'RUN_IN_APP':mode==='INDEPENDENT_AGENT_REVIEW'?'SEND_VERIFICATION_PACKAGE':mode==='HUMAN_INSPECTION'?'COMPLETE_INSPECTION':mode==='EXTERNAL_SYSTEM'?'RUN_EXTERNAL_SYSTEM':'SEND_VERIFICATION_PACKAGE';const handoff={filesToSend:send,recordsToSend:[],filesToWithhold:withhold,instruction:native?'Run the selected native test in the application.':'Transfer exactly the listed available files to the declared executor. Browser-local custody is not external receipt.',expectedReturnFiles:expectBack,expectedStructuredResponse:!native,requiredEvidenceDescription,send,withhold,expectBack};return {testId:recordId(test,'tests'),requirementId:testRequirementId(test),testType:upper(recordValue(test,'TEST_TYPE'))||'UNKNOWN',executionMode:mode,requiredCapability,executorClass:executorMap[mode]||'UNAVAILABLE',executionRoute,userAction,executableNow,blockingReason,artifactRequirements,artifactRequired,productRequired,productId:productResolution.productId,productArtifactIdentitySha256:productRequired?productResolution.identitySha256:null,futureProductBinding,evidenceIds,artifactIds,requiredArtifactIds:artifactIds,requiredArtifactNames,missingArtifactIds,unverifiedArtifactIds,artifactReady,bindingReady,bindingIssues,applicationExecutorSupported,capabilityReady,operatorAction,operatorActionText:executableNow?(TEST_EXECUTION_ACTIONS[mode]||''):(blockingReason||'Blocked.'),returnRequirements,handoff};});
  const counts=Object.fromEntries(Object.keys(TEST_EXECUTION_ACTIONS).map(mode=>[mode,items.filter(item=>item.executionMode===mode).length])),incompleteTestIds=items.filter(item=>!TEST_EXECUTION_ACTIONS[item.executionMode]||!item.requiredCapability||!item.artifactRequirements).map(item=>item.testId),unavailableTestIds=items.filter(item=>item.executionMode==='UNAVAILABLE').map(item=>item.testId),unsupportedApplicationTestIds=items.filter(item=>item.executionMode==='APPLICATION_DETERMINISTIC'&&!item.applicationExecutorSupported).map(item=>item.testId),missingArtifactTestIds=items.filter(item=>!item.artifactReady).map(item=>item.testId);return {total:items.length,counts,incompleteTestIds,unavailableTestIds,unsupportedApplicationTestIds,missingArtifactTestIds,items};
}
function resolveExecutableArtifactBindings(project,test){const bindings=recordValue(test,'EXECUTABLE_INPUT_BINDINGS')||{},artifacts=recordsForCurrentScope(project,'artifacts'),resolved=[],issues=[];for(const [bindingName,binding] of Object.entries(bindings)){if(binding&&typeof binding==='object'&&upper(binding.kind)==='CANONICAL_VALUE')continue;let matches=[];if(typeof binding==='string')matches=artifacts.filter(item=>recordId(item,'artifacts')===binding);else if(binding&&typeof binding==='object')matches=artifacts.filter(item=>{if(binding.artifactId&&recordId(item,'artifacts')!==String(binding.artifactId))return false;if(binding.filename&&String(recordValue(item,'FILENAME')||'')!==String(binding.filename))return false;if(binding.artifactRole&&String(recordValue(item,'ROLE')||'')!==String(binding.artifactRole))return false;if(binding.source==='CURRENT_PRODUCT'&&String(item.scope?.productId||'')!==String(project.job?.CURRENT_PRODUCT_ID||''))return false;return true;});if(matches.length!==1){issues.push(bindingName+' resolves to '+matches.length+' current canonical artifacts.');continue;}resolved.push({bindingName,artifact:matches[0]});}return {resolved,issues};}
function recordApplicationDeterministicResult(project,{testId,productId,runtimeResult,inputArtifacts=[]}={}){
  ensureShape(project);const tid=String(testId||''),pid=String(productId||''),test=recordsForCurrentScope(project,'tests').find(item=>recordId(item,'tests')===tid),product=recordsForCurrentScope(project,'products').find(item=>recordId(item,'products')===pid);if(!test||!applicationTestSupported(test))throw new Error(`Test ${tid||'UNKNOWN'} is not a valid current application-native Test IR test.`);if(!product)throw new Error(`Product ${pid||'UNKNOWN'} is not the current product.`);
  const determination=upper(runtimeResult?.determination||'UNDETERMINED'),runtime=deterministicRuntime();if(!['SATISFIED','VIOLATED','UNDETERMINED'].includes(determination))throw new Error('Native deterministic runtime returned an invalid determination.');if(runtimeResult?.status!=='COMPLETE')throw new Error('Only a complete native runtime execution may enter canonical deterministic results.');const runtimeBuildIdentity=String(runtimeResult?.runtimeBuildIdentity||''),workerSha256=String(runtimeResult?.testWorkerSha256||''),workerProtocolVersion=String(runtimeResult?.workerProtocolVersion||'');if(!runtimeBuildIdentity||runtime?.RUNTIME_BUILD_ID&&runtimeBuildIdentity!==String(runtime.RUNTIME_BUILD_ID))throw new Error('Native result runtime build identity is missing or does not match the current runtime.');if(!/^[a-f0-9]{64}$/i.test(workerSha256)||runtime?.TEST_WORKER_SHA256&&workerSha256.toLowerCase()!==String(runtime.TEST_WORKER_SHA256).toLowerCase())throw new Error('Native result worker-byte identity is missing or does not match the current worker.');if(!workerProtocolVersion||runtime?.WORKER_PROTOCOL_VERSION&&workerProtocolVersion!==String(runtime.WORKER_PROTOCOL_VERSION))throw new Error('Native result worker protocol does not match the current runtime.');
  const spec=recordValue(test,'EXECUTABLE_SPEC'),specSha256=String(recordValue(test,'EXECUTABLE_SPEC_SHA256')||recordValue(test,'CANONICAL_TEST_IR_SHA256')||hash.sha256Value(spec)),runtimeSpecSha256=String(runtimeResult?.testSpecSha256||'');if(!runtimeSpecSha256||runtimeSpecSha256!==specSha256)throw new Error('Native result does not match the exact current Test IR specification hash.');const bindingResolution=resolveExecutableArtifactBindings(project,test);if(bindingResolution.issues.length)throw new Error('Native Test IR bindings are unresolved: '+bindingResolution.issues.join(' '));const artifactIdentities=safe(inputArtifacts).map(item=>({artifactId:String(item.artifactId||''),filename:String(item.filename||''),byteSize:Number(item.byteSize),sha256:String(item.sha256||'').toLowerCase()})),expectedIds=bindingResolution.resolved.map(item=>recordId(item.artifact,'artifacts')).sort(),submittedIds=artifactIdentities.map(item=>item.artifactId).sort();if(hash.sha256Value(expectedIds)!==hash.sha256Value(submittedIds))throw new Error('Native deterministic input artifacts do not exactly match the accepted Test IR bindings.');for(const identity of artifactIdentities){const canonical=bindingResolution.resolved.map(item=>item.artifact).find(item=>recordId(item,'artifacts')===identity.artifactId);if(!canonical||upper(recordValue(canonical,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||String(recordValue(canonical,'SHA256')||'').toLowerCase()!==identity.sha256||Number(recordValue(canonical,'BYTE_SIZE'))!==identity.byteSize||String(recordValue(canonical,'FILENAME')||'')!==identity.filename)throw new Error(`Native deterministic input ${identity.artifactId||'UNKNOWN'} is not the exact application-verified bound bytes.`);}
  const returnedIds=safe(runtimeResult?.inputArtifactIds).map(String).sort(),returnedHashes=safe(runtimeResult?.inputArtifactSha256Values).map(value=>String(value).toLowerCase()).sort(),expectedHashes=artifactIdentities.map(item=>item.sha256).sort();if(hash.sha256Value(returnedIds)!==hash.sha256Value(expectedIds)||hash.sha256Value(returnedHashes)!==hash.sha256Value(expectedHashes))throw new Error('Native runtime result input identities or hashes do not match the accepted bound artifacts.');const evidencePayload={testId:tid,productId:pid,determination,runtimeVersion:String(runtimeResult?.runtimeVersion||runtimeResult?.executorVersion||''),runtimeBuildIdentity,testWorkerSha256:workerSha256,workerProtocolVersion,specVersion:String(runtimeResult?.testSpecVersion||schema.TEST_IR.version),testSpecSha256:specSha256,inputArtifacts:artifactIdentities,observations:safe(runtimeResult?.observations),expected:runtimeResult?.expected??null,actual:runtimeResult?.actual??null,parserIdentities:safe(runtimeResult?.parserIdentities),startedAtDeviceTime:runtimeResult?.startedAtDeviceTime||null,endedAtDeviceTime:runtimeResult?.endedAtDeviceTime||null};const identityHash=hash.sha256Value(evidencePayload),existing=recordsForCurrentScope(project,'deterministicResults').find(item=>item.source==='APPLICATION_TEST_RUNTIME'&&String(recordValue(item,'TEST_ID')||item.relationships?.TEST_ID||'')===tid&&String(recordValue(item,'PRODUCT_ID')||item.relationships?.PRODUCT_ID||'')===pid&&item.nativeExecutionIdentitySha256===identityHash);if(existing)return existing;if(recordsForCurrentScope(project,'deterministicResults').some(item=>String(recordValue(item,'TEST_ID')||item.relationships?.TEST_ID||'')===tid))throw new Error(`A current deterministic result already exists for ${tid}; invalidate/correct it before another result is committed.`);
  const evidence=commandRecord(project,'evidenceRecords',{APPLICATION_EVIDENCE_KIND:'APPLICATION_DETERMINISTIC_EXECUTION',APPLICATION_EVIDENCE_DESCRIPTION:`Application-native deterministic execution evidence for ${tid}.`,APPLICATION_EVIDENCE_CONTENT:JSON.stringify(evidencePayload),SHA256:identityHash,STATUS:'CURRENT'},{stage:22,source:'APPLICATION_TEST_RUNTIME'}),result=commandRecord(project,'deterministicResults',{PRODUCT_ID:pid,PRODUCT_SHA256:hash.sha256Value(artifactIdentities),TEST_ID:tid,APPLICATION_DETERMINATION:determination,RUNTIME_VERSION:evidencePayload.runtimeVersion,RUNTIME_BUILD_IDENTITY:runtimeBuildIdentity,TEST_WORKER_SHA256:workerSha256,WORKER_PROTOCOL_VERSION:workerProtocolVersion,TEST_SPEC_SHA256:specSha256,INPUT_ARTIFACT_IDENTITIES:JSON.stringify(artifactIdentities),RUNTIME_OBSERVATIONS:JSON.stringify(safe(runtimeResult?.observations))},{stage:22,source:'APPLICATION_TEST_RUNTIME'});result.relationships={...(result.relationships||{}),PRODUCT_ID:pid,TEST_ID:tid};result.evidenceRefs=[recordId(evidence,'evidenceRecords')];result.nativeExecutionIdentitySha256=identityHash;refreshRecordHashes(result,'deterministicResults');
  let observation=null;if(collectionAvailable('observationRecords')){const targetPropositions=propositionIdsFor(test),deferred=deferredFailureTestForCurrentTest(project,tid,22),deferredMutationId=deferred?.contract?.mutationId||null,observationFields={ORIGIN_CLASS:'NATIVE_APPLICATION_OBSERVATION',SUBMITTING_ACTOR:runtimeBuildIdentity,SUBJECT_DESCRIPTION:'Application-native Test IR assertion for '+tid,ARTIFACT_ID:artifactIdentities.length===1?artifactIdentities[0].artifactId:null,PRODUCT_ID:pid,RESULT_ID:recordId(result,'deterministicResults'),TEST_ID:tid,MUTATION_ID:deferredMutationId,NATIVE_OBSERVED_VALUE:{determination,expected:runtimeResult?.expected??null,actual:runtimeResult?.actual??null,observations:safe(runtimeResult?.observations)},NATIVE_OBSERVED_LOCATION:'TEST_IR_FINAL_ASSERTION',NATIVE_METHOD_OR_TOOL:evidencePayload.runtimeVersion,INPUT_IDENTITIES_AND_HASHES:artifactIdentities,OUTPUT_IDENTITIES_AND_HASHES:[{testSpecSha256:specSha256,nativeExecutionIdentitySha256:identityHash,testWorkerSha256:workerSha256}],EPISTEMIC_BASIS:'APPLICATION_OBSERVED',FRESHNESS_STATUS:'CURRENT',SOURCE_EVIDENCE_IDS:[recordId(evidence,'evidenceRecords')],CURRENT_SCOPE:currentScope(project),RAW_OR_EXECUTION_PROVENANCE:{nativeExecutionIdentitySha256:identityHash,runtimeBuildIdentity,testWorkerSha256:workerSha256,workerProtocolVersion,startedAtDeviceTime:runtimeResult?.startedAtDeviceTime||null,endedAtDeviceTime:runtimeResult?.endedAtDeviceTime||null},STATUS:'CURRENT'},observationRecord=commandRecord(project,'observationRecords',observationFields,{stage:22,source:'APPLICATION_TEST_RUNTIME'});observationRecord.relationships={...(observationRecord.relationships||{}),RESULT_ID:recordId(result,'deterministicResults'),TEST_ID:tid,PRODUCT_ID:pid,...(deferredMutationId?{MUTATION_ID:deferredMutationId}:{}),SOURCE_EVIDENCE_IDS:[recordId(evidence,'evidenceRecords')],ARTIFACT_ID:artifactIdentities.map(item=>item.artifactId),...(targetPropositions.length===1?{PROPOSITION_ID:targetPropositions[0]}:{PROPOSITION_ID:targetPropositions})};observationRecord.evidenceRefs=[recordId(evidence,'evidenceRecords')];refreshRecordHashes(observationRecord,'observationRecords');observation=observationRecord;result.relationships.OBSERVATION_ID=recordId(observationRecord,'observationRecords');result.fields.OBSERVATION_ID=recordId(observationRecord,'observationRecords');result.OBSERVATION_ID=recordId(observationRecord,'observationRecords');result.fields.OBSERVATION_ORIGIN='NATIVE_APPLICATION_OBSERVATION';result.OBSERVATION_ORIGIN='NATIVE_APPLICATION_OBSERVATION';result.fields.EPISTEMIC_BASIS='APPLICATION_OBSERVED';result.EPISTEMIC_BASIS='APPLICATION_OBSERVED';result.fields.FRESHNESS_STATUS='CURRENT';result.FRESHNESS_STATUS='CURRENT';if(targetPropositions.length===1){result.fields.PROPOSITION_ID=targetPropositions[0];result.PROPOSITION_ID=targetPropositions[0];result.relationships.PROPOSITION_ID=targetPropositions[0];}if(collectionAvailable('entailmentReviews'))for(const propositionId of targetPropositions){const relation=semanticCoverage(test)==='EQUIVALENT'?(determination==='SATISFIED'?'ESTABLISHES':determination==='VIOLATED'?'REFUTES':'UNKNOWN'):'SUPPORTS_ONLY',obligation=recordsForCurrentScope(project,'proofObligations').find(item=>relationshipIds(item,'PROPOSITION_ID').includes(propositionId)),entailment=commandRecord(project,'entailmentReviews',{OBSERVATION_ID:recordId(observationRecord,'observationRecords'),PROPOSITION_ID:propositionId,PROOF_OBLIGATION_ID:recordId(obligation,'proofObligations')||null,PROOF_LEAF_KEY:tid,ENTAILMENT_FINDING:relation,REASONING:'Application-normalized Test IR assertion under the accepted current test semantic-coverage disposition.',REVIEWER_ID:'APPLICATION_TEST_RUNTIME_NORMALIZATION',EVIDENCE_IDS:[recordId(evidence,'evidenceRecords')],ACCEPTED_RELATION:relation,CURRENT_SCOPE:currentScope(project),STATUS:'CURRENT'},{stage:22,source:'APPLICATION_TEST_RUNTIME_NORMALIZATION'});entailment.relationships={...(entailment.relationships||{}),OBSERVATION_ID:recordId(observationRecord,'observationRecords'),PROPOSITION_ID:propositionId,PROOF_OBLIGATION_ID:recordId(obligation,'proofObligations')||null,EVIDENCE_IDS:[recordId(evidence,'evidenceRecords')],TEST_ID:tid};refreshRecordHashes(entailment,'entailmentReviews');result.relationships.ENTAILMENT_ID=recordId(entailment,'entailmentReviews');result.fields.ENTAILMENT_ID=recordId(entailment,'entailmentReviews');result.ENTAILMENT_ID=recordId(entailment,'entailmentReviews');if(obligation){result.relationships.PROOF_OBLIGATION_ID=recordId(obligation,'proofObligations');result.fields.PROOF_OBLIGATION_ID=recordId(obligation,'proofObligations');result.PROOF_OBLIGATION_ID=recordId(obligation,'proofObligations');}}}
  refreshRecordHashes(result,'deterministicResults');const nativeEvent={nativeExecutionEventId:allocateInfrastructureId(project,'NATIVE-EXECUTION','nativeExecutionEvents'),stage:22,testId:tid,productId:pid,resultId:recordId(result,'deterministicResults'),evidenceId:recordId(evidence,'evidenceRecords'),observationId:observation?recordId(observation,'observationRecords'):null,testSpecSha256:specSha256,runtimeVersion:evidencePayload.runtimeVersion,runtimeBuildIdentity,testWorkerSha256:workerSha256,workerProtocolVersion,inputArtifactIds:artifactIdentities.map(item=>item.artifactId),inputArtifactSha256Values:artifactIdentities.map(item=>item.sha256),normalizedDetermination:determination,projectRevision:projectRevision(project),deviceTimestamp:now(),nativeExecutionIdentitySha256:identityHash};project.projectData.nativeExecutionEvents.push(nativeEvent);addHistory(project,'APPLICATION_TEST_EXECUTED',{stage:22,recordId:recordId(result,'deterministicResults'),testId:tid,productId:pid,determination,evidenceId:recordId(evidence,'evidenceRecords'),observationId:observation?recordId(observation,'observationRecords'):null,nativeExecutionEventId:nativeEvent.nativeExecutionEventId,testSpecSha256:specSha256,runtimeBuildIdentity,testWorkerSha256:workerSha256,inputArtifactIds:artifactIdentities.map(item=>item.artifactId)});recalculate(project);return result;
}
function evaluateRuleOfThreeApplicability(project,{failureCount=null}={}){
  ensureShape(project);const reasons=[],contracts=safe(project.projectData.metricApplicabilityContracts),contract=contracts.filter(item=>isActiveRecord(item)&&upper(item.metric||item.METRIC)==='RULE_OF_THREE').at(-1)||project.job?.RULE_OF_THREE_CONTRACT||project.stages?.[11]?.humanData?.RULE_OF_THREE_CONTRACT||null;
  if(!contract||typeof contract!=='object')reasons.push('No current predeclared rule-of-three applicability contract exists.');
  const get=key=>contract?.[key]??contract?.[key.toUpperCase()],event=String(get('bernoulliFailureEvent')||get('failureEvent')||'').trim(),predeclared=truth(get('predeclared')),representative=truth(get('representativeExposure')),comparable=truth(get('materiallyComparableExposure')),adaptive=truth(get('adaptiveStopping')),excluded=Number(get('excludedFailureCount')??0);
  if(!event)reasons.push('A single Bernoulli failure event was not declared.');if(!predeclared)reasons.push('The failure event was not affirmatively predeclared.');if(!representative)reasons.push('Representative exposure is not affirmatively established.');if(!comparable)reasons.push('Material comparability of exposure is not affirmatively established.');if(adaptive)reasons.push('Adaptive stopping is present.');if(!Number.isSafeInteger(excluded)||excluded!==0)reasons.push('Excluded failures are present or unknown.');
  const activeRuns=records(project,'runs').filter(run=>['COMPLETED','SATISFIED','SUCCESS','PASSED'].includes(upper(recordValue(run,'EXECUTION_STATUS')||run.completionState||run.status||''))),byIteration=new Map();for(const run of activeRuns){const iterationId=String(recordValue(run,'ITERATION_ID')||run.scope?.iterationId||'');if(!byIteration.has(iterationId))byIteration.set(iterationId,[]);byIteration.get(iterationId).push(run);}
  const eligibleRuns=[];for(const [iterationId,runs] of byIteration){const independence=evaluateContextIndependence(project,{role:'RUN_BATCH',iterationId,requiredDimensions:['APPLICATION_SESSION_DISTINCTNESS','APPLICATION_INPUT_ISOLATION','EXTERNAL_CONTEXT_IDENTIFIER_DISTINCTNESS','PROVIDER_CONTEXT_INDEPENDENCE']}),provider=independence.dimensions.PROVIDER_CONTEXT_INDEPENDENCE;if(provider?.truthValue==='TRUE'&&['VERIFIED_EXTERNAL','EXTERNALLY_SUPPORTED'].includes(provider.epistemicBasis)&&!independence.requiredDimensions.some(name=>independence.dimensions[name]?.truthValue!=='TRUE'))eligibleRuns.push(...runs);else reasons.push('Eligible trials lack sufficiently supported provider-context independence.');}
  const observedFailures=failureCount===null?confirmedDefects(project,{allScopes:true}).length:Number(failureCount);if(!Number.isSafeInteger(observedFailures)||observedFailures<0)reasons.push('Observed failure count is invalid.');else if(observedFailures!==0)reasons.push('The rule-of-three display requires zero observed failures.');if(!eligibleRuns.length)reasons.push('No eligible trials meet the declared independence and exposure contract.');
  const applicable=reasons.length===0,N=eligibleRuns.length;return {applicable,reasons:[...new Set(reasons)],eligibleTrialCount:N,observedFailures:Number.isSafeInteger(observedFailures)?observedFailures:null,approximate95PercentUpperBound:applicable?3/N:null,assumptions:{bernoulliFailureEvent:event||null,predeclared,representativeExposure:representative,materiallyComparableExposure:comparable,adaptiveStopping:adaptive,excludedFailureCount:Number.isSafeInteger(excluded)?excluded:null}};
}
function operationalMetrics(project){
  ensureShape(project);const history=safe(project.projectData.history),validations=safe(project.projectData.responseValidations),proposals=safe(project.projectData.responseProposals),releases=safe(project.projectData.releaseRecords),accepted=safe(project.projectData.acceptedChanges).length,rejected=safe(project.projectData.rejectedResponses).length,stale=proposals.filter(x=>x.status==='STALE'||x.invalidatedBy).length,defects=confirmedDefects(project,{allScopes:true}),baselineEvent=history.find(x=>x.type==='BASELINE_FROZEN')?.eventSequence||Infinity,identityMismatches=records(project,'artifactIdentities',{active:false}).filter(r=>!truth(recordValue(r,'EXACT_HASH_MATCH'))||!truth(recordValue(r,'EXACT_SIZE_MATCH'))).length,regressionRecurrences=records(project,'regressionExecutions',{active:false}).filter(r=>upper(recordValue(r,'PHASE'))!=='PRE_CORRECTION'&&['VIOLATED','FAILED','FAIL'].includes(upper(recordValue(r,'RESULT')))).length;
  const activeRuns=records(project,'runs'),iterationIds=[...new Set(activeRuns.map(r=>String(recordValue(r,'ITERATION_ID')||r.scope?.iterationId||'')).filter(Boolean))],establishedIterations=new Set(iterationIds.filter(iterationId=>evaluateContextIndependence(project,{role:'RUN_BATCH',iterationId,requiredDimensions:['APPLICATION_SESSION_DISTINCTNESS','APPLICATION_INPUT_ISOLATION','EXTERNAL_CONTEXT_IDENTIFIER_DISTINCTNESS','PROVIDER_CONTEXT_INDEPENDENCE']}).determination==='APPLICATION_ESTABLISHED')),completedRuns=activeRuns.filter(r=>establishedIterations.has(String(recordValue(r,'ITERATION_ID')||r.scope?.iterationId||''))&&['COMPLETED','SATISFIED','SUCCESS','PASSED'].includes(upper(recordValue(r,'EXECUTION_STATUS')||r.completionState||r.status||''))).length,independentVerifications=records(project,'verification').filter(r=>releaseVerificationTrust(project,r).determination==='APPLICATION_ESTABLISHED').length,independentOps=completedRuns+independentVerifications,silentFailures=defects.filter(d=>Number(d.eventSequence||Infinity)>baselineEvent).length,ruleOfThree=evaluateRuleOfThreeApplicability(project,{failureCount:silentFailures}),humanCorrections=history.filter(x=>['ACCEPTED_RESPONSE_INVALIDATED','STAGE_AUTHORITY_CHANGED'].includes(x.type)).length;
  return {rawResponses:safe(project.projectData.rawResponses).length,validationFailures:validations.filter(x=>x.valid===false).length,staleResponses:stale,rejectedProposals:rejected,acceptedDataChanges:accepted,clarificationCycles:safe(project.projectData.humanInputAnswers).length,controlledCorrections:history.filter(x=>['ACCEPTED_RESPONSE_INVALIDATED','STAGE_AUTHORITY_CHANGED','DOWNSTREAM_INVALIDATED'].includes(x.type)).length,storageFailures:history.filter(x=>String(x.type||'').includes('STORAGE')&&String(x.type||'').includes('FAIL')).length,gateRegressions:history.filter(x=>x.type==='DOWNSTREAM_INVALIDATED').length,releaseRejections:releases.filter(x=>upper(recordValue(x,'DETERMINATION'))==='REJECTED').length,releaseBlocks:releases.filter(x=>upper(recordValue(x,'DETERMINATION'))==='BLOCKED').length,artifactIdentityMismatches:identityMismatches,regressionRecurrences,humanCorrectionFrequency:accepted?humanCorrections/accepted:0,materiallyIndependentAcceptedOperations:independentOps,observedSilentFailures:silentFailures,ruleOfThreeApplicability:ruleOfThree,zeroFailure95PercentUpperBound:ruleOfThree.approximate95PercentUpperBound};
}

globalThis.closedLoopWorkflowEngine=Object.freeze({recordMigratedAcceptedChange,
  version:'closed-loop-workflow-engine/1',__controllingCompletionAmendmentVersion:'closed-loop-controlling-completion/53-70/1',STAGE_STATES,FORMAL_STATES,ALL_COLLECTIONS,ACTION_TYPES,TRUTH_VALUES,EPISTEMIC_BASES,FRESHNESS_STATES,APPLICABILITY_STATES,ENTAILMENT_RELATIONS,PROOF_EXPRESSION_OPERATORS,INDEPENDENCE_DIMENSIONS,DELIVERY_STATES,RESPONSIBLE_STAGE_BY_COLLECTION,
  clone,now,safe,upper,truth,falsey,numeric,exactRatio,ratioNumber,recordFields,recordValue,recordId,isActiveRecord,records,refreshRecordHashes,registerGeneratedPrompt,createHumanBlocker,reconcileArtifactCustodyVerification,resolveHumanBlocker,registerFreshContext,recordHumanDecision,recordHumanInspectionEvidence,invalidateAcceptedResponse,invalidateStageForAuthorityChange,reserveRunBatch,registerArtifactBytes,freezeCandidate,beginUnchangedConfirmationIteration,freezeBaseline,reserveProductExecution,createNewJobReset,recordApplicationDeterministicResult,
  ensureShape,addHistory,allocateId,allocateInfrastructureId,nextVersion,registerStageVersion,
  unresolvedHumanRequests,openBlockers,acceptedChanges,stageSuboperationClosure,hasStageActivity,mandatoryRequirements,confirmedDefects,unresolvedMaterialDefects,
  currentScope,recordsForScope,recordsForCurrentScope,scopeForIteration,recordsForIteration,requiredVerificationRelationSet,verificationMatrix,evaluateIteration,currentStageOperation,DERIVATIONS,coverageMetrics,convergenceMetrics,releaseMetrics,applicationTestCapabilities,capabilityAffirmativelyAvailable,canonicalTestBindingCatalog,testRequiresCurrentProduct,currentProductArtifactResolution,testExecutionPlan,executionHandoff,deferredFailureTestContract,deferredFailureTestExecutionState,deferredFailureTestsDue,evaluateContextIndependence,evaluateEvidenceSufficiency,evaluateEvidenceContract,releaseVerificationTrust,evaluateResultConsistency,effectiveDetermination,validateTraceIntegrity,detectEvidenceJustificationCycles,detectCurrentContradictions,executionStability,evidenceChainExplanation,stage16CorrectionPlan,operationalNextAction,operationalMetrics,evaluateRuleOfThreeApplicability,intakeCoverageManifest,parseCapturedInputSet,evaluateIntakeAccounting,obligationManifest,evaluateObligationAccounting,representationInspectionCoverage,gate,deriveStageData,recalculate,invalidateDownstream,responsibleStageForMutation,invalidateForMutation,applicationInitialFields,
  sourceSearchAssessment,normalizeSourceConsequences,sourceIdsForRequirement,evaluateSourceDependencies,backupArtifactMetadataManifest,backupArtifactManifest,backupCheckpointSubject,evaluateBackupDisclosure,recordBackupDisclosureDecision,validateBackupRestoreProof,validateBackupPackageBinding,recordPendingBackupExport,confirmBackupExternalCustody,recordBackupCheckpoint,backupCheckpointStatus,
  evaluateApplicability,evaluateObservationEntailment,evaluateProofExpression,deriveProofObligationRegistry,evaluatePropositionState,evaluateEnvironmentDependency,evaluateRegistryIntegrity,terminalDeliveryState,recordDelivery,normalizeAcceptedAgentConsequences,
  executeIdempotentCommand,reserveOperation,orphanOperationReservation,resumeOperationReservation,cancelOperationReservation,acceptOperationReservation,orphanActiveOperationReservations,
  recordHumanInputVersion,recordStageConfirmation,recordReleaseDetermination,acceptedControlEvents,constructEvidenceChains,verifyArtifactIdentity
});
})();
