(()=>{
'use strict';

const core=globalThis.closedLoopCore;
const schema=globalThis.closedLoopWorkflowSchema;
const hash=globalThis.closedLoopHash;
if(!core||!schema||!hash)throw new Error('workbook.js, hash.js, and workflow-schema.js, test-runtime.js must load before workflow-engine.js.');

const STAGE_STATES=Object.freeze(['NOT STARTED','IN PROGRESS','BLOCKED','READY','COMPLETE']);
const FORMAL_STATES=Object.freeze(['UNKNOWN','NONE','NOT APPLICABLE','TRUE','FALSE','SATISFIED','VIOLATED','UNDETERMINED','ACCEPTED','REJECTED','BLOCKED']);
const INFRA_COLLECTIONS=Object.freeze([
  'inputVersions','rawResponses','responseProposals','responseValidations','acceptedChanges','rejectedResponses','extractionManifests',
  'humanInputRequests','humanInputAnswers','stageConfirmations','artifactVersions','generatedPrompts','generatedOutputs','outputReceipts',
  'history','newJobResets','reviews','recoveredProjects','responseDispositions','executionFailures','intakeCoverageManifests',
  'obligationManifests','promptContextManifests','blindAliasMaps','nativeExecutionEvents','commandReceipts'
]);
const ALL_COLLECTIONS=Object.freeze([...new Set([...Object.keys(schema.RECORD_SCHEMAS),...INFRA_COLLECTIONS])]);

const clone=value=>value===undefined?undefined:JSON.parse(JSON.stringify(value));
const now=()=>new Date().toISOString();
const safe=value=>Array.isArray(value)?value:[];
const upper=value=>String(value??'').trim().toUpperCase();
const truth=value=>['TRUE','YES','SATISFIED','ACCEPTED','AUTHORIZED','CONFIRMED','CONVERGED','COMPLETE','SUCCESS','SUCCEEDED','EFFECTIVE'].includes(upper(value));
const falsey=value=>['FALSE','NO','NONE','0','NOT APPLICABLE'].includes(upper(value));
const numeric=value=>{const parsed=Number(String(value??'').replace(/[^0-9.-]/g,''));return Number.isFinite(parsed)?parsed:0;};
const recordFields=record=>record?.fields&&typeof record.fields==='object'?record.fields:record||{};
const recordValue=(record,key)=>recordFields(record)?.[key]??record?.[key];
const recordId=(record,collection)=>{
  const idField=schema.RECORD_SCHEMAS[collection]?.idField;
  return String(record?.id||record?.recordId||(idField?recordValue(record,idField):'')||'').trim();
};
const isActiveRecord=record=>record?.active!==false&&!record?.invalidatedBy&&!['RETIRED','INVALIDATED','SUPERSEDED'].includes(upper(recordValue(record,'STATUS')||record?.validity));
const records=(project,collection,{stage,active=true}={})=>safe(project?.projectData?.[collection]).filter(record=>(stage===undefined||Number(record?.stage)===Number(stage))&&(!active||isActiveRecord(record)));

function ensureShape(project){
  if(!project||typeof project!=='object')throw new TypeError('Project must be an object.');
  project.projectData=project.projectData&&typeof project.projectData==='object'?project.projectData:{};
  for(const collection of ALL_COLLECTIONS)project.projectData[collection]=safe(project.projectData[collection]);
  project.projectData.permanentRegistry=project.projectData.permanentRegistry&&typeof project.projectData.permanentRegistry==='object'?project.projectData.permanentRegistry:{};
  project.projectData.stageRecords=project.projectData.stageRecords&&typeof project.projectData.stageRecords==='object'?project.projectData.stageRecords:{};
  project.projectData.userEntered=project.projectData.userEntered&&typeof project.projectData.userEntered==='object'?project.projectData.userEntered:{};
  project.projectData.idCounters=project.projectData.idCounters&&typeof project.projectData.idCounters==='object'?project.projectData.idCounters:{};
  project.projectData.eventSequence=Number.isInteger(project.projectData.eventSequence)?project.projectData.eventSequence:0;
  project.stages=project.stages&&typeof project.stages==='object'?project.stages:{};
  for(let stage=1;stage<=30;stage++){
    const prior=project.stages[stage]||project.stages[String(stage)]||{};
    project.stages[stage]={
      number:stage,status:'NOT STARTED',decision:'',decisionEvidence:'',nextStage:'',decidedBy:'',dateTime:'',
      draftRecord:prior.draftRecord||core.stageTemplate(core.STAGES[stage-1]),responseDraft:'',authorizedFiles:[],humanChecks:{},gateChecks:{},evidenceChecks:{},revisions:[],
      agentData:{},humanData:{},derivedData:{},acceptedData:{},acceptedDataChangeIds:[],acceptedControlEventIds:[],acceptedResponseIds:[],currentPromptId:null,gate:{complete:false,blocked:false,reasons:[]},invalidatedBy:null,
      ...prior,
      number:stage,
      authorizedFiles:safe(prior.authorizedFiles),revisions:safe(prior.revisions),agentData:prior.agentData&&typeof prior.agentData==='object'?prior.agentData:(prior.acceptedData&&typeof prior.acceptedData==='object'?prior.acceptedData:{}),humanData:prior.humanData&&typeof prior.humanData==='object'?prior.humanData:{},derivedData:prior.derivedData&&typeof prior.derivedData==='object'?prior.derivedData:{},acceptedDataChangeIds:safe(prior.acceptedDataChangeIds),acceptedControlEventIds:safe(prior.acceptedControlEventIds),acceptedResponseIds:safe(prior.acceptedResponseIds)
    };
  }
  project.job=project.job&&typeof project.job==='object'?project.job:{};
  project.release={gateState:'',auditedDraft:[],releaseDraft:[],comparisons:[],authorization:'NOT AUTHORIZED',authorizedArtifactIds:[],...(project.release||{})};
  for(const name of ['auditedDraft','releaseDraft','comparisons','authorizedArtifactIds'])project.release[name]=safe(project.release[name]);
  return project;
}

const APPLICATION_INITIAL_FIELDS=Object.freeze({
  requirements:Object.freeze({STATUS:'ACTIVE'}),candidateRequirements:Object.freeze({STATUS:'ACTIVE'}),requirementResolutions:Object.freeze({STATUS:'RESOLVED'}),tests:Object.freeze({STATUS:'READY'}),instructions:Object.freeze({STATUS:'CURRENT'}),instructionTraces:Object.freeze({STATUS:'COMPLETE'}),preflightRecords:Object.freeze({STATUS:'COMPLETE'}),iterations:Object.freeze({STATUS:'FROZEN'}),candidateFreezes:Object.freeze({STATUS:'FROZEN'}),defects:Object.freeze({STATUS:'CONFIRMED'}),regressions:Object.freeze({ACTIVE_RETIRED_STATE:'ACTIVE'}),baselines:Object.freeze({STATUS:'FROZEN'}),products:Object.freeze({STATUS:'RESERVED'}),blockers:Object.freeze({STATUS:'OPEN'}),evidenceRecords:Object.freeze({STATUS:'PRESERVED'})
});
function applicationInitialFields(collection){return clone(APPLICATION_INITIAL_FIELDS[collection]||{});}

function addHistory(project,type,details={}){
  ensureShape(project);
  const eventSequence=++project.projectData.eventSequence;
  const deviceTimestamp=now();
  const event={eventId:`EVENT-${String(eventSequence).padStart(9,'0')}`,eventSequence,deviceTimestamp,createdAt:deviceTimestamp,type,...clone(details)};
  project.projectData.history.push(event);
  return event;
}

function existingIds(project,collection){return new Set(records(project,collection,{active:false}).map(record=>recordId(record,collection)).filter(Boolean));}
function allocateId(project,collection){
  ensureShape(project);
  const definition=schema.RECORD_SCHEMAS[collection];
  if(!definition)throw new Error(`Unknown canonical collection: ${collection}.`);
  const existing=existingIds(project,collection);
  let counter=Math.max(0,Number(project.projectData.idCounters[collection]||0));
  let candidate='';
  do{counter+=1;candidate=`${definition.prefix}-${String(counter).padStart(6,'0')}`;}while(existing.has(candidate));
  project.projectData.idCounters[collection]=counter;
  return candidate;
}
function allocateInfrastructureId(project,prefix,collection){
  ensureShape(project);
  const key=`infra:${collection}:${prefix}`;
  let counter=Math.max(0,Number(project.projectData.idCounters[key]||0));
  const existing=new Set(safe(project.projectData[collection]).map(record=>String(record?.id||record?.rawResponseId||record?.proposalId||record?.validationId||record?.changeId||record?.manifestId||record?.receiptId||record?.promptId||'')).filter(Boolean));
  let candidate='';
  do{counter+=1;candidate=`${prefix}-${String(counter).padStart(6,'0')}`;}while(existing.has(candidate));
  project.projectData.idCounters[key]=counter;
  return candidate;
}

function nextVersion(current,prefix){
  const match=String(current||'').match(/v(\d+)$/i);
  const number=match?Number(match[1])+1:1;
  return `${prefix}-v${String(number).padStart(3,'0')}`;
}
// CURRENT_INPUT_VERSION is versioned only by application-controlled human-input mutation paths.
// Accepting the Stage 01 agent interpretation must not create a new User Job Input version,
// otherwise the accepted intake manifest instantly becomes stale and the user is asked to repeat information.
const VERSION_BY_STAGE=Object.freeze({
  2:['CURRENT_SOURCE_SET_VERSION','SOURCE-SET'],3:['CURRENT_RESEARCH_VERSION','RESEARCH'],
  4:['CURRENT_REQUIREMENTS_VERSION','REQUIREMENTS'],6:['CURRENT_TEST_SUITE_VERSION','TEST-SUITE'],
  7:['CURRENT_MUTATION_SUITE_VERSION','MUTATION-SUITE'],8:['CURRENT_INSTRUCTION_VERSION','INSTRUCTION']
});
function versionCollections(stage){return schema.STAGE_CONTRACTS[stage]?.primaryCollections||[];}
const VERSION_SCOPE_KEY_BY_STAGE=Object.freeze({2:'sourceSetVersion',4:'requirementsVersion',5:'requirementsVersion',6:'testSuiteVersion',8:'instructionVersion'});
function acceptedChangeVersionTargets(project,stage,acceptedChangeId){
  const change=safe(project.projectData.acceptedChanges).find(record=>record.changeId===acceptedChangeId&&record.status==='COMMITTED'&&!record.invalidatedBy);
  if(!change)throw new Error(`Stage ${stage} version registration cannot resolve accepted change ${acceptedChangeId}.`);
  if(Number(change.stage)!==Number(stage))throw new Error(`Accepted change ${acceptedChangeId} belongs to Stage ${change.stage}, not Stage ${stage}.`);
  const proposal=safe(project.projectData.responseProposals).find(record=>record.proposalId===change.proposalId);
  if(!proposal)throw new Error(`Stage ${stage} version registration cannot resolve proposal ${change.proposalId||'UNKNOWN'}.`);
  const committedIds=new Set(safe(change.canonicalRecordIds).map(String).filter(Boolean)),references=[];
  for(const [collection,items]of Object.entries(proposal.canonicalRecords||{}))for(const item of safe(items)){const id=String(item?.id||'');if(id&&committedIds.has(id))references.push({collection,id});}
  for(const item of safe(proposal.evidence)){const id=String(item?.id||'');if(id&&committedIds.has(id))references.push({collection:'evidenceRecords',id});}
  const unique=new Map();
  for(const reference of references){const key=`${reference.collection}\u0000${reference.id}`;if(unique.has(key))throw new Error(`Stage ${stage} accepted change ${acceptedChangeId} contains duplicate canonical target ${reference.collection}/${reference.id}.`);unique.set(key,reference);}
  const resolvedIds=new Set([...unique.values()].map(reference=>reference.id)),unresolved=[...committedIds].filter(id=>!resolvedIds.has(id));
  if(unresolved.length)throw new Error(`Stage ${stage} accepted change ${acceptedChangeId} has committed records absent from its accepted proposal: ${unresolved.join(', ')}.`);
  return [...unique.values()].sort((left,right)=>hash.compareUnicodeScalarSequence(left.collection,right.collection)||hash.compareUnicodeScalarSequence(left.id,right.id)).map(reference=>{
    const matches=safe(project.projectData[reference.collection]).filter(record=>isActiveRecord(record)&&recordId(record,reference.collection)===reference.id);
    if(matches.length!==1)throw new Error(`Stage ${stage} accepted change ${acceptedChangeId} resolves ${matches.length} active ${reference.collection} records for ${reference.id}.`);
    return {...reference,record:matches[0]};
  });
}
function acceptedVersionRecordProjection(target,scopeKey,version){
  const exact=clone(target.record);
  if(scopeKey&&version)exact.scope={...(exact.scope||{}),[scopeKey]:version};
  refreshRecordHashes(exact,target.collection);
  return {collection:target.collection,id:target.id,stage:Number(exact.stage||schema.RECORD_SCHEMAS[target.collection]?.stage||0),scope:clone(exact.scope||{}),fields:clone(recordFields(exact)),relationships:clone(exact.relationships||{}),evidenceRefs:clone(exact.evidenceRefs||[]),rawResponseId:String(exact.rawResponseId||''),sourceProposalId:String(exact.sourceProposalId||''),contentSha256:String(exact.contentSha256||''),recordSha256:String(exact.recordSha256||'')};
}
function stampAcceptedChangeVersionMembership(targets,scopeKey,version){for(const target of targets){if(scopeKey&&version)target.record.scope={...(target.record.scope||{}),[scopeKey]:version};refreshRecordHashes(target.record,target.collection);}}
function registerStageVersion(project,stage,acceptedChangeId){
  ensureShape(project);
  const config=VERSION_BY_STAGE[stage];
  if(!config)return null;
  if(Number(stage)===2&&upper(project.stages[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE'){project.job.CURRENT_SOURCE_SET_VERSION='NOT APPLICABLE';return null;}
  const [jobField,prefix]=config;
  const targets=acceptedChangeVersionTargets(project,stage,acceptedChangeId),existing=safe(project.projectData.artifactVersions).find(item=>item.stage===stage&&item.kind===prefix&&item.acceptedChangeId===acceptedChangeId);
  if(existing){const expected=targets.map(target=>`${target.collection}\u0000${target.id}`).sort(),bound=safe(existing.payload?.records).map(record=>`${record.collection}\u0000${record.id}`).sort();if(hash.sha256Value(bound)!==hash.sha256Value(expected)||existing.sha256!==hash.sha256Value(existing.payload))throw new Error(`Stage ${stage} version ${existing.version} no longer binds the exact canonical records of accepted change ${acceptedChangeId}.`);project.job[jobField]=existing.version;stampAcceptedChangeVersionMembership(targets,VERSION_SCOPE_KEY_BY_STAGE[stage],existing.version);return existing;}
  const version=nextVersion(project.job[jobField],prefix);
  const payload={stage,kind:prefix,acceptedChangeId,records:targets.map(target=>acceptedVersionRecordProjection(target,VERSION_SCOPE_KEY_BY_STAGE[stage],version)),acceptedData:clone(project.stages[stage].acceptedData||{})};
  const sha256=hash.sha256Value(payload);
  const record={versionId:allocateInfrastructureId(project,'VERSION','artifactVersions'),stage,kind:prefix,version,sha256,createdAt:now(),acceptedChangeId,payload};
  project.projectData.artifactVersions.push(record);
  project.job[jobField]=version;
  stampAcceptedChangeVersionMembership(targets,VERSION_SCOPE_KEY_BY_STAGE[stage],version);
  return record;
}

function unresolvedHumanRequests(project,stage){return safe(project?.projectData?.humanInputRequests).filter(request=>Number(request.stage)===Number(stage)&&upper(request.status||'OPEN')==='OPEN'&&request.blocking!==false);}
function openBlockers(project,stage){
  return records(project,'blockers').filter(blocker=>{
    const status=upper(recordValue(blocker,'STATUS')||blocker.status||'OPEN');
    if(!['OPEN','BLOCKED','IN PROGRESS','UNKNOWN'].includes(status))return false;
    if(stage===undefined)return true;
    const blockerStage=Number(blocker.stage||recordValue(blocker,'STAGE_DISCOVERED')||0);
    if(blockerStage===Number(stage))return true;
    const affected=String(recordValue(blocker,'AFFECTED_ARTIFACTS')||recordValue(blocker,'DOWNSTREAM_WORK_STOPPED')||'');
    return new RegExp(`(?:STAGE\\s*0?${stage}\\b|\\b${stage}\\b)`,`i`).test(affected);
  });
}
function acceptedChanges(project,stage){return safe(project?.projectData?.acceptedChanges).filter(change=>Number(change.stage)===Number(stage)&&change.status==='COMMITTED'&&change.responseType==='DATA_PROPOSAL'&&!change.invalidatedBy);}
function acceptedControlEvents(project,stage){return safe(project?.projectData?.responseDispositions).filter(item=>Number(item.stage)===Number(stage)&&['ACCEPTED_HUMAN_QUESTION_SET','ACCEPTED_BLOCKER_EVENT','ACCEPTED_EXECUTION_FAILURE'].includes(item.type)&&!item.invalidatedBy);}

function hasStageActivity(project,stage){
  if(acceptedChanges(project,stage).length)return true;
  if(safe(project?.projectData?.rawResponses).some(item=>Number(item.stage)===Number(stage)))return true;
  if(unresolvedHumanRequests(project,stage).length)return true;
  if((schema.STAGE_CONTRACTS[stage]?.allowedCollections||[]).some(collection=>records(project,collection,{stage}).length))return true;
  return Boolean(Object.keys(project?.stages?.[stage]?.acceptedData||{}).length);
}
function latestIteration(project,stages=[10,17,19]){
  const candidates=records(project,'iterations').filter(record=>stages.includes(Number(record.stage)));
  return candidates.at(-1)||null;
}
function mandatoryRequirements(project,scopeRule=null){
  const source=scopeRule?recordsForScope(project,'requirements',scopeRule):recordsForCurrentScope(project,'requirements');
  return source.filter(record=>{
    const value=upper(recordValue(record,'MANDATORY_OPTIONAL_STATUS')||recordValue(record,'MANDATORY')||'MANDATORY');
    return value!=='OPTIONAL'&&value!=='FALSE'&&value!=='NO';
  });
}
function confirmedDefects(project,{allScopes=false}={}){
  const source=allScopes?records(project,'defects'):records(project,'defects').filter(record=>!record?.scope||Object.keys(record.scope).length===0||scopeMatches(record,currentScope(project)));
  return source.filter(record=>{
    const status=upper(recordValue(record,'STATUS')||'CONFIRMED');
    return !['REJECTED','DUPLICATE','NOT A DEFECT','RETIRED'].includes(status);
  });
}
function defectResolvedByRegression(project,defect){const defectId=recordId(defect,'defects'),linked=records(project,'regressions').filter(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===defectId&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED');if(!linked.length)return false;const iterationId=recordId(latestIteration(project,[10,17,19]),'iterations')||String(project.job.CURRENT_ITERATION||'').trim(),source=iterationId?currentRegressionExecutions(project,iterationId):records(project,'regressionExecutions');return linked.every(reg=>{const id=recordId(reg,'regressions'),executions=source.filter(x=>String(recordValue(x,'REG_ID')||x.relationships?.REG_ID||'')===id&&upper(recordValue(x,'PHASE'))!=='PRE_CORRECTION');return executions.length===1&&effectiveRegressionDetermination(project,executions[0]).determination==='SATISFIED';});}
function unresolvedMaterialDefects(project){return confirmedDefects(project).filter(record=>{const severity=upper(recordValue(record,'SEVERITY')),status=upper(recordValue(record,'STATUS'));return ['CRITICAL','MAJOR'].includes(severity)&&!['CLOSED','CLOSED VERIFIED','CLOSED_VERIFIED','RESOLVED','CORRECTED AND VERIFIED','RETIRED'].includes(status)&&!defectResolvedByRegression(project,record);});}
function requirementId(record){return recordId(record,'requirements');}
function testRequirementId(record){return String(recordValue(record,'REQ_ID')||record.relationships?.REQ_ID||'');}
function resultRequirementId(project,record){const direct=String(recordValue(record,'REQ_ID')||record.relationships?.REQ_ID||'').trim();if(direct)return direct;const testId=String(recordValue(record,'TEST_ID')||record.relationships?.TEST_ID||'').trim();if(!testId)return '';const test=records(project,'tests').find(item=>recordId(item,'tests')===testId);return test?testRequirementId(test):'';}
function runIterationId(record){return String(recordValue(record,'ITERATION_ID')||record.relationships?.ITERATION_ID||'');}
function verificationKey(record){return [recordValue(record,'REQ_ID')||record.relationships?.REQ_ID,recordValue(record,'RUN_ID')||record.relationships?.RUN_ID,recordValue(record,'TEST_ID')||record.relationships?.TEST_ID].join('|');}
function iterationCandidateId(project,iterationId){const iteration=records(project,'iterations').find(record=>recordId(record,'iterations')===String(iterationId||''));return String(recordValue(iteration,'CANDIDATE_ID')||iteration?.relationships?.CANDIDATE_ID||iteration?.scope?.candidateId||'').trim();}

const SCOPE_KEYS=Object.freeze(['inputVersion','sourceSetVersion','requirementsVersion','testSuiteVersion','instructionVersion','iterationId','candidateId','baselineId','productId']);
const VERSION_SCOPE_KEYS=new Set(['inputVersion','sourceSetVersion','requirementsVersion','testSuiteVersion','instructionVersion']);
const scopeValue=value=>{const text=String(value??'').trim();return !text||['NONE','NOT APPLICABLE','UNKNOWN','PENDING','UNASSIGNED'].includes(text.toUpperCase())?null:value;};
function currentScope(project){return {inputVersion:scopeValue(project.job.CURRENT_INPUT_VERSION),sourceSetVersion:scopeValue(project.job.CURRENT_SOURCE_SET_VERSION),requirementsVersion:scopeValue(project.job.CURRENT_REQUIREMENTS_VERSION),testSuiteVersion:scopeValue(project.job.CURRENT_TEST_SUITE_VERSION),instructionVersion:scopeValue(project.job.CURRENT_INSTRUCTION_VERSION),iterationId:scopeValue(recordId(latestIteration(project,[10,17,19]),'iterations')||project.job.CURRENT_ITERATION),candidateId:scopeValue(recordId(records(project,'candidateFreezes').at(-1),'candidateFreezes')),baselineId:scopeValue(recordId(records(project,'baselines').at(-1),'baselines')||project.job.CURRENT_BASELINE_ID),productId:scopeValue(recordId(records(project,'products').at(-1),'products')||project.job.CURRENT_PRODUCT_ID)};}
function requiredVersionScopeKeys(collection,record=null){const declaredStage=Number(schema.RECORD_SCHEMAS[collection]?.stage),recordStage=Number(record?.stage),stage=Number.isInteger(declaredStage)&&declaredStage>0?declaredStage:Number.isInteger(recordStage)&&recordStage>0?recordStage:0,keys=[];if(stage>=1)keys.push('inputVersion');if(stage>=2)keys.push('sourceSetVersion');if(stage>=4)keys.push('requirementsVersion');if(stage>=6)keys.push('testSuiteVersion');if(stage>=8)keys.push('instructionVersion');return keys;}
function scopeMatches(record,scopeRule={},requiredKeys=null){const rs=record?.scope||{},fixedStageVersionScope=Array.isArray(requiredKeys),required=new Set(requiredKeys||[]);for(const key of required){const expected=scopeRule[key];if(expected===undefined||expected===null)continue;if(!Object.prototype.hasOwnProperty.call(rs,key)||rs[key]===undefined||rs[key]===null||rs[key]!==expected)return false;}for(const [key,value] of Object.entries(rs)){if(value===undefined||value===null)continue;if(fixedStageVersionScope&&VERSION_SCOPE_KEYS.has(key)&&!required.has(key))continue;if(Object.prototype.hasOwnProperty.call(scopeRule,key)&&(scopeRule[key]??null)!==value)return false;}return true;}
function recordsForScope(project,collection,scopeRule={}){const applicable=Object.fromEntries(Object.entries(scopeRule||{}).filter(([,v])=>v!==undefined&&v!==null));return records(project,collection).filter(record=>{if(!record.scope||!Object.keys(record.scope).length)return false;const required=requiredVersionScopeKeys(collection,record).filter(key=>Object.prototype.hasOwnProperty.call(applicable,key));return scopeMatches(record,applicable,required);});}
function recordsForCurrentScope(project,collection,scopeRule={}){return recordsForScope(project,collection,{...currentScope(project),...scopeRule});}
function scopeForIteration(project,iterationId){const id=String(iterationId||''),iteration=records(project,'iterations').find(r=>recordId(r,'iterations')===id);if(!iteration)return {iterationId:id||null};const candidateId=String(recordValue(iteration,'CANDIDATE_ID')||iteration.relationships?.CANDIDATE_ID||iteration.scope?.candidateId||'');return {...clone(iteration.scope||{}),iterationId:id,candidateId:candidateId||null};}
function recordsForIteration(project,collection,iterationId){return recordsForScope(project,collection,scopeForIteration(project,iterationId));}
function applicableTests(project,requirement,scopeRule=null){const req=requirementId(requirement),source=scopeRule?recordsForScope(project,'tests',scopeRule):recordsForCurrentScope(project,'tests');return source.filter(test=>testRequirementId(test)===req&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY')));}
const adjudication_NONE=Object.freeze(new Set(['','NONE','NOT APPLICABLE','N/A','NO','FALSE','0','EMPTY','NO FINDINGS','NO DEFECTS','NO FAILURES','NO DEVIATIONS','NO BLOCKERS','NOT PRESENT']));
const adjudication_SUCCESS=Object.freeze(new Set(['SATISFIED','SUCCESS','SUCCEEDED','PASS','PASSED','ACCEPTED','AUTHORIZED','CONFIRMED','COMPLETE','COMPLETED','VALID','MATCH','MATCHED','IDENTICAL','SAME','COMPLIANT','TRUE','YES','REJECTED_INVALID']));
const adjudication_FAILURE=Object.freeze(new Set(['VIOLATED','FAIL','FAILED','FAILURE','REJECTED','INVALID','MISMATCH','MISMATCHED','NONCOMPLIANT','FALSE','NO','ACCEPTED_INVALID']));
const adjudication_UNKNOWN=Object.freeze(new Set(['UNDETERMINED','UNKNOWN','BLOCKED','NOT RUN','NOT_RUN','UNAVAILABLE','UNVERIFIED','NOT ESTABLISHED']));
function adjudicationEmpty(value){if(value===null||value===undefined)return true;if(Array.isArray(value))return value.length===0||value.every(adjudicationEmpty);if(typeof value==='object')return Object.keys(value).length===0||Object.values(value).every(adjudicationEmpty);return adjudication_NONE.has(upper(value));}
function adjudicationAdverse(value){if(value===null||value===undefined)return false;if(typeof value==='boolean')return value;if(typeof value==='number')return value>0;if(Array.isArray(value))return value.some(v=>!adjudicationEmpty(v));if(typeof value==='object')return Object.values(value).some(adjudicationAdverse);return !adjudicationEmpty(value);}
function controlledOutcome(value){const v=upper(value);if(adjudication_UNKNOWN.has(v))return 'UNDETERMINED';if(adjudication_FAILURE.has(v))return 'VIOLATED';if(adjudication_SUCCESS.has(v))return 'SATISFIED';return null;}
function evidenceRecordsFor(project,result,canonicalEvidence=null){if(Array.isArray(canonicalEvidence))return canonicalEvidence;const ids=[...new Set(safe(result?.evidenceRefs).map(String).filter(Boolean))];return ids.map(id=>records(project,'evidenceRecords').find(e=>recordId(e,'evidenceRecords')===id)).filter(Boolean);}
function resultExecutionIdentity(project,result){return String(recordValue(result,'EXECUTION_ID')||recordValue(result,'REG_EXEC_ID')||recordValue(result,'RUN_ID')||result?.relationships?.RUN_ID||recordValue(result,'PRODUCT_ID')||result?.relationships?.PRODUCT_ID||'').trim();}
function resultContextIdentity(project,result){
  const normalize=value=>{const text=String(value??'').trim();return !text||['NONE','UNKNOWN','NOT APPLICABLE','PENDING','UNASSIGNED'].includes(upper(text))?'':text;};
  const direct=normalize(recordValue(result,'CONTEXT_ID')||recordValue(result,'VERIFIER_CONTEXT_ID')||recordValue(result,'PRODUCTION_CONTEXT_ID')||result?.relationships?.CONTEXT_ID);if(direct)return direct;
  const runId=normalize(recordValue(result,'RUN_ID')||result?.relationships?.RUN_ID);if(runId){const run=records(project,'runs').find(r=>recordId(r,'runs')===runId);const runContext=normalize(recordValue(run,'CONTEXT_ID')||run?.relationships?.CONTEXT_ID);if(runContext)return runContext;}
  const rawId=normalize(result?.rawResponseId),proposalId=normalize(result?.sourceProposalId);if(rawId)return 'RAW_RESPONSE:'+rawId;if(proposalId)return 'PROPOSAL:'+proposalId;return '';
}
function observedValue(result){for(const key of ['OBSERVED_RESULT','ACTUAL_RESULT','OBSERVED_MEANING','OBSERVATIONS','RESULT','VALIDATOR_RESULTS','MEANING_VERIFICATION_RESULTS','PROCESS_EVIDENCE','PRODUCT_EVIDENCE']){const value=recordValue(result,key);if(!adjudicationEmpty(value))return value;}return null;}
function evaluateEvidenceContract(test,result,canonicalEvidence,project){
  const evidence=evidenceRecordsFor(project,result,canonicalEvidence),reasons=[],mode=upper(recordValue(test,'EXECUTION_MODE')),artifactReq=upper(recordValue(test,'ARTIFACT_REQUIREMENTS')),requiresAttachment=Boolean(test&&artifactReq&&!adjudication_NONE.has(artifactReq));
  if(!evidence.length)reasons.push('No canonical evidence record is linked to the result; narrative alone is non-controlling.');
  for(const item of evidence){const native=item?.source==='APPLICATION_TEST_RUNTIME',authority=native?'APPLICATION':upper(recordValue(item,'AUTHORITY_TYPE')),attachmentId=String(recordValue(item,'ATTACHMENT_ID')||item?.relationships?.ATTACHMENT_ID||'').trim();if(adjudicationEmpty(native?recordValue(item,'APPLICATION_EVIDENCE_KIND'):recordValue(item,'KIND')))reasons.push('Canonical evidence kind is missing.');if(!authority||authority==='UNKNOWN')reasons.push('Canonical evidence authority type is missing or unknown.');if(adjudicationEmpty(native?recordValue(item,'APPLICATION_EVIDENCE_CONTENT'):recordValue(item,'CONTENT'))&&adjudicationEmpty(native?recordValue(item,'APPLICATION_EVIDENCE_DESCRIPTION'):recordValue(item,'DESCRIPTION'))&&adjudicationEmpty(recordValue(item,'LOCATION')))reasons.push('Canonical evidence contains no preserved observation payload.');if(attachmentId&&attachmentId!=='UNKNOWN'){const a=records(project,'artifacts').find(x=>recordId(x,'artifacts')===attachmentId);if(!a||upper(recordValue(a,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED')reasons.push('Referenced evidence attachment bytes are not application-verified.');}}
  if(requiresAttachment&&!evidence.some(item=>{const id=String(recordValue(item,'ATTACHMENT_ID')||item?.relationships?.ATTACHMENT_ID||'').trim();return id&&id!=='UNKNOWN';}))reasons.push('The controlling test requires an attachment-backed evidence record.');
  const executionId=resultExecutionIdentity(project,result),contextId=resultContextIdentity(project,result);
  if(test&&mode!=='APPLICATION_DETERMINISTIC'&&!executionId)reasons.push('Execution identity is not established.');
  if(test&&['INDEPENDENT_AGENT_REVIEW','EXTERNAL_AGENT_TOOL','HUMAN_INSPECTION','EXTERNAL_SYSTEM'].includes(mode)&&!contextId)reasons.push('Execution/reviewer context identity is not established.');
  return {sufficient:reasons.length===0,reasons,evidenceIds:evidence.map(e=>recordId(e,'evidenceRecords')),executionId,contextId};
}
function claimedDetermination(collection,record){if(collection==='deterministicResults'&&record?.source==='APPLICATION_TEST_RUNTIME')return upper(recordValue(record,'APPLICATION_DETERMINATION'));if(collection==='processAudits')return upper(recordValue(record,'PROCESS_DETERMINATION'));if(collection==='productAudits')return upper(recordValue(record,'PRODUCT_DETERMINATION'));if(collection==='regressionExecutions')return upper(recordValue(record,'RESULT'));if(collection==='products')return upper(recordValue(record,'STATUS'));return upper(recordValue(record,'DETERMINATION'));}
function testForResult(project,record){const id=String(recordValue(record,'TEST_ID')||record?.relationships?.TEST_ID||'').trim();return id?records(project,'tests').find(t=>recordId(t,'tests')===id):null;}
function effectiveRegressionDetermination(project,record){const evidence=evaluateEvidenceContract(null,record,null,project);if(!evidence.sufficient)return {determination:'UNDETERMINED',reasons:evidence.reasons};const phase=upper(recordValue(record,'PHASE')),out=controlledOutcome(recordValue(record,'RESULT'));if(!out)return {determination:'UNDETERMINED',reasons:['Regression RESULT is not a controlled application-evaluable outcome.']};if(phase==='PRE_CORRECTION')return {determination:out==='VIOLATED'?'SATISFIED':'VIOLATED',reasons:out==='VIOLATED'?[]:['Pre-correction regression did not demonstrate the expected failure.']};return {determination:out,reasons:out==='SATISFIED'?[]:['Post-correction regression did not establish success.']};}
function comparisonFacts(project,reqId,iterationId){const rows=(iterationId?recordsForIteration(project,'verification',iterationId):recordsForCurrentScope(project,'verification')).filter(r=>String(recordValue(r,'REQ_ID')||r.relationships?.REQ_ID||'')===reqId),determinations=rows.map(r=>effectiveDetermination('verification',r,testForResult(project,r),project)),ds=determinations;return {rows,determinations,runDeterminations:rows.map((r,i)=>({runId:String(recordValue(r,'RUN_ID')||r.relationships?.RUN_ID||''),testId:String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||''),determination:ds[i]})),allSatisfied:rows.length>0&&ds.every(d=>d==='SATISFIED'),anyViolation:ds.includes('VIOLATED'),anyUndetermined:ds.some(d=>d!=='SATISFIED'&&d!=='VIOLATED')};}
function validateRootCauseRecord(record,project){const reasons=[],defectId=String(recordValue(record,'DEFECT_ID')||record?.relationships?.DEFECT_ID||'').trim(),defect=records(project,'defects').find(d=>recordId(d,'defects')===defectId);if(!defect)reasons.push('RCA defect identity is missing or does not resolve.');for(const key of ['LAYER_TRACE','EARLIEST_DEFECTIVE_LAYER','ROOT_CAUSE','DOWNSTREAM_INVALIDATION'])if(adjudicationEmpty(recordValue(record,key)))reasons.push('RCA '+key+' is missing.');if(adjudicationEmpty(recordValue(record,'EVIDENCE'))&&!safe(record?.evidenceRefs).length)reasons.push('RCA evidence linkage is missing.');return {valid:reasons.length===0,reasons,defectId,responsibleLayer:String(recordValue(record,'EARLIEST_DEFECTIVE_LAYER')||'').trim()};}
function validateChangeTrace(record,project){const reasons=[],triggerRaw=recordValue(record,'TRIGGERING_DEFECT_IDS'),triggerIds=Array.isArray(triggerRaw)?triggerRaw.map(String):String(triggerRaw||'').split(/[;,\s]+/).filter(Boolean),layer=String(recordValue(record,'RESPONSIBLE_LAYER')||'').trim();if(!triggerIds.length)reasons.push('Changeset has no triggering defect identity.');if(!layer)reasons.push('Changeset responsible layer is missing.');if(adjudicationEmpty(recordValue(record,'EXACT_MODIFICATION')))reasons.push('Changeset exact modification is missing.');if(adjudicationEmpty(recordValue(record,'NEW_ARTIFACT_VERSION')))reasons.push('Changeset changed artifact/version identity is missing.');if(adjudicationEmpty(recordValue(record,'DOWNSTREAM_INVALIDATION')))reasons.push('Changeset downstream invalidation statement is missing.');for(const id of triggerIds){const rca=records(project,'rootCauses').find(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===id);if(!rca){reasons.push('No RCA is linked to '+id+'.');continue;}const rv=validateRootCauseRecord(rca,project);if(!rv.valid)reasons.push(...rv.reasons.map(x=>id+': '+x));if(rv.responsibleLayer&&layer&&upper(rv.responsibleLayer)!==upper(layer)&&upper(recordValue(record,'AUTHORIZATION'))!=='CONTROLLED OVERRIDE')reasons.push('Changeset responsible layer does not match RCA for '+id+' and no controlled override exists.');}return {valid:reasons.length===0,reasons,triggerIds};}
function confirmationDetermination(project,record){const reasons=[],iterationId=String(recordValue(record,'CONFIRMATION_ITERATION_ID')||record?.relationships?.CONFIRMATION_ITERATION_ID||record?.scope?.iterationId||'').trim(),iteration=records(project,'iterations').find(r=>recordId(r,'iterations')===iterationId);if(!iteration){reasons.push('Confirmation iteration identity is missing.');return {determination:'UNDETERMINED',reasons};}const ev=evaluateIteration(project,iterationId,'UNCHANGED_CONFIRMATION');if(!ev.complete)reasons.push(...ev.reasons);const matrix=verificationMatrix(project,iterationId);if(matrix.expected.length===0||matrix.missing.length||matrix.duplicates.length||matrix.invalid.length)reasons.push('Confirmation verification matrix is incomplete or invalid.');const bad=matrix.verification.map(r=>effectiveDetermination('verification',r,testForResult(project,r),project)).filter(x=>x!=='SATISFIED');if(bad.length)reasons.push('Confirmation contains a non-satisfied effective verification result.');const regs=records(project,'regressions').filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED');for(const reg of regs){const id=recordId(reg,'regressions'),exec=currentRegressionExecutions(project,iterationId).filter(x=>String(recordValue(x,'REG_ID')||x.relationships?.REG_ID||'')===id&&upper(recordValue(x,'PHASE'))!=='PRE_CORRECTION');if(exec.length!==1||effectiveRegressionDetermination(project,exec[0]).determination!=='SATISFIED')reasons.push('Active regression '+id+' is not successfully evidenced in confirmation.');}const defects=confirmedDefects(project).filter(d=>String(d.scope?.iterationId||'')===iterationId);if(defects.length)reasons.push('Confirmation iteration has new confirmed defects.');for(const req of mandatoryRequirements(project,scopeForIteration(project,iterationId))){const f=comparisonFacts(project,requirementId(req),iterationId);if(!f.allSatisfied)reasons.push('Confirmation comparison is not all-satisfied for '+requirementId(req)+'.');}return {determination:reasons.length?'UNDETERMINED':'SATISFIED',reasons};}
function auditDetermination(collection,record,project){const reasons=[],e=evaluateEvidenceContract(null,record,null,project);if(!e.sufficient)reasons.push(...e.reasons);if(collection==='processAudits'){for(const key of ['UNAUTHORIZED_MODIFICATION','PROCESS_DEFECTS','BLOCKERS'])if(adjudicationAdverse(recordValue(record,key)))reasons.push(key+' is materially adverse.');for(const key of ['APPROVED_INPUTS_VS_ACTUAL','APPROVED_INSTRUCTION_VS_ACTUAL','APPROVED_TOOLS_VS_ACTUAL','REQUIRED_TESTS_VS_EXECUTED','CHAIN_OF_CUSTODY']){const v=recordValue(record,key),out=controlledOutcome(v);if(out!=='SATISFIED'&& !['MATCH','MATCHED','IDENTICAL','SAME','COMPLIANT','COMPLETE','COMPLETED','INTACT'].includes(upper(v)))reasons.push(key+' is not application-establishable as satisfied.');}}else{for(const key of ['CRITICAL_DEFECTS','MAJOR_DEFECTS','MANDATORY_UNKNOWNS'])if(numeric(recordValue(record,key))>0)reasons.push(key+' is nonzero.');for(const key of ['PRODUCT_DEFECTS','BLOCKERS'])if(adjudicationAdverse(recordValue(record,key)))reasons.push(key+' is materially adverse.');for(const key of ['VALIDATOR_RESULTS','MEANING_VERIFICATION_RESULTS'])if(controlledOutcome(recordValue(record,key))!=='SATISFIED')reasons.push(key+' does not establish success.');const mandatory=numeric(recordValue(record,'MANDATORY_REQUIREMENT_COUNT')),affirmative=numeric(recordValue(record,'AFFIRMATIVE_SATISFACTION_COUNT'));if(mandatory>0&&affirmative!==mandatory)reasons.push('Affirmative satisfaction count does not equal mandatory requirement count.');}return {determination:reasons.length?'UNDETERMINED':'SATISFIED',reasons};}
function parseRepresentationInspectionObservation(record){
  const reasons=[],normalizeList=value=>[...new Set(safe(value).map(x=>String(x??'').trim()).filter(Boolean))].sort(),sameSet=(a,b)=>hash.sha256Value(normalizeList(a))===hash.sha256Value(normalizeList(b));
  let payload=null;try{payload=JSON.parse(String(recordValue(record,'OBSERVATIONS')||''));}catch{reasons.push('OBSERVATIONS must be the contracted strict JSON coverage object.');return {valid:false,reasons,payload:null};}
  if(!payload||typeof payload!=='object'||Array.isArray(payload)){reasons.push('OBSERVATIONS coverage payload is not an object.');return {valid:false,reasons,payload:null};}
  const requiredPageOrViewIds=normalizeList(payload.requiredPageOrViewIds),inspectedPageOrViewIds=normalizeList(payload.inspectedPageOrViewIds),requiredPackagedFileIds=normalizeList(payload.requiredPackagedFileIds),openedOrTestedPackagedFileIds=normalizeList(payload.openedOrTestedPackagedFileIds),requiredTransformationIds=normalizeList(payload.requiredTransformationIds),inspectedTransformationIds=normalizeList(payload.inspectedTransformationIds),observation=String(payload.observation||'').trim();
  if(!sameSet(requiredPageOrViewIds,inspectedPageOrViewIds))reasons.push('required page/view identities do not exactly match inspected page/view identities.');
  if(!sameSet(requiredPackagedFileIds,openedOrTestedPackagedFileIds))reasons.push('required packaged-file identities do not exactly match opened/tested packaged-file identities.');
  if(!sameSet(requiredTransformationIds,inspectedTransformationIds))reasons.push('required transformation identities do not exactly match inspected transformation identities.');
  if(!observation)reasons.push('representation observation is empty.');
  return {valid:reasons.length===0,reasons,payload,requiredPageOrViewIds,inspectedPageOrViewIds,requiredPackagedFileIds,openedOrTestedPackagedFileIds,requiredTransformationIds,inspectedTransformationIds,observation};
}

function evaluateResultConsistency(collection,record,test,project){
  const reasons=[],claimed=claimedDetermination(collection,record),evidence=evaluateEvidenceContract(test,record,null,project);let determination='UNDETERMINED';
  if(collection==='deterministicResults'&&record?.source==='APPLICATION_TEST_RUNTIME'){const app=upper(recordValue(record,'APPLICATION_DETERMINATION')||'UNDETERMINED'),epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});const nativeReasons=[...evidence.reasons,...epistemic.reasons];return {determination:nativeReasons.length&&app==='SATISFIED'?'UNDETERMINED':app,reasons:[...new Set(nativeReasons)],claimedDetermination:app,evidence};}
  if(collection==='regressionExecutions')return {...effectiveRegressionDetermination(project,record),claimedDetermination:claimed};
  if(collection==='preflightRecords'){const adverseTokens=new Set(['TRUE','YES','PRESENT','FOUND','UNRESOLVED','VIOLATED','FAILED','BLOCKED']);for(const key of ['MULTIPLE_INTERPRETATIONS','UNDEFINED_OBJECTS','UNSUPPLIED_DEPENDENCIES','INTERNAL_CONFLICTS','UNAVAILABLE_CAPABILITIES']){const value=recordValue(record,key);if(typeof value==='boolean'?value:adverseTokens.has(upper(value)))reasons.push(key+' establishes an unresolved preflight defect.');}const findings=upper(recordValue(record,'FINDINGS'));if(['DEFECT FOUND','MATERIAL DEFECT','UNRESOLVED DEFECT','MATERIAL AMBIGUITY','MATERIAL CONFLICT'].includes(findings))reasons.push('FINDINGS establishes an unresolved preflight defect.');if(!evidence.sufficient)reasons.push(...evidence.reasons);determination=reasons.length?'UNDETERMINED':'SATISFIED';}
  else if(collection==='confirmationRecords')return {...confirmationDetermination(project,record),claimedDetermination:claimed};
  else if(collection==='processAudits'||collection==='productAudits')return {...auditDetermination(collection,record,project),claimedDetermination:claimed};
  else if(collection==='products'){if(adjudicationAdverse(recordValue(record,'FAILURES')))reasons.push('Production execution reports failures.');if(adjudicationAdverse(recordValue(record,'DEVIATIONS')))reasons.push('Production execution reports deviations.');if(reasons.length)determination='VIOLATED';else determination=record?.completionState==='COMPLETED'?'SATISFIED':'UNDETERMINED';}
  else if(collection==='verification'){if(!evidence.sufficient)reasons.push(...evidence.reasons);const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const runId=String(recordValue(record,'RUN_ID')||record.relationships?.RUN_ID||''),iterationId=String(record.scope?.iterationId||runIterationId(record)||''),ind=evaluateContextIndependence(project,{role:'VERIFICATION',iterationId,runId,verifierContextId:String(recordValue(record,'VERIFIER_CONTEXT_ID')||'')});if(!['APPLICATION_ESTABLISHED','EXTERNALLY_SUPPORTED'].includes(ind.determination))reasons.push('Verification independence is neither application-established nor externally supported.');const observed=recordValue(record,'OBSERVED_RESULT'),expected=recordValue(record,'EXPECTED_RESULT')||recordValue(test,'EXPECTED_RESULT'),out=controlledOutcome(observed);if(out)determination=out;else if(!adjudicationEmpty(observed)&&!adjudicationEmpty(expected)&&upper(observed)===upper(expected))determination='SATISFIED';else determination='UNDETERMINED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}
  else if(collection==='deterministicResults'){if(!evidence.sufficient)reasons.push(...evidence.reasons);const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const actual=recordValue(record,'ACTUAL_RESULT'),expected=recordValue(record,'EXPECTED_RESULT')||recordValue(test,'EXPECTED_RESULT'),out=controlledOutcome(actual);if(out)determination=out;else if(!adjudicationEmpty(actual)&&!adjudicationEmpty(expected)&&upper(actual)===upper(expected))determination='SATISFIED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}
  else if(collection==='meaningResults'){if(!evidence.sufficient)reasons.push(...evidence.reasons);const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const comparison=controlledOutcome(recordValue(record,'EVIDENCE_BASED_COMPARISON')),observed=recordValue(record,'OBSERVED_MEANING'),required=recordValue(record,'REQUIRED_MEANING');if(comparison)determination=comparison;else if(!adjudicationEmpty(observed)&&!adjudicationEmpty(required)&&upper(observed)===upper(required))determination='SATISFIED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}
  else if(collection==='adversarialResults'){if(!evidence.sufficient)reasons.push(...evidence.reasons);const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const defectId=String(recordValue(record,'DEFECT_ID')||record.relationships?.DEFECT_ID||'').trim(),actual=upper(recordValue(record,'ACTUAL_RESULT')),testId=String(recordValue(record,'TEST_ID')||record.relationships?.TEST_ID||'').trim(),regId=String(recordValue(record,'REG_ID')||record.relationships?.REG_ID||'').trim();if(defectId)determination='VIOLATED';else if(regId){const executions=records(project,'regressionExecutions').filter(x=>String(recordValue(x,'REG_ID')||x.relationships?.REG_ID||'')===regId&&upper(recordValue(x,'PHASE'))!=='PRE_CORRECTION'),latest=executions.at(-1);determination=latest&&effectiveRegressionDetermination(project,latest).determination==='SATISFIED'?'SATISFIED':'UNDETERMINED';}else{const expected=upper(recordValue(test,'EXPECTED_RESULT')),failure=upper(recordValue(test,'FAILURE_CONDITION')),out=controlledOutcome(actual);if(out)determination=out;else if(expected&&actual===expected)determination='SATISFIED';else if(failure&&actual===failure)determination='VIOLATED';else if(['NO MATERIAL ADVERSARIAL DEFECT FOUND'].includes(actual))determination='SATISFIED';else determination='UNDETERMINED';}if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}
  else if(collection==='representationInspections'){if(!evidence.sufficient)reasons.push(...evidence.reasons);const epistemic=evaluateEvidenceSufficiency(project,{result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const defectId=String(recordValue(record,'DEFECT_ID')||record.relationships?.DEFECT_ID||'').trim(),artifactId=String(recordValue(record,'ARTIFACT_ID')||record.relationships?.ARTIFACT_ID||''),artifact=recordsForCurrentScope(project,'artifacts').find(a=>recordId(a,'artifacts')===artifactId),rendered=!adjudicationEmpty(recordValue(record,'RENDERING_OPENING_EVIDENCE')),coverage=parseRepresentationInspectionObservation(record);if(!coverage.valid)reasons.push(...coverage.reasons);if(defectId)determination='VIOLATED';else if(!artifact||!rendered||!coverage.valid)determination='UNDETERMINED';else determination='SATISFIED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}
  else determination=controlledOutcome(claimed)||'UNDETERMINED';
  if(claimed==='SATISFIED'&&determination!=='SATISFIED')reasons.push('Claimed favorable determination conflicts with application-derived effective determination.');
  return {determination,reasons,claimedDetermination:claimed,evidence};
}
function effectiveDetermination(collection,record,test,project){return evaluateResultConsistency(collection,record,test,project).determination;}
function adjudicatedClone(project){
  const collections=['verification','deterministicResults','meaningResults','adversarialResults','representationInspections','preflightRecords','confirmationRecords','processAudits','productAudits','regressionExecutions','products'];
  const copy={...project,job:{...(project?.job||{})},release:{...(project?.release||{})},stages:{...(project?.stages||{})},projectData:{...(project?.projectData||{})}};
  for(const collection of collections)copy.projectData[collection]=safe(project?.projectData?.[collection]).map(record=>clone(record));
  for(const collection of collections)for(const r of records(copy,collection)){const test=testForResult(copy,r),d=effectiveDetermination(collection,r,test,copy);if(collection==='processAudits')recordFields(r).PROCESS_DETERMINATION=d;else if(collection==='productAudits')recordFields(r).PRODUCT_DETERMINATION=d;else if(collection==='regressionExecutions'){const phase=upper(recordValue(r,'PHASE'));recordFields(r).RESULT=phase==='PRE_CORRECTION'?(d==='SATISFIED'?'VIOLATED':d==='VIOLATED'?'SATISFIED':'UNDETERMINED'):d;}else if(collection==='products')recordFields(r).STATUS=d==='SATISFIED'?'COMPLETED':d==='VIOLATED'?'FAILED':'BLOCKED';else recordFields(r).DETERMINATION=d;}
  return copy;
}
function validateTraceIntegrity(kind,record,project){if(kind==='RCA')return validateRootCauseRecord(record,project);if(kind==='CHANGESET')return validateChangeTrace(record,project);return {valid:false,reasons:['Unknown trace-integrity kind.']};}
function verificationMatrix(project,iterationId){const scope=scopeForIteration(project,iterationId),requirements=mandatoryRequirements(project,scope),runs=recordsForIteration(project,'runs',iterationId).filter(r=>runIterationId(r)===String(iterationId||'')),verification=recordsForIteration(project,'verification',iterationId),expected=[];for(const req of requirements)for(const test of applicableTests(project,req,scope))for(const run of runs)expected.push([requirementId(req),recordId(run,'runs'),recordId(test,'tests')].join('|'));const expectedSet=new Set(expected),counts=new Map();for(const r of verification){const key=verificationKey(r);counts.set(key,(counts.get(key)||0)+1);}const missing=expected.filter(k=>!counts.has(k)),duplicates=[...counts].filter(([k,c])=>expectedSet.has(k)&&c!==1).map(([key,count])=>({key,count}));const invalid=verification.filter(r=>{const key=verificationKey(r),test=testForResult(project,r),evaluation=evaluateResultConsistency('verification',r,test,project);return !expectedSet.has(key)||!['SATISFIED','VIOLATED','UNDETERMINED'].includes(evaluation.determination)||!evaluation.evidence?.sufficient||evaluation.reasons.some(reason=>reason.includes('independence is not application-established'));});return {scope,requirements,runs,verification,expected,counts,missing,duplicates,invalid,coverage:expected.length?expected.filter(k=>counts.get(k)===1).length/expected.length:0};}
function currentRegressionExecutions(project,iterationId){return recordsForIteration(project,'regressionExecutions',iterationId).filter(r=>!iterationId||String(recordValue(r,'ITERATION_ID')||r.relationships?.ITERATION_ID||r.scope?.iterationId||'')===String(iterationId));}
function activeRegressions(project){return records(project,'regressions').filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED');}
function acceptedOperationSet(project,stage,scopeRule={}){
  const proposals=safe(project.projectData.responseProposals),out=new Set(),keys=['iterationId','candidateId','baselineId','productId'];
  for(const c of acceptedChanges(project,stage)){
    const proposal=proposals.find(p=>p.proposalId===c.proposalId),op=c.operation||proposal?.envelope?.operation,scope={...(proposal?.envelope?.scope||{}),...(c.scope||{}),...(c.applicationScopeBinding||{})};
    if(keys.some(key=>scopeRule[key]!==undefined&&scopeRule[key]!==null&&String(scope[key]??'')!==String(scopeRule[key])))continue;
    if(op)out.add(String(op));
  }
  if(Number(stage)===17){
    const appIteration=records(project,'iterations').find(r=>Number(r.stage)===17&&isActiveRecord(r)&&(!scopeRule.iterationId||recordId(r,'iterations')===String(scopeRule.iterationId))&&(!scopeRule.candidateId||String(recordValue(r,'CANDIDATE_ID')||r.scope?.candidateId||'')===String(scopeRule.candidateId)));
    if(appIteration){
      out.add('FREEZE');
      if(out.has('COMPARE')){
        const iterationId=recordId(appIteration,'iterations'),scope=scopeForIteration(project,iterationId),matrix=verificationMatrix(project,iterationId),requirements=mandatoryRequirements(project,scope),comparisons=recordsForIteration(project,'comparisons',iterationId),defects=recordsForIteration(project,'defects',iterationId),comparisonByRequirement=new Map();
        for(const comparison of comparisons){const requirementIdValue=String(recordValue(comparison,'REQ_ID')||comparison.relationships?.REQ_ID||'');if(!comparisonByRequirement.has(requirementIdValue))comparisonByRequirement.set(requirementIdValue,[]);comparisonByRequirement.get(requirementIdValue).push(comparison);}
        const matrixSatisfied=matrix.expected.length>0&&!matrix.missing.length&&!matrix.duplicates.length&&!matrix.invalid.length&&matrix.verification.every(record=>effectiveDetermination('verification',record,testForResult(project,record),project)==='SATISFIED');
        const comparisonsSatisfied=requirements.length>0&&requirements.every(requirement=>{const requirementIdValue=requirementId(requirement),rows=comparisonByRequirement.get(requirementIdValue)||[],facts=comparisonFacts(project,requirementIdValue,iterationId);return rows.length===1&&facts.allSatisfied&&!facts.anyViolation&&!facts.anyUndetermined&&!truth(recordValue(rows[0],'CORRECTNESS_AFFECTING_VARIANCE'))&&adjudicationEmpty(recordValue(rows[0],'INCONCLUSIVE_TESTS'))&&adjudicationEmpty(recordValue(rows[0],'DEFECT_IDS'));});
        if(matrixSatisfied&&comparisonsSatisfied&&defects.length===0)out.add('ROOT_CAUSE');
        if(out.has('ROOT_CAUSE')&&out.has('REGRESSION')){const executions=currentRegressionExecutions(project,iterationId),regressions=activeRegressions(project),regressionsSatisfied=regressions.every(regression=>{const id=recordId(regression,'regressions'),matches=executions.filter(execution=>String(recordValue(execution,'REG_ID')||execution.relationships?.REG_ID||'')===id&&upper(recordValue(execution,'PHASE'))!=='PRE_CORRECTION');return matches.length===1&&effectiveRegressionDetermination(project,matches[0]).determination==='SATISFIED';});if(regressionsSatisfied)out.add('CORRECT');}
      }
    }
  }
  if(Number(stage)===19){const appIteration=records(project,'iterations').find(r=>Number(r.stage)===19&&isActiveRecord(r)&&upper(recordValue(r,'PURPOSE'))==='UNCHANGED_CONFIRMATION'&&(!scopeRule.iterationId||recordId(r,'iterations')===String(scopeRule.iterationId))&&(!scopeRule.candidateId||String(recordValue(r,'CANDIDATE_ID')||r.scope?.candidateId||'')===String(scopeRule.candidateId)));if(appIteration)out.add('CONFIRM_FREEZE');}
  return out;
}
function candidateComponentIdentity(project,candidateId){const c=records(project,'candidateFreezes').find(r=>recordId(r,'candidateFreezes')===String(candidateId||''));return c?hash.sha256Value(recordValue(c,'COMPONENT_HASHES')||{}):'';}
function evaluateIteration(project,iterationId,mode='INITIAL'){
  const matrix=verificationMatrix(project,iterationId),runs=matrix.runs,scope=matrix.scope,iteration=records(project,'iterations').find(r=>recordId(r,'iterations')===String(iterationId||'')),stage=Number(runs[0]?.stage||iteration?.stage||0),expectedCandidate=String(scope.candidateId||''),candidateIdentity=candidateComponentIdentity(project,expectedCandidate),requiredRunOperation=mode==='INITIAL'?'COMPLETE':'EXECUTE_RUN',runChanges=acceptedChanges(project,stage).filter(c=>String(c.operation||'')===requiredRunOperation&&String(c.scope?.iterationId||'')===String(iterationId)),raw=safe(project.projectData.rawResponses),receipts=safe(project.projectData.outputReceipts),runFailures=[];
  for(const run of runs){const runId=recordId(run,'runs'),contextId=String(recordValue(run,'CONTEXT_ID')||run.relationships?.CONTEXT_ID||'');const links=runChanges.filter(c=>String(c.scope?.runId||'')===runId&&String(c.scope?.contextId||'')===contextId);if(links.length!==1){runFailures.push(runId);continue;}const rr=raw.find(x=>x.rawResponseId===links[0].rawResponseId&&!x.invalidatedBy),rc=receipts.find(x=>x.rawResponseId===links[0].rawResponseId);if(!rr||!rc||String(rc.runId||'')!==runId||String(rc.contextId||'')!==contextId)runFailures.push(runId);}
  const independence=evaluateContextIndependence(project,{role:'RUN_BATCH',iterationId}),comparisons=recordsForIteration(project,'comparisons',iterationId),defects=recordsForIteration(project,'defects',iterationId),rca=recordsForIteration(project,'rootCauses',iterationId),regExec=currentRegressionExecutions(project,iterationId),regFailures=[];
  if(mode!=='INITIAL')for(const reg of activeRegressions(project)){const id=recordId(reg,'regressions'),xs=regExec.filter(x=>String(recordValue(x,'REG_ID')||x.relationships?.REG_ID||'')===id),latest=xs.at(-1);if(xs.length!==1||!latest||effectiveRegressionDetermination(project,latest).determination!=='SATISFIED')regFailures.push(id);}
  const unexplained=comparisons.filter(r=>truth(recordValue(r,'CORRECTNESS_AFFECTING_VARIANCE'))&&!truth(recordValue(r,'AUTHORIZED_VARIANCE'))),ops=acceptedOperationSet(project,stage,{iterationId}),requiredOps=stage===17?['FREEZE','EXECUTE_RUN','VERIFY','COMPARE','ROOT_CAUSE','REGRESSION','CORRECT']:stage===19?['CONFIRM_FREEZE','EXECUTE_RUN','VERIFY','COMPARE','REGRESSION_VERIFY','CONFIRM']:[],reasons=[];
  if(runs.length!==10)reasons.push('Exactly ten current runs are required.');if(independence.determination!=='APPLICATION_ESTABLISHED')reasons.push(...(independence.reasons.length?independence.reasons:['Ten current runs are not application-established as independent.']));if(!candidateIdentity)reasons.push('Every run must resolve to one exact frozen candidate identity and component hash set.');if(runFailures.length)reasons.push('Every current run requires exactly one accepted run-bound raw response and receipt: '+runFailures.join(', ')+'.');if(mode!=='INITIAL'&&(matrix.expected.length===0||matrix.missing.length||matrix.duplicates.length||matrix.invalid.length))reasons.push('The current REQ × RUN × TEST matrix is incomplete, non-independent, or evidentially insufficient.');if(regFailures.length)reasons.push('Every active permanent regression requires exactly one sufficiently evidenced successful execution in the current iteration: '+regFailures.join(', ')+'.');const missingOps=requiredOps.filter(op=>!ops.has(op));if(missingOps.length)reasons.push('Required stage operations are missing: '+missingOps.join(', ')+'.');if(unexplained.length)reasons.push('Unexplained correctness-affecting variance remains.');
  const stability=executionStability(project,iterationId);
  return {mode,iterationId,runs:runs.map(r=>recordId(r,'runs')),contextCount:independence.contextCount||0,candidateIds:independence.candidateIds||[],candidateIdentity,matrix,independence,stability,comparisonCount:comparisons.length,defectCount:defects.length,rootCauseCount:rca.length,regressionExecutionCount:regExec.length,unexplainedVarianceCount:unexplained.length,complete:reasons.length===0,reasons};
}
function deriveMandatoryTestCoverage(project){const requirements=mandatoryRequirements(project);const covered=requirements.filter(r=>applicableTests(project,r).length).length;return {value:requirements.length?covered/requirements.length:0,inputReferences:requirements.map(requirementId),calculationVersion:'1'};}
function deriveVerificationMatrix(project){const iteration=latestIteration(project,[10,17,19]);const id=recordId(iteration,'iterations');const m=verificationMatrix(project,id);return {value:{expected:m.expected.length,complete:m.expected.length-m.missing.length,missing:m.missing,duplicates:m.duplicates,coverage:m.coverage},inputReferences:[...m.expected],calculationVersion:'1'};}
function deriveConvergence(project){return {value:convergenceMetrics(project),inputReferences:[recordId(latestIteration(project,[17]),'iterations')],calculationVersion:'1'};}
function deriveRelease(project){const value=releaseMetrics(project);return {value,inputReferences:value.inputReferences||[],calculationVersion:'1'};}
function deriveArtifactIdentity(project){const current=records(project,'artifactIdentities');return {value:{authorized:current.length>0&&current.every(r=>upper(recordValue(r,'AUTHORIZATION'))==='AUTHORIZED'),count:current.length},inputReferences:current.map(r=>recordId(r,'artifactIdentities')),calculationVersion:'1'};}
function deriveEvidenceChains(project){const current=records(project,'evidenceChains');return {value:{complete:current.length>0&&current.every(r=>upper(recordValue(r,'STATUS'))==='COMPLETE'),count:current.length},inputReferences:current.map(r=>recordId(r,'evidenceChains')),calculationVersion:'1'};}
const DERIVATIONS=Object.freeze({'stage06.mandatoryTestCoverage':deriveMandatoryTestCoverage,'stage12.verificationMatrix':deriveVerificationMatrix,'stage18.convergence':deriveConvergence,'stage27.release':deriveRelease,'stage28.artifactIdentity':deriveArtifactIdentity,'stage29.evidenceChains':deriveEvidenceChains});

function coverageMetrics(project,iterationIdOverride=null){const iterationId=iterationIdOverride||recordId(latestIteration(project,[10,17,19]),'iterations')||project.job.CURRENT_ITERATION||'',scope=iterationId?scopeForIteration(project,iterationId):currentScope(project),requirements=mandatoryRequirements(project,scope),tests=iterationId?recordsForScope(project,'tests',scope):recordsForCurrentScope(project,'tests'),covered=new Set(tests.map(testRequirementId).filter(Boolean)),requirementCoverage=requirements.length?requirements.filter(req=>covered.has(requirementId(req))).length/requirements.length:0,matrix=verificationMatrix(project,iterationId),regressions=records(project,'regressions').filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),executions=currentRegressionExecutions(project,iterationId),successful=new Set(executions.filter(r=>upper(recordValue(r,'PHASE'))!=='PRE_CORRECTION'&&effectiveRegressionDetermination(project,r).determination==='SATISFIED').map(r=>String(recordValue(r,'REG_ID')||r.relationships?.REG_ID||'')));return {mandatoryRequirementCount:requirements.length,requirementsWithTests:requirements.filter(req=>covered.has(requirementId(req))).length,requirementCoverage,iterationRunCount:matrix.runs.length,expectedVerificationCount:matrix.expected.length,actualVerificationPairCount:matrix.expected.filter(key=>matrix.counts.get(key)===1).length,actualVerificationTripleCount:matrix.expected.filter(key=>matrix.counts.get(key)===1).length,verificationCoverage:matrix.coverage,missingVerificationTriples:matrix.missing,duplicateVerificationTriples:matrix.duplicates,activeRegressionCount:regressions.length,successfulRegressionCount:regressions.filter(r=>successful.has(recordId(r,'regressions'))).length,regressionSuccess:regressions.length?regressions.filter(r=>successful.has(recordId(r,'regressions'))).length/regressions.length:1};}
function convergenceMetrics(project){const latest=latestIteration(project,[17]),iterationId=recordId(latest,'iterations'),coverage=coverageMetrics(project,iterationId),material=unresolvedMaterialDefects(project).filter(r=>!iterationId||!r.scope?.iterationId||r.scope.iterationId===iterationId),critical=material.filter(r=>upper(recordValue(r,'SEVERITY'))==='CRITICAL').length,major=material.filter(r=>upper(recordValue(r,'SEVERITY'))==='MAJOR').length,verification=iterationId?recordsForIteration(project,'verification',iterationId):recordsForCurrentScope(project,'verification'),mandatoryUnknowns=verification.filter(r=>effectiveDetermination('verification',r,testForResult(project,r),project)!=='SATISFIED').length+openBlockers(project).length,comparisons=iterationId?recordsForIteration(project,'comparisons',iterationId):recordsForCurrentScope(project,'comparisons'),contradictions=detectCurrentContradictions(project).filter(x=>x.severity==='RELEASE_MATERIAL').length,ambiguities=comparisons.filter(r=>adjudicationAdverse(recordValue(r,'INTERPRETATION_VARIANCE'))).length,unexplainedVariance=comparisons.filter(r=>adjudicationAdverse(recordValue(r,'CORRECTNESS_AFFECTING_VARIANCE'))&&!truth(recordValue(r,'AUTHORIZED_VARIANCE'))).length,result={iterationId,requirementCoverage:coverage.requirementCoverage,verificationCoverage:coverage.verificationCoverage,regressionSuccess:coverage.regressionSuccess,criticalDefects:critical,majorDefects:major,mandatoryUnresolvedUnknowns:mandatoryUnknowns,contradictions,ambiguities,unexplainedVariance};result.converged=Boolean(iterationId)&&result.requirementCoverage===1&&result.verificationCoverage===1&&result.regressionSuccess===1&&critical===0&&major===0&&mandatoryUnknowns===0&&contradictions===0&&ambiguities===0&&unexplainedVariance===0;return result;}
function releaseVerificationTrust(project,record){
  const runId=String(recordValue(record,'RUN_ID')||record?.relationships?.RUN_ID||'').trim(),iterationId=String(record?.scope?.iterationId||'').trim(),verifierContextId=String(recordValue(record,'VERIFIER_CONTEXT_ID')||'').trim(),ind=evaluateContextIndependence(project,{role:'VERIFICATION',iterationId,runId,verifierContextId});
  if(ind.determination==='APPLICATION_ESTABLISHED')return {determination:'APPLICATION_ESTABLISHED',reasons:[],basis:'REGISTERED_CONTEXT'};
  const rawId=String(record?.rawResponseId||'').trim(),proposalId=String(record?.sourceProposalId||'').trim(),stage=Number(record?.stage||0),accepted=acceptedChanges(project,stage).find(change=>change.rawResponseId===rawId&&(!proposalId||change.proposalId===proposalId)),receipt=safe(project.projectData.outputReceipts).find(item=>item.rawResponseId===rawId),proposal=safe(project.projectData.responseProposals).find(item=>item.proposalId===(proposalId||accepted?.proposalId)),run=records(project,'runs').find(item=>recordId(item,'runs')===runId),runContext=String(recordValue(run,'CONTEXT_ID')||run?.relationships?.CONTEXT_ID||'').trim(),generatorChanges=acceptedChanges(project,Number(run?.stage||0)).filter(change=>String(change?.scope?.runId||'')===runId),generatorRawIds=new Set(generatorChanges.map(change=>String(change.rawResponseId||'')).filter(Boolean)),scope=record?.scope||{},reasons=[];
  if(!rawId||!proposalId||!accepted||!receipt||!proposal)reasons.push('No canonical accepted external verification execution receipt resolves to this verification record.');
  if(generatorRawIds.has(rawId))reasons.push('Verifier execution receipt is the same accepted response as the generating run.');
  if(!verifierContextId||['UNKNOWN','NONE','NOT APPLICABLE','UNASSIGNED','PENDING'].includes(upper(verifierContextId)))reasons.push('Verifier context identity is not supplied.');
  if(runContext&&upper(verifierContextId)===upper(runContext))reasons.push('Verifier context identity matches the generating run context.');
  if(receipt){if(Number(receipt.stage)!==stage)reasons.push('Verifier receipt stage does not match the verification record.');if(scope.inputVersion&&safe(receipt.inputIdentities).length&&!safe(receipt.inputIdentities).includes(scope.inputVersion))reasons.push('Verifier receipt is not bound to the authorized input version.');for(const [key,receiptKey] of [['requirementsVersion','requirementsVersion'],['testSuiteVersion','testSuiteVersion'],['instructionVersion','instructionVersion']])if(scope[key]&&receipt[receiptKey]&&String(scope[key])!==String(receipt[receiptKey]))reasons.push('Verifier receipt '+receiptKey+' does not match the authorized verification scope.');}
  const op=String(accepted?.operation||proposal?.envelope?.operation||'COMPLETE');if(stage===12&&op!=='COMPLETE')reasons.push('Stage 12 verifier receipt is not the canonical COMPLETE verification response.');if([17,19].includes(stage)&&op!=='VERIFY')reasons.push('Iterative verifier receipt is not the canonical VERIFY response.');
  return {determination:reasons.length?'UNDETERMINED':'APPLICATION_ESTABLISHED',reasons,basis:reasons.length?'NONE':'CANONICAL_EXTERNAL_EXECUTION_RECEIPT',rawResponseId:rawId||null};
}
function releaseMetrics(project){const requirements=mandatoryRequirements(project,currentScope(project)),deterministic=recordsForCurrentScope(project,'deterministicResults'),meaning=recordsForCurrentScope(project,'meaningResults'),adversarial=recordsForCurrentScope(project,'adversarialResults'),inspections=recordsForCurrentScope(project,'representationInspections'),processAudits=recordsForCurrentScope(project,'processAudits'),productAudits=recordsForCurrentScope(project,'productAudits'),confirmations=recordsForCurrentScope(project,'confirmationRecords'),regressions=records(project,'regressions').filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),regExec=recordsForCurrentScope(project,'regressionExecutions'),baseline=recordsForCurrentScope(project,'baselines').at(-1),product=recordsForCurrentScope(project,'products').at(-1),blockers=openBlockers(project),defects=unresolvedMaterialDefects(project),resultCollections=[['deterministicResults',deterministic],['meaningResults',meaning],['adversarialResults',adversarial],['representationInspections',inspections]],evaluated=resultCollections.flatMap(([collection,rows])=>rows.map(r=>({collection,record:r,e:evaluateResultConsistency(collection,r,testForResult(project,r),project)}))),failed=evaluated.filter(x=>x.e.determination==='VIOLATED'),unknown=evaluated.filter(x=>x.e.determination!=='SATISFIED'&&x.e.determination!=='VIOLATED');let satisfied=0,violated=0,undetermined=0;const blockingRequirements=[],violatedRequirements=[];for(const req of requirements){const id=requirementId(req),expectedTests=recordsForCurrentScope(project,'tests').filter(test=>testRequirementId(test)===id&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY'))),results=[...deterministic,...meaning,...adversarial].filter(r=>resultRequirementId(project,r)===id&&String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'')),byTest=new Map();for(const result of results){const testId=String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||'');if(!byTest.has(testId))byTest.set(testId,[]);byTest.get(testId).push(result);}const groups=expectedTests.map(test=>(byTest.get(recordId(test,'tests'))||[]).map(r=>effectiveDetermination(resultCollections.find(([,rows])=>rows.includes(r))?.[0]||'verification',r,test,project))),hasViolation=groups.some(group=>group.includes('VIOLATED')),complete=expectedTests.length>0&&groups.every(group=>group.length===1&&group[0]==='SATISFIED');if(hasViolation){violated++;violatedRequirements.push(id);}else if(complete)satisfied++;else{undetermined++;blockingRequirements.push(id);}}const activeRegIds=new Set(regressions.map(r=>recordId(r,'regressions'))),passedReg=new Set(regExec.filter(r=>upper(recordValue(r,'PHASE'))!=='PRE_CORRECTION'&&effectiveRegressionDetermination(project,r).determination==='SATISFIED').map(r=>String(recordValue(r,'REG_ID')||r.relationships?.REG_ID||''))),missingReg=[...activeRegIds].filter(id=>!passedReg.has(id)),auditsOk=processAudits.length===1&&productAudits.length===1&&effectiveDetermination('processAudits',processAudits[0],null,project)==='SATISFIED'&&effectiveDetermination('productAudits',productAudits[0],null,project)==='SATISFIED',confirmationOk=confirmations.length===1&&effectiveDetermination('confirmationRecords',confirmations[0],null,project)==='SATISFIED',productOk=product&&effectiveDetermination('products',product,null,project)==='SATISFIED',contradictions=detectCurrentContradictions(project),insufficient=evaluated.filter(x=>!x.e.evidence?.sufficient),releaseIteration=latestIteration(project,[19,17,10]),releaseIterationId=recordId(releaseIteration,'iterations'),releaseVerificationMatrix=releaseIterationId?verificationMatrix(project,releaseIterationId):{expected:[],missing:['NO_ITERATION'],duplicates:[],verification:[]},releaseVerificationTrustChecks=releaseVerificationMatrix.verification.map(record=>({record,trust:releaseVerificationTrust(project,record)})),releaseVerificationTrustFailures=releaseVerificationTrustChecks.filter(item=>item.trust.determination!=='APPLICATION_ESTABLISHED');let determination='ACCEPTED';if(violated||failed.length||defects.some(r=>['CRITICAL','MAJOR'].includes(upper(recordValue(r,'SEVERITY')))))determination='REJECTED';else if(!requirements.length||undetermined||unknown.length||blockers.length||!baseline||!productOk||!auditsOk||!confirmationOk||missingReg.length||contradictions.length||insufficient.length||!releaseVerificationMatrix.expected.length||releaseVerificationMatrix.missing.length||releaseVerificationMatrix.duplicates.length||releaseVerificationTrustFailures.length)determination='BLOCKED';const inputReferences=[...requirements.map(requirementId),...evaluated.map(x=>recordId(x.record,x.collection)),...processAudits.map(r=>recordId(r,'processAudits')),...productAudits.map(r=>recordId(r,'productAudits')),...confirmations.map(r=>recordId(r,'confirmationRecords')),...regExec.map(r=>recordId(r,'regressionExecutions')),recordId(baseline,'baselines'),recordId(product,'products')].filter(Boolean);return {determination,mandatoryRequirementCount:requirements.length,satisfied,violated,undetermined,validatorCount:evaluated.length,failedValidatorIds:failed.map(x=>recordId(x.record,x.collection)),unknownValidatorIds:unknown.map(x=>recordId(x.record,x.collection)),criticalDefects:defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='CRITICAL').length,majorDefects:defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='MAJOR').length,blockingRequirements,violatedRequirements,blockerIds:blockers.map(r=>recordId(r,'blockers')),missingRegressionIds:missingReg,baselineId:recordId(baseline,'baselines')||null,productId:recordId(product,'products')||null,auditsOk,confirmationOk,productOk,contradictions,insufficientEvidenceResultIds:insufficient.map(x=>recordId(x.record,x.collection)).filter(Boolean),releaseVerificationTrustFailures:releaseVerificationTrustFailures.map(item=>({verificationId:recordId(item.record,'verification'),reasons:item.trust.reasons,basis:item.trust.basis})),inputReferences};}
function gate(stage,project){
  ensureShape(project);
  const reasons=[];
  if(stage>1&&project.stages[stage-1].status!=='COMPLETE')reasons.push(`Stage ${String(stage-1).padStart(2,'0')} is not complete.`);
  const questions=unresolvedHumanRequests(project,stage);
  if(questions.length)reasons.push(`${questions.length} blocking human-input request${questions.length===1?' is':'s are'} unresolved.`);
  const blockers=openBlockers(project,stage);
  if(blockers.length)reasons.push(`${blockers.length} open mandatory blocker${blockers.length===1?' affects':'s affect'} this stage.`);
  const executionFailures=safe(project.projectData.executionFailures).filter(x=>Number(x.stage)===Number(stage)&&!x.invalidatedBy&&!x.resolvedBy&&upper(x.status||'OPEN')!=='RESOLVED');
  if(executionFailures.length)reasons.push(`${executionFailures.length} accepted execution failure${executionFailures.length===1?' blocks':'s block'} this stage until a successful replacement response is accepted.`);
  const changes=acceptedChanges(project,stage);
  const hasAccepted=changes.length>0;
  const collection=name=>records(project,name,{stage});
  const all=name=>records(project,name);
  const requireAccepted=()=>{if(!hasAccepted)reasons.push('No validated agent response has been accepted for this stage.');};
  const requireCount=(name,min,message)=>{if(collection(name).length<min)reasons.push(message||`${min} ${name} record${min===1?' is':'s are'} required.`);};
  const previousReasons=reasons.length;

  switch(stage){
    case 1:{
      if(!String(project.job.EXACT_USER_OBJECTIVE_VERBATIM||'').trim())reasons.push('Verbatim User Job Input is required.');
      requireAccepted();
      const latest=changes.at(-1),confirmed=safe(project.projectData.stageConfirmations).some(item=>Number(item.stage)===1&&item.confirmed===true&&!item.invalidatedBy&&item.acceptedChangeId===latest?.changeId&&item.inputVersion===project.job.CURRENT_INPUT_VERSION);
      if(!confirmed)reasons.push('Human confirmation bound to the current accepted change and input version is required.');
      break;
    }
    case 2:{
      requireAccepted();const determination=upper(project.stages[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION);const sources=collection('sources');
      if(!sources.length){if(determination!=='NO_APPLICABLE_EXTERNAL_SOURCE')reasons.push('Either at least one legitimate independent external source or an explicit NO_APPLICABLE_EXTERNAL_SOURCE determination is required.');else{const latest=changes.at(-1),proposal=safe(project.projectData.responseProposals).find(x=>x.proposalId===latest?.proposalId);if(!safe(proposal?.evidence).length)reasons.push('NO_APPLICABLE_EXTERNAL_SOURCE requires preserved supporting evidence.');}}
      else if(determination!=='APPLICABLE_SOURCES_ESTABLISHED')reasons.push('Established source records require SOURCE_APPLICABILITY_DETERMINATION = APPLICABLE_SOURCES_ESTABLISHED.');
      for(const source of sources)reasons.push(...schema.sourceClassificationIssues(recordFields(source)).map(issue=>`${recordId(source,'sources')}: ${issue}`));
      if(collection('sourceConflicts').some(record=>['UNRESOLVED','BLOCKED','UNKNOWN','OPEN'].includes(upper(recordValue(record,'RESOLUTION_STATUS')))))reasons.push('An external-source conflict remains unresolved or blocked.');
      break;
    }
    case 3:{
      requireAccepted();const sourceIds=recordsForCurrentScope(project,'sources').map(record=>recordId(record,'sources')),noSource=upper(project.stages[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE';
      if(!sourceIds.length){if(!noSource)reasons.push('Stage 03 cannot proceed without a current Stage 02 source set or valid no-source determination.');break;}
      requireCount('research',1);const researched=new Set(recordsForCurrentScope(project,'research').map(record=>String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||''))),missing=sourceIds.filter(id=>!researched.has(id));if(missing.length)reasons.push(`Research is missing for source(s): ${missing.join(', ')}.`);
      const researchData=project.stages[3]?.agentData||{};
      const allKnownControllingSourcesExamined=sourceIds.length>0&&missing.length===0;if(!allKnownControllingSourcesExamined)reasons.push('Stage 03 has not established current research coverage for every controlling source.');
      if(!truth(researchData.SECOND_CONFLICT_AND_EXCEPTION_PASS_COMPLETED))reasons.push('Stage 03 second conflict and exception pass is incomplete.');
      if(numeric(researchData.LATEST_PASS_NUMBER)<2)reasons.push('Stage 03 requires a completed second conflict and exception pass.');
      if(!falsey(researchData.NEW_MATERIAL_CATEGORY_FOUND_IN_LATEST_PASS))reasons.push('Stage 03 is not exhausted because the latest pass still found new material.');
      break;
    }
    case 4:{
      if(project.stages?.[1]?.status!=='COMPLETE')reasons.push('Stage 04 requires exhausted Stage 01 human-authority intake.');
      if(project.stages?.[3]?.status!=='COMPLETE')reasons.push('Stage 04 requires exhausted Stage 03 source research.');
      requireAccepted();requireCount('requirements',1);
      for(const req of collection('requirements')){
        for(const name of schema.RECORD_SCHEMAS.requirements.required)if(!String(recordValue(req,name)||'').trim())reasons.push(`${recordId(req,'requirements')}: ${name} is missing.`);
        const sourceId=String(recordValue(req,'SOURCE_ID')||req.relationships?.SOURCE_ID||'').trim(),userRelationship=String(recordValue(req,'USER_INPUT_RELATIONSHIP')||'').trim();
        if(!sourceId&&!userRelationship)reasons.push(`${recordId(req,'requirements')}: requirement lacks source provenance or an explicit User Job Input relationship.`);
      }
      break;
    }
    case 5:
      requireAccepted();
      if(collection('requirementResolutions').some(record=>['OPEN','UNRESOLVED','BLOCKED','UNKNOWN'].includes(upper(recordValue(record,'STATUS')))))reasons.push('A requirement-set defect remains unresolved.');
      break;
    case 6:{
      requireAccepted();
      const metrics=coverageMetrics(project),mandatoryIds=new Set(mandatoryRequirements(project).map(requirementId)),mandatoryTests=collection('tests').filter(test=>mandatoryIds.has(testRequirementId(test)));
      if(metrics.mandatoryRequirementCount===0)reasons.push('No active mandatory requirements exist to cover.');
      if(metrics.requirementCoverage!==1)reasons.push(`Mandatory requirement-to-test coverage is ${(metrics.requirementCoverage*100).toFixed(2)}%, not 100%.`);
      const incomplete=mandatoryTests.filter(test=>!TEST_EXECUTION_ACTIONS[upper(recordValue(test,'EXECUTION_MODE'))]||!String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim()||!String(recordValue(test,'ARTIFACT_REQUIREMENTS')||'').trim());
      if(incomplete.length)reasons.push(`${incomplete.length} mandatory test definition(s) lack complete execution responsibility, capability, or artifact requirements.`);
      const unavailable=mandatoryTests.filter(test=>upper(recordValue(test,'EXECUTION_MODE'))==='UNAVAILABLE');
      if(unavailable.length)reasons.push(`${unavailable.length} mandatory test definition(s) have unavailable execution capability and remain blocked.`);
      const unsupportedApplication=mandatoryTests.filter(test=>upper(recordValue(test,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&!applicationTestSupported(test));
      if(unsupportedApplication.length)reasons.push(`${unsupportedApplication.length} mandatory test definition(s) claim APPLICATION_DETERMINISTIC without a registered application-native executor.`);
      const executionPlan=testExecutionPlan(project),missingCurrentArtifacts=executionPlan.items.filter(item=>mandatoryIds.has(item.requirementId)&&item.artifactRequired&&item.artifactIds.length>0&&!item.artifactReady);
      if(missingCurrentArtifacts.length)reasons.push(`${missingCurrentArtifacts.length} mandatory test definition(s) depend on exact artifact bytes that are missing or no longer application-verified.`);
      // Stage 06 does not require future product outputs that cannot yet exist, but an exact canonical artifact dependency must remain currently verified.
      break;
    }
    case 7:{
      requireAccepted();
      const reqs=mandatoryRequirements(project),covered=new Set(all('failureTests').map(testRequirementId));
      const missing=reqs.filter(req=>!covered.has(requirementId(req))).map(requirementId);
      if(missing.length)reasons.push(`Failure tests are missing for: ${missing.join(', ')}.`);
      for(const mutation of all('failureTests')){if(!String(recordValue(mutation,'FIXTURE')||'').trim())reasons.push(recordId(mutation,'failureTests')+': failure fixture is not identifiable.');if(!String(recordValue(mutation,'EXPECTED_REJECTION')||'').trim())reasons.push(recordId(mutation,'failureTests')+': expected rejection is missing.');if(!String(recordValue(mutation,'ACTUAL_RESULT')||'').trim())reasons.push(recordId(mutation,'failureTests')+': actual execution result is missing.');if(!String(recordValue(mutation,'EVIDENCE')||'').trim()&&!safe(mutation.evidenceRefs).length)reasons.push(recordId(mutation,'failureTests')+': execution evidence is missing.');const actual=upper(recordValue(mutation,'ACTUAL_RESULT')),expected=upper(recordValue(mutation,'EXPECTED_REJECTION')),acceptedInvalid=/\b(?:ACCEPTED|ALLOWED|PASSED|NOT REJECTED|VALID)\b/.test(actual)&&!/\b(?:REJECTED|BLOCKED|FAILED|INVALID)\b/.test(actual);if(expected.includes('REJECT')&&acceptedInvalid){const defectId=String(recordValue(mutation,'VALIDATOR_DEFECT_ID')||mutation.relationships?.VALIDATOR_DEFECT_ID||'').trim();if(!defectId)reasons.push('A known-invalid fixture was accepted without a linked validator defect.');reasons.push(recordId(mutation,'failureTests')+': known-invalid fixture was accepted; Stage 07 remains blocked until the validator is corrected and the preserved fixture is actually rejected.');}}
      break;
    }
    case 8:{requireAccepted();requireCount('instructions',1);const reqs=mandatoryRequirements(project);const traced=new Set(recordsForCurrentScope(project,'instructionTraces').map(r=>String(recordValue(r,'REQ_ID')||r.relationships?.REQ_ID||'')));const missing=reqs.filter(r=>!traced.has(requirementId(r))).map(requirementId);if(missing.length)reasons.push('Instruction traces are missing for: '+missing.join(', ')+'.');break;}
    case 9:{
      requireAccepted();requireCount('preflightRecords',1);
      if(collection('preflightRecords').some(record=>['VIOLATED','UNDETERMINED','BLOCKED','REJECTED'].includes(upper(recordValue(record,'DETERMINATION')))))reasons.push('Instruction preflight contains an unresolved material finding.');
      const preflightChange=changes.at(-1),preflightReviewer=String(preflightChange?.scope?.contextId||''),preflightIndependence=evaluateContextIndependence(project,{role:'PREFLIGHT_REVIEW',reviewerContextId:preflightReviewer});
      if(!['APPLICATION_ESTABLISHED','EXTERNALLY_SUPPORTED'].includes(preflightIndependence.determination))reasons.push('Preflight reviewer independence is not established: '+preflightIndependence.reasons.join(' '));
      break;
    }
    case 10:requireCount('iterations',1);requireCount('candidateFreezes',1);break;
    case 11:{requireAccepted();const iteration=latestIteration(project,[10,11]);const ev=evaluateIteration(project,recordId(iteration,'iterations'),'INITIAL');if(!ev.complete)reasons.push(...ev.reasons);break;}
    case 12:{requireAccepted();const iteration=latestIteration(project,[10]);const m=verificationMatrix(project,recordId(iteration,'iterations'));if(m.runs.length!==10)reasons.push('The current verification batch does not contain exactly ten runs.');if(m.expected.length===0||m.coverage!==1)reasons.push('REQ × RUN × TEST verification coverage is not 100%.');if(m.duplicates.length)reasons.push('Duplicate verification triples exist.');if(m.invalid.length)reasons.push('A verification triple lacks independently established/supported verification or sufficient evidence.');break;}
    case 13:{
      requireAccepted();const iteration=latestIteration(project,[10]),iterationId=recordId(iteration,'iterations'),scope=scopeForIteration(project,iterationId),reqs=mandatoryRequirements(project,scope),compared=new Set(recordsForIteration(project,'comparisons',iterationId).map(record=>String(recordValue(record,'REQ_ID')||record.relationships?.REQ_ID||'')));
      const missing=reqs.filter(req=>!compared.has(requirementId(req))).map(requirementId);
      if(missing.length)reasons.push(`Cross-run comparison is missing for: ${missing.join(', ')}.`);
      break;
    }
    case 14:{
      requireAccepted();const defects=confirmedDefects(project),analysed=new Set(all('rootCauses').map(record=>String(recordValue(record,'DEFECT_ID')||record.relationships?.DEFECT_ID||'')));
      const missing=defects.filter(defect=>!analysed.has(recordId(defect,'defects'))).map(defect=>recordId(defect,'defects'));
      if(missing.length)reasons.push(`Root-cause analysis is missing for: ${missing.join(', ')}.`);
      break;
    }
    case 15:{requireAccepted();const defects=confirmedDefects(project),regs=records(project,'regressions'),covered=new Map(regs.map(r=>[String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||''),r]));const missing=defects.filter(d=>!covered.has(recordId(d,'defects'))).map(d=>recordId(d,'defects'));if(missing.length)reasons.push('Permanent regression definitions are missing for: '+missing.join(', ')+'.');const executions=records(project,'regressionExecutions');for(const reg of regs){const id=recordId(reg,'regressions');if(!executions.some(e=>String(recordValue(e,'REG_ID')||e.relationships?.REG_ID||'')===id&&upper(recordValue(e,'PHASE'))==='PRE_CORRECTION'&&effectiveRegressionDetermination(project,e).determination==='SATISFIED'))reasons.push('Regression '+id+' lacks an actual sufficiently evidenced pre-correction failing execution.');}break;}
    case 16:requireAccepted();if(confirmedDefects(project).length&&!collection('changes').length)reasons.push('A responsible-layer changeset or blocker is required for confirmed defects.');break;
    case 17:{requireAccepted();const iteration=latestIteration(project,[17]);const ev=evaluateIteration(project,recordId(iteration,'iterations'),'CORRECTED');if(!ev.complete)reasons.push(...ev.reasons);break;}
    case 18:{
      requireAccepted();const metrics=convergenceMetrics(project);
      if(!metrics.converged)reasons.push('All convergence conditions are not simultaneously satisfied.');
      break;
    }
    case 19:{requireAccepted();const iteration=latestIteration(project,[19]);const ev=evaluateIteration(project,recordId(iteration,'iterations'),'UNCHANGED_CONFIRMATION');if(!ev.complete)reasons.push(...ev.reasons);requireCount('confirmationRecords',1);if(collection('confirmationRecords').some(r=>upper(recordValue(r,'DETERMINATION'))!=='SATISFIED'))reasons.push('Unchanged confirmation is not affirmatively satisfied.');break;}
    case 20:{requireAccepted();const confirmations=recordsForCurrentScope(project,'confirmationRecords').filter(record=>upper(recordValue(record,'DETERMINATION'))==='SATISFIED'),baselines=recordsForCurrentScope(project,'baselines'),iteration=latestIteration(project,[19]),iterationId=recordId(iteration,'iterations'),candidateId=iterationCandidateId(project,iterationId);if(confirmations.length!==1)reasons.push('Exactly one current successful unchanged confirmation is required.');if(baselines.length!==1)reasons.push('Exactly one current production baseline is required.');for(const baseline of baselines){const approved=recordValue(baseline,'APPROVED_VERSIONS')||{};if(upper(recordValue(baseline,'HUMAN_AUTHORIZATION'))!=='AUTHORIZED'||String(recordValue(baseline,'SUPPORTING_CONFIRMATION_ID')||baseline.relationships?.SUPPORTING_CONFIRMATION_ID||'')!==recordId(confirmations[0],'confirmationRecords'))reasons.push('The current baseline is not bound to the current authorized unchanged confirmation.');if(String(approved.iterationId||'')!==iterationId||String(approved.candidateId||'')!==candidateId||!candidateComponentIdentity(project,candidateId))reasons.push('The current baseline does not preserve the exact unchanged-confirmation iteration and frozen candidate identity.');}break;}
    case 21:{requireAccepted();const products=recordsForCurrentScope(project,'products'),product=products.at(-1),productId=recordId(product,'products'),baselines=recordsForCurrentScope(project,'baselines'),baseline=baselines.at(-1),artifacts=recordsForCurrentScope(project,'artifacts').filter(a=>productId&&String(a.scope?.productId||'')===productId),verified=artifacts.filter(a=>upper(recordValue(a,'AVAILABILITY'))==='BYTES_PERSISTED_AND_VERIFIED'&&/^[a-f0-9]{64}$/i.test(String(recordValue(a,'SHA256')||''))&&Number.isInteger(Number(recordValue(a,'BYTE_SIZE')))&&Number(recordValue(a,'BYTE_SIZE'))>=0&&String(recordValue(a,'STORAGE_REFERENCE')||'').startsWith('indexeddb:')),inventory=safe(recordValue(product,'GENERATED_ARTIFACT_INVENTORY')).map(String).sort(),verifiedIds=verified.map(a=>recordId(a,'artifacts')).sort(),contextId=String(recordValue(product,'PRODUCTION_CONTEXT_ID')||product?.relationships?.PRODUCTION_CONTEXT_ID||''),context=records(project,'freshContexts').find(r=>recordId(r,'freshContexts')===contextId&&isActiveRecord(r));if(products.length!==1)reasons.push('Exactly one current product execution identity is required.');if(baselines.length!==1||String(recordValue(product,'BASELINE_ID')||product?.relationships?.BASELINE_ID||'')!==recordId(baseline,'baselines'))reasons.push('The current product execution is not bound to exactly one current approved baseline.');if(!context||Number(context.stage)!==21)reasons.push('The product execution does not use a dedicated current Stage 21 production context.');if(upper(recordValue(product,'STATUS'))!=='COMPLETED'||product?.completionState!=='COMPLETED')reasons.push('The reserved product execution has not been completed by an accepted Stage 21 response.');if(!verified.length||verified.length!==artifacts.length)reasons.push('Every current finished-product artifact must have actual IndexedDB bytes, byte size, and application-computed SHA-256 before Stage 21 can complete.');if(hash.sha256Value(inventory)!==hash.sha256Value(verifiedIds))reasons.push('The application-generated product artifact inventory does not exactly match the verified current product bytes.');break;}
    case 22:{
      const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),expected=recordsForCurrentScope(project,'tests').filter(test=>mandatoryIds.has(testRequirementId(test))&&upper(recordValue(test,'TEST_TYPE'))==='DETERMINISTIC'&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY')));
      if(expected.some(test=>upper(recordValue(test,'EXECUTION_MODE'))!=='APPLICATION_DETERMINISTIC'))requireAccepted();
      const results=recordsForCurrentScope(project,'deterministicResults'),counts=new Map();for(const result of results){const id=String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||'');counts.set(id,(counts.get(id)||0)+1);}const expectedIds=new Set(expected.map(test=>recordId(test,'tests'))),missing=[...expectedIds].filter(id=>counts.get(id)!==1),unexpected=results.filter(result=>!expectedIds.has(String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||'')));if(missing.length)reasons.push('Exactly one deterministic result is required for each applicable deterministic test: '+missing.join(', ')+'.');if(unexpected.length)reasons.push('Unexpected deterministic results exist outside the current applicable deterministic-test set.');if(results.some(record=>effectiveDetermination('deterministicResults',record,expected.find(t=>recordId(t,'tests')===String(recordValue(record,'TEST_ID')||record.relationships?.TEST_ID||'')),project)!=='SATISFIED'))reasons.push('A deterministic finished-product verification is violated or undetermined.');for(const result of results){const test=expected.find(t=>recordId(t,'tests')===String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''));if(test&&!evaluateEvidenceSufficiency(project,{test,result}).sufficient)reasons.push('Deterministic result '+recordId(result,'deterministicResults')+' has evidence insufficient for its proposition.');}break;
    }
    case 23:{
      requireAccepted();const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),expected=recordsForCurrentScope(project,'tests').filter(test=>mandatoryIds.has(testRequirementId(test))&&upper(recordValue(test,'TEST_TYPE'))==='MEANING'&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY'))),expectedById=new Map(expected.map(test=>[recordId(test,'tests'),test])),expectedIds=new Set(expectedById.keys()),results=recordsForCurrentScope(project,'meaningResults'),counts=new Map();for(const result of results){const id=String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||'');counts.set(id,(counts.get(id)||0)+1);}const missing=[...expectedIds].filter(id=>counts.get(id)!==1),unexpected=results.filter(result=>!expectedIds.has(String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''))),mismatched=results.filter(result=>{const test=expectedById.get(String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''));return test&&String(recordValue(result,'REQ_ID')||result.relationships?.REQ_ID||'')!==testRequirementId(test);});if(missing.length)reasons.push('Exactly one meaning review is required for every current applicable meaning test: '+missing.join(', ')+'.');if(unexpected.length)reasons.push('Unexpected meaning-review records exist outside the current meaning-test set.');if(mismatched.length)reasons.push('A meaning-review requirement identity does not match its controlling Stage 6 test.');if(results.some(record=>upper(recordValue(record,'DETERMINATION'))!=='SATISFIED'))reasons.push('A meaning verification is violated or undetermined.');for(const result of results){const test=expectedById.get(String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''));if(test&&!evaluateEvidenceSufficiency(project,{test,result}).sufficient)reasons.push('Meaning review '+recordId(result,'meaningResults')+' has evidence insufficient for the proposition.');}const meaningChange=changes.at(-1),meaningReviewer=String(meaningChange?.scope?.contextId||''),meaningProduct=recordsForCurrentScope(project,'products').at(-1),meaningProducer=String(recordValue(meaningProduct,'PRODUCTION_CONTEXT_ID')||meaningProduct?.relationships?.PRODUCTION_CONTEXT_ID||'');const meaningIndependence=evaluateContextIndependence(project,{role:'MEANING_REVIEW',reviewerContextId:meaningReviewer,productionContextId:meaningProducer});if(!['APPLICATION_ESTABLISHED','EXTERNALLY_SUPPORTED'].includes(meaningIndependence.determination))reasons.push('Meaning reviewer independence is not established: '+meaningIndependence.reasons.join(' '));break;
    }
    case 24:{
      requireAccepted();const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),expectedTests=recordsForCurrentScope(project,'tests').filter(test=>mandatoryIds.has(testRequirementId(test))&&upper(recordValue(test,'TEST_TYPE'))==='ADVERSARIAL'&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY'))),expectedTestIds=new Set(expectedTests.map(test=>recordId(test,'tests'))),expectedRegs=records(project,'regressions').filter(reg=>upper(recordValue(reg,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'&&upper(recordValue(reg,'APPLICABILITY')||'APPLICABLE')!=='NOT APPLICABLE'),expectedRegIds=new Set(expectedRegs.map(reg=>recordId(reg,'regressions'))),results=recordsForCurrentScope(project,'adversarialResults'),testCounts=new Map(),regCounts=new Map();for(const result of results){const testId=String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''),regId=String(recordValue(result,'REG_ID')||result.relationships?.REG_ID||'');if(testId)testCounts.set(testId,(testCounts.get(testId)||0)+1);if(regId)regCounts.set(regId,(regCounts.get(regId)||0)+1);}const missingTests=[...expectedTestIds].filter(id=>testCounts.get(id)!==1),missingRegs=[...expectedRegIds].filter(id=>regCounts.get(id)!==1),orphan=results.filter(result=>!String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||'')&&!String(recordValue(result,'REG_ID')||result.relationships?.REG_ID||'')),unexpected=results.filter(result=>{const testId=String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''),regId=String(recordValue(result,'REG_ID')||result.relationships?.REG_ID||'');return (testId&&!expectedTestIds.has(testId))||(regId&&!expectedRegIds.has(regId));});if(!expectedTestIds.size)reasons.push('At least one current mandatory adversarial verification test must be defined at Stage 6.');if(missingTests.length)reasons.push('Exactly one adversarial result is required for every current applicable adversarial test: '+missingTests.join(', ')+'.');if(missingRegs.length)reasons.push('Exactly one adversarial regression challenge is required for every active applicable permanent regression: '+missingRegs.join(', ')+'.');if(orphan.length)reasons.push('Every adversarial result must identify the Stage 6 adversarial test or permanent regression it challenges.');if(unexpected.length)reasons.push('Unexpected adversarial results exist outside the current required adversarial/regression set.');if(results.some(record=>upper(recordValue(record,'DETERMINATION'))!=='SATISFIED'))reasons.push('Adversarial verification found a violated, failed, or undetermined result.');for(const result of results){const testId=String(recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||''),test=expectedTests.find(t=>recordId(t,'tests')===testId);if(!evaluateEvidenceSufficiency(project,{test,result}).sufficient)reasons.push('Adversarial result '+recordId(result,'adversarialResults')+' has insufficient evidence.');}const adversarialChange=changes.at(-1),adversarialReviewer=String(adversarialChange?.scope?.contextId||''),adversarialProduct=recordsForCurrentScope(project,'products').at(-1),adversarialProducer=String(recordValue(adversarialProduct,'PRODUCTION_CONTEXT_ID')||adversarialProduct?.relationships?.PRODUCTION_CONTEXT_ID||'');const adversarialIndependence=evaluateContextIndependence(project,{role:'ADVERSARIAL_REVIEW',reviewerContextId:adversarialReviewer,productionContextId:adversarialProducer});if(!['APPLICATION_ESTABLISHED','EXTERNALLY_SUPPORTED'].includes(adversarialIndependence.determination))reasons.push('Adversarial reviewer independence is not established: '+adversarialIndependence.reasons.join(' '));break;
    }
    case 25:{requireAccepted();const productId=currentScope(project).productId,artifacts=recordsForCurrentScope(project,'artifacts').filter(a=>productId&&String(a.scope?.productId||'')===String(productId)),inspections=recordsForCurrentScope(project,'representationInspections'),counts=new Map();for(const inspection of inspections){const id=String(recordValue(inspection,'ARTIFACT_ID')||inspection.relationships?.ARTIFACT_ID||'');counts.set(id,(counts.get(id)||0)+1);}const expectedIds=new Set(artifacts.map(a=>recordId(a,'artifacts'))),missing=[...expectedIds].filter(id=>counts.get(id)!==1),unexpected=inspections.filter(i=>!expectedIds.has(String(recordValue(i,'ARTIFACT_ID')||i.relationships?.ARTIFACT_ID||'')));if(!expectedIds.size)reasons.push('No current product or delivery artifacts exist for representation inspection.');if(missing.length)reasons.push('Exactly one current representation inspection is required for every current product artifact: '+missing.join(', ')+'.');if(unexpected.length)reasons.push('Unexpected representation inspections exist outside the current product artifact set.');if(inspections.some(record=>upper(recordValue(record,'DETERMINATION'))!=='SATISFIED'))reasons.push('A final representation inspection is violated or undetermined.');for(const inspection of inspections)if(!evaluateEvidenceSufficiency(project,{result:inspection}).sufficient)reasons.push('Representation inspection '+recordId(inspection,'representationInspections')+' has insufficient evidence.');break;
    }
    case 26:
      requireAccepted();requireCount('processAudits',1);requireCount('productAudits',1);
      if(collection('processAudits').some(record=>upper(recordValue(record,'PROCESS_DETERMINATION'))!=='SATISFIED'))reasons.push('Process audit is not SATISFIED.');
      if(collection('productAudits').some(record=>upper(recordValue(record,'PRODUCT_DETERMINATION'))!=='SATISFIED'))reasons.push('Product audit is not SATISFIED.');for(const audit of [...collection('processAudits'),...collection('productAudits')])if(!evaluateEvidenceSufficiency(project,{result:audit}).sufficient)reasons.push('Stage 26 audit '+String(audit.id||audit.recordId||'UNKNOWN')+' has insufficient evidence.');const stage26Contradictions=detectCurrentContradictions(project);if(stage26Contradictions.length)reasons.push('Current process/product evidence contains unresolved contradictions: '+stage26Contradictions.map(x=>x.type+':'+x.key).join(', ')+'.');
      break;
    case 27:{requireAccepted();const r=recordsForCurrentScope(project,'releaseRecords').at(-1);if(!r)reasons.push('The application has not recorded a current release determination.');else if(!['ACCEPTED','REJECTED','BLOCKED'].includes(upper(recordValue(r,'DETERMINATION'))))reasons.push('Current release determination is invalid.');break;}
    case 28:{
      const release=all('releaseRecords').at(-1);
      if(upper(recordValue(release,'DETERMINATION'))!=='ACCEPTED')reasons.push('Stage 27 must be ACCEPTED before artifact identity verification.');
      if(!all('artifactIdentities').length)reasons.push('No audited-versus-delivery artifact identity comparison exists.');
      if(all('artifactIdentities').some(record=>!truth(recordValue(record,'EXACT_HASH_MATCH'))||!truth(recordValue(record,'EXACT_SIZE_MATCH'))||upper(recordValue(record,'AUTHORIZATION'))!=='AUTHORIZED'))reasons.push('At least one release artifact does not exactly match the audited artifact.');
      break;
    }
    case 29:{
      const reqs=mandatoryRequirements(project),chains=all('evidenceChains'),byReq=new Map(chains.map(record=>[String(recordValue(record,'REQ_ID')||record.relationships?.REQ_ID||''),record]));
      const incomplete=reqs.filter(req=>upper(recordValue(byReq.get(requirementId(req)),'STATUS'))!=='COMPLETE').map(requirementId);
      if(incomplete.length)reasons.push(`Complete evidence chains are missing for: ${incomplete.join(', ')}.`);
      break;
    }
    case 30:{requireAccepted();const defects=confirmedDefects(project,{allScopes:true}),regs=records(project,'regressions'),covered=new Set(regs.map(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||''))),executions=recordsForCurrentScope(project,'regressionExecutions');const missing=defects.filter(d=>!covered.has(recordId(d,'defects'))).map(d=>recordId(d,'defects'));if(missing.length)reasons.push('Permanent regression information is missing for: '+missing.join(', ')+'.');for(const reg of regs.filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED')){const id=recordId(reg,'regressions');const latest=executions.filter(e=>String(recordValue(e,'REG_ID')||e.relationships?.REG_ID||'')===id).at(-1);if(!latest||effectiveRegressionDetermination(project,latest).determination!=='SATISFIED')reasons.push('Latest applicable regression execution is not successful for '+id+'.');}break;}
  }
  const blocked=questions.length>0||blockers.length>0||executionFailures.length>0;
  return {stage,complete:reasons.length===0,blocked,reasons,acceptedResponseCount:changes.length,checkedAt:now(),priorReasonCount:previousReasons};
}


const INTAKE_ACCOUNTING_DISPOSITIONS=Object.freeze(['incorporated into the job definition','retained as context','unresolved human-only','later-resolvable','inapplicable with reason']);
const INTAKE_STATEMENT_CLASSES=Object.freeze(['FACT','REQUIREMENT','CONSTRAINT','DECISION','PROHIBITION','REQUESTED_OUTPUT','ACCEPTANCE_CONDITION','MATERIAL_REFERENCE','UNRESOLVED_HUMAN_ONLY','CONTEXT']);
const OBLIGATION_DISPOSITIONS=Object.freeze(['retained nonnormative context','inapplicable','blocked']);
function enumerateInputLeaves(value,sourceLocation,kind,label,units,{inputVersion,extra={}}={}){
  if(value===undefined||value===null)return;
  if(typeof value==='string'){
    const normalized=value.replace(/\r\n/g,'\n').replace(/\r/g,'\n'),lines=normalized.split('\n');let emitted=0;
    lines.forEach((rawLine,index)=>{if(!rawLine.trim())return;const location=lines.length>1?`${sourceLocation}#line=${index+1}`:sourceLocation,rawValueSha256=hash.sha256Value(rawLine),unitId=`INPUT-UNIT-${hash.sha256Value({inputVersion,kind,sourceLocation:location,rawValueSha256}).slice(0,20).toUpperCase()}`;units.push({unitId,kind,sourceLocation:location,label:lines.length>1?`${label} line ${index+1}`:label,rawValueSha256,rawValueText:rawLine,...clone(extra)});emitted++;});
    if(!emitted&&value.length){const rawValueSha256=hash.sha256Value(value),unitId=`INPUT-UNIT-${hash.sha256Value({inputVersion,kind,sourceLocation,rawValueSha256}).slice(0,20).toUpperCase()}`;units.push({unitId,kind,sourceLocation,label,rawValueSha256,rawValueText:value,...clone(extra)});}
    return;
  }
  if(Array.isArray(value)){value.forEach((item,index)=>enumerateInputLeaves(item,`${sourceLocation}[${index}]`,kind,`${label}[${index}]`,units,{inputVersion,extra}));return;}
  if(typeof value==='object'){for(const key of Object.keys(value).sort())enumerateInputLeaves(value[key],`${sourceLocation}.${key}`,kind,`${label}.${key}`,units,{inputVersion,extra});return;}
  enumerateInputLeaves(String(value),sourceLocation,kind,label,units,{inputVersion,extra});
}
function intakeCoverageManifest(project){
  ensureShape(project);const inputVersion=project.job.CURRENT_INPUT_VERSION||'UNKNOWN',units=[];
  for(const [name,definition] of Object.entries(schema.JOB_FIELDS||{})){if(!['HUMAN','HUMAN_DECISION'].includes(definition?.producer))continue;enumerateInputLeaves(project.job[name],`job.${name}`,definition.producer==='HUMAN'?'JOB_FIELD':'HUMAN_DECISION',name,units,{inputVersion});}
  const mirroredUserEnteredKeys=new Set([
    'objective','suppliedMaterials','requiredOutputFormat','deadlineOrTemporalScope','desiredSourceCount','knownAuthorities','availableTools','prohibitedActions','explicitRequirements','suppliedArtifactFiles','suppliedArtifactText',
    ...Object.entries(schema.JOB_FIELDS||{}).filter(([,definition])=>['HUMAN','HUMAN_DECISION'].includes(definition?.producer)).map(([name])=>name)
  ]);
  const nonMirroredUserEntered=Object.fromEntries(Object.entries(project.projectData.userEntered||{}).filter(([key])=>!mirroredUserEnteredKeys.has(key)));
  if(Object.keys(nonMirroredUserEntered).length)enumerateInputLeaves(nonMirroredUserEntered,'projectData.userEntered','ORIGINAL_USER_INPUT','Original user input',units,{inputVersion});
  for(const answer of safe(project.projectData.humanInputAnswers).filter(item=>!item.invalidatedBy)){const answerId=String(answer.answerId||answer.requestId||'UNKNOWN');enumerateInputLeaves(answer.answer,`projectData.humanInputAnswers.${answerId}.answer`,'HUMAN_ANSWER',`Human answer ${answerId}`,units,{inputVersion,extra:{answerId}});}
  const stageOneSelected=new Set(safe(project.stages?.[1]?.authorizedFiles).map(item=>String(item?.artifactId||item?.id||'')).filter(Boolean));
  const stageOneArtifacts=recordsForCurrentScope(project,'artifacts').filter(record=>Number(record?.stage||recordValue(record,'STAGE')||record?.lineage?.stage||0)===1||stageOneSelected.has(recordId(record,'artifacts')));
  for(const artifact of stageOneArtifacts){const artifactId=recordId(artifact,'artifacts'),filename=String(recordValue(artifact,'FILENAME')||artifactId),sha256=String(recordValue(artifact,'SHA256')||''),byteSize=Number(recordValue(artifact,'BYTE_SIZE')||0),availability=String(recordValue(artifact,'AVAILABILITY')||'UNKNOWN'),sourceLocation=`artifact.${artifactId}`,rawValueText=JSON.stringify({artifactId,filename,sha256,byteSize,availability}),rawValueSha256=hash.sha256Value(rawValueText),unitId=`INPUT-UNIT-${hash.sha256Value({inputVersion,kind:'SUPPLIED_MATERIAL',sourceLocation,rawValueSha256}).slice(0,20).toUpperCase()}`;units.push({unitId,kind:'SUPPLIED_MATERIAL',sourceLocation,label:`Supplied material ${filename}`,rawValueSha256,rawValueText,artifactId,filename,artifactSha256:sha256,byteSize,availability});}
  const seen=new Set(),deduped=[];for(const unit of units){const key=`${unit.sourceLocation}|${unit.rawValueSha256}`;if(seen.has(key))continue;seen.add(key);deduped.push(unit);}const base={schema:'closed-loop-intake-manifest/1',inputVersion,units:deduped,unitCount:deduped.length};return {...base,manifestSha256:hash.sha256Value(base)};
}
function parseCapturedInputSet(stageOrProject){const stageOne=stageOrProject?.stages?.[1]||stageOrProject||{},raw=stageOne?.agentData?.INPUT_SET_CONTENTS??stageOne?.acceptedData?.INPUT_SET_CONTENTS??'';if(!String(raw||'').trim())return {format:'EMPTY',units:[],raw:''};try{const parsed=JSON.parse(String(raw));if(parsed&&typeof parsed==='object'&&Array.isArray(parsed.units))return {format:'STRUCTURED',...parsed,raw:String(raw)};}catch{}return {format:'LEGACY_TEXT',units:[],raw:String(raw)};}
function evaluateIntakeAccounting(project,{capture}={}){
  ensureShape(project);const manifest=intakeCoverageManifest(project),parsed=capture===undefined?parseCapturedInputSet(project):(()=>{try{const value=typeof capture==='string'?JSON.parse(capture):capture;return value&&typeof value==='object'&&Array.isArray(value.units)?{format:'STRUCTURED',...value}:{format:'INVALID',units:[]};}catch{return {format:'INVALID',units:[]};}})(),reasons=[];
  if(parsed.format!=='STRUCTURED')reasons.push('Stage 01 INPUT_SET_CONTENTS must be the structured current intake accounting capture.');
  if(String(parsed.inputVersion||'')!==String(manifest.inputVersion))reasons.push('Stage 01 intake accounting is not bound to the current input version.');
  if(String(parsed.manifestSha256||'')!==String(manifest.manifestSha256))reasons.push('Stage 01 intake accounting is not bound to the current application intake manifest.');
  const expected=new Map(manifest.units.map(unit=>[unit.unitId,unit])),counts=new Map(),accounted=new Set();
  for(const [index,unit] of safe(parsed.units).entries()){
    const id=String(unit?.sourceUnitId||'').trim();if(!id){reasons.push(`Stage 01 intake unit ${index+1} has no sourceUnitId.`);continue;}counts.set(id,(counts.get(id)||0)+1);const source=expected.get(id);if(!source){reasons.push(`Stage 01 intake accounting contains unknown unit ${id}.`);continue;}
    const hashMatches=String(unit?.sourceRawValueSha256||'')===String(source.rawValueSha256);if(!hashMatches)reasons.push(`Stage 01 intake unit ${id} is not bound to the exact source raw-value hash.`);
    const disposition=String(unit?.disposition||'').trim().toLowerCase();if(!INTAKE_ACCOUNTING_DISPOSITIONS.includes(disposition))reasons.push(`Stage 01 intake unit ${id} has an invalid disposition.`);if(disposition==='inapplicable with reason'&&!String(unit?.reason||'').trim())reasons.push(`Stage 01 intake unit ${id} requires an inapplicability reason.`);
    const statements=safe(unit?.extractedStatements);if(disposition!=='inapplicable with reason'&&!statements.length)reasons.push(`Stage 01 intake unit ${id} contains no extracted human-authority statement.`);const statementKeys=new Set();let statementsValid=true;
    for(const statement of statements){const key=String(statement?.statementKey||'').trim(),text=String(statement?.text||'').trim(),classification=String(statement?.statementClass||'').trim().toUpperCase();if(!key||statementKeys.has(key)){reasons.push(`Stage 01 intake unit ${id} has a missing or duplicate statementKey.`);statementsValid=false;}statementKeys.add(key);if(!text){reasons.push(`Stage 01 intake unit ${id} contains an empty extracted statement.`);statementsValid=false;}if(!INTAKE_STATEMENT_CLASSES.includes(classification)){reasons.push(`Stage 01 intake unit ${id} has an invalid statementClass.`);statementsValid=false;}}
    if(hashMatches&&INTAKE_ACCOUNTING_DISPOSITIONS.includes(disposition)&&statementsValid&&(disposition==='inapplicable with reason'?Boolean(String(unit?.reason||'').trim()):statements.length>0))accounted.add(id);
  }
  for(const [id,count] of counts)if(count!==1)reasons.push(`Stage 01 intake unit ${id} is accounted ${count} times; exactly one accounting entry is required.`);for(const id of expected.keys())if(!counts.has(id))reasons.push(`Stage 01 intake accounting omitted controlled input unit ${id}.`);const coverage=manifest.unitCount?accounted.size/manifest.unitCount:1;return {complete:reasons.length===0,coverage,reasons,manifest,parsed,expectedUnitCount:manifest.unitCount,accountedUnitCount:accounted.size};
}
function semanticLeaves(value,emit,path='value'){if(value===undefined||value===null)return;if(typeof value==='string'){const lines=value.replace(/\r\n/g,'\n').replace(/\r/g,'\n').split('\n');for(const [index,line] of lines.entries())if(line.trim())emit(line,lines.length>1?`${path}#line=${index+1}`:path);return;}if(Array.isArray(value)){value.forEach((item,index)=>semanticLeaves(item,emit,`${path}[${index}]`));return;}if(typeof value==='object'){for(const key of Object.keys(value).sort())semanticLeaves(value[key],emit,`${path}.${key}`);return;}emit(String(value),path);}
function obligationManifest(project){
  ensureShape(project);const inputVersion=project.job.CURRENT_INPUT_VERSION||'UNKNOWN',sourceSetVersion=project.job.CURRENT_SOURCE_SET_VERSION||null,items=[];const add=(text,origin,provenance,sourceIdentity=null,evidenceRefs=[],classification='UNCLASSIFIED')=>{const value=String(text??'');if(!value.trim())return;const obligationId=`OBLIGATION-${hash.sha256Value({origin,provenance,text:value}).slice(0,20).toUpperCase()}`;items.push({obligationId,text:value,origin,classification,provenance:clone(provenance),sourceIdentity:sourceIdentity||null,evidenceRefs:[...new Set(safe(evidenceRefs).map(String))]});};
  const directHumanIntake=intakeCoverageManifest(project);for(const unit of safe(directHumanIntake.units)){if(String(unit?.kind||'').toUpperCase()==='SUPPLIED_MATERIAL')continue;add(unit?.rawValueText,'CURRENT_USER_JOB_INPUT',{sourceUnitId:String(unit?.unitId||'UNKNOWN'),sourceLocation:String(unit?.sourceLocation||'UNKNOWN'),rawValueSha256:String(unit?.rawValueSha256||''),inputVersion},null,[],'HUMAN_AUTHORITY_RAW');}
  const capture=parseCapturedInputSet(project);for(const unit of safe(capture.units))for(const [index,statement] of safe(unit?.extractedStatements).entries())add(statement?.text,'STAGE01_CAPTURED_HUMAN_AUTHORITY',{sourceUnitId:String(unit?.sourceUnitId||'UNKNOWN'),statementKey:String(statement?.statementKey||index+1),inputVersion},null,[],String(statement?.statementClass||'UNCLASSIFIED').toUpperCase());
  const stageOne=project.stages?.[1]?.agentData||project.stages?.[1]?.acceptedData||{};for(const field of ['EXACT_DELIVERABLE_REQUESTED','ASSUMPTIONS','UNKNOWN_INFORMATION'])semanticLeaves(stageOne[field],(text,location)=>add(text,'STAGE01_ACCEPTED_JOB_DEFINITION',{field,location,inputVersion}),null,[],'STAGE01_JOB_DEFINITION');
  const stageThree=project.stages?.[3]?.agentData||project.stages?.[3]?.acceptedData||{};for(const field of ['EXCEPTIONS_AND_EDGE_CONDITIONS','CONFLICTING_OR_INVALIDATING_MATERIAL','RESEARCH_GAPS_AND_BLOCKERS'])semanticLeaves(stageThree[field],(text,location)=>add(text,'STAGE03_ACCEPTED_RESEARCH_SUMMARY',{field,location,sourceSetVersion}),null,[],'STAGE03_RESEARCH_SUMMARY');
  const researchFields=['MANDATORY_STATEMENTS','RECOMMENDATIONS','OPTIONAL_PRACTICES','EXAMPLES','EXPLANATORY_MATERIAL','PROHIBITIONS','EXCEPTIONS','DEPENDENCIES','APPLICABILITY_FACTS','RESTRICTIONS','INVALIDATING_MATERIAL'];for(const record of recordsForCurrentScope(project,'research')){const researchId=recordId(record,'research'),sourceId=String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||'')||null;for(const field of researchFields)semanticLeaves(recordValue(record,field),(text,location)=>add(text,'STAGE03_SOURCE_RESEARCH',{researchId,field,location,sourceId,sourceSetVersion},sourceId,record.evidenceRefs,field));}
  for(const record of recordsForCurrentScope(project,'candidateRequirements')){const candidateRequirementId=recordId(record,'candidateRequirements'),sourceId=String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||'')||null;semanticLeaves(recordValue(record,'CANDIDATE_OBLIGATION'),(text,location)=>add(text,'STAGE03_CANDIDATE_REQUIREMENT',{candidateRequirementId,location,sourceId,sourceSetVersion},sourceId,record.evidenceRefs,'CANDIDATE_OBLIGATION'));}
  const unique=[],seen=new Set();for(const item of items){if(seen.has(item.obligationId))continue;seen.add(item.obligationId);unique.push(item);}const sourceContext=recordsForCurrentScope(project,'sources').map(record=>({sourceId:recordId(record,'sources'),contentSha256:record.contentSha256||record.sha256||hash.sha256Value(recordFields(record)),fields:recordFields(record)})),evidenceContext=recordsForCurrentScope(project,'evidenceRecords').filter(record=>[2,3].includes(Number(record.stage))).map(record=>({evidenceId:recordId(record,'evidenceRecords'),sourceId:String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||''),sha256:record.sha256||recordValue(record,'SHA256')||hash.sha256Value(recordFields(record))}));const base={schema:'closed-loop-obligation-manifest/1',inputVersion,sourceSetVersion,items:unique,obligationCount:unique.length,sourceContext,evidenceContext};return {...base,manifestSha256:hash.sha256Value(base)};
}
function obligationIdsFromRelationship(value){return [...new Set(String(value??'').match(/OBLIGATION-[A-F0-9]{20}/g)||[])];}
function evaluateObligationAccounting(project,{envelope}={}){
  ensureShape(project);const manifest=obligationManifest(project),expected=new Map(manifest.items.map(item=>[item.obligationId,item])),mapped=new Map(),dispositions=new Map(),reasons=[];const requirements=envelope?safe(envelope.records?.requirements):recordsForCurrentScope(project,'requirements'),evidence=envelope?safe(envelope.evidence):recordsForCurrentScope(project,'evidenceRecords').filter(record=>Number(record.stage)===4);
  for(const requirement of requirements){const fields=envelope?(requirement?.fields||{}):recordFields(requirement),refs=obligationIdsFromRelationship(fields.USER_INPUT_RELATIONSHIP);for(const id of refs){if(!expected.has(id))reasons.push(`Stage 04 requirement accounting references unknown obligation ${id}.`);else{const list=mapped.get(id)||[];list.push(envelope?(requirement.tempKey||requirement.targetId||'response-requirement'):recordId(requirement,'requirements'));mapped.set(id,list);}}}
  for(const item of evidence){const kind=String(envelope?item?.kind:recordValue(item,'KIND')||'').trim().toUpperCase();if(kind!=='OBLIGATION_DISPOSITION')continue;const raw=String(envelope?item?.content:recordValue(item,'CONTENT')||'');let parsed;try{parsed=JSON.parse(raw);}catch{reasons.push('Stage 04 OBLIGATION_DISPOSITION evidence contains invalid JSON.');continue;}const id=String(parsed?.obligationId||'').trim(),disposition=String(parsed?.disposition||'').trim().toLowerCase(),reason=String(parsed?.reason||'').trim();if(!expected.has(id)){reasons.push(`Stage 04 disposition references unknown obligation ${id||'MISSING'}.`);continue;}if(!OBLIGATION_DISPOSITIONS.includes(disposition))reasons.push(`Stage 04 obligation ${id} has an invalid non-requirement disposition.`);if(!reason)reasons.push(`Stage 04 obligation ${id} non-requirement disposition requires a reason.`);const list=dispositions.get(id)||[];list.push({disposition,reason});dispositions.set(id,list);}
  let accounted=0;for(const id of expected.keys()){const mappings=mapped.get(id)||[],choices=dispositions.get(id)||[];if(!mappings.length&&!choices.length)reasons.push(`Stage 04 obligation accounting omitted ${id}.`);if(mappings.length&&choices.length)reasons.push(`Stage 04 obligation ${id} is both mapped to requirement(s) and given a non-requirement disposition.`);if(choices.length>1)reasons.push(`Stage 04 obligation ${id} has duplicate non-requirement dispositions.`);const validChoice=choices.length===1&&OBLIGATION_DISPOSITIONS.includes(choices[0].disposition)&&Boolean(choices[0].reason);if((mappings.length>0&&!choices.length)||(!mappings.length&&validChoice))accounted++;}const coverage=manifest.obligationCount?accounted/manifest.obligationCount:1;return {complete:reasons.length===0,coverage,reasons,manifest,expectedObligationCount:manifest.obligationCount,accountedObligationCount:accounted,mapped,dispositions};
}

function representationInspectionCoverage(project){
  ensureShape(project);
  const scope=currentScope(project),productId=String(scope.productId||''),artifacts=recordsForCurrentScope(project,'artifacts').filter(a=>productId&&String(a.scope?.productId||'')===productId),inspections=recordsForCurrentScope(project,'representationInspections'),reasons=[],details=[];
  let totalPagesOrViewsRequired=0,totalPagesOrViewsInspected=0,totalPackagedFilesRequired=0,totalPackagedFilesOpenedOrTested=0,totalTransformationsRequired=0,totalTransformationsInspected=0;
  for(const artifact of artifacts){
    const artifactId=recordId(artifact,'artifacts'),rows=inspections.filter(i=>String(recordValue(i,'ARTIFACT_ID')||i.relationships?.ARTIFACT_ID||'')===artifactId);
    if(rows.length!==1){reasons.push(`Exactly one current aggregate representation inspection is required for ${artifactId}; found ${rows.length}.`);continue;}
    const inspection=rows[0],coverage=parseRepresentationInspectionObservation(inspection);if(!coverage.valid){for(const reason of coverage.reasons)reasons.push(`${recordId(inspection,'representationInspections')}: ${reason}`);if(!coverage.payload)continue;}
    const {requiredPageOrViewIds,inspectedPageOrViewIds,requiredPackagedFileIds,openedOrTestedPackagedFileIds,requiredTransformationIds,inspectedTransformationIds,observation}=coverage;
    totalPagesOrViewsRequired+=requiredPageOrViewIds.length;totalPagesOrViewsInspected+=inspectedPageOrViewIds.length;totalPackagedFilesRequired+=requiredPackagedFileIds.length;totalPackagedFilesOpenedOrTested+=openedOrTestedPackagedFileIds.length;totalTransformationsRequired+=requiredTransformationIds.length;totalTransformationsInspected+=inspectedTransformationIds.length;
    details.push({artifactId,requiredPageOrViewIds,inspectedPageOrViewIds,requiredPackagedFileIds,openedOrTestedPackagedFileIds,requiredTransformationIds,inspectedTransformationIds,observation});
  }
  const inspectionArtifactIds=new Set(inspections.map(i=>String(recordValue(i,'ARTIFACT_ID')||i.relationships?.ARTIFACT_ID||''))),expectedArtifactIds=new Set(artifacts.map(a=>recordId(a,'artifacts')));
  for(const id of inspectionArtifactIds)if(id&&!expectedArtifactIds.has(id))reasons.push(`Representation inspection references non-current product artifact ${id}.`);
  return {complete:artifacts.length>0&&reasons.length===0,reasons,productId,totalDeliveryArtifacts:artifacts.length,totalPagesOrViewsRequired,totalPagesOrViewsInspected,totalPackagedFilesRequired,totalPackagedFilesOpenedOrTested,totalTransformationsRequired,totalTransformationsInspected,details};
}

const applicationadjudicationGate=gate;
gate=function adjudicationGate(stage,project){
  const adjudicated=adjudicatedClone(project),base=applicationadjudicationGate(stage,adjudicated),reasons=[...base.reasons];
  const add=x=>{for(const r of safe(x))if(r&&!reasons.includes(r))reasons.push(r);};
  if(stage===1){const accounting=evaluateIntakeAccounting(project);if(!accounting.complete)add(accounting.reasons);}
  if(stage===4){const accounting=evaluateObligationAccounting(project);if(!accounting.complete)add(accounting.reasons);}
  if(stage===7){for(const mutation of recordsForCurrentScope(project,'failureTests')){const explicitOutcome=upper(recordValue(mutation,'EXECUTION_OUTCOME')),legacyActual=upper(recordValue(mutation,'ACTUAL_RESULT')),expected=upper(recordValue(mutation,'EXPECTED_REJECTION')),legacyOutcome=!explicitOutcome&&expected.includes('REJECT')&&['REJECTED','REJECT','DENIED','BLOCKED'].includes(legacyActual)?'REJECTED_INVALID':!explicitOutcome&&expected.includes('REJECT')&&['ACCEPTED','PASS','PASSED','SUCCESS','SUCCEEDED'].includes(legacyActual)?'ACCEPTED_INVALID':'',outcome=explicitOutcome||legacyOutcome;if(!['REJECTED_INVALID','ACCEPTED_INVALID','UNDETERMINED','NOT_RUN'].includes(outcome))add([recordId(mutation,'failureTests')+': controlled EXECUTION_OUTCOME is required (REJECTED_INVALID/ACCEPTED_INVALID/UNDETERMINED/NOT_RUN); only the finite legacy rejection/acceptance vocabulary is migrated.']);if(outcome==='ACCEPTED_INVALID'&&!String(recordValue(mutation,'VALIDATOR_DEFECT_ID')||mutation.relationships?.VALIDATOR_DEFECT_ID||'').trim())add(['A known-invalid fixture was accepted without a linked validator defect.']);if(['UNDETERMINED','NOT_RUN'].includes(outcome))add([recordId(mutation,'failureTests')+': failure test is not conclusively executed.']);}}
  if(stage===7){for(const mutation of recordsForCurrentScope(project,'failureTests')){const id=recordId(mutation,'failureTests'),reqId=String(recordValue(mutation,'REQ_ID')||mutation.relationships?.REQ_ID||''),evidence=evaluateEvidenceContract(null,mutation,null,project),plans=testExecutionPlan(project).items.filter(item=>String(item.requirementId||'')===reqId),capable=plans.some(item=>item.operatorAction!=='BLOCKED'&&item.executionRoute!=='BLOCKED'&&item.capabilityReady!==false);if(!evidence.sufficient)add(evidence.reasons.map(reason=>id+': '+reason));if(!capable)add([id+': no capable current execution route is established for the mutated requirement.']);if(adjudicationEmpty(recordValue(mutation,'FIXTURE')))add([id+': actual failure fixture is not identifiable.']);if(adjudicationEmpty(recordValue(mutation,'EXPECTED_REJECTION')))add([id+': expected rejection is missing.']);if(adjudicationEmpty(recordValue(mutation,'ACTUAL_RESULT')))add([id+': actual execution result is missing.']);}}
  if(stage===9)for(const r of recordsForCurrentScope(project,'preflightRecords')){const e=evaluateResultConsistency('preflightRecords',r,null,project);if(e.determination!=='SATISFIED')add(e.reasons.length?e.reasons:['Preflight is not application-derived SATISFIED.']);}
  if(stage===13){const iteration=latestIteration(project,[10]),iterationId=recordId(iteration,'iterations');for(const req of mandatoryRequirements(project,scopeForIteration(project,iterationId))){const facts=comparisonFacts(project,requirementId(req),iterationId),rows=recordsForIteration(project,'comparisons',iterationId).filter(r=>String(recordValue(r,'REQ_ID')||r.relationships?.REQ_ID||'')===requirementId(req));if(rows.length!==1)add(['Exactly one comparison record is required for '+requirementId(req)+'.']);if(!facts.allSatisfied&&facts.anyViolation)add(['Derived comparison contains a violation for '+requirementId(req)+'.']);if(facts.anyUndetermined)add(['Derived comparison contains an undetermined result for '+requirementId(req)+'.']);}}
  if(stage===14)for(const defect of confirmedDefects(project)){const id=recordId(defect,'defects'),rcas=records(project,'rootCauses').filter(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===id);if(rcas.length!==1)add(['Exactly one complete RCA is required for '+id+'.']);else{const v=validateRootCauseRecord(rcas[0],project);if(!v.valid)add(v.reasons.map(x=>id+': '+x));}}
  if(stage===16){for(const defect of confirmedDefects(project)){const id=recordId(defect,'defects'),changes=records(project,'changes',{stage:16}).filter(c=>{const raw=recordValue(c,'TRIGGERING_DEFECT_IDS'),ids=Array.isArray(raw)?raw.map(String):String(raw||'').split(/[;,\s]+/).filter(Boolean);return ids.includes(id);});if(changes.length!==1)add(['Exactly one responsible-layer changeset trace is required for '+id+'.']);else{const v=validateChangeTrace(changes[0],project);if(!v.valid)add(v.reasons.map(x=>id+': '+x));}}}
  if(stage===19)for(const r of recordsForCurrentScope(project,'confirmationRecords')){const e=confirmationDetermination(project,r);if(e.determination!=='SATISFIED')add(e.reasons.length?e.reasons:['Unchanged confirmation is not application-derived SATISFIED.']);}
  if(stage===20){const ok=recordsForCurrentScope(project,'confirmationRecords').filter(r=>effectiveDetermination('confirmationRecords',r,null,project)==='SATISFIED');if(ok.length!==1)add(['Exactly one application-derived successful unchanged confirmation is required.']);}
  if(stage===21)for(const r of recordsForCurrentScope(project,'products')){const e=evaluateResultConsistency('products',r,null,project);if(e.determination!=='SATISFIED')add(e.reasons.length?e.reasons:['Production execution is not application-derived SATISFIED.']);}
  const stageCollection={22:'deterministicResults',23:'meaningResults',24:'adversarialResults',25:'representationInspections'}[stage];if(stageCollection)for(const r of recordsForCurrentScope(project,stageCollection)){const e=evaluateResultConsistency(stageCollection,r,testForResult(project,r),project);if(e.determination!=='SATISFIED')add(e.reasons.length?e.reasons:[stageCollection+' contains a non-satisfied effective result.']);}
  if(stage===25){const coverage=representationInspectionCoverage(project);if(!coverage.complete)add(coverage.reasons.length?coverage.reasons:['Stage 25 representation coverage is incomplete.']);}
  if(stage===26){for(const r of recordsForCurrentScope(project,'processAudits')){const e=auditDetermination('processAudits',r,project);if(e.determination!=='SATISFIED')add(e.reasons);}for(const r of recordsForCurrentScope(project,'productAudits')){const e=auditDetermination('productAudits',r,project);if(e.determination!=='SATISFIED')add(e.reasons);}}
  if(stage===26){const process=recordsForCurrentScope(project,'processAudits'),product=recordsForCurrentScope(project,'productAudits'),contradictions=detectCurrentContradictions(project);if(process.length!==1)add([`Exactly one current process audit is required; found ${process.length}.`]);if(product.length!==1)add([`Exactly one current product audit is required; found ${product.length}.`]);if(openBlockers(project,26).length)add(['Stage 26 has unresolved reconciliation blockers.']);if(contradictions.length)add(['Stage 26 cannot reconcile while current contradictions remain.']);}
  if(stage===30){const baseline=recordsForCurrentScope(project,'baselines').at(-1),baselineTime=Date.parse(String(baseline?.createdAt||baseline?.deviceTimestamp||'')),active=activeRegressions(project);if(!baseline||!Number.isFinite(baselineTime))add(['Stage 30 requires the current approved baseline identity and approval time.']);for(const reg of active){const id=recordId(reg,'regressions'),allExec=records(project,'regressionExecutions').filter(e=>String(recordValue(e,'REG_ID')||e.relationships?.REG_ID||'')===id),pre=allExec.filter(e=>upper(recordValue(e,'PHASE'))==='PRE_CORRECTION'&&effectiveRegressionDetermination(project,e).determination==='SATISFIED'),post=allExec.filter(e=>upper(recordValue(e,'PHASE'))!=='PRE_CORRECTION').filter(e=>{const t=Date.parse(String(e.deviceTimestamp||e.createdAt||recordValue(e,'ENDED_AT_DEVICE_TIME')||''));return Number.isFinite(t)&&Number.isFinite(baselineTime)&&t<=baselineTime;}).sort((a,b)=>Date.parse(String(a.deviceTimestamp||a.createdAt||recordValue(a,'ENDED_AT_DEVICE_TIME')||''))-Date.parse(String(b.deviceTimestamp||b.createdAt||recordValue(b,'ENDED_AT_DEVICE_TIME')||'')));if(!pre.length)add([`Permanent regression ${id} lacks preserved pre-correction failure reproduction.`]);const latest=post.at(-1);if(!latest||effectiveRegressionDetermination(project,latest).determination!=='SATISFIED')add([`Permanent regression ${id} lacks a successful latest applicable execution before baseline approval.`]);}}
  if(stage>=27&&releaseMetrics(project).determination==='ACCEPTED'&&detectCurrentContradictions(project).length)add(['Release cannot be ACCEPTED while a adjudication contradiction exists.']);
  return {...base,complete:reasons.length===0,reasons,blocked:base.blocked||reasons.some(r=>/UNDETERMINED|BLOCKED|missing|unavailable|not established|not conclusively/i.test(r))};
};
function deriveStageData(project,stage){ensureShape(project);const derived={};const ids=collection=>recordsForCurrentScope(project,collection).filter(r=>Number(r.stage)===Number(stage)).map(r=>recordId(r,collection));const metrics=[4,6,12].includes(Number(stage))?coverageMetrics(project):null;const convergence=Number(stage)===18?convergenceMetrics(project):null;const release=Number(stage)===27?releaseMetrics(project):null;switch(stage){case 1:{const intake=evaluateIntakeAccounting(project);Object.assign(derived,{JOB_ID:project.job.JOB_ID,DATE_OPENED:project.job.DATE_OPENED,INPUT_SET_VERSION:project.job.CURRENT_INPUT_VERSION,INPUT_SET_HASH_OR_MANIFEST:project.job.INPUT_SET_HASH_OR_MANIFEST||intake.manifest?.manifestSha256||'UNKNOWN',JOB_RECORD_STATUS:intake.complete&&acceptedChanges(project,1).length?'READY':'NOT READY',STATUS_EVIDENCE:intake.complete?`Application accounted ${intake.accountedUnitCount}/${intake.expectedUnitCount} controlled input units.`:intake.reasons.join('; ')});break;}case 2:{const conflicts=recordsForCurrentScope(project,'sourceConflicts'),unresolved=conflicts.filter(r=>['UNRESOLVED','BLOCKED','UNKNOWN','OPEN'].includes(upper(recordValue(r,'RESOLUTION_STATUS'))));Object.assign(derived,{SOURCE_SET_VERSION:project.job.CURRENT_SOURCE_SET_VERSION||'NOT APPLICABLE',SOURCE_RECORDS:ids('sources'),SOURCE_CONFLICT_RECORDS:ids('sourceConflicts'),UNRESOLVED_CONTROLLING_CONFLICTS:unresolved.map(r=>recordId(r,'sourceConflicts'))});break;}case 3:{const sourceIds=recordsForCurrentScope(project,'sources').map(r=>recordId(r,'sources')),research=recordsForCurrentScope(project,'research'),researched=new Set(research.map(r=>String(recordValue(r,'SOURCE_ID')||r.relationships?.SOURCE_ID||''))),candidate=recordsForCurrentScope(project,'candidateRequirements');Object.assign(derived,{RESEARCH_VERSION:'RESEARCH-'+hash.sha256Value(research.map(r=>r.recordSha256||r.sha256||recordId(r,'research'))).slice(0,16).toUpperCase(),SOURCE_RESEARCH_RECORDS:research.map(r=>recordId(r,'research')),CANDIDATE_REQUIREMENT_RECORDS:candidate.map(r=>recordId(r,'candidateRequirements')),ALL_KNOWN_CONTROLLING_SOURCES_EXAMINED:sourceIds.length===0?upper(project.stages[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE':sourceIds.every(id=>researched.has(id))});break;}case 4:{const reqs=recordsForCurrentScope(project,'requirements'),accounting=evaluateObligationAccounting(project),atomicityIssues=reqs.filter(req=>{const obligation=String(recordValue(req,'OBLIGATION')||'').trim(),success=String(recordValue(req,'OBSERVABLE_SATISFACTION_CONDITION')||'').trim(),failure=String(recordValue(req,'FAILURE_CONDITION')||'').trim();return !obligation||!success||!failure;}).map(req=>recordId(req,'requirements'));Object.assign(derived,{REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NOT APPLICABLE',REQUIREMENT_RECORDS:reqs.map(r=>recordId(r,'requirements')),ATOMICITY_REVIEW_RESULTS:{reviewed:reqs.length,structurallyIncomplete:atomicityIssues,obligationAccountingComplete:accounting.complete,obligationAccountingCoverage:accounting.coverage},TOTAL_REQUIREMENTS:reqs.length,MANDATORY_REQUIREMENTS:metrics.mandatoryRequirementCount});break;}case 5:{const resolutions=recordsForCurrentScope(project,'requirementResolutions'),unresolved=resolutions.filter(r=>['OPEN','UNRESOLVED','BLOCKED','UNKNOWN'].includes(upper(recordValue(r,'STATUS')))),blockers=openBlockers(project,5);Object.assign(derived,{INPUT_REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NONE',OUTPUT_REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NONE',REQUIREMENT_DEFECT_RECORDS:resolutions.map(r=>recordId(r,'requirementResolutions')),UNRESOLVED_CONFLICTS:unresolved.map(r=>recordId(r,'requirementResolutions')),MISSING_PREREQUISITES:blockers.filter(b=>/prerequisite/i.test(String(recordValue(b,'MISSING_ITEM_TYPE')||recordValue(b,'WHY_WORK_CANNOT_CONTINUE')||''))).map(b=>recordId(b,'blockers')),MANDATORY_BLOCKERS:blockers.map(b=>recordId(b,'blockers'))});break;}case 6:{const tests=recordsForCurrentScope(project,'tests'),mandatoryIds=new Set(mandatoryRequirements(project).map(requirementId)),mandatoryTests=tests.filter(t=>mandatoryIds.has(testRequirementId(t))),executionPlan=testExecutionPlan(project),currentArtifactBlockedTestIds=new Set(executionPlan.items.filter(item=>mandatoryIds.has(item.requirementId)&&item.artifactRequired&&item.artifactIds.length>0&&!item.artifactReady).map(item=>item.testId)),blocked=mandatoryTests.filter(t=>upper(recordValue(t,'EXECUTION_MODE'))==='UNAVAILABLE'||upper(recordValue(t,'STATUS'))==='BLOCKED'||(upper(recordValue(t,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&!applicationTestSupported(t))||currentArtifactBlockedTestIds.has(recordId(t,'tests')));Object.assign(derived,{TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NOT APPLICABLE',TEST_RECORDS:tests.map(r=>recordId(r,'tests')),COVERAGE_RECORDS:mandatoryRequirements(project).map(req=>({requirementId:requirementId(req),testIds:tests.filter(t=>testRequirementId(t)===requirementId(req)).map(t=>recordId(t,'tests'))})),TOTAL_ACTIVE_MANDATORY_REQUIREMENTS:metrics.mandatoryRequirementCount,ACTIVE_MANDATORY_REQUIREMENTS_WITH_AT_LEAST_ONE_READY_TEST:metrics.requirementsWithTests,MANDATORY_TEST_COVERAGE:metrics.requirementCoverage,BLOCKED_MANDATORY_REQUIREMENTS:[...new Set(blocked.map(t=>testRequirementId(t)))]});break;}case 7:{const reqs=mandatoryRequirements(project),mutations=recordsForCurrentScope(project,'failureTests'),covered=new Set(mutations.map(testRequirementId)),acceptedInvalid=[],defective=[];for(const m of mutations){const actual=upper(recordValue(m,'ACTUAL_RESULT')),expected=upper(recordValue(m,'EXPECTED_REJECTION')),bad=expected.includes('REJECT')&&/\b(?:ACCEPTED|ALLOWED|PASSED|NOT REJECTED|VALID)\b/.test(actual)&&!/\b(?:REJECTED|BLOCKED|FAILED|INVALID)\b/.test(actual);if(bad){acceptedInvalid.push(recordId(m,'failureTests'));const defect=String(recordValue(m,'VALIDATOR_DEFECT_ID')||m.relationships?.VALIDATOR_DEFECT_ID||'');if(defect)defective.push(defect);}}Object.assign(derived,{MUTATION_SUITE_VERSION:'MUTATION-'+hash.sha256Value(mutations.map(r=>r.recordSha256||r.sha256||recordId(r,'failureTests'))).slice(0,16).toUpperCase(),FAILURE_TEST_RECORDS:mutations.map(r=>recordId(r,'failureTests')),ACTIVE_REQUIREMENTS:reqs.map(requirementId),REQUIREMENTS_WITH_AT_LEAST_ONE_FAILURE_TEST:reqs.filter(r=>covered.has(requirementId(r))).map(requirementId),FAILURE_TEST_COVERAGE:reqs.length?reqs.filter(r=>covered.has(requirementId(r))).length/reqs.length:1,INVALID_FIXTURES_ACCEPTED:acceptedInvalid,DEFECTIVE_VALIDATORS:[...new Set(defective)]});break;}case 8:{const traces=recordsForCurrentScope(project,'instructionTraces');Object.assign(derived,{DRAFT_INSTRUCTION_VERSION:project.job.CURRENT_INSTRUCTION_VERSION||'NONE',INSTRUCTION_TRACE_RECORDS:traces.map(r=>recordId(r,'instructionTraces'))});break;}case 9:{const reviews=recordsForCurrentScope(project,'preflightRecords'),change=acceptedChanges(project,9).at(-1),contextId=String(change?.scope?.contextId||''),ind=evaluateContextIndependence(project,{role:'PREFLIGHT_REVIEW',reviewerContextId:contextId});Object.assign(derived,{INPUT_INSTRUCTION_VERSION:project.job.CURRENT_INSTRUCTION_VERSION||'NONE',OUTPUT_INSTRUCTION_VERSION:project.job.CURRENT_INSTRUCTION_VERSION||'NONE',PREFLIGHT_REVIEWER_ID:contextId||'UNKNOWN',REVIEW_CONTEXT_INDEPENDENT_FROM_AUTHOR:ind.determination,SENTENCE_REVIEW_RECORDS:reviews.map(r=>recordId(r,'preflightRecords')),PREFLIGHT_ITERATION_RECORDS:safe(project.projectData.acceptedChanges).filter(c=>Number(c.stage)===9&&!c.invalidatedBy).map(c=>c.changeId)});break;}case 10:{const iteration=latestIteration(project,[10]),iterationId=recordId(iteration,'iterations'),candidateId=iterationCandidateId(project,iterationId),candidate=records(project,'candidateFreezes').find(r=>recordId(r,'candidateFreezes')===candidateId),manifest=safe(recordValue(candidate,'COMPONENT_MANIFEST')),hashes=recordValue(candidate,'COMPONENT_HASHES')||{};Object.assign(derived,{CANDIDATE_ID:candidateId||'NONE',ITERATION_ID:iterationId||'NONE',FREEZE_DATE:candidate?.createdAt||'NONE',FROZEN_COMPONENT_RECORDS:manifest.length,TOOL_CONFIGURATION_RECORDS:adjudicationEmpty(recordValue(candidate,'TOOL_CONFIGURATION'))?0:1,ALL_REQUIRED_COMPONENTS_PRESENT:manifest.length>0&&manifest.every(x=>x.artifactId&&x.filename&&x.sha256),ALL_RUNS_WILL_RECEIVE_IDENTICAL_FROZEN_MATERIALS:Boolean(candidateId&&candidateComponentIdentity(project,candidateId)),HASHES_RECORDED_WHERE_PRACTICAL:manifest.length>0&&Object.keys(hashes).length===manifest.length,CHANGES_ALLOWED_DURING_BATCH:'NO IN-PLACE MATERIAL CHANGES; ANY MATERIAL CHANGE REQUIRES A NEW CANDIDATE'});break;}case 11:case 19:{const it=latestIteration(project,[stage]),iterationId=recordId(it,'iterations'),ev=evaluateIteration(project,iterationId,stage===19?'UNCHANGED_CONFIRMATION':'INITIAL');Object.assign(derived,{ITERATION_ID:ev.iterationId,RUN_COUNT:ev.runs.length,FRESH_CONTEXT_COUNT:ev.contextCount,ITERATION_COMPLETE:ev.complete,ITERATION_REASONS:ev.reasons,STABILITY_SUMMARY:stage===19?ev.stability:null});if(stage===11){const runs=ev.runs,contexts=runs.map(r=>String(recordValue(r,'CONTEXT_ID')||r.relationships?.CONTEXT_ID||''));Object.assign(derived,{CANDIDATE_ID:iterationCandidateId(project,iterationId)||'NONE',RUN_RECORDS:runs.map(r=>recordId(r,'runs')),FRESH_CONTEXTS_CREATED:new Set(contexts.filter(Boolean)).size,RUNS_RECEIVING_EXACT_PACKAGE:ev.reasons.some(r=>/candidate|package/i.test(r))?0:runs.length,CONTAMINATED_RUNS:runs.filter(r=>{const c=records(project,'freshContexts').find(x=>recordId(x,'freshContexts')===String(recordValue(r,'CONTEXT_ID')||r.relationships?.CONTEXT_ID||''));return ['YES','TRUE','CONTAMINATED'].includes(upper(recordValue(c,'CONTAMINATION_STATUS')||recordValue(r,'CONTAMINATION_CHECK')));}).map(r=>recordId(r,'runs')),OUTPUTS_SAVED_SEPARATELY:runs.filter(r=>!adjudicationEmpty(recordValue(r,'COMPLETE_OUTPUT'))).length});}if(stage===19){const confirmation=recordsForCurrentScope(project,'confirmationRecords').at(-1),sourceId=String(recordValue(confirmation,'SOURCE_ITERATION_ID')||confirmation?.relationships?.SOURCE_ITERATION_ID||''),matrix=verificationMatrix(project,iterationId),regs=records(project,'regressions').filter(r=>isActiveRecord(r)&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),regExec=currentRegressionExecutions(project,iterationId),defects=recordsForIteration(project,'defects',iterationId),comparisonCount=recordsForIteration(project,'comparisons',iterationId).length,reqCount=mandatoryRequirements(project,scopeForIteration(project,iterationId)).length,determination=confirmationDetermination(project,confirmation);Object.assign(derived,{SOURCE_CONVERGED_ITERATION:sourceId||'NONE',CONFIRMATION_ITERATION_ID:iterationId||'NONE',ZERO_CHANGE_AUDIT:!ev.reasons.some(r=>/change|candidate|hash/i.test(r)),TEN_NEW_CONTEXTS_CREATED:ev.contextCount,SAME_FROZEN_PACKAGE_USED:!ev.reasons.some(r=>/package|candidate/i.test(r)),RUNS_COMPLETED:ev.runs.length,COMPLETE_TEST_SUITE_RUN:matrix.expected.length>0&&!matrix.missing.length&&!matrix.duplicates.length,ALL_REGRESSION_TESTS_RUN:regs.every(r=>regExec.some(e=>String(recordValue(e,'REG_ID')||e.relationships?.REG_ID||'')===recordId(r,'regressions'))),CROSS_RUN_COMPARISON_COMPLETED:comparisonCount===reqCount,NEW_CRITICAL_DEFECTS:defects.filter(d=>upper(recordValue(d,'SEVERITY'))==='CRITICAL').length,NEW_MAJOR_DEFECTS:defects.filter(d=>upper(recordValue(d,'SEVERITY'))==='MAJOR').length,NEW_REQUIREMENTS_DISCOVERED:0,INJECTED_DEFECTS_NOT_DETECTED:0,NEW_CORRECTNESS_AFFECTING_VARIANCE:ev.stability?.unexplainedVarianceCount||0,CONFIRMATION_ACCEPTANCE_CRITERIA_SATISFIED:determination.determination==='SATISFIED',REQUIRED_RETURN_STAGE:determination.determination==='SATISFIED'?'NONE':'STAGE 14'});}break;}case 12:{const iteration=latestIteration(project,[10]),iterationId=recordId(iteration,'iterations'),matrix=verificationMatrix(project,iterationId),rows=recordsForIteration(project,'verification',iterationId),ds=rows.map(r=>effectiveDetermination('verification',r,testForResult(project,r),project)),selfValidated=rows.filter(r=>{const runId=String(recordValue(r,'RUN_ID')||r.relationships?.RUN_ID||''),run=records(project,'runs').find(x=>recordId(x,'runs')===runId),generator=String(recordValue(run,'CONTEXT_ID')||run?.relationships?.CONTEXT_ID||''),reviewer=resultContextIdentity(project,r);return generator&&reviewer&&generator===reviewer;});Object.assign(derived,{ITERATION_ID:iterationId||'NONE',REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NONE',TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NONE',VERIFICATION_RECORDS:rows.map(r=>recordId(r,'verification')),ACTIVE_MANDATORY_REQUIREMENTS:mandatoryRequirements(project,scopeForIteration(project,iterationId)).map(requirementId),RUNS:matrix.runs.map(r=>recordId(r,'runs')),EXPECTED_MANDATORY_RECORDS:matrix.expected.length,ACTUAL_MANDATORY_RECORDS:matrix.verification.length,MISSING_RECORDS:matrix.missing.length,SATISFIED_RECORDS:ds.filter(d=>d==='SATISFIED').length,VIOLATED_RECORDS:ds.filter(d=>d==='VIOLATED').length,UNDETERMINED_RECORDS:ds.filter(d=>d==='UNDETERMINED').length,SELF_VALIDATED_RECORDS:selfValidated.length,VERIFICATION_COVERAGE:matrix.coverage});break;}case 13:{const it=latestIteration(project,[10]),iterationId=recordId(it,'iterations'),scope=iterationId?scopeForIteration(project,iterationId):currentScope(project),facts={},reqs=mandatoryRequirements(project,scope);for(const req of reqs){const f=comparisonFacts(project,requirementId(req),iterationId);facts[requirementId(req)]={RUN_DETERMINATIONS:f.runDeterminations,ALL_TEN_SATISFIED:f.allSatisfied,ANY_VIOLATION:f.anyViolation,ANY_UNDETERMINED:f.anyUndetermined,SATISFIED_COUNT:f.determinations.filter(x=>x==='SATISFIED').length,VIOLATED_COUNT:f.determinations.filter(x=>x==='VIOLATED').length,UNDETERMINED_COUNT:f.determinations.filter(x=>!['SATISFIED','VIOLATED'].includes(x)).length};}const stability=iterationId?executionStability(project,iterationId):{repeatedDefectCount:0,uniqueDefectCount:0},comparisons=iterationId?recordsForIteration(project,'comparisons',iterationId):[];Object.assign(derived,{ITERATION_ID:iterationId||'NONE',COMPARISON_VERSION:'COMPARISON-'+hash.sha256Value(facts).slice(0,16).toUpperCase(),REQUIREMENT_COMPARISON_RECORDS:comparisons.length,REQUIREMENTS_SATISFIED_BY_ALL_TEN:reqs.filter(r=>facts[requirementId(r)]?.ALL_TEN_SATISFIED).map(requirementId),REQUIREMENTS_WITH_AT_LEAST_ONE_VIOLATION:reqs.filter(r=>facts[requirementId(r)]?.ANY_VIOLATION).map(requirementId),REQUIREMENTS_WITH_AT_LEAST_ONE_UNDETERMINED:reqs.filter(r=>facts[requirementId(r)]?.ANY_UNDETERMINED).map(requirementId),CORRECTNESS_AFFECTING_DISAGREEMENTS:comparisons.filter(r=>truth(recordValue(r,'CORRECTNESS_AFFECTING_VARIANCE'))).map(r=>recordId(r,'comparisons')),PROHIBITED_OUTPUT_VARIANCES:comparisons.filter(r=>adjudicationAdverse(recordValue(r,'PROHIBITED_VARIANCE'))).map(r=>recordId(r,'comparisons')),INCONCLUSIVE_TESTS:reqs.filter(r=>facts[requirementId(r)]?.ANY_UNDETERMINED).map(requirementId),REPEATED_FAILURE_GROUPS:stability.repeatedDefectCount,UNIQUE_FAILURES:stability.uniqueDefectCount,APPLICATION_DERIVED_COMPARISON_FACTS:facts,STABILITY_SUMMARY:stability});break;}case 14:{const defects=confirmedDefects(project),rcas=recordsForCurrentScope(project,'rootCauses'),byDefect=new Map();for(const r of rcas){const id=String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'');if(!byDefect.has(id))byDefect.set(id,[]);byDefect.get(id).push(r);}const confirmed=[],unknown=[],blocked=[];for(const d of defects){const id=recordId(d,'defects'),rows=byDefect.get(id)||[];if(rows.length===1){const v=validateRootCauseRecord(rows[0],project);if(v.valid)confirmed.push(id);else unknown.push(id);}else if(rows.length===0)unknown.push(id);else blocked.push(id);}Object.assign(derived,{ROOT_CAUSE_ANALYSIS_VERSION:'RCA-'+hash.sha256Value(rcas.map(r=>r.sha256||r.recordSha256||recordId(r,'rootCauses'))).slice(0,16).toUpperCase(),DEFECT_ROOT_CAUSE_RECORDS:rcas.length,TOTAL_MATERIAL_DEFECTS:defects.length,CONFIRMED_ROOT_CAUSES:confirmed,UNDETERMINED_ROOT_CAUSES:unknown,BLOCKED_ANALYSES:blocked});break;}case 15:{const defects=confirmedDefects(project),regs=records(project,'regressions').filter(r=>isActiveRecord(r)),execs=records(project,'regressionExecutions'),covered=[],preProven=[],missing=[];for(const d of defects){const id=recordId(d,'defects'),rs=regs.filter(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===id);if(rs.length)covered.push(id);else missing.push(id);if(rs.some(r=>execs.some(e=>String(recordValue(e,'REG_ID')||e.relationships?.REG_ID||'')===recordId(r,'regressions')&&upper(recordValue(e,'PHASE'))==='PRE_CORRECTION'&&effectiveRegressionDetermination(project,e).determination==='SATISFIED')))preProven.push(id);}Object.assign(derived,{INPUT_TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NONE',OUTPUT_TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NONE',REGRESSION_FIXTURE_VERSION:'REGRESSION-'+hash.sha256Value(regs.map(r=>r.sha256||recordId(r,'regressions'))).slice(0,16).toUpperCase(),REGRESSION_RECORDS:regs.length,CONFIRMED_DEFECTS:defects.length,CONFIRMED_DEFECTS_WITH_REGRESSION_TEST:covered,PRE_CORRECTION_FAILURES_PROVEN:preProven,UNCONVERTED_CONFIRMED_DEFECTS:missing});break;}case 16:{const changes=recordsForCurrentScope(project,'changes').filter(r=>Number(r.stage)===16),defects=confirmedDefects(project),triggered=[...new Set(changes.flatMap(c=>{const raw=recordValue(c,'TRIGGERING_DEFECT_IDS');return Array.isArray(raw)?raw.map(String):String(raw||'').split(/[;,\s]+/).filter(Boolean);} ))],changedInstructions=changes.some(c=>/INSTRUCTION/i.test(String(recordValue(c,'RESPONSIBLE_LAYER')||''))),changedArtifacts=changes.filter(c=>String(recordValue(c,'NEW_ARTIFACT_VERSION')||'').trim()),inPlace=changes.filter(c=>truth(recordValue(c,'IN_PLACE_MODIFICATION')));Object.assign(derived,{CHANGE_SET_ID:changes.map(c=>recordId(c,'changes')),TRIGGERING_DEFECT_IDS:triggered,RCA_VERSION:'RCA-'+hash.sha256Value(recordsForCurrentScope(project,'rootCauses').map(r=>r.sha256||recordId(r,'rootCauses'))).slice(0,16).toUpperCase(),DATE:changes.at(-1)?.createdAt||now(),ARTIFACT_CHANGE_RECORDS:changedArtifacts.length,INSTRUCTION_CHANGED:changedInstructions,IF_EXECUTION_ONLY_DEFECT_WAS_INSTRUCTION_PRESERVED:!changedInstructions,PREFLIGHT_REPEATED_IF_CHANGED:changedInstructions?recordsForCurrentScope(project,'preflightRecords').length>0:true,ARTIFACTS_CHANGED:changedArtifacts.length>0,NEW_VERSIONS_CREATED:changedArtifacts.length,IN_PLACE_MODIFICATIONS:inPlace.length,DOWNSTREAM_VERIFICATIONS_INVALIDATED:safe(project.projectData.history).filter(h=>h.type==='DOWNSTREAM_INVALIDATED'&&h.stage===16).at(-1)?.invalidatedStages||[]});break;}case 17:{const iterations=records(project,'iterations').filter(r=>Number(r.stage)===17&&isActiveRecord(r)),currentIt=iterations.at(-1),previous=records(project,'iterations').filter(r=>isActiveRecord(r)&&recordId(r,'iterations')!==recordId(currentIt,'iterations')).at(-1),ev=evaluateIteration(project,recordId(currentIt,'iterations'),'CORRECTED'),matrix=recordId(currentIt,'iterations')?verificationMatrix(project,recordId(currentIt,'iterations')):{coverage:0},comparisons=recordId(currentIt,'iterations')?recordsForIteration(project,'comparisons',recordId(currentIt,'iterations')):[],defects=recordId(currentIt,'iterations')?recordsForIteration(project,'defects',recordId(currentIt,'iterations')):[],rcas=recordsForCurrentScope(project,'rootCauses'),regs=records(project,'regressions').filter(r=>isActiveRecord(r)),changes=recordsForCurrentScope(project,'changes');Object.assign(derived,{PREVIOUS_ITERATION_ID:recordId(previous,'iterations')||'NONE',NEW_ITERATION_ID:recordId(currentIt,'iterations')||'NONE',PREVIOUS_CANDIDATE_ID:previous?iterationCandidateId(project,recordId(previous,'iterations')):'NONE',NEW_CANDIDATE_ID:currentIt?iterationCandidateId(project,recordId(currentIt,'iterations')):'NONE',CHANGESET_ID:recordId(changes.at(-1),'changes')||'NONE',NEW_FROZEN_VERSIONS:recordValue(currentIt,'FROZEN_VERSIONS')||{},OLD_CONVERSATIONS_CONTINUED:false,TEN_NEW_CONTEXTS_CREATED:ev.contextCount,RUN_NAMESPACE:ev.runs.map(r=>recordId(r,'runs')),IDENTICAL_PACKAGE_CONFIRMED_FOR_ALL_RUNS:!ev.reasons.some(r=>/candidate|package/i.test(r)),PRIOR_OUTPUTS_WITHHELD:!ev.reasons.some(r=>/prior|contamin/i.test(r)),EXECUTE_COMPLETED:ev.runs.length===10,VERIFY_COMPLETED:matrix.coverage===1,COMPARE_COMPLETED:comparisons.length===mandatoryRequirements(project,scopeForIteration(project,recordId(currentIt,'iterations'))).length,ROOT_CAUSE_COMPLETED:defects.every(d=>rcas.some(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===recordId(d,'defects'))),REGRESSION_TESTS_ADDED:defects.every(d=>regs.some(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')===recordId(d,'defects'))),CORRECTIONS_COMPLETED:defects.every(d=>changes.some(c=>String(recordValue(c,'TRIGGERING_DEFECT_IDS')||'').includes(recordId(d,'defects'))))});break;}case 18:{const reqs=mandatoryRequirements(project),regs=records(project,'regressions').filter(r=>isActiveRecord(r)&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),completeReqs=reqs.filter(r=>!adjudicationEmpty(recordValue(r,'OBLIGATION'))&&!adjudicationEmpty(recordValue(r,'APPLICABILITY'))&&!adjudicationEmpty(recordValue(r,'OBSERVABLE_SATISFACTION_CONDITION'))),failed=[];if(convergence.requirementCoverage!==1)failed.push('MANDATORY_REQUIREMENT_COVERAGE');if(convergence.verificationCoverage!==1)failed.push('MANDATORY_VERIFICATION_COVERAGE');if(convergence.regressionSuccess!==1)failed.push('REGRESSION_TEST_SUCCESS');if(convergence.criticalDefects)failed.push('CRITICAL_DEFECTS');if(convergence.majorDefects)failed.push('MAJOR_DEFECTS');if(convergence.mandatoryUnresolvedUnknowns)failed.push('MANDATORY_UNRESOLVED_UNKNOWNS');if(convergence.contradictions)failed.push('KNOWN_CORRECTNESS_AFFECTING_CONTRADICTIONS');if(convergence.ambiguities)failed.push('KNOWN_CORRECTNESS_AFFECTING_AMBIGUITIES');if(convergence.unexplainedVariance)failed.push('UNEXPLAINED_CORRECTNESS_AFFECTING_EXECUTION_VARIANCE');Object.assign(derived,{ITERATION_ID:convergence.iterationId||project.job.CURRENT_ITERATION||'NONE',METRICS_VERSION:'METRICS-'+hash.sha256Value(convergence).slice(0,16).toUpperCase(),TOTAL_MANDATORY_REQUIREMENTS:reqs.length,MANDATORY_REQUIREMENTS_WITH_COMPLETE_SPECIFICATION_AND_APPLICABILITY:completeReqs.length,MANDATORY_REQUIREMENT_COVERAGE:convergence.requirementCoverage,MANDATORY_REQUIREMENTS_WITH_AFFIRMATIVE_APPLICABLE_VERIFICATION:Math.round(reqs.length*convergence.verificationCoverage),MANDATORY_VERIFICATION_COVERAGE:convergence.verificationCoverage,TOTAL_STILL_APPLICABLE_REGRESSION_TESTS:regs.length,SUCCESSFUL_REGRESSION_TESTS:Math.round(regs.length*convergence.regressionSuccess),REGRESSION_TEST_SUCCESS:convergence.regressionSuccess,CRITICAL_DEFECTS:convergence.criticalDefects,MAJOR_DEFECTS:convergence.majorDefects,MANDATORY_UNRESOLVED_UNKNOWNS:convergence.mandatoryUnresolvedUnknowns,KNOWN_CORRECTNESS_AFFECTING_CONTRADICTIONS:convergence.contradictions,KNOWN_CORRECTNESS_AFFECTING_AMBIGUITIES:convergence.ambiguities,UNEXPLAINED_CORRECTNESS_AFFECTING_EXECUTION_VARIANCE:convergence.unexplainedVariance,ALL_CONDITIONS_SIMULTANEOUSLY_TRUE:convergence.converged,FAILED_CONDITIONS:failed,STABILITY_SUMMARY:convergence.iterationId?executionStability(project,convergence.iterationId):null});break;}case 20:{const confirmation=recordsForCurrentScope(project,'confirmationRecords').filter(r=>effectiveDetermination('confirmationRecords',r,null,project)==='SATISFIED').at(-1),baseline=recordsForCurrentScope(project,'baselines').at(-1),approved=recordValue(baseline,'APPROVED_VERSIONS')||{},candidateId=String(approved.candidateId||''),candidate=records(project,'candidateFreezes').find(r=>recordId(r,'candidateFreezes')===candidateId),manifest=safe(recordValue(candidate,'COMPONENT_MANIFEST'));Object.assign(derived,{BASELINE_ID:recordId(baseline,'baselines')||'NONE',BASELINE_APPROVAL_DATE:baseline?.createdAt||'NONE',SUPPORTING_CONFIRMATION_ITERATION:String(recordValue(confirmation,'CONFIRMATION_ITERATION_ID')||recordValue(confirmation,'ITERATION_ID')||confirmation?.scope?.iterationId||'NONE'),APPROVED_VERSIONS:approved,BASELINE_FILE_RECORDS:manifest,UNCHANGED_CONFIRMATION_SUCCEEDED:Boolean(confirmation),ALL_APPROVED_COMPONENTS_PRESENT:manifest.length>0&&manifest.every(x=>x.artifactId&&x.filename&&x.sha256),ALL_IMMUTABLE_FILES_HASHED:manifest.length>0&&manifest.every(x=>/^[a-f0-9]{64}$/i.test(String(x.sha256||''))),ANY_CHANGED_COMPONENT_RETAINS_BASELINE_STATUS:false});break;}case 21:{const product=recordsForCurrentScope(project,'products').at(-1),productId=recordId(product,'products'),baselineId=String(recordValue(product,'BASELINE_ID')||product?.relationships?.BASELINE_ID||project.job.CURRENT_BASELINE_ID||''),artifacts=recordsForCurrentScope(project,'artifacts').filter(a=>productId&&String(a.scope?.productId||'')===productId);Object.assign(derived,{PRODUCT_ID:productId||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',BASELINE_ID:baselineId||'NONE',EXECUTION_ID:recordValue(product,'EXECUTION_ID')||'NONE',OUTPUT_FILE_RECORDS:artifacts.map(a=>recordId(a,'artifacts'))});break;}case 22:{const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),tests=recordsForCurrentScope(project,'tests').filter(t=>mandatoryIds.has(testRequirementId(t))&&upper(recordValue(t,'TEST_TYPE'))==='DETERMINISTIC'&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(t,'STATUS')||'READY'))),results=recordsForCurrentScope(project,'deterministicResults'),byTest=new Map();for(const r of results){const id=String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'');if(!byTest.has(id))byTest.set(id,[]);byTest.get(id).push(r);}const determinations=tests.map(t=>{const rows=byTest.get(recordId(t,'tests'))||[];return rows.length===1?effectiveDetermination('deterministicResults',rows[0],t,project):'UNDETERMINED';}),product=recordsForCurrentScope(project,'products').at(-1),productArtifacts=recordsForCurrentScope(project,'artifacts').filter(a=>String(a.scope?.productId||'')===String(currentScope(project).productId||''));Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',PRODUCT_HASHES_BEFORE_TEST:Object.fromEntries(productArtifacts.map(a=>[recordId(a,'artifacts'),recordValue(a,'SHA256')||'UNKNOWN'])),TEST_SUITE_VERSION:project.job.CURRENT_TEST_SUITE_VERSION||'NONE',VALIDATOR_VERSION:globalThis.closedLoopTestRuntime?.version||'EXTERNAL',DETERMINISTIC_TEST_RESULTS:results.length,APPLICABLE_MANDATORY_DETERMINISTIC_TESTS:tests.map(t=>recordId(t,'tests')),EXECUTED:determinations.filter(d=>d!=='UNDETERMINED').length,SATISFIED:determinations.filter(d=>d==='SATISFIED').length,VIOLATED:determinations.filter(d=>d==='VIOLATED').length,UNDETERMINED:determinations.filter(d=>d==='UNDETERMINED').length,MISSING_TEST_RESULTS:tests.filter(t=>(byTest.get(recordId(t,'tests'))||[]).length!==1).map(t=>recordId(t,'tests')),PRODUCT_REJECTED_BY_MANDATORY_FAILURE:determinations.includes('VIOLATED')});break;}case 23:{const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),tests=recordsForCurrentScope(project,'tests').filter(t=>mandatoryIds.has(testRequirementId(t))&&upper(recordValue(t,'TEST_TYPE'))==='MEANING'),results=recordsForCurrentScope(project,'meaningResults'),ds=results.map(r=>effectiveDetermination('meaningResults',r,testForResult(project,r),project)),change=acceptedChanges(project,23).at(-1),reviewer=String(change?.scope?.contextId||''),product=recordsForCurrentScope(project,'products').at(-1),producer=String(recordValue(product,'PRODUCTION_CONTEXT_ID')||product?.relationships?.PRODUCTION_CONTEXT_ID||''),ind=evaluateContextIndependence(project,{role:'MEANING_REVIEW',reviewerContextId:reviewer,productionContextId:producer});Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',REQUIREMENTS_VERSION:project.job.CURRENT_REQUIREMENTS_VERSION||'NONE',MEANING_RUBRIC_VERSION:'MEANING-'+hash.sha256Value(tests.map(t=>t.sha256||recordId(t,'tests'))).slice(0,16).toUpperCase(),EVALUATOR_ID:reviewer||'UNKNOWN',EVALUATOR_INDEPENDENT_FROM_GENERATOR:ind.determination,MEANING_REQUIREMENT_RECORDS:results.length,ACTIVE_MEANING_REQUIREMENTS:tests.length,MEANING_RECORDS_COMPLETED:results.length,SATISFIED:ds.filter(d=>d==='SATISFIED').length,VIOLATED:ds.filter(d=>d==='VIOLATED').length,UNDETERMINED:ds.filter(d=>d==='UNDETERMINED').length,UNSUPPORTED_BARE_CONCLUSIONS:results.filter(r=>!evaluateEvidenceSufficiency(project,{test:testForResult(project,r),result:r}).sufficient).length});break;}case 24:{const mandatoryIds=new Set(mandatoryRequirements(project,currentScope(project)).map(requirementId)),tests=recordsForCurrentScope(project,'tests').filter(t=>mandatoryIds.has(testRequirementId(t))&&upper(recordValue(t,'TEST_TYPE'))==='ADVERSARIAL'),regs=records(project,'regressions').filter(r=>isActiveRecord(r)&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),results=recordsForCurrentScope(project,'adversarialResults'),ds=results.map(r=>effectiveDetermination('adversarialResults',r,testForResult(project,r),project)),change=acceptedChanges(project,24).at(-1),reviewer=String(change?.scope?.contextId||''),product=recordsForCurrentScope(project,'products').at(-1),producer=String(recordValue(product,'PRODUCTION_CONTEXT_ID')||product?.relationships?.PRODUCTION_CONTEXT_ID||''),ind=evaluateContextIndependence(project,{role:'ADVERSARIAL_REVIEW',reviewerContextId:reviewer,productionContextId:producer}),linkedDefects=results.map(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||'')).filter(Boolean),defectRows=records(project,'defects').filter(d=>linkedDefects.includes(recordId(d,'defects')));Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',ADVERSARIAL_REVIEW_VERSION:'ADVERSARIAL-'+hash.sha256Value(results.map(r=>r.sha256||recordId(r,'adversarialResults'))).slice(0,16).toUpperCase(),REVIEWER_ID:reviewer||'UNKNOWN',REVIEWER_INDEPENDENT:ind.determination,ADVERSARIAL_CHECK_RECORDS:results.length,ATTACKS_EXECUTED:results.length,MANDATORY_DEFECTS_FOUND:ds.filter(d=>d==='VIOLATED').length,CRITICAL_DEFECTS_FOUND:defectRows.filter(d=>upper(recordValue(d,'SEVERITY'))==='CRITICAL').length,MAJOR_DEFECTS_FOUND:defectRows.filter(d=>upper(recordValue(d,'SEVERITY'))==='MAJOR').length,UNDETERMINED_ATTACKS:ds.filter(d=>d==='UNDETERMINED').length,REGRESSIONS_FOUND:results.filter(r=>String(recordValue(r,'REG_ID')||r.relationships?.REG_ID||'')).length,RETURN_TO_ROOT_CAUSE_REQUIRED:ds.some(d=>d!=='SATISFIED')});break;}case 25:{const coverage=representationInspectionCoverage(project),baseline=recordsForCurrentScope(project,'baselines').at(-1),product=recordsForCurrentScope(project,'products').at(-1),defects=unresolvedMaterialDefects(project);Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||project.job.CURRENT_PRODUCT_ID||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',REPRESENTATION_REVIEW_VERSION:'REPRESENTATION-'+hash.sha256Value(coverage.details).slice(0,16).toUpperCase(),APPROVED_BASELINE_ID:recordId(baseline,'baselines')||project.job.CURRENT_BASELINE_ID||'NONE',DELIVERY_ARTIFACT_INVENTORY:coverage.details.map(x=>x.artifactId),TRANSFORMATION_CHAIN_RECORDS:coverage.totalTransformationsInspected,PAGE_OR_VIEW_INSPECTION_RECORDS:coverage.totalPagesOrViewsInspected,PACKAGE_INSPECTION_RECORDS:coverage.totalPackagedFilesOpenedOrTested,TOTAL_DELIVERY_ARTIFACTS:coverage.totalDeliveryArtifacts,TOTAL_PAGES_OR_VIEWS_REQUIRED:coverage.totalPagesOrViewsRequired,TOTAL_PAGES_OR_VIEWS_INSPECTED:coverage.totalPagesOrViewsInspected,TOTAL_PACKAGED_FILES_REQUIRED:coverage.totalPackagedFilesRequired,TOTAL_PACKAGED_FILES_OPENED_OR_TESTED:coverage.totalPackagedFilesOpenedOrTested,UNRESOLVED_CRITICAL_DEFECTS:defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='CRITICAL').length,UNRESOLVED_MAJOR_DEFECTS:defects.filter(r=>upper(recordValue(r,'SEVERITY'))==='MAJOR').length,UNRESOLVED_REPRESENTATION_UNKNOWNS:coverage.reasons.length,FINAL_REPRESENTATION_DETERMINATION:coverage.complete?'SATISFIED':'UNDETERMINED'});break;}case 26:{const process=recordsForCurrentScope(project,'processAudits'),productAudits=recordsForCurrentScope(project,'productAudits'),product=recordsForCurrentScope(project,'products').at(-1),baseline=recordsForCurrentScope(project,'baselines').at(-1),allAudits=[...process,...productAudits],missing=allAudits.filter(a=>!evaluateEvidenceSufficiency(project,{result:a}).sufficient).map(a=>recordId(a,process.includes(a)?'processAudits':'productAudits')),defects=[...new Set(allAudits.map(a=>String(recordValue(a,'DEFECT_ID')||a.relationships?.DEFECT_ID||'')).filter(Boolean))],blockers=openBlockers(project,26).map(b=>recordId(b,'blockers'));Object.assign(derived,{PRODUCT_ID:recordId(product,'products')||'NONE',PRODUCT_VERSION:recordValue(product,'PRODUCT_VERSION')||'NONE',BASELINE_ID:recordId(baseline,'baselines')||project.job.CURRENT_BASELINE_ID||'NONE',REVIEW_VERSION:'RECONCILIATION-'+hash.sha256Value(allAudits.map(a=>a.recordSha256||a.sha256||a.id)).slice(0,16).toUpperCase(),MISSING_EVIDENCE_LINKS:missing,RECONCILIATION_DEFECT_IDS:defects,RECONCILIATION_BLOCKER_IDS:blockers});break;}case 27:{const rr=recordsForCurrentScope(project,'releaseRecords').at(-1),allResults=[...recordsForCurrentScope(project,'deterministicResults'),...recordsForCurrentScope(project,'meaningResults'),...recordsForCurrentScope(project,'adversarialResults')];Object.assign(derived,{RELEASE_GATE_ID:recordId(rr,'releaseRecords')||'PENDING',DATE_AND_TIME:rr?.createdAt||now(),PRODUCT_ID:release.productId||'NONE',PRODUCT_VERSION:recordValue(recordsForCurrentScope(project,'products').at(-1),'PRODUCT_VERSION')||'NONE',BASELINE_ID:release.baselineId||'NONE',RECONCILED_REVIEW_VERSION:'RECONCILED-'+hash.sha256Value([...recordsForCurrentScope(project,'processAudits'),...recordsForCurrentScope(project,'productAudits')].map(r=>r.sha256||r.recordSha256||r.id)).slice(0,16).toUpperCase(),TOTAL_MANDATORY_REQUIREMENTS:release.mandatoryRequirementCount,MANDATORY_REQUIREMENTS_WITH_AFFIRMATIVE_SUPPORTING_EVIDENCE:release.satisfied,MANDATORY_REQUIREMENTS_DEMONSTRABLY_VIOLATED:release.violated,MANDATORY_REQUIREMENTS_NOT_ESTABLISHED:release.undetermined,TOTAL_MANDATORY_VALIDATORS:release.validatorCount,MANDATORY_VALIDATORS_SUCCEEDED:release.validatorCount-release.failedValidatorIds.length-release.unknownValidatorIds.length,MANDATORY_VALIDATORS_FAILED:release.failedValidatorIds.length,MANDATORY_VALIDATORS_UNDETERMINED_OR_NOT_RUN:release.unknownValidatorIds.length,UNRESOLVED_CRITICAL_DEFECTS:release.criticalDefects,UNRESOLVED_MAJOR_DEFECTS:release.majorDefects,BLOCKING_REQUIREMENT_IDS:release.blockingRequirements,VIOLATED_REQUIREMENT_IDS:release.violatedRequirements,FAILED_TEST_IDS:release.failedValidatorIds,UNDETERMINED_OR_MISSING_TEST_IDS:release.unknownValidatorIds,UNRESOLVED_DEFECT_IDS:unresolvedMaterialDefects(project).map(d=>recordId(d,'defects')),BLOCKER_IDS:release.blockerIds,SELECTED_RELEASE_STATE:release.determination,CONTROLLING_DECISION_RULE:release.determination==='REJECTED'?'AFFIRMATIVE_CURRENT_NONCONFORMANCE_REJECTS':release.determination==='BLOCKED'?'INSUFFICIENT_OR_CONTRADICTORY_CURRENT_EVIDENCE_BLOCKS':'ALL_CURRENT_RELEASE_CONDITIONS_SATISFIED',CONTROLLING_REASON:release.determination==='ACCEPTED'?'Every mandatory requirement is currently established with sufficient noncontradictory evidence and release-critical identity conditions are satisfied.':release.determination==='REJECTED'?'Current evidence establishes mandatory nonconformance.':'Current evidence is incomplete, insufficient, stale, blocked, or contradictory.',AFFIRMATIVE_EVIDENCE:release.inputReferences});break;}case 28:Object.assign(derived,DERIVATIONS['stage28.artifactIdentity'](project).value);break;case 29:Object.assign(derived,DERIVATIONS['stage29.evidenceChains'](project).value);break;case 30:{const defects=records(project,'defects',{active:false}),confirmed=confirmedDefects(project,{allScopes:true}),regs=records(project,'regressions',{active:false}),activeRegs=regs.filter(r=>isActiveRecord(r)&&upper(recordValue(r,'ACTIVE_RETIRED_STATE')||'ACTIVE')!=='RETIRED'),retiredRegs=regs.filter(r=>upper(recordValue(r,'ACTIVE_RETIRED_STATE'))==='RETIRED'),execs=records(project,'regressionExecutions',{active:false}),missingFields=defects.filter(d=>['OBSERVED_FAILURE','EXPECTED_CONDITION','SEVERITY','STATUS'].some(k=>adjudicationEmpty(recordValue(d,k)))).map(d=>recordId(d,'defects')),covered=new Set(regs.map(r=>String(recordValue(r,'DEFECT_ID')||r.relationships?.DEFECT_ID||''))),missingRegs=confirmed.filter(d=>!covered.has(recordId(d,'defects'))).map(d=>recordId(d,'defects'));Object.assign(derived,{DEFECT_REGISTRY_VERSION:'DEFECT-REGISTRY-'+hash.sha256Value(defects.map(d=>d.recordSha256||d.sha256||recordId(d,'defects'))).slice(0,16).toUpperCase(),REGRESSION_REGISTRY_VERSION:'REGRESSION-REGISTRY-'+hash.sha256Value(regs.map(r=>r.recordSha256||r.sha256||recordId(r,'regressions'))).slice(0,16).toUpperCase(),REGISTRY_IS_APPEND_ONLY:true,DEFECT_RECORDS:defects.length,REGRESSION_RECORDS:regs.length,FUTURE_BASELINE_REGRESSION_EXECUTION_RECORDS:execs.length,TOTAL_DEFECT_RECORDS:defects.length,TOTAL_CONFIRMED_DEFECTS_WITH_REGRESSION_TESTS:confirmed.length-missingRegs.length,TOTAL_ACTIVE_REGRESSION_TESTS:activeRegs.length,TOTAL_RETIRED_REGRESSION_TESTS:retiredRegs.length,DEFECT_RECORDS_MISSING_REQUIRED_FIELDS:missingFields,CONFIRMED_DEFECTS_MISSING_REGRESSION_TESTS:missingRegs,FINAL_REGISTRY_DETERMINATION:missingFields.length||missingRegs.length?'UNDETERMINED':'SATISFIED'});break;}}derived.STAGE_DECISION=project.stages[stage].status==='COMPLETE'?'READY TO PROCEED':project.stages[stage].status==='BLOCKED'?'BLOCKED':'NOT READY - CORRECTION REQUIRED';derived.DECISION_EVIDENCE=project.stages[stage].gate?.reasons?.length?project.stages[stage].gate.reasons.join('; '):'Canonical current-scope records and deterministic calculations satisfy the stage gate.';return derived;}

function recalculate(project){
  ensureShape(project);
  project.job.INPUT_SET_HASH_OR_MANIFEST=intakeCoverageManifest(project).manifestSha256;
  let previousComplete=true;
  for(let stage=1;stage<=30;stage++){
    let state=project.stages[stage];
    if(!previousComplete){
      const prerequisite=`Stage ${String(stage-1).padStart(2,'0')} is not complete.`;
      state.status='NOT STARTED';
      state.gate={complete:false,blocked:false,reasons:[prerequisite]};
      state.decision='';
      state.decisionEvidence=prerequisite;
      state.derivedData={STAGE_DECISION:'NOT READY - CORRECTION REQUIRED',DECISION_EVIDENCE:prerequisite};
      continue;
    }
    const wasComplete=state.status==='COMPLETE',result=gate(stage,project);
    // gate() normalizes project shape and may replace stage objects; always mutate the current canonical object.
    state=project.stages[stage];
    state.gate=result;
    if(result.blocked){state.status='BLOCKED';}
    else if(result.complete){state.status='COMPLETE';}
    else if(hasStageActivity(project,stage)){state.status='IN PROGRESS';}
    else state.status='READY';
    state.decision=state.status==='COMPLETE'?'READY TO PROCEED':state.status==='BLOCKED'?'BLOCKED':'';
    state.decisionEvidence=result.reasons.length?result.reasons.join('; '):'Derived canonical stage gate satisfied.';
    state.derivedData=deriveStageData(project,stage);
    previousComplete=state.status==='COMPLETE';
  }
  const completed=Object.values(project.stages).filter(state=>state.status==='COMPLETE').length;
  const currentStage=completed===30?30:Math.max(1,Object.values(project.stages).find(state=>state.status!=='COMPLETE')?.number||30);
  const current=project.stages[currentStage];
  project.activeStage=Math.max(1,Math.min(30,Number(project.activeStage||currentStage)));
  project.job.CURRENT_STAGE=`STAGE ${String(currentStage).padStart(2,'0')}`;
  const projectOpenBlockers=openBlockers(project);project.job.CURRENT_STATE=projectOpenBlockers.length?'BLOCKED':completed===30?'COMPLETE':current.status==='BLOCKED'?'BLOCKED':current.status==='IN PROGRESS'?'IN PROGRESS':'READY';
  project.job.CURRENT_BLOCKERS=projectOpenBlockers.length?projectOpenBlockers.map(record=>recordId(record,'blockers')).join(', '):'NONE';
  project.job.NEXT_REQUIRED_ACTION=completed===30?actionEnvelope(project,currentStage,{actionType:'COMPLETE',heading:'Workflow complete',explanation:'Preserve the completed workflow and exact release evidence.',primaryButton:null}):operationalNextAction(project,currentStage);
  project.job.LATEST_EVIDENCE_REFERENCE=safe(project.projectData.acceptedChanges).at(-1)?.changeId||project.job.LATEST_EVIDENCE_REFERENCE||'NONE';
  project.job.JOB_RECORD_STATUS=project.stages[1].status==='COMPLETE'?'READY':'NOT READY';
  project.job.STATUS_EVIDENCE=project.stages[1].gate?.reasons?.join('; ')||'Stage 01 canonical evidence is complete.';
  return project;
}

function invalidateDownstream(project,stage,changeId,reason='Material upstream change'){ensureShape(project);const invalidatedStages=[];for(let number=Number(stage)+1;number<=30;number++){const state=project.stages[number];if(state.status!=='NOT STARTED'||state.acceptedDataChangeIds.length||state.acceptedControlEventIds.length)invalidatedStages.push(number);state.status='NOT STARTED';state.invalidatedBy=changeId;state.gate={complete:false,blocked:false,reasons:['Invalidated by '+changeId+': '+reason+'.']};for(const collection of Object.keys(schema.RECORD_SCHEMAS)){for(const record of records(project,collection,{stage:number,active:false}))if(!record.invalidatedBy){record.invalidatedBy=changeId;record.active=false;record.validity='INVALIDATED';refreshRecordHashes(record,collection);}}for(const change of safe(project.projectData.acceptedChanges).filter(x=>Number(x.stage)===number&&!x.invalidatedBy))change.invalidatedBy=changeId;for(const prompt of safe(project.projectData.generatedPrompts).filter(x=>Number(x.stage)===number&&!x.invalidatedBy))prompt.invalidatedBy=changeId;for(const proposal of safe(project.projectData.responseProposals).filter(x=>Number(x.stage)===number&&!x.invalidatedBy&&x.status==='PENDING_OPERATOR_REVIEW')){proposal.invalidatedBy=changeId;proposal.status='STALE';}for(const confirmation of safe(project.projectData.stageConfirmations).filter(x=>Number(x.stage)===number&&!x.invalidatedBy))confirmation.invalidatedBy=changeId;}for(const collection of ['releaseRecords','artifactIdentities','evidenceChains','convergenceRecords'])for(const record of records(project,collection,{active:false}))if(!record.invalidatedBy){record.invalidatedBy=changeId;record.active=false;record.validity='INVALIDATED';}project.release.gateState='';project.release.authorization='NOT AUTHORIZED';project.release.authorizedArtifactIds=[];addHistory(project,'DOWNSTREAM_INVALIDATED',{stage,changeId,reason,invalidatedStages});recalculate(project);return invalidatedStages;}

function invalidateStageForAuthorityChange(project,{stage=project.activeStage,reason='Authoritative human input changed.',operatorLabel='HUMAN_OPERATOR'}={}){ensureShape(project);const number=Number(stage),event=addHistory(project,'STAGE_AUTHORITY_CHANGED',{stage:number,reason,operatorLabel,identityAssurance:'SELF_ASSERTED'}),id=event.eventId,changes=acceptedChanges(project,number),proposalIds=new Set(changes.map(c=>c.proposalId));for(const change of changes)change.invalidatedBy=id;for(const prompt of safe(project.projectData.generatedPrompts).filter(x=>Number(x.stage)===number&&!x.invalidatedBy))prompt.invalidatedBy=id;for(const proposal of safe(project.projectData.responseProposals).filter(x=>Number(x.stage)===number&&!x.invalidatedBy&&x.status==='PENDING_OPERATOR_REVIEW')){proposal.invalidatedBy=id;proposal.status='STALE';}for(const confirmation of safe(project.projectData.stageConfirmations).filter(x=>Number(x.stage)===number&&!x.invalidatedBy))confirmation.invalidatedBy=id;for(const collection of Object.keys(schema.RECORD_SCHEMAS))for(const record of records(project,collection,{stage:number,active:false}))if(!record.invalidatedBy&&(proposalIds.has(record.sourceProposalId)||changes.some(c=>c.rawResponseId&&c.rawResponseId===record.rawResponseId))){record.invalidatedBy=id;record.active=false;record.validity='INVALIDATED';refreshRecordHashes(record,collection);}const state=project.stages[number];state.agentData={};state.acceptedData={};state.acceptedDataChangeIds=[];state.acceptedResponseIds=[];state.currentPromptId=null;if(number<30)invalidateDownstream(project,number,id,reason);else recalculate(project);return event;}

function recordHumanInputVersion(project,changedFields,operator='HUMAN_OPERATOR'){
  ensureShape(project);
  const payload={...Object.fromEntries(schema.HUMAN_INTAKE_FIELDS.map(name=>[name,project.job[name]??''])),clarifications:clone(project.projectData.userEntered?.clarifications||[]),suppliedArtifactFiles:clone(project.projectData.userEntered?.suppliedArtifactFiles||{}),suppliedArtifactText:clone(project.projectData.userEntered?.suppliedArtifactText||{})};
  const sha256=hash.sha256Value(payload);
  const latest=safe(project.projectData.inputVersions).at(-1);
  if(latest?.sha256===sha256)return latest;
  const version=nextVersion(project.job.CURRENT_INPUT_VERSION,'INPUT');
  const record={inputVersionId:allocateInfrastructureId(project,'INPUT-VERSION','inputVersions'),version,sha256,createdAt:now(),operator,changedFields:[...changedFields],payload};
  project.projectData.inputVersions.push(record);
  project.job.CURRENT_INPUT_VERSION=version;
  project.job.INPUT_SET_HASH_OR_MANIFEST=sha256;
  project.projectData.userEntered={...project.projectData.userEntered,...payload};
  addHistory(project,'USER_JOB_INPUT_VERSIONED',{recordId:record.inputVersionId,version,changedFields:[...changedFields],sha256});
  return record;
}

function recordStageConfirmation(project,stage,confirmed,statement,operatorLabel='HUMAN_OPERATOR',options={}){
  ensureShape(project);const number=Number(stage);const latest=acceptedChanges(project,number).at(-1);if(number===1&&!latest)throw new Error('Stage 01 confirmation requires a current accepted DATA_PROPOSAL.');
  for(const prior of safe(project.projectData.stageConfirmations).filter(x=>Number(x.stage)===number&&!x.invalidatedBy))prior.invalidatedBy=options.invalidatedBy||'SUPERSEDED_CONFIRMATION';
  const history=addHistory(project,'HUMAN_STAGE_CONFIRMATION_RECORDED',{stage:number,confirmed:Boolean(confirmed)});
  const prompt=safe(project.projectData.generatedPrompts).filter(x=>Number(x.stage)===number&&!x.invalidatedBy).at(-1);
  const record={confirmationId:allocateInfrastructureId(project,'STAGE-CONFIRMATION','stageConfirmations'),stage:number,confirmed:Boolean(confirmed),statement:String(statement||''),acceptedChangeId:options.acceptedChangeId||latest?.changeId||null,inputVersion:options.inputVersion||project.job.CURRENT_INPUT_VERSION||null,instructionId:options.instructionId||prompt?.instructionId||prompt?.promptId||null,contextSignature:options.contextSignature||prompt?.contextSignature||null,operatorLabel:String(options.operatorLabel||operatorLabel||'UNSPECIFIED'),identityAssurance:'SELF_ASSERTED',eventSequence:history.eventSequence,deviceTimestamp:history.deviceTimestamp,createdAt:history.deviceTimestamp};
  project.projectData.stageConfirmations.push(record);recalculate(project);return record;
}

function recordReleaseDetermination(project){ensureShape(project);const metrics=releaseMetrics(project);const releaseEvidenceSha256=hash.sha256Value({metrics,inputReferences:metrics.inputReferences});const existing=records(project,'releaseRecords').find(r=>r.releaseEvidenceSha256===releaseEvidenceSha256);if(existing)return existing;for(const prior of records(project,'releaseRecords',{active:false}))if(!prior.invalidatedBy){prior.active=false;prior.validity='SUPERSEDED';prior.invalidatedBy='RELEASE-EVIDENCE-CHANGED';refreshRecordHashes(prior,'releaseRecords');}project.release.authorization='NOT AUTHORIZED';project.release.authorizedArtifactIds=[];const id=allocateId(project,'releaseRecords');const fields={RELEASE_ID:id,PRODUCT_ID:metrics.productId||'UNKNOWN',BASELINE_ID:metrics.baselineId||'UNKNOWN',DETERMINATION:metrics.determination,MANDATORY_REQUIREMENT_COUNTS:metrics.mandatoryRequirementCount,AFFIRMATIVE_EVIDENCE_COUNTS:metrics.satisfied,VIOLATED_COUNTS:metrics.violated,UNDETERMINED_COUNTS:metrics.undetermined,VALIDATOR_COUNTS:metrics.validatorCount,FAILED_VALIDATORS:metrics.failedValidatorIds,UNKNOWN_VALIDATORS:metrics.unknownValidatorIds,CRITICAL_DEFECTS:metrics.criticalDefects,MAJOR_DEFECTS:metrics.majorDefects,BLOCKING_REQUIREMENTS:metrics.blockingRequirements,VIOLATIONS:metrics.violatedRequirements,BLOCKERS:metrics.blockerIds,CONTROLLING_EVIDENCE:releaseEvidenceSha256};const record={id,stage:27,createdAt:now(),active:true,fields,...fields,releaseEvidenceSha256,scope:currentScope(project),source:'APPLICATION_DERIVATION',derivationKey:'stage27.release'};refreshRecordHashes(record,'releaseRecords');project.projectData.releaseRecords.push(record);project.release.gateState=metrics.determination;addHistory(project,'RELEASE_DETERMINATION_CALCULATED',{recordId:id,determination:metrics.determination,releaseEvidenceSha256});recalculate(project);return record;}

function constructEvidenceChains(project){ensureShape(project);const requirements=mandatoryRequirements(project,currentScope(project)),instructions=recordsForCurrentScope(project,'instructions'),product=recordsForCurrentScope(project,'products').at(-1),productId=recordId(product,'products'),executionId=String(recordValue(product,'EXECUTION_ID')||'').trim(),release=recordsForCurrentScope(project,'releaseRecords').at(-1),identities=recordsForCurrentScope(project,'artifactIdentities'),traces=recordsForCurrentScope(project,'instructionTraces'),created=[];
  for(const requirement of requirements){const reqId=requirementId(requirement),sourceId=String(recordValue(requirement,'SOURCE_ID')||requirement.relationships?.SOURCE_ID||recordValue(requirement,'USER_INPUT_RELATIONSHIP')||''),trace=traces.find(r=>String(recordValue(r,'REQ_ID')||r.relationships?.REQ_ID||'')===reqId),instructionId=String(recordValue(trace,'INSTRUCTION_ID')||trace?.relationships?.INSTRUCTION_ID||'').trim(),instruction=instructionId?instructions.find(r=>recordId(r,'instructions')===instructionId):null,tests=applicableTests(project,requirement),results=[...recordsForCurrentScope(project,'verification'),...recordsForCurrentScope(project,'deterministicResults'),...recordsForCurrentScope(project,'meaningResults'),...recordsForCurrentScope(project,'adversarialResults')].filter(r=>resultRequirementId(project,r)===reqId),evidenceIds=new Set(),missing=[];
    for(const r of results)for(const id of evidenceReferences(r))evidenceIds.add(id);if(!sourceId)missing.push('AUTHORITY');if(!trace)missing.push('INSTRUCTION_TRACE');if(!instruction)missing.push('INSTRUCTION');if(!productId)missing.push('PRODUCT');if(productId&&!executionId)missing.push('EXECUTION');for(const test of tests){const tid=recordId(test,'tests'),testResults=results.filter(r=>String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'')===tid);if(!testResults.length)missing.push('TEST_RESULT:'+tid);else for(const result of testResults){const collection=['verification','deterministicResults','meaningResults','adversarialResults'].find(name=>recordsForCurrentScope(project,name).includes(result))||'verification',effective=effectiveDetermination(collection,result,test,project),contract=evaluateEvidenceContract(test,result,null,project),sufficiency=evaluateEvidenceSufficiency(project,{requirement,test,result});if(effective!=='SATISFIED')missing.push('NON_SATISFIED_EFFECTIVE_RESULT:'+tid);if(!contract.sufficient||!sufficiency.sufficient)missing.push('INSUFFICIENT_EVIDENCE:'+tid);}}if(!tests.length)missing.push('TEST');if(!evidenceIds.size)missing.push('CANONICAL_EVIDENCE');if(!release)missing.push('RELEASE_DECISION');else if(upper(recordValue(release,'DETERMINATION'))!=='ACCEPTED')missing.push('RELEASE_NOT_ACCEPTED');const authorizedIdentities=identities.filter(r=>upper(recordValue(r,'AUTHORIZATION'))==='AUTHORIZED'&&truth(recordValue(r,'EXACT_HASH_MATCH'))&&truth(recordValue(r,'EXACT_SIZE_MATCH')));if(!identities.length||authorizedIdentities.length!==identities.length)missing.push('DELIVERY_ARTIFACT_IDENTITY');
    const prior=records(project,'evidenceChains',{active:false}).find(r=>String(recordValue(r,'REQ_ID')||r.relationships?.REQ_ID||'')===reqId&&!r.invalidatedBy),id=prior?recordId(prior,'evidenceChains'):allocateId(project,'evidenceChains'),fields={CHAIN_ID:id,REQ_ID:reqId,AUTHORITY_ID:sourceId||'UNKNOWN',INSTRUCTION_ID:instruction?instructionId:'UNKNOWN',EXECUTION_ID:executionId||'UNKNOWN',PRODUCT_ELEMENT:productId||'UNKNOWN',TEST_ID:tests.map(t=>recordId(t,'tests')),TEST_RESULT_ID:results.map(r=>r.id||r.recordId).filter(Boolean),EVIDENCE_ID:[...evidenceIds],RELEASE_DECISION_ID:recordId(release,'releaseRecords')||'UNKNOWN',ARTIFACT_HASH_IDENTITY:identities.map(r=>recordId(r,'artifactIdentities')),STATUS:missing.length?'INCOMPLETE':'COMPLETE',MISSING_LINKS:[...new Set(missing)]},record={id,stage:29,createdAt:prior?.createdAt||now(),updatedAt:now(),active:true,fields,...fields,scope:currentScope(project),source:'APPLICATION_DERIVATION',derivationKey:'stage29.evidenceChains'};refreshRecordHashes(record,'evidenceChains');if(prior)Object.assign(prior,record);else project.projectData.evidenceChains.push(record);created.push(record);}
  addHistory(project,'EVIDENCE_CHAINS_CONSTRUCTED',{count:created.length,complete:created.filter(r=>r.STATUS==='COMPLETE').length});recalculate(project);return created;
}
function verifyArtifactIdentity(project,audited,delivery){
  ensureShape(project);const release=records(project,'releaseRecords').at(-1);if(upper(recordValue(release,'DETERMINATION'))!=='ACCEPTED')throw new Error('Artifact identity verification is prohibited until Stage 27 is ACCEPTED.');
  const a=safe(audited),d=safe(delivery),duplicate=(list,key)=>{const seen=new Set();for(const x of list){const v=String(x[key]??'');if(!v||seen.has(v))return true;seen.add(v);}return false;};if(duplicate(a,'artifactId')||duplicate(d,'artifactId')||duplicate(a,'name')||duplicate(d,'name'))throw new Error('Duplicate artifact identity or filename is prohibited.');if(a.length!==d.length)throw new Error('Audited and delivery artifact counts differ.');
  const normalized=list=>list.map(x=>({artifactId:String(x.artifactId||''),name:String(x.name||''),version:String(x.version||'UNKNOWN'),storageReference:String(x.storageReference||'INDEXEDDB'),size:Number(x.size),sha256:String(x.sha256||'')})).sort((x,y)=>x.artifactId.localeCompare(y.artifactId)||x.name.localeCompare(y.name));
  const identityEvidenceSha256=hash.sha256Value({releaseId:recordId(release,'releaseRecords'),audited:normalized(a),delivery:normalized(d)}),existing=records(project,'artifactIdentities').filter(r=>r.identityEvidenceSha256===identityEvidenceSha256);if(existing.length){project.release.authorization=existing.every(r=>r.AUTHORIZATION==='AUTHORIZED')?'AUTHORIZED':'NOT AUTHORIZED';project.release.authorizedArtifactIds=existing.map(r=>r.ARTIFACT_ID);recalculate(project);return existing;}
  const supersessionId=`ARTIFACT-IDENTITY-EVIDENCE-${identityEvidenceSha256}`;for(const prior of records(project,'artifactIdentities',{active:false}))if(isActiveRecord(prior)){prior.active=false;prior.validity='SUPERSEDED';prior.invalidatedBy=supersessionId;refreshRecordHashes(prior,'artifactIdentities');}
  const byId=new Map(d.map(x=>[String(x.artifactId),x])),created=[];for(const left of a){const right=byId.get(String(left.artifactId));if(!right)throw new Error('Missing delivery artifact '+left.artifactId+'.');const id=allocateId(project,'artifactIdentities');const fields={IDENTITY_ID:id,ARTIFACT_ID:left.artifactId,AUDITED_FILENAME:left.name,AUDITED_VERSION:left.version||'UNKNOWN',AUDITED_STORAGE_REFERENCE:left.storageReference||'INDEXEDDB',AUDITED_BYTE_SIZE:left.size,AUDITED_SHA256:left.sha256,RELEASE_FILENAME:right.name,RELEASE_VERSION:right.version||'UNKNOWN',RELEASE_STORAGE_REFERENCE:right.storageReference||'INDEXEDDB',RELEASE_BYTE_SIZE:right.size,PRE_DELIVERY_SHA256:right.sha256,EXACT_HASH_MATCH:Boolean(left.sha256&&right.sha256&&left.sha256===right.sha256),EXACT_SIZE_MATCH:Number(left.size)===Number(right.size),POST_AUDIT_MODIFICATION_EVIDENCE:left.sha256===right.sha256?'NONE':'MISMATCH',AUTHORIZATION:'NOT AUTHORIZED'};fields.AUTHORIZATION=fields.EXACT_HASH_MATCH&&fields.EXACT_SIZE_MATCH&&fields.AUDITED_FILENAME===fields.RELEASE_FILENAME?'AUTHORIZED':'NOT AUTHORIZED';const record={id,stage:28,createdAt:now(),active:true,fields,...fields,scope:currentScope(project),identityEvidenceSha256,source:'APPLICATION_DERIVATION',derivationKey:'stage28.artifactIdentity'};refreshRecordHashes(record,'artifactIdentities');project.projectData.artifactIdentities.push(record);created.push(record);}project.release.authorization=created.length&&created.every(r=>r.AUTHORIZATION==='AUTHORIZED')?'AUTHORIZED':'NOT AUTHORIZED';project.release.authorizedArtifactIds=created.map(r=>r.ARTIFACT_ID);addHistory(project,'ARTIFACT_IDENTITY_VERIFIED',{count:created.length,authorization:project.release.authorization,identityEvidenceSha256});recalculate(project);return created;
}

function refreshRecordHashes(record,collection){
  if(!record)return record;const definition=schema.RECORD_SCHEMAS[collection];if(!definition)return record;record.contentSha256=hash.contentRecordSha256(record,definition.idField);record.recordSha256=hash.recordSha256(record);record.sha256=record.recordSha256;return record;
}
function commandRecord(project,collection,fields,{stage=null,source='APPLICATION_COMMAND',scope=null}={}){
  ensureShape(project);const definition=schema.RECORD_SCHEMAS[collection];if(!definition)throw new Error(`Unknown canonical collection: ${collection}.`);
  const id=allocateId(project,collection),deviceTimestamp=now(),full={...clone(fields),[definition.idField]:id};
  const record={id,stage:(stage??Number(project.activeStage||0))||null,createdAt:deviceTimestamp,updatedAt:deviceTimestamp,active:true,scope:scope||currentScope(project),fields:full,...full,source};
  refreshRecordHashes(record,collection);
  project.projectData[collection].push(record);return record;
}
function createHumanBlocker(project,{stage=project.activeStage,reason,operatorLabel='HUMAN_OPERATOR'}={}){
  if(!String(reason||'').trim())throw new Error('A blocker reason is required.');
  const record=commandRecord(project,'blockers',{MISSING_ITEM_TYPE:'UNRESOLVED',MISSING_FACT_INPUT_AUTHORITY_EVIDENCE_CAPABILITY_DECISION_RULE:String(reason).trim(),AFFECTED_REQUIREMENTS:'UNKNOWN',AFFECTED_TESTS:'UNKNOWN',AFFECTED_ARTIFACTS:'UNKNOWN',WHY_WORK_CANNOT_CONTINUE:String(reason).trim(),ATTEMPTED_RESOLUTIONS:'NONE',DOWNSTREAM_WORK_STOPPED:`STAGE ${String(stage).padStart(2,'0')} AND DEPENDENT WORK`,OWNER:operatorLabel,STATUS:'OPEN',RESOLUTION_EVIDENCE:'NONE',CLOSURE:'OPEN',REEVALUATION:'REQUIRED',REQUIRED_REVALIDATION:'REQUIRED'},{stage,source:'HUMAN_DECISION'});
  addHistory(project,'HUMAN_BLOCKER_CREATED',{stage,recordId:record.id,operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return record;
}
function reconcileArtifactCustodyVerification(project,{report,stage=project.activeStage}={}){
  ensureShape(project);if(!report||typeof report!=='object')throw new Error('An application artifact-verification report is required.');const reportRows=safe(report.artifacts),canonicalArtifacts=records(project,'artifacts').filter(isActiveRecord),byId=new Map(canonicalArtifacts.map(record=>[recordId(record,'artifacts'),record])),open=records(project,'blockers',{active:false}).filter(record=>record.source==='APPLICATION'&&record.applicationBlockerKind==='ARTIFACT_CUSTODY'&&isActiveRecord(record)&&['OPEN','BLOCKED','IN PROGRESS','UNKNOWN'].includes(upper(recordValue(record,'STATUS')||'OPEN'))),verifiedRows=new Map(reportRows.filter(item=>item?.verified===true&&item.canonicalExpectation!==false).map(item=>[String(item.artifactId||''),item]));let changed=false;
  if(report.verified){const unresolved=[];for(const artifact of canonicalArtifacts){const artifactId=recordId(artifact,'artifacts'),item=verifiedRows.get(artifactId),matches=item&&String(item.filename||'')===String(recordValue(artifact,'FILENAME')||'')&&Number(item.actualByteSize)===Number(recordValue(artifact,'BYTE_SIZE'))&&String(item.actualSha256||'').toLowerCase()===String(recordValue(artifact,'SHA256')||'').toLowerCase();if(!matches){unresolved.push(artifactId);continue;}if(upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'){artifact.fields=artifact.fields&&typeof artifact.fields==='object'?artifact.fields:{};artifact.fields.AVAILABILITY='BYTES_PERSISTED_AND_VERIFIED';artifact.AVAILABILITY='BYTES_PERSISTED_AND_VERIFIED';artifact.updatedAt=now();refreshRecordHashes(artifact,'artifacts');changed=true;}}
    if(unresolved.length)return reconcileArtifactCustodyVerification(project,{report:{...report,verified:false,artifacts:[...reportRows,...unresolved.filter(id=>!reportRows.some(item=>String(item.artifactId)===id)).map(artifactId=>({artifactId,verified:false,issue:'CANONICAL_ARTIFACT_NOT_REVERIFIED'}))]},stage});
    for(const blocker of open){const updates={STATUS:'RESOLVED',RESOLUTION_EVIDENCE:`Application re-read ${report.artifactCount} stored Blob(s); all required canonical byte identities reconciled at ${report.at||now()}.`,CLOSURE:'CLOSED',REEVALUATION:'COMPLETED',REQUIRED_REVALIDATION:'RECALCULATED'};blocker.fields=blocker.fields&&typeof blocker.fields==='object'?blocker.fields:{};for(const [key,value] of Object.entries(updates)){blocker.fields[key]=value;blocker[key]=value;}blocker.updatedAt=now();blocker.resolvedBy='APPLICATION';refreshRecordHashes(blocker,'blockers');addHistory(project,'APPLICATION_ARTIFACT_CUSTODY_RESTORED',{recordId:recordId(blocker,'blockers'),artifactIds:[...verifiedRows.keys()],verificationAt:report.at||now(),downstreamRevalidationStillRequired:true});changed=true;}if(changed)recalculate(project);return {verified:true,changed,resolvedBlockerIds:open.map(record=>recordId(record,'blockers')),restoredArtifactIds:[...verifiedRows.keys()]};}
  const failures=reportRows.filter(item=>!item.verified),artifactIds=[...new Set(failures.map(item=>String(item.artifactId||'')).filter(Boolean))],reason=`Stored artifact custody verification failed: ${failures.map(item=>`${item.artifactId||'UNKNOWN'} (${item.issue||'byte/hash/metadata mismatch'})`).join(', ')||'required canonical bytes are missing or mismatched'}.`;for(const artifactId of artifactIds){const artifact=byId.get(artifactId);if(artifact&&upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_UNAVAILABLE_OR_UNVERIFIED'){artifact.fields=artifact.fields&&typeof artifact.fields==='object'?artifact.fields:{};artifact.fields.AVAILABILITY='BYTES_UNAVAILABLE_OR_UNVERIFIED';artifact.AVAILABILITY='BYTES_UNAVAILABLE_OR_UNVERIFIED';artifact.updatedAt=now();refreshRecordHashes(artifact,'artifacts');changed=true;}}
  let blocker=open[0];if(!blocker){blocker=commandRecord(project,'blockers',{MISSING_ITEM_TYPE:'ARTIFACT_CUSTODY',MISSING_FACT_INPUT_AUTHORITY_EVIDENCE_CAPABILITY_DECISION_RULE:reason,AFFECTED_REQUIREMENTS:'UNKNOWN',AFFECTED_TESTS:'UNKNOWN',AFFECTED_ARTIFACTS:artifactIds.length?artifactIds:'UNKNOWN',WHY_WORK_CANNOT_CONTINUE:reason,ATTEMPTED_RESOLUTIONS:'APPLICATION BYTE RE-READ AND SHA-256 VERIFICATION',DOWNSTREAM_WORK_STOPPED:'RELEASE AND DEPENDENT WORK',OWNER:'APPLICATION',STATUS:'OPEN',RESOLUTION_EVIDENCE:'NONE',CLOSURE:'OPEN',REEVALUATION:'REQUIRED',REQUIRED_REVALIDATION:'REQUIRED'},{stage,source:'APPLICATION'});blocker.applicationBlockerKind='ARTIFACT_CUSTODY';refreshRecordHashes(blocker,'blockers');addHistory(project,'APPLICATION_ARTIFACT_CUSTODY_BLOCKED',{recordId:recordId(blocker,'blockers'),artifactIds,verificationAt:report.at||now()});changed=true;}
  const authorizedDeliveries=safe(project.projectData.deliveryRecords).filter(record=>isActiveRecord(record)&&upper(recordValue(record,'DELIVERY_STATE'))==='AUTHORIZED');if(project.release.authorization!=='NOT AUTHORIZED'||safe(project.release.authorizedArtifactIds).length){project.release.authorization='NOT AUTHORIZED';project.release.authorizedArtifactIds=[];changed=true;}for(const collection of ['artifactIdentities','evidenceChains','releaseRecords'])for(const record of records(project,collection,{active:false}))if(isActiveRecord(record)){record.active=false;record.validity='INVALIDATED';record.invalidatedBy=recordId(blocker,'blockers');refreshRecordHashes(record,collection);changed=true;}if(authorizedDeliveries.length){const lifecycleCommand=globalThis.closedLoopWorkflowEngine?.withdrawDeliveryAuthorization;if(typeof lifecycleCommand!=='function')throw new Error('Delivery lifecycle authority is unavailable during artifact-custody invalidation.');lifecycleCommand(project,{sourceRecords:authorizedDeliveries,changeId:recordId(blocker,'blockers'),reason:`Final delivery authorization was withdrawn for future use because exact artifact custody verification failed for: ${artifactIds.join(', ')||'required current artifacts'}.`});changed=true;}for(const backup of records(project,'backupRecords',{active:false}))if(isActiveRecord(backup)&&safe(recordValue(backup,'ARTIFACT_IDS')).some(id=>artifactIds.includes(String(id)))&&upper(recordValue(backup,'STATUS'))==='CURRENT'){backup.fields=backup.fields&&typeof backup.fields==='object'?backup.fields:{};backup.fields.STATUS='STALE';backup.STATUS='STALE';backup.updatedAt=now();refreshRecordHashes(backup,'backupRecords');changed=true;}for(const reservation of records(project,'operationReservations',{active:false}))if(isActiveRecord(reservation)&&['ACTIVE','ORPHANED'].includes(upper(recordValue(reservation,'STATUS')))){reservation.fields=reservation.fields&&typeof reservation.fields==='object'?reservation.fields:{};reservation.fields.STATUS='SUPERSEDED';reservation.STATUS='SUPERSEDED';reservation.active=false;reservation.validity='SUPERSEDED';reservation.invalidatedBy=recordId(blocker,'blockers');refreshRecordHashes(reservation,'operationReservations');changed=true;}if(changed)recalculate(project);return {verified:false,changed,blockerId:recordId(blocker,'blockers'),unavailableArtifactIds:artifactIds};
}
function resolveHumanBlocker(project,{blockerId,resolutionEvidence,operatorLabel='HUMAN_OPERATOR'}={}){
  ensureShape(project);const id=String(blockerId||'').trim(),evidence=String(resolutionEvidence||'').trim();if(!id)throw new Error('A blocker identity is required.');if(!evidence)throw new Error('Resolution evidence is required.');const blocker=records(project,'blockers',{active:false}).find(record=>recordId(record,'blockers')===id);if(!blocker)throw new Error(`Blocker ${id} does not exist.`);if(blocker.source!=='HUMAN_DECISION')throw new Error('Only independently human-created blockers may be resolved by this human authority control.');if(!isActiveRecord(blocker)||!['OPEN','BLOCKED','IN PROGRESS','UNKNOWN'].includes(upper(recordValue(blocker,'STATUS')||'OPEN')))throw new Error(`Blocker ${id} is not open.`);
  const updates={STATUS:'RESOLVED',RESOLUTION_EVIDENCE:evidence,CLOSURE:'CLOSED',REEVALUATION:'COMPLETED',REQUIRED_REVALIDATION:'RECALCULATED'};blocker.fields=blocker.fields&&typeof blocker.fields==='object'?blocker.fields:{};for(const [key,value] of Object.entries(updates)){blocker.fields[key]=value;blocker[key]=value;}blocker.updatedAt=now();blocker.resolvedBy=operatorLabel;blocker.identityAssurance='SELF_ASSERTED';blocker.contentSha256=hash.contentRecordSha256?hash.contentRecordSha256(blocker,'BLOCKER_ID'):hash.sha256Value(blocker.fields);blocker.recordSha256=hash.recordSha256?hash.recordSha256(blocker):hash.sha256Value(blocker);blocker.sha256=blocker.recordSha256;addHistory(project,'HUMAN_BLOCKER_RESOLVED',{stage:Number(blocker.stage||0),recordId:id,operatorLabel,identityAssurance:'SELF_ASSERTED',resolutionEvidence:evidence});recalculate(project);return blocker;
}
function registerFreshContext(project,{stage=project.activeStage,externalContextIdentifier,operatorLabel='HUMAN_OPERATOR',purpose='GENERAL'}={}){
  ensureShape(project);const identifier=String(externalContextIdentifier||'').trim();const placeholder=new Set(['','UNASSIGNED','UNKNOWN','PENDING','NOT APPLICABLE','NONE']);if(placeholder.has(upper(identifier)))throw new Error('A real external fresh-context identifier is required.');
  const iterationId=String(project.job.CURRENT_ITERATION||'NOT APPLICABLE');const current=records(project,'freshContexts',{stage}).filter(record=>String(recordValue(record,'ITERATION_ID')||record.scope?.iterationId||'')===iterationId);if(current.some(record=>upper(recordValue(record,'EXTERNAL_CONTEXT_IDENTIFIER'))===upper(identifier)))throw new Error('This external context identifier is already registered for the current iteration.');
  const reviewer=upper(purpose)==='REVIEWER';let record=reviewer?null:current.find(record=>placeholder.has(upper(recordValue(record,'EXTERNAL_CONTEXT_IDENTIFIER'))));
  if(record){record.fields=record.fields&&typeof record.fields==='object'?record.fields:{};record.fields.EXTERNAL_CONTEXT_IDENTIFIER=identifier;record.EXTERNAL_CONTEXT_IDENTIFIER=identifier;record.fulfilledAt=now();record.fulfilledBy=operatorLabel;record.identityAssurance='SELF_ASSERTED';refreshRecordHashes(record,'freshContexts');}
  else record=commandRecord(project,'freshContexts',{EXTERNAL_CONTEXT_IDENTIFIER:identifier,ROLE:reviewer?`${core.STAGES[Number(stage)-1]?.role||'UNKNOWN'} — INDEPENDENT REVIEWER`:core.STAGES[Number(stage)-1]?.role||'UNKNOWN',ITERATION_ID:iterationId,RUN_ID:'NOT APPLICABLE',AUTHORIZED_PROJECT_INPUTS:project.job.CURRENT_INPUT_VERSION||'UNKNOWN',AUTHORIZED_EXTERNAL_SOURCE_MATERIAL:project.job.CURRENT_SOURCE_SET_VERSION||'NOT APPLICABLE',FROZEN_ARTIFACT_VERSIONS:'CURRENT AUTHORIZED VERSIONS',TOOL_AVAILABILITY:'UNKNOWN',CONTAMINATION_STATUS:'UNKNOWN',OUTPUT_IDENTITY:'UNKNOWN',DEVIATIONS:'NONE RECORDED',EVIDENCE:reviewer?'Dedicated independent reviewer context identifier supplied by operator.':'Context identifier supplied by operator.',USABILITY_DETERMINATION:'UNKNOWN'},{stage,source:reviewer?'HUMAN_REVIEWER_CONTEXT':'HUMAN_INPUT'});
  addHistory(project,'FRESH_CONTEXT_REGISTERED',{stage,recordId:record.id,externalContextIdentifier:identifier,operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return record;
}
function recordHumanDecision(project,{stage,field,value,operatorLabel='HUMAN_OPERATOR',reason='Explicit human authority input.'}={}){
  ensureShape(project);const number=Number(stage),definition=schema.STAGE_FIELDS?.[number]?.[field];if(!definition)throw new Error(`Unknown Stage ${number} field ${field}.`);
  const actor=definition.producer;const authorization=schema.authorizeMutation({fieldDefinition:definition,actor,mutationType:'SET'});if(authorization===false||authorization?.authorized===false||authorization?.allowed===false)throw new Error(authorization?.reason||`Mutation of ${field} is not authorized for ${actor}.`);
  if(!['HUMAN','HUMAN_DECISION'].includes(String(actor)))throw new Error(`${field} is not human authority.`);project.stages[number].humanData[field]=clone(value);
  const history=addHistory(project,'HUMAN_DECISION_RECORDED',{stage:number,field,value:clone(value),operatorLabel,identityAssurance:'SELF_ASSERTED',reason});const record={decisionId:allocateInfrastructureId(project,'HUMAN-DECISION','humanDecisions'),stage:number,field,value:clone(value),operatorLabel,identityAssurance:'SELF_ASSERTED',reason,eventSequence:history.eventSequence,deviceTimestamp:history.deviceTimestamp,createdAt:history.deviceTimestamp};project.projectData.humanDecisions=safe(project.projectData.humanDecisions);project.projectData.humanDecisions.push(record);return record;
}
function invalidateAcceptedResponse(project,{stage=project.activeStage,rawResponseId,reason='Corrected response required.',operatorLabel='HUMAN_OPERATOR'}={}){
  ensureShape(project);const number=Number(stage),current=acceptedChanges(project,number),selected=current.find(item=>!rawResponseId||item.rawResponseId===rawResponseId);if(!selected)throw new Error('No current accepted data change exists for invalidation.');
  const proposal=safe(project.projectData.responseProposals).find(item=>item.proposalId===selected.proposalId),operation=selected.operation||proposal?.envelope?.operation||'COMPLETE',scope=clone(selected.scope||proposal?.scope||proposal?.envelope?.scope||{}),laneKeys=['iterationId','candidateId','runId','contextId','baselineId','productId'],scalar=value=>String(value??''),sameLane=item=>String(item?.operation||item?.envelope?.operation||'COMPLETE')===String(operation)&&laneKeys.every(key=>scalar((item?.scope||item?.envelope?.scope||{})[key])===scalar(scope?.[key]));
  const affected=current.filter(sameLane);if(!affected.length)throw new Error('The selected accepted response no longer belongs to a current refinement lane.');
  const event=addHistory(project,'ACCEPTED_RESPONSE_INVALIDATED',{stage:number,rawResponseId:selected.rawResponseId,promptId:selected.promptId||proposal?.promptId||null,operation,scope,reason,operatorLabel,identityAssurance:'SELF_ASSERTED'}),id=event.eventId,proposalIds=new Set(affected.map(change=>change.proposalId)),rawIds=new Set(affected.map(change=>change.rawResponseId)),canonicalIds=new Set(affected.flatMap(change=>safe(change.canonicalRecordIds))),snapshotMap=new Map();
  for(const change of affected){change.invalidatedBy=id;for(const snapshot of safe(change.reservedTargetSnapshots))snapshotMap.set(`${snapshot.collection}:${snapshot.id}`,clone(snapshot.record));}
  for(const prompt of safe(project.projectData.generatedPrompts).filter(item=>Number(item.stage)===number&&!item.invalidatedBy&&sameLane(item)))prompt.invalidatedBy=id;
  for(const pending of safe(project.projectData.responseProposals).filter(item=>Number(item.stage)===number&&!item.invalidatedBy&&sameLane(item))){if(proposalIds.has(pending.proposalId)||pending.status==='PENDING_OPERATOR_REVIEW'||pending.status==='ACCEPTED'){pending.invalidatedBy=id;pending.status='INVALIDATED_FOR_REFINEMENT';}}
  for(const confirmation of safe(project.projectData.stageConfirmations).filter(item=>Number(item.stage)===number&&!item.invalidatedBy))confirmation.invalidatedBy=id;
  for(const collection of Object.keys(schema.RECORD_SCHEMAS)){const list=safe(project.projectData[collection]);for(let index=0;index<list.length;index++){const record=list[index],recordIdentity=recordId(record,collection),key=`${collection}:${recordIdentity}`;if(snapshotMap.has(key)){list[index]=clone(snapshotMap.get(key));continue;}if(!record.invalidatedBy&&(proposalIds.has(record.sourceProposalId)||rawIds.has(record.rawResponseId)||canonicalIds.has(recordIdentity))){record.invalidatedBy=id;record.active=false;record.validity='INVALIDATED';refreshRecordHashes(record,collection);}}}
  const remaining=acceptedChanges(project,number).sort((a,b)=>Number(a.eventSequence||0)-Number(b.eventSequence||0)),rebuilt={};for(const change of remaining){const acceptedProposal=safe(project.projectData.responseProposals).find(item=>item.proposalId===change.proposalId);Object.assign(rebuilt,clone(acceptedProposal?.proposedStageData||{}));}
  const state=project.stages[number];state.agentData=rebuilt;state.acceptedData=clone(rebuilt);state.acceptedDataChangeIds=remaining.map(change=>change.changeId);state.acceptedResponseIds=remaining.map(change=>change.rawResponseId);state.currentPromptId=null;state.status='NOT STARTED';state.invalidatedBy=null;
  if(number<30)invalidateDownstream(project,number,id,reason);else recalculate(project);return event;
}
function reserveRunBatch(project,{stage=project.activeStage,iterationId=null,candidateId=null,count=10}={}){
  ensureShape(project);if(count!==10)throw new Error('Run batches must contain exactly ten slots.');const resolvedIteration=String(iterationId||project.job.CURRENT_ITERATION||'');if(!resolvedIteration)throw new Error('A current iteration is required before reserving runs.');const stageNumber=Number(stage),iteration=records(project,'iterations').find(r=>recordId(r,'iterations')===resolvedIteration&&isActiveRecord(r));if(!iteration)throw new Error('The requested current iteration does not exist or is inactive.');const iterationCandidate=iterationCandidateId(project,resolvedIteration),resolvedCandidate=String(candidateId||iterationCandidate||'');if(!resolvedCandidate)throw new Error('A frozen candidate is required before reserving runs.');if(iterationCandidate&&resolvedCandidate!==iterationCandidate)throw new Error('The requested candidate does not match the current iteration candidate.');const candidate=records(project,'candidateFreezes').find(r=>recordId(r,'candidateFreezes')===resolvedCandidate&&isActiveRecord(r));if(!candidate||!candidateComponentIdentity(project,resolvedCandidate))throw new Error('The current iteration must resolve to one active frozen candidate with application-computed component hashes.');const iterationScope=scopeForIteration(project,resolvedIteration),existingRuns=records(project,'runs').filter(run=>Number(run.stage)===stageNumber&&String(recordValue(run,'ITERATION_ID')||run.scope?.iterationId||'')===resolvedIteration);
  if(existingRuns.length){if(existingRuns.length!==count)throw new Error(`The current Stage ${String(stageNumber).padStart(2,'0')} iteration already contains ${existingRuns.length} active run slots; expected exactly ${count}. Resolve the partial or contradictory batch instead of allocating more.`);const existing=existingRuns.map(run=>{const runId=recordId(run,'runs'),contextId=String(recordValue(run,'CONTEXT_ID')||run.relationships?.CONTEXT_ID||''),runCandidate=String(recordValue(run,'CANDIDATE_ID')||run.scope?.candidateId||'');if(runCandidate!==resolvedCandidate)throw new Error(`Existing run ${runId} is bound to candidate ${runCandidate||'UNKNOWN'}, not current candidate ${resolvedCandidate}.`);const context=records(project,'freshContexts').find(item=>recordId(item,'freshContexts')===contextId&&String(recordValue(item,'ITERATION_ID')||item.scope?.iterationId||'')===resolvedIteration);if(!context)throw new Error(`Existing run ${runId} does not resolve to its active fresh-context slot.`);return {runId,contextId};});if(new Set(existing.map(item=>item.contextId)).size!==count)throw new Error('The existing run batch does not contain ten distinct active context slots.');return existing;}
  const created=[];for(let i=0;i<count;i++){const context=commandRecord(project,'freshContexts',{EXTERNAL_CONTEXT_IDENTIFIER:'UNASSIGNED',ROLE:core.STAGES[stageNumber-1]?.role||'UNKNOWN',ITERATION_ID:resolvedIteration,RUN_ID:'PENDING',AUTHORIZED_PROJECT_INPUTS:project.job.CURRENT_INPUT_VERSION||'UNKNOWN',AUTHORIZED_EXTERNAL_SOURCE_MATERIAL:project.job.CURRENT_SOURCE_SET_VERSION||'NOT APPLICABLE',FROZEN_ARTIFACT_VERSIONS:'CURRENT AUTHORIZED VERSIONS',TOOL_AVAILABILITY:'UNKNOWN',CONTAMINATION_STATUS:'UNKNOWN',OUTPUT_IDENTITY:'UNKNOWN',DEVIATIONS:'NONE',EVIDENCE:'Application-reserved context slot.',USABILITY_DETERMINATION:'PENDING'},{stage:stageNumber,source:'APPLICATION_RESERVATION',scope:iterationScope});const run=commandRecord(project,'runs',{ITERATION_ID:resolvedIteration,CANDIDATE_ID:resolvedCandidate,CONTEXT_ID:context.id,FRESH_CONTEXT_RECORD:context.id,CONTAMINATION_CHECK:'PENDING',TOOL_CONFIGURATION:'PENDING',EXECUTION_STATUS:'RESERVED',COMPLETE_OUTPUT:''},{stage:stageNumber,source:'APPLICATION_RESERVATION',scope:{...iterationScope,contextId:context.id}});context.fields.RUN_ID=context.RUN_ID=run.id;context.scope={...iterationScope,runId:run.id,contextId:context.id};run.scope={...iterationScope,runId:run.id,contextId:context.id};refreshRecordHashes(context,'freshContexts');refreshRecordHashes(run,'runs');created.push({runId:run.id,contextId:context.id});}
  addHistory(project,'RUN_BATCH_RESERVED',{stage:stageNumber,iterationId:resolvedIteration,candidateId:resolvedCandidate,count,slots:created});recalculate(project);return created;
}
function registerArtifactBytes(project,{stage=project.activeStage,artifactId,filename,mediaType,byteSize,sha256,lineage={},role='STAGE_ARTIFACT'}={}){
  ensureShape(project);if(!artifactId||!filename||!Number.isInteger(Number(byteSize))||Number(byteSize)<0||!/^[a-f0-9]{64}$/i.test(String(sha256||'')))throw new Error('Verified artifact identity is incomplete.');
  if(records(project,'artifacts',{active:false}).some(r=>recordId(r,'artifacts')===String(artifactId)))return records(project,'artifacts',{active:false}).find(r=>recordId(r,'artifacts')===String(artifactId));
  const fields={FILENAME:String(filename),TYPE:String(mediaType||'application/octet-stream'),VERSION:'APPLICATION-CONTROLLED',BYTE_SIZE:Number(byteSize),SHA256:String(sha256).toLowerCase(),ROLE:role,STORAGE_REFERENCE:`indexeddb:${artifactId}`,AVAILABILITY:'BYTES_PERSISTED_AND_VERIFIED',NOTES:JSON.stringify(lineage)};
  const definition=schema.RECORD_SCHEMAS.artifacts,id=String(artifactId),deviceTimestamp=now(),full={...fields,[definition.idField]:id};const record={id,stage,createdAt:deviceTimestamp,updatedAt:deviceTimestamp,active:true,scope:currentScope(project),fields:full,...full,source:'APPLICATION_BYTES_VERIFIED'};refreshRecordHashes(record,'artifacts');project.projectData.artifacts.push(record);const productId=String(lineage?.productId||'');if(productId){const product=records(project,'products').find(r=>recordId(r,'products')===productId&&isActiveRecord(r));if(!product)throw new Error('Artifact lineage references a missing or inactive product.');const inventory=[...new Set([...safe(recordValue(product,'GENERATED_ARTIFACT_INVENTORY')),id])];product.fields={...(product.fields||{}),GENERATED_ARTIFACT_INVENTORY:inventory};product.GENERATED_ARTIFACT_INVENTORY=inventory;product.updatedAt=now();refreshRecordHashes(product,'products');}addHistory(project,'ARTIFACT_BYTES_REGISTERED',{stage,artifactId:id,byteSize:Number(byteSize),sha256:String(sha256).toLowerCase(),productId:productId||null});recalculate(project);return record;
}
function selectedArtifacts(project,artifactIds){const ids=[...new Set(safe(artifactIds).map(String))],items=records(project,'artifacts',{active:true}).filter(a=>ids.includes(recordId(a,'artifacts')));if(items.length!==ids.length)throw new Error('Every selected artifact must resolve to one active canonical artifact.');for(const a of items)if(upper(recordValue(a,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||!recordValue(a,'SHA256')||!Number.isFinite(Number(recordValue(a,'BYTE_SIZE'))))throw new Error(`Artifact ${recordId(a,'artifacts')} does not have verified stored bytes.`);return items;}
function freezeCandidate(project,{stage=project.activeStage||10,artifactIds=[],operatorLabel='HUMAN_OPERATOR',purpose='TEST_CANDIDATE',toolConfiguration='CURRENT AUTHORIZED CONFIGURATION',settings='CURRENT AUTHORIZED SETTINGS',permissions='CURRENT AUTHORIZED PERMISSIONS',limitations='RECORDED LIMITATIONS',roleDistribution='WORKFLOW ROLE MAP',batchChangeRule='ANY MATERIAL CHANGE REQUIRES A NEW CANDIDATE'}={}){
  ensureShape(project);const freezeStage=Number(stage);if(![10,17,19].includes(freezeStage))throw new Error('Candidate freezes are allowed only at Stages 10, 17, and 19.');const artifacts=selectedArtifacts(project,artifactIds);if(!artifacts.length)throw new Error('At least one verified artifact is required to freeze a candidate.');const manifest=artifacts.map(a=>({artifactId:recordId(a,'artifacts'),filename:recordValue(a,'FILENAME'),byteSize:Number(recordValue(a,'BYTE_SIZE')),sha256:recordValue(a,'SHA256'),storageReference:recordValue(a,'STORAGE_REFERENCE')})),componentHashes=Object.fromEntries(manifest.map(a=>[a.artifactId,a.sha256])),createdAt=now(),previousIteration=latestIteration(project,[10,17,19]),previousCandidate=records(project,'candidateFreezes').at(-1);let candidateId,candidate;if(freezeStage===19){if(!previousCandidate)throw new Error('Stage 19 unchanged confirmation requires an existing candidate.');const priorManifest=recordValue(previousCandidate,'COMPONENT_MANIFEST')||[];if(hash.sha256Value(priorManifest)!==hash.sha256Value(manifest))throw new Error('Stage 19 requires the exact unchanged candidate artifact identity.');candidateId=recordId(previousCandidate,'candidateFreezes');candidate=previousCandidate;}else candidateId=allocateId(project,'candidateFreezes');const iterationId=allocateId(project,'iterations');
  const frozenScope={...currentScope(project),iterationId,candidateId};const iterationFields={ITERATION_ID:iterationId,CANDIDATE_ID:candidateId,PREVIOUS_ITERATION_ID:recordId(previousIteration,'iterations')||'',CHANGESET_ID:recordId(records(project,'changes').at(-1),'changes')||'',PURPOSE:purpose,STATUS:'FROZEN',LINEAGE:hash.sha256Value(manifest),EVIDENCE:`Application freeze from ${manifest.length} verified artifact(s).`};const iteration={id:iterationId,stage:freezeStage,createdAt,active:true,scope:frozenScope,fields:iterationFields,...iterationFields,source:'APPLICATION_DERIVATION'};iteration.contentSha256=hash.contentRecordSha256(iteration,'ITERATION_ID');iteration.recordSha256=hash.recordSha256(iteration);iteration.sha256=iteration.recordSha256;project.projectData.iterations.push(iteration);
  if(freezeStage!==19){const candidateFields={CANDIDATE_ID:candidateId,ITERATION_ID:iterationId,COMPONENT_MANIFEST:manifest,COMPONENT_VERSIONS:Object.fromEntries(manifest.map(a=>[a.artifactId,'APPLICATION-CONTROLLED'])),COMPONENT_HASHES:componentHashes,ROLE_DISTRIBUTION:roleDistribution,IMMUTABLE_LOCATIONS:manifest.map(a=>a.storageReference),TOOL_CONFIGURATION:toolConfiguration,SETTINGS:settings,PERMISSIONS:permissions,LIMITATIONS:limitations,BATCH_CHANGE_RULE:batchChangeRule,STATUS:'FROZEN',EVIDENCE:hash.sha256Value({manifest,operatorLabel})};candidate={id:candidateId,stage:freezeStage,createdAt,active:true,scope:frozenScope,fields:candidateFields,...candidateFields,source:'APPLICATION_DERIVATION'};candidate.contentSha256=hash.contentRecordSha256(candidate,'CANDIDATE_ID');candidate.recordSha256=hash.recordSha256(candidate);candidate.sha256=candidate.recordSha256;project.projectData.candidateFreezes.push(candidate);}
  project.job.CURRENT_ITERATION=iterationId;addHistory(project,'CANDIDATE_FROZEN',{stage:freezeStage,iterationId,candidateId,artifactIds:manifest.map(a=>a.artifactId),unchangedConfirmation:freezeStage===19,operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return {iteration,candidate};
}
function beginUnchangedConfirmationIteration(project,{candidateId,operatorLabel='HUMAN_OPERATOR'}={}){
  ensureShape(project);if(project.stages?.[18]?.status!=='COMPLETE')throw new Error('Stage 18 must be complete before unchanged confirmation can begin.');const candidate=records(project,'candidateFreezes').find(record=>recordId(record,'candidateFreezes')===String(candidateId||'')&&Number(record.stage)===17&&isActiveRecord(record));if(!candidate)throw new Error('A current Stage 17 frozen candidate is required for unchanged confirmation.');const componentHashes=recordValue(candidate,'COMPONENT_HASHES')||{};if(!componentHashes||typeof componentHashes!=='object'||Array.isArray(componentHashes)||!Object.keys(componentHashes).length)throw new Error('The unchanged-confirmation candidate must have application-computed component hashes.');const previousIterationId=String(recordValue(candidate,'ITERATION_ID')||candidate.scope?.iterationId||'');const previousIteration=records(project,'iterations').find(record=>recordId(record,'iterations')===previousIterationId);if(!previousIteration)throw new Error('The candidate source iteration does not exist.');const iterationId=allocateId(project,'iterations'),createdAt=now(),baseScope=clone(previousIteration.scope||candidate.scope||currentScope(project));const fields={ITERATION_ID:iterationId,CANDIDATE_ID:String(candidateId),PREVIOUS_ITERATION_ID:previousIterationId,CHANGESET_ID:'',PURPOSE:'UNCHANGED_CONFIRMATION',STATUS:'FROZEN',LINEAGE:hash.sha256Value({candidateId:String(candidateId),componentHashes}),EVIDENCE:'Application allocated a new unchanged-confirmation iteration using the exact existing frozen candidate identity.'};const iteration={id:iterationId,stage:19,createdAt,active:true,scope:{...baseScope,iterationId,candidateId:String(candidateId)},fields,...fields,source:'APPLICATION_DERIVATION'};iteration.contentSha256=hash.contentRecordSha256(iteration,'ITERATION_ID');iteration.recordSha256=hash.recordSha256(iteration);iteration.sha256=iteration.recordSha256;project.projectData.iterations.push(iteration);project.job.CURRENT_ITERATION=iterationId;addHistory(project,'UNCHANGED_CONFIRMATION_ITERATION_STARTED',{stage:19,iterationId,candidateId:String(candidateId),previousIterationId,operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return iteration;
}
function freezeBaseline(project,{artifactIds=[],operatorLabel='HUMAN_OPERATOR',authorization='AUTHORIZED'}={}){
  ensureShape(project);const confirmation=recordsForCurrentScope(project,'confirmationRecords').filter(r=>upper(recordValue(r,'DETERMINATION'))==='SATISFIED').at(-1);if(!confirmation)throw new Error('A current successful unchanged confirmation is required before baseline freeze.');const currentIteration=records(project,'iterations').find(r=>recordId(r,'iterations')===String(project.job.CURRENT_ITERATION||'')&&isActiveRecord(r));if(!currentIteration||Number(currentIteration.stage)!==19)throw new Error('The current unchanged-confirmation iteration is required before baseline freeze.');const currentCandidateId=iterationCandidateId(project,recordId(currentIteration,'iterations')),currentCandidate=records(project,'candidateFreezes').find(r=>recordId(r,'candidateFreezes')===currentCandidateId&&isActiveRecord(r));if(!currentCandidate||!candidateComponentIdentity(project,currentCandidateId))throw new Error('The unchanged-confirmation iteration does not resolve to an active frozen candidate.');const manifest=safe(recordValue(currentCandidate,'COMPONENT_MANIFEST')),candidateArtifactIds=manifest.map(item=>String(item?.artifactId||'')).filter(Boolean);if(!candidateArtifactIds.length||candidateArtifactIds.length!==manifest.length||new Set(candidateArtifactIds).size!==candidateArtifactIds.length)throw new Error('The confirmed candidate manifest does not contain one unique canonical artifact identity for every component.');const requested=[...new Set(safe(artifactIds).map(String))];if(requested.length&&hash.sha256Value([...requested].sort())!==hash.sha256Value([...candidateArtifactIds].sort()))throw new Error('Baseline authorization must use the exact artifact set from the unchanged-confirmed frozen candidate.');const artifacts=selectedArtifacts(project,candidateArtifactIds),byId=new Map(artifacts.map(a=>[recordId(a,'artifacts'),a]));for(const item of manifest){const artifact=byId.get(String(item.artifactId));if(!artifact||String(recordValue(artifact,'FILENAME'))!==String(item.filename)||Number(recordValue(artifact,'BYTE_SIZE'))!==Number(item.byteSize)||String(recordValue(artifact,'SHA256')).toLowerCase()!==String(item.sha256||'').toLowerCase())throw new Error(`Confirmed candidate artifact ${item.artifactId||'UNKNOWN'} no longer matches its frozen filename, byte size, and SHA-256.`);}const baselineId=allocateId(project,'baselines'),createdAt=now(),approvedVersions={inputVersion:project.job.CURRENT_INPUT_VERSION,sourceSetVersion:project.job.CURRENT_SOURCE_SET_VERSION,requirementsVersion:project.job.CURRENT_REQUIREMENTS_VERSION,testSuiteVersion:project.job.CURRENT_TEST_SUITE_VERSION,instructionVersion:project.job.CURRENT_INSTRUCTION_VERSION,iterationId:recordId(currentIteration,'iterations'),candidateId:currentCandidateId,hashes:Object.fromEntries(artifacts.map(a=>[recordId(a,'artifacts'),recordValue(a,'SHA256')]))};const fields={BASELINE_ID:baselineId,SUPPORTING_CONFIRMATION_ID:recordId(confirmation,'confirmationRecords'),APPROVED_VERSIONS:approvedVersions,IMMUTABLE_ARTIFACT_RECORDS:artifacts.map(a=>recordId(a,'artifacts')),HASHES:approvedVersions.hashes,HUMAN_AUTHORIZATION:authorization,AUTHORIZED_RECIPIENT_ROLES:'CURRENT AUTHORIZED ROLES',CONTROLLED_STORAGE:'INDEXEDDB VERIFIED BYTES',STATUS:'FROZEN',EVIDENCE:hash.sha256Value({approvedVersions,operatorLabel})};const record={id:baselineId,stage:20,createdAt,active:true,scope:{...currentScope(project),baselineId},fields,...fields,source:'APPLICATION_DERIVATION'};record.contentSha256=hash.contentRecordSha256(record,'BASELINE_ID');record.recordSha256=hash.recordSha256(record);record.sha256=record.recordSha256;project.projectData.baselines.push(record);project.job.CURRENT_BASELINE_ID=baselineId;addHistory(project,'BASELINE_FROZEN',{stage:20,baselineId,artifactIds:fields.IMMUTABLE_ARTIFACT_RECORDS,iterationId:approvedVersions.iterationId,candidateId:approvedVersions.candidateId,operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return record;
}
function reserveProductExecution(project,{operatorLabel='HUMAN_OPERATOR'}={}){ensureShape(project);const baseline=recordsForCurrentScope(project,'baselines').at(-1);if(!baseline)throw new Error('A current frozen baseline is required.');const noRun=value=>['','NONE','NOT APPLICABLE','UNASSIGNED','PENDING'].includes(upper(value));const usedContextIds=new Set(records(project,'products').filter(isActiveRecord).map(r=>String(recordValue(r,'PRODUCTION_CONTEXT_ID')||''))),contexts=recordsForCurrentScope(project,'freshContexts').filter(r=>Number(r.stage)===21&&noRun(recordValue(r,'RUN_ID'))&&!usedContextIds.has(recordId(r,'freshContexts'))),context=contexts.at(-1);if(!context)throw new Error('A current unused fresh production context is required.');const productId=allocateId(project,'products'),executionId=allocateInfrastructureId(project,'EXECUTION','products'),createdAt=now(),fields={PRODUCT_ID:productId,PRODUCT_VERSION:'PRODUCT-v001',BASELINE_ID:recordId(baseline,'baselines'),EXECUTION_ID:executionId,PRODUCTION_CONTEXT_ID:recordId(context,'freshContexts'),INSTRUCTION_VERSION:project.job.CURRENT_INSTRUCTION_VERSION,BASELINE_MATERIALS:recordValue(baseline,'IMMUTABLE_ARTIFACT_RECORDS')||[],EXECUTION_TIMESTAMPS:{reservedAt:createdAt},TOOL_CONFIGURATION:'PENDING AGENT COMPLETION',DEVIATIONS:'NONE RECORDED',FAILURES:'NONE RECORDED',GENERATED_ARTIFACT_INVENTORY:[],STATUS:'RESERVED'};const record={id:productId,stage:21,createdAt,active:true,reserved:true,completionState:'AWAITING_AGENT_COMPLETION',scope:{...currentScope(project),baselineId:fields.BASELINE_ID,productId},fields,...fields,source:'APPLICATION_RESERVED'};record.contentSha256=hash.contentRecordSha256(record,'PRODUCT_ID');record.recordSha256=hash.recordSha256(record);record.sha256=record.recordSha256;project.projectData.products.push(record);project.job.CURRENT_PRODUCT_ID=productId;addHistory(project,'PRODUCT_EXECUTION_RESERVED',{stage:21,productId,executionId,contextId:fields.PRODUCTION_CONTEXT_ID,operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return record;}
function createNewJobReset(project){ensureShape(project);const id=allocateInfrastructureId(project,'NEW-JOB-RESET','newJobResets'),createdAt=now(),fields={NEW_JOB_ID:project.job.JOB_ID,OLD_JOB_MATERIAL_REUSED:'FALSE',AUTHORIZED_REUSED_ARTIFACTS:'NONE',NEW_JOB_START_STAGE:'STAGE 01',RESET_COMPLETED_BY:'APPLICATION',RESET_DATE_AND_TIME:createdAt,RESET_EVIDENCE:'A clean 30-stage project was created without carrying active state from another job.'},record={id,stage:1,createdAt,active:true,fields,...fields,source:'APPLICATION_DERIVATION'};record.sha256=hash.sha256Value(fields);project.projectData.newJobResets.push(record);addHistory(project,'NEW_JOB_RESET_RECORDED',{stage:1,recordId:id});return record;}

function recordMigratedAcceptedChange(project,record){ensureShape(project);if(!record?.changeId)throw new Error('A migrated accepted change requires an identity.');const existing=safe(project.projectData.acceptedChanges).find(x=>x.changeId===record.changeId);if(existing)return existing;const value={...clone(record),source:'DETERMINISTIC_MIGRATION'};project.projectData.acceptedChanges.push(value);addHistory(project,'MIGRATED_ACCEPTED_CHANGE_RECORDED',{stage:Number(value.stage),recordId:value.changeId,migrationSchema:value.migrationSchema||'UNKNOWN'});return value;}

function samePromptTarget(a,b){return Number(a?.stage)===Number(b?.stage)&&String(a?.operation||'COMPLETE')===String(b?.operation||'COMPLETE')&&['iterationId','candidateId','runId','contextId','baselineId','productId'].every(key=>String(a?.scope?.[key]??'')===String(b?.scope?.[key]??''));}
function registerGeneratedPrompt(project,promptRecord){
  ensureShape(project);if(!promptRecord?.instructionId||!Number.isInteger(Number(promptRecord?.stage)))throw new Error('A complete generated prompt record is required.');
  const expectedRevision=Number(project.revision||0)+1;if(Number(promptRecord.scope?.projectRevision)!==expectedRevision)throw new Error(`Generated prompt revision ${promptRecord.scope?.projectRevision} does not match next committed revision ${expectedRevision}.`);
  const existing=safe(project.projectData.generatedPrompts).find(x=>x.instructionId===promptRecord.instructionId);if(existing)return existing;
  const invalidationId=`PROMPT-SUPERSEDED-${promptRecord.instructionId}`;for(const prior of safe(project.projectData.generatedPrompts).filter(x=>!x.invalidatedBy&&samePromptTarget(x,promptRecord))){prior.invalidatedBy=invalidationId;for(const map of safe(project.projectData.blindAliasMaps).filter(x=>!x.invalidatedBy&&x.instructionId===prior.instructionId))map.invalidatedBy=invalidationId;}
  const record={...clone(promptRecord),source:'APPLICATION_PROMPT_REGISTRATION'};project.projectData.generatedPrompts.push(record);const aliases=safe(record.contextManifest?.blindAliasMap);if(aliases.length&&!project.projectData.blindAliasMaps.some(x=>x.instructionId===record.instructionId&&!x.invalidatedBy)){project.projectData.blindAliasMaps.push({mapId:`BLIND-MAP-${hash.sha256Value({instructionId:record.instructionId,contextSignature:record.contextSignature}).slice(0,20).toUpperCase()}`,instructionId:record.instructionId,stage:Number(record.stage),operation:record.operation,contextSignature:record.contextSignature,entries:clone(aliases),active:true,createdAt:now(),source:'APPLICATION_PROMPT_REGISTRATION'});}project.stages[Number(record.stage)].currentPromptId=record.instructionId;
  addHistory(project,'INSTRUCTION_SAVED',{recordId:record.instructionId,stage:Number(record.stage),sha256:record.bodySha256||record.sha256,projectRevision:expectedRevision});return record;
}


function recordHumanInspectionEvidence(project,{testId,observation,evidenceDescription='',operatorLabel='HUMAN_OPERATOR',operationReservationId,expectedRevision,idempotencyKey,commandId}={}){
  ensureShape(project);const tid=String(testId||'').trim(),text=String(observation||'').trim(),description=String(evidenceDescription||'').trim(),stage=Number(project.activeStage||0),reservationId=String(operationReservationId||'').trim();if(!tid)throw new Error('A current HUMAN_INSPECTION test is required.');if(!text)throw new Error('Record the actual human observation before continuing.');const test=recordsForCurrentScope(project,'tests').find(record=>recordId(record,'tests')===tid);if(!test||upper(recordValue(test,'EXECUTION_MODE'))!=='HUMAN_INSPECTION')throw new Error('The selected test is not a current HUMAN_INSPECTION test.');const plan=testExecutionPlan(project).items.find(item=>item.testId===tid);if(!plan?.executableNow)throw new Error(plan?.blockingReason||'The human inspection is not currently executable.');const reservations=scoped(project,'operationReservations').filter(record=>recordId(record,'operationReservations')===reservationId),reservation=reservations.length===1?reservations[0]:null,reservationPayload=recordValue(reservation,'PAYLOAD')||{},reservedTestIds=list(reservationPayload.testIds||reservationPayload.testId);if(!reservation||upper(recordValue(reservation,'STATUS'))!=='ACTIVE'||Number(recordValue(reservation,'STAGE'))!==stage||!reservedTestIds.includes(tid))throw new Error('Human inspection requires one exact current ACTIVE operation reservation bound to this test and stage.');const artifactIds=list(reservationPayload.artifactIds),artifactState=artifactIds.map(id=>{const rows=recordsForCurrentScope(project,'artifacts').filter(record=>recordId(record,'artifacts')===id);if(rows.length!==1||upper(recordValue(rows[0],'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED')throw new Error(`Reserved inspection artifact ${id} is missing, duplicated, or no longer byte-verified.`);return{artifactId:id,filename:String(recordValue(rows[0],'FILENAME')||''),byteSize:Number(recordValue(rows[0],'BYTE_SIZE')),sha256:String(recordValue(rows[0],'SHA256')||'')};});const payload={testId:tid,requirementId:testRequirementId(test),observation:text,evidenceDescription:description||'Human-owned observation recorded directly by the operator.',operatorLabel:String(operatorLabel||'HUMAN_OPERATOR'),identityAssurance:'SELF_ASSERTED',stage,scope:currentScope(project),operationReservationId:reservationId,reservedArtifactIdentities:artifactState},identity=hash.sha256Value(payload),expected=Number(expectedRevision==null?project.revision:expectedRevision);
  return idem(project,{commandType:'RECORD_HUMAN_INSPECTION_EVIDENCE',target:`${stage}:${tid}:${reservationId}`,expectedRevision:expected,idempotencyKey,commandId,payload,execute:()=>{const existing=recordsForCurrentScope(project,'evidenceRecords').find(record=>record?.source==='HUMAN_OBSERVATION'&&String(record?.humanInspectionTestId||'')===tid&&String(recordValue(record,'SHA256')||'')===identity);if(existing){set(reservation,'operationReservations','STATUS','ACCEPTED');return existing;}const evidence=commandRecord(project,'evidenceRecords',{APPLICATION_EVIDENCE_KIND:'HUMAN_OBSERVATION',APPLICATION_EVIDENCE_DESCRIPTION:payload.evidenceDescription,APPLICATION_EVIDENCE_CONTENT:JSON.stringify(payload),SHA256:identity,STATUS:'CURRENT'},{stage,source:'HUMAN_OBSERVATION'});evidence.humanInspectionTestId=tid;evidence.humanInspectionReservationId=reservationId;evidence.humanAuthority={operatorLabel:payload.operatorLabel,identityAssurance:'SELF_ASSERTED'};refreshRecordHashes(evidence,'evidenceRecords');set(reservation,'operationReservations','STATUS','ACCEPTED');addHistory(project,'HUMAN_INSPECTION_OBSERVATION_RECORDED',{stage,testId:tid,evidenceId:recordId(evidence,'evidenceRecords'),operationReservationId:reservationId,artifactIds,operatorLabel:payload.operatorLabel,identityAssurance:'SELF_ASSERTED'});recalculate(project);return evidence;}});
}

function evidenceReferences(result){const ids=new Set();for(const id of safe(result?.evidenceRefs||result?.evidenceIds))if(String(id||'').trim())ids.add(String(id));for(const key of ['EVIDENCE_ID','EXACT_EVIDENCE','EVIDENCE','PROCESS_EVIDENCE','PRODUCT_EVIDENCE','CONTROLLING_EVIDENCE','PRE_CORRECTION_EVIDENCE','POST_CORRECTION_EVIDENCE']){const value=recordValue(result,key);if(Array.isArray(value))for(const id of value)if(/^EVIDENCE-/i.test(String(id)))ids.add(String(id));else if(/^EVIDENCE-/i.test(String(value||'')))ids.add(String(value));}return [...ids];}
function evaluateEvidenceSufficiency(project,{requirement=null,test=null,result=null}={}){
  const ids=evidenceReferences(result),referencedEvidence=ids.map(id=>records(project,'evidenceRecords').find(x=>recordId(x,'evidenceRecords')===id)).filter(Boolean),mode=upper(recordValue(test,'EXECUTION_MODE')||''),testId=String(recordId(test,'tests')||''),humanEvidence=mode==='HUMAN_INSPECTION'?recordsForCurrentScope(project,'evidenceRecords').filter(x=>x?.source==='HUMAN_OBSERVATION'&&String(x?.humanInspectionTestId||'')===testId):[],evidence=[...new Map([...referencedEvidence,...humanEvidence].map(x=>[recordId(x,'evidenceRecords'),x])).values()],nativeEvidence=evidence.filter(x=>x?.source==='APPLICATION_TEST_RUNTIME'),narrative=nativeEvidence.map(x=>String(recordValue(x,'APPLICATION_EVIDENCE_CONTENT')||recordValue(x,'APPLICATION_EVIDENCE_DESCRIPTION')||'').trim()).find(Boolean)||['EXACT_EVIDENCE','EVIDENCE','PROCESS_EVIDENCE','PRODUCT_EVIDENCE','CONTROLLING_EVIDENCE'].map(k=>String(recordValue(result,k)||'').trim()).find(Boolean)||'',type=upper(recordValue(test,'TEST_TYPE')||''),artifactRequirements=String(recordValue(test,'ARTIFACT_REQUIREMENTS')||''),artifactRequired=Boolean(artifactRequirements&&!['NONE','NOT APPLICABLE','N/A'].includes(upper(artifactRequirements))),testText=[type,artifactRequirements,recordValue(test,'PROCEDURE'),recordValue(test,'EXPECTED_RESULT'),recordValue(test,'EVIDENCE_TO_PRESERVE')].join(' '),byteTest=artifactRequired||/\b(?:SHA-?256|HASH|BYTE(?:S|[- ]IDENTITY)?|CRYPTOGRAPHIC IDENTITY)\b/i.test(testText),reasons=[],requiredEvidenceClasses=[];
  const attached=evidence.map(e=>String(recordValue(e,'ATTACHMENT_ID')||e.relationships?.ATTACHMENT_ID||'')).filter(Boolean),verifiedArtifacts=records(project,'artifacts').filter(a=>attached.includes(recordId(a,'artifacts'))&&upper(recordValue(a,'AVAILABILITY'))==='BYTES_PERSISTED_AND_VERIFIED'&&/^[a-f0-9]{64}$/i.test(String(recordValue(a,'SHA256')||''))),productSha=String(recordValue(result,'PRODUCT_SHA256')||''),matchingProductArtifact=productSha&&recordsForCurrentScope(project,'artifacts').some(a=>upper(recordValue(a,'AVAILABILITY'))==='BYTES_PERSISTED_AND_VERIFIED'&&String(recordValue(a,'SHA256')||'')===productSha),hasEvidence=Boolean(ids.length||narrative||evidence.length);
  if(mode==='UNAVAILABLE'){requiredEvidenceClasses.push('CAPABLE_EXECUTION');reasons.push('UNAVAILABLE execution cannot establish satisfaction.');}
  if(!hasEvidence){requiredEvidenceClasses.push('CANONICAL_OR_EXACT_EVIDENCE');reasons.push('No substantive evidence is attached to the result.');}
  if(byteTest){requiredEvidenceClasses.push('APPLICATION_VERIFIED_BYTES');if(!verifiedArtifacts.length&&!matchingProductArtifact)reasons.push('Byte-identity evidence requires application-verified artifact bytes and SHA-256; prose or a claimed hash is insufficient.');}
  if(mode==='HUMAN_INSPECTION'){requiredEvidenceClasses.push('HUMAN_OBSERVATION');const human=evidence.some(e=>{const authority=upper(e?.source||recordValue(e,'AUTHORITY_TYPE')||recordValue(e,'APPLICATION_EVIDENCE_KIND')||recordValue(e,'KIND')),content=String(recordValue(e,'APPLICATION_EVIDENCE_CONTENT')||recordValue(e,'CONTENT')||recordValue(e,'APPLICATION_EVIDENCE_DESCRIPTION')||recordValue(e,'DESCRIPTION')||'').trim(),applicationRecorded=e?.source==='HUMAN_OBSERVATION'&&String(e?.humanAuthority?.identityAssurance||'')==='SELF_ASSERTED',canonicalHuman=authority==='HUMAN_OBSERVATION';return Boolean(content&&(applicationRecorded||canonicalHuman));});if(!human)reasons.push('Human inspection requires an explicit human-owned observation recorded through the application; an agent-labelled HUMAN evidence claim is insufficient.');}
  if(mode==='EXTERNAL_SYSTEM'){requiredEvidenceClasses.push('EXTERNAL_SYSTEM_OUTPUT');const capability=String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim(),system=evidence.some(e=>/SYSTEM|LAB|MACHINE|EXTERNAL/.test(upper(recordValue(e,'AUTHORITY_TYPE')||recordValue(e,'KIND')))||capability&&String(recordValue(e,'CONTENT')||'').toUpperCase().includes(capability.toUpperCase()));if(!system)reasons.push('External-system verification requires evidence attributable to the declared external capability.');}
  if(Number(result?.stage)===23||result&&Object.prototype.hasOwnProperty.call(recordFields(result),'OBSERVED_MEANING')){requiredEvidenceClasses.push('MEANING_COMPARISON');for(const key of ['PRODUCT_LOCATION','REQUIRED_MEANING','OBSERVED_MEANING','EVIDENCE_BASED_COMPARISON'])if(!String(recordValue(result,key)||'').trim())reasons.push('Meaning evidence is missing '+key+'.');}
  if(mode==='EXTERNAL_AGENT_TOOL'){requiredEvidenceClasses.push('CAPABILITY_EXECUTION');if(!String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim())reasons.push('External tool execution lacks a declared capability identity.');if(!String(recordValue(result,'OBSERVED_RESULT')||recordValue(result,'ACTUAL_RESULT')||recordValue(result,'DETERMINATION')||'').trim())reasons.push('External tool execution lacks an observed result.');}
  return {sufficient:reasons.length===0,reasons:[...new Set(reasons)],requiredEvidenceClasses:[...new Set(requiredEvidenceClasses)],presentEvidenceIds:[...new Set(evidence.map(e=>recordId(e,'evidenceRecords')).filter(Boolean))],verifiedArtifactIds:verifiedArtifacts.map(a=>recordId(a,'artifacts'))};
}
function evaluateContextIndependence(project,{role='RUN_BATCH',iterationId=null,runId=null,verifierContextId=null,generatorContextId=null,reviewerContextId=null,productionContextId=null}={}){
  const reasons=[],evidence=[],forbidden=/prior\s*(?:run|output|verification|verifier|review)|reviewer\s*feedback|root\s*cause|\bRCA\b|proposed\s*correction/i;
  if(role==='RUN_BATCH'){
    const runs=recordsForIteration(project,'runs',iterationId),contexts=recordsForIteration(project,'freshContexts',iterationId),byId=new Map(contexts.map(c=>[recordId(c,'freshContexts'),c])),internal=[],external=[],candidates=[];
    for(const run of runs){const rid=recordId(run,'runs'),cid=String(recordValue(run,'CONTEXT_ID')||run.relationships?.CONTEXT_ID||''),ctx=byId.get(cid),ext=String(recordValue(ctx,'EXTERNAL_CONTEXT_IDENTIFIER')||'').trim(),candidate=String(recordValue(run,'CANDIDATE_ID')||run.relationships?.CANDIDATE_ID||run.scope?.candidateId||'');internal.push(cid);external.push(ext);candidates.push(candidate);if(!ctx)reasons.push(rid+' has no current fresh-context record.');if(ctx&&String(recordValue(ctx,'RUN_ID')||ctx.relationships?.RUN_ID||'')&&String(recordValue(ctx,'RUN_ID')||ctx.relationships?.RUN_ID)!==rid)reasons.push(cid+' is linked to a different run.');if(forbidden.test(JSON.stringify(recordValue(ctx,'AUTHORIZED_PROJECT_INPUTS')||[])))reasons.push(rid+' includes prohibited prior output or reviewer material.');if(!['NONE','FALSE','CLEAN','NOT CONTAMINATED'].includes(upper(recordValue(run,'CONTAMINATION_CHECK'))))reasons.push(rid+' contamination is affirmative or unknown.');if(ctx&&['TRUE','YES','CONTAMINATED','DETECTED','POSITIVE'].includes(upper(recordValue(ctx,'CONTAMINATION_STATUS'))))reasons.push(cid+' context contamination is affirmative.');}
    if(runs.length!==10)reasons.push('Exactly ten current runs are required for independence evaluation.');if(new Set(internal.filter(Boolean)).size!==10)reasons.push('Run CONTEXT_ID values are not ten distinct identities.');if(external.some(x=>!x)||new Set(external.map(upper)).size!==10)reasons.push('External fresh-context identifiers are missing or reused.');if(new Set(candidates.filter(Boolean)).size!==1)reasons.push('Runs do not use one identical candidate identity.');evidence.push(...internal.filter(Boolean),...external.filter(Boolean));return {determination:reasons.length?'VIOLATED':'APPLICATION_ESTABLISHED',reasons,evidence,contextCount:new Set(external.map(upper).filter(Boolean)).size,candidateIds:[...new Set(candidates.filter(Boolean))]};
  }
  if(role==='PREFLIGHT_REVIEW'){
    const reviewer=String(reviewerContextId||'').trim(),contexts=records(project,'freshContexts').filter(c=>isActiveRecord(c)&&Number(c.stage)===9),reviewerRecord=contexts.find(c=>recordId(c,'freshContexts')===reviewer),external=String(recordValue(reviewerRecord,'EXTERNAL_CONTEXT_IDENTIFIER')||'').trim();
    if(!reviewer||!reviewerRecord||!external)return {determination:'UNKNOWN',reasons:['A current Stage 09 reviewer context with an external context identifier is required.'],evidence:[reviewer].filter(Boolean)};
    if(['TRUE','YES','CONTAMINATED','DETECTED','POSITIVE'].includes(upper(recordValue(reviewerRecord,'CONTAMINATION_STATUS'))))return {determination:'VIOLATED',reasons:['The Stage 09 reviewer context is contaminated.'],evidence:[reviewer,external]};
    if(forbidden.test(JSON.stringify(recordValue(reviewerRecord,'AUTHORIZED_PROJECT_INPUTS')||[])))return {determination:'VIOLATED',reasons:['The Stage 09 reviewer context contains prohibited prior review, root-cause, or correction material.'],evidence:[reviewer,external]};
    return {determination:'EXTERNALLY_SUPPORTED',reasons:['The application binds a distinct current Stage 09 reviewer context, but the external Stage 08 author context is not fully observable and is not falsely claimed as application-proven.'],evidence:[reviewer,external]};
  }
  if(role==='VERIFICATION'){
    const run=recordsForIteration(project,'runs',iterationId).find(r=>recordId(r,'runs')===String(runId||'')),gen=String(generatorContextId||recordValue(run,'CONTEXT_ID')||run?.relationships?.CONTEXT_ID||''),ver=String(verifierContextId||'').trim(),contexts=recordsForIteration(project,'freshContexts',iterationId),genRecord=contexts.find(c=>recordId(c,'freshContexts')===gen),verRecord=contexts.find(c=>recordId(c,'freshContexts')===ver||String(recordValue(c,'EXTERNAL_CONTEXT_IDENTIFIER')||'')===ver),genExternal=String(recordValue(genRecord,'EXTERNAL_CONTEXT_IDENTIFIER')||''),verExternal=String(recordValue(verRecord,'EXTERNAL_CONTEXT_IDENTIFIER')||'');if(!ver)return {determination:'UNKNOWN',reasons:['Verifier context identity is unavailable.'],evidence:[]};if(ver===gen||(verExternal&&genExternal&&upper(verExternal)===upper(genExternal))||(genExternal&&upper(ver)===upper(genExternal)))return {determination:'VIOLATED',reasons:['Verifier reuses the generator context.'],evidence:[gen,ver]};if(verRecord&&forbidden.test(JSON.stringify(recordValue(verRecord,'AUTHORIZED_PROJECT_INPUTS')||[])))return {determination:'VIOLATED',reasons:['Verifier context contains prohibited prior verification/reviewer/correction material.'],evidence:[ver]};if(verRecord)return {determination:'APPLICATION_ESTABLISHED',reasons:[],evidence:[gen,recordId(verRecord,'freshContexts')]};return {determination:'EXTERNALLY_SUPPORTED',reasons:['Verifier identity is distinct from the generator but the external environment is not fully observable by the application.'],evidence:[gen,ver]};
  }
  const reviewer=String(reviewerContextId||'').trim(),production=String(productionContextId||'').trim();if(!reviewer||!production)return {determination:'UNKNOWN',reasons:['Reviewer/production context identity is not fully available.'],evidence:[reviewer,production].filter(Boolean)};const contexts=records(project,'freshContexts'),reviewerRecord=contexts.find(c=>isActiveRecord(c)&&recordId(c,'freshContexts')===reviewer),productionRecord=contexts.find(c=>isActiveRecord(c)&&recordId(c,'freshContexts')===production),reviewerExternal=String(recordValue(reviewerRecord,'EXTERNAL_CONTEXT_IDENTIFIER')||'').trim(),productionExternal=String(recordValue(productionRecord,'EXTERNAL_CONTEXT_IDENTIFIER')||'').trim();if(reviewer===production||reviewerExternal&&productionExternal&&upper(reviewerExternal)===upper(productionExternal))return {determination:'VIOLATED',reasons:['Reviewer reuses the production context.'],evidence:[reviewer,production]};if(!reviewerRecord)return {determination:'UNKNOWN',reasons:['Reviewer context is not a current canonical fresh-context record.'],evidence:[reviewer,production]};if(['TRUE','YES','CONTAMINATED','DETECTED','POSITIVE'].includes(upper(recordValue(reviewerRecord,'CONTAMINATION_STATUS'))))return {determination:'VIOLATED',reasons:['Reviewer context contamination is affirmative.'],evidence:[reviewer,production]};if(forbidden.test(JSON.stringify(recordValue(reviewerRecord,'AUTHORIZED_PROJECT_INPUTS')||[])))return {determination:'VIOLATED',reasons:['Reviewer context contains prohibited prior conclusions, root-cause material, or proposed corrections.'],evidence:[reviewer,production]};if(productionRecord&&reviewerExternal&&productionExternal)return {determination:'APPLICATION_ESTABLISHED',reasons:[],evidence:[reviewer,production,reviewerExternal,productionExternal]};return {determination:'EXTERNALLY_SUPPORTED',reasons:['Canonical reviewer and production context identities are distinct; the external environment remains only partially observable.'],evidence:[reviewer,production]};
}
function detectCurrentContradictions(project){
  const out=[],push=(type,key,details,severity='RELEASE_MATERIAL')=>out.push({type,key,details,severity}),adjudicationCollections=['verification','deterministicResults','meaningResults','adversarialResults','representationInspections','preflightRecords','confirmationRecords','processAudits','productAudits','regressionExecutions','products'];
  const evaluation=(collection,r)=>evaluateResultConsistency(collection,r,testForResult(project,r),project),supports=(e,state)=>e.determination===state||e.claimedDetermination===state;
  for(const collection of adjudicationCollections)for(const r of recordsForCurrentScope(project,collection)){const e=evaluation(collection,r),id=recordId(r,collection)||String(r.id||r.recordId||'UNKNOWN');if(e.claimedDetermination==='SATISFIED'&&e.determination!=='SATISFIED')push('CLAIMED_FAVORABLE_EFFECTIVE_CONFLICT',collection+':'+id,[e.claimedDetermination,e.determination,...e.reasons]);}
  const groups=new Map();for(const r of recordsForCurrentScope(project,'verification')){const key=verificationKey(r),e=evaluation('verification',r),state=e.determination==='UNDETERMINED'&&['SATISFIED','VIOLATED'].includes(e.claimedDetermination)?e.claimedDetermination:e.determination;if(!groups.has(key))groups.set(key,new Set());groups.get(key).add(state);}for(const [key,ds] of groups)if(ds.size>1)push('VERIFICATION_DETERMINATION_CONFLICT',key,[...ds]);
  const det=recordsForCurrentScope(project,'deterministicResults'),meaning=recordsForCurrentScope(project,'meaningResults'),adv=recordsForCurrentScope(project,'adversarialResults');for(const m of meaning){const req=resultRequirementId(project,m),me=evaluation('meaningResults',m);if(supports(me,'VIOLATED')&&det.some(d=>resultRequirementId(project,d)===req&&supports(evaluation('deterministicResults',d),'SATISFIED')))push('DETERMINISTIC_MEANING_CONFLICT',req,['DETERMINISTIC SATISFIED claim/effective state','MEANING VIOLATED claim/effective state']);if(supports(me,'SATISFIED')&&adv.some(a=>resultRequirementId(project,a)===req&&supports(evaluation('adversarialResults',a),'VIOLATED')))push('MEANING_ADVERSARIAL_CONFLICT',req,['MEANING SATISFIED claim/effective state','ADVERSARIAL VIOLATION claim/effective state']);}
  const artifacts=new Map();for(const a of recordsForCurrentScope(project,'artifacts')){const id=recordId(a,'artifacts'),sig=[recordValue(a,'VERSION'),recordValue(a,'BYTE_SIZE'),recordValue(a,'SHA256')].join('|');if(artifacts.has(id)&&artifacts.get(id)!==sig)push('ARTIFACT_IDENTITY_CONFLICT',id,[artifacts.get(id),sig]);else artifacts.set(id,sig);}const product=recordsForCurrentScope(project,'products').at(-1),baseline=recordsForCurrentScope(project,'baselines').at(-1);if(product&&baseline&&String(recordValue(product,'BASELINE_ID')||product.relationships?.BASELINE_ID||'')!==recordId(baseline,'baselines'))push('PRODUCT_BASELINE_CONFLICT',recordId(product,'products'),['Product baseline differs from current baseline.']);const release=recordsForCurrentScope(project,'releaseRecords').at(-1),releaseAccepted=upper(recordValue(release,'DETERMINATION'))==='ACCEPTED';if(releaseAccepted){const blockers=openBlockers(project);if(blockers.length)push('ACCEPTED_RELEASE_WITH_BLOCKER',recordId(release,'releaseRecords'),blockers.map(b=>recordId(b,'blockers')));const violated=[...recordsForCurrentScope(project,'verification').map(r=>['verification',r]),...recordsForCurrentScope(project,'deterministicResults').map(r=>['deterministicResults',r]),...recordsForCurrentScope(project,'meaningResults').map(r=>['meaningResults',r]),...recordsForCurrentScope(project,'adversarialResults').map(r=>['adversarialResults',r])].filter(([collection,r])=>effectiveDetermination(collection,r,testForResult(project,r),project)==='VIOLATED').map(([collection,r])=>collection+':'+(recordId(r,collection)||String(r.id||'UNKNOWN')));if(violated.length)push('ACCEPTED_RELEASE_WITH_MANDATORY_VIOLATION',recordId(release,'releaseRecords'),violated);const material=unresolvedMaterialDefects(project);if(material.length)push('ACCEPTED_RELEASE_WITH_MATERIAL_DEFECT',recordId(release,'releaseRecords'),material.map(d=>recordId(d,'defects')));}if(upper(project?.release?.authorization)==='AUTHORIZED'){const identities=recordsForCurrentScope(project,'artifactIdentities'),identityArtifactIds=identities.map(r=>String(recordValue(r,'ARTIFACT_ID')||r.relationships?.ARTIFACT_ID||'')).filter(Boolean).sort(),authorizedIds=safe(project.release.authorizedArtifactIds).map(String).sort(),identityCurrent=releaseAccepted&&identities.length>0&&identities.every(r=>upper(recordValue(r,'AUTHORIZATION'))==='AUTHORIZED'&&truth(recordValue(r,'EXACT_HASH_MATCH'))&&truth(recordValue(r,'EXACT_SIZE_MATCH')))&&hash.sha256Value(identityArtifactIds)===hash.sha256Value(authorizedIds);if(!identityCurrent)push('STALE_DELIVERY_AUTHORIZATION','DELIVERY_AUTHORIZATION',['Delivery authorization is not backed by the current accepted release and current exact Stage 28 artifact identities.']);}return out;
}
function executionStability(project,iterationId){
  const matrix=verificationMatrix(project,iterationId),byReq={},byTest={},byReqRun=new Map(),byTestValues=new Map();
  const combine=list=>list.some(x=>x==='VIOLATED')?'VIOLATED':list.some(x=>x==='UNDETERMINED')?'UNDETERMINED':list.length&&list.every(x=>x==='SATISFIED')?'SATISFIED':'UNDETERMINED';
  for(const v of matrix.verification){
    const determination=effectiveDetermination('verification',v,testForResult(project,v),project),reqId=String(recordValue(v,'REQ_ID')||v.relationships?.REQ_ID||''),runId=String(recordValue(v,'RUN_ID')||v.relationships?.RUN_ID||''),testId=String(recordValue(v,'TEST_ID')||v.relationships?.TEST_ID||''),reqRunKey=reqId+'|'+runId;
    if(!byReqRun.has(reqRunKey))byReqRun.set(reqRunKey,[]);byReqRun.get(reqRunKey).push(determination);
    if(!byTestValues.has(testId))byTestValues.set(testId,[]);byTestValues.get(testId).push(determination);
  }
  for(const req of matrix.requirements){const rid=requirementId(req),perRun=matrix.runs.map(run=>combine(byReqRun.get(rid+'|'+recordId(run,'runs'))||[])),counts={satisfied:perRun.filter(x=>x==='SATISFIED').length,violated:perRun.filter(x=>x==='VIOLATED').length,undetermined:perRun.filter(x=>x==='UNDETERMINED').length};byReq[rid]={...counts,agreementRate:matrix.runs.length?Math.max(...Object.values(counts))/matrix.runs.length:0};}
  for(const test of recordsForScope(project,'tests',matrix.scope)){const tid=recordId(test,'tests'),vals=byTestValues.get(tid)||[],counts={satisfied:vals.filter(x=>x==='SATISFIED').length,violated:vals.filter(x=>x==='VIOLATED').length,undetermined:vals.filter(x=>x==='UNDETERMINED').length};byTest[tid]={...counts,agreementRate:matrix.runs.length?Math.max(...Object.values(counts))/matrix.runs.length:0};}
  const defects=recordsForIteration(project,'defects',iterationId),patterns=new Map(),newDefectsByRun={};for(const d of defects){const observed=recordValue(d,'OBSERVED_FAILURE'),expected=recordValue(d,'EXPECTED_CONDITION'),key=hash.sha256Value([observed===undefined?null:observed,expected===undefined?null:expected]);patterns.set(key,(patterns.get(key)||0)+1);const run=String(recordValue(d,'RUN_ID')||d.relationships?.RUN_ID||'UNASSIGNED');newDefectsByRun[run]=(newDefectsByRun[run]||0)+1;}
  return {runCount:matrix.runs.length,requirementStability:byReq,testStability:byTest,totalDistinctDefects:defects.length,repeatedDefectCount:[...patterns.values()].filter(n=>n>1).reduce((a,n)=>a+n,0),uniqueDefectCount:[...patterns.values()].filter(n=>n===1).length,newDefectsByRun,unexplainedVarianceCount:recordsForIteration(project,'comparisons',iterationId).filter(r=>truth(recordValue(r,'CORRECTNESS_AFFECTING_VARIANCE'))&&!truth(recordValue(r,'AUTHORIZED_VARIANCE'))).length};
}
function executionHandoff(project,{stage=Number(project.activeStage||0),operation=null,testIds=null,runIds=null}={}){
  const op=String(operation||'').toUpperCase(),testStages=[7,12,15,22,23,24].includes(stage)||(stage===17&&['VERIFY','REGRESSION'].includes(op))||(stage===19&&['VERIFY','REGRESSION_VERIFY'].includes(op)),ids=testIds?new Set(testIds.map(String)):null,allItems=testExecutionPlan(project).items,defectRequirementIds=stage===15?new Set(confirmedDefects(project).map(d=>String(recordValue(d,'REQ_ID')||d.relationships?.REQ_ID||'')).filter(Boolean)):null,stageItems=stage===22?allItems.filter(i=>i.testType==='DETERMINISTIC'):stage===23?allItems.filter(i=>i.testType==='MEANING'):stage===24?allItems.filter(i=>i.testType==='ADVERSARIAL'):stage===15&&defectRequirementIds?.size?allItems.filter(i=>defectRequirementIds.has(String(i.requirementId))):allItems,items=testStages?stageItems.filter(i=>!ids||ids.has(i.testId)):[],send=new Map(),withhold=new Map(),expectBack=new Map(),artifacts=recordsForCurrentScope(project,'artifacts'),artifactsById=new Map(artifacts.map(a=>[recordId(a,'artifacts'),a]));
  const exactArtifact=a=>{if(!a||upper(recordValue(a,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED')return null;const id=recordId(a,'artifacts');return {artifactId:id,filename:String(recordValue(a,'FILENAME')||id),byteSize:Number(recordValue(a,'BYTE_SIZE')||0),sha256:String(recordValue(a,'SHA256')||'UNKNOWN'),role:String(recordValue(a,'ROLE')||'AUTHORIZED_INPUT')};};
  const addArtifact=a=>{const exact=exactArtifact(a);if(!exact)return false;send.set(exact.artifactId,exact);return true;};
  const addReferenced=value=>{for(const id of new Set((JSON.stringify(value||'').match(/ARTIFACT-[A-Za-z0-9-]+/g)||[])))addArtifact(artifactsById.get(id));};
  for(const item of items){for(const a of item.handoff.send)addArtifact(artifactsById.get(a.artifactId));for(const x of item.handoff.withhold)withhold.set(x.artifactIdOrCategory,x);for(const x of item.handoff.expectBack)expectBack.set(x.kind+'|'+x.filenameOrPattern,x);}
  if(stage===1){const selectedIds=new Set(safe(project.stages?.[1]?.authorizedFiles).map(item=>String(item?.artifactId||item?.id||'')).filter(Boolean));for(const id of selectedIds)addArtifact(artifactsById.get(id));}
  const current=currentScope(project),runExecution=stage===11||(stage===17&&op==='EXECUTE_RUN')||(stage===19&&op==='EXECUTE_RUN');
  if(stage===1){for(const file of safe(project?.stages?.[1]?.authorizedFiles)){const id=String(file?.artifactId||file?.id||'').trim();if(id)addArtifact(artifactsById.get(id));}}
  if(runExecution){const candidate=recordsForCurrentScope(project,'candidateFreezes').find(c=>recordId(c,'candidateFreezes')===String(current.candidateId||''))||recordsForCurrentScope(project,'candidateFreezes').at(-1);addReferenced(recordValue(candidate,'COMPONENT_MANIFEST'));addReferenced(recordValue(candidate,'IMMUTABLE_LOCATIONS'));}
  if(stage===21){const baseline=recordsForCurrentScope(project,'baselines').find(b=>recordId(b,'baselines')===String(current.baselineId||''))||recordsForCurrentScope(project,'baselines').at(-1);addReferenced(recordValue(baseline,'IMMUTABLE_ARTIFACT_RECORDS'));addReferenced(recordValue(baseline,'APPROVED_VERSIONS'));}
  if([22,23,24,25].includes(stage))for(const a of artifacts)if(String(a.scope?.productId||'')===String(current.productId||''))addArtifact(a);
  if(stage===12){const wanted=runIds?new Set(runIds.map(String)):null;for(const run of recordsForCurrentScope(project,'runs'))if(!wanted||wanted.has(recordId(run,'runs')))addReferenced(recordValue(run,'OUTPUT_ARTIFACT_IDENTITIES'));}
  const stageWithhold={11:['outputs from other runs','reviewer feedback','failure explanations','proposed corrections'],12:['other verifiers’ determinations','Stage 13 comparison findings','root-cause analysis','correction proposals'],23:['Stage 21 generator correctness claims','unneeded deterministic pass conclusions','adversarial findings'],24:['generator reasoning or self-evaluation','prior reviewer conclusions that tell the adversarial reviewer what to find']};for(const label of stageWithhold[stage]||[])withhold.set(label,{artifactIdOrCategory:label,reason:'Withheld to preserve information isolation and reduce verification bias.'});
  const externalWork=[1,7,11,12,15,17,19,21,23,24,25].includes(stage);if(externalWork)expectBack.set('STRUCTURED_RESPONSE|final strict JSON response',{kind:'STRUCTURED_RESPONSE',filenameOrPattern:'final strict JSON response',required:true});
  return {send:[...send.values()],withhold:[...withhold.values()],expectBack:[...expectBack.values()]};
}
function canonicalTestBindingCatalog(project){
  ensureShape(project);
  const allowed=['JOB_ID','CURRENT_INPUT_VERSION','CURRENT_SOURCE_SET_VERSION','CURRENT_REQUIREMENTS_VERSION','CURRENT_TEST_SUITE_VERSION','CURRENT_INSTRUCTION_VERSION','CURRENT_ITERATION','CURRENT_BASELINE_ID','CURRENT_PRODUCT_ID'];
  const out={};
  for(const fieldName of allowed){const value=project?.job?.[fieldName];if(value===undefined||value===null||value==='')continue;const canonicalKey=`JOB.${fieldName}`;out[canonicalKey]={canonicalKey,value:clone(value),valueSha256:hash.sha256Value(value)};}
  return out;
}
function evidenceChainExplanation(project,chain){const req=String(recordValue(chain,'REQ_ID')||chain.relationships?.REQ_ID||''),requirement=recordsForCurrentScope(project,'requirements').find(r=>requirementId(r)===req)||records(project,'requirements').find(r=>requirementId(r)===req)||null,tests=safe(recordValue(chain,'TEST_ID')),results=safe(recordValue(chain,'TEST_RESULT_ID')),evidence=safe(recordValue(chain,'EVIDENCE_ID')),identities=safe(recordValue(chain,'ARTIFACT_HASH_IDENTITY')),support=[],unresolved=[...safe(recordValue(chain,'MISSING_LINKS'))],resultCollections=['verification','deterministicResults','meaningResults','adversarialResults'];for(const t of tests)support.push(t+' applies to '+req);for(const id of results){let hit=null;for(const collection of resultCollections){const record=recordsForCurrentScope(project,collection).find(r=>recordId(r,collection)===String(id));if(record){hit={collection,record};break;}}if(!hit){unresolved.push('CURRENT_RESULT:'+id);continue;}const test=testForResult(project,hit.record),effective=effectiveDetermination(hit.collection,hit.record,test,project),contract=evaluateEvidenceContract(test,hit.record,null,project),sufficiency=evaluateEvidenceSufficiency(project,{requirement,test,result:hit.record});support.push('Effective determination for '+id+' is '+effective);if(contract.sufficient&&sufficiency.sufficient)support.push((contract.evidenceIds||sufficiency.presentEvidenceIds||[]).join(', ')+' satisfies the structural evidence contract and is capable of proving the proposition for '+id);else unresolved.push('INSUFFICIENT_EVIDENCE:'+id);if(effective!=='SATISFIED')unresolved.push('NON_SATISFIED_EFFECTIVE_RESULT:'+id);}for(const e of evidence)support.push(e+' is canonical evidence linked to the current proposition');for(const id of identities){const record=recordsForCurrentScope(project,'artifactIdentities').find(x=>recordId(x,'artifactIdentities')===id);if(record&&upper(recordValue(record,'AUTHORIZATION'))==='AUTHORIZED'&&truth(recordValue(record,'EXACT_HASH_MATCH'))&&truth(recordValue(record,'EXACT_SIZE_MATCH')))support.push(id+' proves audited delivery bytes match: '+String(recordValue(record,'PRE_DELIVERY_SHA256')||'UNKNOWN'));else unresolved.push('UNAUTHORIZED_ARTIFACT_IDENTITY:'+id);}return {proposition:'Delivered artifact satisfies '+req,support,unresolved:[...new Set(unresolved)]};}

function stage16CorrectionPlan(project){
  ensureShape(project);
  const roots=recordsForCurrentScope(project,'rootCauses');
  if(!roots.length)return {actionType:'BLOCKED',heading:'Root cause required before correction',explanation:'Stage 16 cannot choose a correction point until Stage 14 has established the earliest defective layer with evidence. Return to root-cause analysis; do not patch the final output by guesswork.',items:[]};
  const items=roots.map(root=>{const defectId=String(recordValue(root,'DEFECT_ID')||root.relationships?.DEFECT_ID||''),earliestDefectiveLayer=String(recordValue(root,'EARLIEST_DEFECTIVE_LAYER')||'').trim(),rootCause=String(recordValue(root,'ROOT_CAUSE')||'').trim(),downstreamInvalidation=String(recordValue(root,'DOWNSTREAM_INVALIDATION')||'').trim(),layer=upper(earliestDefectiveLayer),cause=upper(rootCause),executionOnly=/\bEXECUTION\b|\bRUN\b/.test(layer)&&!/INSTRUCTION|REQUIREMENT|TEST|SOURCE|RESEARCH|CONFIGURATION|INPUT/.test(layer),humanAuthority=/HUMAN|USER INPUT|HUMAN AUTHORITY/.test(layer+' '+cause);if(executionOnly)return {defectId,earliestDefectiveLayer,rootCause,downstreamInvalidation,actionType:'NO_SPECIFICATION_CHANGE',requiredAction:'Preserve the governing requirement, test, and instruction; keep the regression active and rerun the required execution path. Do not invent a specification change.'};if(humanAuthority)return {defectId,earliestDefectiveLayer,rootCause,downstreamInvalidation,actionType:'HUMAN_AUTHORITY',requiredAction:'Confirm only the corrected human-owned fact or decision when requested. The application versions it and invalidates dependent downstream work.'};return {defectId,earliestDefectiveLayer,rootCause,downstreamInvalidation,actionType:'CONTROLLED_CORRECTION',requiredAction:'Correct this earliest defective controlled artifact through the current Stage 16 instruction. Do not patch downstream outputs in place.'};});
  const executionOnly=items.filter(x=>x.actionType==='NO_SPECIFICATION_CHANGE').length,human=items.filter(x=>x.actionType==='HUMAN_AUTHORITY').length,controlled=items.filter(x=>x.actionType==='CONTROLLED_CORRECTION').length,heading=controlled||human?'Correct the established root cause'+(items.length===1?'':'s'):'No specification correction required',explanation=(controlled?controlled+' controlled correction'+(controlled===1?'':'s')+' required. ':'')+(human?human+' human-authority correction'+(human===1?'':'s')+' required. ':'')+(executionOnly?executionOnly+' execution-only defect'+(executionOnly===1?' requires':'s require')+' no specification edit. ':'')+'The application preserves prior versions and determines downstream invalidation and rerun scope.';
  return {actionType:controlled?'CONTROLLED_CORRECTION':human?'HUMAN_AUTHORITY':'NO_SPECIFICATION_CHANGE',heading,explanation,items};
}
const ACTION_TYPES=Object.freeze(['RUN_APP_TESTS','CALCULATE_RELEASE','BUILD_EVIDENCE_CHAINS','FREEZE_CANDIDATE','RESERVE_RUN_BATCH','BEGIN_UNCHANGED_CONFIRMATION','FREEZE_BASELINE','REGISTER_PRODUCTION_CONTEXT','RESERVE_PRODUCT_EXECUTION','AI_REVIEW','EXTERNAL_AGENT_TOOL','HUMAN_INSPECTION','EXTERNAL_SYSTEM','ATTACH_REQUIRED_FILES','CONTINUE_AGENT_CONVERSATION','PASTE_FINAL_JSON','REVIEW_PROPOSAL','BLOCKED','COMPLETE']);
function actionReceiptState(project,stage){
  const latestReceipt=safe(project?.projectData?.outputReceipts).filter(item=>Number(item.stage)===Number(stage)).at(-1)||null;
  const latestAccepted=acceptedChanges(project,stage).at(-1)||null;
  return {
    canonicalStateChanged:latestReceipt?.canonicalStateChanged===true||Boolean(latestAccepted&&latestReceipt?.canonicalStateChanged!==false),
    acceptedChange:latestReceipt?.acceptedChange||latestReceipt?.changeId||latestAccepted?.changeId||null,
    downstreamInvalidated:safe(latestReceipt?.downstreamInvalidated||latestReceipt?.invalidatedStages),
    newPromptRequired:Boolean(latestReceipt?.newPromptRequired)
  };
}
function actionEnvelope(project,stage,values={}){
  const receipt=actionReceiptState(project,stage),stageDefinition=core.STAGES?.[Number(stage)-1],humanChecklist=safe(stageDefinition?.humanChecklist),operatorChecks=humanChecklist.length?humanChecklist:safe(stageDefinition?.completionGate);
  const action={
    actionType:'PASTE_FINAL_JSON',heading:'Complete the current stage',explanation:'Return the current final structured response and any required returned files.',primaryButton:null,secondaryAction:null,
    filesToSend:[],filesToWithhold:[],expectedReturnFiles:[],operatorChecks,blockingReason:null,
    ...receipt,...values
  };
  if(!ACTION_TYPES.includes(action.actionType))throw new Error('Unknown operational action type: '+action.actionType+'.');
  action.filesToSend=safe(action.filesToSend);action.filesToWithhold=safe(action.filesToWithhold);action.expectedReturnFiles=safe(action.expectedReturnFiles);action.operatorChecks=safe(action.operatorChecks);action.downstreamInvalidated=safe(action.downstreamInvalidated);
  return action;
}
function operationalNextAction(project,currentStage){
  const stage=Number(currentStage||1),requests=unresolvedHumanRequests(project,stage);
  const proposals=safe(project?.projectData?.responseProposals).filter(item=>Number(item.stage)===stage&&!item.invalidatedBy&&['PENDING','PENDING_OPERATOR_REVIEW'].includes(upper(item.status||item.state)));
  if(proposals.length)return actionEnvelope(project,stage,{actionType:'REVIEW_PROPOSAL',heading:'Review the proposed canonical change',explanation:'Validation succeeded. Review the current value beside the proposed value, provenance, and validation result before accepting or rejecting this proposal.',primaryButton:'Review proposal'});
  if(requests.length)return actionEnvelope(project,stage,{actionType:'CONTINUE_AGENT_CONVERSATION',heading:'Answer the current human-only question',explanation:'Continue the human conversation. Saving an answer creates a new User Job Input version, invalidates the old prompt and proposal, and requires a replacement prompt for this same stage.',primaryButton:'Continue conversation',newPromptRequired:true});
  if(stage===16){const correction=stage16CorrectionPlan(project);if(correction.actionType==='BLOCKED')return actionEnvelope(project,stage,{actionType:'BLOCKED',heading:correction.heading,explanation:correction.explanation,blockingReason:correction.explanation});if(correction.actionType==='HUMAN_AUTHORITY')return actionEnvelope(project,stage,{actionType:'CONTINUE_AGENT_CONVERSATION',heading:correction.heading,explanation:correction.explanation,primaryButton:'Provide human authority'});return actionEnvelope(project,stage,{actionType:'REVIEW_PROPOSAL',heading:correction.heading,explanation:correction.explanation,primaryButton:'Review correction'});}

  if(stage===10&&acceptedChanges(project,10).length){
    const freezes=recordsForCurrentScope(project,'candidateFreezes').filter(r=>Number(r.stage)===10&&isActiveRecord(r));
    if(!freezes.length)return actionEnvelope(project,stage,{actionType:'FREEZE_CANDIDATE',heading:'Freeze the exact test candidate',explanation:'Select the exact verified component files for this candidate. The application allocates iteration/candidate identities, hashes the actual bytes, freezes the component set, and prevents in-place mutation. No additional agent response belongs to this control step.',primaryButton:'Freeze candidate'});
  }
  if(stage===11){
    const iteration=latestIteration(project,[10]),iterationId=recordId(iteration,'iterations');
    if(iterationId){const runs=records(project,'runs').filter(r=>isActiveRecord(r)&&String(recordValue(r,'ITERATION_ID')||r.relationships?.ITERATION_ID||r.scope?.iterationId||'')===iterationId);if(!runs.length)return actionEnvelope(project,stage,{actionType:'RESERVE_RUN_BATCH',heading:'Reserve the ten independent execution lanes',explanation:'The application must create exactly ten current run identities and ten fresh context identities for the frozen candidate before any run prompt is sent. You do not allocate run IDs or context IDs.',primaryButton:'Reserve 10 runs'});if(runs.length!==10)return actionEnvelope(project,stage,{actionType:'BLOCKED',heading:'Run reservation is inconsistent',explanation:'The current iteration contains '+runs.length+' reserved run records instead of exactly ten. Do not create ad hoc runs or reuse contexts.',blockingReason:'Exactly ten application-reserved run lanes are required.'});}
  }
  if(stage===17){
    const iteration=latestIteration(project,[17]),iterationId=recordId(iteration,'iterations'),freeze=recordsForCurrentScope(project,'candidateFreezes').filter(r=>Number(r.stage)===17&&isActiveRecord(r)).at(-1);
    if(!iterationId||!freeze)return actionEnvelope(project,stage,{actionType:'FREEZE_CANDIDATE',heading:'Freeze the corrected candidate',explanation:'Select the exact corrected candidate files. The application creates the new iteration/candidate identities, hashes the exact bytes, preserves the prior candidate, and determines downstream scope. Do not continue an old execution conversation.',primaryButton:'Freeze corrected candidate'});
    const runs=records(project,'runs').filter(r=>isActiveRecord(r)&&String(recordValue(r,'ITERATION_ID')||r.relationships?.ITERATION_ID||r.scope?.iterationId||'')===iterationId);
    if(!runs.length)return actionEnvelope(project,stage,{actionType:'RESERVE_RUN_BATCH',heading:'Reserve ten new corrected-run lanes',explanation:'The corrected iteration requires exactly ten new independent run/context identities before execution. The application allocates them; prior run conversations and outputs remain outside authorized inputs.',primaryButton:'Reserve 10 runs'});
    if(runs.length!==10)return actionEnvelope(project,stage,{actionType:'BLOCKED',heading:'Corrected run reservation is inconsistent',explanation:'The corrected iteration contains '+runs.length+' current run records instead of exactly ten.',blockingReason:'Exactly ten application-reserved corrected run lanes are required.'});
  }
  if(stage===19){
    const iteration=latestIteration(project,[19]),iterationId=recordId(iteration,'iterations');
    if(!iterationId)return actionEnvelope(project,stage,{actionType:'BEGIN_UNCHANGED_CONFIRMATION',heading:'Begin the unchanged confirmation iteration',explanation:'The application first binds a new confirmation iteration to the exact converged candidate and proves the frozen versions/hashes are unchanged. No external CONFIRM_FREEZE prompt is required.',primaryButton:'Begin unchanged confirmation'});
    const runs=records(project,'runs').filter(r=>isActiveRecord(r)&&String(recordValue(r,'ITERATION_ID')||r.relationships?.ITERATION_ID||r.scope?.iterationId||'')===iterationId);
    if(!runs.length)return actionEnvelope(project,stage,{actionType:'RESERVE_RUN_BATCH',heading:'Reserve ten new confirmation lanes',explanation:'The unchanged confirmation requires ten new independent run/context identities. The application allocates them before any execution prompt is sent.',primaryButton:'Reserve 10 runs'});
    if(runs.length!==10)return actionEnvelope(project,stage,{actionType:'BLOCKED',heading:'Confirmation run reservation is inconsistent',explanation:'The confirmation iteration contains '+runs.length+' current run records instead of exactly ten.',blockingReason:'Exactly ten application-reserved confirmation run lanes are required.'});
  }
  if(stage===20&&acceptedChanges(project,20).length){
    const baselines=recordsForCurrentScope(project,'baselines').filter(isActiveRecord);
    if(!baselines.length)return actionEnvelope(project,stage,{actionType:'FREEZE_BASELINE',heading:'Authorize and freeze the production baseline',explanation:'Select the exact approved component files. The application binds them to the successful unchanged confirmation, computes byte identities, allocates the baseline identity, and freezes the immutable baseline.',primaryButton:'Freeze baseline'});
  }
  if(stage===21){
    const productionContext=records(project,'freshContexts').filter(r=>isActiveRecord(r)&&Number(r.stage)===21).at(-1);
    if(!productionContext)return actionEnvelope(project,stage,{actionType:'REGISTER_PRODUCTION_CONTEXT',heading:'Register a fresh production context',explanation:'Open a fresh external production conversation/context. Enter its identifier in the Stage 21 supporting-record control; the application binds it before creating the product execution.',primaryButton:'Register production context'});
    const products=recordsForCurrentScope(project,'products').filter(isActiveRecord);
    if(!products.length)return actionEnvelope(project,stage,{actionType:'RESERVE_PRODUCT_EXECUTION',heading:'Reserve the finished-product execution',explanation:'The application must allocate product/execution identities and bind them to the approved baseline and fresh production context before the Stage 21 production instruction is sent.',primaryButton:'Reserve product execution'});
  }

  if([9,23,24].includes(stage)){const reviewer=records(project,'freshContexts').filter(r=>isActiveRecord(r)&&Number(r.stage)===stage).at(-1);if(!reviewer){const handoff=executionHandoff(project,{stage,operation:'COMPLETE'}),preflight=stage===9;return actionEnvelope(project,stage,{actionType:'AI_REVIEW',heading:'Open a fresh independent reviewer context',explanation:preflight?'Use a new reviewer context that did not author the Stage 08 production instruction. Register its external context identifier before sending the Stage 09 preflight instruction so the application can bind the reviewer identity into the current prompt.':'Use a reviewer context that did not generate the product. Register its external context identifier before sending any product or review material so the application can bind and evaluate independence.',primaryButton:'Register reviewer context',filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack});}}
  const plan=testExecutionPlan(project),allRelevant=stage===12?plan.items:stage===22?plan.items.filter(item=>item.testType==='DETERMINISTIC'):stage===23?plan.items.filter(item=>item.testType==='MEANING'):stage===24?plan.items.filter(item=>item.testType==='ADVERSARIAL'):[],resultCollection=stage===22?'deterministicResults':stage===23?'meaningResults':stage===24?'adversarialResults':null,completedTestIds=resultCollection?new Set(recordsForCurrentScope(project,resultCollection).map(record=>String(recordValue(record,'TEST_ID')||record.relationships?.TEST_ID||'')).filter(Boolean)):new Set(),relevant=resultCollection?allRelevant.filter(item=>!completedTestIds.has(String(item.testId))):allRelevant;
  if(resultCollection&&allRelevant.length&&!relevant.length){const currentResults=recordsForCurrentScope(project,resultCollection),problems=currentResults.map(result=>evaluateResultConsistency(resultCollection,result,testForResult(project,result),project)).filter(item=>item.determination!=='SATISFIED');if(problems.length)return actionEnvelope(project,stage,{actionType:'BLOCKED',heading:'Current verification found a failure',explanation:'Every applicable '+(stage===22?'deterministic':stage===23?'meaning-based':'adversarial')+' test has a current result, but at least one current result is violated, undetermined, contradictory, or insufficiently evidenced. Preserve that result as failure evidence and return through root-cause and controlled correction; do not rerun or overwrite it merely to make the failure disappear.',blockingReason:problems.flatMap(item=>item.reasons||[]).join('; ')||'Current verification is not fully satisfied.'});}
  const blocked=relevant.find(item=>!item.executableNow);
  if(blocked){const missingBytes=!blocked.artifactReady||/artifact|bytes?|file/i.test(String(blocked.blockingReason||'')),handoff=blocked.handoff||{send:[],withhold:[],expectBack:[]};return actionEnvelope(project,stage,{actionType:missingBytes?'ATTACH_REQUIRED_FILES':'BLOCKED',heading:missingBytes?'Attach the exact required files':'Verification is blocked',explanation:blocked.blockingReason||'The declared verification route is not executable with the current capability and artifact state.',primaryButton:missingBytes?'Attach required files':null,filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack,blockingReason:blocked.blockingReason||'Required execution evidence is not currently obtainable.'});}
  const native=relevant.find(item=>item.executionMode==='APPLICATION_DETERMINISTIC'&&item.executableNow&&(item.operatorAction==='RUN_IN_APP'||item.operatorAction==='NO_ACTION'));
  if(native){const handoff=native.handoff||{send:[],withhold:[],expectBack:[]};return actionEnvelope(project,stage,{actionType:'RUN_APP_TESTS',heading:'Run application verification',explanation:'No external action is required for the current supported deterministic test. The application will execute the registered Test IR against the exact verified inputs and record an application-owned result.',primaryButton:'Run tests',filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack});}
  const item=relevant.find(item=>item.operatorAction!=='NO_ACTION');
  if(item){const handoff=item.handoff||{send:[],withhold:[],expectBack:[]},common={filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack};if(item.operatorAction==='SEND_TO_INDEPENDENT_REVIEWER')return actionEnvelope(project,stage,{...common,actionType:'AI_REVIEW',heading:'Send to a fresh independent reviewer',explanation:'Prepare the exact bounded verification package, send only the listed material, withhold the listed context, and return the contracted structured result and evidence.',primaryButton:'Prepare verification package'});if(item.operatorAction==='SEND_TO_TOOL_AGENT')return actionEnvelope(project,stage,{...common,actionType:'EXTERNAL_AGENT_TOOL',heading:'Run verification with the required external tool capability',explanation:'Use an external tool-capable environment that actually provides '+(item.requiredCapability||'the declared capability')+'. Send the exact listed files and return the contracted evidence.',primaryButton:'Prepare verification package'});if(item.operatorAction==='HUMAN_INSPECTION'){const recorded=recordsForCurrentScope(project,'evidenceRecords').find(e=>e?.source==='HUMAN_OBSERVATION'&&String(e?.humanInspectionTestId||'')===item.testId);if(recorded)return actionEnvelope(project,stage,{...common,actionType:'PASTE_FINAL_JSON',heading:'Human observation recorded — return the structured result',explanation:'The application has preserved the human-owned observation for '+item.testId+'. The current stage executor may now use that exact evidence to return the contracted final JSON; it must not invent, replace, or relabel the human observation.',primaryButton:'Paste final JSON'});return actionEnvelope(project,stage,{...common,actionType:'HUMAN_INSPECTION',heading:'Human inspection is required',explanation:'Perform the exact inspection for '+item.testId+' and record the observation directly in the application. An AI assertion that a human inspected the product is insufficient.',primaryButton:'Record human observation',testId:item.testId,requirementId:item.requirementId,requiredEvidenceDescription:item.returnRequirements?.requiredEvidenceDescription||''});}if(item.operatorAction==='USE_EXTERNAL_SYSTEM')return actionEnvelope(project,stage,{...common,actionType:'EXTERNAL_SYSTEM',heading:'Use the required external system',explanation:'Perform the declared operation in '+(item.requiredCapability||'the required external system')+' and return evidence attributable to that system.',primaryButton:'Prepare external-system package'});}
  if(stage===1){const handoff=executionHandoff(project,{stage:1,operation:'COMPLETE'});return actionEnvelope(project,stage,{actionType:'CONTINUE_AGENT_CONVERSATION',heading:'Complete the one-time Stage 01 intake',explanation:'Send the current Stage 01 instruction and every listed supplied project file to the same external conversation once. The agent must exhaustively capture the project intent and all human-authority content from those materials, ask only genuinely human-only questions, and return final JSON only when intake is complete. Later stages reuse the accepted Stage 01 capture and must not require these original project files again.',primaryButton:'Continue conversation',filesToSend:handoff.send,filesToWithhold:handoff.withhold,expectedReturnFiles:handoff.expectBack});}
  if(stage===27&&acceptedChanges(project,27).length&&!recordsForCurrentScope(project,'releaseRecords').length)return actionEnvelope(project,stage,{actionType:'CALCULATE_RELEASE',heading:'Calculate the release determination',explanation:'The advisory Stage 27 review is accepted. No agent or human may select the release state. The application must now evaluate the complete current release evidence, blockers, violations, unknowns, contradictions, baseline identity, product identity, and evidence sufficiency and record exactly one idempotent ACCEPTED, REJECTED, or BLOCKED determination.',primaryButton:'Calculate release'});
  if(stage===28)return actionEnvelope(project,stage,{actionType:'ATTACH_REQUIRED_FILES',heading:'Verify the exact delivery bytes',explanation:'No external agent response belongs to Stage 28. Open Release control, select the audited canonical files and the exact delivery files, then let the application hash and compare them one-to-one before delivery authorization.',primaryButton:null,blockingReason:null});
  if(stage===29){const reqs=mandatoryRequirements(project,currentScope(project)),chains=recordsForCurrentScope(project,'evidenceChains'),chainReqIds=new Set(chains.map(record=>String(recordValue(record,'REQ_ID')||record.relationships?.REQ_ID||''))),missing=reqs.filter(req=>!chainReqIds.has(requirementId(req)));if(!chains.length||missing.length)return actionEnvelope(project,stage,{actionType:'BUILD_EVIDENCE_CHAINS',heading:'Construct the current evidence chains',explanation:'No external agent should construct canonical evidence-chain truth. The application will build one current chain for every mandatory requirement from the exact current authority, requirement, instruction, execution, test/result, evidence, release, and artifact-identity records. If any chain remains incomplete afterward, the Stage 29 agent may investigate only the missing evidence.',primaryButton:'Build evidence chains'});}
  if(stage===4)return actionEnvelope(project,stage,{actionType:'PASTE_FINAL_JSON',heading:'Compile the complete requirement specification',explanation:'Use the current Stage 04 instruction. It already contains the exhausted Stage 01 human-authority capture, the complete Stage 03 research, and the application obligation manifest. Do not reattach or restate the original project-intent file. Return one final strict JSON response only after every obligation in the manifest has been accounted for.',primaryButton:'Paste final JSON'});
  return actionEnvelope(project,stage,{actionType:'PASTE_FINAL_JSON',heading:'Return the final structured response',explanation:'Use the current Stage '+String(stage).padStart(2,'0')+' instruction. Continue the external work or conversation until the executor is ready to return one final strict JSON response and any required files.',primaryButton:'Paste final JSON'});
}

function deterministicRuntime(){return globalThis.closedLoopTestRuntime||null;}
function applicationTestCapabilities(){const runtime=deterministicRuntime();return runtime?.capabilities?runtime.capabilities():Object.freeze([]);}
function applicationTestSupported(test){const runtime=deterministicRuntime();return Boolean(runtime?.supports&&runtime.supports(test));}
const TEST_EXECUTION_ACTIONS=Object.freeze({
  APPLICATION_DETERMINISTIC:'No operator execution is required only when the exact REQUIRED_CAPABILITY names a registered application-native test executor. Otherwise request a corrected test definition; the browser must not pretend it can run the test.',
  EXTERNAL_AGENT_TOOL:'Use the generated verification instruction in an external agent/tool environment that actually has the declared capability and exact required artifacts, then ingest its structured result and evidence.',
  INDEPENDENT_AGENT_REVIEW:'Use a fresh independent reviewer context with the declared inputs and evidence; do not reuse the producing context as its own verifier.',
  HUMAN_INSPECTION:'Perform only the irreducible human/domain inspection described by the test, preserve the observation/evidence, and make that evidence available to the verification step.',
  EXTERNAL_SYSTEM:'Obtain the result from the declared specialized system, lab, machine, or software; preserve exact report/output evidence and any returned files.',
  UNAVAILABLE:'The required capability is unavailable. Do not claim execution; this remains blocking until a valid capability or equivalent verification path exists.'
});
function capabilityAffirmativelyAvailable(project,requiredCapability,mode,test){
  const capability=upper(requiredCapability);if(!capability)return false;
  if(mode==='APPLICATION_DETERMINISTIC')return applicationTestSupported(test);
  if(['INDEPENDENT_AGENT_REVIEW','HUMAN_INSPECTION'].includes(mode))return true;
  if(mode==='UNAVAILABLE')return false;
  const declarations=[project?.job?.AVAILABLE_TOOLS,...recordsForCurrentScope(project,'freshContexts').map(record=>recordValue(record,'TOOL_AVAILABILITY'))],negative=/\b(?:UNAVAILABLE|NOT AVAILABLE|NO ACCESS|MISSING|ABSENT|UNKNOWN)\b/;
  return declarations.some(value=>{const text=upper(typeof value==='string'?value:JSON.stringify(value??''));return text.includes(capability)&&!negative.test(text);});
}
function testExecutionPlan(project){
  ensureShape(project);const evidenceById=new Map(records(project,'evidenceRecords').map(item=>[recordId(item,'evidenceRecords'),item])),artifactsById=new Map(records(project,'artifacts').map(item=>[recordId(item,'artifacts'),item])),currentArtifacts=recordsForCurrentScope(project,'artifacts'),currentArtifactById=new Map(currentArtifacts.map(a=>[recordId(a,'artifacts'),a])),activeStage=Number(project.activeStage||0),currentProductId=String(project.job?.CURRENT_PRODUCT_ID||'');
  const executorMap={APPLICATION_DETERMINISTIC:'APPLICATION',EXTERNAL_AGENT_TOOL:'EXTERNAL_AGENT_TOOL',INDEPENDENT_AGENT_REVIEW:'INDEPENDENT_REVIEWER',HUMAN_INSPECTION:'HUMAN',EXTERNAL_SYSTEM:'EXTERNAL_SYSTEM',UNAVAILABLE:'UNAVAILABLE'},actionMap={APPLICATION_DETERMINISTIC:'RUN_IN_APP',EXTERNAL_AGENT_TOOL:'SEND_TO_TOOL_AGENT',INDEPENDENT_AGENT_REVIEW:'SEND_TO_INDEPENDENT_REVIEWER',HUMAN_INSPECTION:'HUMAN_INSPECTION',EXTERNAL_SYSTEM:'USE_EXTERNAL_SYSTEM',UNAVAILABLE:'BLOCKED'};
  const bindingArtifacts=test=>{const bindings=recordValue(test,'EXECUTABLE_INPUT_BINDINGS')||{},resolved=[],issues=[];for(const [name,binding] of Object.entries(bindings)){let matches=[];if(typeof binding==='string')matches=currentArtifacts.filter(a=>recordId(a,'artifacts')===binding);else if(binding&&typeof binding==='object'){matches=currentArtifacts.filter(a=>{if(binding.artifactId&&recordId(a,'artifacts')!==String(binding.artifactId))return false;if(binding.filename&&String(recordValue(a,'FILENAME')||'')!==String(binding.filename))return false;if(binding.artifactRole&&String(recordValue(a,'ROLE')||'')!==String(binding.artifactRole))return false;if(binding.source==='CURRENT_PRODUCT'&&String(a.scope?.productId||'')!==currentProductId)return false;return true;});if(binding.source==='CURRENT_PRODUCT'&&!currentProductId&&activeStage<21)continue;}if(matches.length===1)resolved.push(recordId(matches[0],'artifacts'));else if(activeStage>=22||typeof binding==='string')issues.push(`${name} resolves to ${matches.length} verified current artifacts`);}return {artifactIds:[...new Set(resolved)],issues};};
  const items=recordsForCurrentScope(project,'tests').map(test=>{const mode=upper(recordValue(test,'EXECUTION_MODE'))||'UNSPECIFIED',requiredCapability=String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim(),artifactRequirements=String(recordValue(test,'ARTIFACT_REQUIREMENTS')||'').trim(),artifactRequired=Boolean(artifactRequirements&&!['NONE','NOT APPLICABLE','N/A'].includes(upper(artifactRequirements))),evidenceIds=safe(test.evidenceRefs).map(String),evidenceArtifactIds=evidenceIds.map(id=>evidenceById.get(id)).map(item=>String(recordValue(item,'ATTACHMENT_ID')||item?.relationships?.ATTACHMENT_ID||'').trim()).filter(Boolean),bindings=mode==='APPLICATION_DETERMINISTIC'?bindingArtifacts(test):{artifactIds:[],issues:[]},artifactIds=[...new Set([...evidenceArtifactIds,...bindings.artifactIds])],missingArtifactIds=artifactIds.filter(id=>!artifactsById.has(id)),unverifiedArtifactIds=artifactIds.filter(id=>{const artifact=artifactsById.get(id);return artifact&&upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED';}),bindingReady=mode!=='APPLICATION_DETERMINISTIC'||activeStage<22||(!bindings.issues.length&&bindings.artifactIds.length>0&&!missingArtifactIds.length&&!unverifiedArtifactIds.length),artifactReady=(!artifactRequired||(artifactIds.length>0&&!missingArtifactIds.length&&!unverifiedArtifactIds.length))&&bindingReady,applicationExecutorSupported=mode!=='APPLICATION_DETERMINISTIC'||applicationTestSupported(test),validMode=Object.prototype.hasOwnProperty.call(TEST_EXECUTION_ACTIONS,mode),capabilityReady=capabilityAffirmativelyAvailable(project,requiredCapability,mode,test),executableNow=validMode&&capabilityReady&&artifactReady,blockingReason=!validMode?'Execution mode is not validly classified.':!requiredCapability?'Required capability is missing.':mode==='UNAVAILABLE'?'Required capability is explicitly unavailable.':mode==='APPLICATION_DETERMINISTIC'&&!applicationExecutorSupported?'No application-native executor is registered for '+requiredCapability+'.':!capabilityReady?'Required capability '+requiredCapability+' is not affirmatively available in current canonical state.':!bindingReady?'Executable input binding is unresolved or ambiguous: '+bindings.issues.join('; ')+'.':!artifactReady?'Required exact artifact bytes are missing or no longer verified.':null,requiredArtifactNames=artifactIds.map(id=>String(recordValue(artifactsById.get(id),'FILENAME')||id)),operatorAction=executableNow?actionMap[mode]||'BLOCKED':'BLOCKED',requiredEvidenceDescription=String(recordValue(test,'EVIDENCE_TO_PRESERVE')||'Preserve evidence capable of establishing the tested proposition.').trim(),native=mode==='APPLICATION_DETERMINISTIC',returnRequirements={structuredResponse:!native,requiredAttachmentKinds:native?[]:(artifactRequired?['CONTRACTED_VERIFICATION_ARTIFACT_OR_REPORT']:[]),requiredEvidenceDescription:native?'Application creates deterministic execution evidence from the exact verified input bytes.':requiredEvidenceDescription},send=native?[]:artifactIds.filter(id=>artifactsById.has(id)).map(id=>{const a=artifactsById.get(id);return {artifactId:id,filename:String(recordValue(a,'FILENAME')||id),sha256:String(recordValue(a,'SHA256')||'UNKNOWN'),role:String(recordValue(a,'ROLE')||artifactRequirements||'TEST_INPUT')};}),withhold=mode==='INDEPENDENT_AGENT_REVIEW'?[{artifactIdOrCategory:'prior verifier conclusions',reason:'Preserve reviewer independence.'},{artifactIdOrCategory:'proposed corrections',reason:'Prevent expectation bias.'}]:[],expectBack=native?[]:[{kind:'STRUCTURED_RESPONSE',filenameOrPattern:'final strict JSON response',required:true},{kind:'EVIDENCE',filenameOrPattern:requiredEvidenceDescription,required:true}],executionRoute=native?'APPLICATION':mode==='INDEPENDENT_AGENT_REVIEW'?'INDEPENDENT_AI':mode==='HUMAN_INSPECTION'?'HUMAN':mode==='EXTERNAL_SYSTEM'?'EXTERNAL_SYSTEM':mode==='EXTERNAL_AGENT_TOOL'?'EXTERNAL_AGENT':'UNAVAILABLE',userAction=!executableNow?'BLOCKED':native?'RUN_IN_APP':mode==='INDEPENDENT_AGENT_REVIEW'?'SEND_VERIFICATION_PACKAGE':mode==='HUMAN_INSPECTION'?'COMPLETE_INSPECTION':mode==='EXTERNAL_SYSTEM'?'RUN_EXTERNAL_SYSTEM':'SEND_VERIFICATION_PACKAGE';return {testId:recordId(test,'tests'),requirementId:testRequirementId(test),testType:upper(recordValue(test,'TEST_TYPE'))||'UNKNOWN',executionMode:mode,requiredCapability,executorClass:executorMap[mode]||'UNAVAILABLE',executionRoute,userAction,executableNow,blockingReason,artifactRequirements,artifactRequired,evidenceIds,artifactIds,requiredArtifactIds:artifactIds,requiredArtifactNames,missingArtifactIds,unverifiedArtifactIds,artifactReady,bindingReady,bindingIssues:bindings.issues,applicationExecutorSupported,capabilityReady,operatorAction,operatorActionText:executableNow?(TEST_EXECUTION_ACTIONS[mode]||''):(blockingReason||'Blocked.'),returnRequirements,handoff:{send,withhold,expectBack}};});
  const counts=Object.fromEntries(Object.keys(TEST_EXECUTION_ACTIONS).map(mode=>[mode,items.filter(item=>item.executionMode===mode).length])),incompleteTestIds=items.filter(item=>!TEST_EXECUTION_ACTIONS[item.executionMode]||!item.requiredCapability||!item.artifactRequirements).map(item=>item.testId),unavailableTestIds=items.filter(item=>item.executionMode==='UNAVAILABLE').map(item=>item.testId),unsupportedApplicationTestIds=items.filter(item=>item.executionMode==='APPLICATION_DETERMINISTIC'&&!item.applicationExecutorSupported).map(item=>item.testId),missingArtifactTestIds=items.filter(item=>!item.artifactReady).map(item=>item.testId);return {total:items.length,counts,incompleteTestIds,unavailableTestIds,unsupportedApplicationTestIds,missingArtifactTestIds,items};
}
function recordApplicationDeterministicResult(project,{testId,productId,runtimeResult,inputArtifacts=[]}={}){
  ensureShape(project);const tid=String(testId||''),pid=String(productId||'');const test=recordsForCurrentScope(project,'tests').find(r=>recordId(r,'tests')===tid),product=recordsForCurrentScope(project,'products').find(r=>recordId(r,'products')===pid);
  if(!test||!applicationTestSupported(test))throw new Error(`Test ${tid||'UNKNOWN'} is not a valid current application-native Test IR test.`);if(!product)throw new Error(`Product ${pid||'UNKNOWN'} is not the current product.`);
  const determination=upper(runtimeResult?.determination||'UNDETERMINED');if(!['SATISFIED','VIOLATED','UNDETERMINED'].includes(determination))throw new Error('Native deterministic runtime returned an invalid determination.');if(runtimeResult?.status!=='COMPLETE'&&determination!=='UNDETERMINED')throw new Error('Incomplete native runtime execution cannot claim a conclusive determination.');
  const spec=recordValue(test,'EXECUTABLE_SPEC'),specSha256=hash.sha256Value(spec),artifactIdentities=inputArtifacts.map(a=>({artifactId:String(a.artifactId),filename:String(a.filename||''),byteSize:Number(a.byteSize),sha256:String(a.sha256||'')}));
  if(!artifactIdentities.length)throw new Error('Application-native deterministic execution requires at least one exact input artifact identity.');for(const a of artifactIdentities){const canonical=recordsForCurrentScope(project,'artifacts').find(r=>recordId(r,'artifacts')===a.artifactId);if(!canonical||upper(recordValue(canonical,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||String(recordValue(canonical,'SHA256'))!==a.sha256||Number(recordValue(canonical,'BYTE_SIZE'))!==a.byteSize)throw new Error(`Native deterministic input ${a.artifactId} is not application-verified current bytes.`);}
  const evidencePayload={testId:tid,productId:pid,determination,runtimeVersion:String(runtimeResult?.runtimeVersion||runtimeResult?.executorVersion||'closed-loop-test-runtime/1'),specVersion:String(runtimeResult?.specVersion||schema.TEST_IR.version),testSpecSha256:specSha256,inputArtifacts:artifactIdentities,observations:safe(runtimeResult?.observations),expected:runtimeResult?.expected??null,actual:runtimeResult?.actual??null};
  const identityHash=hash.sha256Value(evidencePayload),existing=recordsForCurrentScope(project,'deterministicResults').find(r=>r.source==='APPLICATION_TEST_RUNTIME'&&String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'')===tid&&String(recordValue(r,'PRODUCT_ID')||r.relationships?.PRODUCT_ID||'')===pid&&String(recordValue(r,'TEST_SPEC_SHA256')||'')===specSha256&&r.nativeExecutionIdentitySha256===identityHash);if(existing)return existing;
  if(recordsForCurrentScope(project,'deterministicResults').some(r=>String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'')===tid))throw new Error(`A current deterministic result already exists for ${tid}; invalidate/correct it before another result is committed.`);
  const evidence=commandRecord(project,'evidenceRecords',{APPLICATION_EVIDENCE_KIND:'APPLICATION_DETERMINISTIC_EXECUTION',APPLICATION_EVIDENCE_DESCRIPTION:`Application-native deterministic execution evidence for ${tid}.`,APPLICATION_EVIDENCE_CONTENT:JSON.stringify(evidencePayload),SHA256:identityHash,STATUS:'CURRENT'},{stage:22,source:'APPLICATION_TEST_RUNTIME'});
  const result=commandRecord(project,'deterministicResults',{PRODUCT_ID:pid,PRODUCT_SHA256:hash.sha256Value(artifactIdentities),TEST_ID:tid,APPLICATION_DETERMINATION:determination,RUNTIME_VERSION:evidencePayload.runtimeVersion,TEST_SPEC_SHA256:specSha256,INPUT_ARTIFACT_IDENTITIES:JSON.stringify(artifactIdentities),RUNTIME_OBSERVATIONS:JSON.stringify(safe(runtimeResult?.observations))},{stage:22,source:'APPLICATION_TEST_RUNTIME'});result.relationships={...(result.relationships||{}),PRODUCT_ID:pid,TEST_ID:tid};result.evidenceRefs=[recordId(evidence,'evidenceRecords')];result.nativeExecutionIdentitySha256=identityHash;refreshRecordHashes(result,'deterministicResults');addHistory(project,'APPLICATION_TEST_EXECUTED',{stage:22,recordId:recordId(result,'deterministicResults'),testId:tid,productId:pid,determination,evidenceId:recordId(evidence,'evidenceRecords'),testSpecSha256:specSha256,inputArtifactIds:artifactIdentities.map(x=>x.artifactId)});recalculate(project);return result;
}
function operationalMetrics(project){
  ensureShape(project);const history=safe(project.projectData.history),validations=safe(project.projectData.responseValidations),proposals=safe(project.projectData.responseProposals),releases=safe(project.projectData.releaseRecords),accepted=safe(project.projectData.acceptedChanges).length,rejected=safe(project.projectData.rejectedResponses).length,stale=proposals.filter(x=>x.status==='STALE'||x.invalidatedBy).length,defects=confirmedDefects(project,{allScopes:true}),baselineEvent=history.find(x=>x.type==='BASELINE_FROZEN')?.eventSequence||Infinity,identityMismatches=records(project,'artifactIdentities',{active:false}).filter(r=>!truth(recordValue(r,'EXACT_HASH_MATCH'))||!truth(recordValue(r,'EXACT_SIZE_MATCH'))).length,regressionRecurrences=records(project,'regressionExecutions',{active:false}).filter(r=>upper(recordValue(r,'PHASE'))!=='PRE_CORRECTION'&&['VIOLATED','FAILED','FAIL'].includes(upper(recordValue(r,'RESULT')))).length;
  const activeRuns=records(project,'runs'),iterationIds=[...new Set(activeRuns.map(r=>String(recordValue(r,'ITERATION_ID')||r.scope?.iterationId||'')).filter(Boolean))],establishedIterations=new Set(iterationIds.filter(iterationId=>evaluateContextIndependence(project,{role:'RUN_BATCH',iterationId}).determination==='APPLICATION_ESTABLISHED')),completedRuns=activeRuns.filter(r=>establishedIterations.has(String(recordValue(r,'ITERATION_ID')||r.scope?.iterationId||''))&&['COMPLETED','SATISFIED','SUCCESS','PASSED'].includes(upper(recordValue(r,'EXECUTION_STATUS')||r.completionState||r.status||''))).length,independentVerifications=records(project,'verification').filter(r=>releaseVerificationTrust(project,r).determination==='APPLICATION_ESTABLISHED').length,independentOps=completedRuns+independentVerifications,silentFailures=defects.filter(d=>Number(d.eventSequence||Infinity)>baselineEvent).length;
  return {rawResponses:safe(project.projectData.rawResponses).length,validationFailures:validations.filter(x=>x.valid===false).length,staleResponses:stale,rejectedProposals:rejected,acceptedDataChanges:accepted,clarificationCycles:safe(project.projectData.humanInputAnswers).length,controlledCorrections:history.filter(x=>['ACCEPTED_RESPONSE_INVALIDATED','STAGE_AUTHORITY_CHANGED','DOWNSTREAM_INVALIDATED'].includes(x.type)).length,storageFailures:history.filter(x=>String(x.type||'').includes('STORAGE')&&String(x.type||'').includes('FAIL')).length,gateRegressions:history.filter(x=>x.type==='DOWNSTREAM_INVALIDATED').length,releaseRejections:releases.filter(x=>upper(recordValue(x,'DETERMINATION'))==='REJECTED').length,releaseBlocks:releases.filter(x=>upper(recordValue(x,'DETERMINATION'))==='BLOCKED').length,artifactIdentityMismatches:identityMismatches,regressionRecurrences,humanCorrectionFrequency:accepted?history.filter(x=>['ACCEPTED_RESPONSE_INVALIDATED','STAGE_AUTHORITY_CHANGED'].includes(x.type)).length/accepted:0,materiallyIndependentAcceptedOperations:independentOps,observedSilentFailures:silentFailures,zeroFailure95PercentUpperBound:silentFailures===0&&independentOps>0?3/independentOps:null};
}

globalThis.closedLoopWorkflowEngine=Object.freeze({recordMigratedAcceptedChange,
  version:'closed-loop-workflow-engine/1',STAGE_STATES,FORMAL_STATES,ALL_COLLECTIONS,ACTION_TYPES,
  clone,now,safe,upper,truth,falsey,numeric,recordFields,recordValue,recordId,isActiveRecord,records,refreshRecordHashes,registerGeneratedPrompt,createHumanBlocker,reconcileArtifactCustodyVerification,resolveHumanBlocker,registerFreshContext,recordHumanDecision,recordHumanInspectionEvidence,invalidateAcceptedResponse,invalidateStageForAuthorityChange,reserveRunBatch,registerArtifactBytes,freezeCandidate,beginUnchangedConfirmationIteration,freezeBaseline,reserveProductExecution,createNewJobReset,recordApplicationDeterministicResult,
  ensureShape,addHistory,allocateId,allocateInfrastructureId,nextVersion,registerStageVersion,
  unresolvedHumanRequests,openBlockers,acceptedChanges,hasStageActivity,mandatoryRequirements,confirmedDefects,unresolvedMaterialDefects,
  currentScope,recordsForScope,recordsForCurrentScope,scopeForIteration,recordsForIteration,verificationMatrix,evaluateIteration,DERIVATIONS,coverageMetrics,convergenceMetrics,releaseMetrics,applicationTestCapabilities,capabilityAffirmativelyAvailable,canonicalTestBindingCatalog,testExecutionPlan,executionHandoff,evaluateContextIndependence,evaluateEvidenceSufficiency,evaluateEvidenceContract,releaseVerificationTrust,evaluateResultConsistency,effectiveDetermination,validateTraceIntegrity,detectCurrentContradictions,executionStability,evidenceChainExplanation,stage16CorrectionPlan,operationalNextAction,operationalMetrics,intakeCoverageManifest,parseCapturedInputSet,evaluateIntakeAccounting,obligationManifest,evaluateObligationAccounting,representationInspectionCoverage,gate,deriveStageData,recalculate,invalidateDownstream,applicationInitialFields,
  recordHumanInputVersion,recordStageConfirmation,recordReleaseDetermination,acceptedControlEvents,constructEvidenceChains,verifyArtifactIdentity
});
})();

/* INTEGRATED CONTROLLING COMPLETION 53-70 */
;(()=>{
'use strict';
const e0=globalThis.closedLoopWorkflowEngine,schema=globalThis.closedLoopWorkflowSchema,h=globalThis.closedLoopHash;
if(!e0||!schema||!h)throw new Error('Base engine/schema/hash must load before integrated completion engine.');
const VERSION='closed-loop-controlling-completion/53-70/1',E=schema.CONTROLLING_COMPLETION_ENUMS,RS=schema.RECORD_SCHEMAS;
const safe=v=>Array.isArray(v)?v:[],up=v=>String(v==null?'':v).trim().toUpperCase(),fv=(r,k)=>e0.recordValue(r,k),scope=p=>e0.currentScope(p),old=(p,c)=>e0.recordsForCurrentScope(p,c);
const list=v=>{if(Array.isArray(v))return v.map(String).map(x=>x.trim()).filter(Boolean);if(v==null||v==='')return[];if(typeof v==='string'){try{const x=JSON.parse(v);if(Array.isArray(x))return x.map(String).map(y=>y.trim()).filter(Boolean);}catch{}return v.split(/[\n,;]+/).map(x=>x.trim()).filter(Boolean);}return[String(v)];};
const NEW=['propositions','propositionEquivalenceReviews','applicabilityRecords','proofExpressions','proofObligations','semanticCoverageReviews','sourceSearchAdequacyReviews','requirementCompilationChallengeReviews','observationRecords','entailmentReviews','environmentDependencies','operationReservations','disclosureAuthorizations','backupRecords','deliveryRecords','deploymentManifests'];
const CONTROL_ARRAYS=['commandReceipts','sourceSearchContracts','semanticChallengeEvaluations'];
const LEGACY_NON_OPERATIONAL_AUDIT_COLLECTIONS=Object.freeze(['atomicityReviews','normativeClassificationReviews','controlledExceptions','crossHashEvidence','redactionRecords']);
function ensure(p){e0.ensureShape(p);for(const c of NEW)if(!Array.isArray(p.projectData[c]))p.projectData[c]=[];for(const c of CONTROL_ARRAYS)if(!Array.isArray(p.projectData[c]))p.projectData[c]=[];p.projectData.controllingCompletionVersion=VERSION;return p;}
function rid(r,c){const f=RS[c]?.idField;return String(r?.id||r?.recordId||(f?fv(r,f):'')||'').trim();}
function active(r){return r?.active!==false&&!r?.invalidatedBy&&!['RETIRED','INVALIDATED','SUPERSEDED'].includes(up(fv(r,'STATUS')||r?.validity));}
function scoped(p,c){return old(p,c).filter(active);}
function alloc(p,c){ensure(p);const k=`completion:${c}`;let n=Number(p.projectData.idCounters[k]||0),taken=new Set(safe(p.projectData[c]).map(r=>rid(r,c))),x;do{x=`${RS[c].prefix}-${String(++n).padStart(6,'0')}`;}while(taken.has(x));p.projectData.idCounters[k]=n;return x;}
function refresh(r,c){r.contentSha256=h.contentRecordSha256(r,RS[c].idField);r.recordSha256=h.recordSha256(r);r.sha256=r.recordSha256;return r;}
function put(p,c,fields,opt={}){ensure(p);const x=opt.id||alloc(p,c),t=new Date().toISOString(),all={...fields,[RS[c].idField]:x},stageValue=opt.stage==null?Number(p.activeStage||0):Number(opt.stage),r={id:x,stage:stageValue||null,createdAt:t,updatedAt:t,active:true,scope:opt.scope||scope(p),fields:all,...all,source:opt.source||'APPLICATION'};refresh(r,c);p.projectData[c].push(r);return r;}
function set(r,c,k,v){r.fields=r.fields&&typeof r.fields==='object'?r.fields:{};if(Object.prototype.hasOwnProperty.call(r.fields,k)&&h.stableStringify(r.fields[k])===h.stableStringify(v))return r;r.fields[k]=v;r[k]=v;r.updatedAt=new Date().toISOString();refresh(r,c);return r;}
function normative(v){const x=up(v);if(x.includes('CONDITIONAL'))return'CONDITIONAL';if(x.includes('OPTIONAL'))return'OPTIONAL';if(x.includes('MANDATORY')||x==='REQUIRED'||x==='TRUE'||x==='YES')return'MANDATORY';return'UNKNOWN';}
function applicabilityEvidenceIds(r){return [...new Set([...list(fv(r,'SUPPORTING_EVIDENCE_IDS')),...safe(r?.evidenceRefs).map(String)])];}
function refreshApplicability(p){
  const groups=new Map();for(const r of scoped(p,'applicabilityRecords')){const pid=String(fv(r,'SUBJECT_ID')||'');if(!groups.has(pid))groups.set(pid,[]);groups.get(pid).push(r);}
  for(const rows of groups.values()){
    const conflicting=new Set(rows.map(r=>up(fv(r,'PROPOSED_APPLICABILITY'))).filter(x=>E.applicability.includes(x))).size>1||rows.length!==1;
    for(const r of rows){const proposed=up(fv(r,'PROPOSED_APPLICABILITY')),evidenceIds=applicabilityEvidenceIds(r),reasoning=String(fv(r,'REASONING')||'').trim();let selected='UNKNOWN',truthValue='UNKNOWN',basis='NONE',freshness='UNKNOWN',reasons=[];
      if(conflicting)reasons.push('Multiple current applicability records prevent one application-selected determination.');
      else if(!E.applicability.includes(proposed))reasons.push('Applicability proposal is missing or invalid.');
      else if(proposed==='NOT_APPLICABLE'&&(!reasoning||!evidenceIds.length))reasons.push('NOT_APPLICABLE requires affirmative current evidence and accepted reasoning.');
      else if(proposed==='UNKNOWN'){reasons.push(reasoning||'Applicability remains unknown.');basis=evidenceIds.length?'EXTERNALLY_SUPPORTED':'NONE';freshness=evidenceIds.length?'CURRENT':'UNKNOWN';}
      else{selected=proposed;truthValue=proposed==='APPLICABLE'?'TRUE':'FALSE';basis=evidenceIds.length?'EXTERNALLY_SUPPORTED':proposed==='APPLICABLE'?'EXTERNALLY_SUPPORTED':'NONE';freshness='CURRENT';reasons.push(reasoning||'Accepted current semantic applicability determination.');}
      set(r,'applicabilityRecords','SELECTED_APPLICABILITY',selected);set(r,'applicabilityRecords','TRUTH_VALUE',truthValue);set(r,'applicabilityRecords','EPISTEMIC_BASIS',basis);set(r,'applicabilityRecords','CURRENT_SCOPE_STATUS','CURRENT');set(r,'applicabilityRecords','FRESHNESS_STATUS',freshness);set(r,'applicabilityRecords','CONTRADICTION_STATUS',conflicting?'CONTRADICTED':'CLEAR');set(r,'applicabilityRecords','REASONS',reasons);set(r,'applicabilityRecords','SUPPORTING_EVIDENCE_IDS',evidenceIds);
    }
  }
}
function applicability(p,pid){const a=scoped(p,'applicabilityRecords').filter(r=>String(fv(r,'SUBJECT_ID'))===String(pid));if(a.length!==1||up(fv(a[0],'CONTRADICTION_STATUS'))==='CONTRADICTED')return'UNKNOWN';const x=up(fv(a[0],'SELECTED_APPLICABILITY'));return E.applicability.includes(x)?x:'UNKNOWN';}
function props(p){return scoped(p,'propositions');}
function expr(p,pid){const rows=scoped(p,'proofExpressions').filter(r=>String(fv(r,'TARGET_PROPOSITION_ID'))===String(pid));return rows.length===1?rows[0]:null;}
const SEMANTIC_REVIEW_TARGET_FIELDS=Object.freeze({TEST:Object.freeze(['TEST_PROPOSITION_TEXT','TARGET_PROPOSITION_IDS','TESTED_SCOPE','POSITIVE_RESULT_MEANING','NEGATIVE_RESULT_MEANING','TEST_ROLE','REQ_ID','EXECUTABLE_KIND','EXECUTABLE_SPEC_VERSION','EXECUTABLE_SPEC','EXECUTABLE_INPUT_BINDINGS']),PROOF_EXPRESSION:Object.freeze(['TARGET_PROPOSITION_ID','NORMALIZED_EXPRESSION'])});
const placeholderContext=value=>['','UNKNOWN','NONE','NOT APPLICABLE','UNASSIGNED','PENDING'].includes(up(value));
function semanticScope(p){const current={...scope(p)};delete current.projectRevision;delete current.contextId;delete current.runId;return current;}
const STAGE_SCOPE_KEYS=Object.freeze({1:Object.freeze(['inputVersion']),2:Object.freeze(['inputVersion','sourceSetVersion']),3:Object.freeze(['inputVersion','sourceSetVersion']),4:Object.freeze(['inputVersion','sourceSetVersion','requirementsVersion']),5:Object.freeze(['inputVersion','sourceSetVersion','requirementsVersion']),6:Object.freeze(['inputVersion','sourceSetVersion','requirementsVersion','testSuiteVersion'])});
function stageSemanticScope(p,stage){const current=semanticScope(p),keys=STAGE_SCOPE_KEYS[Number(stage)]||Object.keys(current);return Object.fromEntries(keys.filter(key=>Object.prototype.hasOwnProperty.call(current,key)).map(key=>[key,current[key]]));}
function stageScopedRows(p,collection,stage){const expected=stageSemanticScope(p,stage),keys=Object.keys(expected);return e0.records(p,collection).filter(record=>active(record)&&keys.every(key=>{const value=record.scope?.[key];return value==null||value===''||value==='NOT APPLICABLE'||expected[key]===value;}));}
function semanticTarget(p,targetKind,targetId){const kind=up(targetKind),collection=kind==='TEST'?'tests':kind==='PROOF_EXPRESSION'?'proofExpressions':'',matches=collection?stageScopedRows(p,collection,6).filter(item=>rid(item,collection)===String(targetId||'')):[];if(matches.length!==1)return null;const record=matches[0];return{kind,collection,record,id:rid(record,collection)};}
function semanticReviewTargetHash(p,targetKind,targetId){const target=semanticTarget(p,targetKind,targetId);if(!target)return'';const semanticFields={};for(const name of SEMANTIC_REVIEW_TARGET_FIELDS[target.kind]){if(target.kind==='PROOF_EXPRESSION'&&name==='NORMALIZED_EXPRESSION'){const proposed=fv(target.record,'PROPOSED_EXPRESSION'),validation=schema.validateProofExpression(proposed);semanticFields[name]=validation.valid?schema.normalizeProofExpression(proposed):null;}else semanticFields[name]=fv(target.record,name)??null;}return h.sha256Value({targetKind:target.kind,targetId:target.id,semanticFields});}
function reviewTargetFromSlot(slot){const match=String(slot||'').match(/^SEMANTIC_REVIEW:(TEST|PROOF_EXPRESSION):(.+)$/);return match?{kind:match[1],id:match[2]}:null;}
function acceptedDataChange(p,record,collection){const rawResponseId=String(record?.rawResponseId||'').trim(),proposalId=String(record?.sourceProposalId||'').trim(),recordId=rid(record,collection);if(!rawResponseId||!recordId)return null;return safe(p.projectData.acceptedChanges).find(change=>change.status==='COMMITTED'&&!change.invalidatedBy&&String(change.rawResponseId||'')===rawResponseId&&(!proposalId||String(change.proposalId||'')===proposalId)&&safe(change.canonicalRecordIds).map(String).includes(recordId))||null;}
function acceptedSemanticAuthorBinding(p,target){
  if(!target)return null;const change=acceptedDataChange(p,target.record,target.collection);if(!change||Number(change.stage)!==6||up(change.operation||'COMPLETE')!=='COMPLETE')return null;
  const rawResponseId=String(target.record?.rawResponseId||change.rawResponseId||''),rawMatches=safe(p.projectData.rawResponses).filter(record=>record.rawResponseId===rawResponseId&&!record.invalidatedBy),raw=rawMatches.length===1?rawMatches[0]:null,promptId=String(change.promptId||raw?.promptInstructionId||''),promptMatches=safe(p.projectData.generatedPrompts).filter(record=>String(record.instructionId||record.promptId)===promptId&&!record.invalidatedBy),prompt=promptMatches.length===1?promptMatches[0]:null,contextId=String(change.scope?.contextId||raw?.contextId||prompt?.scope?.contextId||''),contextMatches=e0.records(p,'freshContexts').filter(record=>active(record)&&Number(record.stage)===6&&rid(record,'freshContexts')===contextId),context=contextMatches.length===1?contextMatches[0]:null,externalContextId=String(fv(context,'EXTERNAL_CONTEXT_IDENTIFIER')||'');
  if(!raw||!prompt||!context||placeholderContext(contextId)||placeholderContext(externalContextId)||String(raw.contextId||'')!==contextId||String(raw.promptInstructionId||'')!==promptId||String(prompt.scope?.contextId||'')!==contextId||String(change.scope?.contextId||'')!==contextId||up(prompt.operation||'COMPLETE')!=='COMPLETE')return null;
  const authorScope=change.scope||{},promptScope=prompt.scope||{},contextScope=context.scope||{},stageKeys=STAGE_SCOPE_KEYS[6];for(const key of stageKeys){const authorValue=authorScope[key]??null;if((promptScope[key]??null)!==authorValue||(contextScope[key]??null)!==authorValue)return null;}
  const currentVersion=String(p.job?.CURRENT_TEST_SUITE_VERSION||''),authorVersion=String(authorScope.testSuiteVersion||''),targetVersion=String(target.record?.scope?.testSuiteVersion||'');if(!currentVersion||targetVersion!==currentVersion)return null;if(authorVersion!==currentVersion){const transitions=safe(p.projectData.artifactVersions).filter(record=>Number(record.stage)===6&&String(record.kind||'')==='TEST-SUITE'&&String(record.acceptedChangeId||'')===String(change.changeId||'')&&String(record.version||'')===currentVersion);if(transitions.length!==1)return null;}
  return{change,raw,prompt,rawResponseId,promptId,contextId,context,externalContextId,authorVersion,currentVersion};
}
function semanticCoverageReviewAssessment(p,review){
  const reviewId=rid(review,'semanticCoverageReviews'),reviewChange=acceptedDataChange(p,review,'semanticCoverageReviews'),reviewRawId=String(review?.rawResponseId||reviewChange?.rawResponseId||''),reviewRaw=safe(p.projectData.rawResponses).find(item=>item.rawResponseId===reviewRawId&&!item.invalidatedBy),reviewPromptId=String(reviewChange?.promptId||reviewRaw?.promptInstructionId||''),reviewPrompt=safe(p.projectData.generatedPrompts).find(item=>String(item.instructionId||item.promptId)===reviewPromptId&&!item.invalidatedBy),reservationId=String(fv(review,'OPERATION_RESERVATION_ID')||reviewPrompt?.operationBinding?.operationReservationId||reviewRaw?.operationReservationId||''),reservation=stageScopedRows(p,'operationReservations',6).find(item=>rid(item,'operationReservations')===reservationId),slot=reviewTargetFromSlot(fv(reservation,'TARGET_SLOT')||reviewPrompt?.operationBinding?.targetSlot||reviewRaw?.targetSlot),target=slot?semanticTarget(p,slot.kind,slot.id):null,reasons=[];
  if(!reviewChange||up(reviewChange.operation)!=='SEMANTIC_REVIEW')reasons.push('Review is not a current accepted Stage 6 SEMANTIC_REVIEW response.');
  if(!reviewPrompt||up(reviewPrompt.operation)!=='SEMANTIC_REVIEW')reasons.push('Review prompt is missing, stale, or has the wrong operation.');
  if(!reservation||up(fv(reservation,'STATUS'))!=='ACCEPTED')reasons.push('Review lacks one accepted current operation reservation.');
  if(!slot||!target)reasons.push('Reservation target slot does not resolve to one current test or proof expression.');
  const targetHash=target?semanticReviewTargetHash(p,target.kind,target.id):'',providedTargetHash=String(reviewPrompt?.contextManifest?.semanticReviewTarget?.targetSemanticHash||fv(review,'TARGET_SEMANTIC_HASH')||'');if(!targetHash||providedTargetHash!==targetHash)reasons.push('Reviewed target semantic hash is missing, wrong, or stale.');
  const authorBinding=acceptedSemanticAuthorBinding(p,target),authorRawId=String(authorBinding?.rawResponseId||target?.record?.rawResponseId||''),authorRaw=authorBinding?.raw||null,authorPromptId=String(authorBinding?.promptId||''),authorPrompt=authorBinding?.prompt||null,authorChange=authorBinding?.change||null,authorContext=String(authorBinding?.contextId||''),reviewerContext=String(reviewRaw?.contextId||reviewPrompt?.scope?.contextId||'');
  const currentContexts=stageScopedRows(p,'freshContexts',6),authorContextRecord=authorBinding?.context||null,reviewerContextRecord=currentContexts.find(item=>rid(item,'freshContexts')===reviewerContext&&Number(item.stage)===6),authorExternalContext=String(authorBinding?.externalContextId||''),reviewerExternalContext=String(fv(reviewerContextRecord,'EXTERNAL_CONTEXT_IDENTIFIER')||'');
  if(!authorBinding)reasons.push('Review target lacks an exact accepted Stage 6 COMPLETE author binding or its controlled test-suite version transition is stale.');
  if(!authorPrompt||!authorContextRecord)reasons.push('Semantic coverage author context is missing, placeholder, stale, or not bound consistently to the accepted Stage 6 COMPLETE response.');
  if(!reviewRawId||reviewRawId===authorRawId)reasons.push('Semantic coverage cannot be self-reviewed in the author raw response.');
  if(!reviewPromptId||reviewPromptId===authorPromptId)reasons.push('Semantic coverage reviewer must use a distinct controlling prompt.');
  if(!reviewerContextRecord||!independentReviewerContext(reviewerContextRecord)||placeholderContext(reviewerContext)||placeholderContext(reviewerExternalContext)||reviewerContext===authorContext||reviewerExternalContext===authorExternalContext)reasons.push('Semantic coverage reviewer context is missing, placeholder, stale, contaminated, not registered as an independent reviewer, or reused from the author.');
  const manifestTarget=reviewPrompt?.contextManifest?.semanticReviewTarget||{},manifestIsolation=reviewPrompt?.contextManifest?.semanticReviewIsolation||{},targetRecords=safe(reviewPrompt?.contextManifest?.readCollections?.[target?.collection]),targetOnly=target&&targetRecords.length===1&&String(targetRecords[0]?.id||'')===target.id,reviewRecords=safe(reviewPrompt?.contextManifest?.readCollections?.semanticCoverageReviews),applicationInputIsolation=Boolean(targetOnly&&!reviewRecords.length&&up(manifestIsolation.applicationInputIsolation)==='APPLICATION_ESTABLISHED'&&up(manifestTarget.targetKind)===target?.kind&&String(manifestTarget.targetId||'')===target?.id&&String(manifestTarget.targetSemanticHash||'')===targetHash&&String(manifestTarget.authorRawResponseId||'')===authorRawId&&String(manifestTarget.authorPromptId||'')===authorPromptId&&String(manifestTarget.authorContextId||'')===authorContext);
  if(!applicationInputIsolation)reasons.push('Application-generated reviewer input isolation is not established for the exact target projection.');
  const evidenceIds=safe(review.evidenceRefs).map(String),evidenceReady=evidenceIds.length>0&&evidenceIds.every(id=>e0.records(p,'evidenceRecords').some(item=>rid(item,'evidenceRecords')===id&&active(item)));if(!evidenceReady)reasons.push('Semantic review lacks current canonical evidence.');
  const disposition=up(fv(review,'SEMANTIC_COVERAGE_DISPOSITION'));if(!E.coverage.includes(disposition))reasons.push('Semantic review disposition is invalid.');if(!String(fv(review,'REASONING')||'').trim())reasons.push('Semantic review reasoning is empty.');
  const current=reasons.length===0,status=current?'ACCEPTED':target&&providedTargetHash&&providedTargetHash!==targetHash?'STALE':'BLOCKED';return{reviewId,current,status,disposition,target,targetHash,authorRawId,authorContext,authorPromptId,reviewRawId,reviewerContext,reviewPromptId,reservationId,packageId:String(fv(reservation,'PACKAGE_ID')||reviewPrompt?.operationBinding?.packageId||''),applicationInputIsolation:applicationInputIsolation?'APPLICATION_ESTABLISHED':'VIOLATED',reviewerContextDistinct:Boolean(authorContextRecord&&reviewerContextRecord&&!placeholderContext(authorExternalContext)&&!placeholderContext(reviewerExternalContext)&&reviewerContext!==authorContext&&reviewerExternalContext!==authorExternalContext),rawResponseDistinct:Boolean(reviewRawId&&authorRawId&&reviewRawId!==authorRawId),reasons:[...new Set(reasons)]};
}
function refreshProofSemantics(p){
  const reviewScope=stageSemanticScope(p,6),byTarget=new Map();for(const review of stageScopedRows(p,'semanticCoverageReviews',6)){const assessment=semanticCoverageReviewAssessment(p,review);for(const [field,value]of Object.entries({TARGET_KIND:assessment.target?.kind||'',TARGET_TEST_ID:assessment.target?.kind==='TEST'?assessment.target.id:'',TARGET_PROOF_EXPRESSION_ID:assessment.target?.kind==='PROOF_EXPRESSION'?assessment.target.id:'',TARGET_SEMANTIC_HASH:assessment.targetHash,AUTHOR_RAW_RESPONSE_ID:assessment.authorRawId,AUTHOR_CONTEXT_ID:assessment.authorContext,AUTHOR_PROMPT_ID:assessment.authorPromptId,REVIEWER_RAW_RESPONSE_ID:assessment.reviewRawId,REVIEWER_CONTEXT_ID:assessment.reviewerContext,REVIEWER_PROMPT_ID:assessment.reviewPromptId,OPERATION_RESERVATION_ID:assessment.reservationId,PACKAGE_ID:assessment.packageId,APPLICATION_INPUT_ISOLATION:assessment.applicationInputIsolation,REVIEWER_CONTEXT_DISTINCT:assessment.reviewerContextDistinct,RAW_RESPONSE_DISTINCT:assessment.rawResponseDistinct,CURRENT_SCOPE:reviewScope,CURRENT_SCOPE_HASH:h.sha256Value(reviewScope),ACCEPTED_STATUS:assessment.status,EPISTEMIC_BASIS:assessment.current?'EXTERNALLY_SUPPORTED':'NONE',FRESHNESS_STATUS:assessment.status==='STALE'?'EXPIRED':assessment.current?'CURRENT':'UNKNOWN',INDEPENDENCE_REASONS:assessment.reasons}))set(review,'semanticCoverageReviews',field,value);if(assessment.current&&assessment.target){const key=`${assessment.target.kind}:${assessment.target.id}`,rows=byTarget.get(key)||[];rows.push(assessment);byTarget.set(key,rows);}}
  for(const expression of stageScopedRows(p,'proofExpressions',6)){
    const proposed=fv(expression,'PROPOSED_EXPRESSION'),validation=schema.validateProofExpression(proposed),reviews=byTarget.get(`PROOF_EXPRESSION:${rid(expression,'proofExpressions')}`)||[],dispositions=new Set(reviews.map(item=>item.disposition)),equivalent=dispositions.size===1&&dispositions.has('EQUIVALENT'),aggregate=equivalent?'EQUIVALENT':dispositions.has('NOT_EQUIVALENT')&&!dispositions.has('EQUIVALENT')?'NOT_EQUIVALENT':dispositions.has('PARTIAL')&&dispositions.size===1?'PARTIAL':'UNKNOWN';
    if(validation.valid){set(expression,'proofExpressions','NORMALIZED_EXPRESSION',schema.normalizeProofExpression(proposed));set(expression,'proofExpressions','CURRENT_SCOPE_HASH',h.sha256Value(reviewScope));}
    set(expression,'proofExpressions','SEMANTIC_EQUIVALENCE_DISPOSITION',aggregate);set(expression,'proofExpressions','ACCEPTED_SEMANTIC_REVIEW_IDS',equivalent?reviews.map(item=>item.reviewId):[]);
    set(expression,'proofExpressions','EVALUATION_STATUS',validation.valid?'STRUCTURALLY_VALID':'INVALID');
  }
  for(const test of stageScopedRows(p,'tests',6)){
    const reviews=byTarget.get(`TEST:${rid(test,'tests')}`)||[],dispositions=new Set(reviews.map(item=>item.disposition)),equivalent=dispositions.size===1&&dispositions.has('EQUIVALENT');set(test,'tests','SEMANTIC_REVIEW_IDS',equivalent?reviews.map(item=>item.reviewId):[]);
  }
}
function proofLeafTestIds(node,out=new Set()){if(!node||typeof node!=='object'||Array.isArray(node))return out;if(up(node.type||node.op)==='LEAF'&&node.testId)out.add(String(node.testId));for(const child of safe(node.children))proofLeafTestIds(child,out);return out;}
function acceptedExpressionTestIds(p,pid){const x=expr(p,pid);if(!x||up(fv(x,'SEMANTIC_EQUIVALENCE_DISPOSITION'))!=='EQUIVALENT'||!list(fv(x,'ACCEPTED_SEMANTIC_REVIEW_IDS')).length)return new Set();return proofLeafTestIds(fv(x,'NORMALIZED_EXPRESSION')||fv(x,'PROPOSED_EXPRESSION'));}
function testReleaseBearing(p,t){const targets=list(fv(t,'TARGET_PROPOSITION_IDS')),coverage=up(fv(t,'SEMANTIC_COVERAGE_DISPOSITION')),reviews=list(fv(t,'SEMANTIC_REVIEW_IDS')),role=up(fv(t,'TEST_ROLE')),id=rid(t,'tests');if(!targets.length||coverage!=='EQUIVALENT'||!reviews.length||!['REQUIRED_PROOF','SUPPORTING_PROOF','REGRESSION'].includes(role))return false;if(role==='REQUIRED_PROOF')return true;return targets.some(pid=>acceptedExpressionTestIds(p,pid).has(id));}
function tests(p,pid){return old(p,'tests').filter(t=>list(fv(t,'TARGET_PROPOSITION_IDS')).includes(pid)&&testReleaseBearing(p,t));}
function markTests(p){for(const t of old(p,'tests'))set(t,'tests','RELEASE_BEARING',testReleaseBearing(p,t));}
function validateNode(node){const result=schema.validateProofExpression(node);return{valid:result.valid,reason:result.valid?'VALID':result.issues.join(' '),issues:result.issues};}
function observationEvidenceIds(record){return[...new Set([...list(fv(record,'SOURCE_EVIDENCE_IDS')),...safe(record?.evidenceRefs).map(String)])];}
function observationAssessment(p,observationId,allowedBases=null){
  const rows=scoped(p,'observationRecords').filter(record=>rid(record,'observationRecords')===String(observationId));
  if(rows.length!==1)return{admissible:false,truthValue:'UNKNOWN',basis:'NONE',freshnessStatus:'UNKNOWN',reasons:[rows.length?'Duplicate current observation identity.':'Observation is missing or stale.'],evidenceIds:[]};
  const record=rows[0],origin=up(fv(record,'OBSERVATION_ORIGIN')),basis=up(fv(record,'EPISTEMIC_BASIS')),freshnessEvaluation=evaluateFreshnessPolicy(record),freshness=freshnessEvaluation.status,evidenceIds=observationEvidenceIds(record),reasons=[];
  if(!E.origin.includes(origin))reasons.push('Observation origin is missing or invalid.');
  if(!E.basis.includes(basis)||basis==='NONE')reasons.push('Observation has no admissible epistemic basis.');
  if(Array.isArray(allowedBases)&&allowedBases.length&&!allowedBases.includes(basis))reasons.push(`Epistemic basis ${basis||'NONE'} is not allowed by the proof obligation.`);
  if(!['CURRENT','NOT_APPLICABLE'].includes(freshness))reasons.push(`Observation freshness is ${freshness||'UNKNOWN'}: ${freshnessEvaluation.reasons.join(' ')}`);
  if(origin==='NATIVE_APPLICATION_OBSERVATION'&&basis!=='APPLICATION_OBSERVED')reasons.push('Native application observation must use APPLICATION_OBSERVED basis.');
  if(origin==='EXTERNAL_CLAIM'&&['APPLICATION_OBSERVED','VERIFIED_EXTERNAL'].includes(basis))reasons.push('An external claim cannot be elevated to application-observed or verified-external basis by normalization.');
  if(origin==='AGENT_SEMANTIC_OBSERVATION'&&basis==='APPLICATION_OBSERVED')reasons.push('An agent semantic observation cannot use application-observed basis.');
  if(origin==='HUMAN_OBSERVATION'&&['APPLICATION_OBSERVED','VERIFIED_EXTERNAL'].includes(basis))reasons.push('A self-asserted human observation cannot be elevated by storage or normalization.');
  if(origin==='VERIFIED_EXTERNAL_OBSERVATION'&&basis==='VERIFIED_EXTERNAL'&&!String(fv(record,'RAW_OR_NATIVE_PROVENANCE')||'').trim())reasons.push('Verified external observation lacks objective verification provenance.');
  if(!evidenceIds.length&&!String(fv(record,'RAW_OR_NATIVE_PROVENANCE')||'').trim())reasons.push('Observation has no source evidence or raw/native provenance.');
  return{record,admissible:reasons.length===0,truthValue:'UNKNOWN',basis,freshnessStatus:freshness,reasons,evidenceIds,origin};
}
function obs(p,observationId,allowedBases=null){const assessment=observationAssessment(p,observationId,allowedBases);return assessment.admissible?assessment.record:null;}
function entail(p,propositionId){return scoped(p,'entailmentReviews').filter(record=>String(fv(record,'TARGET_PROPOSITION_ID'))===String(propositionId)&&up(fv(record,'ACCEPTED_STATUS')||'ACCEPTED')==='ACCEPTED');}
function propositionObligation(p,propositionId){return scoped(p,'proofObligations').find(record=>String(fv(record,'PROPOSITION_ID'))===String(propositionId))||null;}
function allowedBasesFor(p,propositionId){const obligation=propositionObligation(p,propositionId),configured=list(fv(obligation,'ALLOWED_EPISTEMIC_BASES')).map(up).filter(x=>E.basis.includes(x)&&x!=='NONE');return configured.length?configured:['APPLICATION_OBSERVED','VERIFIED_EXTERNAL','EXTERNALLY_SUPPORTED','SELF_ASSERTED'];}
function observedTruthDetail(p,propositionId,observationId='',subjectId=''){
  const establishes=[],refutes=[],supportingEvidenceIds=new Set(),reviewIds=[],reasons=[],allowed=allowedBasesFor(p,propositionId);
  for(const review of entail(p,propositionId)){
    const oid=String(fv(review,'OBSERVATION_ID')||'');if(observationId&&oid!==String(observationId))continue;
    const assessment=observationAssessment(p,oid,allowed),observedSubject=String(fv(assessment.record,'SUBJECT_ID')||'');if(subjectId&&observedSubject!==String(subjectId))continue;if(!subjectId&&observedSubject!==String(propositionId)){reasons.push(`${oid||'UNKNOWN'}: observation subject ${observedSubject||'UNKNOWN'} does not exactly identify proposition ${propositionId}; test, artifact, and dependency observations must enter through their accepted proof-expression leaf.`);continue;}
    if(!assessment.admissible){reasons.push(...assessment.reasons.map(reason=>`${oid||'UNKNOWN'}: ${reason}`));continue;}
    const relation=up(fv(review,'ACCEPTED_RELATION')||fv(review,'ENTAILMENT_FINDING'));reviewIds.push(rid(review,'entailmentReviews'));for(const id of assessment.evidenceIds)supportingEvidenceIds.add(id);
    if(relation==='ESTABLISHES')establishes.push({review,assessment});else if(relation==='REFUTES')refutes.push({review,assessment});else if(relation==='UNKNOWN')reasons.push(`${rid(review,'entailmentReviews')} has UNKNOWN entailment.`);
  }
  const contradicted=establishes.length>0&&refutes.length>0,truthValue=refutes.length?'FALSE':establishes.length?'TRUE':'UNKNOWN',bases=[...new Set([...establishes,...refutes].map(x=>x.assessment.basis))],freshness=[...new Set([...establishes,...refutes].map(x=>x.assessment.freshnessStatus))];
  return{truthValue,contradictionStatus:contradicted?'CONTRADICTED':'CLEAR',epistemicBasis:bases.length===1?bases[0]:bases.length?'EXTERNALLY_SUPPORTED':'NONE',freshnessStatus:freshness.every(x=>['CURRENT','NOT_APPLICABLE'].includes(x))&&freshness.length?(freshness.every(x=>x==='NOT_APPLICABLE')?'NOT_APPLICABLE':'CURRENT'):'UNKNOWN',reasons,supportingEvidenceIds:[...supportingEvidenceIds],reviewIds,establishes,refutes};
}
function observedTruth(p,propositionId,observationId=''){const x=observedTruthDetail(p,propositionId,observationId);return x.contradictionStatus==='CONTRADICTED'?'CONTRADICTED':x.truthValue;}
function leafDetail(p,propositionId,node){
  if(node.observationId){const detail=observedTruthDetail(p,propositionId,node.observationId);return{...detail,leafType:'OBSERVATION',leafId:String(node.observationId)};}
  if(node.testId){if(!tests(p,propositionId).some(test=>rid(test,'tests')===String(node.testId)))return{truthValue:'UNKNOWN',contradictionStatus:'CLEAR',epistemicBasis:'NONE',freshnessStatus:'UNKNOWN',reasons:['Test is not a current release-bearing test for this proposition.'],supportingEvidenceIds:[],reviewIds:[],leafType:'TEST',leafId:String(node.testId)};const detail=observedTruthDetail(p,propositionId,'',String(node.testId));return{...detail,leafType:'TEST',leafId:String(node.testId)};}
  if(node.artifactId){const rows=old(p,'artifacts').filter(record=>rid(record,'artifacts')===String(node.artifactId)),artifact=rows.length===1?rows[0]:null,valid=Boolean(artifact&&up(fv(artifact,'AVAILABILITY'))==='BYTES_PERSISTED_AND_VERIFIED'&&/^[a-f0-9]{64}$/i.test(String(fv(artifact,'SHA256')||''))&&Number.isSafeInteger(Number(fv(artifact,'BYTE_SIZE')))&&Number(fv(artifact,'BYTE_SIZE'))>=0);return{truthValue:valid?'TRUE':'UNKNOWN',contradictionStatus:rows.length>1?'CONTRADICTED':'CLEAR',epistemicBasis:valid?'APPLICATION_OBSERVED':'NONE',freshnessStatus:valid?'CURRENT':'UNKNOWN',reasons:valid?[]:['Artifact bytes are unavailable, unverified, duplicated, or lack an authoritative digest/size.'],supportingEvidenceIds:valid?[rid(artifact,'artifacts')]:[],reviewIds:[],leafType:'ARTIFACT',leafId:String(node.artifactId)};}
  if(node.dependencyId){const rows=scoped(p,'environmentDependencies').filter(record=>rid(record,'environmentDependencies')===String(node.dependencyId)),dependency=rows.length===1?rows[0]:null,truthValue=dependency&&E.truth.includes(up(fv(dependency,'TRUTH_VALUE')))?up(fv(dependency,'TRUTH_VALUE')):'UNKNOWN',basis=dependency&&E.basis.includes(up(fv(dependency,'EPISTEMIC_BASIS')))?up(fv(dependency,'EPISTEMIC_BASIS')):'NONE',freshnessEvaluation=dependency?evaluateFreshnessPolicy(dependency):{status:'UNKNOWN',reasons:['Dependency is missing.']},freshness=freshnessEvaluation.status,reasons=[];if(rows.length!==1)reasons.push(rows.length?'Duplicate current dependency identity.':'Environment dependency is missing or stale.');if(!['CURRENT','NOT_APPLICABLE'].includes(freshness))reasons.push(`Dependency freshness is ${freshness}: ${freshnessEvaluation.reasons.join(' ')}`);if(basis==='NONE')reasons.push('Dependency has no admissible epistemic basis.');return{truthValue:reasons.length? 'UNKNOWN':truthValue,contradictionStatus:rows.length>1||up(fv(dependency,'CONTRADICTION_STATUS'))==='CONTRADICTED'?'CONTRADICTED':'CLEAR',epistemicBasis:basis,freshnessStatus:freshness,reasons,supportingEvidenceIds:list(fv(dependency,'SUPPORTING_EVIDENCE_IDS')),reviewIds:list(fv(dependency,'DERIVATION_OR_REVIEW_IDS')),leafType:'DEPENDENCY',leafId:String(node.dependencyId)};}
  return{truthValue:'UNKNOWN',contradictionStatus:'CLEAR',epistemicBasis:'NONE',freshnessStatus:'UNKNOWN',reasons:['Unknown proof leaf.'],supportingEvidenceIds:[],reviewIds:[]};
}
function evalExpr(p,propositionId,node){
  const validation=validateNode(node);if(!validation.valid)return{truthValue:'UNKNOWN',epistemicBasis:'NONE',currentScopeStatus:'CURRENT',freshnessStatus:'UNKNOWN',contradictionStatus:'CLEAR',reasons:[validation.reason],supportingEvidenceIds:[],derivationOrReviewIds:[],leaves:[]};
  const leaves=[],evaluated=schema.evaluateProofExpression(node,({kind,id})=>{const detail=leafDetail(p,propositionId,{[kind]:id,type:'LEAF'});leaves.push(detail);return detail.truthValue;}),truthValue=evaluated.truthValue,contradicted=leaves.some(x=>x.contradictionStatus==='CONTRADICTED'),bases=[...new Set(leaves.map(x=>x.epistemicBasis).filter(x=>x&&x!=='NONE'))],freshness=leaves.map(x=>x.freshnessStatus),reasons=leaves.flatMap(x=>x.reasons||[]);
  return{truthValue,epistemicBasis:bases.length===1?bases[0]:bases.length?'EXTERNALLY_SUPPORTED':'NONE',currentScopeStatus:'CURRENT',freshnessStatus:freshness.length&&freshness.every(x=>['CURRENT','NOT_APPLICABLE'].includes(x))?(freshness.every(x=>x==='NOT_APPLICABLE')?'NOT_APPLICABLE':'CURRENT'):'UNKNOWN',contradictionStatus:contradicted?'CONTRADICTED':'CLEAR',reasons:[...new Set(reasons)],supportingEvidenceIds:[...new Set(leaves.flatMap(x=>x.supportingEvidenceIds||[]))],derivationOrReviewIds:[...new Set(leaves.flatMap(x=>x.reviewIds||[]))],leaves};
}
function pstate(p,proposition){
  const propositionId=rid(proposition,'propositions'),direct=observedTruthDetail(p,propositionId),expression=expr(p,propositionId),reasons=[...direct.reasons],reviewIds=[...direct.reviewIds],supportingEvidenceIds=[...direct.supportingEvidenceIds];let evaluated={truthValue:'UNKNOWN',epistemicBasis:'NONE',freshnessStatus:'UNKNOWN',contradictionStatus:'CLEAR',reasons:[],supportingEvidenceIds:[],derivationOrReviewIds:[]};
  if(expression&&up(fv(expression,'SEMANTIC_EQUIVALENCE_DISPOSITION'))==='EQUIVALENT'&&list(fv(expression,'ACCEPTED_SEMANTIC_REVIEW_IDS')).length)evaluated=evalExpr(p,propositionId,fv(expression,'NORMALIZED_EXPRESSION')||fv(expression,'PROPOSED_EXPRESSION'));
  else reasons.push(expression?'Proof expression lacks an accepted EQUIVALENT semantic review.':'No current proof expression exists.');
  reasons.push(...evaluated.reasons);reviewIds.push(...evaluated.derivationOrReviewIds,...list(fv(expression,'ACCEPTED_SEMANTIC_REVIEW_IDS')));supportingEvidenceIds.push(...evaluated.supportingEvidenceIds);
  const contradictionStatus=direct.contradictionStatus==='CONTRADICTED'||evaluated.contradictionStatus==='CONTRADICTED'?'CONTRADICTED':'CLEAR';let truthValue='UNKNOWN';if(contradictionStatus==='CLEAR'){if(direct.truthValue==='FALSE'||evaluated.truthValue==='FALSE')truthValue='FALSE';else if(direct.truthValue==='TRUE'||evaluated.truthValue==='TRUE')truthValue='TRUE';}const status=contradictionStatus==='CONTRADICTED'?'UNDETERMINED':truthValue==='TRUE'?'SATISFIED':truthValue==='FALSE'?'VIOLATED':'UNDETERMINED',bases=[direct.epistemicBasis,evaluated.epistemicBasis].filter(x=>x&&x!=='NONE'),fresh=[direct.freshnessStatus,evaluated.freshnessStatus].filter(Boolean);
  return{truthValue,status,epistemicBasis:bases.length===1?bases[0]:bases.length?'EXTERNALLY_SUPPORTED':'NONE',currentScopeStatus:'CURRENT',freshnessStatus:fresh.length&&fresh.every(x=>['CURRENT','NOT_APPLICABLE'].includes(x))?(fresh.every(x=>x==='NOT_APPLICABLE')?'NOT_APPLICABLE':'CURRENT'):'UNKNOWN',contradictionStatus,reasons:[...new Set(reasons)],supportingEvidenceIds:[...new Set(supportingEvidenceIds)],derivationOrReviewIds:[...new Set(reviewIds)]};
}
function refreshProps(p){for(const proposition of props(p)){const state=pstate(p,proposition);for(const [field,value]of Object.entries({TRUTH_VALUE:state.truthValue,STATUS:state.status,EPISTEMIC_BASIS:state.epistemicBasis,CURRENT_SCOPE_STATUS:state.currentScopeStatus,FRESHNESS_STATUS:state.freshnessStatus,CONTRADICTION_STATUS:state.contradictionStatus,REASONS:state.reasons,SUPPORTING_EVIDENCE_IDS:state.supportingEvidenceIds,DERIVATION_OR_REVIEW_IDS:state.derivationOrReviewIds,CURRENT_SCOPE:scope(p)}))set(proposition,'propositions',field,value);}}
function testAllowedBases(test){const mode=up(fv(test,'EXECUTION_MODE')),type=up(fv(test,'TEST_TYPE'));if(mode==='APPLICATION_DETERMINISTIC')return['APPLICATION_OBSERVED'];if(mode==='HUMAN_INSPECTION')return['SELF_ASSERTED'];if(type==='MEANING'||type==='ADVERSARIAL'||mode==='INDEPENDENT_AGENT_REVIEW')return['VERIFIED_EXTERNAL','EXTERNALLY_SUPPORTED'];if(mode==='EXTERNAL_SYSTEM'||mode==='EXTERNAL_AGENT_TOOL')return['VERIFIED_EXTERNAL','EXTERNALLY_SUPPORTED'];return['APPLICATION_OBSERVED','VERIFIED_EXTERNAL','EXTERNALLY_SUPPORTED'];}
function proofNodeReferences(node,out={tests:new Set(),observations:new Set(),artifacts:new Set(),dependencies:new Set()}){if(!node||typeof node!=='object'||Array.isArray(node))return out;if(up(node.type||node.op)==='LEAF'){if(node.testId)out.tests.add(String(node.testId));if(node.observationId)out.observations.add(String(node.observationId));if(node.artifactId)out.artifacts.add(String(node.artifactId));if(node.dependencyId)out.dependencies.add(String(node.dependencyId));}for(const child of safe(node.children))proofNodeReferences(child,out);return out;}
function effectiveApplicability(p,proposition,normativeClass){
  const propositionId=rid(proposition,'propositions'),explicit=applicability(p,propositionId);if(normativeClass!=='CONDITIONAL')return{applicability:explicit,activationPropositionId:''};
  const activationPropositionId=String(fv(proposition,'ACTIVATION_PROPOSITION_ID')||'').trim();if(!activationPropositionId)return{applicability:'UNKNOWN',activationPropositionId:''};const activation=props(p).find(record=>rid(record,'propositions')===activationPropositionId);if(!activation)return{applicability:'UNKNOWN',activationPropositionId};const state=pstate(p,activation);if(state.truthValue==='TRUE'&&state.contradictionStatus==='CLEAR')return{applicability:'APPLICABLE',activationPropositionId};if(state.truthValue==='FALSE'&&state.contradictionStatus==='CLEAR')return{applicability:'NOT_APPLICABLE',activationPropositionId};return{applicability:'UNKNOWN',activationPropositionId};
}
function obligations(p){
  ensure(p);refreshProofSemantics(p);markTests(p);const currentScope=scope(p),scopeHash=h.sha256Value(currentScope),requirements=new Map(old(p,'requirements').map(record=>[rid(record,'requirements'),record])),spec=[];
  for(const proposition of props(p)){
    const propositionId=rid(proposition,'propositions'),requirementId=String(fv(proposition,'REQUIREMENT_ID')||''),requirement=requirements.get(requirementId),normativeClass=effectiveNormativeClass(p,requirement);
    if(normativeClass==='OPTIONAL')continue;
    const proposedNormativeClass=normative(fv(requirement,'MANDATORY_OPTIONAL_STATUS')),applicabilityClass=normativeClass==='UNKNOWN'&&proposedNormativeClass==='CONDITIONAL'?'CONDITIONAL':normativeClass,applicabilityState=effectiveApplicability(p,proposition,applicabilityClass),applicationState=applicabilityState.applicability,expression=expr(p,propositionId),expressionNode=expression&&(fv(expression,'NORMALIZED_EXPRESSION')||fv(expression,'PROPOSED_EXPRESSION')),validation=expressionNode?validateNode(expressionNode):{valid:false,reason:'Proof expression is missing.'},references=expressionNode?proofNodeReferences(expressionNode):{tests:new Set(),observations:new Set(),artifacts:new Set(),dependencies:new Set()},requiredTests=tests(p,propositionId),dependencies=scoped(p,'environmentDependencies').filter(record=>list(fv(record,'TARGET_PROPOSITION_IDS')).includes(propositionId)),blockers=[];
    if(normativeClass==='UNKNOWN')blockers.push('UNKNOWN_NORMATIVE_CLASS');
    if(normativeClass==='CONDITIONAL'&&!applicabilityState.activationPropositionId)blockers.push('MISSING_ACTIVATION_PROPOSITION');
    if(applicationState==='UNKNOWN')blockers.push('UNKNOWN_APPLICABILITY');
    if(applicationState==='APPLICABLE'&&!expression)blockers.push('MISSING_PROOF_EXPRESSION');
    if(expression&&up(fv(expression,'SEMANTIC_EQUIVALENCE_DISPOSITION'))!=='EQUIVALENT')blockers.push('PROOF_EXPRESSION_NOT_EQUIVALENT');
    if(expression&&!list(fv(expression,'ACCEPTED_SEMANTIC_REVIEW_IDS')).length)blockers.push('MISSING_PROOF_EXPRESSION_SEMANTIC_REVIEW');
    if(expression&&!validation.valid)blockers.push(`INVALID_PROOF_EXPRESSION:${validation.reason}`);
    if(applicationState==='APPLICABLE'&&!requiredTests.length)blockers.push('MISSING_RELEASE_BEARING_TEST');
    for(const test of requiredTests){if(up(fv(test,'SEMANTIC_COVERAGE_DISPOSITION'))!=='EQUIVALENT')blockers.push(`TEST_NOT_EQUIVALENT:${rid(test,'tests')}`);if(!list(fv(test,'SEMANTIC_REVIEW_IDS')).length)blockers.push(`TEST_SEMANTIC_REVIEW_MISSING:${rid(test,'tests')}`);if(!String(fv(test,'TEST_PROPOSITION_TEXT')||'').trim())blockers.push(`TEST_PROPOSITION_MISSING:${rid(test,'tests')}`);}
    for(const dependency of dependencies){const id=rid(dependency,'environmentDependencies'),truthValue=up(fv(dependency,'TRUTH_VALUE')),basis=up(fv(dependency,'EPISTEMIC_BASIS')),freshness=evaluateFreshnessPolicy(dependency).status,contradiction=up(fv(dependency,'CONTRADICTION_STATUS'));if(truthValue!=='TRUE')blockers.push(`DEPENDENCY_NOT_TRUE:${id}`);if(!E.basis.includes(basis)||basis==='NONE')blockers.push(`DEPENDENCY_BASIS_INSUFFICIENT:${id}`);if(!['CURRENT','NOT_APPLICABLE'].includes(freshness))blockers.push(`DEPENDENCY_NOT_FRESH:${id}`);if(contradiction==='CONTRADICTED')blockers.push(`DEPENDENCY_CONTRADICTED:${id}`);}
    const allowedBases=[...new Set(requiredTests.flatMap(testAllowedBases))],state=pstate(p,proposition),requiredTestIds=requiredTests.map(test=>rid(test,'tests')).sort(h.compareUnicodeScalarSequence),requiredDependencyIds=[...new Set([...dependencies.map(record=>rid(record,'environmentDependencies')),...references.dependencies])].sort(h.compareUnicodeScalarSequence),requiredArtifactIds=[...references.artifacts].sort(h.compareUnicodeScalarSequence),requiredObservationIds=[...references.observations].sort(h.compareUnicodeScalarSequence);
    spec.push({PROPOSITION_ID:propositionId,REQUIREMENT_ID:requirementId,NORMATIVE_CLASS:normativeClass,APPLICABILITY:applicationState,ACTIVATION_PROPOSITION_ID:applicabilityState.activationPropositionId,PROOF_EXPRESSION_ID:expression?rid(expression,'proofExpressions'):'',REQUIRED_TEST_IDS:requiredTestIds,REQUIRED_OBSERVATION_LEAF_IDS:requiredObservationIds,REQUIRED_EVIDENCE_CLASSES:['OBSERVATION_RECORD','ACCEPTED_ENTAILMENT','CURRENT_SOURCE_EVIDENCE'],ALLOWED_EPISTEMIC_BASES:allowedBases.length?allowedBases:['APPLICATION_OBSERVED','VERIFIED_EXTERNAL','EXTERNALLY_SUPPORTED'],REQUIRED_ARTIFACT_IDS:requiredArtifactIds,REQUIRED_DEPENDENCY_IDS:requiredDependencyIds,FRESHNESS_REQUIREMENTS:{required:true,allowed:['CURRENT','NOT_APPLICABLE']},INDEPENDENCE_REQUIREMENTS:requiredTests.filter(test=>['INDEPENDENT_AGENT_REVIEW','MEANING','ADVERSARIAL'].includes(up(fv(test,'EXECUTION_MODE')))||['MEANING','ADVERSARIAL'].includes(up(fv(test,'TEST_TYPE')))).map(test=>rid(test,'tests')),REQUIRED_RUN_DIMENSIONS:requiredTests.filter(test=>['REQUIRED_PROOF','REGRESSION'].includes(up(fv(test,'TEST_ROLE')))).map(test=>rid(test,'tests')),SATISFACTION_STATE:state.status,TRUTH_VALUE:state.truthValue,EPISTEMIC_BASIS:state.epistemicBasis,CURRENT_SCOPE_STATUS:state.currentScopeStatus,FRESHNESS_STATUS:state.freshnessStatus,CONTRADICTION_STATUS:state.contradictionStatus,REASONS:state.reasons,SUPPORTING_EVIDENCE_IDS:state.supportingEvidenceIds,DERIVATION_OR_REVIEW_IDS:state.derivationOrReviewIds,BLOCKING_REASONS:[...new Set(blockers)],CURRENT_SCOPE:currentScope,CURRENT_SCOPE_HASH:scopeHash});
  }
  spec.sort((a,b)=>h.compareUnicodeScalarSequence(h.stableStringify(a),h.stableStringify(b)));const dynamicFields=new Set(['SATISFACTION_STATE','TRUTH_VALUE','EPISTEMIC_BASIS','CURRENT_SCOPE_STATUS','FRESHNESS_STATUS','CONTRADICTION_STATUS','REASONS','SUPPORTING_EVIDENCE_IDS','DERIVATION_OR_REVIEW_IDS','BLOCKING_REASONS']),structuralSpec=spec.map(item=>Object.fromEntries(Object.entries(item).filter(([key])=>!dynamicFields.has(key)))),setHash=h.sha256Value(structuralSpec),present=scoped(p,'proofObligations').filter(record=>fv(record,'PROOF_OBLIGATION_SET_HASH')===setHash);
  if(present.length===spec.length&&(spec.length||!props(p).length)){for(const fields of spec){const record=present.find(item=>String(fv(item,'PROPOSITION_ID'))===fields.PROPOSITION_ID);if(record)for(const key of dynamicFields)set(record,'proofObligations',key,fields[key]);}return{setHash,records:present};}
  for(const record of scoped(p,'proofObligations')){record.active=false;record.validity='SUPERSEDED';refresh(record,'proofObligations');}
  return{setHash,records:spec.map(fields=>put(p,'proofObligations',{...fields,PROOF_OBLIGATION_SET_HASH:setHash},{stage:6,source:'APPLICATION_DERIVED',scope:currentScope}))};
}
function currentValidRuns(p){return old(p,'runs').filter(run=>{const state=up(fv(run,'EXECUTION_STATUS')||run.completionState||run.status);return['COMPLETED','SATISFIED','SUCCESS','PASSED','ACCEPTED'].includes(state);});}
function relationSet(p){const tuples=[],seen=new Set(),runs=currentValidRuns(p);for(const obligation of obligations(p).records){if(up(fv(obligation,'APPLICABILITY'))!=='APPLICABLE')continue;const propositionId=String(fv(obligation,'PROPOSITION_ID')),requirementId=String(fv(obligation,'REQUIREMENT_ID')),requiredRunTests=new Set(list(fv(obligation,'REQUIRED_RUN_DIMENSIONS')));for(const testId of list(fv(obligation,'REQUIRED_TEST_IDS'))){if(requiredRunTests.size&&!requiredRunTests.has(testId))continue;const test=old(p,'tests').find(record=>rid(record,'tests')===testId);if(!test||!testReleaseBearing(p,test)||!['REQUIRED_PROOF','REGRESSION'].includes(up(fv(test,'TEST_ROLE'))))continue;for(const run of runs){const tuple={PROPOSITION_ID:propositionId,REQ_ID:requirementId,RUN_ID:rid(run,'runs'),TEST_ID:testId},key=[tuple.PROPOSITION_ID,tuple.REQ_ID,tuple.RUN_ID,tuple.TEST_ID].join('|');if(!seen.has(key)){seen.add(key);tuples.push(tuple);}}}}
  tuples.sort((a,b)=>h.compareUnicodeScalarSequence(h.stableStringify(a),h.stableStringify(b)));return{id:`RELSET-${h.sha256Value(tuples).slice(0,20).toUpperCase()}`,tuples};
}
function dueDeferredFailureTests(p,stage){return old(p,'failureTests').filter(record=>up(fv(record,'AVAILABILITY_CLASS'))==='DEFERRED_TARGET_DEPENDENT'&&Number(fv(record,'DEFERRED_EXECUTION_TRIGGER_STAGE')||Infinity)<=Number(stage)&&!old(p,'regressionExecutions').some(execution=>String(fv(execution,'REG_ID')||fv(execution,'MUTATION_ID')||'')===rid(record,'failureTests')&&['SATISFIED','PASSED','REJECTED'].includes(up(fv(execution,'RESULT')))));}
function reasons(p,stage){
  const out=[];
  if(stage>=1){const challenge=semanticChallengeStatus(p);if(challenge.required&&!challenge.complete)out.push(...challenge.reasons);}
  if(stage>=2){const sourceDisposition=up(p.stages?.[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION||p.stages?.[2]?.derivedData?.SOURCE_APPLICABILITY_DETERMINATION||'');if(sourceDisposition==='NO_APPLICABLE_EXTERNAL_SOURCE'){const search=evaluateSourceSearchContract(p);if(!search.complete)out.push(...search.reasons.map(reason=>`Stage 2 bounded source search: ${reason}`));}}
  if(stage>=4){const challenge=requirementCompilationChallengeStatus(p);if(challenge.required&&!challenge.complete)out.push(...challenge.reasons);for(const requirement of old(p,'requirements')){const requirementId=rid(requirement,'requirements'),matches=props(p).filter(proposition=>String(fv(proposition,'REQUIREMENT_ID'))===requirementId);if(matches.length!==1)out.push(`Requirement ${requirementId} must have exactly one current primary proposition; found ${matches.length}.`);}}
  if(stage>=5)for(const proposition of props(p)){const propositionId=rid(proposition,'propositions'),requirement=old(p,'requirements').find(record=>rid(record,'requirements')===String(fv(proposition,'REQUIREMENT_ID'))),requirementId=rid(requirement,'requirements'),normativeClass=effectiveNormativeClass(p,requirement),effective=effectiveApplicability(p,proposition,normativeClass),atomicity=up(fv(proposition,'ATOMICITY_STATUS')||fv(requirement,'ATOMICITY_STATUS')||'');if(normativeClass==='UNKNOWN')out.push(`Proposition ${propositionId} has UNKNOWN normative classification.`);if(normativeClass==='CONDITIONAL'&&!effective.activationPropositionId)out.push(`Conditional proposition ${propositionId} lacks an activation proposition.`);if(normativeClass!=='OPTIONAL'&&effective.applicability==='UNKNOWN')out.push(`Proposition ${propositionId} has UNKNOWN applicability.`);if(['NON_ATOMIC','COMPOUND','UNKNOWN'].includes(atomicity)&&currentAtomicityReview(p,requirementId)?.reviewOutcome!=='RESOLVED')out.push(`Requirement ${requirementId} has unresolved atomicity status ${atomicity}.`);if(['CONDITIONAL','OPTIONAL'].includes(normative(fv(requirement,'MANDATORY_OPTIONAL_STATUS')))&&!currentNormativeReview(p,requirementId))out.push(`Requirement ${requirementId} reduces a release obligation without an independent current classification review.`);}
  if(stage>=6)for(const obligation of obligations(p).records)for(const blocker of safe(fv(obligation,'BLOCKING_REASONS')))out.push(`Proof obligation ${rid(obligation,'proofObligations')}: ${blocker}.`);
  if(stage>=12){const relation=relationSet(p),verification=old(p,'verification'),counts=new Map();for(const row of verification){const propositionId=String(fv(row,'PROPOSITION_ID')||''),key=[propositionId,String(fv(row,'REQ_ID')||row.relationships?.REQ_ID||''),String(fv(row,'RUN_ID')||row.relationships?.RUN_ID||''),String(fv(row,'TEST_ID')||row.relationships?.TEST_ID||'')].join('|');counts.set(key,(counts.get(key)||0)+1);}if(stage===12)for(const tuple of relation.tuples){const exact=[tuple.PROPOSITION_ID,tuple.REQ_ID,tuple.RUN_ID,tuple.TEST_ID].join('|'),legacy=['',tuple.REQ_ID,tuple.RUN_ID,tuple.TEST_ID].join('|'),count=(counts.get(exact)||0)+(counts.get(legacy)||0);if(count!==1)out.push(`Required verification tuple ${exact} has ${count} current records.`);}}
  if(stage>=14)for(const defect of old(p,'defects')){const status=up(fv(defect,'STATUS')||fv(defect,'DEFECT_STATUS')||'');if(['CONFIRMED','OPEN','ACTIVE','UNRESOLVED'].includes(status)){const routing=routeUnknownSeverity(p,defect);if(routing.severity==='UNKNOWN')out.push(`Defect ${routing.defectId} has UNKNOWN severity and requires material routing review.`);}}
  if(stage>=18){refreshProps(p);for(const proposition of props(p)){const propositionId=rid(proposition,'propositions'),requirement=old(p,'requirements').find(record=>rid(record,'requirements')===String(fv(proposition,'REQUIREMENT_ID'))),normativeClass=effectiveNormativeClass(p,requirement),effective=effectiveApplicability(p,proposition,normativeClass);if(normativeClass!=='OPTIONAL'&&effective.applicability==='APPLICABLE'&&up(fv(proposition,'STATUS'))!=='SATISFIED')out.push(`Mandatory proposition ${propositionId} is not SATISFIED.`);}for(const deferred of dueDeferredFailureTests(p,stage))out.push(`Deferred failure test ${rid(deferred,'failureTests')} is due and has not executed successfully.`);const graph=validateJustificationGraph(p);if(!graph.acyclic)out.push(...graph.reasons);const contradictions=detectCompletionContradictions(p).filter(item=>item.severity==='RELEASE_MATERIAL');for(const contradiction of contradictions)out.push(`Current contradiction ${contradiction.type}: ${contradiction.key}.`);}
  if(stage>=20&&old(p,'baselines').length){const backup=backupCheckpointState(p,'BASELINE_APPROVAL');if(!backup.complete)out.push(...backup.reasons);}
  if(stage>=30){const backup=backupCheckpointState(p,'FINAL_DELIVERY');if(!backup.complete)out.push(...backup.reasons);}
  return[...new Set(out)];
}
function gate(stageOrProject,projectOrStage){const stage=typeof stageOrProject==='number'?Number(stageOrProject):Number(projectOrStage),p=typeof stageOrProject==='number'?projectOrStage:stageOrProject;ensure(p);const base=e0.gate(stage,p),all=[...(base.reasons||[]),...reasons(p,stage)];return{...base,complete:Boolean(base.complete)&&all.length===0,blocked:Boolean(base.blocked)||all.length>0,reasons:[...new Set(all)]};}
function equivalenceClasses(p){const parent=new Map(),find=id=>{if(!parent.has(id))parent.set(id,id);let x=id;while(parent.get(x)!==x)x=parent.get(x);while(parent.get(id)!==id){const next=parent.get(id);parent.set(id,x);id=next;}return x;},join=(a,b)=>{const ra=find(a),rb=find(b);if(ra!==rb)parent.set(rb,ra);};for(const proposition of props(p))find(rid(proposition,'propositions'));for(const review of scoped(p,'propositionEquivalenceReviews')){const disposition=up(fv(review,'DISPOSITION')),accepted=up(fv(review,'ACCEPTED_STATUS')||'ACCEPTED')==='ACCEPTED';if(accepted&&disposition==='EQUIVALENT')join(String(fv(review,'PROPOSITION_A_ID')),String(fv(review,'PROPOSITION_B_ID')));}const groups=new Map();for(const id of parent.keys()){const root=find(id);if(!groups.has(root))groups.set(root,[]);groups.get(root).push(id);}return{find,groups};}
function detectCompletionContradictions(p){
  const output=[...e0.detectCurrentContradictions(p)],seen=new Set(output.map(item=>`${item.type}|${item.key}`)),push=(type,key,details,severity='RELEASE_MATERIAL')=>{const fingerprint=`${type}|${key}`;if(!seen.has(fingerprint)){seen.add(fingerprint);output.push({type,key,details,severity});}};
  const classes=equivalenceClasses(p);for(const ids of classes.groups.values()){let trueIds=[],falseIds=[];for(const id of ids){const proposition=props(p).find(record=>rid(record,'propositions')===id);if(!proposition)continue;const state=pstate(p,proposition);if(state.contradictionStatus==='CONTRADICTED')push('PROPOSITION_EVIDENCE_CONTRADICTION',id,state.reasons);if(state.truthValue==='TRUE')trueIds.push(id);if(state.truthValue==='FALSE')falseIds.push(id);}if(trueIds.length&&falseIds.length)push('EQUIVALENT_PROPOSITION_CONTRADICTION',ids.slice().sort().join('|'),[`TRUE:${trueIds.join(',')}`,`FALSE:${falseIds.join(',')}`]);}
  for(const review of scoped(p,'propositionEquivalenceReviews')){const a=String(fv(review,'PROPOSITION_A_ID')),b=String(fv(review,'PROPOSITION_B_ID')),disposition=up(fv(review,'DISPOSITION'));if(['UNKNOWN','NOT_EQUIVALENT'].includes(disposition)&&a&&b&&classes.find(a)===classes.find(b))push('SEMANTIC_EQUIVALENCE_CLASS_CONFLICT',[a,b].sort().join('|'),[disposition,rid(review,'propositionEquivalenceReviews')]);}
  const bySubject=new Map();for(const record of scoped(p,'applicabilityRecords')){const id=String(fv(record,'SUBJECT_ID'));if(!bySubject.has(id))bySubject.set(id,[]);bySubject.get(id).push(record);}for(const [id,rows]of bySubject){const values=new Set(rows.map(record=>up(fv(record,'SELECTED_APPLICABILITY')||fv(record,'PROPOSED_APPLICABILITY'))));if(rows.length!==1||values.size!==1||rows.some(record=>up(fv(record,'CONTRADICTION_STATUS'))==='CONTRADICTED'))push('APPLICABILITY_CONTRADICTION',id,rows.map(record=>rid(record,'applicabilityRecords')));}
  const currentRelease=old(p,'releaseRecords').at(-1);if(currentRelease&&up(fv(currentRelease,'DETERMINATION'))==='ACCEPTED')for(const proposition of props(p)){const requirement=old(p,'requirements').find(record=>rid(record,'requirements')===String(fv(proposition,'REQUIREMENT_ID'))),normativeClass=effectiveNormativeClass(p,requirement),effective=effectiveApplicability(p,proposition,normativeClass),state=pstate(p,proposition);if(normativeClass!=='OPTIONAL'&&effective.applicability==='APPLICABLE'&&state.status!=='SATISFIED')push('ACCEPTED_ELIGIBILITY_WITH_UNPROVEN_PROPOSITION',rid(proposition,'propositions'),[state.status,state.contradictionStatus]);}
  const authorized=scoped(p,'deliveryRecords').filter(record=>up(fv(record,'DELIVERY_STATE'))==='AUTHORIZED');if(authorized.length>1)push('MULTIPLE_CURRENT_AUTHORIZED_DELIVERIES','DELIVERY_RECORD',authorized.map(record=>rid(record,'deliveryRecords')));return output;
}
function artifactIdentityDetail(p){
  const identities=old(p,'artifactIdentities'),reasons=[],seenArtifacts=new Set(),seenFilenames=new Set(),artifacts=[];
  if(!identities.length)reasons.push('No current audited-versus-delivery identity records exist.');
  for(const identity of identities){const artifactId=String(fv(identity,'ARTIFACT_ID')||''),auditedName=String(fv(identity,'AUDITED_FILENAME')||''),deliveryName=String(fv(identity,'RELEASE_FILENAME')||'');if(!artifactId||seenArtifacts.has(artifactId))reasons.push(`Artifact identity ${artifactId||'UNKNOWN'} is missing or duplicated.`);seenArtifacts.add(artifactId);if(!deliveryName||seenFilenames.has(deliveryName))reasons.push(`Delivery filename ${deliveryName||'UNKNOWN'} is missing or duplicated.`);seenFilenames.add(deliveryName);if(auditedName!==deliveryName)reasons.push(`Unauthorized rename for ${artifactId}.`);if(fv(identity,'EXACT_HASH_MATCH')!==true||fv(identity,'EXACT_SIZE_MATCH')!==true||up(fv(identity,'AUTHORIZATION'))!=='AUTHORIZED')reasons.push(`Artifact identity ${artifactId} is not an exact authorized match.`);if(!['','NONE'].includes(up(fv(identity,'POST_AUDIT_MODIFICATION_EVIDENCE'))))reasons.push(`Post-audit modification evidence exists for ${artifactId}.`);const matches=old(p,'artifacts').filter(record=>rid(record,'artifacts')===artifactId);if(matches.length!==1){reasons.push(`Current canonical artifact ${artifactId} resolves ${matches.length} times.`);continue;}const artifact=matches[0];if(up(fv(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||!/^([a-f0-9]{64})$/i.test(String(fv(artifact,'SHA256')||''))||String(fv(artifact,'SHA256'))!==String(fv(identity,'PRE_DELIVERY_SHA256'))||Number(fv(artifact,'BYTE_SIZE'))!==Number(fv(identity,'RELEASE_BYTE_SIZE')))reasons.push(`Current bytes for ${artifactId} are unavailable, unverified, or no longer match Stage 28.`);artifacts.push(artifact);}
  const authorizedIds=list(p.stages?.[28]?.derivedData?.EXACT_AUTHORIZED_ARTIFACT_IDS||p.release?.authorizedArtifactIds);if(authorizedIds.length&&h.sha256Value(authorizedIds.slice().sort(h.compareUnicodeScalarSequence))!==h.sha256Value([...seenArtifacts].sort(h.compareUnicodeScalarSequence)))reasons.push('Authorized artifact selection does not exactly match the identity-review set.');return{complete:reasons.length===0,reasons:[...new Set(reasons)],identities,artifacts};
}
function artifactOK(p){return artifactIdentityDetail(p).complete;}
function intentDetail(p){
  const decisions=safe(p.projectData?.humanDecisions).filter(record=>Number(record.stage)===28&&['DELIVERY_AUTHORIZATION','HUMAN_DELIVERY_INTENT','DELIVERY_SELECTION_AUTHORIZATION'].includes(up(record.field))&&['AUTHORIZED','AUTHORIZE','TRUE','YES','APPROVED','SELECTED'].includes(up(record.value))),latest=decisions.at(-1);if(latest)return{authorized:true,authorizationId:String(latest.decisionId),identityAssurance:String(latest.identityAssurance||'SELF_ASSERTED'),record:latest};
  const history=safe(p.projectData?.history).filter(record=>record.type==='ARTIFACT_IDENTITY_VERIFIED'&&up(record.authorization)==='AUTHORIZED'),event=history.at(-1);if(event)return{authorized:true,authorizationId:String(event.eventId||`EVENT-${event.eventSequence}`),identityAssurance:'SELF_ASSERTED',record:event};
  return{authorized:false,authorizationId:'',identityAssurance:'SELF_ASSERTED',record:null};
}
function intent(p){return intentDetail(p).authorized;}
function chainDetail(p){
  const required=obligations(p).records.filter(record=>up(fv(record,'APPLICABILITY'))==='APPLICABLE'),chains=old(p,'evidenceChains'),reasons=[];if(!required.length)reasons.push('No current applicable mandatory proof obligations exist.');for(const obligation of required){const propositionId=String(fv(obligation,'PROPOSITION_ID')),requirementId=String(fv(obligation,'REQUIREMENT_ID')),state=props(p).find(record=>rid(record,'propositions')===propositionId);if(!state||pstate(p,state).status!=='SATISFIED')reasons.push(`Proposition ${propositionId} is not currently satisfied.`);const matches=chains.filter(record=>String(fv(record,'PROPOSITION_ID')||'')===propositionId||String(fv(record,'REQ_ID')||record.relationships?.REQ_ID||'')===requirementId);if(matches.length!==1)reasons.push(`Evidence chain for ${propositionId} resolves ${matches.length} times.`);else if(up(fv(matches[0],'STATUS'))!=='COMPLETE'||list(fv(matches[0],'MISSING_LINKS')).length)reasons.push(`Evidence chain ${rid(matches[0],'evidenceChains')} is incomplete.`);}
  const contradictions=detectCompletionContradictions(p).filter(item=>item.severity==='RELEASE_MATERIAL');if(contradictions.length)reasons.push('Release-material contradictions affect current evidence chains.');return{complete:reasons.length===0,reasons:[...new Set(reasons)],chains,setHash:h.sha256Value(chains.map(record=>({id:rid(record,'evidenceChains'),sha256:record.recordSha256||record.sha256||''})).sort((a,b)=>h.compareUnicodeScalarSequence(a.id,b.id)))};
}
function chainOK(p){return chainDetail(p).complete;}
function regressionState(record){return up(fv(record,'RETIREMENT_STATUS')||fv(record,'ACTIVE_RETIRED_STATE')||'ACTIVE');}
function registry(p){
  const defects=e0.records(p,'defects'),regressions=e0.records(p,'regressions'),executions=e0.records(p,'regressionExecutions'),missingDefectIds=[],missingExecutionIds=[],invalidRetirementIds=[],invalidRegressionIds=[];
  for(const defect of defects){const defectId=rid(defect,'defects'),linked=regressions.filter(regression=>String(fv(regression,'DEFECT_ID')||regression.relationships?.DEFECT_ID||'')===defectId);if(!linked.length)missingDefectIds.push(defectId);}
  for(const regression of regressions){const regressionId=rid(regression,'regressions'),state=regressionState(regression),linked=executions.filter(execution=>String(fv(execution,'REG_ID')||execution.relationships?.REG_ID||'')===regressionId),pre=linked.filter(execution=>up(fv(execution,'PHASE'))==='PRE_CORRECTION'),post=linked.filter(execution=>up(fv(execution,'PHASE'))!=='PRE_CORRECTION'&&['SATISFIED','PASSED','SUCCESS'].includes(up(fv(execution,'RESULT'))));if(!pre.length||!post.length||pre.some(a=>post.some(b=>rid(a,'regressionExecutions')===rid(b,'regressionExecutions'))))invalidRegressionIds.push(regressionId);if(state==='RETIRED'){const reason=String(fv(regression,'RETIREMENT_REASON')||fv(regression,'RETIREMENT_AUTHORITY_AND_EVIDENCE')||'').trim(),review=String(fv(regression,'RETIREMENT_REVIEW_ID')||'').trim(),app=up(fv(regression,'APPLICABILITY'));if(!reason||!review||!['NOT_APPLICABLE','NOT APPLICABLE'].includes(app))invalidRetirementIds.push(regressionId);}else if(state==='ACTIVE'){const current=old(p,'regressionExecutions').filter(execution=>String(fv(execution,'REG_ID')||execution.relationships?.REG_ID||'')===regressionId&&up(fv(execution,'PHASE'))!=='PRE_CORRECTION').at(-1);if(!current||!['SATISFIED','PASSED','SUCCESS'].includes(up(fv(current,'RESULT'))))missingExecutionIds.push(regressionId);}}
  const hash=h.sha256Value({defects:defects.map(record=>record.recordSha256||record.sha256||''),regressions:regressions.map(record=>record.recordSha256||record.sha256||''),executions:executions.map(record=>record.recordSha256||record.sha256||'')});return{complete:!missingDefectIds.length&&!missingExecutionIds.length&&!invalidRetirementIds.length&&!invalidRegressionIds.length,missingDefectIds,missingExecutionIds,invalidRetirementIds,invalidRegressionIds,hash};
}
function completionReleaseMetrics(p){const base=e0.releaseMetrics(p),mandatory=obligations(p).records.filter(record=>up(fv(record,'APPLICABILITY'))==='APPLICABLE'),states=mandatory.map(record=>({id:String(fv(record,'PROPOSITION_ID')),state:props(p).find(proposition=>rid(proposition,'propositions')===String(fv(record,'PROPOSITION_ID')))})).map(item=>({...item,result:item.state?pstate(p,item.state):{status:'UNDETERMINED'},direct:item.state?observedTruthDetail(p,item.id):{refutes:[]}})),violated=states.filter(item=>item.result.status==='VIOLATED'),affirmativelyRefuted=states.filter(item=>item.direct.refutes.length>0),undetermined=states.filter(item=>item.result.status!=='SATISFIED'&&item.result.status!=='VIOLATED'),contradictions=detectCompletionContradictions(p),dependencyBlockers=mandatory.flatMap(record=>safe(fv(record,'BLOCKING_REASONS')).filter(reason=>String(reason).startsWith('DEPENDENCY_'))),proofBlockers=mandatory.flatMap(record=>safe(fv(record,'BLOCKING_REASONS')));let determination=base.determination;if(violated.length||affirmativelyRefuted.length)determination='REJECTED';else if(determination==='ACCEPTED'&&(undetermined.length||contradictions.length||dependencyBlockers.length||proofBlockers.length||dueDeferredFailureTests(p,27).length))determination='BLOCKED';return{...base,determination,mandatoryPropositionCount:mandatory.length,satisfiedPropositions:states.filter(item=>item.result.status==='SATISFIED').map(item=>item.id),violatedPropositions:[...new Set([...violated,...affirmativelyRefuted].map(item=>item.id))],undeterminedPropositions:undetermined.map(item=>item.id),proofObligationSetHash:obligations(p).setHash,completionContradictions:contradictions,dependencyBlockers};}
function released(p){const current=old(p,'releaseRecords').filter(record=>up(fv(record,'DETERMINATION'))==='ACCEPTED');if(current.length!==1)return null;const metrics=completionReleaseMetrics(p);return metrics.determination==='ACCEPTED'&&reasons(p,27).length===0?current[0]:null;}
function backupArtifactManifest(p){return e0.records(p,'artifacts').filter(active).map(artifact=>({artifactId:rid(artifact,'artifacts'),filename:String(fv(artifact,'FILENAME')||''),byteSize:Number(fv(artifact,'BYTE_SIZE')),hashAlgorithm:'SHA-256',digest:String(fv(artifact,'SHA256')||'').toLowerCase(),availability:up(fv(artifact,'AVAILABILITY')||'')})).sort((left,right)=>h.compareUnicodeScalarSequence(left.artifactId,right.artifactId));}
function backupCheckpointState(p,checkpointEvent){
  ensure(p);const event=up(checkpointEvent),reasons=[],baseline=old(p,'baselines').at(-1),product=old(p,'products').at(-1),currentArtifactManifest=backupArtifactManifest(p),currentArtifactIds=currentArtifactManifest.map(item=>item.artifactId),baselineArtifactIds=[...new Set(list(fv(baseline,'IMMUTABLE_ARTIFACT_RECORDS')))].sort(h.compareUnicodeScalarSequence),records=safe(p.projectData.backupRecords).filter(record=>active(record)&&up(fv(record,'CHECKPOINT_EVENT'))===event&&up(fv(record,'STATUS'))==='CURRENT'),valid=[],assessments=[];
  if(new Set(currentArtifactIds).size!==currentArtifactIds.length)reasons.push('Active canonical artifact custody contains duplicate artifact identities.');
  if(!['BASELINE_APPROVAL','FINAL_DELIVERY'].includes(event))return{complete:false,event,reasons:['Backup checkpoint event must be BASELINE_APPROVAL or FINAL_DELIVERY.'],record:null,records:[]};
  for(const record of records){const recordReasons=[],ids=[...new Set(list(fv(record,'ARTIFACT_IDS')))].sort(h.compareUnicodeScalarSequence),recordScope=fv(record,'CURRENT_SCOPE')||record.scope||{},recordFields={...(record.fields||{})},storedHash=String(fv(record,'BACKUP_RECORD_HASH')||''),calculatedHash=h.sha256Value(Object.fromEntries(Object.entries(recordFields).filter(([key])=>key!=='BACKUP_RECORD_HASH')));if(String(fv(record,'JOB_ID')||'')!==String(p.job?.JOB_ID||''))recordReasons.push('Backup belongs to another job.');if(fv(record,'ARTIFACT_BYTES_INCLUDED')!==true)recordReasons.push('Backup did not include the exact artifact bytes.');if(up(fv(record,'RESTORE_TEST_STATUS'))!=='PASSED'||!String(fv(record,'RESTORE_TEST_RECEIPT_ID')||'')||!/^([a-f0-9]{64})$/i.test(String(fv(record,'RESTORE_TEST_RECEIPT_SHA256')||'')))recordReasons.push('Backup lacks a successful bound nonactivating restore-test receipt.');for(const field of ['PROJECT_SHA256','CHECKPOINT_SHA256','ARTIFACT_MANIFEST_SHA256','ARTIFACT_SET_SHA256','PACKAGE_SHA256','PACKAGE_BLOB_SHA256'])if(!/^([a-f0-9]{64})$/i.test(String(fv(record,field)||'')))recordReasons.push(`Backup ${field} is not an exact SHA-256 identity.`);if(!storedHash||storedHash!==calculatedHash)recordReasons.push('Backup record hash is invalid.');if(recordScope.baselineId!==scope(p).baselineId)recordReasons.push('Backup baseline scope is stale.');if(event==='FINAL_DELIVERY'&&recordScope.productId!==scope(p).productId)recordReasons.push('Final-delivery backup product scope is stale.');if(currentArtifactManifest.some(item=>item.availability!=='BYTES_PERSISTED_AND_VERIFIED'))recordReasons.push('One or more current canonical artifact byte sets are unavailable or unverified.');if(event==='BASELINE_APPROVAL'){if(!baseline)recordReasons.push('No current frozen baseline exists.');if(baselineArtifactIds.some(id=>!ids.includes(id)))recordReasons.push('Baseline backup omits one or more immutable baseline artifacts.');}else{if(!product)recordReasons.push('No current finished product exists.');if(h.sha256Value(ids)!==h.sha256Value(currentArtifactIds))recordReasons.push('Final-delivery backup does not contain the complete current canonical artifact set.');const recordedRevision=Number(fv(record,'PROJECT_REVISION')),currentRevision=Number(p.revision||0);if(![currentRevision,currentRevision-1].includes(recordedRevision))recordReasons.push('Final-delivery backup is not bound to the immediately current terminal project revision.');}
    const encryption=up(fv(record,'ENCRYPTION_STATUS')),ack=fv(record,'UNENCRYPTED_SENSITIVE_EXPORT_ACKNOWLEDGED')===true,reason=String(fv(record,'HANDLING_DECISION_REASON')||'').trim();if(encryption==='UNENCRYPTED_ACKNOWLEDGED'&&(!ack||!reason||up(fv(record,'HUMAN_IDENTITY_ASSURANCE'))!=='SELF_ASSERTED'||!String(fv(record,'HANDLING_AUTHORIZATION_ID')||'')))recordReasons.push('Unencrypted sensitive backup lacks its explicit bound human handling decision.');if(!['ENCRYPTED','UNENCRYPTED_ACKNOWLEDGED','NOT_REQUIRED'].includes(encryption))recordReasons.push('Backup encryption/handling status is invalid.');assessments.push({backupId:rid(record,'backupRecords'),valid:recordReasons.length===0,reasons:[...recordReasons]});if(!recordReasons.length)valid.push(record);
  }
  if(valid.length!==1)reasons.push(`Exactly one current valid ${event} backup and successful restore test are required; found ${valid.length}.`);return{complete:valid.length===1,event,reasons,record:valid[0]||null,records,assessments,artifactIds:currentArtifactIds,baselineArtifactIds};
}
function finalBackupReadiness(p){
  ensure(p);const stage29=gate(29,p),binding=acceptedStageOperationBinding(p,30,'COMPLETE'),acceptedIds=new Set(safe(p.stages?.[30]?.acceptedDataChangeIds).map(String)),prompt=binding?.prompt,reservationId=String(prompt?.operationBinding?.operationReservationId||prompt?.contextManifest?.operationBinding?.operationReservationId||''),reservations=stageScopedRows(p,'operationReservations',30).filter(record=>rid(record,'operationReservations')===reservationId),reservation=reservations.length===1?reservations[0]:null,targetSlot=String(prompt?.operationBinding?.targetSlot||''),authorized=Boolean(binding&&acceptedIds.has(String(binding.change?.changeId||''))&&reservation&&up(fv(reservation,'STATUS'))==='ACCEPTED'&&up(fv(reservation,'OPERATION'))==='COMPLETE'&&String(fv(reservation,'PROMPT_ID')||'')===String(binding.promptId||'')&&String(fv(reservation,'TARGET_SLOT')||'')===targetSlot),reasons=[];
  if(!stage29.complete)reasons.push('Stage 29 mandatory evidence-chain work is not current and complete.');if(!authorized)reasons.push('Current Stage 30 registry-integrity work lacks one exact accepted response, prompt, nonplaceholder context, state binding, and operation reservation.');return{ready:reasons.length===0,reasons,stage29Gate:stage29,acceptedStage30ChangeIds:authorized?[String(binding.change.changeId)]:[],stage30Binding:authorized?{changeId:String(binding.change.changeId),rawResponseId:binding.rawResponseId,promptId:binding.promptId,contextId:binding.contextId,operationReservationId:reservationId,targetSlot}:null};
}
function recordBackupRestoreVerification(p,options={}){
  ensure(p);const event=up(options.checkpointEvent),result=options.backupResult||options,backup=result.backupRecord||{},checkpoint=result.checkpoint||{},restore=result.restoreTestReceipt||{},expectedRevision=Number(options.expectedRevision==null?p.revision:options.expectedRevision);if(!['BASELINE_APPROVAL','FINAL_DELIVERY'].includes(event))throw new Error('Backup checkpoint event must be BASELINE_APPROVAL or FINAL_DELIVERY.');if(expectedRevision!==Number(p.revision||0))throw new Error('Expected project revision for backup verification is stale.');if(event==='BASELINE_APPROVAL'&&!old(p,'baselines').length)throw new Error('Baseline backup requires the current frozen baseline.');if(event==='FINAL_DELIVERY'){const readiness=finalBackupReadiness(p);if(!readiness.ready)throw new Error(`Final-delivery backup requires current Stage 29 completion and accepted Stage 30 registry-integrity work: ${readiness.reasons.join(' ')}`);}
  const jobId=String(p.job?.JOB_ID||''),projectDigest=String(p.projectSha256||globalThis.closedLoopProjectStore?.projectSha256?.(p)||''),manifest=backupArtifactManifest(p),expectedManifest=manifest.map(({availability,...item})=>item),artifactIds=expectedManifest.map(item=>item.artifactId),checkpointBody=Object.fromEntries(Object.entries(checkpoint).filter(([key])=>key!=='checkpointSha256')),restoreBody=Object.fromEntries(Object.entries(restore).filter(([key])=>key!=='receiptSha256')),checkpointManifest=safe(checkpoint.artifacts).map(item=>({artifactId:String(item.artifactId||''),filename:String(item.filename||''),byteSize:Number(item.byteSize),hashAlgorithm:String(item.hashAlgorithm||''),digest:String(item.digest||'').toLowerCase()})).sort((left,right)=>h.compareUnicodeScalarSequence(left.artifactId,right.artifactId)),checkpointIdentity=checkpointManifest.map(item=>({artifactId:item.artifactId,filename:item.filename,byteSize:item.byteSize,sha256:item.digest})),artifactManifestSha256=h.sha256Value(checkpointManifest),artifactSetSha256=h.sha256Value(checkpointIdentity);
  if(checkpoint.schema!=='closed-loop-integrity-checkpoint/1'||String(checkpoint.jobId||'')!==jobId||Number(checkpoint.projectRevision)!==expectedRevision||String(checkpoint.projectDigest?.digest||'')!==projectDigest||String(checkpoint.checkpointSha256||'')!==h.sha256Value(checkpointBody))throw new Error('Backup integrity checkpoint does not bind the exact current project identity and revision.');if(h.sha256Value(checkpointManifest)!==h.sha256Value(expectedManifest)||String(checkpoint.artifactManifestDigest?.digest||'')!==artifactManifestSha256||manifest.some(item=>item.availability!=='BYTES_PERSISTED_AND_VERIFIED'))throw new Error('Backup integrity checkpoint does not bind every current verified canonical artifact byte identity.');if(String(backup.jobId||'')!==jobId||String(backup.checkpointSha256||'')!==String(checkpoint.checkpointSha256)||Number(backup.artifactCount)!==artifactIds.length||String(backup.status||'')!=='RESTORE_TEST_SUCCEEDED')throw new Error('Verified backup record does not reconcile with the current checkpoint and artifact set.');if(restore.schema!=='closed-loop-restore-test-receipt/1'||String(restore.jobId||'')!==jobId||Number(restore.projectRevision)!==expectedRevision||String(restore.projectSha256||'')!==projectDigest||String(restore.packageSha256||'')!==String(backup.packageSha256||'')||String(restore.artifactSetSha256||'')!==artifactSetSha256||Number(restore.artifactCount)!==artifactIds.length||up(restore.status)!=='SUCCEEDED'||restore.activationPerformed!==false||restore.existingProjectChanged!==false||String(restore.receiptSha256||'')!==h.sha256Value(restoreBody))throw new Error('Backup restore-test receipt is missing, stale, activating, or inconsistent with the exact current package.');for(const hashValue of [backup.packageSha256,restore.packageBlobSha256,checkpoint.checkpointSha256,restore.receiptSha256])if(!/^([a-f0-9]{64})$/i.test(String(hashValue||'')))throw new Error('Backup verification requires exact SHA-256 package, checkpoint, blob, and restore-receipt identities.');
  const activeArtifacts=e0.records(p,'artifacts').filter(active),sensitive=manifest.filter(item=>{const artifact=activeArtifacts.find(record=>rid(record,'artifacts')===item.artifactId);return['CONFIDENTIAL','RESTRICTED','CREDENTIAL_SECRET','UNKNOWN'].includes(up(fv(artifact,'DISCLOSURE_CLASSIFICATION')||artifact?.disclosureClassification||'UNKNOWN'));}),encrypted=backup.encrypted===true,ack=backup.unencryptedSensitiveAcknowledgment||null;if(sensitive.length&&!encrypted&&(!ack||up(ack.identityAssurance)!=='SELF_ASSERTED'||!String(ack.decisionReason||'').trim()||!/^([a-f0-9]{64})$/i.test(String(ack.acknowledgmentSha256||''))))throw new Error('Unencrypted sensitive backup requires its explicit bound human acknowledgment and reason.');const encryptionStatus=encrypted?'ENCRYPTED':sensitive.length?'UNENCRYPTED_ACKNOWLEDGED':'NOT_REQUIRED',handlingAuthorizationId=sensitive.length&&!encrypted?String(ack.acknowledgmentSha256):'NOT_APPLICABLE',commandPayload={version:'closed-loop-backup-verification-command/1',event,jobId,expectedRevision,projectDigest,checkpointSha256:String(checkpoint.checkpointSha256),artifactManifestSha256,artifactSetSha256,artifactIds,packageSha256:String(backup.packageSha256),packageBlobSha256:String(restore.packageBlobSha256),restoreTestId:String(restore.restoreTestId),restoreReceiptSha256:String(restore.receiptSha256),encryptionStatus,handlingAuthorizationId},payloadHash=h.sha256Value(commandPayload),providedKey=String(options.idempotencyKey||''),prior=providedKey?p.projectData.commandReceipts.find(receipt=>receipt.idempotencyKey===providedKey):null;if(prior){if(prior.payloadHash!==payloadHash)throw new Error('Idempotency key was reused with different backup verification content.');return safe(p.projectData.backupRecords).find(record=>rid(record,'backupRecords')===String(prior.result?.backupId||''))||prior.result;}
  const backupId=String(backup.backupId||`BACKUP-${String(backup.packageSha256).slice(0,24).toUpperCase()}`),existing=safe(p.projectData.backupRecords).find(record=>rid(record,'backupRecords')===backupId);if(existing){if(String(fv(existing,'PACKAGE_SHA256'))!==String(backup.packageSha256)||String(fv(existing,'RESTORE_TEST_RECEIPT_SHA256'))!==String(restore.receiptSha256))throw new Error('Backup identity is already bound to different content.');return existing;}for(const record of safe(p.projectData.backupRecords).filter(record=>active(record)&&up(fv(record,'CHECKPOINT_EVENT'))===event)){set(record,'backupRecords','STATUS','SUPERSEDED');record.active=false;record.validity='SUPERSEDED';}
  const commandId=String(options.commandId||`COMMAND-${nonce().toUpperCase()}`),fields={JOB_ID:jobId,CHECKPOINT_EVENT:event,PROJECT_REVISION:expectedRevision,PROJECT_SHA256:projectDigest,CHECKPOINT_SHA256:String(checkpoint.checkpointSha256),ARTIFACT_MANIFEST_SHA256:artifactManifestSha256,ARTIFACT_SET_SHA256:artifactSetSha256,ARTIFACT_IDS:artifactIds,ARTIFACT_BYTES_INCLUDED:true,PACKAGE_SHA256:String(backup.packageSha256),PACKAGE_BLOB_SHA256:String(restore.packageBlobSha256),ENCRYPTION_STATUS:encryptionStatus,HANDLING_AUTHORIZATION_ID:handlingAuthorizationId,RESTORE_TEST_STATUS:'PASSED',RESTORE_TEST_RECEIPT_ID:String(restore.restoreTestId),RESTORE_TEST_RECEIPT_SHA256:String(restore.receiptSha256),RESTORE_TESTED_AT_DEVICE_TIME:String(restore.endedAtDeviceTime||''),CURRENT_SCOPE:scope(p),STATUS:'CURRENT',APPLICATION_RECEIPT:commandId,UNENCRYPTED_SENSITIVE_EXPORT_ACKNOWLEDGED:Boolean(sensitive.length&&!encrypted),HANDLING_DECISION_REASON:sensitive.length&&!encrypted?String(ack.decisionReason).trim():'NOT APPLICABLE',HUMAN_IDENTITY_ASSURANCE:sensitive.length&&!encrypted?'SELF_ASSERTED':'NOT_APPLICABLE',BACKUP_RECORD_HASH:''},record=put(p,'backupRecords',fields,{id:backupId,stage:event==='BASELINE_APPROVAL'?20:30,source:'APPLICATION_VERIFIED_BACKUP',scope:scope(p)});set(record,'backupRecords','BACKUP_RECORD_HASH',h.sha256Value(Object.fromEntries(Object.entries(record.fields).filter(([key])=>key!=='BACKUP_RECORD_HASH'))));const idempotencyKey=providedKey||h.sha256Value({commandType:'RECORD_BACKUP_RESTORE_VERIFICATION',target:event,expectedRevision,currentScope:scope(p),payloadHash});p.projectData.commandReceipts.push({commandId,idempotencyKey,expectedRevision,committedRevision:expectedRevision+1,payloadHash,result:{backupId,checkpointEvent:event,packageSha256:String(backup.packageSha256),restoreTestId:String(restore.restoreTestId)},createdAt:new Date().toISOString()});recalc(p);return record;
}
function terminal(p){
  const registryState=registry(p),proof=obligations(p),artifact=artifactIdentityDetail(p),humanIntent=intentDetail(p),chains=chainDetail(p),baselineBackup=backupCheckpointState(p,'BASELINE_APPROVAL'),finalBackup=backupCheckpointState(p,'FINAL_DELIVERY'),reasons=[];if(!released(p))reasons.push('Stage 27 product release eligibility is not current ACCEPTED.');reasons.push(...artifact.reasons);if(!humanIntent.authorized)reasons.push('Current human delivery-selection authorization is missing.');reasons.push(...chains.reasons);if(!registryState.complete)reasons.push('Stage 30 registry integrity is incomplete.');if(!baselineBackup.complete)reasons.push(...baselineBackup.reasons);if(!finalBackup.complete)reasons.push(...finalBackup.reasons);if(scoped(p,'operationReservations').some(record=>['ACTIVE','ORPHANED'].includes(up(fv(record,'STATUS')))))reasons.push('A current external operation reservation is still active or orphaned.');if(e0.records(p,'blockers').some(record=>active(record)&&up(fv(record,'STATUS'))!=='RESOLVED'))reasons.push('A current blocker remains open.');return{complete:reasons.length===0,reasons:[...new Set(reasons)],registryHash:registryState.hash,proofObligationSetHash:proof.setHash,evidenceChainSetHash:chains.setHash,artifacts:artifact.artifacts,artifactIdentityRecords:artifact.identities,humanIntent,registry:registryState,baselineBackup,finalBackup};
}
function activeDeliveryRecords(p){return safe(p.projectData?.deliveryRecords).filter(record=>active(record)).sort((left,right)=>Number(fv(left,'EVENT_SEQUENCE')||0)-Number(fv(right,'EVENT_SEQUENCE')||0));}
function withdrawDeliveryAuthorization(p,{reason='A release-bearing terminal prerequisite is no longer current.',changeId='TERMINAL-PREREQUISITE-LOSS',state='WITHDRAWN_FOR_FUTURE_USE',sourceRecords=null}={}){
  ensure(p);const sources=(sourceRecords||activeDeliveryRecords(p).filter(record=>up(fv(record,'DELIVERY_STATE'))==='AUTHORIZED')).filter(Boolean);if(!sources.length)return null;const source=sources.at(-1),sourceId=rid(source,'deliveryRecords'),normalizedState=up(state);if(!['WITHDRAWN_FOR_FUTURE_USE','SUPERSEDED','BLOCKED'].includes(normalizedState))throw new Error('Delivery lifecycle state must be WITHDRAWN_FOR_FUTURE_USE, SUPERSEDED, or BLOCKED.');const trigger=String(changeId||'TERMINAL-PREREQUISITE-LOSS'),controllingReason=String(reason||'A release-bearing terminal prerequisite is no longer current.').trim(),existing=activeDeliveryRecords(p).find(record=>String(fv(record,'PREVIOUS_DELIVERY_ID')||'')===sourceId&&String(fv(record,'LIFECYCLE_CHANGE_ID')||'')===trigger&&up(fv(record,'DELIVERY_STATE'))===normalizedState);if(existing)return existing;
  const expectedRevision=Number(p.revision||0),payload={version:'closed-loop-delivery-lifecycle-command/1',sourceDeliveryId:sourceId,state:normalizedState,changeId:trigger,reason:controllingReason,expectedRevision,currentScope:scope(p)},commandId=`COMMAND-${nonce().toUpperCase()}`,idempotencyKey=h.sha256Value({commandType:'RECORD_DELIVERY_LIFECYCLE_CHANGE',target:sourceId,expectedRevision,currentScope:scope(p),payloadHash:h.sha256Value(payload)});
  return idem(p,{commandType:'RECORD_DELIVERY_LIFECYCLE_CHANGE',target:sourceId,expectedRevision,committedRevision:expectedRevision+1,idempotencyKey,commandId,payload,execute:()=>{for(const record of activeDeliveryRecords(p)){record.active=false;record.validity='SUPERSEDED';record.invalidatedBy=trigger;record.updatedAt=new Date().toISOString();refresh(record,'deliveryRecords');}const eventSequence=Number(p.projectData.eventSequence||0)+1;p.projectData.eventSequence=eventSequence;const copied={...(source.fields||{})};delete copied.DELIVERY_ID;copied.EXPECTED_PRECONDITION_REVISION=expectedRevision;copied.COMMITTED_PROJECT_REVISION=expectedRevision+1;copied.DELIVERY_STATE=normalizedState;copied.CONTROLLING_REASON=controllingReason;copied.APPLICATION_RECEIPT=commandId;copied.EVENT_SEQUENCE=eventSequence;copied.PREVIOUS_DELIVERY_ID=sourceId;copied.LIFECYCLE_CHANGE_ID=trigger;copied.DELIVERY_RECORD_HASH='';const record=put(p,'deliveryRecords',copied,{stage:30,source:'APPLICATION_DERIVED',scope:scope(p)});record.lifecycleFingerprint=h.sha256Value(payload);set(record,'deliveryRecords','DELIVERY_RECORD_HASH',h.sha256Value(Object.fromEntries(Object.entries(record.fields).filter(([key])=>key!=='DELIVERY_RECORD_HASH'))));return record;}});
}
function delivery(p,options={}){
  ensure(p);const terminalState=terminal(p);if(!terminalState.complete)return null;const release=released(p),ids=terminalState.artifacts.map(artifact=>rid(artifact,'artifacts')).sort(h.compareUnicodeScalarSequence),names=terminalState.artifactIdentityRecords.slice().sort((a,b)=>h.compareUnicodeScalarSequence(String(fv(a,'ARTIFACT_ID')),String(fv(b,'ARTIFACT_ID')))).map(identity=>String(fv(identity,'RELEASE_FILENAME'))),digests=terminalState.artifacts.map(artifact=>({artifactId:rid(artifact,'artifacts'),hashAlgorithm:'SHA-256',digest:String(fv(artifact,'SHA256')),byteLength:Number(fv(artifact,'BYTE_SIZE'))})).sort((a,b)=>h.compareUnicodeScalarSequence(a.artifactId,b.artifactId)),expectedRevision=Number(options.expectedRevision==null?p.revision:options.expectedRevision);if(expectedRevision!==Number(p.revision||0))throw new Error('Expected project revision for final delivery is stale.');
  const terminalHash=h.sha256Value({releaseId:rid(release,'releaseRecords'),registryHash:terminalState.registryHash,proofObligationSetHash:terminalState.proofObligationSetHash,evidenceChainSetHash:terminalState.evidenceChainSetHash,digests,humanAuthorizationId:terminalState.humanIntent.authorizationId}),finger=h.sha256Value({terminalHash,ids,names}),commandPayload={version:'closed-loop-delivery-command/1',terminalFingerprint:finger,terminalEvidenceHash:terminalHash,releaseId:rid(release,'releaseRecords'),authorizedArtifactIds:ids,authorizedFilenames:names,digests,humanAuthorizationId:terminalState.humanIntent.authorizationId,registryHash:terminalState.registryHash,proofObligationSetHash:terminalState.proofObligationSetHash,evidenceChainSetHash:terminalState.evidenceChainSetHash},payloadHash=h.sha256Value(commandPayload),providedKey=String(options.idempotencyKey||''),prior=providedKey?p.projectData.commandReceipts.find(receipt=>receipt.idempotencyKey===providedKey):null;
  if(prior){if(prior.payloadHash!==payloadHash)throw new Error('Idempotency key was reused with different delivery content.');const deliveryId=String(prior.result?.deliveryId||prior.result?.id||prior.result?.fields?.DELIVERY_ID||'');return safe(p.projectData.deliveryRecords).find(record=>rid(record,'deliveryRecords')===deliveryId)||prior.result;}
  const existing=scoped(p,'deliveryRecords').find(record=>record.terminalFingerprint===finger&&up(fv(record,'DELIVERY_STATE'))==='AUTHORIZED');if(existing)return existing;
  const commandId=String(options.commandId||`COMMAND-${nonce().toUpperCase()}`),idempotencyKey=providedKey||h.sha256Value({commandType:'CREATE_DELIVERY_RECORD',target:finger,expectedRevision,currentScope:scope(p),payloadHash});
  return idem(p,{commandType:'CREATE_DELIVERY_RECORD',target:finger,expectedRevision,committedRevision:expectedRevision+1,idempotencyKey,commandId,payload:commandPayload,execute:()=>{
    for(const record of activeDeliveryRecords(p)){record.active=false;record.validity='SUPERSEDED';record.invalidatedBy=`DELIVERY-AUTHORIZED-${finger}`;record.updatedAt=new Date().toISOString();refresh(record,'deliveryRecords');}
    const eventSequence=Number(p.projectData.eventSequence||0)+1;p.projectData.eventSequence=eventSequence;
    const record=put(p,'deliveryRecords',{JOB_ID:String(p.job?.JOB_ID||''),PRODUCT_ID:String(p.job?.CURRENT_PRODUCT_ID||fv(release,'PRODUCT_ID')||''),PRODUCT_VERSION:String(p.stages?.[21]?.derivedData?.PRODUCT_VERSION||''),BASELINE_ID:String(p.job?.CURRENT_BASELINE_ID||fv(release,'BASELINE_ID')||''),RELEASE_ID:rid(release,'releaseRecords'),HASH_REVIEW_ID:terminalState.artifactIdentityRecords.map(identity=>rid(identity,'artifactIdentities')).sort(h.compareUnicodeScalarSequence).join(','),EVIDENCE_CHAIN_VERSION:String(p.stages?.[29]?.derivedData?.EVIDENCE_CHAIN_VERSION||terminalState.evidenceChainSetHash.slice(0,20)),EVIDENCE_CHAIN_SET_SHA256:terminalState.evidenceChainSetHash,DEFECT_REGISTRY_VERSION:String(p.stages?.[30]?.derivedData?.DEFECT_REGISTRY_VERSION||''),REGRESSION_REGISTRY_VERSION:String(p.stages?.[30]?.derivedData?.REGRESSION_REGISTRY_VERSION||''),REGISTRY_INTEGRITY_HASH:terminalState.registryHash,HUMAN_DELIVERY_AUTHORIZATION_ID:terminalState.humanIntent.authorizationId,HUMAN_IDENTITY_ASSURANCE:terminalState.humanIntent.identityAssurance,AUTHORIZED_ARTIFACT_IDS:ids,AUTHORIZED_FILENAMES:names,BYTE_SIZES:digests.map(item=>String(item.byteLength)),HASH_ALGORITHMS_AND_DIGESTS:digests,EXPECTED_PRECONDITION_REVISION:expectedRevision,COMMITTED_PROJECT_REVISION:expectedRevision+1,TERMINAL_PROOF_OBLIGATION_SET_HASH:terminalState.proofObligationSetHash,TERMINAL_EVIDENCE_HASH:terminalHash,DELIVERY_STATE:'AUTHORIZED',CONTROLLING_REASON:'Stages 27-30, current human intent, exact bytes, evidence chains, and registry integrity are simultaneously satisfied.',APPLICATION_RECEIPT:commandId,EVENT_SEQUENCE:eventSequence,DELIVERY_RECORD_HASH:''},{stage:30,source:'APPLICATION_DERIVED'});record.terminalFingerprint=finger;set(record,'deliveryRecords','DELIVERY_RECORD_HASH',h.sha256Value(Object.fromEntries(Object.entries(record.fields).filter(([key])=>key!=='DELIVERY_RECORD_HASH'))));return record;
  }});
}
function recordReleaseDetermination(p){ensure(p);refreshApplicability(p);refreshProofSemantics(p);markTests(p);refreshProps(p);const metrics=completionReleaseMetrics(p),releaseEvidenceSha256=h.sha256Value({proofObligationSetHash:metrics.proofObligationSetHash,metrics:{...metrics,completionContradictions:metrics.completionContradictions.map(item=>({type:item.type,key:item.key,severity:item.severity}))},inputReferences:metrics.inputReferences}),existing=old(p,'releaseRecords').find(record=>record.completionReleaseEvidenceSha256===releaseEvidenceSha256);if(existing)return existing;for(const prior of old(p,'releaseRecords')){prior.active=false;prior.validity='SUPERSEDED';prior.invalidatedBy='RELEASE-EVIDENCE-CHANGED';refresh(prior,'releaseRecords');}const record=put(p,'releaseRecords',{PRODUCT_ID:metrics.productId||'UNKNOWN',BASELINE_ID:metrics.baselineId||'UNKNOWN',DETERMINATION:metrics.determination,MANDATORY_REQUIREMENT_COUNTS:metrics.mandatoryRequirementCount,AFFIRMATIVE_EVIDENCE_COUNTS:metrics.satisfied,VIOLATED_COUNTS:metrics.violated,UNDETERMINED_COUNTS:metrics.undetermined,VALIDATOR_COUNTS:metrics.validatorCount,FAILED_VALIDATORS:metrics.failedValidatorIds,UNKNOWN_VALIDATORS:metrics.unknownValidatorIds,CRITICAL_DEFECTS:metrics.criticalDefects,MAJOR_DEFECTS:metrics.majorDefects,BLOCKING_REQUIREMENTS:[...new Set([...(metrics.blockingRequirements||[]),...(metrics.undeterminedPropositions||[])])],VIOLATIONS:[...new Set([...(metrics.violatedRequirements||[]),...(metrics.violatedPropositions||[])])],BLOCKERS:metrics.blockerIds,CONTROLLING_DECISION_RULE:metrics.determination==='REJECTED'?'AFFIRMATIVE_CURRENT_NONCONFORMANCE_REJECTS':metrics.determination==='BLOCKED'?'UNKNOWN_STALE_CONTRADICTORY_OR_INSUFFICIENT_BLOCKS':'CURRENT_PROOF_OBLIGATIONS_SATISFIED',CONTROLLING_EVIDENCE:releaseEvidenceSha256},{stage:27,source:'APPLICATION_DERIVATION'});record.completionReleaseEvidenceSha256=releaseEvidenceSha256;record.releaseEvidenceSha256=releaseEvidenceSha256;p.release.gateState=metrics.determination;p.release.authorization='NOT AUTHORIZED';p.release.authorizedArtifactIds=[];refresh(record,'releaseRecords');recalc(p);return record;}
function recalc(p){
  ensure(p);e0.recalculate(p);ensure(p);refreshSourceSearchAdequacyReviews(p);refreshRequirementCompilationChallengeReviews(p);refreshApplicability(p);refreshProofSemantics(p);markTests(p);if(old(p,'requirements').length&&props(p).length)obligations(p);refreshProps(p);
  for(const stage of[27,28,29,30])p.stages[stage].derivedData=p.stages[stage].derivedData&&typeof p.stages[stage].derivedData==='object'?p.stages[stage].derivedData:{};
  const release=released(p),artifact=artifactIdentityDetail(p),chains=chainDetail(p),registryState=registry(p);p.stages[27].derivedData.PRODUCT_RELEASE_ELIGIBILITY=release?'PRODUCT_RELEASE_ELIGIBLE':'BLOCKED';p.stages[28].derivedData.DELIVERY_ARTIFACT_IDENTITY_VERIFIED=artifact.complete;p.stages[28].derivedData.DELIVERY_AUTHORIZATION_EFFECTIVE=false;p.stages[29].derivedData.ALL_MANDATORY_EVIDENCE_CHAINS_COMPLETE=chains.complete;p.stages[29].derivedData.EVIDENCE_CHAIN_SET_SHA256=chains.setHash;p.stages[29].derivedData.DELIVERY_AUTHORIZATION_EFFECTIVE=false;p.stages[30].derivedData.REGISTRY_INTEGRITY_STATUS=registryState.complete?'COMPLETE':'BLOCKED';p.stages[30].derivedData.REGISTRY_INTEGRITY_HASH=registryState.hash;p.stages[30].derivedData.DELIVERY_STATE='BLOCKED';p.stages[30].derivedData.DELIVERY_AUTHORIZATION_EFFECTIVE=false;p.release.authorization='NOT AUTHORIZED';p.release.authorizedArtifactIds=[];
  for(let stage=1;stage<=30;stage++){
    const state=p.stages[stage],invalidationId=String(state.invalidatedBy||'');
    if(invalidationId){
      const replacement=safe(p.projectData.acceptedChanges).some(change=>Number(change.stage)===stage&&change.status==='COMMITTED'&&!change.invalidatedBy);
      if(!replacement){const message=`Invalidated by ${invalidationId}; repeat the current Stage ${String(stage).padStart(2,'0')} operation against current scope.`;state.status='NOT STARTED';state.gate={stage,complete:false,blocked:false,reasons:[message]};continue;}
      state.invalidatedBy=null;
    }
    const amended=gate(stage,p);state.gate=amended;if(amended.complete)state.status='COMPLETE';else if(amended.blocked||state.status==='COMPLETE')state.status='BLOCKED';
  }
  const terminalState=terminal(p);if(e0.gate(30,p).complete&&terminalState.complete){const record=delivery(p);if(record){p.stages[30].derivedData.DELIVERY_ID=rid(record,'deliveryRecords');p.stages[30].derivedData.DELIVERY_RECORD_HASH=fv(record,'DELIVERY_RECORD_HASH');p.stages[30].derivedData.DELIVERY_STATE='AUTHORIZED';p.stages[30].derivedData.DELIVERY_AUTHORIZATION_EFFECTIVE=true;p.release.authorization='AUTHORIZED';p.release.authorizedArtifactIds=list(fv(record,'AUTHORIZED_ARTIFACT_IDS'));}}else{const authorized=activeDeliveryRecords(p).filter(record=>up(fv(record,'DELIVERY_STATE'))==='AUTHORIZED');if(authorized.length)withdrawDeliveryAuthorization(p,{sourceRecords:authorized,changeId:`TERMINAL-PREREQUISITES-${h.sha256Value(terminalState.reasons)}`,reason:`Final delivery authorization was withdrawn for future use because current terminal prerequisites are no longer simultaneously satisfied: ${terminalState.reasons.join(' ')}`});}
  return p;
}
function supersedeReservations(p,reason){for(const reservation of scoped(p,'operationReservations'))if(['ACTIVE','ORPHANED'].includes(up(fv(reservation,'STATUS')))){set(reservation,'operationReservations','STATUS','SUPERSEDED');reservation.active=false;reservation.validity='SUPERSEDED';reservation.invalidatedBy=reason;const promptId=String(fv(reservation,'PROMPT_ID')||'');for(const prompt of safe(p.projectData.generatedPrompts))if(promptId&&String(prompt.instructionId||prompt.promptId)===promptId&&!prompt.invalidatedBy)prompt.invalidatedBy=reason;}}
function invalidate(p,stageOrFamily,changeId,reason='Material upstream change',details=null){const stage=typeof stageOrFamily==='string'?responsibleStageForMutation(stageOrFamily,details||{}):Number(stageOrFamily),trigger=String(changeId||'UPSTREAM-CHANGE'),priorCurrent=activeDeliveryRecords(p),authorized=priorCurrent.filter(record=>up(fv(record,'DELIVERY_STATE'))==='AUTHORIZED'),lifecycleMarker=priorCurrent.filter(record=>up(fv(record,'DELIVERY_STATE'))!=='AUTHORIZED').at(-1)||null;supersedeReservations(p,trigger);p.release.authorization='NOT AUTHORIZED';p.release.authorizedArtifactIds=[];const result=e0.invalidateDownstream(p,stage,trigger,String(reason||'Material upstream change'));ensure(p);if(authorized.length)withdrawDeliveryAuthorization(p,{sourceRecords:authorized,changeId:trigger,reason:`Final delivery authorization was withdrawn for future use after ${trigger}: ${String(reason||'Material upstream change')}.`});else if(lifecycleMarker){lifecycleMarker.active=true;lifecycleMarker.validity='CURRENT';lifecycleMarker.invalidatedBy=null;lifecycleMarker.updatedAt=new Date().toISOString();refresh(lifecycleMarker,'deliveryRecords');}recalc(p);return result;}
function next(p){const base=e0.operationalNextAction(p),stage=Number(p.activeStage||p.job?.CURRENT_STAGE||0);if(stage===1){const challenge=semanticChallengeStatus(p);if(challenge.required&&!challenge.challenge?.current){const target=challenge.projection,targetSlot=`SEMANTIC_CHALLENGE:${target.targetHash}`;return{...base,actionType:'AI_REVIEW',heading:'Run the independent intake omission challenge',explanation:'A distinct fresh-context reviewer must independently challenge the exact current raw-input coverage before intake can be reconciled. The application binds the raw units, hashes, author provenance, reservation, and package.',primaryButton:'Register a fresh challenge context',blockingReason:challenge.reasons.join(' '),newPromptRequired:true,reviewerContextRequired:true,operation:'SEMANTIC_CHALLENGE',targetSlot,reservationRequired:true,packageRequired:true,intakeSemanticChallengeTarget:target};}if(challenge.required&&!challenge.reconciliation?.current){const targetSlot=`RECONCILE_INTAKE:${challenge.challengeSetHash}`;return{...base,actionType:'AI_REVIEW',actor:'Intake reconciliation agent',heading:'Reconcile the independent intake findings',explanation:'Reconcile the author intake and independent challenge against every current raw unit. Preserve every unresolved material omission as blocked.',primaryButton:'Prepare reconciliation package',blockingReason:challenge.reasons.join(' '),newPromptRequired:true,reviewerContextRequired:false,contextRequired:true,operation:'RECONCILE_INTAKE',targetSlot,reservationRequired:true,packageRequired:true,intakeReconciliationTarget:{targetHash:challenge.projection.targetHash,challengeSetHash:challenge.challengeSetHash,challengeRawResponseId:challenge.challenge?.reviewerRawResponseId||''}};}}
  if(stage===2){const disposition=up(p.stages?.[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION||p.stages?.[2]?.derivedData?.SOURCE_APPLICABILITY_DETERMINATION||'');if(disposition==='NO_APPLICABLE_EXTERNAL_SOURCE'&&!acceptedSourceSearchAdequacyReview(p)){const target=sourceSearchContractProjection(p),targetSlot=`SEARCH_ADEQUACY_REVIEW:${target.sourceSearchContractId}`;return{...base,actionType:'AI_REVIEW',heading:'Review the bounded source search independently',explanation:'A distinct reviewer must assess the exact current search universe, procedure, evidence, source dispositions, limitations, and residual discovery risk. This review cannot prove universal absence.',primaryButton:'Register a fresh search reviewer context',blockingReason:evaluateSourceSearchContract(p).reasons.join(' '),newPromptRequired:true,reviewerContextRequired:true,operation:'SEARCH_ADEQUACY_REVIEW',targetSlot,reservationRequired:true,packageRequired:true,sourceSearchAdequacyTarget:target};}}
  if(stage===4){const challenge=requirementCompilationChallengeStatus(p);if(challenge.pendingBatches.length){const batch=challenge.pendingBatches[0],operation=`${batch.challengeKind}_CHALLENGE`,targetSlot=`REQUIREMENT_CHALLENGE_BATCH:${batch.challengeKind}:${batch.batchHash}`;return{...base,actionType:'AI_REVIEW',heading:batch.challengeKind==='DISPOSITION'?'Challenge the requirement dispositions as one batch':'Challenge requirement atomicity as one batch',explanation:`A distinct fresh-context reviewer receives the exact application-selected ${batch.challengeKind.toLowerCase()} batch once and returns one independently evidenced determination for each of its ${batch.targetCount} item(s). The compiler cannot self-approve the batch.`,primaryButton:'Register one fresh batch reviewer context',blockingReason:challenge.reasons.join(' '),newPromptRequired:true,reviewerContextRequired:true,operation,targetSlot,reservationRequired:true,packageRequired:true,requirementCompilationChallengeBatch:batch};}if(challenge.reconciliationRequired&&!challenge.reconciliation?.current){const targetSlot=`RECONCILE_REQUIREMENTS:${challenge.challengeSetHash}`;return{...base,actionType:'AI_REVIEW',actor:'Requirement reconciliation agent',heading:'Reconcile the requirement challenges',explanation:'Correct and republish the current requirement and proposition set against the complete bound challenge set. No challenged obligation or semantic distinction may disappear.',primaryButton:'Prepare reconciliation package',blockingReason:challenge.reasons.join(' '),newPromptRequired:true,reviewerContextRequired:false,contextRequired:true,operation:'RECONCILE_REQUIREMENTS',targetSlot,reservationRequired:true,packageRequired:true,requirementCompilationReconciliationTarget:{challengeSetHash:challenge.challengeSetHash,reviewIds:[...challenge.accepted,...challenge.correctionRequired,...challenge.unknown].map(item=>item.reviewId)}};}}
  if(stage===6){const pending=semanticReviewTargets(p);if(pending.length){const target=pending[0],targetSlot=`SEMANTIC_REVIEW:${target.targetKind}:${target.targetId}`;return{...base,actionType:'AI_REVIEW',heading:'Run the next independent semantic coverage review',explanation:`${pending.length} current test or proof-expression target${pending.length===1?' requires':'s require'} a distinct fresh-context semantic review before release-bearing use. The application allocates the target identity, hash, reservation, package, and prompt; the reviewer must return EQUIVALENT, PARTIAL, UNKNOWN, or NOT_EQUIVALENT with evidence.`,primaryButton:'Register a fresh reviewer context',blockingReason:'Author self-review, missing or reused reviewer context, missing reservation/package, stale target, partial, unknown, or conflicting review cannot satisfy this gate.',newPromptRequired:true,reviewerContextRequired:true,operation:'SEMANTIC_REVIEW',targetSlot,reservationRequired:true,packageRequired:true,semanticReviewTarget:target};}}
  if(stage===20&&old(p,'baselines').length){const backup=backupCheckpointState(p,'BASELINE_APPROVAL');if(!backup.complete)return{...base,actionType:'CREATE_BACKUP_AND_RESTORE_TEST',actor:'Application and human operator',heading:'Create and restore-test the baseline backup',explanation:'The application exports the complete current project with every verified artifact byte, creates the checkpoint and hashes, then validates the package through a nonactivating restore test. Sensitive unencrypted handling requires one explicit human decision.',primaryButton:'Create, restore-test, and download backup',blockingReason:backup.reasons.join(' '),checkpointEvent:'BASELINE_APPROVAL',applicationNative:true,reservationRequired:false,packageRequired:false};}
  if(stage===30){const finalBackup=backupCheckpointState(p,'FINAL_DELIVERY'),baselineBackup=backupCheckpointState(p,'BASELINE_APPROVAL'),registryState=registry(p),artifact=artifactIdentityDetail(p),chains=chainDetail(p),humanIntent=intentDetail(p),otherTerminalReady=Boolean(released(p))&&baselineBackup.complete&&registryState.complete&&artifact.complete&&chains.complete&&humanIntent.authorized&&!scoped(p,'operationReservations').some(record=>['ACTIVE','ORPHANED'].includes(up(fv(record,'STATUS'))))&&!e0.records(p,'blockers').some(record=>active(record)&&up(fv(record,'STATUS'))!=='RESOLVED');if(otherTerminalReady&&!finalBackup.complete)return{...base,actionType:'CREATE_BACKUP_AND_RESTORE_TEST',actor:'Application and human operator',heading:'Create and restore-test the final-delivery backup',explanation:'Before final delivery authorization, the application exports the complete current terminal project and every verified artifact byte, creates exact package/checkpoint identities, and proves a nonactivating restore. This prerequisite does not itself authorize delivery.',primaryButton:'Create, restore-test, and download final backup',blockingReason:finalBackup.reasons.join(' '),checkpointEvent:'FINAL_DELIVERY',applicationNative:true,reservationRequired:false,packageRequired:false};}
  if(stage===27)return externalReservationAction(p,stage,{...base,label:released(p)?'Product is eligible for final delivery checks':'Resolve release-eligibility blockers'});if(stage===28)return externalReservationAction(p,stage,{...base,label:artifactOK(p)?'Artifact identity verified — delivery is not yet authorized':'Verify exact delivery artifact identity'});if(stage===29)return externalReservationAction(p,stage,{...base,label:chainOK(p)?'Evidence chains complete — delivery is not yet authorized':'Close mandatory evidence chains'});if(stage===30)return externalReservationAction(p,stage,{...base,label:scoped(p,'deliveryRecords').some(record=>up(fv(record,'DELIVERY_STATE'))==='AUTHORIZED')?'Final delivery is authorized':`Close final delivery blockers: ${terminal(p).reasons.join(' ')}`});return externalReservationAction(p,stage,base);}
function nonce(){const bytes=new Uint8Array(16);crypto.getRandomValues(bytes);return Array.from(bytes,value=>value.toString(16).padStart(2,'0')).join('');}
function previewId(p,collection){const counterKey=`completion:${collection}`,taken=new Set(safe(p.projectData[collection]).map(record=>rid(record,collection))),prefix=RS[collection].prefix;let counter=Number(p.projectData.idCounters[counterKey]||0),id;do{id=`${prefix}-${String(++counter).padStart(6,'0')}`;}while(taken.has(id));return{id,counter,counterKey};}
function prepareReservation(p,options={}){ensure(p);const stage=Number(options.stage==null?p.activeStage:options.stage),operation=String(options.operation||'COMPLETE'),targetSlot=String(options.targetSlot||'DEFAULT'),expectedRevision=Number(p.revision||0)+1,currentScope={...normalizedReservationScope(p,options.scope||{}),projectRevision:expectedRevision},payload=e0.clone(options.payload||{}),payloadHash=h.sha256Value(payload),allocation=previewId(p,'operationReservations'),challengeNonce=nonce(),idempotencyKey=h.sha256Value({commandType:'RESERVE_OPERATION',stage,operation,targetSlot,expectedRevision,currentScope,payloadHash}),commandId=`COMMAND-${nonce().toUpperCase()}`,fields={OPERATION_RESERVATION_ID:allocation.id,JOB_ID:String(p.job?.JOB_ID||''),STAGE:stage,OPERATION:operation,TARGET_SLOT:targetSlot,PACKAGE_ID:String(options.packageId||''),PROMPT_ID:String(options.promptId||''),SCOPE:currentScope,EXPECTED_REVISION:expectedRevision,CHALLENGE_NONCE:challengeNonce,STATUS:'PREPARED',OWNING_TAB_INSTANCE:String(options.owningTabInstance||'UNKNOWN'),IDEMPOTENCY_KEY:idempotencyKey,PAYLOAD:payload,PAYLOAD_HASH:payloadHash,COMMAND_ID:commandId};return{...fields,fields:{...fields},id:allocation.id,operationReservationId:allocation.id,challengeNonce,scope:currentScope,expectedRevision,idempotencyKey,commandId,payload,reservationPayload:payload,__preparedCounter:allocation.counter};}
const EXTERNAL_ACTION_TYPES=new Set(['AI_REVIEW','EXTERNAL_AGENT_TOOL','EXTERNAL_SYSTEM','HUMAN_INSPECTION','CONTINUE_AGENT_CONVERSATION','PASTE_FINAL_JSON']);
const RESERVATION_VERSION_SCOPE_KEYS=Object.freeze(['inputVersion','sourceSetVersion','requirementsVersion','testSuiteVersion','instructionVersion']);
const RESERVATION_LANE_SCOPE_KEYS=Object.freeze(['contextId','runId','iterationId','candidateId','baselineId','productId']);
function normalizedReservationScope(p,supplied={}){const provided=supplied&&typeof supplied==='object'&&!Array.isArray(supplied)?supplied:{},current=scope(p);for(const key of RESERVATION_VERSION_SCOPE_KEYS)if(Object.prototype.hasOwnProperty.call(provided,key)&&provided[key]!=null&&provided[key]!==current[key])throw new Error(`External operation reservation cannot select stale or alternate application-owned ${key}.`);const result={...current,runId:null,contextId:null};for(const key of RESERVATION_LANE_SCOPE_KEYS)if(Object.prototype.hasOwnProperty.call(provided,key))result[key]=provided[key]??null;return result;}
function operationReservationTarget(p,options={}){ensure(p);const stage=Number(options.stage==null?p.activeStage:options.stage),operation=up(options.operation||schema.STAGE_CONTRACTS?.[stage]?.operations?.[0]||'COMPLETE'),requestedScope=normalizedReservationScope(p,options.scope);const targetScope={...requestedScope};delete targetScope.projectRevision;delete targetScope.contextId;const testIds=[...new Set(list(options.testIds||options.testId))].sort(h.compareUnicodeScalarSequence),requirementIds=[...new Set(list(options.requirementIds||options.requirementId))].sort(h.compareUnicodeScalarSequence),artifactIds=[...new Set(list(options.artifactIds||options.artifactId))].sort(h.compareUnicodeScalarSequence),targetIds=[...new Set(list(options.targetIds||options.targetId))].sort(h.compareUnicodeScalarSequence);if(options.applicationNative===true||up(options.executorClass)==='APPLICATION'||['RUN_APP_TESTS','CALCULATE_RELEASE','BUILD_EVIDENCE_CHAINS','FREEZE_CANDIDATE','RESERVE_RUN_BATCH','BEGIN_UNCHANGED_CONFIRMATION','FREEZE_BASELINE','REGISTER_PRODUCTION_CONTEXT','REGISTER_AUTHOR_CONTEXT','RESERVE_PRODUCT_EXECUTION','COMPLETE','BLOCKED','REVIEW_PROPOSAL','ATTACH_REQUIRED_FILES'].includes(up(options.actionType)))return{external:false,stage,operation,targetSlot:null,payload:null,scope:requestedScope};if(stage===1&&operation==='SEMANTIC_CHALLENGE'){const target=intakeSemanticChallengeProjection(p);return{external:true,stage,operation,targetSlot:`SEMANTIC_CHALLENGE:${target.targetHash}`,payload:{targetHash:target.targetHash,inputVersion:target.inputVersion,intakeCoverageManifestHash:target.intakeCoverageManifestHash,rawInputSetHash:target.rawInputSetHash,authorRawResponseId:target.author?.rawResponseId||'',authorPromptId:target.author?.promptId||'',authorContextId:target.author?.contextId||''},scope:requestedScope};}if(stage===1&&operation==='RECONCILE_INTAKE'){const challenge=intakeSemanticChallengeAssessment(p);return{external:true,stage,operation,targetSlot:challenge.current?`RECONCILE_INTAKE:${challenge.challengeSetHash}`:null,payload:challenge.current?{targetHash:challenge.projection.targetHash,challengeSetHash:challenge.challengeSetHash,challengeRawResponseId:challenge.reviewerRawResponseId,challengePromptId:challenge.reviewerPromptId,challengeContextId:challenge.reviewerContextId}:null,scope:requestedScope};}if(stage===2&&operation==='SEARCH_ADEQUACY_REVIEW'){const target=sourceSearchContractProjection(p),author=acceptedStageOperationBinding(p,2,'COMPLETE');return{external:true,stage,operation,targetSlot:`SEARCH_ADEQUACY_REVIEW:${target.sourceSearchContractId}`,payload:{sourceSearchContractId:target.sourceSearchContractId,sourceSearchContractHash:target.sourceSearchContractHash,authorRawResponseId:author?.rawResponseId||'',authorPromptId:author?.promptId||'',authorContextId:author?.contextId||''},scope:requestedScope};}if(stage===4&&['DISPOSITION_CHALLENGE','ATOMICITY_CHALLENGE'].includes(operation)){const challengeKind=operation.replace('_CHALLENGE',''),batch=requirementCompilationChallengeBatches(p).find(item=>item.challengeKind===challengeKind);return{external:true,stage,operation,targetSlot:batch?`REQUIREMENT_CHALLENGE_BATCH:${challengeKind}:${batch.batchHash}`:null,payload:batch?{challengeKind,batchHash:batch.batchHash,targets:batch.targets.map(target=>({targetHash:target.targetHash,targetObligationIds:target.targetObligationIds,targetRequirementIds:target.targetRequirementIds,reasons:target.reasons})),authorRawResponseId:batch.authorRawResponseId,authorPromptId:batch.authorPromptId,authorContextId:batch.authorContextId}:null,scope:requestedScope};}if(stage===4&&operation==='RECONCILE_REQUIREMENTS'){const status=requirementCompilationChallengeStatus(p);return{external:true,stage,operation,targetSlot:status.challengeSetHash?`RECONCILE_REQUIREMENTS:${status.challengeSetHash}`:null,payload:status.challengeSetHash?{challengeSetHash:status.challengeSetHash,reviewIds:[...status.accepted,...status.correctionRequired,...status.unknown].map(item=>item.reviewId).sort(h.compareUnicodeScalarSequence),targetHashes:status.targets.map(item=>item.targetHash).sort(h.compareUnicodeScalarSequence)}:null,scope:requestedScope};}if(stage===6&&operation==='SEMANTIC_REVIEW'){const targetKind=up(options.targetKind),targetId=String(options.targetId||''),target=semanticTarget(p,targetKind,targetId);return{external:true,stage,operation,targetSlot:target?`SEMANTIC_REVIEW:${targetKind}:${targetId}`:null,payload:target?{targetKind,targetId,targetSemanticHash:semanticReviewTargetHash(p,targetKind,targetId),authorRawResponseId:String(target.record.rawResponseId||'')}:null,scope:requestedScope};}if(stage===6&&operation==='COMPLETE')return{external:true,stage,operation,targetSlot:'STAGE6:COMPLETE:AUTHORING',payload:{stage,operation,targetScope,contextId:String(requestedScope.contextId||''),requirementsVersion:String(p.job?.CURRENT_REQUIREMENTS_VERSION||''),proofInputHash:h.sha256Value(old(p,'requirements').map(record=>record.recordSha256||record.sha256||rid(record,'requirements')))},scope:requestedScope};const identity={version:'closed-loop-operation-target/1',jobId:String(p.job?.JOB_ID||''),stage,operation,targetScope,testIds,requirementIds,artifactIds,targetIds},targetHash=h.sha256Value(identity);return{external:true,stage,operation,targetSlot:`OPERATION:${String(stage).padStart(2,'0')}:${operation}:${targetHash.slice(0,24).toUpperCase()}`,payload:identity,scope:requestedScope};}
function prepareCurrentOperationReservation(p,options={}){
  ensure(p);const stage=Number(options.stage==null?p.activeStage:options.stage),operation=up(options.operation||schema.STAGE_CONTRACTS?.[stage]?.operations?.[0]||'COMPLETE'),contextId=String(options.contextId||options.reviewerContextId||options.scope?.contextId||''),target=operationReservationTarget(p,{...options,stage,operation,scope:{...(options.scope||{}),...(contextId?{contextId}:{})}});if(!target.external)throw new Error('The selected operation is application-native or internal and must not create an external reservation.');if(!target.targetSlot||!target.payload)throw new Error('The current external operation target is incomplete or stale.');
  const context=currentFreshContext(p,contextId,stage),external=String(fv(context,'EXTERNAL_CONTEXT_IDENTIFIER')||'');if(!context||placeholderContext(contextId)||placeholderContext(external))throw new Error(`Stage ${stage} ${operation} requires one current registered nonplaceholder external context.`);
  if(independentReviewOperation(stage,operation)){if(!independentReviewerContext(context))throw new Error(`Stage ${stage} ${operation} requires a context registered with the INDEPENDENT REVIEWER role and no observed contamination.`);const authorContextIds=independentAuthorContextIds(p,stage,operation,options),reviewerExternal=up(external);for(const authorContextId of authorContextIds){const author=currentFreshContext(p,authorContextId),authorExternal=up(fv(author,'EXTERNAL_CONTEXT_IDENTIFIER')||'');if(authorContextId===contextId||(authorExternal&&authorExternal===reviewerExternal))throw new Error(`Stage ${stage} ${operation} reviewer context must differ from every producing context in the selected current lane.`);}}
  if(stage===1&&operation==='SEMANTIC_CHALLENGE')return prepareIntakeSemanticChallengeReservation(p,{...options,reviewerContextId:contextId});if(stage===1&&operation==='RECONCILE_INTAKE')return prepareIntakeReconciliationReservation(p,{...options,contextId});if(stage===2&&operation==='SEARCH_ADEQUACY_REVIEW')return prepareSearchAdequacyReviewReservation(p,{...options,reviewerContextId:contextId});if(stage===4&&['DISPOSITION_CHALLENGE','ATOMICITY_CHALLENGE'].includes(operation))return prepareRequirementCompilationChallengeReservation(p,{...options,challengeKind:operation.replace('_CHALLENGE',''),batchHash:String(options.batchHash||target.payload.batchHash),reviewerContextId:contextId});if(stage===4&&operation==='RECONCILE_REQUIREMENTS')return prepareRequirementReconciliationReservation(p,{...options,contextId});if(stage===6&&operation==='SEMANTIC_REVIEW')return prepareSemanticReviewReservation(p,{...options,reviewerContextId:contextId});return prepareReservation(p,{stage,operation,targetSlot:target.targetSlot,scope:{...target.scope,runId:target.scope.runId??null,contextId},payload:target.payload,packageId:options.packageId,promptId:options.promptId,owningTabInstance:options.owningTabInstance});
}
function externalReservationAction(p,stage,action){if(action?.reservationRequired===true)return action;if(!EXTERNAL_ACTION_TYPES.has(up(action?.actionType)))return action;const operation=up(action.operation||schema.STAGE_CONTRACTS?.[stage]?.operations?.[0]||'COMPLETE'),artifactIds=safe(action.filesToSend).map(item=>String(item?.artifactId||'')).filter(Boolean),target=operationReservationTarget(p,{stage,operation,scope:action.scope||{},actionType:action.actionType,testId:action.testId,requirementId:action.requirementId,targetId:action.targetId,artifactIds});return{...action,operation,targetSlot:target.targetSlot,reservationRequired:true,packageRequired:Boolean(safe(action.filesToSend).length||['AI_REVIEW','EXTERNAL_AGENT_TOOL','EXTERNAL_SYSTEM'].includes(up(action.actionType))),reservationTarget:target};}
function semanticReviewTargets(p){ensure(p);refreshProofSemantics(p);const targets=[];for(const test of stageScopedRows(p,'tests',6))if(up(fv(test,'SEMANTIC_COVERAGE_DISPOSITION'))==='EQUIVALENT'&&!list(fv(test,'SEMANTIC_REVIEW_IDS')).length)targets.push({targetKind:'TEST',targetId:rid(test,'tests'),targetSemanticHash:semanticReviewTargetHash(p,'TEST',rid(test,'tests')),authorRawResponseId:String(test.rawResponseId||''),blockingReason:'Independent EQUIVALENT semantic coverage review is required.'});for(const expression of stageScopedRows(p,'proofExpressions',6))if(up(fv(expression,'SEMANTIC_EQUIVALENCE_DISPOSITION'))!=='EQUIVALENT'||!list(fv(expression,'ACCEPTED_SEMANTIC_REVIEW_IDS')).length)targets.push({targetKind:'PROOF_EXPRESSION',targetId:rid(expression,'proofExpressions'),targetSemanticHash:semanticReviewTargetHash(p,'PROOF_EXPRESSION',rid(expression,'proofExpressions')),authorRawResponseId:String(expression.rawResponseId||''),blockingReason:'Independent EQUIVALENT proof-expression review is required.'});return targets;}
function prepareSemanticReviewReservation(p,options={}){ensure(p);const targetKind=up(options.targetKind),targetId=String(options.targetId||''),target=semanticTarget(p,targetKind,targetId);if(!target)throw new Error('Semantic-review target is not exactly one active current-scope test or proof expression.');const author=acceptedSemanticAuthorBinding(p,target);if(!author)throw new Error('Semantic-review target does not have an exact current accepted Stage 6 COMPLETE author binding and controlled test-suite version transition.');const reviewerContextId=String(options.reviewerContextId||'').trim(),reviewerContext=stageScopedRows(p,'freshContexts',6).find(record=>rid(record,'freshContexts')===reviewerContextId&&Number(record.stage)===6),reviewerExternalIdentifier=String(fv(reviewerContext,'EXTERNAL_CONTEXT_IDENTIFIER')||'');if(!reviewerContext||!independentReviewerContext(reviewerContext)||placeholderContext(reviewerContextId)||placeholderContext(reviewerExternalIdentifier))throw new Error('A registered nonplaceholder current Stage 6 independent-reviewer context is required.');if(reviewerContextId===author.contextId||reviewerExternalIdentifier===author.externalContextId)throw new Error('Semantic-review context and external context identifier must differ from the author context.');const targetSemanticHash=semanticReviewTargetHash(p,targetKind,targetId),targetSlot=`SEMANTIC_REVIEW:${targetKind}:${targetId}`,payload={targetKind,targetId,targetSemanticHash,authorRawResponseId:author.rawResponseId,authorPromptId:author.promptId,authorContextId:author.contextId,reviewerContextId},reviewScope={...scope(p),runId:null,contextId:reviewerContextId};return prepareReservation(p,{stage:6,operation:'SEMANTIC_REVIEW',targetSlot,scope:reviewScope,payload,packageId:options.packageId,promptId:options.promptId,owningTabInstance:options.owningTabInstance});}
function reserve(p,options={}){ensure(p);const prepared=options.preparedReservation||prepareReservation(p,options),reservationId=String(fv(prepared,'OPERATION_RESERVATION_ID')||prepared.operationReservationId||prepared.id||''),stage=Number(fv(prepared,'STAGE')),operation=String(fv(prepared,'OPERATION')||'COMPLETE'),targetSlot=String(fv(prepared,'TARGET_SLOT')||'DEFAULT'),currentScope=fv(prepared,'SCOPE')||scope(p),expectedRevision=Number(fv(prepared,'EXPECTED_REVISION')),payload=e0.clone(fv(prepared,'PAYLOAD')||prepared.payload||{}),payloadHash=String(fv(prepared,'PAYLOAD_HASH')||''),idempotencyKey=String(fv(prepared,'IDEMPOTENCY_KEY')||''),challengeNonce=String(fv(prepared,'CHALLENGE_NONCE')||'');if(!reservationId||!/^([a-f0-9]{32})$/i.test(challengeNonce)||expectedRevision!==Number(p.revision||0)+1||!payloadHash||h.sha256Value(payload)!==payloadHash||!idempotencyKey)throw new Error('Prepared reservation identity, nonce, revision, or idempotency binding is invalid.');const current=scoped(p,'operationReservations').filter(record=>Number(fv(record,'STAGE'))===stage&&String(fv(record,'OPERATION'))===operation&&String(fv(record,'TARGET_SLOT'))===targetSlot&&['ACTIVE','ORPHANED'].includes(up(fv(record,'STATUS'))));if(current.length){if(String(fv(current[0],'IDEMPOTENCY_KEY'))===idempotencyKey)return current[0];throw new Error('An authoritative reservation already controls this target operation slot.');}if(safe(p.projectData.operationReservations).some(record=>rid(record,'operationReservations')===reservationId))throw new Error('Prepared operation reservation identity is already used.');const suffix=Number(reservationId.match(/(\d+)$/)?.[1]||0),counterKey='completion:operationReservations';p.projectData.idCounters[counterKey]=Math.max(Number(p.projectData.idCounters[counterKey]||0),Number(prepared.__preparedCounter||suffix));return put(p,'operationReservations',{JOB_ID:String(p.job?.JOB_ID||''),STAGE:stage,OPERATION:operation,TARGET_SLOT:targetSlot,PACKAGE_ID:String(options.packageId??fv(prepared,'PACKAGE_ID')??''),PROMPT_ID:String(options.promptId??fv(prepared,'PROMPT_ID')??''),SCOPE:currentScope,EXPECTED_REVISION:expectedRevision,CHALLENGE_NONCE:challengeNonce,STATUS:'ACTIVE',OWNING_TAB_INSTANCE:String(fv(prepared,'OWNING_TAB_INSTANCE')||'UNKNOWN'),IDEMPOTENCY_KEY:idempotencyKey,PAYLOAD:payload,PAYLOAD_HASH:payloadHash},{id:reservationId,stage,source:'APPLICATION_RESERVATION',scope:currentScope});}
function orphan(p,tabInstance){ensure(p);for(const record of scoped(p,'operationReservations'))if(up(fv(record,'STATUS'))==='ACTIVE'&&String(fv(record,'OWNING_TAB_INSTANCE'))!==String(tabInstance||''))set(record,'operationReservations','STATUS','ORPHANED');return scoped(p,'operationReservations');}
function idem(p,options={}){ensure(p);if(typeof options.execute!=='function')throw new TypeError('executeIdempotentCommand requires execute.');const expectedRevision=Number(options.expectedRevision==null?p.revision:options.expectedRevision),payloadHash=h.sha256Value(options.payload||{}),idempotencyKey=String(options.idempotencyKey||h.sha256Value({commandType:String(options.commandType),target:String(options.target||''),expectedRevision,currentScope:scope(p),payloadHash})),prior=p.projectData.commandReceipts.find(receipt=>receipt.idempotencyKey===idempotencyKey);if(prior){if(prior.payloadHash!==payloadHash)throw new Error('Idempotency key was reused with different content.');return prior.result;}if(Number(p.revision||0)!==expectedRevision)throw new Error('Expected project revision is stale.');const result=options.execute(),receipt={commandId:String(options.commandId||`COMMAND-${nonce().toUpperCase()}`),idempotencyKey,expectedRevision,committedRevision:Number(options.committedRevision==null?expectedRevision+1:options.committedRevision),payloadHash,result,createdAt:new Date().toISOString()};p.projectData.commandReceipts.push(receipt);return result;}
function currentFreshContext(p,contextId,stage=null){const id=String(contextId||'').trim(),source=stage==null?scoped(p,'freshContexts'):stageScopedRows(p,'freshContexts',stage),matches=source.filter(record=>rid(record,'freshContexts')===id&&(stage==null||Number(record.stage)===Number(stage)));return matches.length===1?matches[0]:null;}
function independentReviewerContext(record){return Boolean(record&&/\bINDEPENDENT REVIEWER\b/i.test(String(fv(record,'ROLE')||''))&&up(fv(record,'CONTAMINATION_STATUS')||'UNKNOWN')!=='CONTAMINATED');}
function independentReviewOperation(stage,operation){const number=Number(stage),op=up(operation);return(number===1&&op==='SEMANTIC_CHALLENGE')||(number===2&&op==='SEARCH_ADEQUACY_REVIEW')||(number===4&&['DISPOSITION_CHALLENGE','ATOMICITY_CHALLENGE'].includes(op))||(number===6&&op==='SEMANTIC_REVIEW')||[9,12,23,24].includes(number)||([17,19].includes(number)&&op==='VERIFY');}
function independentAuthorContextIds(p,stage,operation,options={}){const ids=new Set(),number=Number(stage);if(number===9){for(const change of safe(p.projectData.acceptedChanges).filter(item=>Number(item.stage)===8&&item.status==='COMMITTED'&&!item.invalidatedBy))if(change.scope?.contextId)ids.add(String(change.scope.contextId));}if([12,17,19].includes(number)&&up(operation)==='VERIFY'){const runIds=[...new Set(list(options.runIds||options.runId||options.scope?.runId))],runs=old(p,'runs').filter(record=>!runIds.length||runIds.includes(rid(record,'runs')));for(const run of runs){const id=String(fv(run,'CONTEXT_ID')||run.scope?.contextId||'');if(id)ids.add(id);}}if([23,24].includes(number)){for(const product of old(p,'products')){const id=String(fv(product,'PRODUCTION_CONTEXT_ID')||product.relationships?.PRODUCTION_CONTEXT_ID||'');if(id)ids.add(id);}}return ids;}
const requirementChallengeIndexCache=new WeakMap();
function currentAtomicityReview(p,requirementId){const index=requirementChallengeIndex(p),target=index.targetByRequirement.get(`ATOMICITY:${String(requirementId)}`),rows=target?index.currentAssessmentsByTarget.get(`ATOMICITY:${target.targetHash}`)||[]:[];if(rows.length!==1)return null;const assessment=rows[0];return{canonicalReviewId:assessment.reviewId,requirementId:String(requirementId),targetHash:assessment.targetHash,disposition:assessment.finding==='ACCEPTED'?'ATOMIC':assessment.finding==='CORRECTION_REQUIRED'?'NON_ATOMIC':'UNKNOWN',status:'ACCEPTED',reviewOutcome:assessment.finding==='ACCEPTED'?'RESOLVED':'BLOCKED',reasoning:String(fv(index.reviewById.get(assessment.reviewId),'REASONING')||''),sourceCollection:'requirementCompilationChallengeReviews'};}
function recordAtomicityReview(p,options={}){ensure(p);const reviewId=String(options.reviewId||options.requirementChallengeReviewId||''),review=stageScopedRows(p,'requirementCompilationChallengeReviews',4).find(record=>rid(record,'requirementCompilationChallengeReviews')===reviewId),assessment=review?requirementCompilationChallengeReviewAssessment(p,review):null;if(!assessment?.current||assessment.challengeKind!=='ATOMICITY')throw new Error('Atomicity review is recorded only through the current Stage 4 ATOMICITY_CHALLENGE response and its canonical batched review record.');return review;}
function sourceNormativeClassFromManifest(requirement,manifestById){if(!requirement)return'UNKNOWN';const classes=requirementObligationIds(requirement).map(id=>up(manifestById.get(id)?.classification||'')).filter(Boolean),mandatory=new Set(['REQUIREMENT','CONSTRAINT','PROHIBITION','ACCEPTANCE_CONDITION','REQUESTED_OUTPUT','MANDATORY_STATEMENTS','PROHIBITIONS','RESTRICTIONS']),advisory=new Set(['OPTIONAL','OPTIONAL_PRACTICES','RECOMMENDATIONS','EXAMPLES','EXPLANATORY_MATERIAL']);if(classes.some(value=>mandatory.has(value)))return'MANDATORY';if(classes.length&&classes.every(value=>advisory.has(value)))return'OPTIONAL';return'UNKNOWN';}
function sourceNormativeClass(p,requirement){if(!requirement)return'UNKNOWN';const cached=requirementChallengeIndexCache.get(p),requirementId=rid(requirement,'requirements');if(cached?.sourceNormativeClassByRequirement?.has(requirementId))return cached.sourceNormativeClassByRequirement.get(requirementId);const manifest=e0.obligationManifest(p),byId=new Map(safe(manifest.items).map(item=>[String(item.obligationId),item]));return sourceNormativeClassFromManifest(requirement,byId);}
function currentNormativeReview(p,requirementId){const id=String(requirementId),index=requirementChallengeIndex(p),requirement=index.requirementById.get(id),target=index.targetByRequirement.get(`DISPOSITION:${id}`),rows=target&&target.reasons.some(reason=>String(reason).startsWith('NORMATIVE_REDUCTION_'))?index.currentAssessmentsByTarget.get(`DISPOSITION:${target.targetHash}`)||[]:[];if(rows.length!==1)return null;const assessment=rows[0],proposedClass=normative(fv(requirement,'MANDATORY_OPTIONAL_STATUS')),priorClass=index.sourceNormativeClassByRequirement.get(id)||'UNKNOWN';return{canonicalReviewId:assessment.reviewId,requirementId:id,targetHash:assessment.targetHash,priorClass,proposedClass,disposition:assessment.finding==='ACCEPTED'?'ACCEPTED':assessment.finding==='CORRECTION_REQUIRED'?'REJECTED':'UNKNOWN',status:'ACCEPTED',sourceCollection:'requirementCompilationChallengeReviews'};}
function effectiveNormativeClass(p,requirement){const proposed=normative(fv(requirement,'MANDATORY_OPTIONAL_STATUS')),priorClass=sourceNormativeClass(p,requirement);if(!['CONDITIONAL','OPTIONAL'].includes(proposed))return proposed;const review=currentNormativeReview(p,rid(requirement,'requirements'));if(!review)return'UNKNOWN';if(review.disposition==='ACCEPTED')return proposed;if(review.disposition==='REJECTED')return priorClass==='UNKNOWN'?'MANDATORY':priorClass;return'UNKNOWN';}
function recordNormativeClassificationReview(p,options={}){ensure(p);const reviewId=String(options.reviewId||options.requirementChallengeReviewId||''),review=stageScopedRows(p,'requirementCompilationChallengeReviews',4).find(record=>rid(record,'requirementCompilationChallengeReviews')===reviewId),assessment=review?requirementCompilationChallengeReviewAssessment(p,review):null;if(!assessment?.current||assessment.challengeKind!=='DISPOSITION'||!assessment.target?.reasons?.some(reason=>String(reason).startsWith('NORMATIVE_REDUCTION_')))throw new Error('Normative-classification review is recorded only through the current Stage 4 DISPOSITION_CHALLENGE response and its canonical batched review record.');return review;}
function recordScopeChangingException(p,options={}){
  ensure(p);const expectedRevision=Number(options.expectedRevision==null?p.revision:options.expectedRevision),reason=String(options.reason||'').trim(),authorityBasis=String(options.authorityBasis||'').trim(),humanDecisionId=String(options.humanDecisionId||'').trim(),affectedPropositionIds=[...new Set(list(options.affectedPropositionIds))],changes=options.humanInputChanges&&typeof options.humanInputChanges==='object'&&!Array.isArray(options.humanInputChanges)?options.humanInputChanges:null;if(options.authorized!==true||!humanDecisionId||!reason||!authorityBasis||!affectedPropositionIds.length||!changes||!Object.keys(changes).length)throw new Error('Scope-changing exception requires a canonical human-decision identity, explicit authorization, authority basis, affected propositions, and actual human-input changes.');if(options.nonwaivable===true)throw new Error('A nonwaivable obligation cannot be removed by controlled exception.');for(const id of affectedPropositionIds)if(props(p).filter(record=>rid(record,'propositions')===id).length!==1)throw new Error(`Affected proposition ${id} is not exactly one active current proposition.`);for(const[name,value]of Object.entries(changes)){const producer=schema.JOB_FIELDS?.[name]?.producer;if(!['HUMAN','HUMAN_DECISION'].includes(producer))throw new Error(`Scope-changing exception cannot mutate non-human Job field ${name}.`);if(h.stableStringify(p.job[name]??null)===h.stableStringify(value))throw new Error(`Scope-changing exception field ${name} does not change current human authority.`);}const payload={reason,authorityBasis,affectedPropositionIds,humanInputChanges:changes,effectivePeriod:String(options.effectivePeriod||''),independentChallengeId:String(options.independentChallengeId||''),humanDecisionId,expectedRevision,scope:stageSemanticScope(p,1)};
  return idem(p,{commandType:'RECORD_SCOPE_CHANGING_EXCEPTION',target:affectedPropositionIds.join('|'),expectedRevision,idempotencyKey:options.idempotencyKey,commandId:options.commandId,payload,execute:()=>{for(const[name,value]of Object.entries(changes))p.job[name]=e0.clone(value);const inputVersion=e0.recordHumanInputVersion(p,Object.keys(changes),String(options.operatorLabel||'HUMAN_OPERATOR')),requirementsVersion=e0.nextVersion(p.job.CURRENT_REQUIREMENTS_VERSION,'REQUIREMENTS'),eventSequence=Number(p.projectData.eventSequence||0)+1,controlledExceptionId=`CONTROLLED-EXCEPTION-${h.sha256Value({humanDecisionId,affectedPropositionIds,inputVersion:inputVersion.version,requirementsVersion,reason}).slice(0,20).toUpperCase()}`,record={eventId:controlledExceptionId,eventSequence,type:'CONTROLLED_SCOPE_EXCEPTION',stage:1,scope:stageSemanticScope(p,1),humanDecisionId,humanIdentityAssurance:'SELF_ASSERTED',reason,authorityBasis,affectedPropositionIds,inputVersion:inputVersion.version,requirementsVersion,historicalViolationsPreserved:true,deviceTimestamp:new Date().toISOString()};p.projectData.eventSequence=eventSequence;p.projectData.history=safe(p.projectData.history);p.projectData.history.push(record);p.job.CURRENT_REQUIREMENTS_VERSION=requirementsVersion;e0.invalidateStageForAuthorityChange(p,{stage:1,reason:`Authorized scope change ${controlledExceptionId}: ${reason}`,operatorLabel:String(options.operatorLabel||'HUMAN_OPERATOR')});return record;}});
}
function routeUnknownSeverity(p,defectOrId){ensure(p);const defect=typeof defectOrId==='object'?defectOrId:e0.records(p,'defects').find(record=>rid(record,'defects')===String(defectOrId||'')),defectId=defect?rid(defect,'defects'):String(defectOrId||''),severity=up(fv(defect,'ACCEPTED_SEVERITY')||fv(defect,'SEVERITY')||fv(defect,'PROPOSED_SEVERITY')||'UNKNOWN');if(!defect)return{defectId,severity:'UNKNOWN',disposition:'BLOCKED',returnStage:14,operatorAction:'ESTABLISH_DEFECT_IDENTITY',reason:'The current defect identity is unavailable.'};if(severity==='UNKNOWN')return{defectId,severity,disposition:'BLOCKED',returnStage:14,operatorAction:'INDEPENDENT_SEVERITY_REVIEW',reason:'UNKNOWN severity cannot suppress root-cause, correction, regression, or release routing.'};return{defectId,severity,disposition:['CRITICAL','MAJOR'].includes(severity)?'BLOCKED':'ROUTED',returnStage:['CRITICAL','MAJOR'].includes(severity)?14:15,operatorAction:['CRITICAL','MAJOR'].includes(severity)?'ROOT_CAUSE_AND_CORRECT':'PRESERVE_AND_VERIFY',reason:`Current accepted severity is ${severity}.`};}
function evaluateFreshnessPolicy(subject={},policyInput=null,options={}){const read=name=>subject?.fields?.[name]??subject?.[name],embedded=read('FRESHNESS_REQUIREMENT'),policy=policyInput&&typeof policyInput==='object'?policyInput:(embedded&&typeof embedded==='object'?embedded:{}),hasPolicy=Object.keys(policy).length>0,requireValidUntil=policy.requireValidUntil===true||policy.validUntilRequired===true,required=policy.required===true||requireValidUntil||policy.requireObservedAt===true||Number.isFinite(policy.maxAgeMs),declaredStatus=up(read('FRESHNESS_STATUS')||read('freshnessStatus')||'UNKNOWN'),observedAt=String(read('OBSERVED_AT')||read('observedAt')||''),observedAtBasis=up(read('OBSERVED_AT_BASIS')||read('observedAtBasis')||'DEVICE_REPORTED'),validFrom=String(read('VALID_FROM')||read('validFrom')||''),validUntil=String(read('VALID_UNTIL')||read('validUntil')||''),supersededBy=String(read('SUPERSEDED_BY')||read('supersededBy')||''),nowValue=options.now instanceof Date?options.now:new Date(options.now||Date.now()),reasons=[];if(!hasPolicy)return{status:E.fresh.includes(declaredStatus)?declaredStatus:'UNKNOWN',current:['CURRENT','NOT_APPLICABLE'].includes(declaredStatus),reasons:['No explicit freshness policy was declared; the recorded status is preserved without elevation.'],observedAt,observedAtBasis,validFrom:validFrom||null,validUntil:validUntil||null,timeBasis:up(options.nowBasis||'DEVICE_REPORTED')};if(policy.required===false&&!required)return{status:'NOT_APPLICABLE',current:true,reasons:['Freshness is not required by the current policy.'],observedAt,observedAtBasis,validFrom:validFrom||null,validUntil:validUntil||null,timeBasis:up(options.nowBasis||'DEVICE_REPORTED')};if(supersededBy)reasons.push(`Evidence is superseded by ${supersededBy}.`);if((required||policy.requireObservedAt!==false)&&!observedAt)reasons.push('Freshness policy requires observedAt.');if(requireValidUntil&&!validUntil)reasons.push('Freshness policy requires validUntil; absence cannot be treated as current.');const parse=(value,label)=>{if(!value)return null;const time=Date.parse(value);if(!Number.isFinite(time)){reasons.push(`${label} is not a valid explicit timestamp.`);return null;}return time;},observedTime=parse(observedAt,'observedAt'),fromTime=parse(validFrom,'validFrom'),untilTime=parse(validUntil,'validUntil'),nowTime=nowValue.getTime();if(!Number.isFinite(nowTime))reasons.push('Evaluation time is invalid.');if(fromTime!=null&&nowTime<fromTime)reasons.push('Evidence is not yet valid.');let expired=Boolean(supersededBy)||(untilTime!=null&&nowTime>untilTime);if(Number.isFinite(policy.maxAgeMs)){if(observedTime==null)reasons.push('Freshness maxAge requires observedAt.');else if(nowTime-observedTime>Number(policy.maxAgeMs))expired=true;}const unknown=reasons.some(reason=>/requires|invalid|not yet valid/i.test(reason));return{status:expired?'EXPIRED':unknown?'UNKNOWN':'CURRENT',current:!expired&&!unknown,reasons:reasons.length?reasons:['Declared freshness policy is satisfied at the available time basis.'],observedAt,observedAtBasis,validFrom:validFrom||null,validUntil:validUntil||null,timeBasis:up(options.nowBasis||'DEVICE_REPORTED')};}
function validateJustificationGraph(projectOrEdges,options={}){const isProject=projectOrEdges&&projectOrEdges.projectData,edges=[];if(Array.isArray(projectOrEdges))for(const edge of projectOrEdges)edges.push({from:String(edge.from||edge.source||''),to:String(edge.to||edge.target||''),kind:String(edge.kind||'EXPLICIT')});else if(Array.isArray(options.edges))for(const edge of options.edges)edges.push({from:String(edge.from||edge.source||''),to:String(edge.to||edge.target||''),kind:String(edge.kind||'EXPLICIT')});if(isProject){const p=projectOrEdges;for(const observation of scoped(p,'observationRecords')){const observationId=rid(observation,'observationRecords');for(const evidenceId of list(fv(observation,'SOURCE_EVIDENCE_IDS')))edges.push({from:evidenceId,to:observationId,kind:'EVIDENCE_OBSERVATION'});}for(const relation of scoped(p,'entailmentReviews')){const relationId=rid(relation,'entailmentReviews'),observationId=String(fv(relation,'OBSERVATION_ID')),propositionId=String(fv(relation,'TARGET_PROPOSITION_ID'));if(observationId)edges.push({from:observationId,to:relationId,kind:'OBSERVATION_ENTAILMENT'});if(propositionId)edges.push({from:relationId,to:propositionId,kind:'ENTAILMENT_PROPOSITION'});}for(const obligation of scoped(p,'proofObligations'))edges.push({from:String(fv(obligation,'PROPOSITION_ID')),to:rid(obligation,'proofObligations'),kind:'PROPOSITION_PROOF_OBLIGATION'});for(const chain of old(p,'evidenceChains')){const chainId=rid(chain,'evidenceChains'),obligationId=String(fv(chain,'PROOF_OBLIGATION_ID'));if(obligationId)edges.push({from:obligationId,to:chainId,kind:'OBLIGATION_EVIDENCE_CHAIN'});}}
  const clean=edges.filter(edge=>edge.from&&edge.to),adjacency=new Map();for(const edge of clean){if(!adjacency.has(edge.from))adjacency.set(edge.from,[]);adjacency.get(edge.from).push(edge.to);}const visiting=new Set(),visited=new Set(),stack=[],cycles=[];const walk=node=>{if(visiting.has(node)){const index=stack.indexOf(node);cycles.push([...stack.slice(index),node]);return;}if(visited.has(node))return;visiting.add(node);stack.push(node);for(const next of adjacency.get(node)||[])walk(next);stack.pop();visiting.delete(node);visited.add(node);};for(const node of adjacency.keys())walk(node);return{acyclic:cycles.length===0,status:cycles.length?'BLOCKED':'ACCEPTED',cycles,edges:clean,reasons:cycles.length?cycles.map(cycle=>`Justification cycle: ${cycle.join(' -> ')}`):[]};}
const SEMANTIC_CHALLENGE_THRESHOLDS=Object.freeze({maxUnitsWithoutChallenge:80,maxBytesWithoutChallenge:500000,maxFilesWithoutChallenge:8});
function semanticChallengeTriggers(p,options={}){const manifest=e0.intakeCoverageManifest(p),capture=e0.parseCapturedInputSet(p),normalized=value=>up(value).replace(/[\s-]+/g,'_'),byUnit=new Map(safe(capture.units).map(unit=>[String(unit.sourceUnitId||''),unit])),fileUnits=safe(manifest.units).filter(unit=>up(unit.kind)==='SUPPLIED_MATERIAL'),totalBytes=fileUnits.reduce((sum,unit)=>sum+Math.max(0,Number(unit.byteSize||0)),0),triggers=[];for(const unit of safe(manifest.units)){const extracted=byUnit.get(String(unit.unitId)),disposition=normalized(extracted?.disposition);if(disposition==='NO_PROJECT_RELEVANT_INFORMATION'&&up(unit.kind)==='SUPPLIED_MATERIAL')triggers.push(`WHOLE_ARTIFACT_NO_RELEVANT_INFORMATION:${unit.unitId}`);if(disposition==='INACCESSIBLE_OR_BLOCKED')triggers.push(`MATERIAL_UNIT_INACCESSIBLE_OR_BLOCKED:${unit.unitId}`);if(up(unit.kind)==='SUPPLIED_MATERIAL'&&extracted&&!safe(extracted.extractedStatements).length&&!['RETAINED_AS_CONTEXT','NO_PROJECT_RELEVANT_INFORMATION'].includes(disposition))triggers.push(`MATERIAL_ARTIFACT_WITHOUT_EXTRACTED_STATEMENT:${unit.unitId}`);}if(manifest.unitCount>SEMANTIC_CHALLENGE_THRESHOLDS.maxUnitsWithoutChallenge)triggers.push('LARGE_UNIT_COUNT');if(totalBytes>SEMANTIC_CHALLENGE_THRESHOLDS.maxBytesWithoutChallenge)triggers.push('LARGE_BYTE_COUNT');if(fileUnits.length>SEMANTIC_CHALLENGE_THRESHOLDS.maxFilesWithoutChallenge)triggers.push('LARGE_FILE_COUNT');if(options.operatorRequested===true||p.projectData.semanticChallengeRequested===true)triggers.push('OPERATOR_REQUESTED');const stageOneData=p.stages?.[1]?.agentData||{},uncertainty=String(stageOneData.UNKNOWN_INFORMATION||'');if(/\b(?:material uncertainty|material contradiction|broad exclusion)\b/i.test(uncertainty))triggers.push('MATERIAL_UNCERTAINTY_OR_CONTRADICTION');if(safe(stageOneData.MATERIAL_OMISSIONS_FOUND).length||safe(p.stages?.[1]?.derivedData?.MATERIAL_OMISSIONS_FOUND).length)triggers.push('MATERIAL_OMISSION_IDENTIFIED');return{triggers:[...new Set(triggers)],manifest,capture};}
function semanticChallengeStatus(p,options={}){ensure(p);const triggerState=semanticChallengeTriggers(p,options),required=triggerState.triggers.length>0,projection=intakeSemanticChallengeProjection(p),challenge=required?intakeSemanticChallengeAssessment(p):null,reconciliation=challenge?.current?intakeReconciliationAssessment(p,challenge):null,stageOneData=p.stages?.[1]?.agentData||{},unresolved=Number(p.stages?.[1]?.derivedData?.UNRESOLVED_OMISSION_CHALLENGES??stageOneData.UNRESOLVED_OMISSION_CHALLENGES??0),reasons=[];if(required&&!projection.author)reasons.push('Current Stage 1 COMPLETE intake lacks an accepted nonplaceholder author-context binding.');if(required&&!challenge?.current)reasons.push(...(challenge?.reasons?.length?challenge.reasons:['Independent Stage 1 semantic challenge has not been accepted for the exact current intake target.']));if(required&&challenge?.current&&!reconciliation?.current)reasons.push(...(reconciliation?.reasons?.length?reconciliation.reasons:['Stage 1 challenge outputs have not been reconciled against the exact current challenge set.']));if(unresolved>0)reasons.push(`${unresolved} material omission challenge(s) remain unresolved.`);const complete=!required||Boolean(projection.author&&challenge?.current&&reconciliation?.current&&unresolved===0);return{required,status:complete?'COMPLETE':required?'BLOCKED':'NOT_REQUIRED',complete,triggers:triggerState.triggers,projection,challenge,reconciliation,challengeSetHash:challenge?.challengeSetHash||'',challengeContextIds:challenge?.reviewerContextId?[challenge.reviewerContextId]:[],unresolvedOmissionChallenges:unresolved,thresholds:SEMANTIC_CHALLENGE_THRESHOLDS,reasons:[...new Set(reasons)]};}
const SOURCE_SEARCH_CONTRACT_FIELDS=Object.freeze(['SEARCH_UNIVERSE','SEARCH_PROCEDURE','SEARCH_LOCATIONS','SEARCH_QUERIES_OR_STRATEGIES','SEARCH_CUTOFF','SEARCH_LIMITATIONS','SEARCH_EXECUTION_EVIDENCE','DISCOVERY_RISK','SOURCE_APPLICABILITY_DETERMINATION']);
function acceptedStageOperationBinding(p,stage,operation='COMPLETE'){
  const changes=safe(p.projectData.acceptedChanges).filter(change=>Number(change.stage)===Number(stage)&&up(change.operation||'COMPLETE')===up(operation)&&change.status==='COMMITTED'&&!change.invalidatedBy),change=changes.at(-1);if(!change)return null;
  const rawResponseId=String(change.rawResponseId||''),raw=safe(p.projectData.rawResponses).find(record=>record.rawResponseId===rawResponseId&&!record.invalidatedBy),promptId=String(change.promptId||raw?.promptInstructionId||''),prompt=safe(p.projectData.generatedPrompts).find(record=>String(record.instructionId||record.promptId)===promptId&&!record.invalidatedBy),contextId=String(change.scope?.contextId||raw?.contextId||prompt?.scope?.contextId||''),context=currentFreshContext(p,contextId,stage),externalContextId=String(fv(context,'EXTERNAL_CONTEXT_IDENTIFIER')||'');if(!raw||!prompt||up(prompt.operation||operation)!==up(operation)||String(prompt.scope?.contextId||'')!==contextId||String(raw.contextId||contextId)!==contextId||!context||placeholderContext(contextId)||placeholderContext(externalContextId))return null;return{change,raw,prompt,rawResponseId,promptId,contextId,context,externalContextId};
}
function acceptedReservationBinding(p,binding,stage,operation,targetSlot){const prompt=binding?.prompt,reservationId=String(prompt?.operationBinding?.operationReservationId||prompt?.contextManifest?.operationBinding?.operationReservationId||binding?.raw?.operationReservationId||''),matches=stageScopedRows(p,'operationReservations',stage).filter(record=>rid(record,'operationReservations')===reservationId),reservation=matches.length===1?matches[0]:null,reasons=[];if(!reservation||up(fv(reservation,'STATUS'))!=='ACCEPTED')reasons.push(`${operation} lacks one accepted current operation reservation.`);if(reservation&&Number(fv(reservation,'STAGE'))!==Number(stage))reasons.push(`${operation} reservation belongs to the wrong stage.`);if(reservation&&up(fv(reservation,'OPERATION'))!==up(operation))reasons.push(`${operation} reservation names the wrong operation.`);if(reservation&&String(fv(reservation,'TARGET_SLOT'))!==String(targetSlot))reasons.push(`${operation} reservation targets stale or wrong semantic material.`);if(reservation&&String(fv(reservation,'PROMPT_ID')||'')&&String(fv(reservation,'PROMPT_ID'))!==String(binding?.promptId||''))reasons.push(`${operation} reservation is bound to a different prompt.`);return{reservation,reservationId,reasons};}
function intakeSemanticChallengeProjection(p){ensure(p);const manifest=e0.intakeCoverageManifest(p),author=acceptedStageOperationBinding(p,1,'COMPLETE'),inputVersion=String(p.job?.CURRENT_INPUT_VERSION||manifest.inputVersion||''),rawUnits=safe(manifest.units).map(unit=>({unitId:String(unit.unitId||''),kind:String(unit.kind||''),rawValueSha256:String(unit.rawValueSha256||''),artifactId:String(unit.artifactId||'')})).sort((a,b)=>h.compareUnicodeScalarSequence(a.unitId,b.unitId)),rawInputSetHash=h.sha256Value({schema:'closed-loop-raw-input-set/1',inputVersion,manifestSha256:String(manifest.manifestSha256||''),rawUnits}),content={version:'closed-loop-intake-semantic-challenge-target/1',scope:stageSemanticScope(p,1),inputVersion,intakeCoverageManifestHash:String(manifest.manifestSha256||h.sha256Value(manifest)),rawInputSetHash,rawUnits},targetHash=h.sha256Value(content);return{...content,targetHash,author};}
function intakeSemanticChallengeAssessment(p){const projection=intakeSemanticChallengeProjection(p),binding=acceptedStageOperationBinding(p,1,'SEMANTIC_CHALLENGE'),targetSlot=`SEMANTIC_CHALLENGE:${projection.targetHash}`,reservationBinding=acceptedReservationBinding(p,binding,1,'SEMANTIC_CHALLENGE',targetSlot),manifestTarget=binding?.prompt?.contextManifest?.intakeSemanticChallengeTarget||{},manifestIsolation=binding?.prompt?.contextManifest?.intakeSemanticChallengeIsolation||{},reasons=[];if(!binding)reasons.push('Stage 1 SEMANTIC_CHALLENGE response is missing, stale, or not bound to a current reviewer context.');if(binding&&!independentReviewerContext(binding.context))reasons.push('Stage 1 semantic challenge context is not a current uncontaminated INDEPENDENT REVIEWER context.');reasons.push(...reservationBinding.reasons);if(!projection.author)reasons.push('Stage 1 semantic challenge lacks a current author response with exact context provenance.');if(binding&&projection.author&&(binding.rawResponseId===projection.author.rawResponseId||binding.promptId===projection.author.promptId||binding.contextId===projection.author.contextId||binding.externalContextId===projection.author.externalContextId))reasons.push('Stage 1 semantic challenge reused the author raw response, prompt, context, or external context identity.');const exactTarget=String(manifestTarget.targetHash||'')===projection.targetHash&&String(manifestTarget.inputVersion||'')===projection.inputVersion&&String(manifestTarget.intakeCoverageManifestHash||'')===projection.intakeCoverageManifestHash&&String(manifestTarget.rawInputSetHash||'')===projection.rawInputSetHash&&String(manifestTarget.authorRawResponseId||'')===String(projection.author?.rawResponseId||'')&&String(manifestTarget.authorPromptId||'')===String(projection.author?.promptId||'')&&String(manifestTarget.authorContextId||'')===String(projection.author?.contextId||'');if(!exactTarget)reasons.push('Stage 1 semantic challenge prompt does not bind the exact current intake target and author provenance.');const isolated=up(manifestIsolation.applicationInputIsolation)==='APPLICATION_ESTABLISHED'&&exactTarget;if(!isolated)reasons.push('Application input isolation is not established for the independent Stage 1 semantic challenge.');const current=reasons.length===0,challengeSetHash=current?h.sha256Value({targetHash:projection.targetHash,challengeRawResponseId:binding.rawResponseId,challengePromptId:binding.promptId,challengeContextId:binding.contextId,canonicalEnvelopeSha256:binding.change.canonicalEnvelopeSha256||''}):'';return{current,status:current?'ACCEPTED':binding&&String(manifestTarget.targetHash||'')&&String(manifestTarget.targetHash)!==projection.targetHash?'STALE':'BLOCKED',projection,binding,reservationId:reservationBinding.reservationId,reviewerRawResponseId:String(binding?.rawResponseId||''),reviewerPromptId:String(binding?.promptId||''),reviewerContextId:String(binding?.contextId||''),applicationInputIsolation:isolated?'APPLICATION_ESTABLISHED':'VIOLATED',challengeSetHash,reasons:[...new Set(reasons)]};}
function intakeReconciliationAssessment(p,challenge=intakeSemanticChallengeAssessment(p)){const binding=acceptedStageOperationBinding(p,1,'RECONCILE_INTAKE'),targetSlot=`RECONCILE_INTAKE:${challenge.challengeSetHash||'MISSING'}`,reservationBinding=acceptedReservationBinding(p,binding,1,'RECONCILE_INTAKE',targetSlot),manifestTarget=binding?.prompt?.contextManifest?.intakeReconciliationTarget||{},reasons=[];if(!challenge.current)reasons.push('Stage 1 intake reconciliation cannot precede a current independently bound semantic challenge.');if(!binding)reasons.push('Stage 1 RECONCILE_INTAKE response is missing, stale, or lacks current context provenance.');reasons.push(...reservationBinding.reasons);if(String(manifestTarget.challengeSetHash||'')!==String(challenge.challengeSetHash||'')||String(manifestTarget.targetHash||'')!==String(challenge.projection?.targetHash||'')||String(manifestTarget.challengeRawResponseId||'')!==String(challenge.reviewerRawResponseId||''))reasons.push('Stage 1 reconciliation prompt is not bound to the exact current intake challenge set.');if(binding&&challenge.binding&&Number(binding.change.eventSequence||0)<=Number(challenge.binding.change.eventSequence||0))reasons.push('Stage 1 reconciliation does not follow the accepted challenge response.');const current=reasons.length===0;return{current,status:current?'ACCEPTED':binding?'STALE':'BLOCKED',binding,reservationId:reservationBinding.reservationId,challengeSetHash:challenge.challengeSetHash||'',reasons:[...new Set(reasons)]};}
function prepareIntakeSemanticChallengeReservation(p,options={}){ensure(p);const projection=intakeSemanticChallengeProjection(p),author=projection.author;if(!author)throw new Error('Stage 1 semantic challenge requires a current accepted COMPLETE response bound to one nonplaceholder author context.');const reviewerContextId=String(options.reviewerContextId||''),reviewer=currentFreshContext(p,reviewerContextId,1),reviewerExternal=String(fv(reviewer,'EXTERNAL_CONTEXT_IDENTIFIER')||'');if(!reviewer||!independentReviewerContext(reviewer)||placeholderContext(reviewerContextId)||placeholderContext(reviewerExternal))throw new Error('Stage 1 semantic challenge requires one current registered nonplaceholder independent-reviewer context.');if(reviewerContextId===author.contextId||reviewerExternal===author.externalContextId)throw new Error('Stage 1 semantic challenge reviewer must use a context distinct from the intake author.');const targetSlot=`SEMANTIC_CHALLENGE:${projection.targetHash}`,payload={targetHash:projection.targetHash,inputVersion:projection.inputVersion,intakeCoverageManifestHash:projection.intakeCoverageManifestHash,rawInputSetHash:projection.rawInputSetHash,authorRawResponseId:author.rawResponseId,authorPromptId:author.promptId,authorContextId:author.contextId,reviewerContextId};return prepareReservation(p,{stage:1,operation:'SEMANTIC_CHALLENGE',targetSlot,scope:{...scope(p),runId:null,contextId:reviewerContextId},payload,packageId:options.packageId,promptId:options.promptId,owningTabInstance:options.owningTabInstance});}
function prepareIntakeReconciliationReservation(p,options={}){ensure(p);const challenge=intakeSemanticChallengeAssessment(p);if(!challenge.current)throw new Error('Stage 1 reconciliation requires one current accepted independent semantic challenge.');const contextId=String(options.contextId||options.reconcilerContextId||''),context=currentFreshContext(p,contextId,1),external=String(fv(context,'EXTERNAL_CONTEXT_IDENTIFIER')||'');if(!context||placeholderContext(contextId)||placeholderContext(external))throw new Error('Stage 1 reconciliation requires one current registered nonplaceholder context.');const targetSlot=`RECONCILE_INTAKE:${challenge.challengeSetHash}`,payload={targetHash:challenge.projection.targetHash,challengeSetHash:challenge.challengeSetHash,challengeRawResponseId:challenge.reviewerRawResponseId,challengePromptId:challenge.reviewerPromptId,challengeContextId:challenge.reviewerContextId,reconcilerContextId:contextId};return prepareReservation(p,{stage:1,operation:'RECONCILE_INTAKE',targetSlot,scope:{...scope(p),runId:null,contextId},payload,packageId:options.packageId,promptId:options.promptId,owningTabInstance:options.owningTabInstance});}
function stage4CompilationAuthorBinding(p){const candidates=['COMPLETE','RECONCILE_REQUIREMENTS'].map(operation=>acceptedStageOperationBinding(p,4,operation)).filter(Boolean).sort((a,b)=>Number(a.change.eventSequence||0)-Number(b.change.eventSequence||0));return candidates.at(-1)||null;}
function requirementObligationIds(requirement){return[...new Set(String(fv(requirement,'USER_INPUT_RELATIONSHIP')||'').match(/OBLIGATION-[A-F0-9]{20}/g)||[])].sort(h.compareUnicodeScalarSequence);}
function currentObligationDispositions(p){const output=[];for(const evidence of old(p,'evidenceRecords').filter(record=>Number(record.stage)===4&&up(fv(record,'KIND'))==='OBLIGATION_DISPOSITION')){try{const value=JSON.parse(String(fv(evidence,'CONTENT')||'')),obligationId=String(value.obligationId||''),disposition=up(value.disposition).replace(/[\s-]+/g,'_');if(obligationId)output.push({obligationId,disposition,reason:String(value.reason||''),evidenceId:rid(evidence,'evidenceRecords'),evidenceHash:String(evidence.recordSha256||evidence.sha256||'')});}catch{}}return output;}
function stage4SemanticRecord(record,collection){const definition=RS[collection],id=rid(record,collection),agentFields=(schema.recordAgentFields?.(collection)||Object.values(definition?.fieldDefinitions||{}).filter(item=>item.producer===schema.PRODUCER?.AGENT).map(item=>item.name)).slice().sort(h.compareUnicodeScalarSequence),fields={};for(const name of agentFields)if(fv(record,name)!==undefined)fields[name]=e0.clone(fv(record,name));const relationships={};for(const name of Object.keys(definition?.relationships||{}).sort(h.compareUnicodeScalarSequence)){const value=record?.relationships?.[name]??fv(record,name);if(value!==undefined&&value!==null&&value!=='')relationships[name]=e0.clone(value);}return{kind:collection==='requirements'?'REQUIREMENT':'PROPOSITION',id,agentSemanticFields:fields,typedRelationships:relationships,semanticContentSha256:h.sha256Value({collection,id,fields,relationships})};}
function computeRequirementCompilationChallengeTargets(p,providedManifest=null){ensure(p);const author=stage4CompilationAuthorBinding(p);if(!author)return[];const manifest=providedManifest||e0.obligationManifest(p),requirements=old(p,'requirements'),propositions=props(p),targets=[];const add=(challengeKind,targetObligationIds,targetRequirementIds,reasons,semanticRecords)=>{const normalizedObligations=[...new Set(targetObligationIds.map(String).filter(Boolean))].sort(h.compareUnicodeScalarSequence),normalizedRequirements=[...new Set(targetRequirementIds.map(String).filter(Boolean))].sort(h.compareUnicodeScalarSequence),content={version:'closed-loop-requirement-challenge-target/1',challengeKind,targetObligationIds:normalizedObligations,targetRequirementIds:normalizedRequirements,reasons:[...new Set(reasons.map(String))].sort(h.compareUnicodeScalarSequence),obligationManifestHash:String(manifest.manifestSha256||''),requirementsVersion:String(p.job?.CURRENT_REQUIREMENTS_VERSION||''),semanticRecords};targets.push({...content,targetHash:h.sha256Value(content),authorRawResponseId:author.rawResponseId,authorPromptId:author.promptId,authorContextId:author.contextId});};
  for(const disposition of currentObligationDispositions(p))if(['INAPPLICABLE','RETAINED_NONNORMATIVE_CONTEXT'].includes(disposition.disposition))add('DISPOSITION',[disposition.obligationId],[],[`CHALLENGE_${disposition.disposition}`],[{kind:'OBLIGATION_DISPOSITION',obligationId:disposition.obligationId,disposition:disposition.disposition,reason:disposition.reason,evidenceId:disposition.evidenceId,semanticContentSha256:h.sha256Value({obligationId:disposition.obligationId,disposition:disposition.disposition,reason:disposition.reason,evidenceId:disposition.evidenceId})}]);
  for(const requirement of requirements){const requirementId=rid(requirement,'requirements'),obligationIds=requirementObligationIds(requirement),classification=normative(fv(requirement,'MANDATORY_OPTIONAL_STATUS')),app=up(fv(requirement,'APPLICABILITY')||fv(requirement,'PROPOSED_APPLICABILITY')||''),uncertain=/\b(?:UNKNOWN|UNCERTAIN|UNRESOLVED|BLOCKED)\b/i.test(String(fv(requirement,'NOTES')||fv(requirement,'ASSUMPTIONS')||fv(requirement,'APPLICABILITY')||'')),reasons=[],requirementSemantic=stage4SemanticRecord(requirement,'requirements');if(['CONDITIONAL','OPTIONAL'].includes(classification))reasons.push(`NORMATIVE_REDUCTION_${classification}`);if(obligationIds.length>1)reasons.push('MULTIPLE_OBLIGATIONS_MERGED');if(['NOT_APPLICABLE','INAPPLICABLE'].includes(app))reasons.push('PROPOSED_NOT_APPLICABLE');if(uncertain)reasons.push('MATERIAL_COMPILER_UNCERTAINTY');if(reasons.length)add('DISPOSITION',obligationIds,[requirementId],reasons,[requirementSemantic]);const linked=propositions.filter(proposition=>String(fv(proposition,'REQUIREMENT_ID'))===requirementId),semanticRecords=[requirementSemantic,...linked.map(proposition=>stage4SemanticRecord(proposition,'propositions'))];add('ATOMICITY',obligationIds,[requirementId],['HIDDEN_CONJUNCTION_AND_INDEPENDENT_TRUTH_CONDITION_CHALLENGE'],semanticRecords);}
  const byHash=new Map();for(const target of targets)if(!byHash.has(target.targetHash))byHash.set(target.targetHash,target);return[...byHash.values()].sort((a,b)=>h.compareUnicodeScalarSequence(`${a.challengeKind}:${a.targetHash}`,`${b.challengeKind}:${b.targetHash}`));}
function requirementCompilationChallengeTargets(p){return requirementChallengeIndex(p).targets;}
function requirementCompilationChallengeBatches(p,providedTargets=null){const targets=providedTargets||requirementCompilationChallengeTargets(p),batches=[];for(const challengeKind of ['DISPOSITION','ATOMICITY']){const members=targets.filter(target=>target.challengeKind===challengeKind).sort((a,b)=>h.compareUnicodeScalarSequence(a.targetHash,b.targetHash));if(!members.length)continue;const batchContent={version:'closed-loop-requirement-challenge-batch/1',challengeKind,scope:stageSemanticScope(p,4),authorRawResponseId:String(members[0].authorRawResponseId||''),authorPromptId:String(members[0].authorPromptId||''),authorContextId:String(members[0].authorContextId||''),targets:members.map(target=>({targetHash:target.targetHash,targetObligationIds:target.targetObligationIds,targetRequirementIds:target.targetRequirementIds,reasons:target.reasons}))},batchHash=h.sha256Value(batchContent);batches.push({...batchContent,batchHash,targetCount:members.length});}return batches;}
function requirementChallengeAuthority(p,targets=null,batches=null){const currentTargets=targets||requirementCompilationChallengeTargets(p),currentBatches=batches||requirementCompilationChallengeBatches(p,currentTargets);return{targets:currentTargets,batches:currentBatches,author:stage4CompilationAuthorBinding(p),rawById:new Map(safe(p.projectData.rawResponses).filter(record=>!record.invalidatedBy).map(record=>[String(record.rawResponseId),record])),promptById:new Map(safe(p.projectData.generatedPrompts).filter(record=>!record.invalidatedBy).map(record=>[String(record.instructionId||record.promptId),record])),reservationById:new Map(stageScopedRows(p,'operationReservations',4).map(record=>[rid(record,'operationReservations'),record])),contextById:new Map(stageScopedRows(p,'freshContexts',4).map(record=>[rid(record,'freshContexts'),record])),evidenceIds:new Set(e0.records(p,'evidenceRecords').filter(active).map(record=>rid(record,'evidenceRecords')))};}
function requirementCompilationChallengeReviewAssessment(p,review,providedAuthority=null){
  const authority=providedAuthority||requirementChallengeAuthority(p),reviewId=rid(review,'requirementCompilationChallengeReviews'),change=acceptedDataChange(p,review,'requirementCompilationChallengeReviews'),rawResponseId=String(review?.rawResponseId||change?.rawResponseId||''),raw=authority.rawById.get(rawResponseId),promptId=String(change?.promptId||raw?.promptInstructionId||''),prompt=authority.promptById.get(promptId),reservationId=String(fv(review,'OPERATION_RESERVATION_ID')||prompt?.operationBinding?.operationReservationId||''),reservation=authority.reservationById.get(reservationId),challengeKind=up(fv(review,'CHALLENGE_KIND')),manifestBatch=prompt?.contextManifest?.requirementCompilationChallengeBatch||{},targetHash=String(fv(review,'TARGET_HASH')||''),batchHash=String(fv(review,'CHALLENGE_BATCH_HASH')||manifestBatch.batchHash||''),batch=authority.batches.find(item=>item.challengeKind===challengeKind&&item.batchHash===batchHash),target=batch?.targets.find(item=>item.targetHash===targetHash)||null,author=authority.author,reviewerContextId=String(raw?.contextId||prompt?.scope?.contextId||''),reviewerContext=authority.contextById.get(reviewerContextId),reviewerExternal=String(fv(reviewerContext,'EXTERNAL_CONTEXT_IDENTIFIER')||''),slot=String(fv(reservation,'TARGET_SLOT')||prompt?.operationBinding?.targetSlot||''),expectedSlot=`REQUIREMENT_CHALLENGE_BATCH:${challengeKind}:${batchHash}`,reasons=[];
  if(!change||!['DISPOSITION_CHALLENGE','ATOMICITY_CHALLENGE'].includes(up(change.operation))||up(change.operation)!==`${challengeKind}_CHALLENGE`)reasons.push('Requirement challenge is not a current accepted matching Stage 4 challenge response.');
  if(!prompt||up(prompt.operation)!==`${challengeKind}_CHALLENGE`)reasons.push('Requirement challenge prompt is missing, stale, or wrong.');
  if(!reservation||up(fv(reservation,'STATUS'))!=='ACCEPTED')reasons.push('Requirement challenge lacks one accepted current operation reservation.');
  if(slot!==expectedSlot)reasons.push('Requirement challenge reservation targets a stale or wrong challenge batch.');
  if(!batch||!target)reasons.push('Requirement challenge batch or individual target no longer resolves to the exact current compilation.');
  if(!author)reasons.push('Current Stage 4 compiler output lacks exact nonplaceholder author provenance.');
  if(!rawResponseId||rawResponseId===author?.rawResponseId||promptId===author?.promptId)reasons.push('Stage 4 compiler cannot self-review its disposition or atomicity output.');
  if(!reviewerContext||!independentReviewerContext(reviewerContext)||placeholderContext(reviewerContextId)||placeholderContext(reviewerExternal)||reviewerContextId===author?.contextId||reviewerExternal===author?.externalContextId)reasons.push('Stage 4 challenge reviewer context is missing, placeholder, stale, contaminated, not registered as an independent reviewer, or reused from the compiler.');
  const exactIds=Boolean(target&&h.sha256Value(list(fv(review,'TARGET_OBLIGATION_IDS')).sort(h.compareUnicodeScalarSequence))===h.sha256Value(target.targetObligationIds)&&h.sha256Value(list(fv(review,'TARGET_REQUIREMENT_IDS')).sort(h.compareUnicodeScalarSequence))===h.sha256Value(target.targetRequirementIds)),manifestTargets=safe(manifestBatch.targets),exactManifest=Boolean(batch&&String(manifestBatch.batchHash||'')===batch.batchHash&&up(manifestBatch.challengeKind)===batch.challengeKind&&String(manifestBatch.authorRawResponseId||'')===String(author?.rawResponseId||'')&&String(manifestBatch.authorPromptId||'')===String(author?.promptId||'')&&String(manifestBatch.authorContextId||'')===String(author?.contextId||'')&&h.sha256Value(manifestTargets.map(item=>({targetHash:String(item.targetHash||''),targetObligationIds:list(item.targetObligationIds).sort(h.compareUnicodeScalarSequence),targetRequirementIds:list(item.targetRequirementIds).sort(h.compareUnicodeScalarSequence)})).sort((a,b)=>h.compareUnicodeScalarSequence(a.targetHash,b.targetHash)))===h.sha256Value(batch.targets.map(item=>({targetHash:item.targetHash,targetObligationIds:item.targetObligationIds,targetRequirementIds:item.targetRequirementIds})).sort((a,b)=>h.compareUnicodeScalarSequence(a.targetHash,b.targetHash))));
  if(!exactIds||!exactManifest)reasons.push('Stage 4 challenge does not bind the exact current batch, per-item target identities, hashes, and author provenance.');
  const isolation=prompt?.contextManifest?.requirementCompilationChallengeIsolation||{},isolated=up(isolation.applicationInputIsolation)==='APPLICATION_ESTABLISHED'&&exactManifest&&!safe(prompt?.contextManifest?.readCollections?.requirementCompilationChallengeReviews).length;
  if(!isolated)reasons.push('Application batch-only input isolation is not established for the Stage 4 challenge.');
  const evidenceIds=[...new Set([...safe(review?.evidenceRefs).map(String),...list(fv(review,'SUPPORTING_EVIDENCE_IDS'))])],evidenceReady=evidenceIds.length>0&&evidenceIds.every(id=>authority.evidenceIds.has(id));
  if(!evidenceReady)reasons.push('Stage 4 challenge lacks current canonical evidence.');
  const finding=up(fv(review,'FINDING'));if(!['ACCEPTED','CORRECTION_REQUIRED','UNKNOWN'].includes(finding))reasons.push('Stage 4 challenge finding is invalid.');if(!String(fv(review,'REASONING')||'').trim())reasons.push('Stage 4 challenge reasoning is empty.');
  const stale=Boolean((batchHash&&!batch)||(targetHash&&!target)),current=reasons.length===0;return{reviewId,current,status:current?'ACCEPTED':stale?'STALE':'BLOCKED',finding,challengeKind,batch,batchHash,target,targetHash,author,change,rawResponseId,promptId,reviewerContextId,reservationId,packageId:String(fv(reservation,'PACKAGE_ID')||prompt?.operationBinding?.packageId||''),applicationInputIsolation:isolated?'APPLICATION_ESTABLISHED':'VIOLATED',reviewerContextDistinct:Boolean(author&&reviewerContext&&reviewerContextId!==author.contextId&&reviewerExternal!==author.externalContextId),rawResponseDistinct:Boolean(rawResponseId&&author?.rawResponseId&&rawResponseId!==author.rawResponseId),evidenceIds,reasons:[...new Set(reasons)]};
}
function buildRequirementChallengeIndex(p){ensure(p);const manifest=e0.obligationManifest(p),targets=computeRequirementCompilationChallengeTargets(p,manifest),batches=requirementCompilationChallengeBatches(p,targets),authority=requirementChallengeAuthority(p,targets,batches),reviews=stageScopedRows(p,'requirementCompilationChallengeReviews',4),assessments=reviews.map(review=>requirementCompilationChallengeReviewAssessment(p,review,authority)),currentAssessmentsByTarget=new Map(),targetByRequirement=new Map(),reviewById=new Map(reviews.map(review=>[rid(review,'requirementCompilationChallengeReviews'),review])),requirementById=new Map(stageScopedRows(p,'requirements',4).map(requirement=>[rid(requirement,'requirements'),requirement])),manifestById=new Map(safe(manifest.items).map(item=>[String(item.obligationId),item])),sourceNormativeClassByRequirement=new Map();for(const requirement of requirementById.values())sourceNormativeClassByRequirement.set(rid(requirement,'requirements'),sourceNormativeClassFromManifest(requirement,manifestById));for(const target of targets)for(const requirementId of target.targetRequirementIds)if(!targetByRequirement.has(`${target.challengeKind}:${requirementId}`))targetByRequirement.set(`${target.challengeKind}:${requirementId}`,target);for(const assessment of assessments.filter(item=>item.current)){const key=`${assessment.challengeKind}:${assessment.targetHash}`,rows=currentAssessmentsByTarget.get(key)||[];rows.push(assessment);currentAssessmentsByTarget.set(key,rows);}const index={manifest,targets,batches,authority,reviews,assessments,currentAssessmentsByTarget,targetByRequirement,reviewById,requirementById,sourceNormativeClassByRequirement};requirementChallengeIndexCache.set(p,index);return index;}
function requirementChallengeIndex(p,{rebuild=false}={}){return!rebuild&&requirementChallengeIndexCache.get(p)||buildRequirementChallengeIndex(p);}
function refreshRequirementCompilationChallengeReviews(p){ensure(p);const index=requirementChallengeIndex(p,{rebuild:true}),{targets,authority}=index,currentScope=stageSemanticScope(p,4),currentScopeHash=h.sha256Value(currentScope);for(const review of index.reviews){const assessment=requirementCompilationChallengeReviewAssessment(p,review,authority);for(const[name,value]of Object.entries({TARGET_OBLIGATION_IDS:assessment.target?.targetObligationIds||list(fv(review,'TARGET_OBLIGATION_IDS')),TARGET_REQUIREMENT_IDS:assessment.target?.targetRequirementIds||list(fv(review,'TARGET_REQUIREMENT_IDS')),TARGET_HASH:assessment.targetHash,CHALLENGE_BATCH_HASH:assessment.batchHash,AUTHOR_RAW_RESPONSE_ID:assessment.author?.rawResponseId||'',AUTHOR_CONTEXT_ID:assessment.author?.contextId||'',AUTHOR_PROMPT_ID:assessment.author?.promptId||'',REVIEWER_RAW_RESPONSE_ID:assessment.rawResponseId,REVIEWER_CONTEXT_ID:assessment.reviewerContextId,REVIEWER_PROMPT_ID:assessment.promptId,OPERATION_RESERVATION_ID:assessment.reservationId,PACKAGE_ID:assessment.packageId,APPLICATION_INPUT_ISOLATION:assessment.applicationInputIsolation,REVIEWER_CONTEXT_DISTINCT:assessment.reviewerContextDistinct,RAW_RESPONSE_DISTINCT:assessment.rawResponseDistinct,CURRENT_SCOPE:currentScope,CURRENT_SCOPE_HASH:currentScopeHash,ACCEPTED_STATUS:assessment.status,EPISTEMIC_BASIS:assessment.current?'EXTERNALLY_SUPPORTED':'NONE',FRESHNESS_STATUS:assessment.status==='STALE'?'EXPIRED':assessment.current?'CURRENT':'UNKNOWN',INDEPENDENCE_REASONS:assessment.reasons,SUPPORTING_EVIDENCE_IDS:assessment.evidenceIds}))set(review,'requirementCompilationChallengeReviews',name,value);}return targets;}
function requirementCompilationChallengeStatus(p){
  ensure(p);const index=requirementChallengeIndex(p),author=index.authority.author,{targets,batches,assessments}=index,byTarget=new Map();
  for(const assessment of assessments.filter(item=>item.current)){const key=`${assessment.challengeKind}:${assessment.targetHash}`,rows=byTarget.get(key)||[];rows.push(assessment);byTarget.set(key,rows);}
  const pending=[],accepted=[],correctionRequired=[],unknown=[],duplicates=[];
  for(const target of targets){const key=`${target.challengeKind}:${target.targetHash}`,rows=byTarget.get(key)||[];if(rows.length!==1){pending.push(target);if(rows.length>1)duplicates.push(target.targetHash);continue;}const row=rows[0];if(row.finding==='ACCEPTED')accepted.push(row);else if(row.finding==='CORRECTION_REQUIRED')correctionRequired.push(row);else unknown.push(row);}
  const pendingHashes=new Set(pending.map(target=>target.targetHash)),duplicateHashes=new Set(duplicates),pendingBatches=batches.filter(batch=>batch.targets.some(target=>pendingHashes.has(target.targetHash)||duplicateHashes.has(target.targetHash))),reviewed=[...accepted,...correctionRequired,...unknown],challengeSetHash=reviewed.length===targets.length&&!pending.length?h.sha256Value(reviewed.map(item=>({challengeKind:item.challengeKind,batchHash:item.batchHash,targetHash:item.targetHash,reviewId:item.reviewId,finding:item.finding,rawResponseId:item.rawResponseId,promptId:item.promptId,contextId:item.reviewerContextId})).sort((a,b)=>h.compareUnicodeScalarSequence(`${a.challengeKind}:${a.targetHash}`,`${b.challengeKind}:${b.targetHash}`))):'',reconciliationRequired=correctionRequired.length>0||unknown.length>0,reconciliation=reconciliationRequired&&challengeSetHash?requirementCompilationReconciliationAssessment(p,{challengeSetHash,reviewed}):null,reasons=[];
  if(!author&&stageScopedRows(p,'requirements',4).length)reasons.push('Current Stage 4 compilation lacks a nonplaceholder accepted author context.');
  if(pending.length)reasons.push(`${pending.length} required Stage 4 per-item disposition or atomicity determination(s) lack exactly one current independently bound batched review.`);
  if(duplicates.length)reasons.push(`Duplicate current challenge determinations exist for ${duplicates.join(', ')}.`);
  if(reconciliationRequired&&!reconciliation?.current)reasons.push(...(reconciliation?.reasons?.length?reconciliation.reasons:['Stage 4 correction-required or unknown challenge findings have not been reconciled.']));
  const complete=Boolean(author&&targets.length&&!pending.length&&!duplicates.length&&!reconciliationRequired);return{required:Boolean(author&&targets.length),complete,status:complete?'COMPLETE':targets.length?'BLOCKED':'NOT_REQUIRED',author,targets,batches,pendingBatches,pending,accepted,correctionRequired,unknown,duplicates,challengeSetHash,reconciliationRequired,reconciliation,reasons:[...new Set(reasons)]};
}
function requirementCompilationReconciliationAssessment(p,state=null){const status=state||requirementCompilationChallengeStatus(p),binding=acceptedStageOperationBinding(p,4,'RECONCILE_REQUIREMENTS'),targetSlot=`RECONCILE_REQUIREMENTS:${status.challengeSetHash||'MISSING'}`,reservationBinding=acceptedReservationBinding(p,binding,4,'RECONCILE_REQUIREMENTS',targetSlot),manifestTarget=binding?.prompt?.contextManifest?.requirementCompilationReconciliationTarget||{},reasons=[];if(!status.challengeSetHash)reasons.push('Requirement reconciliation lacks a complete current challenge set.');if(!binding)reasons.push('Stage 4 RECONCILE_REQUIREMENTS response is missing, stale, or lacks current context provenance.');reasons.push(...reservationBinding.reasons);if(String(manifestTarget.challengeSetHash||'')!==String(status.challengeSetHash||''))reasons.push('Stage 4 reconciliation prompt is not bound to the complete current challenge-set hash.');const maxReviewSequence=Math.max(0,...safe(status.reviewed).map(item=>Number(item?.change?.eventSequence||0)));if(binding&&maxReviewSequence&&Number(binding.change.eventSequence||0)<=maxReviewSequence)reasons.push('Stage 4 reconciliation does not follow the accepted challenge set.');const current=reasons.length===0;return{current,status:current?'ACCEPTED':binding?'STALE':'BLOCKED',binding,reservationId:reservationBinding.reservationId,challengeSetHash:status.challengeSetHash||'',reasons:[...new Set(reasons)]};}
function prepareRequirementCompilationChallengeReservation(p,options={}){
  ensure(p);const challengeKind=up(options.challengeKind),batch=requirementCompilationChallengeBatches(p).find(item=>item.challengeKind===challengeKind),author=stage4CompilationAuthorBinding(p);if(!batch||!author)throw new Error('Stage 4 challenge batch is missing, stale, or lacks a current compiler-author binding.');if(options.batchHash&&String(options.batchHash)!==batch.batchHash)throw new Error('Stage 4 challenge batch hash is stale or wrong.');if(options.targetHash&&!batch.targets.some(target=>target.targetHash===String(options.targetHash)))throw new Error('Requested Stage 4 item is not a member of the current challenge batch.');
  const reviewerContextId=String(options.reviewerContextId||''),reviewer=currentFreshContext(p,reviewerContextId,4),reviewerExternal=String(fv(reviewer,'EXTERNAL_CONTEXT_IDENTIFIER')||'');if(!reviewer||!independentReviewerContext(reviewer)||placeholderContext(reviewerContextId)||placeholderContext(reviewerExternal))throw new Error('Stage 4 challenge requires one current registered nonplaceholder independent-reviewer context.');if(reviewerContextId===author.contextId||reviewerExternal===author.externalContextId)throw new Error('Stage 4 challenge reviewer must use a context distinct from the compiler author.');
  const operation=`${challengeKind}_CHALLENGE`,targetSlot=`REQUIREMENT_CHALLENGE_BATCH:${challengeKind}:${batch.batchHash}`,payload={challengeKind,batchHash:batch.batchHash,targets:batch.targets.map(target=>({targetHash:target.targetHash,targetObligationIds:target.targetObligationIds,targetRequirementIds:target.targetRequirementIds,reasons:target.reasons})),authorRawResponseId:author.rawResponseId,authorPromptId:author.promptId,authorContextId:author.contextId,reviewerContextId};return prepareReservation(p,{stage:4,operation,targetSlot,scope:{...stageSemanticScope(p,4),runId:null,contextId:reviewerContextId},payload,packageId:options.packageId,promptId:options.promptId,owningTabInstance:options.owningTabInstance});
}
function prepareRequirementReconciliationReservation(p,options={}){ensure(p);const status=requirementCompilationChallengeStatus(p);if(!status.challengeSetHash||!status.reconciliationRequired)throw new Error('Stage 4 reconciliation requires a complete current challenge set with a correction-required or unknown finding.');const contextId=String(options.contextId||options.reconcilerContextId||''),context=currentFreshContext(p,contextId,4),external=String(fv(context,'EXTERNAL_CONTEXT_IDENTIFIER')||'');if(!context||placeholderContext(contextId)||placeholderContext(external))throw new Error('Stage 4 reconciliation requires one current registered nonplaceholder context.');const targetSlot=`RECONCILE_REQUIREMENTS:${status.challengeSetHash}`,payload={challengeSetHash:status.challengeSetHash,reviewIds:[...status.accepted,...status.correctionRequired,...status.unknown].map(item=>item.reviewId).sort(h.compareUnicodeScalarSequence),targetHashes:status.targets.map(item=>item.targetHash).sort(h.compareUnicodeScalarSequence),reconcilerContextId:contextId};return prepareReservation(p,{stage:4,operation:'RECONCILE_REQUIREMENTS',targetSlot,scope:{...scope(p),runId:null,contextId},payload,packageId:options.packageId,promptId:options.promptId,owningTabInstance:options.owningTabInstance});}
function sourceSearchContractProjection(p){
  ensure(p);const stageData=p.stages?.[2]?.agentData||{},searchFields={};for(const name of SOURCE_SEARCH_CONTRACT_FIELDS){const value=stageData[name]??p.stages?.[2]?.derivedData?.[name]??null;searchFields[name]=['SEARCH_LOCATIONS','SEARCH_QUERIES_OR_STRATEGIES','SEARCH_EXECUTION_EVIDENCE'].includes(name)?list(value):value;}
  const sources=stageScopedRows(p,'sources',2).map(record=>({sourceId:rid(record,'sources'),recordSha256:String(record.recordSha256||record.sha256||''),disposition:String(fv(record,'CONTROLLING_STATE')||fv(record,'INSPECTION_STATUS')||fv(record,'STATUS')||'')})).sort((a,b)=>h.compareUnicodeScalarSequence(a.sourceId,b.sourceId)),scopeValue=stageSemanticScope(p,2),content={version:'closed-loop-source-search-contract/1',scope:scopeValue,searchFields,sources},contentHash=h.sha256Value(content),sourceSearchContractId=String(p.stages?.[2]?.derivedData?.SOURCE_SEARCH_CONTRACT_ID||`SOURCE-SEARCH-${contentHash.slice(0,20).toUpperCase()}`),sourceSearchContractHash=h.sha256Value({version:'closed-loop-source-search-contract/1',sourceSearchContractId,...content});return{sourceSearchContractId,sourceSearchContractHash,searchFields,sources,scope:scopeValue,contentHash};
}
function sourceSearchAdequacyReviewAssessment(p,review){
  const projection=sourceSearchContractProjection(p),reviewId=rid(review,'sourceSearchAdequacyReviews'),change=acceptedDataChange(p,review,'sourceSearchAdequacyReviews'),rawResponseId=String(review?.rawResponseId||change?.rawResponseId||''),raw=safe(p.projectData.rawResponses).find(record=>record.rawResponseId===rawResponseId&&!record.invalidatedBy),promptId=String(change?.promptId||raw?.promptInstructionId||''),prompt=safe(p.projectData.generatedPrompts).find(record=>String(record.instructionId||record.promptId)===promptId&&!record.invalidatedBy),reservationId=String(fv(review,'OPERATION_RESERVATION_ID')||prompt?.operationBinding?.operationReservationId||''),reservation=stageScopedRows(p,'operationReservations',2).find(record=>rid(record,'operationReservations')===reservationId),author=acceptedStageOperationBinding(p,2,'COMPLETE'),reviewerContextId=String(raw?.contextId||prompt?.scope?.contextId||''),reviewerContext=currentFreshContext(p,reviewerContextId,2),reviewerExternalContextId=String(fv(reviewerContext,'EXTERNAL_CONTEXT_IDENTIFIER')||''),slot=String(fv(reservation,'TARGET_SLOT')||prompt?.operationBinding?.targetSlot||''),manifestTarget=prompt?.contextManifest?.sourceSearchAdequacyTarget||{},manifestIsolation=prompt?.contextManifest?.sourceSearchAdequacyIsolation||{},reasons=[];
  if(!change||up(change.operation)!=='SEARCH_ADEQUACY_REVIEW')reasons.push('Review is not a current accepted Stage 2 SEARCH_ADEQUACY_REVIEW response.');if(!prompt||up(prompt.operation)!=='SEARCH_ADEQUACY_REVIEW')reasons.push('Search-adequacy review prompt is missing, stale, or wrong.');if(!reservation||up(fv(reservation,'STATUS'))!=='ACCEPTED')reasons.push('Search-adequacy review lacks one accepted current operation reservation.');if(slot!==`SEARCH_ADEQUACY_REVIEW:${projection.sourceSearchContractId}`)reasons.push('Search-adequacy reservation targets the wrong source-search contract.');if(String(fv(review,'SOURCE_SEARCH_CONTRACT_ID')||manifestTarget.sourceSearchContractId||'')!==projection.sourceSearchContractId||String(fv(review,'SOURCE_SEARCH_CONTRACT_HASH')||manifestTarget.sourceSearchContractHash||'')!==projection.sourceSearchContractHash)reasons.push('Reviewed source-search contract identity or hash is missing, wrong, or stale.');if(!author)reasons.push('Current Stage 2 source-search author response lacks an exact nonplaceholder context binding.');if(!rawResponseId||rawResponseId===author?.rawResponseId)reasons.push('Source-search author cannot self-review the bounded search contract.');if(!reviewerContext||!independentReviewerContext(reviewerContext)||placeholderContext(reviewerContextId)||placeholderContext(reviewerExternalContextId)||reviewerContextId===author?.contextId||reviewerExternalContextId===author?.externalContextId)reasons.push('Search-adequacy reviewer context is missing, placeholder, stale, contaminated, not registered as an independent reviewer, or reused from the author.');const applicationInputIsolation=Boolean(up(manifestIsolation.applicationInputIsolation)==='APPLICATION_ESTABLISHED'&&String(manifestTarget.sourceSearchContractId||'')===projection.sourceSearchContractId&&String(manifestTarget.sourceSearchContractHash||'')===projection.sourceSearchContractHash&&String(manifestTarget.authorRawResponseId||'')===author?.rawResponseId&&String(manifestTarget.authorPromptId||'')===author?.promptId&&String(manifestTarget.authorContextId||'')===author?.contextId&&!safe(prompt?.contextManifest?.readCollections?.sourceSearchAdequacyReviews).length);if(!applicationInputIsolation)reasons.push('Application-generated target-only search-adequacy input isolation is not established.');const evidenceIds=[...new Set([...safe(review?.evidenceRefs).map(String),...list(fv(review,'SUPPORTING_EVIDENCE_IDS'))])],evidenceReady=evidenceIds.length>0&&evidenceIds.every(id=>e0.records(p,'evidenceRecords').some(record=>rid(record,'evidenceRecords')===id&&active(record)));if(!evidenceReady)reasons.push('Search-adequacy review lacks current canonical evidence.');const finding=up(fv(review,'ADEQUACY_FINDING'));if(!['ADEQUATE','PARTIAL','UNKNOWN','INADEQUATE'].includes(finding))reasons.push('Search-adequacy finding is invalid.');if(!String(fv(review,'REASONING')||'').trim())reasons.push('Search-adequacy reasoning is empty.');const providedContractId=String(fv(review,'SOURCE_SEARCH_CONTRACT_ID')||manifestTarget.sourceSearchContractId||''),providedContractHash=String(fv(review,'SOURCE_SEARCH_CONTRACT_HASH')||manifestTarget.sourceSearchContractHash||''),stale=Boolean(providedContractHash&&providedContractHash!==projection.sourceSearchContractHash),current=reasons.length===0,status=current?'ACCEPTED':stale?'STALE':'BLOCKED';return{reviewId,current,status,finding,projection,providedContractId,providedContractHash,author,rawResponseId,promptId,reviewerContextId,reservationId,packageId:String(fv(reservation,'PACKAGE_ID')||prompt?.operationBinding?.packageId||''),applicationInputIsolation:applicationInputIsolation?'APPLICATION_ESTABLISHED':'VIOLATED',reviewerContextDistinct:Boolean(author&&reviewerContext&&independentReviewerContext(reviewerContext)&&reviewerContextId!==author.contextId&&reviewerExternalContextId!==author.externalContextId),rawResponseDistinct:Boolean(rawResponseId&&author?.rawResponseId&&rawResponseId!==author.rawResponseId),evidenceIds,reasons:[...new Set(reasons)]};
}
function refreshSourceSearchAdequacyReviews(p){
  const projection=sourceSearchContractProjection(p);p.stages[2].derivedData=p.stages[2].derivedData&&typeof p.stages[2].derivedData==='object'?p.stages[2].derivedData:{};p.stages[2].derivedData.SOURCE_SEARCH_CONTRACT_ID=projection.sourceSearchContractId;p.stages[2].derivedData.SOURCE_SEARCH_CONTRACT_HASH=projection.sourceSearchContractHash;
  if(!RS.sourceSearchAdequacyReviews)return projection;for(const review of stageScopedRows(p,'sourceSearchAdequacyReviews',2)){const assessment=sourceSearchAdequacyReviewAssessment(p,review);for(const[fieldName,value]of Object.entries({SOURCE_SEARCH_CONTRACT_ID:assessment.providedContractId||assessment.projection.sourceSearchContractId,SOURCE_SEARCH_CONTRACT_HASH:assessment.providedContractHash||assessment.projection.sourceSearchContractHash,AUTHOR_RAW_RESPONSE_ID:assessment.author?.rawResponseId||'',AUTHOR_PROMPT_ID:assessment.author?.promptId||'',AUTHOR_CONTEXT_ID:assessment.author?.contextId||'',REVIEWER_RAW_RESPONSE_ID:assessment.rawResponseId,REVIEWER_PROMPT_ID:assessment.promptId,REVIEWER_CONTEXT_ID:assessment.reviewerContextId,OPERATION_RESERVATION_ID:assessment.reservationId,PACKAGE_ID:assessment.packageId,APPLICATION_INPUT_ISOLATION:assessment.applicationInputIsolation,REVIEWER_CONTEXT_DISTINCT:assessment.reviewerContextDistinct,RAW_RESPONSE_DISTINCT:assessment.rawResponseDistinct,SUPPORTING_EVIDENCE_IDS:assessment.evidenceIds,CURRENT_SCOPE:assessment.projection.scope,CURRENT_SCOPE_HASH:h.sha256Value(assessment.projection.scope),ACCEPTED_STATUS:assessment.status,EPISTEMIC_BASIS:assessment.current?'EXTERNALLY_SUPPORTED':'NONE',FRESHNESS_STATUS:assessment.status==='STALE'?'EXPIRED':assessment.current?'CURRENT':'UNKNOWN',INDEPENDENCE_REASONS:assessment.reasons}))set(review,'sourceSearchAdequacyReviews',fieldName,value);}return projection;
}
function acceptedSourceSearchAdequacyReview(p){if(!RS.sourceSearchAdequacyReviews)return null;refreshSourceSearchAdequacyReviews(p);const accepted=stageScopedRows(p,'sourceSearchAdequacyReviews',2).filter(review=>{const assessment=sourceSearchAdequacyReviewAssessment(p,review);return assessment.current&&assessment.finding==='ADEQUATE';});return accepted.length===1?accepted[0]:null;}
function prepareSearchAdequacyReviewReservation(p,options={}){ensure(p);const projection=sourceSearchContractProjection(p),author=acceptedStageOperationBinding(p,2,'COMPLETE');if(!author)throw new Error('Search-adequacy review requires a current accepted Stage 2 COMPLETE response bound to one nonplaceholder author context.');const reviewerContextId=String(options.reviewerContextId||''),reviewerContext=currentFreshContext(p,reviewerContextId,2),reviewerExternalContextId=String(fv(reviewerContext,'EXTERNAL_CONTEXT_IDENTIFIER')||'');if(!reviewerContext||!independentReviewerContext(reviewerContext)||placeholderContext(reviewerContextId)||placeholderContext(reviewerExternalContextId))throw new Error('Search-adequacy review requires one current registered nonplaceholder independent-reviewer Stage 2 context.');if(reviewerContextId===author.contextId||reviewerExternalContextId===author.externalContextId)throw new Error('Search-adequacy reviewer must use a context distinct from the Stage 2 search author.');const targetSlot=`SEARCH_ADEQUACY_REVIEW:${projection.sourceSearchContractId}`,payload={sourceSearchContractId:projection.sourceSearchContractId,sourceSearchContractHash:projection.sourceSearchContractHash,authorRawResponseId:author.rawResponseId,authorPromptId:author.promptId,authorContextId:author.contextId,reviewerContextId};return prepareReservation(p,{stage:2,operation:'SEARCH_ADEQUACY_REVIEW',targetSlot,scope:{...scope(p),runId:null,contextId:reviewerContextId},payload,packageId:options.packageId,promptId:options.promptId,owningTabInstance:options.owningTabInstance});}
function evaluateSourceSearchContract(projectOrContract,contractOrOptions=null,maybeOptions={}){
  const p=projectOrContract?.projectData?projectOrContract:null,contract=(p?(contractOrOptions&&typeof contractOrOptions==='object'?contractOrOptions:(p.stages?.[2]?.agentData||{})):projectOrContract)||{},options=p?maybeOptions:(contractOrOptions||{}),read=(...names)=>{for(const name of names){const value=contract[name];if(value!==undefined&&value!==null&&value!=='')return value;}return null;},universe=read('SEARCH_UNIVERSE','searchUniverse'),procedure=read('SEARCH_PROCEDURE','searchProcedure'),locations=list(read('SEARCH_LOCATIONS','searchLocations')),strategies=list(read('SEARCH_QUERIES_OR_STRATEGIES','searchQueriesOrStrategies')),cutoff=read('SEARCH_CUTOFF','searchCutoff'),limitations=read('SEARCH_LIMITATIONS','searchLimitations'),evidenceIds=list(read('SEARCH_EXECUTION_EVIDENCE','searchExecutionEvidence')),risk=up(read('DISCOVERY_RISK','discoveryRisk')||'UNKNOWN'),noApplicable=up(read('SOURCE_APPLICABILITY_DETERMINATION','sourceApplicabilityDetermination')||options.sourceApplicabilityDetermination)==='NO_APPLICABLE_EXTERNAL_SOURCE',reasons=[];
  if(!universe||!procedure||!locations.length||!strategies.length||!cutoff||limitations==null)reasons.push('Bounded source search requires universe, procedure, locations, strategies, cutoff, and limitations.');if(!evidenceIds.length)reasons.push('Bounded source search requires current execution evidence.');if(p&&evidenceIds.some(id=>!e0.records(p,'evidenceRecords').some(record=>rid(record,'evidenceRecords')===id&&active(record))))reasons.push('Source-search execution evidence is missing or stale.');
  let adequacyReview=null,adequacyAssessment=null;if(p){adequacyReview=acceptedSourceSearchAdequacyReview(p);if(adequacyReview)adequacyAssessment=sourceSearchAdequacyReviewAssessment(p,adequacyReview);p.stages[2].derivedData=p.stages[2].derivedData&&typeof p.stages[2].derivedData==='object'?p.stages[2].derivedData:{};p.stages[2].derivedData.SEARCH_ADEQUACY_REVIEW_ID=adequacyReview?rid(adequacyReview,'sourceSearchAdequacyReviews'):'';}else if(options.adequacyReview&&up(options.adequacyReview.finding||options.adequacyReview.ADEQUACY_FINDING)==='ADEQUATE'&&options.adequacyReview.applicationInputIsolation==='APPLICATION_ESTABLISHED'&&options.adequacyReview.reviewerContextDistinct===true)adequacyReview=options.adequacyReview;
  if(noApplicable&&!adequacyReview)reasons.push('NO_APPLICABLE_EXTERNAL_SOURCE requires exactly one current independently bound ADEQUATE search-adequacy review.');if(noApplicable&&adequacyAssessment&&!adequacyAssessment.current)reasons.push(...adequacyAssessment.reasons);if(noApplicable&&['MATERIAL','UNKNOWN','UNRESOLVED','HIGH'].includes(risk))reasons.push('Material or unknown source-discovery risk blocks NO_APPLICABLE_EXTERNAL_SOURCE.');const candidates=p?old(p,'sources'):safe(options.candidateSources),undisposed=candidates.filter(source=>!String(fv(source,'CONTROLLING_STATE')||fv(source,'INSPECTION_STATUS')||fv(source,'STATUS')||'').trim());if(noApplicable&&undisposed.length)reasons.push('Every discovered candidate source requires a current disposition.');const adequacyReviewId=p&&adequacyReview?rid(adequacyReview,'sourceSearchAdequacyReviews'):String(adequacyReview?.id||adequacyReview?.reviewId||'');return{complete:reasons.length===0,status:reasons.length?'BLOCKED':'ACCEPTED',noApplicableExternalSource:noApplicable,bounded:Boolean(universe&&procedure&&locations.length&&strategies.length&&cutoff),discoveryRisk:risk,adequacyReviewId,evidenceIds,undisposedSourceIds:undisposed.map(source=>rid(source,'sources')),sourceSearchContract:p?sourceSearchContractProjection(p):null,reasons};
}
function appendCrossHashEvidence(p,options={}){ensure(p);const artifactId=String(options.artifactId||''),matches=old(p,'artifacts').filter(record=>rid(record,'artifacts')===artifactId);if(matches.length!==1)throw new Error('Cross-hash evidence requires exactly one current artifact.');const algorithm=up(options.hashAlgorithm||options.algorithm),digest=String(options.digest||'').trim(),basis=up(options.epistemicBasis||'SELF_ASSERTED'),evidenceIds=[...new Set(list(options.evidenceIds))];if(!algorithm||algorithm==='SHA-256'||!digest)throw new Error('A future cross-hash record requires a distinct explicit algorithm and digest.');if(!['SELF_ASSERTED','EXTERNALLY_SUPPORTED'].includes(basis))throw new Error('Cross-hash evidence cannot elevate an external digest claim to application-observed or verified-external basis.');if(evidenceIds.some(id=>!e0.records(p,'evidenceRecords').some(record=>rid(record,'evidenceRecords')===id&&active(record))))throw new Error('Cross-hash evidence references missing or stale evidence.');const payload={artifactId,originalSha256:String(fv(matches[0],'SHA256')||''),hashAlgorithm:algorithm,digest,epistemicBasis:basis,evidenceIds,scope:semanticScope(p)};return idem(p,{commandType:'APPEND_CROSS_HASH_EVIDENCE',target:`${artifactId}:${algorithm}`,expectedRevision:options.expectedRevision,idempotencyKey:options.idempotencyKey,commandId:options.commandId,payload,execute:()=>put(p,'evidenceRecords',{APPLICATION_EVIDENCE_KIND:'CROSS_HASH_EXTERNAL_CLAIM',APPLICATION_EVIDENCE_DESCRIPTION:`${algorithm} digest claim for current artifact ${artifactId}; historical SHA-256 remains unchanged.`,APPLICATION_EVIDENCE_CONTENT:h.stableStringify(payload),KIND:'CROSS_HASH_EXTERNAL_CLAIM',DESCRIPTION:`Append-only ${algorithm} cross-hash claim for ${artifactId}.`,AUTHORITY_TYPE:basis,LOCATION:`artifact:${artifactId}`,CONTENT:h.stableStringify({algorithm,digest,originalSha256:payload.originalSha256,evidenceIds}),ATTACHMENT_ID:artifactId,SHA256:payload.originalSha256,STATUS:'PRESERVED',OBSERVATION_ORIGIN:'EXTERNAL_CLAIM',EPISTEMIC_BASIS:basis,CURRENT_SCOPE_STATUS:'CURRENT',FRESHNESS_STATUS:'CURRENT',CONTRADICTION_STATUS:'CLEAR',REASONS:['Historical SHA-256 identity was not rewritten.'],DERIVATION_OR_REVIEW_IDS:evidenceIds},{stage:Number(p.activeStage||0),source:'APPLICATION_NORMALIZED_EXTERNAL_CLAIM',scope:semanticScope(p)})});}
const OBJECTIVE_ATTESTATION_SCHEMA='closed-loop-objective-attestation/1',OBJECTIVE_ATTESTATION_CONTRACT_SCHEMA='closed-loop-objective-attestation-contract/1';
function exactClosedKeys(value,required,optional=[]){if(!value||typeof value!=='object'||Array.isArray(value))return false;const allowed=new Set([...required,...optional]),keys=Object.keys(value);return required.every(key=>Object.prototype.hasOwnProperty.call(value,key))&&keys.every(key=>allowed.has(key));}
function decodeBase64Url(value){if(value instanceof Uint8Array)return new Uint8Array(value);if(value instanceof ArrayBuffer)return new Uint8Array(value);const text=String(value||'').trim();if(!/^[A-Za-z0-9_-]+={0,2}$/.test(text))throw new Error('Attestation signature is not canonical base64url.');const normalized=text.replace(/-/g,'+').replace(/_/g,'/').replace(/=+$/,'')+'='.repeat((4-text.replace(/=+$/,'').length%4)%4);let binary;if(typeof atob==='function')binary=atob(normalized);else throw new Error('Base64url decoding is unavailable.');return Uint8Array.from(binary,character=>character.charCodeAt(0));}
async function verifyObjectiveExternalAttestation(p,options={}){
  ensure(p);const subtle=globalThis.crypto?.subtle;if(!subtle)throw new Error('WebCrypto verification is unavailable; external observation cannot be elevated.');const observationId=String(options.observationId||''),observationMatches=scoped(p,'observationRecords').filter(record=>rid(record,'observationRecords')===observationId);if(observationMatches.length!==1)throw new Error('Objective attestation requires exactly one current external-claim observation.');const observation=observationMatches[0],origin=up(fv(observation,'OBSERVATION_ORIGIN'));if(!['EXTERNAL_CLAIM','VERIFIED_EXTERNAL_OBSERVATION'].includes(origin))throw new Error('Only an external claim can be normalized through objective external attestation.');const contract=options.contract,payload=options.payload;
  if(!exactClosedKeys(contract,['schema','contractId','algorithm','publicKeyJwk','authorizedAttesterId'],['allowedSubjectIds','validFrom','validUntil']))throw new Error('Objective-attestation contract has an unknown or missing property.');if(contract.schema!==OBJECTIVE_ATTESTATION_CONTRACT_SCHEMA||contract.algorithm!=='ECDSA_P256_SHA256')throw new Error('Objective-attestation contract schema or algorithm is unsupported.');const jwk=contract.publicKeyJwk;if(!exactClosedKeys(jwk,['kty','crv','x','y'],['use','key_ops','alg','ext'])||jwk.kty!=='EC'||jwk.crv!=='P-256'||jwk.d)throw new Error('Objective-attestation public key must be a public P-256 JWK.');
  const payloadKeys=['schema','contractId','jobId','observationId','subjectId','targetPropositionId','relation','observationValueSha256','inputBindingsSha256','evidenceSetSha256','publicKeySha256','attesterId','issuedAt','validUntil','nonce'];if(!exactClosedKeys(payload,payloadKeys))throw new Error('Objective-attestation payload has an unknown or missing property.');if(payload.schema!==OBJECTIVE_ATTESTATION_SCHEMA||String(payload.contractId)!==String(contract.contractId)||String(payload.jobId)!==String(p.job?.JOB_ID||'')||String(payload.observationId)!==observationId)throw new Error('Objective-attestation payload is bound to the wrong schema, contract, job, or observation.');const subjectId=String(fv(observation,'SUBJECT_ID')||''),targetPropositionId=String(payload.targetPropositionId||''),propositionMatches=props(p).filter(record=>rid(record,'propositions')===targetPropositionId);if(String(payload.subjectId)!==subjectId||propositionMatches.length!==1)throw new Error('Objective-attestation subject or proposition is missing, wrong, or stale.');if(safe(contract.allowedSubjectIds).length&&!safe(contract.allowedSubjectIds).map(String).includes(subjectId))throw new Error('Objective-attestation contract does not authorize this subject.');if(String(payload.attesterId)!==String(contract.authorizedAttesterId)||!['ESTABLISHES','REFUTES'].includes(up(payload.relation)))throw new Error('Objective-attestation actor or relation is not authorized by the closed contract.');
  const publicJwk={kty:'EC',crv:'P-256',x:String(jwk.x),y:String(jwk.y),ext:true,key_ops:['verify']},publicKeySha256=h.sha256Value(publicJwk),observationValueSha256=h.sha256Value(fv(observation,'EXTERNAL_OR_AGENT_OBSERVED_VALUE')),inputBindingsSha256=h.sha256Value(fv(observation,'INPUT_IDENTITIES_AND_HASHES')||[]),evidenceIds=[...new Set(observationEvidenceIds(observation))].sort(h.compareUnicodeScalarSequence),evidenceSetSha256=h.sha256Value(evidenceIds);if(String(payload.publicKeySha256)!==publicKeySha256||String(payload.observationValueSha256)!==observationValueSha256||String(payload.inputBindingsSha256)!==inputBindingsSha256||String(payload.evidenceSetSha256)!==evidenceSetSha256)throw new Error('Objective-attestation payload does not bind the exact current key, observation, inputs, and evidence set.');if(evidenceIds.length===0||evidenceIds.some(id=>!e0.records(p,'evidenceRecords').some(record=>rid(record,'evidenceRecords')===id&&active(record))))throw new Error('Objective attestation requires current canonical evidence.');if(!/^[a-f0-9]{32,}$/i.test(String(payload.nonce||'')))throw new Error('Objective-attestation nonce must contain at least 128 bits.');
  const issuedAt=Date.parse(String(payload.issuedAt||'')),validUntil=Date.parse(String(payload.validUntil||'')),now=Date.now(),contractFrom=contract.validFrom?Date.parse(String(contract.validFrom)):null,contractUntil=contract.validUntil?Date.parse(String(contract.validUntil)):null;if(!Number.isFinite(issuedAt)||!Number.isFinite(validUntil)||validUntil<issuedAt||now>validUntil||(contractFrom!=null&&(!Number.isFinite(contractFrom)||issuedAt<contractFrom))||(contractUntil!=null&&(!Number.isFinite(contractUntil)||issuedAt>contractUntil)))throw new Error('Objective-attestation validity interval is invalid, expired, or outside the registered contract.');
  const encoded=new TextEncoder().encode(h.stableStringify(payload)),signature=decodeBase64Url(options.signature);if(signature.byteLength!==64)throw new Error('Objective-attestation ECDSA signature must use the 64-byte P-256 representation.');const key=await subtle.importKey('jwk',publicJwk,{name:'ECDSA',namedCurve:'P-256'},false,['verify']),verified=await subtle.verify({name:'ECDSA',hash:'SHA-256'},key,signature,encoded);if(!verified)throw new Error('Objective-attestation signature verification failed; the external claim remains unverified.');const signatureSha256=await h.sha256Bytes(signature),payloadSha256=await h.sha256Bytes(encoded),attestationId=`OBJECTIVE-ATTESTATION-${payloadSha256.slice(0,24).toUpperCase()}`,commandPayload={attestationId,observationId,targetPropositionId,relation:up(payload.relation),payloadSha256,signatureSha256,publicKeySha256,contractId:String(contract.contractId),evidenceIds};
  return idem(p,{commandType:'VERIFY_OBJECTIVE_EXTERNAL_ATTESTATION',target:observationId,expectedRevision:options.expectedRevision,idempotencyKey:options.idempotencyKey,commandId:options.commandId,payload:commandPayload,execute:()=>{set(observation,'observationRecords','OBSERVATION_ORIGIN','VERIFIED_EXTERNAL_OBSERVATION');set(observation,'observationRecords','EPISTEMIC_BASIS','VERIFIED_EXTERNAL');set(observation,'observationRecords','FRESHNESS_STATUS','CURRENT');set(observation,'observationRecords','ATTESTATION_CONTRACT_ID',String(contract.contractId));set(observation,'observationRecords','ATTESTATION_PAYLOAD_SHA256',payloadSha256);set(observation,'observationRecords','ATTESTATION_SIGNATURE_SHA256',signatureSha256);set(observation,'observationRecords','ATTESTATION_PUBLIC_KEY_SHA256',publicKeySha256);set(observation,'observationRecords','ATTESTATION_VERIFICATION_STATUS','VERIFIED');const existing=scoped(p,'entailmentReviews').find(record=>String(fv(record,'OBSERVATION_ID'))===observationId&&String(fv(record,'TARGET_PROPOSITION_ID'))===targetPropositionId&&String(fv(record,'OBJECTIVE_ATTESTATION_ID'))===attestationId),entailment=existing||put(p,'entailmentReviews',{OBSERVATION_ID:observationId,TARGET_PROPOSITION_ID:targetPropositionId,TARGET_LEAF_ID:subjectId,ACCEPTED_RELATION:up(payload.relation),ACCEPTED_STATUS:'ACCEPTED',CURRENT_SCOPE:scope(p),GATE_CONSEQUENCE:up(payload.relation),OBJECTIVE_ATTESTATION_ID:attestationId,ATTESTATION_PAYLOAD_SHA256:payloadSha256},{stage:Number(options.stage||p.activeStage||22),source:'APPLICATION_VERIFIED_EXTERNAL_ATTESTATION'});recalc(p);return{attestationId,observationId,entailmentId:rid(entailment,'entailmentReviews'),payloadSha256,signatureSha256,publicKeySha256,verified:true};}});
}
async function createRedactedArtifact(p,options={}){ensure(p);const sourceArtifactId=String(options.sourceArtifactId||''),sourceMatches=old(p,'artifacts').filter(record=>rid(record,'artifacts')===sourceArtifactId);if(sourceMatches.length!==1)throw new Error('Redaction requires exactly one current source artifact.');const source=sourceMatches[0],blob=options.blob;if(!(blob instanceof Blob))throw new TypeError('Redaction requires the exact derived artifact Blob.');const reason=String(options.redactionDescription||options.reason||'').trim(),filename=String(options.filename||'').trim();if(!reason||!filename)throw new Error('Redaction description and exact derived filename are required.');const store=globalThis.closedLoopProjectStore;if(!store?.stageAttachment||!store?.commitProjectWithStagedAttachments||!store?.getArtifact)throw new Error('The single canonical project store cannot perform atomic redaction custody.');const expectedRevision=Number(options.expectedRevision==null?p.revision:options.expectedRevision);if(expectedRevision!==Number(p.revision||0))throw new Error('Expected project revision is stale.');const sourceBytes=await store.getArtifact(sourceArtifactId);if(!sourceBytes||String(sourceBytes.jobId)!==String(p.job.JOB_ID))throw new Error('Source artifact bytes are unavailable in canonical custody.');const actualSourceBytes=new Uint8Array(await sourceBytes.blob.arrayBuffer()),actualSourceSha256=await h.sha256Bytes(actualSourceBytes);if(actualSourceBytes.byteLength!==Number(fv(source,'BYTE_SIZE'))||actualSourceSha256!==String(fv(source,'SHA256')))throw new Error('Source artifact bytes fail current canonical identity verification.');const next=e0.clone(p);ensure(next);const artifactId=String(options.artifactId||e0.allocateId(next,'artifacts')),originalClassification=up(fv(source,'DISCLOSURE_CLASSIFICATION')||sourceBytes.disclosureClassification||'UNKNOWN'),classification=up(options.disclosureClassification||originalClassification);if(classification!==originalClassification&&!String(options.classificationDecisionId||'').trim())throw new Error('Changing disclosure classification during redaction requires a current human decision identity.');const lineage={kind:'REDACTION',REDACTED_FROM:sourceArtifactId,sourceSha256:actualSourceSha256,redactionDescription:reason,classificationDecisionId:String(options.classificationDecisionId||'')},staged=await store.stageAttachment({jobId:String(p.job.JOB_ID),targetArtifactId:artifactId,blob,filename,mediaType:String(options.mediaType||blob.type||'application/octet-stream'),lineage,disclosureClassification:classification,expectedProjectRevision:expectedRevision,commandId:options.commandId,idempotencyKey:options.stagingIdempotencyKey});try{const committed=await store.commitProjectWithStagedAttachments({jobId:String(p.job.JOB_ID),expectedProjectRevision:expectedRevision,stagingIds:[String(staged.stagingId||staged.artifactId)],payload:{operation:'CREATE_REDACTED_ARTIFACT',sourceArtifactId,artifactId,reason,testedPropositionUnaffected:options.testedPropositionUnaffected===true},commandId:options.commitCommandId,idempotencyKey:options.idempotencyKey,mutator:target=>{e0.registerArtifactBytes(target,{stage:Number(options.stage||p.activeStage||1),artifactId,filename:String(staged.filename),mediaType:String(staged.mediaType),byteSize:Number(staged.byteSize),sha256:String(staged.sha256),lineage,role:'REDACTED_DERIVED_ARTIFACT'});ensure(target);const eventSequence=Number(target.projectData.eventSequence||0)+1;target.projectData.eventSequence=eventSequence;target.projectData.history=safe(target.projectData.history);target.projectData.history.push({eventId:`REDACTION-${h.sha256Value({sourceArtifactId,artifactId,sourceSha256:actualSourceSha256,redactedSha256:String(staged.sha256)}).slice(0,20).toUpperCase()}`,eventSequence,type:'ARTIFACT_REDACTION_DERIVATION',stage:Number(options.stage||p.activeStage||1),sourceArtifactId,redactedArtifactId:artifactId,sourceSha256:actualSourceSha256,redactedSha256:String(staged.sha256),redactionDescription:reason,disclosureClassification:classification,testedPropositionUnaffected:options.testedPropositionUnaffected===true,relationship:'REDACTED_FROM',deviceTimestamp:new Date().toISOString()});}});return{...committed,artifactId,sourceArtifactId,relationship:'REDACTED_FROM'};}catch(error){throw Object.assign(error,{stagedAttachmentId:String(staged.stagingId||staged.artifactId),canonicalStateChanged:false});}}
function recordHumanDisclosureAuthorization(p,options={}){
  ensure(p);if(!RS.disclosureAuthorizations)throw new Error('Disclosure-authorization schema authority is unavailable.');const expectedRevision=Number(options.expectedRevision==null?p.revision:options.expectedRevision),committedRevision=expectedRevision+1,providedIdempotencyKey=String(options.idempotencyKey||''),priorReceipt=providedIdempotencyKey?p.projectData.commandReceipts.find(receipt=>receipt.idempotencyKey===providedIdempotencyKey):null;if(priorReceipt){const priorId=String(priorReceipt.result?.disclosureAuthorizationId||'');return safe(p.projectData.disclosureAuthorizations).find(record=>rid(record,'disclosureAuthorizations')===priorId)||priorReceipt.result;}if(expectedRevision!==Number(p.revision||0))throw new Error('Expected project revision is stale.');const artifactIds=[...new Set(list(options.artifactIds))].sort(h.compareUnicodeScalarSequence);if(!artifactIds.length)throw new Error('At least one exact current artifact is required.');const artifacts=artifactIds.map(id=>{const matches=old(p,'artifacts').filter(record=>rid(record,'artifacts')===id);if(matches.length!==1)throw new Error(`Artifact ${id} is not exactly one current canonical artifact.`);return matches[0];}),classifications=artifacts.map(artifact=>up(fv(artifact,'DISCLOSURE_CLASSIFICATION')||artifact.disclosureClassification||'UNKNOWN'));if(options.classifications&&h.sha256Value(list(options.classifications).map(up))!==h.sha256Value(classifications))throw new Error('Submitted disclosure classifications do not match application-derived current artifact classifications.');const decision=up(options.decision||options.disclosureDecision),recipient=String(options.recipientOrProvider||'').trim(),suitability=up(options.recipientSuitabilityDecision||(options.recipientSuitable===true?'SUITABLE':options.recipientSuitable===false?'NOT_SUITABLE':'UNKNOWN')),purpose=String(options.purposeAndLimits||'').trim(),period=String(options.effectivePeriodDecision||'').trim(),reason=String(options.decisionReason||options.reason||'').trim();if(!['AUTHORIZE','DENY','REVOKE'].includes(decision))throw new Error('Disclosure decision must be AUTHORIZE, DENY, or REVOKE.');if(!recipient||!purpose||!period||!reason)throw new Error('Recipient/provider, purpose/limits, effective-period decision, and decision reason are required.');if(decision==='AUTHORIZE'&&suitability!=='SUITABLE')throw new Error('Disclosure authorization requires a SUITABLE recipient decision.');if(decision==='AUTHORIZE'&&classifications.includes('CREDENTIAL_SECRET'))throw new Error('CREDENTIAL_SECRET material can never be externally authorized.');const payload={artifactIds,classifications,decision,recipient,suitability,purpose,period,reason,scope:scope(p),projectRevision:committedRevision},authorizationHash=h.sha256Value(payload),existing=safe(p.projectData.disclosureAuthorizations).find(record=>String(fv(record,'AUTHORIZATION_HASH'))===authorizationHash);if(existing)return existing;const eventSequence=Number(p.projectData.eventSequence||0)+1,eventId=`EVENT-${String(eventSequence).padStart(8,'0')}`;p.projectData.eventSequence=eventSequence;p.projectData.history=safe(p.projectData.history);p.projectData.history.push({eventId,eventSequence,type:'HUMAN_DISCLOSURE_DECISION_RECORDED',stage:Number(p.activeStage||0),artifactIds,decision,recipient,expectedRevision,committedRevision,identityAssurance:'SELF_ASSERTED',deviceTimestamp:new Date().toISOString()});for(const prior of scoped(p,'disclosureAuthorizations'))if(list(fv(prior,'ARTIFACT_IDS')).some(id=>artifactIds.includes(id))){set(prior,'disclosureAuthorizations','STATUS','SUPERSEDED');prior.active=false;prior.validity='SUPERSEDED';}const status=decision==='AUTHORIZE'?'ACTIVE':decision==='DENY'?'DENIED':'REVOKED',record=put(p,'disclosureAuthorizations',{JOB_ID:String(p.job?.JOB_ID||''),ARTIFACT_IDS:artifactIds,DISCLOSURE_CLASSIFICATIONS:classifications,CURRENT_SCOPE:scope(p),PROJECT_REVISION:committedRevision,STATUS:status,AUTHORIZATION_HASH:authorizationHash,HUMAN_IDENTITY_ASSURANCE:'SELF_ASSERTED',DECISION_EVENT_ID:eventId,EVENT_SEQUENCE:eventSequence,DISCLOSURE_DECISION:decision,RECIPIENT_OR_PROVIDER:recipient,RECIPIENT_SUITABILITY_DECISION:suitability,PURPOSE_AND_LIMITS:purpose,EFFECTIVE_PERIOD_DECISION:period,DECISION_REASON:reason},{stage:Number(p.activeStage||0),source:'HUMAN_DECISION',scope:scope(p)});supersedeReservations(p,`DISCLOSURE-AUTHORIZATION-${rid(record,'disclosureAuthorizations')}`);const idempotencyKey=providedIdempotencyKey||h.sha256Value({commandType:'RECORD_DISCLOSURE_AUTHORIZATION',expectedRevision,payloadHash:authorizationHash}),commandId=String(options.commandId||`COMMAND-${nonce().toUpperCase()}`);p.projectData.commandReceipts.push({commandId,idempotencyKey,expectedRevision,committedRevision,payloadHash:authorizationHash,result:{disclosureAuthorizationId:rid(record,'disclosureAuthorizations'),authorizationHash,status},createdAt:new Date().toISOString()});return record;
}
function recordNativeResult(p,options={}){const runtimeResult=options.runtimeResult||{},workerSha=String(runtimeResult.testWorkerSha256||''),buildIdentity=String(runtimeResult.runtimeBuildIdentity||''),protocol=String(runtimeResult.workerProtocolVersion||'');if(runtimeResult.status!=='COMPLETE')throw new Error('Native deterministic execution must complete before canonical result creation.');if(!buildIdentity||!/^([a-f0-9]{64})$/i.test(workerSha)||protocol!=='closed-loop-test-worker-protocol/1')throw new Error('Native deterministic result lacks exact runtime build, worker-byte, or worker-protocol identity.');const runtimeArtifactIds=list(runtimeResult.inputArtifactIds),runtimeHashes=list(runtimeResult.inputArtifactSha256Values),provided=options.inputArtifacts||[],runtimeInputs=new Map();if(runtimeArtifactIds.length!==runtimeHashes.length||new Set(runtimeArtifactIds).size!==runtimeArtifactIds.length)throw new Error('Native deterministic result contains ambiguous input artifact identities.');for(let index=0;index<runtimeArtifactIds.length;index++)runtimeInputs.set(runtimeArtifactIds[index],runtimeHashes[index].toLowerCase());if(runtimeInputs.size!==provided.length||new Set(provided.map(artifact=>String(artifact.artifactId))).size!==provided.length||provided.some(artifact=>runtimeInputs.get(String(artifact.artifactId))!==String(artifact.sha256||'').toLowerCase()))throw new Error('Native deterministic result input identities do not match the exact canonical command inputs.');const result=e0.recordApplicationDeterministicResult(p,options),test=old(p,'tests').find(record=>rid(record,'tests')===String(options.testId)),propositionId=list(fv(test,'TARGET_PROPOSITION_IDS'))[0]||'',obligation=propositionObligation(p,propositionId),existingObservation=scoped(p,'observationRecords').find(record=>String(fv(record,'RAW_OR_NATIVE_PROVENANCE'))===rid(result,'deterministicResults')),observation=existingObservation||put(p,'observationRecords',{APPLICATION_OBSERVED_VALUE:runtimeResult.actual??runtimeResult.observations,OBSERVATION_ORIGIN:'NATIVE_APPLICATION_OBSERVATION',SUBMITTING_ACTOR_OR_RUNTIME:buildIdentity,SUBJECT_ID:String(options.testId),OBSERVED_LOCATION:'APPLICATION_TEST_RUNTIME',METHOD_OR_TOOL_IDENTITY:`${runtimeResult.runtimeVersion||runtimeResult.executorVersion||'closed-loop-test-runtime/1'}:${workerSha}`,INPUT_IDENTITIES_AND_HASHES:provided.map(artifact=>({artifactId:String(artifact.artifactId),sha256:String(artifact.sha256),byteSize:Number(artifact.byteSize)})),OUTPUT_IDENTITIES_AND_HASHES:[],EPISTEMIC_BASIS:'APPLICATION_OBSERVED',FRESHNESS_STATUS:'CURRENT',SOURCE_EVIDENCE_IDS:safe(result.evidenceRefs).map(String),CURRENT_SCOPE:scope(p),RAW_OR_NATIVE_PROVENANCE:rid(result,'deterministicResults')},{stage:22,source:'APPLICATION_TEST_RUNTIME'}),relation=up(runtimeResult.determination)==='SATISFIED'?'ESTABLISHES':up(runtimeResult.determination)==='VIOLATED'?'REFUTES':'UNKNOWN',existingEntailment=scoped(p,'entailmentReviews').find(record=>String(fv(record,'OBSERVATION_ID'))===rid(observation,'observationRecords')&&String(fv(record,'TARGET_PROPOSITION_ID'))===propositionId),entailmentRecord=existingEntailment||put(p,'entailmentReviews',{OBSERVATION_ID:rid(observation,'observationRecords'),TARGET_PROPOSITION_ID:propositionId,TARGET_LEAF_ID:String(options.testId),ACCEPTED_RELATION:relation,ACCEPTED_STATUS:'ACCEPTED',CURRENT_SCOPE:scope(p),GATE_CONSEQUENCE:relation},{stage:22,source:'APPLICATION_TEST_RUNTIME'});for(const [field,value]of Object.entries({PROPOSITION_ID:propositionId,PROOF_OBLIGATION_ID:obligation?rid(obligation,'proofObligations'):'',OBSERVATION_ORIGIN:'NATIVE_APPLICATION_OBSERVATION',OBSERVATION_ID:rid(observation,'observationRecords'),ENTAILMENT_ID:rid(entailmentRecord,'entailmentReviews'),EPISTEMIC_BASIS:'APPLICATION_OBSERVED',INPUT_BINDING_BASIS:'NATIVE_APPLICATION_BOUND',RUNTIME_BUILD_IDENTITY:buildIdentity,TEST_WORKER_SHA256:workerSha,WORKER_PROTOCOL_VERSION:protocol,PARSER_OR_ADAPTER_IDENTITIES:safe(runtimeResult.parserOrAdapterIdentities).map(String),ENVIRONMENT_MANIFEST_ID:String(runtimeResult.environmentManifestId||''),FRESHNESS_STATUS:'CURRENT'}))set(result,'deterministicResults',field,value);recalc(p);return result;}
function metrics(p){const base=e0.operationalMetrics(p);return{...base,zeroFailure95PercentUpperBound:null,ruleOfThreeStatus:'SUPPRESSED_UNLESS_DEDICATED_APPLICABILITY_EVALUATOR_PROVES_ALL_SECTION_63_6_ASSUMPTIONS'};}
const RESPONSIBLE_STAGE_MAP=Object.freeze({propositions:4,propositionEquivalenceReviews:5,applicabilityRecords:5,proofExpressions:6,proofObligations:6,semanticCoverageReviews:6,sourceSearchAdequacyReviews:2,requirementCompilationChallengeReviews:4,observationRecords:12,entailmentReviews:12,environmentDependencies:6,operationReservations:1,disclosureAuthorizations:1,backupRecords:20,deliveryRecords:30,deploymentManifests:27,requirements:4,tests:6,failureTests:7,instructions:8,runs:11,verification:12,comparisons:13,defects:14,regressions:15,changes:16,baselines:20,products:21,deterministicResults:22,meaningResults:23,adversarialResults:24,representationInspections:25,processAudits:26,productAudits:26,releaseRecords:27,artifactIdentities:28,evidenceChains:29});
function responsibleStageForMutation(family,mutation={}){if(['observationRecords','entailmentReviews','operationReservations','disclosureAuthorizations'].includes(family)){const stage=Number(mutation.stage||fv(mutation,'STAGE')||0);if(Number.isInteger(stage)&&stage>=1&&stage<=30)return stage;}return RESPONSIBLE_STAGE_MAP[family]??1;}
const engine=Object.freeze({...e0,version:'closed-loop-workflow-engine/3',__controllingCompletionAmendmentVersion:VERSION,ensureShape:ensure,gate,recalculate:recalc,invalidateDownstream:invalidate,operationalNextAction:next,operationalMetrics:metrics,releaseMetrics:completionReleaseMetrics,recordReleaseDetermination,detectCurrentContradictions:detectCompletionContradictions,evaluateProofExpression:evalExpr,evaluatePropositionState:pstate,propositionState:pstate,deriveProofObligations:obligations,proofObligationSet:obligations,deriveRequiredVerificationRelationSet:relationSet,requiredVerificationRelationSet:relationSet,semanticReviewTargetHash,semanticCoverageReviewAssessment,semanticReviewTargets,prepareSemanticReviewReservation,intakeSemanticChallengeProjection,intakeSemanticChallengeAssessment,intakeReconciliationAssessment,prepareIntakeSemanticChallengeReservation,prepareIntakeReconciliationReservation,requirementCompilationChallengeTargets,requirementCompilationChallengeBatches,requirementCompilationChallengeReviewAssessment,refreshRequirementCompilationChallengeReviews,requirementCompilationChallengeStatus,requirementCompilationReconciliationAssessment,prepareRequirementCompilationChallengeReservation,prepareRequirementReconciliationReservation,sourceSearchContractProjection,sourceSearchAdequacyReviewAssessment,acceptedSourceSearchAdequacyReview,refreshSourceSearchAdequacyReviews,prepareSearchAdequacyReviewReservation,operationReservationTarget,prepareCurrentOperationReservation,recordAtomicityReview,recordNormativeClassificationReview,recordScopeChangingException,routeUnknownSeverity,evaluateFreshnessPolicy,validateJustificationGraph,semanticChallengeStatus,evaluateSourceSearchContract,createRedactedArtifact,appendCrossHashEvidence,verifyObjectiveExternalAttestation,SEMANTIC_CHALLENGE_THRESHOLDS,LEGACY_NON_OPERATIONAL_AUDIT_COLLECTIONS,responsibleStageForMutation,RESPONSIBLE_STAGE_MAP,prepareOperationReservation:prepareReservation,reserveOperation:reserve,markOrphanedReservations:orphan,executeIdempotentCommand:idem,recordHumanDisclosureAuthorization,recordBackupRestoreVerification,backupCheckpointState,finalBackupReadiness,recordApplicationDeterministicResult:recordNativeResult,terminalPrerequisites:terminal,deriveTerminalDeliveryRecord:delivery,createDeliveryRecord:delivery,withdrawDeliveryAuthorization,registryIntegrity:registry,evidenceChainComplete:chainOK,artifactIdentityComplete:artifactOK,humanDeliveryIntent:intent});globalThis.closedLoopWorkflowEngine=engine;
})();
