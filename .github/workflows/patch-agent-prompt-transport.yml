name: Prompt transport migration retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm migration status
        run: echo 'The verified release already uses the corrected large-prompt transport. This retired migration does not modify main.'
