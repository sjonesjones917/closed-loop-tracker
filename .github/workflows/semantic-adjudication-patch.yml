name: Semantic adjudication patch (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary competing patch workflow disabled; coordinated branch workflow is authoritative.'
