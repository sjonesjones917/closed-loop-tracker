name: Apply final residual prompt recovery
on:
  push:
    branches:
      - semantic-reliability/prompt-recovery-80af
permissions:
  contents: write
jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: python3 .prompt-recovery-80af.py
      - name: Verify deterministic lifecycle and prompt semantics
        run: |
          node --check workbook.js
          node --check workflow-engine.js
          node --check prompt-engine.js
          node verify.mjs
          node verify-ingestion.mjs
          node verify-complete.mjs
          node verify-full-cycle.mjs
          node verify-prompt-semantics.mjs
      - name: Commit clean source-only result
        run: |
          rm -f .prompt-recovery-80af.py .github/workflows/apply-prompt-recovery-80af.yml
          git config user.name 'sjonesjones917'
          git config user.email 'sjones.jones917@gmail.com'
          git add -A
          git commit -m 'Close final prompt recovery contradictions'
          git push origin HEAD:semantic-reliability/prompt-recovery-80af
