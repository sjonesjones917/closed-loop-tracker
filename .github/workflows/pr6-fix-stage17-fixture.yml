name: PR6 fix Stage 17 fixture
on:
  push:
    branches: [practical-100/pr6-completion]
permissions:
  contents: write
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Remove application-owned fixture assertions
        run: |
          python .pr6-fix-stage17-fixture.py
          rm -f .pr6-fix-stage17-fixture.py .github/workflows/pr6-fix-stage17-fixture.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --check
          git commit -m 'Keep Stage 17 fixture within agent authority'
          git push
