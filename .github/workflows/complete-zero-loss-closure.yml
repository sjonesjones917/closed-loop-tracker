name: Complete zero-loss intake and obligation closure

on:
  push:
    branches:
      - fix/closed-loop-v3-final-20260830

permissions:
  contents: write

concurrency:
  group: complete-zero-loss-closure
  cancel-in-progress: false

jobs:
  repair:
    if: ${{ !contains(github.event.head_commit.message, '[zero-loss-closure-applied]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/closed-loop-v3-final-20260830
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply application-owned zero-loss closure
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          def one(text, old, new, label):
              count=text.count(old)
              if count != 1:
                  raise SystemExit(f'{label}: expected one target, found {count}')
              return text.replace(old,new,1)

          # workflow-engine.js: one application authority for Stage 01 and Stage 04 accounting.
          p=Path('workflow-engine.js'); t=p.read_text()
          if 'function evaluateIntakeAccounting(project' not in t:
              helper=r'''
const INTAKE_ACCOUNTING_DISPOSITIONS=Object.freeze(['incorporated into the job definition','retained as context','unresolved human-only','later-resolvable','inapplicable with reason']);
const INTAKE_STATEMENT_CLASSES=Object.freeze(['FACT','REQUIREMENT','CONSTRAINT','DECISION','PROHIBITION','REQUESTED_OUTPUT','ACCEPTANCE_CONDITION','MATERIAL_REFERENCE','UNRESOLVED_HUMAN_ONLY','CONTEXT']);
const OBLIGATION_DISPOSITIONS=Object.freeze(['retained nonnormative context','inapplicable','blocked']);
function enumerateInputLeaves(value,sourceLocation,kind,label,units,{inputVersion,extra={}}={}){
  if(value===undefined||value===null)return;
  if(typeof value==='string'){
    const normalized=value.replace(/\r\n/g,'\n').replace(/\r/g,'\n'),lines=normalized.split('\n');
    let emitted=0;
    lines.forEach((rawLine,index)=>{if(!rawLine.trim())return;const location=lines.length>1?`${sourceLocation}#line=${index+1}`:sourceLocation,rawValueSha256=hash.sha256Value(rawLine),unitId=`INPUT-UNIT-${hash.sha256Value({inputVersion,kind,sourceLocation:location,rawValueSha256}).slice(0,20).toUpperCase()}`;units.push({unitId,kind,sourceLocation:location,label:lines.length>1?`${label} line ${index+1}`:label,rawValueSha256,rawValueText:rawLine,...clone(extra)});emitted++;});
    if(!emitted&&value.length){const rawValueSha256=hash.sha256Value(value),unitId=`INPUT-UNIT-${hash.sha256Value({inputVersion,kind,sourceLocation,rawValueSha256}).slice(0,20).toUpperCase()}`;units.push({unitId,kind,sourceLocation,label,rawValueSha256,rawValueText:value,...clone(extra)});}
    return;
  }
  if(Array.isArray(value)){value.forEach((item,index)=>enumerateInputLeaves(item,`${sourceLocation}[${index}]`,kind,`${label}[${index}]`,units,{inputVersion,extra}));return;}
  if(typeof value==='object'){for(const key of Object.keys(value).sort())enumerateInputLeaves(value[key],`${sourceLocation}.${key}`,kind,`${label}.${key}`,units,{inputVersion,extra});return;}
  enumerateInputLeaves(String(value),sourceLocation,kind,label,units,{inputVersion,extra});
}
function intakeCoverageManifest(project){
  ensureShape(project);const inputVersion=project.job.CURRENT_INPUT_VERSION||'UNKNOWN',units=[];
  for(const [name,definition] of Object.entries(schema.JOB_FIELDS||{})){if(!['HUMAN','HUMAN_DECISION'].includes(definition?.producer))continue;enumerateInputLeaves(project.job[name],`job.${name}`,definition.producer==='HUMAN'?'JOB_FIELD':'HUMAN_DECISION',name,units,{inputVersion});}
  enumerateInputLeaves(project.projectData.userEntered,'projectData.userEntered','ORIGINAL_USER_INPUT','Original user input',units,{inputVersion});
  for(const answer of safe(project.projectData.humanInputAnswers).filter(item=>!item.invalidatedBy)){const answerId=String(answer.answerId||answer.requestId||'UNKNOWN');enumerateInputLeaves(answer.answer,`projectData.humanInputAnswers.${answerId}.answer`,'HUMAN_ANSWER',`Human answer ${answerId}`,units,{inputVersion,extra:{answerId}});}
  const seen=new Set(),deduped=[];for(const unit of units){const key=`${unit.sourceLocation}|${unit.rawValueSha256}`;if(seen.has(key))continue;seen.add(key);deduped.push(unit);}const base={schema:'closed-loop-intake-manifest/1',inputVersion,units:deduped,unitCount:deduped.length};return {...base,manifestSha256:hash.sha256Value(base)};
}
function parseCapturedInputSet(stageOrProject){const stageOne=stageOrProject?.stages?.[1]||stageOrProject||{},raw=stageOne?.agentData?.INPUT_SET_CONTENTS??stageOne?.acceptedData?.INPUT_SET_CONTENTS??'';if(!String(raw||'').trim())return {format:'EMPTY',units:[],raw:''};try{const parsed=JSON.parse(String(raw));if(parsed&&typeof parsed==='object'&&Array.isArray(parsed.units))return {format:'STRUCTURED',...parsed,raw:String(raw)};}catch{}return {format:'LEGACY_TEXT',units:[],raw:String(raw)};}
function evaluateIntakeAccounting(project,{capture}={}){
  ensureShape(project);const manifest=intakeCoverageManifest(project),parsed=capture===undefined?parseCapturedInputSet(project):(()=>{try{const value=typeof capture==='string'?JSON.parse(capture):capture;return value&&typeof value==='object'&&Array.isArray(value.units)?{format:'STRUCTURED',...value}:{format:'INVALID',units:[]};}catch{return {format:'INVALID',units:[]};}})(),reasons=[];
  if(parsed.format!=='STRUCTURED')reasons.push('Stage 01 INPUT_SET_CONTENTS must be the structured current intake accounting capture.');
  if(String(parsed.inputVersion||'')!==String(manifest.inputVersion))reasons.push('Stage 01 intake accounting is not bound to the current input version.');
  if(String(parsed.manifestSha256||'')!==String(manifest.manifestSha256))reasons.push('Stage 01 intake accounting is not bound to the current application intake manifest.');
  const expected=new Map(manifest.units.map(unit=>[unit.unitId,unit])),counts=new Map(),accounted=new Set();
  for(const [index,unit] of safe(parsed.units).entries()){
    const id=String(unit?.sourceUnitId||'').trim();if(!id){reasons.push(`Stage 01 intake unit ${index+1} has no sourceUnitId.`);continue;}counts.set(id,(counts.get(id)||0)+1);const source=expected.get(id);if(!source){reasons.push(`Stage 01 intake accounting contains unknown unit ${id}.`);continue;}
    const hashMatches=String(unit?.sourceRawValueSha256||'')===String(source.rawValueSha256);if(!hashMatches)reasons.push(`Stage 01 intake unit ${id} is not bound to the exact source raw-value hash.`);
    const disposition=String(unit?.disposition||'').trim().toLowerCase();if(!INTAKE_ACCOUNTING_DISPOSITIONS.includes(disposition))reasons.push(`Stage 01 intake unit ${id} has an invalid disposition.`);if(disposition==='inapplicable with reason'&&!String(unit?.reason||'').trim())reasons.push(`Stage 01 intake unit ${id} requires an inapplicability reason.`);
    const statements=safe(unit?.extractedStatements);if(disposition!=='inapplicable with reason'&&!statements.length)reasons.push(`Stage 01 intake unit ${id} contains no extracted human-authority statement.`);const statementKeys=new Set();let statementsValid=true;
    for(const statement of statements){const key=String(statement?.statementKey||'').trim(),text=String(statement?.text||'').trim(),classification=String(statement?.statementClass||'').trim().toUpperCase();if(!key||statementKeys.has(key)){reasons.push(`Stage 01 intake unit ${id} has a missing or duplicate statementKey.`);statementsValid=false;}statementKeys.add(key);if(!text){reasons.push(`Stage 01 intake unit ${id} contains an empty extracted statement.`);statementsValid=false;}if(!INTAKE_STATEMENT_CLASSES.includes(classification)){reasons.push(`Stage 01 intake unit ${id} has an invalid statementClass.`);statementsValid=false;}}
    if(hashMatches&&INTAKE_ACCOUNTING_DISPOSITIONS.includes(disposition)&&statementsValid&&(disposition==='inapplicable with reason'?Boolean(String(unit?.reason||'').trim()):statements.length>0))accounted.add(id);
  }
  for(const [id,count] of counts)if(count!==1)reasons.push(`Stage 01 intake unit ${id} is accounted ${count} times; exactly one accounting entry is required.`);for(const id of expected.keys())if(!counts.has(id))reasons.push(`Stage 01 intake accounting omitted controlled input unit ${id}.`);const coverage=manifest.unitCount?accounted.size/manifest.unitCount:1;return {complete:reasons.length===0,coverage,reasons,manifest,parsed,expectedUnitCount:manifest.unitCount,accountedUnitCount:accounted.size};
}
function semanticLeaves(value,emit,path='value'){if(value===undefined||value===null)return;if(typeof value==='string'){const lines=value.replace(/\r\n/g,'\n').replace(/\r/g,'\n').split('\n');for(const [index,line] of lines.entries())if(line.trim())emit(line,lines.length>1?`${path}#line=${index+1}`:path);return;}if(Array.isArray(value)){value.forEach((item,index)=>semanticLeaves(item,emit,`${path}[${index}]`));return;}if(typeof value==='object'){for(const key of Object.keys(value).sort())semanticLeaves(value[key],emit,`${path}.${key}`);return;}emit(String(value),path);}
function obligationManifest(project){
  ensureShape(project);const inputVersion=project.job.CURRENT_INPUT_VERSION||'UNKNOWN',sourceSetVersion=project.job.CURRENT_SOURCE_SET_VERSION||null,items=[];const add=(text,origin,provenance,sourceIdentity=null,evidenceRefs=[],classification='UNCLASSIFIED')=>{const value=String(text??'');if(!value.trim())return;const obligationId=`OBLIGATION-${hash.sha256Value({origin,provenance,text:value}).slice(0,20).toUpperCase()}`;items.push({obligationId,text:value,origin,classification,provenance:clone(provenance),sourceIdentity:sourceIdentity||null,evidenceRefs:[...new Set(safe(evidenceRefs).map(String))]});};
  const capture=parseCapturedInputSet(project);for(const unit of safe(capture.units))for(const [index,statement] of safe(unit?.extractedStatements).entries())add(statement?.text,'STAGE01_CAPTURED_HUMAN_AUTHORITY',{sourceUnitId:String(unit?.sourceUnitId||'UNKNOWN'),statementKey:String(statement?.statementKey||index+1),inputVersion},null,[],String(statement?.statementClass||'UNCLASSIFIED').toUpperCase());
  const stageOne=project.stages?.[1]?.agentData||project.stages?.[1]?.acceptedData||{};for(const field of ['EXACT_DELIVERABLE_REQUESTED','ASSUMPTIONS','UNKNOWN_INFORMATION'])semanticLeaves(stageOne[field],(text,location)=>add(text,'STAGE01_ACCEPTED_JOB_DEFINITION',{field,location,inputVersion}),null,[],'STAGE01_JOB_DEFINITION');
  const stageThree=project.stages?.[3]?.agentData||project.stages?.[3]?.acceptedData||{};for(const field of ['EXCEPTIONS_AND_EDGE_CONDITIONS','CONFLICTING_OR_INVALIDATING_MATERIAL','RESEARCH_GAPS_AND_BLOCKERS'])semanticLeaves(stageThree[field],(text,location)=>add(text,'STAGE03_ACCEPTED_RESEARCH_SUMMARY',{field,location,sourceSetVersion}),null,[],'STAGE03_RESEARCH_SUMMARY');
  const researchFields=['MANDATORY_STATEMENTS','RECOMMENDATIONS','OPTIONAL_PRACTICES','EXAMPLES','EXPLANATORY_MATERIAL','PROHIBITIONS','EXCEPTIONS','DEPENDENCIES','APPLICABILITY_FACTS','RESTRICTIONS','INVALIDATING_MATERIAL'];for(const record of recordsForCurrentScope(project,'research')){const researchId=recordId(record,'research'),sourceId=String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||'')||null;for(const field of researchFields)semanticLeaves(recordValue(record,field),(text,location)=>add(text,'STAGE03_SOURCE_RESEARCH',{researchId,field,location,sourceId,sourceSetVersion},sourceId,record.evidenceRefs,field));}
  for(const record of recordsForCurrentScope(project,'candidateRequirements')){const candidateRequirementId=recordId(record,'candidateRequirements'),sourceId=String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||'')||null;semanticLeaves(recordValue(record,'CANDIDATE_OBLIGATION'),(text,location)=>add(text,'STAGE03_CANDIDATE_REQUIREMENT',{candidateRequirementId,location,sourceId,sourceSetVersion},sourceId,record.evidenceRefs,'CANDIDATE_OBLIGATION'));}
  const unique=[],seen=new Set();for(const item of items){if(seen.has(item.obligationId))continue;seen.add(item.obligationId);unique.push(item);}const sourceContext=recordsForCurrentScope(project,'sources').map(record=>({sourceId:recordId(record,'sources'),contentSha256:record.contentSha256||record.sha256||hash.sha256Value(recordFields(record)),fields:recordFields(record)})),evidenceContext=recordsForCurrentScope(project,'evidenceRecords').filter(record=>[2,3].includes(Number(record.stage))).map(record=>({evidenceId:recordId(record,'evidenceRecords'),sourceId:String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||''),sha256:record.sha256||recordValue(record,'SHA256')||hash.sha256Value(recordFields(record))}));const base={schema:'closed-loop-obligation-manifest/1',inputVersion,sourceSetVersion,items:unique,obligationCount:unique.length,sourceContext,evidenceContext};return {...base,manifestSha256:hash.sha256Value(base)};
}
function obligationIdsFromRelationship(value){return [...new Set(String(value??'').match(/OBLIGATION-[A-F0-9]{20}/g)||[])];}
function evaluateObligationAccounting(project,{envelope}={}){
  ensureShape(project);const manifest=obligationManifest(project),expected=new Map(manifest.items.map(item=>[item.obligationId,item])),mapped=new Map(),dispositions=new Map(),reasons=[];const requirements=envelope?safe(envelope.records?.requirements):recordsForCurrentScope(project,'requirements'),evidence=envelope?safe(envelope.evidence):recordsForCurrentScope(project,'evidenceRecords').filter(record=>Number(record.stage)===4);
  for(const requirement of requirements){const fields=envelope?(requirement?.fields||{}):recordFields(requirement),refs=obligationIdsFromRelationship(fields.USER_INPUT_RELATIONSHIP);for(const id of refs){if(!expected.has(id))reasons.push(`Stage 04 requirement accounting references unknown obligation ${id}.`);else{const list=mapped.get(id)||[];list.push(envelope?(requirement.tempKey||requirement.targetId||'response-requirement'):recordId(requirement,'requirements'));mapped.set(id,list);}}}
  for(const item of evidence){const kind=String(envelope?item?.kind:recordValue(item,'KIND')||'').trim().toUpperCase();if(kind!=='OBLIGATION_DISPOSITION')continue;const raw=String(envelope?item?.content:recordValue(item,'CONTENT')||'');let parsed;try{parsed=JSON.parse(raw);}catch{reasons.push('Stage 04 OBLIGATION_DISPOSITION evidence contains invalid JSON.');continue;}const id=String(parsed?.obligationId||'').trim(),disposition=String(parsed?.disposition||'').trim().toLowerCase(),reason=String(parsed?.reason||'').trim();if(!expected.has(id)){reasons.push(`Stage 04 disposition references unknown obligation ${id||'MISSING'}.`);continue;}if(!OBLIGATION_DISPOSITIONS.includes(disposition))reasons.push(`Stage 04 obligation ${id} has an invalid non-requirement disposition.`);if(!reason)reasons.push(`Stage 04 obligation ${id} non-requirement disposition requires a reason.`);const list=dispositions.get(id)||[];list.push({disposition,reason});dispositions.set(id,list);}
  let accounted=0;for(const id of expected.keys()){const mappings=mapped.get(id)||[],choices=dispositions.get(id)||[];if(!mappings.length&&!choices.length)reasons.push(`Stage 04 obligation accounting omitted ${id}.`);if(mappings.length&&choices.length)reasons.push(`Stage 04 obligation ${id} is both mapped to requirement(s) and given a non-requirement disposition.`);if(choices.length>1)reasons.push(`Stage 04 obligation ${id} has duplicate non-requirement dispositions.`);const validChoice=choices.length===1&&OBLIGATION_DISPOSITIONS.includes(choices[0].disposition)&&Boolean(choices[0].reason);if((mappings.length>0&&!choices.length)||(!mappings.length&&validChoice))accounted++;}const coverage=manifest.obligationCount?accounted/manifest.obligationCount:1;return {complete:reasons.length===0,coverage,reasons,manifest,expectedObligationCount:manifest.obligationCount,accountedObligationCount:accounted,mapped,dispositions};
}
'''
              marker='const applicationadjudicationGate=gate;'
              if t.count(marker)!=1: raise SystemExit('workflow-engine.js: accounting insertion marker not unique')
              t=t.replace(marker,helper+'\n'+marker,1)
              # Gate wrapper: semantic closure is application-calculated, not trusted from agent status.
              gate_marker="  const add=x=>{for(const r of safe(x))if(r&&!reasons.includes(r))reasons.push(r);};\n"
              if t.count(gate_marker)!=1: raise SystemExit('workflow-engine.js: gate marker not unique')
              t=t.replace(gate_marker,gate_marker+"  if(stage===1){const accounting=evaluateIntakeAccounting(project);if(!accounting.complete)add(accounting.reasons);}\n  if(stage===4){const accounting=evaluateObligationAccounting(project);if(!accounting.complete)add(accounting.reasons);}\n",1)
              t=one(t,"function recalculate(project){\n  ensureShape(project);\n","function recalculate(project){\n  ensureShape(project);\n  project.job.INPUT_SET_HASH_OR_MANIFEST=intakeCoverageManifest(project).manifestSha256;\n",'workflow-engine recalculate')
              t=one(t,'stage16CorrectionPlan,operationalNextAction,operationalMetrics,gate,deriveStageData,recalculate','stage16CorrectionPlan,operationalNextAction,operationalMetrics,intakeCoverageManifest,parseCapturedInputSet,evaluateIntakeAccounting,obligationManifest,evaluateObligationAccounting,gate,deriveStageData,recalculate','workflow-engine exports')
              p.write_text(t)

          # prompt-engine.js: delegate accounting to the engine and embed the full required Stage 04 union.
          p=Path('prompt-engine.js'); t=p.read_text()
          if "closed-loop-prompt-engine/33" not in t:
              t=t.replace("const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine/32';","const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine/33';",1)
              pattern=r"function intakeCoverageManifest\(state\)\{.*?\nconst stageSpecial="
              replacement="function intakeCoverageManifest(state){return workflow.intakeCoverageManifest(state);}\nfunction parseCapturedInputSet(stageOne){return workflow.parseCapturedInputSet(stageOne);}\nfunction obligationManifest(state){return workflow.obligationManifest(state);}\nconst stageSpecial="
              t,count=re.subn(pattern,replacement,t,count=1,flags=re.S)
              if count!=1: raise SystemExit('prompt-engine.js: accounting delegation replacement failed')
              stage4_pattern=r"function stage4ExhaustedInputs\(state\)\{.*?\nfunction assertStage4UpstreamExhausted"
              stage4_repl="function stage4ExhaustedInputs(state){const active=list=>safe(list).filter(r=>r?.active!==false&&!r?.invalidatedBy).map(r=>({id:r?.id||r?.recordId||null,stage:r?.stage??null,scope:r?.scope||{},fields:recordFields(r),relationships:r?.relationships||{},evidenceRefs:r?.evidenceRefs||[]})),humanJobInput=Object.fromEntries(Object.entries(schema.JOB_FIELDS||{}).filter(([,definition])=>['HUMAN','HUMAN_DECISION'].includes(definition?.producer)).map(([name])=>[name,state?.job?.[name]??null]));return {currentUserJobInput:humanJobInput,originalUserEntered:state?.projectData?.userEntered||{},stage01AcceptedCapture:parseCapturedInputSet(state),stage01AcceptedDefinition:{agentData:state?.stages?.[1]?.agentData||state?.stages?.[1]?.acceptedData||{},humanData:state?.stages?.[1]?.humanData||{},derivedData:state?.stages?.[1]?.derivedData||{}},stage03AcceptedData:{agentData:state?.stages?.[3]?.agentData||state?.stages?.[3]?.acceptedData||{},humanData:state?.stages?.[3]?.humanData||{},derivedData:state?.stages?.[3]?.derivedData||{}},stage03Research:active(state?.projectData?.research),stage03CandidateRequirements:active(state?.projectData?.candidateRequirements),applicableSources:active(state?.projectData?.sources),applicableEvidence:active(state?.projectData?.evidenceRecords).filter(record=>[2,3].includes(Number(record.stage)))};}\nfunction assertStage4UpstreamExhausted"
              t,count=re.subn(stage4_pattern,stage4_repl,t,count=1,flags=re.S)
              if count!=1: raise SystemExit('prompt-engine.js: Stage 04 exhaustive context replacement failed')
              upstream_pattern=r"function assertStage4UpstreamExhausted\(state\)\{.*?\nfunction buildPromptRecord"
              upstream_repl="function assertStage4UpstreamExhausted(state){const fail=reason=>{const error=new Error(reason);error.code='STAGE4_UPSTREAM_INCOMPLETE';throw error;};const one=state?.stages?.[1],three=state?.stages?.[3];if(String(one?.status||'').toUpperCase()!=='COMPLETE'||one?.gate?.complete===false)fail('Stage 04 cannot be generated until Stage 01 human-authority intake is complete.');const intake=workflow.evaluateIntakeAccounting(state);if(!intake.complete)fail('Stage 04 cannot be generated because Stage 01 controlled-input accounting is incomplete: '+intake.reasons.join('; '));if(String(three?.status||'').toUpperCase()!=='COMPLETE'||three?.gate?.complete===false)fail('Stage 04 cannot be generated until Stage 03 research is complete.');const noSource=upper(state?.stages?.[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION||state?.stages?.[2]?.acceptedData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE';if(!noSource){const sources=safe(state?.projectData?.sources).filter(r=>r?.active!==false&&!r?.invalidatedBy),research=safe(state?.projectData?.research).filter(r=>r?.active!==false&&!r?.invalidatedBy),covered=new Set(research.map(r=>clean(recordValue(r,'SOURCE_ID')||r?.relationships?.SOURCE_ID)).filter(Boolean));for(const src of sources){const sid=recordId(src,'sources');if(sid&&!covered.has(sid))fail('Stage 04 cannot be generated because Stage 03 has not researched every current Stage 02 source.');}const d=three?.agentData||three?.acceptedData||{};if(!['TRUE','YES','COMPLETE','COMPLETED'].includes(upper(d.ALL_KNOWN_CONTROLLING_SOURCES_EXAMINED)))fail('Stage 04 cannot be generated until Stage 03 confirms all controlling sources were examined.');if(!['TRUE','YES','COMPLETE','COMPLETED'].includes(upper(d.SECOND_CONFLICT_AND_EXCEPTION_PASS_COMPLETED)))fail('Stage 04 cannot be generated until Stage 03 completes the second conflict and exception pass.');if(['TRUE','YES','FOUND','DETECTED'].includes(upper(d.NEW_MATERIAL_CATEGORY_FOUND_IN_LATEST_PASS)))fail('Stage 04 cannot be generated while the latest Stage 03 pass found a new material requirement category.');}return true;}\nfunction buildPromptRecord"
              t,count=re.subn(upstream_pattern,upstream_repl,t,count=1,flags=re.S)
              if count!=1: raise SystemExit('prompt-engine.js: Stage 04 upstream closure replacement failed')
              t=t.replace('INPUT_SET_CONTENTS must be a JSON string whose root is {"units":[]} and contains one entry for every manifest unit. Each entry must contain sourceUnitId, disposition, and extractedStatements.','INPUT_SET_CONTENTS must be a JSON string whose root contains inputVersion, manifestSha256, and units. inputVersion and manifestSha256 must exactly echo the APPLICATION INTAKE MANIFEST. Every units entry must contain sourceUnitId, sourceRawValueSha256, disposition, and extractedStatements.',1)
              t=t.replace('{"units":[{"sourceUnitId":"INPUT-UNIT-...","disposition":','{"inputVersion":"INPUT-v...","manifestSha256":"...","units":[{"sourceUnitId":"INPUT-UNIT-...","sourceRawValueSha256":"...","disposition":',1)
              t=t.replace('Every unitId in APPLICATION INTAKE MANIFEST must appear exactly once. The application rejects omission or duplicate accounting.','Every unitId in APPLICATION INTAKE MANIFEST must appear exactly once, bound to its exact sourceRawValueSha256. The application rejects omitted, duplicated, unknown, stale, or hash-mismatched accounting.',1)
              t=t.replace('Process every obligationId exactly once before final JSON; schema-valid output with an omitted obligation is invalid.','Account for every obligationId before final JSON; schema-valid output with an omitted obligation is invalid. One obligation may map to multiple atomic requirements, but each obligation must have exactly one disposition category: mapped to requirement(s), retained nonnormative context, inapplicable, or blocked.',1)
              t=t.replace('Every obligationId in APPLICATION OBLIGATION MANIFEST must have a disposition.','Every obligationId in APPLICATION OBLIGATION MANIFEST must be completely accounted.',1)
              p.write_text(t)

          # response-ingestion.js: reject incomplete Stage 01/04 semantics before any canonical mutation.
          p=Path('response-ingestion.js'); t=p.read_text()
          if 'INCOMPLETE_INTAKE_ACCOUNTING' not in t:
              marker="    if(!safe(envelope.evidence).length)issues.push(issue('MISSING_PROVENANCE','/evidence','DATA_PROPOSAL requires evidence.'));\n"
              addition=marker+"    if(stageNumber===1){const accounting=workflow.evaluateIntakeAccounting(project,{capture:envelope.stageData?.INPUT_SET_CONTENTS});for(const reason of accounting.reasons)issues.push(issue('INCOMPLETE_INTAKE_ACCOUNTING','/stageData/INPUT_SET_CONTENTS',reason));}\n    if(stageNumber===4){const accounting=workflow.evaluateObligationAccounting(project,{envelope});for(const reason of accounting.reasons)issues.push(issue('INCOMPLETE_OBLIGATION_ACCOUNTING','/records/requirements',reason));}\n"
              t=one(t,marker,addition,'response-ingestion DATA_PROPOSAL closure')
              p.write_text(t)

          # app-core.js: deterministic legacy migration to the structured Stage 01 capture; locked Stage 04 remains inspectable.
          p=Path('app-core.js'); t=p.read_text()
          if 'legacyStage01StructuredCapture' not in t:
              old="p.stages[1].agentData={EXACT_DELIVERABLE_REQUESTED:p.job.EXACT_DELIVERABLE_REQUESTED||'',ASSUMPTIONS:p.job.ASSUMPTIONS||'',UNKNOWN_INFORMATION:p.job.UNKNOWN_INFORMATION||'',INPUT_SET_CONTENTS:legacyStage01Capture};"
              new="const legacyIntakeManifest=engine.intakeCoverageManifest(p),legacyCompatibility=JSON.parse(legacyStage01Capture),legacyStage01StructuredCapture=JSON.stringify({...legacyCompatibility,schema:'closed-loop-stage01-capture/1',inputVersion:legacyIntakeManifest.inputVersion,manifestSha256:legacyIntakeManifest.manifestSha256,units:legacyIntakeManifest.units.map((unit,index)=>({sourceUnitId:unit.unitId,sourceRawValueSha256:unit.rawValueSha256,disposition:'retained as context',reason:'Deterministically migrated historical human-authority input.',extractedStatements:[{statementKey:`migration-${index+1}`,text:unit.rawValueText||unit.label||unit.sourceLocation,statementClass:unit.kind==='SUPPLIED_MATERIAL'?'MATERIAL_REFERENCE':'CONTEXT'}]}))});p.stages[1].agentData={EXACT_DELIVERABLE_REQUESTED:p.job.EXACT_DELIVERABLE_REQUESTED||'',ASSUMPTIONS:p.job.ASSUMPTIONS||'',UNKNOWN_INFORMATION:p.job.UNKNOWN_INFORMATION||'',INPUT_SET_CONTENTS:legacyStage01StructuredCapture};"
              t=one(t,old,new,'app-core legacy Stage 01 capture')
          if "error?.code==='STAGE4_UPSTREAM_INCOMPLETE'" not in t:
              old="if(error?.code==='MISSING_REQUIRED_PROMPT_SCOPE')return `CONTROLLING INSTRUCTION UNAVAILABLE\\n\\n${error.message}\\n\\nReserve/select the exact execution lane shown for this stage, then save or copy the instruction.`;throw error;"
              new="if(error?.code==='MISSING_REQUIRED_PROMPT_SCOPE')return `CONTROLLING INSTRUCTION UNAVAILABLE\\n\\n${error.message}\\n\\nReserve/select the exact execution lane shown for this stage, then save or copy the instruction.`;if(error?.code==='STAGE4_UPSTREAM_INCOMPLETE')return `CONTROLLING INSTRUCTION UNAVAILABLE\\n\\n${error.message}\\n\\nStage 04 remains inspectable, but no Stage 04 controlling instruction can be generated, saved, copied, or executed until complete Stage 01 human-authority intake and complete Stage 03 research are established. Do not ask the user to resupply prior project information.`;throw error;"
              t=one(t,old,new,'app-core locked Stage 04')
          p.write_text(t)

          # Permanent zero-loss mutation regression.
          Path('verify-zero-loss-accounting.mjs').write_text(r'''import fs from 'node:fs';
import vm from 'node:vm';
const assert=(x,m)=>{if(!x)throw new Error(m);};
globalThis.Event=globalThis.Event||class Event{constructor(type){this.type=type;}};globalThis.dispatchEvent=globalThis.dispatchEvent||(()=>true);
for(const file of ['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js','prompt-engine.js','response-ingestion.js'])vm.runInThisContext(fs.readFileSync(file,'utf8'),{filename:file});
const core=globalThis.closedLoopCore,engine=globalThis.closedLoopWorkflowEngine,prompts=globalThis.closedLoopPromptEngine,ingestion=globalThis.closedLoopResponseIngestion,schema=globalThis.closedLoopWorkflowSchema,hash=globalThis.closedLoopHash;
const project=core.createBlankState('JOB-ZERO-LOSS');
Object.assign(project.job,{JOB_TITLE:'Zero loss fixture',JOB_OWNER:'Human owner',EXACT_USER_OBJECTIVE_VERBATIM:'Build exactly what the user requested.\nNever forget project information.',EXPLICIT_USER_REQUIREMENTS:'The user supplies project information once.\nStage 04 must reuse Stage 01 and Stage 03.',SUPPLIED_MATERIALS_INVENTORY:'intent.txt',CURRENT_INPUT_VERSION:'INPUT-v001',CURRENT_SOURCE_SET_VERSION:'SOURCE-SET-v001'});
project.projectData.userEntered={objective:'Build exactly what the user requested.',customConstraint:'Do not ask for the same project fact again.',nested:{acceptance:'Every supplied project requirement remains traceable.'}};
engine.ensureShape(project);
const manifest=engine.intakeCoverageManifest(project);assert(manifest.unitCount>=6,'Intake manifest did not enumerate complete human input.');assert(manifest.units.some(u=>u.sourceLocation.includes('projectData.userEntered.customConstraint')),'Original userEntered custom project data was omitted.');assert(manifest.units.some(u=>u.sourceLocation==='job.JOB_TITLE'),'Human-decision job title was omitted from intake authority.');
const capture={schema:'closed-loop-stage01-capture/1',inputVersion:manifest.inputVersion,manifestSha256:manifest.manifestSha256,units:manifest.units.map((u,i)=>({sourceUnitId:u.unitId,sourceRawValueSha256:u.rawValueSha256,disposition:'retained as context',reason:'Preserved project authority.',extractedStatements:[{statementKey:`s-${i+1}`,text:u.rawValueText,statementClass:'CONTEXT'}]}))};
const missing=structuredClone(capture);missing.units.pop();assert(!engine.evaluateIntakeAccounting(project,{capture:JSON.stringify(missing)}).complete,'Incomplete Stage 01 accounting was not detected.');assert(engine.evaluateIntakeAccounting(project,{capture:JSON.stringify(capture)}).complete,'Complete Stage 01 accounting did not close.');
const p1=prompts.buildPromptRecord(1,project,{operation:'COMPLETE'}),env={schema:schema.RESPONSE_SCHEMA,jobId:project.job.JOB_ID,stage:1,operation:'COMPLETE',promptIdentity:{instructionId:p1.instructionId,bodySha256:p1.bodySha256,contractSha256:p1.contractSha256,contextSignature:p1.contextSignature},scope:p1.scope,responseType:'DATA_PROPOSAL',humanInputRequests:[],stageData:{INPUT_SET_CONTENTS:JSON.stringify(missing)},records:{},evidence:[{temporaryKey:'e1',kind:'INTAKE',description:'Stage 01 capture',authorityType:'AGENT_CLAIM',location:'response',content:'capture'}],unresolved:[],warnings:[],attachments:[]};let v=ingestion.validateEnvelope(project,env,{stage:1,promptRecord:p1,rawSha256:hash.sha256Value(env),files:[]});assert(v.issues.some(i=>i.code==='INCOMPLETE_INTAKE_ACCOUNTING'),'Ingestion accepted incomplete Stage 01 accounting.');env.stageData.INPUT_SET_CONTENTS=JSON.stringify(capture);v=ingestion.validateEnvelope(project,env,{stage:1,promptRecord:p1,rawSha256:hash.sha256Value(env),files:[]});assert(!v.issues.some(i=>i.code==='INCOMPLETE_INTAKE_ACCOUNTING'),'Ingestion rejected repaired Stage 01 accounting.');
project.stages[1].agentData={EXACT_DELIVERABLE_REQUESTED:'Exact requested product.',ASSUMPTIONS:'NONE',UNKNOWN_INFORMATION:'NONE',INPUT_SET_CONTENTS:JSON.stringify(capture)};
project.projectData.sources=[{id:'SOURCE-000001',stage:2,active:true,scope:{inputVersion:'INPUT-v001',sourceSetVersion:'SOURCE-SET-v001'},fields:{SOURCE_ID:'SOURCE-000001',TITLE:'Authority'}}];
project.projectData.research=[{id:'RESEARCH-000001',stage:3,active:true,scope:{inputVersion:'INPUT-v001',sourceSetVersion:'SOURCE-SET-v001'},fields:{RESEARCH_ID:'RESEARCH-000001',SOURCE_ID:'SOURCE-000001',MANDATORY_STATEMENTS:'External mandatory obligation.',RECOMMENDATIONS:'External recommendation.',OPTIONAL_PRACTICES:'Optional practice.',EXAMPLES:'Non-normative example.',EXPLANATORY_MATERIAL:'Explanation.',PROHIBITIONS:'External prohibition.',EXCEPTIONS:'External exception.',DEPENDENCIES:'External dependency.',APPLICABILITY_FACTS:'Applicability fact.',RESTRICTIONS:'Restriction.',INVALIDATING_MATERIAL:'Invalidating material.'},relationships:{SOURCE_ID:'SOURCE-000001'},evidenceRefs:[]}];
project.projectData.candidateRequirements=[{id:'CANDIDATE-REQ-000001',stage:3,active:true,scope:{inputVersion:'INPUT-v001',sourceSetVersion:'SOURCE-SET-v001'},fields:{CANDIDATE_REQ_ID:'CANDIDATE-REQ-000001',SOURCE_ID:'SOURCE-000001',CANDIDATE_OBLIGATION:'Candidate external obligation.'},relationships:{SOURCE_ID:'SOURCE-000001'},evidenceRefs:[]}];
const o=engine.obligationManifest(project),texts=o.items.map(x=>x.text);for(const required of ['Do not ask for the same project fact again.','External mandatory obligation.','External recommendation.','Optional practice.','Non-normative example.','Explanation.','External prohibition.','External exception.','External dependency.','Applicability fact.','Restriction.','Invalidating material.','Candidate external obligation.'])assert(texts.includes(required),`Stage 04 manifest omitted ${required}`);assert(o.sourceContext.some(s=>s.sourceId==='SOURCE-000001'),'Stage 04 manifest omitted applicable source identity.');
const reqs=o.items.map((item,i)=>({tempKey:`r${i+1}`,fields:{USER_INPUT_RELATIONSHIP:item.obligationId}})),complete={records:{requirements:reqs},evidence:[]};assert(engine.evaluateObligationAccounting(project,{envelope:complete}).complete,'Complete Stage 04 accounting did not close.');assert(!engine.evaluateObligationAccounting(project,{envelope:{records:{requirements:reqs.slice(0,-1)},evidence:[]}}).complete,'Incomplete Stage 04 obligation accounting was not detected.');const first=o.items[0]?.obligationId;if(first){const multiple=structuredClone(complete);multiple.records.requirements.push({tempKey:'extra',fields:{USER_INPUT_RELATIONSHIP:first}});assert(engine.evaluateObligationAccounting(project,{envelope:multiple}).complete,'One obligation could not map to multiple atomic requirements.');}
const source=fs.readFileSync('prompt-engine.js','utf8');assert(source.includes('currentUserJobInput')&&source.includes('originalUserEntered')&&source.includes('applicableSources')&&source.includes('applicableEvidence'),'Stage 04 exhaustive context union is incomplete.');const css=fs.readFileSync('index.html','utf8');assert(css.includes('.expandable-prompt{max-height:280px}.expandable-prompt.expanded{max-height:none}'),'Visual prompt-box baseline changed.');
console.log(JSON.stringify({zeroLossStage01:true,zeroLossStage04:true,completeStage03ResearchUnion:true,oneTimeProjectInput:true,incompleteIntakeRejected:true,incompleteObligationRejected:true,visualBaselinePreserved:true}));''')

          # Update browser regression from old legacy-object shape to current structured capture.
          p=Path('verify-browser.mjs'); t=p.read_text()
          old="assert(parsedRetainedInputCapture?.humanAuthority?.EXACT_USER_OBJECTIVE_VERBATIM===String(retained.job.EXACT_USER_OBJECTIVE_VERBATIM||''),'Legacy Stage 01 accepted input-set capture does not exactly preserve the canonical user objective after JSON decoding.');assert(parsedRetainedInputCapture?.humanAuthority?.EXPLICIT_USER_REQUIREMENTS===String(retained.job.EXPLICIT_USER_REQUIREMENTS||''),'Legacy Stage 01 accepted input-set capture does not exactly preserve the canonical explicit requirements after JSON decoding.');"
          new="const retainedStatements=(parsedRetainedInputCapture?.units||[]).flatMap(u=>(u.extractedStatements||[]).map(s=>String(s.text||'')));assert(parsedRetainedInputCapture?.inputVersion===retained.job.CURRENT_INPUT_VERSION&&String(parsedRetainedInputCapture?.manifestSha256||'').length>0,'Legacy Stage 01 accepted input-set capture is not bound to the current intake manifest.');assert(retainedStatements.includes(String(retained.job.EXACT_USER_OBJECTIVE_VERBATIM||'').split(/\\r?\\n/).find(Boolean)),'Legacy Stage 01 structured capture does not preserve the canonical user objective.');for(const requirement of String(retained.job.EXPLICIT_USER_REQUIREMENTS||'').split(/\\r?\\n/).filter(Boolean))assert(retainedStatements.includes(requirement),`Legacy Stage 01 structured capture omitted explicit requirement: ${requirement}`);"
          if old in t:t=t.replace(old,new,1)
          p.write_text(t)
          PY
          git diff --check
      - name: Prove focused zero-loss semantics and browser behavior
        shell: bash
        run: |
          node --check workflow-engine.js
          node --check prompt-engine.js
          node --check response-ingestion.js
          node --check app-core.js
          node --check verify-zero-loss-accounting.mjs
          node verify-zero-loss-accounting.mjs
          node build-test-project.mjs
          export BROWSER="$(command -v google-chrome || command -v chromium || command -v chrome || true)"
          test -n "$BROWSER"
          python3 -m http.server 4173 >/tmp/closed-loop-http.log 2>&1 &
          SERVER_PID=$!
          trap 'kill "$SERVER_PID" 2>/dev/null || true' EXIT
          node verify-browser.mjs
          node verify-browser-extra.mjs
          kill "$SERVER_PID" 2>/dev/null || true
          trap - EXIT
      - name: Commit integrated repair and remove this one-time workflow
        shell: bash
        run: |
          git config user.name 'closed-loop-repair'
          git config user.email 'closed-loop-repair@users.noreply.github.com'
          git rm .github/workflows/complete-zero-loss-closure.yml
          git add workflow-engine.js prompt-engine.js response-ingestion.js app-core.js verify-browser.mjs verify-zero-loss-accounting.mjs
          git commit -m 'Enforce zero-loss Stage 01 and Stage 04 accounting [zero-loss-closure-applied]'
          git push origin HEAD:fix/closed-loop-v3-final-20260830
