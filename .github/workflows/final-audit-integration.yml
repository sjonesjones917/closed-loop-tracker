name: Final audit integration
on:
  push:
    branches: [final-audit-minimum-fixes]
permissions:
  contents: write
jobs:
  integrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: {fetch-depth: 0}
      - uses: actions/setup-node@v4
        with: {node-version: '22'}
      - name: Apply minimum audited fixes
        run: |
          python3 - <<'PY'
          from pathlib import Path
          def once(text, old, new, name):
              n=text.count(old)
              if n!=1: raise SystemExit(f'{name}: expected one match, found {n}')
              return text.replace(old,new,1)

          p=Path('workflow-engine.js'); t=p.read_text()
          marker="function acceptedControlEvents(project,stage){return safe(project?.projectData?.responseDispositions).filter(item=>Number(item.stage)===Number(stage)&&['ACCEPTED_HUMAN_QUESTION_SET','ACCEPTED_BLOCKER_EVENT','ACCEPTED_EXECUTION_FAILURE'].includes(item.type)&&!item.invalidatedBy);}\n"
          add="""function acceptedChangeOperation(project,change){if(change?.operation)return String(change.operation);const proposal=safe(project?.projectData?.responseProposals).find(item=>item.proposalId===change?.proposalId);return String(proposal?.envelope?.operation||'COMPLETE');}
function acceptedChangeScope(project,change){if(change?.scope&&typeof change.scope==='object')return change.scope;const proposal=safe(project?.projectData?.responseProposals).find(item=>item.proposalId===change?.proposalId);return proposal?.envelope?.scope||proposal?.scope||{};}
function nextPromptOperation(project,stage){ensureShape(project);const number=Number(stage),contract=schema.STAGE_CONTRACTS[number],operations=contract?.operations||['COMPLETE'];if(![17,19].includes(number))return {operation:operations[0],scope:{}};const changes=acceptedChanges(project,number),opOf=change=>acceptedChangeOperation(project,change),first=operations[0];if(!changes.some(change=>opOf(change)===first))return {operation:first,scope:{}};const iteration=latestIteration(project,[number]),iterationId=recordId(iteration,'iterations'),candidateId=iterationCandidateId(project,iterationId);if(!iterationId||!candidateId)return {operation:'EXECUTE_RUN',scope:{},blockedReason:number===17?'Freeze the corrected candidate before generating execution prompts.':'Start the unchanged-confirmation iteration from the exact converged candidate before generating execution prompts.'};const iterationScope={iterationId,candidateId},runs=recordsForCurrentScope(project,'runs',iterationScope).filter(run=>runIterationId(run)===iterationId);if(runs.length!==10)return {operation:'EXECUTE_RUN',scope:iterationScope,blockedReason:'Reserve exactly ten run/context slots for the current iteration before generating execution prompts.'};const executions=changes.filter(change=>opOf(change)==='EXECUTE_RUN'&&String(acceptedChangeScope(project,change).iterationId||'')===iterationId&&String(acceptedChangeScope(project,change).candidateId||'')===candidateId),done=new Set(executions.map(change=>String(acceptedChangeScope(project,change).runId||'')));const nextRun=runs.find(run=>!done.has(recordId(run,'runs')));if(nextRun){const runId=recordId(nextRun,'runs'),contextId=String(recordValue(nextRun,'CONTEXT_ID')||nextRun.relationships?.CONTEXT_ID||nextRun.scope?.contextId||'');return {operation:'EXECUTE_RUN',scope:{...iterationScope,runId,contextId}};}for(const operation of operations.slice(2)){const accepted=changes.some(change=>opOf(change)===operation&&String(acceptedChangeScope(project,change).iterationId||iterationId)===iterationId);if(!accepted)return {operation,scope:iterationScope};}return {operation:operations.at(-1),scope:iterationScope,complete:true};}
"""
          t=once(t,marker,marker+add,'operation helpers')

          old="function reserveRunBatch(project,{stage=project.activeStage,iterationId=null,candidateId=null,count=10}={}){\n  ensureShape(project);if(count!==10)throw new Error('Run batches must contain exactly ten slots.');const resolvedIteration=iterationId||project.job.CURRENT_ITERATION;if(!resolvedIteration)throw new Error('A current iteration is required before reserving runs.');\n  const created=[];for(let i=0;i<count;i++){const context=commandRecord(project,'freshContexts',{EXTERNAL_CONTEXT_IDENTIFIER:'UNASSIGNED',ROLE:core.STAGES[Number(stage)-1]?.role||'UNKNOWN',ITERATION_ID:resolvedIteration,RUN_ID:'PENDING',AUTHORIZED_PROJECT_INPUTS:project.job.CURRENT_INPUT_VERSION||'UNKNOWN',AUTHORIZED_EXTERNAL_SOURCE_MATERIAL:project.job.CURRENT_SOURCE_SET_VERSION||'NOT APPLICABLE',FROZEN_ARTIFACT_VERSIONS:'CURRENT AUTHORIZED VERSIONS',TOOL_AVAILABILITY:'UNKNOWN',CONTAMINATION_STATUS:'UNKNOWN',OUTPUT_IDENTITY:'UNKNOWN',DEVIATIONS:'NONE',EVIDENCE:'Application-reserved context slot.',USABILITY_DETERMINATION:'PENDING'},{stage,source:'APPLICATION_RESERVATION'});const run=commandRecord(project,'runs',{ITERATION_ID:resolvedIteration,CANDIDATE_ID:candidateId||'PENDING',CONTEXT_ID:context.id,FRESH_CONTEXT_RECORD:context.id,CONTAMINATION_CHECK:'PENDING',TOOL_CONFIGURATION:'PENDING',EXECUTION_STATUS:'RESERVED',COMPLETE_OUTPUT:''},{stage,source:'APPLICATION_RESERVATION'});context.fields.RUN_ID=context.RUN_ID=run.id;created.push({runId:run.id,contextId:context.id});}\n  addHistory(project,'RUN_BATCH_RESERVED',{stage,iterationId:resolvedIteration,count,slots:created});recalculate(project);return created;\n}"
          new="function reserveRunBatch(project,{stage=project.activeStage,iterationId=null,candidateId=null,count=10}={}){\n  ensureShape(project);if(count!==10)throw new Error('Run batches must contain exactly ten slots.');const resolvedIteration=String(iterationId||project.job.CURRENT_ITERATION||'').trim();if(!resolvedIteration)throw new Error('A current iteration is required before reserving runs.');const iteration=records(project,'iterations').find(record=>recordId(record,'iterations')===resolvedIteration&&isActiveRecord(record));if(!iteration)throw new Error('The current iteration does not resolve to an active application-controlled record.');const resolvedCandidate=String(candidateId||recordValue(iteration,'CANDIDATE_ID')||iteration.scope?.candidateId||'').trim();if(!resolvedCandidate||['PENDING','UNKNOWN','NONE','NOT APPLICABLE','UNASSIGNED'].includes(upper(resolvedCandidate)))throw new Error('A frozen application-controlled candidate is required before reserving runs.');const scope={...currentScope(project),iterationId:resolvedIteration,candidateId:resolvedCandidate};\n  const created=[];for(let i=0;i<count;i++){const context=commandRecord(project,'freshContexts',{EXTERNAL_CONTEXT_IDENTIFIER:'UNASSIGNED',ROLE:core.STAGES[Number(stage)-1]?.role||'UNKNOWN',ITERATION_ID:resolvedIteration,RUN_ID:'PENDING',AUTHORIZED_PROJECT_INPUTS:project.job.CURRENT_INPUT_VERSION||'UNKNOWN',AUTHORIZED_EXTERNAL_SOURCE_MATERIAL:project.job.CURRENT_SOURCE_SET_VERSION||'NOT APPLICABLE',FROZEN_ARTIFACT_VERSIONS:'CURRENT AUTHORIZED VERSIONS',TOOL_AVAILABILITY:'UNKNOWN',CONTAMINATION_STATUS:'UNKNOWN',OUTPUT_IDENTITY:'UNKNOWN',DEVIATIONS:'NONE',EVIDENCE:'Application-reserved context slot.',USABILITY_DETERMINATION:'PENDING'},{stage,source:'APPLICATION_RESERVATION',scope});const run=commandRecord(project,'runs',{ITERATION_ID:resolvedIteration,CANDIDATE_ID:resolvedCandidate,CONTEXT_ID:context.id,FRESH_CONTEXT_RECORD:context.id,CONTAMINATION_CHECK:'PENDING',TOOL_CONFIGURATION:'PENDING',EXECUTION_STATUS:'RESERVED',COMPLETE_OUTPUT:''},{stage,source:'APPLICATION_RESERVATION',scope});context.fields.RUN_ID=context.RUN_ID=run.id;created.push({runId:run.id,contextId:context.id});}\n  addHistory(project,'RUN_BATCH_RESERVED',{stage,iterationId:resolvedIteration,candidateId:resolvedCandidate,count,slots:created});recalculate(project);return created;\n}"
          t=once(t,old,new,'reserveRunBatch')

          old="function invalidateAcceptedResponse(project,{stage=project.activeStage,rawResponseId,reason='Corrected response required.',operatorLabel='HUMAN_OPERATOR'}={}){\n  ensureShape(project);const change=acceptedChanges(project,stage).find(item=>!rawResponseId||item.rawResponseId===rawResponseId);if(!change)throw new Error('No current accepted data change exists for invalidation.');\n  const event=addHistory(project,'ACCEPTED_RESPONSE_INVALIDATED',{stage,rawResponseId:change.rawResponseId,reason,operatorLabel,identityAssurance:'SELF_ASSERTED'});change.invalidatedBy=event.eventId;invalidateDownstream(project,stage,event.eventId,reason);recalculate(project);return event;\n}"
          new="function invalidateAcceptedResponse(project,{stage=project.activeStage,rawResponseId,reason='Corrected response required.',operatorLabel='HUMAN_OPERATOR'}={}){\n  ensureShape(project);const number=Number(stage),change=acceptedChanges(project,number).find(item=>!rawResponseId||item.rawResponseId===rawResponseId);if(!change)throw new Error('No current accepted data change exists for invalidation.');const proposal=safe(project.projectData.responseProposals).find(item=>item.proposalId===change.proposalId);\n  const event=addHistory(project,'ACCEPTED_RESPONSE_INVALIDATED',{stage:number,rawResponseId:change.rawResponseId,reason:String(reason||'Corrected response required.'),operatorLabel,identityAssurance:'SELF_ASSERTED'});change.invalidatedBy=event.eventId;for(const prompt of safe(project.projectData.generatedPrompts).filter(item=>Number(item.stage)===number&&!item.invalidatedBy))prompt.invalidatedBy=event.eventId;for(const pending of safe(project.projectData.responseProposals).filter(item=>Number(item.stage)===number&&item.status==='PENDING_OPERATOR_REVIEW')){pending.status='STALE';pending.invalidatedBy=event.eventId;}const invalidEvidence=new Set(records(project,'evidenceRecords',{active:false}).filter(record=>record.rawResponseId===change.rawResponseId||record.sourceProposalId===change.proposalId).map(record=>recordId(record,'evidenceRecords')));for(const record of records(project,'evidenceRecords',{active:false}))if(invalidEvidence.has(recordId(record,'evidenceRecords'))){record.active=false;record.validity='INVALIDATED';record.invalidatedBy=event.eventId;}for(const [collection,list] of Object.entries(proposal?.canonicalRecords||{})){for(const canonical of safe(list)){const target=records(project,collection,{active:false}).find(record=>recordId(record,collection)===canonical.id);if(!target)continue;if(canonical.updateTargetId){const snapshot=target.lastAgentUpdate?.proposalId===change.proposalId?target.lastAgentUpdate:null;if(snapshot){target.fields=clone(snapshot.fields||{});Object.assign(target,target.fields);target.relationships=clone(snapshot.relationships||{});target.evidenceRefs=clone(snapshot.evidenceRefs||[]);target.status=snapshot.status;target.completionState=snapshot.completionState;delete target.lastAgentUpdate;}else{const def=schema.RECORD_SCHEMAS[collection];for(const [name,fieldDef] of Object.entries(def?.fieldDefinitions||{}))if(fieldDef.producer===schema.PRODUCER.AGENT){delete target.fields?.[name];delete target[name];}target.evidenceRefs=safe(target.evidenceRefs).filter(id=>!invalidEvidence.has(id));target.status='RESERVED';target.completionState='AWAITING_AGENT_COMPLETION';}target.contentSha256=hash.contentRecordSha256(target,schema.RECORD_SCHEMAS[collection].idField);target.recordSha256=hash.recordSha256(target);target.sha256=target.recordSha256;}else{target.active=false;target.validity='INVALIDATED';target.invalidatedBy=event.eventId;}}}\n  const state=project.stages[number];state.acceptedDataChangeIds=safe(state.acceptedDataChangeIds).filter(id=>id!==change.changeId);state.acceptedResponseIds=safe(state.acceptedResponseIds).filter(id=>id!==change.rawResponseId);state.agentData={};for(const remaining of acceptedChanges(project,number).sort((a,b)=>(a.eventSequence||0)-(b.eventSequence||0))){const prior=safe(project.projectData.responseProposals).find(item=>item.proposalId===remaining.proposalId);Object.assign(state.agentData,clone(prior?.proposedStageData||{}));}state.acceptedData=state.agentData;if(number===1)for(const name of schema.AGENT_JOB_FIELDS)project.job[name]=Object.hasOwn(state.agentData,name)?clone(state.agentData[name]):'';invalidateDownstream(project,number,event.eventId,String(reason||'Corrected response required.'));recalculate(project);return event;\n}"
          t=once(t,old,new,'invalidateAcceptedResponse')

          old="const stage=Number(runs[0]?.stage||0);const rawCount=safe(project.projectData.rawResponses).filter(x=>Number(x.stage)===stage&&!x.invalidatedBy).length;const receiptCount=safe(project.projectData.outputReceipts).filter(x=>Number(x.stage)===stage&&!x.invalidatedBy).length;const comparisons="
          new="const stage=Number(runs[0]?.stage||0),expectedExecutionOperation=stage===11?'COMPLETE':'EXECUTE_RUN';const executionChanges=acceptedChanges(project,stage).filter(change=>acceptedChangeOperation(project,change)===expectedExecutionOperation&&String(acceptedChangeScope(project,change).iterationId||'')===String(iterationId||'')&&String(acceptedChangeScope(project,change).candidateId||candidateId||'')===String(candidateId||''));const missingExecutionProof=runs.flatMap(run=>{const runId=recordId(run,'runs'),contextId=String(recordValue(run,'CONTEXT_ID')||run.relationships?.CONTEXT_ID||run.scope?.contextId||''),matches=executionChanges.filter(change=>String(acceptedChangeScope(project,change).runId||'')===runId&&String(acceptedChangeScope(project,change).contextId||'')===contextId);if(matches.length!==1)return [runId];const change=matches[0],raw=safe(project.projectData.rawResponses).find(item=>item.rawResponseId===change.rawResponseId&&!item.invalidatedBy),receipt=safe(project.projectData.outputReceipts).find(item=>(item.acceptedCanonicalChangeId===change.changeId||item.rawResponseId===change.rawResponseId)&&!item.invalidatedBy);return raw&&String(raw.promptScope?.runId||'')===runId&&String(raw.promptScope?.contextId||'')===contextId&&receipt&&String(receipt.runId||'')===runId&&String(receipt.contextId||'')===contextId?[]:[runId];});const comparisons="
          t=once(t,old,new,'iteration execution proof declaration')
          old="if(rawCount<10)reasons.push('Complete raw outputs are not preserved for all ten runs.');if(receiptCount<10)reasons.push('Complete receipts are not preserved for all ten runs.');"
          new="if(missingExecutionProof.length)reasons.push('Every current run requires exactly one accepted execution response with matching raw-response and receipt run/context identity.');"
          t=once(t,old,new,'iteration execution proof gate')
          t=once(t,"regressionExecutionCount:regExec.length,unexplainedVarianceCount:unexplained.length,complete:","regressionExecutionCount:regExec.length,missingExecutionProof,unexplainedVarianceCount:unexplained.length,complete:",'iteration result proof')
          old="case 17:{requireAccepted();const iteration=latestIteration(project,[17]);const ev=evaluateIteration(project,recordId(iteration,'iterations'),'CORRECTED');if(!ev.complete)reasons.push(...ev.reasons);break;}"
          new="case 17:{requireAccepted();const missingOps=schema.STAGE_CONTRACTS[17].operations.filter(operation=>!changes.some(change=>acceptedChangeOperation(project,change)===operation));if(missingOps.length)reasons.push('Required Stage 17 operation responses are missing: '+missingOps.join(', ')+'.');const iteration=latestIteration(project,[17]);const ev=evaluateIteration(project,recordId(iteration,'iterations'),'CORRECTED');if(!ev.complete)reasons.push(...ev.reasons);break;}"
          t=once(t,old,new,'stage17 operations')
          old="case 19:{requireAccepted();const iteration=latestIteration(project,[19]);const ev=evaluateIteration(project,recordId(iteration,'iterations'),'UNCHANGED_CONFIRMATION');if(!ev.complete)reasons.push(...ev.reasons);requireCount('confirmationRecords',1);if(collection('confirmationRecords').some(r=>upper(recordValue(r,'DETERMINATION'))!=='SATISFIED'))reasons.push('Unchanged confirmation is not affirmatively satisfied.');break;}"
          new="case 19:{requireAccepted();const missingOps=schema.STAGE_CONTRACTS[19].operations.filter(operation=>!changes.some(change=>acceptedChangeOperation(project,change)===operation));if(missingOps.length)reasons.push('Required Stage 19 operation responses are missing: '+missingOps.join(', ')+'.');const iteration=latestIteration(project,[19]);const ev=evaluateIteration(project,recordId(iteration,'iterations'),'UNCHANGED_CONFIRMATION');if(!ev.complete)reasons.push(...ev.reasons);requireCount('confirmationRecords',1);if(collection('confirmationRecords').some(r=>upper(recordValue(r,'DETERMINATION'))!=='SATISFIED'))reasons.push('Unchanged confirmation is not affirmatively satisfied.');break;}"
          t=once(t,old,new,'stage19 operations')
          t=once(t,"unresolvedHumanRequests,openBlockers,acceptedChanges,hasStageActivity,mandatoryRequirements", "unresolvedHumanRequests,openBlockers,acceptedChanges,acceptedChangeOperation,acceptedChangeScope,nextPromptOperation,hasStageActivity,mandatoryRequirements",'engine exports')
          p.write_text(t)

          p=Path('response-ingestion.js'); t=p.read_text()
          old="else if(type==='DATE'){if(typeof value!=='string'||!/^\\d{4}-\\d{2}-\\d{2}$/.test(value)||Number.isNaN(Date.parse(value+'T00:00:00Z')))fail('An ISO YYYY-MM-DD date is required.');}"
          new="else if(type==='DATE'){const parsed=typeof value==='string'&&/^\\d{4}-\\d{2}-\\d{2}$/.test(value)?new Date(value+'T00:00:00Z'):null;if(!parsed||Number.isNaN(parsed.getTime())||parsed.toISOString().slice(0,10)!==value)fail('A real ISO YYYY-MM-DD date is required.');}"
          t=once(t,old,new,'strict date')
          old="receiptId,jobId:project.job.JOB_ID,stage:Number(stage),role:globalThis.closedLoopCore?.STAGES?.[Number(stage)-1]?.role||'UNKNOWN',contextId:'UNKNOWN',\n    iteration:project.job.CURRENT_ITERATION||'NOT APPLICABLE',runId:'NOT APPLICABLE',"
          new="receiptId,jobId:project.job.JOB_ID,stage:Number(stage),role:globalThis.closedLoopCore?.STAGES?.[Number(stage)-1]?.role||'UNKNOWN',contextId:promptRecord.scope?.contextId||'NOT APPLICABLE',\n    iteration:promptRecord.scope?.iterationId||project.job.CURRENT_ITERATION||'NOT APPLICABLE',runId:promptRecord.scope?.runId||'NOT APPLICABLE',"
          t=once(t,old,new,'receipt scope')
          old="if(canonical.updateTargetId){const target=next.projectData[collection].find(r=>workflow.recordId(r,collection)===canonical.id&&workflow.isActiveRecord(r));if(!target)throw new Error(`Reserved target disappeared: ${canonical.id}.`);target.fields="
          new="if(canonical.updateTargetId){const target=next.projectData[collection].find(r=>workflow.recordId(r,collection)===canonical.id&&workflow.isActiveRecord(r));if(!target)throw new Error(`Reserved target disappeared: ${canonical.id}.`);target.lastAgentUpdate={proposalId,rawResponseId:proposal.rawResponseId,fields:clone(target.fields||{}),relationships:clone(target.relationships||{}),evidenceRefs:clone(target.evidenceRefs||[]),status:target.status,completionState:target.completionState};target.fields="
          t=once(t,old,new,'reserved snapshot')
          old="stage,responseType:'DATA_PROPOSAL',operatorLabel:operator,"
          new="stage,operation:proposal.envelope?.operation||'COMPLETE',scope:clone(proposal.envelope?.scope||proposal.scope||{}),responseType:'DATA_PROPOSAL',operatorLabel:operator,"
          t=once(t,old,new,'accepted operation scope')
          p.write_text(t)

          p=Path('prompt-engine.js'); t=p.read_text()
          old="function scopeFor(stage,state,overrides={}){const j=state?.job||{};const value={projectRevision:Number(state?.revision||0),inputVersion:j.CURRENT_INPUT_VERSION||null,sourceSetVersion:j.CURRENT_SOURCE_SET_VERSION||null,requirementsVersion:j.CURRENT_REQUIREMENTS_VERSION||null,testSuiteVersion:j.CURRENT_TEST_SUITE_VERSION||null,instructionVersion:j.CURRENT_INSTRUCTION_VERSION||null,iterationId:j.CURRENT_ITERATION||null,candidateId:state?.projectData?.candidateFreezes?.filter(x=>x?.active!==false&&!x?.invalidatedBy).at(-1)?.id||null,runId:overrides.runId||null,contextId:overrides.contextId||null,baselineId:j.CURRENT_BASELINE_ID&&!['NONE','NOT APPLICABLE'].includes(String(j.CURRENT_BASELINE_ID))?j.CURRENT_BASELINE_ID:null,productId:j.CURRENT_PRODUCT_ID&&!['NONE','NOT APPLICABLE'].includes(String(j.CURRENT_PRODUCT_ID))?j.CURRENT_PRODUCT_ID:null};return value;}"
          new="function scopeFor(stage,state,overrides={}){const j=state?.job||{},pick=(key,fallback)=>Object.prototype.hasOwnProperty.call(overrides,key)?overrides[key]:fallback;return {projectRevision:Number(pick('projectRevision',state?.revision||0)),inputVersion:pick('inputVersion',j.CURRENT_INPUT_VERSION||null),sourceSetVersion:pick('sourceSetVersion',j.CURRENT_SOURCE_SET_VERSION||null),requirementsVersion:pick('requirementsVersion',j.CURRENT_REQUIREMENTS_VERSION||null),testSuiteVersion:pick('testSuiteVersion',j.CURRENT_TEST_SUITE_VERSION||null),instructionVersion:pick('instructionVersion',j.CURRENT_INSTRUCTION_VERSION||null),iterationId:pick('iterationId',j.CURRENT_ITERATION||null),candidateId:pick('candidateId',state?.projectData?.candidateFreezes?.filter(x=>x?.active!==false&&!x?.invalidatedBy).at(-1)?.id||null),runId:pick('runId',null),contextId:pick('contextId',null),baselineId:pick('baselineId',j.CURRENT_BASELINE_ID&&!['NONE','NOT APPLICABLE'].includes(String(j.CURRENT_BASELINE_ID))?j.CURRENT_BASELINE_ID:null),productId:pick('productId',j.CURRENT_PRODUCT_ID&&!['NONE','NOT APPLICABLE'].includes(String(j.CURRENT_PRODUCT_ID))?j.CURRENT_PRODUCT_ID:null)};}"
          t=once(t,old,new,'scope overrides')
          old="if(answered.length)parts.push(`ANSWERED HUMAN CLARIFICATIONS\\n${show(answered)}`);\n for(const collection"
          new="if(answered.length)parts.push(`ANSWERED HUMAN CLARIFICATIONS\\n${show(answered)}`);\n const correction=(state?.projectData?.history||[]).filter(x=>x.type==='ACCEPTED_RESPONSE_INVALIDATED'&&Number(x.stage)===Number(stage)).at(-1);if(correction)parts.push(`OPERATOR CORRECTION / REFINEMENT REQUEST\\n${show({reason:correction.reason,operatorLabel:correction.operatorLabel||'UNSPECIFIED',eventSequence:correction.eventSequence})}`);\n for(const collection"
          t=once(t,old,new,'correction context')
          p.write_text(t)

          p=Path('app-core.js'); t=p.read_text()
          t=once(t,"case 'BOOLEAN':return `<select data-human-answer=\"${id}\"><option value=\"\"></option><option value=\"true\">true</option><option value=\"false\">false</option></select>`;","case 'BOOLEAN':return `<select data-human-answer=\"${id}\" data-answer-type=\"BOOLEAN\"><option value=\"\"></option><option value=\"true\">True</option><option value=\"false\">False</option></select>`;",'boolean control')
          old="x.multiple?[...x.selectedOptions].map(o=>o.value):x.type==='number'?(x.value===''?'':Number(x.value)):x.value"
          new="x.multiple?[...x.selectedOptions].map(o=>o.value):x.dataset.answerType==='BOOLEAN'?(x.value===''?'':x.value==='true'):x.type==='number'?(x.value===''?'':Number(x.value)):x.value"
          t=once(t,old,new,'boolean conversion')
          old="function currentStagePrompt(n){const saved=currentPromptRecord(n);if(saved?.prompt)return saved.prompt;const preview=clone(current);preview.revision=Number(current.revision||0)+1;return globalThis.closedLoopPromptEngine.buildPromptRecord(n,preview).prompt;}"
          new="function currentStagePrompt(n){const saved=currentPromptRecord(n);if(saved?.prompt)return saved.prompt;const plan=engine.nextPromptOperation(current,n);if(plan.blockedReason)return `APPLICATION PREREQUISITE REQUIRED\\n${plan.blockedReason}`;const preview=clone(current);preview.revision=Number(current.revision||0)+1;return globalThis.closedLoopPromptEngine.buildPromptRecord(n,preview,{operation:plan.operation,scope:plan.scope}).prompt;}"
          t=once(t,old,new,'current prompt operation')
          old="const candidate=globalThis.closedLoopPromptEngine.buildPromptRecord(n,preview),next=clone(current),record={...candidate,generatedAt:new Date().toISOString(),iteration:current.job.CURRENT_ITERATION||'NOT APPLICABLE'};"
          new="const plan=engine.nextPromptOperation(preview,n);if(plan.blockedReason)throw new Error(plan.blockedReason);const candidate=globalThis.closedLoopPromptEngine.buildPromptRecord(n,preview,{operation:plan.operation,scope:plan.scope}),next=clone(current),record={...candidate,generatedAt:new Date().toISOString(),iteration:current.job.CURRENT_ITERATION||'NOT APPLICABLE'};"
          t=once(t,old,new,'save prompt operation')
          t=once(t,"const applicable=[10,20,21,25,28].includes(n)","const applicable=[10,17,19,20,21,25,28].includes(n)",'artifact stages')
          old="${n===10?`<button id=\"freeze-candidate\"${locked?' disabled':''}>Freeze selected candidate</button>`:''}"
          new="${[10,17,19].includes(n)?`<button id=\"freeze-candidate\"${locked?' disabled':''}>${n===17?'Freeze corrected candidate':n===19?'Start unchanged confirmation iteration':'Freeze selected candidate'}</button>`:''}"
          t=once(t,old,new,'candidate UI stages')
          old="engine.freezeCandidate(next,{artifactIds:selectedArtifactIds(),operatorLabel:$('#operator-label')?.value.trim()||'HUMAN_OPERATOR'});"
          new="let artifactIds=selectedArtifactIds();if(current.activeStage===19&&!artifactIds.length){const prior=engine.records(next,'candidateFreezes').at(-1),manifest=recordValue(prior,'COMPONENT_MANIFEST');artifactIds=safe(manifest).map(item=>String(item.artifactId||'')).filter(Boolean);}engine.freezeCandidate(next,{stage:current.activeStage,artifactIds,operatorLabel:$('#operator-label')?.value.trim()||'HUMAN_OPERATOR'});"
          t=once(t,old,new,'candidate wire')
          old="<button id=\"request-correction\">Request correction</button></div></div>`;}"
          new="<button id=\"request-correction\">Request correction</button></div><div class=\"field\"><label>Correction / refinement requested</label><textarea id=\"proposal-correction-reason\" placeholder=\"State exactly what is missing, wrong, or should be more complete.\"></textarea></div></div>`;}"
          t=once(t,old,new,'proposal correction field')
          old="reason:requestCorrection?'Correction requested after proposal review.':'Rejected after proposal review.'"
          new="reason:requestCorrection?($('#proposal-correction-reason')?.value.trim()||'Correction requested after proposal review.'):'Rejected after proposal review.'"
          t=once(t,old,new,'proposal correction reason')
          old="<p class=\"section-intro\">Agent-owned and application-derived data are read-only after validated ingestion. Advanced corrections require a controlled change record.</p>"
          new="<p class=\"section-intro\">Agent-owned and application-derived data are read-only after validated ingestion. If the accepted response is incomplete or wrong, request a corrected response below; the application invalidates that response and dependent work without rewriting history.</p>${engine.acceptedChanges(current,n).length?`<div class=\"field\"><label>Correction / refinement requested</label><textarea id=\"accepted-correction-reason\" placeholder=\"State exactly what is missing, wrong, or should be more complete.\"></textarea></div><div class=\"button-row\"><button id=\"invalidate-accepted-response\">Request corrected / more complete response</button></div>`:''}"
          t=once(t,old,new,'accepted correction UI')
          old="if($('#request-correction'))$('#request-correction').onclick=()=>rejectPendingProposal(true);"
          new="if($('#request-correction'))$('#request-correction').onclick=()=>rejectPendingProposal(true);if($('#invalidate-accepted-response'))$('#invalidate-accepted-response').onclick=async()=>{const reason=$('#accepted-correction-reason')?.value.trim();if(!reason){alert('Describe what must be corrected or made more complete.');return;}try{const next=clone(current),change=engine.acceptedChanges(next,current.activeStage).at(-1);engine.invalidateAcceptedResponse(next,{stage:current.activeStage,rawResponseId:change?.rawResponseId,reason,operatorLabel:$('#operator-label')?.value.trim()||'HUMAN_OPERATOR'});await persistReplacement(next);announce('response invalidated for correction');render();}catch(error){alert(error.message||error);}};"
          t=once(t,old,new,'accepted correction wire')
          old="function workflow(){const n=current.activeStage,d=core.STAGES[n-1],s=current.stages[n],lock=stageLocked(n),locked=Boolean(lock)&&s.status!=='COMPLETE',prompt=currentStagePrompt(n),responseLocked=locked||s.status==='COMPLETE';"
          new="function workflow(){const n=current.activeStage,d=core.STAGES[n-1],s=current.stages[n],lock=stageLocked(n),locked=Boolean(lock)&&s.status!=='COMPLETE',plan=engine.nextPromptOperation(current,n),prompt=currentStagePrompt(n),responseLocked=locked||s.status==='COMPLETE'||Boolean(plan.blockedReason);"
          t=once(t,old,new,'workflow plan')
          old="<div class=\"panel\"><h2 class=\"section-title\">Generated instruction</h2><p class=\"section-intro\">This exact saved instruction is the controlling request. Its identity and strict response contract are embedded below.</p>"
          new="<div class=\"panel\"><h2 class=\"section-title\">Generated instruction</h2><p class=\"section-intro\">Current operation: ${esc(plan.operation)}. This exact saved instruction is the controlling request. Its identity and strict response contract are embedded below.</p>${plan.blockedReason?`<div class=\"notice warn\">${esc(plan.blockedReason)}</div>`:''}"
          t=once(t,old,new,'operation wording')
          p.write_text(t)

          p=Path('verify-complete.mjs'); t=p.read_text()
          old="p.projectData.rawResponses.push({rawResponseId:`RAW-STAGE11-${i}`,stage:11});p.projectData.outputReceipts.push({receiptId:`RECEIPT-STAGE11-${i}`,stage:11});"
          new="p.projectData.rawResponses.push({rawResponseId:`RAW-STAGE11-${i}`,stage:11,promptScope:{iterationId:'ITERATION-STAGE11',candidateId:'CANDIDATE-STAGE11',runId:`RUN-STAGE11-${i}`,contextId:`CONTEXT-STAGE11-${i}`}});p.projectData.outputReceipts.push({receiptId:`RECEIPT-STAGE11-${i}`,stage:11,rawResponseId:`RAW-STAGE11-${i}`,acceptedCanonicalChangeId:`CHANGE-STAGE11-${i}`,iteration:'ITERATION-STAGE11',runId:`RUN-STAGE11-${i}`,contextId:`CONTEXT-STAGE11-${i}`});p.projectData.acceptedChanges.push({changeId:`CHANGE-STAGE11-${i}`,rawResponseId:`RAW-STAGE11-${i}`,stage:11,status:'COMMITTED',responseType:'DATA_PROPOSAL',operation:'COMPLETE',scope:{iterationId:'ITERATION-STAGE11',candidateId:'CANDIDATE-STAGE11',runId:`RUN-STAGE11-${i}`,contextId:`CONTEXT-STAGE11-${i}`}});"
          t=once(t,old,new,'stage11 exact proof fixture')
          insert="""

// Final operator-path audit: run reservation derives candidate; scope overrides are exact; accepted-response invalidation removes same-stage authority.
{
  const p=project('JOB-FINAL-OPERATOR');p.job.CURRENT_ITERATION='ITERATION-OP';const it=record('iterations',17,{CANDIDATE_ID:'CANDIDATE-OP',STATUS:'FROZEN'},'ITERATION-OP');it.scope={...engine.currentScope(p),iterationId:'ITERATION-OP',candidateId:'CANDIDATE-OP'};p.projectData.iterations.push(it);const slots=engine.reserveRunBatch(p,{stage:17,count:10});assert(slots.length===10&&p.projectData.runs.every(r=>engine.recordValue(r,'CANDIDATE_ID')==='CANDIDATE-OP'),'Run reservation did not derive the frozen candidate.');const scoped=prompts.scopeFor(17,p,{iterationId:'ITERATION-X',candidateId:'CANDIDATE-X',runId:'RUN-X',contextId:'CONTEXT-X'});assert(scoped.iterationId==='ITERATION-X'&&scoped.candidateId==='CANDIDATE-X'&&scoped.runId==='RUN-X'&&scoped.contextId==='CONTEXT-X','Prompt scope overrides were ignored.');
  const req=record('requirements',4,{OBLIGATION:'invalidated',REQUIREMENT_TYPE:'FUNCTIONAL',MANDATORY_OPTIONAL_STATUS:'MANDATORY',APPLICABILITY:'APPLICABLE',OBSERVABLE_SATISFACTION_CONDITION:'yes',INTENDED_VERIFICATION_METHOD:'test',EXPECTED_EVIDENCE:'e',FAILURE_CONDITION:'f',SEVERITY:'MAJOR',STATUS:'ACTIVE'},'REQ-INVALIDATE');req.rawResponseId='RAW-INVALIDATE';req.sourceProposalId='PROPOSAL-INVALIDATE';p.projectData.requirements.push(req);p.projectData.responseProposals.push({proposalId:'PROPOSAL-INVALIDATE',stage:4,status:'ACCEPTED',proposedStageData:{ATOMICITY_REVIEW_RESULTS:'old'},canonicalRecords:{requirements:[{id:'REQ-INVALIDATE'}]}});p.projectData.acceptedChanges.push({changeId:'CHANGE-INVALIDATE',rawResponseId:'RAW-INVALIDATE',proposalId:'PROPOSAL-INVALIDATE',stage:4,status:'COMMITTED',responseType:'DATA_PROPOSAL',eventSequence:1});p.stages[4].agentData={ATOMICITY_REVIEW_RESULTS:'old'};p.stages[4].acceptedData=p.stages[4].agentData;p.stages[4].acceptedDataChangeIds=['CHANGE-INVALIDATE'];p.stages[4].acceptedResponseIds=['RAW-INVALIDATE'];engine.invalidateAcceptedResponse(p,{stage:4,rawResponseId:'RAW-INVALIDATE',reason:'Need a more complete requirement.'});assert(req.active===false&&!Object.keys(p.stages[4].agentData).length&&!p.stages[4].acceptedResponseIds.length,'Accepted-response invalidation left same-stage canonical authority active.');
}
"""
          t=once(t,"\nconsole.log(JSON.stringify({finalRequirementRegression:true",insert+"\nconsole.log(JSON.stringify({finalRequirementRegression:true",'final operator tests')
          p.write_text(t)

          p=Path('verify-ingestion.mjs'); t=p.read_text()
          insert="""

// Invalid calendar dates fail closed instead of being normalized by Date.parse.
{
  let p=core.createBlankState('JOB-DATE-STRICT');p.job.EXACT_USER_OBJECTIVE_VERBATIM='date validation';engine.ensureShape(p);engine.recalculate(p);const pr={...prompts.buildPromptRecord(1,p),generatedAt:new Date().toISOString()};p.projectData.generatedPrompts.push(pr);const q={schema:schema.RESPONSE_SCHEMA,jobId:p.job.JOB_ID,stage:1,operation:pr.operation,promptIdentity:{instructionId:pr.instructionId,bodySha256:pr.bodySha256,contractSha256:pr.contractSha256,contextSignature:pr.contextSignature},scope:pr.scope,responseType:'HUMAN_INPUT_REQUIRED',humanInputRequests:[{temporaryKey:'q-date',question:'Choose a real date.',whyRequired:'Date is required.',affectedStageFields:[],affectedRecords:[],answerType:'DATE',allowedValues:[],blocking:true}],stageData:{},records:{},evidence:[],unresolved:[],warnings:[],attachments:[]};let prepared=ingestion.prepare(p,{stage:1,text:JSON.stringify(q),promptRecord:pr});p=ingestion.commit(prepared.project,prepared.proposal.proposalId,{operator:'VERIFY'}).project;const request=p.projectData.humanInputRequests.at(-1);let rejected=false;try{ingestion.answerHumanInput(p,{[request.requestId]:'2026-02-31'},{operator:'VERIFY'});}catch(error){rejected=error.code==='INVALID_HUMAN_ANSWER';}assert(rejected,'Impossible ISO calendar date was accepted.');
}
"""
          t=once(t,"\nconsole.log(JSON.stringify({",insert+"\nconsole.log(JSON.stringify({",'strict date test')
          p.write_text(t)
          PY
      - name: Verify source and lifecycle
        run: |
          node --check workflow-engine.js
          node --check response-ingestion.js
          node --check prompt-engine.js
          node --check app-core.js
          node --check verify-complete.mjs
          node --check verify-ingestion.mjs
          node verify.mjs
          node verify-ingestion.mjs
          node verify-complete.mjs
          node verify-full-cycle.mjs
          node verify-prompt-semantics.mjs
      - name: Commit clean runtime delta
        run: |
          git rm .github/workflows/final-audit-integration.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add workflow-engine.js response-ingestion.js prompt-engine.js app-core.js verify-complete.mjs verify-ingestion.mjs
          git commit -m 'Close final operator recovery and operation-flow gaps'
          git push origin HEAD:final-audit-minimum-fixes
