name: Export blind-context source
on:
  push:
    branches:
      - finish/blind-context-and-prompt-authority-43
permissions:
  contents: read
jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Stage exact source
        shell: bash
        run: |
          mkdir -p /tmp/closed-loop-blind-source
          rsync -a --exclude='.git' ./ /tmp/closed-loop-blind-source/
      - uses: actions/upload-artifact@v4
        with:
          name: closed-loop-blind-source
          path: /tmp/closed-loop-blind-source
          include-hidden-files: true
          retention-days: 1
