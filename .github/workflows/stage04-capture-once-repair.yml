name: Stage 04 capture-once repair

on:
  push:
    branches:
      - fix/stage04-capture-once

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo stage04-repair-runner-ok
