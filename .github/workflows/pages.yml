name: Deploy application
on:
  push:
    branches: [main, finish/final-live-stage-walkthrough-20260830]
  pull_request:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: false
jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "application deployment ready"
      - name: Export exact branch source
        if: github.ref == 'refs/heads/finish/final-live-stage-walkthrough-20260830'
        shell: bash
        run: |
          mkdir -p /tmp/closed-loop-final-source
          rsync -a --exclude='.git' ./ /tmp/closed-loop-final-source/
      - uses: actions/upload-artifact@v4
        if: github.ref == 'refs/heads/finish/final-live-stage-walkthrough-20260830'
        with:
          name: closed-loop-final-source
          path: /tmp/closed-loop-final-source
          include-hidden-files: true
          retention-days: 1
  deploy:
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: test
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - name: Build static application
        run: |
          rm -rf _site
          mkdir _site
          cp index.html workbook.js hash.js workflow-schema.js test-runtime.js test-worker.js workflow-engine.js prompt-engine.js response-ingestion.js project-store.js app-core.js TEST_PROJECT.json AUTHORIZED_OPERATION_01.txt .nojekyll _site/
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: Deploy application
        id: deployment
        uses: actions/deploy-pages@v4
