name: Test real workflow engine and deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  test-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run real 30-stage engine E2E job
        run: node e2e-test.mjs
      - name: Build exact standalone deployment
        run: |
          rm -rf dist
          mkdir -p dist
          cp index.html app-core.js dist/
          printf '' > dist/.nojekyll
      - uses: actions/configure-pages@v5
      - name: Upload tested application
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - name: Deploy tested application
        id: deployment
        uses: actions/deploy-pages@v4
