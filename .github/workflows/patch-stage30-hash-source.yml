name: Correct Stage 30 hash source (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm migration status
        run: echo 'The exact generated app file is already the verified Stage 30 hash source. This historical migration no longer mutates main automatically.'
