name: Agent dispatch migration retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm migration status
        run: echo 'Stage-bound agent dispatch is already present in the verified release. This retired migration does not modify main.'
