name: Temporary audit source export
on:
  push:
    branches:
      - audit/integrity-source-export-20260826
permissions:
  contents: read
jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: audit-integrity-source-32202649
          path: .
          include-hidden-files: true
          retention-days: 1
