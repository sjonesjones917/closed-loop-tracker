name: One-time exact scope matrix repair
on:
  push:
    branches: [repair/spec-scope-current-20260903]
permissions:
  contents: write
jobs:
  repair:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: repair/spec-scope-current-20260903
          fetch-depth: 0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: '22'
      - name: Preserve workflow-controlled files during source repair
        run: |
          cp .github/workflows/pages.yml /tmp/pages.yml
          cp .github/workflows/repair-scope.yml /tmp/repair-scope.yml
          cp .repair-scope-contract.mjs /tmp/repair-scope-contract.mjs
      - name: Apply exact scope matrix correction
        run: node .repair-scope-contract.mjs
      - name: Restore workflow-controlled files before source commit
        run: |
          cp /tmp/pages.yml .github/workflows/pages.yml
          cp /tmp/repair-scope.yml .github/workflows/repair-scope.yml
          cp /tmp/repair-scope-contract.mjs .repair-scope-contract.mjs
      - name: Prove repaired owner files and regression
        run: |
          set -euo pipefail
          node --check workflow-schema.js
          node --check prompt-engine.js
          node --check response-ingestion.js
          node verify-stage-operation-scope-matrix.mjs
      - name: Commit only canonical source repair
        run: |
          set -euo pipefail
          git config user.name closed-loop-repair
          git config user.email closed-loop-repair@users.noreply.github.com
          git add workflow-schema.js prompt-engine.js response-ingestion.js verify-stage-operation-scope-matrix.mjs
          git diff --cached --check
          git commit -m "Conform exact stage-operation scope matrix"
          git push origin HEAD:repair/spec-scope-current-20260903
