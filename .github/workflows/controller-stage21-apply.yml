name: Controller Stage 21 convergence applicator
on:
  push:
    branches:
      - controller-v2/stage21-convergence-20260904
permissions:
  contents: write
jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply Stage 21 convergence correction
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re

          engine_path=Path('workflow-engine.js')
          engine=engine_path.read_text()

          marker='function coverageMetrics(project,iterationIdOverride=null)'
          if marker not in engine:
              raise SystemExit('coverageMetrics marker missing')
          helper=r'''function acceptedEmptyUniverseReview(project,metricId,scopeRule=currentScope(project){
}'''
          PY
