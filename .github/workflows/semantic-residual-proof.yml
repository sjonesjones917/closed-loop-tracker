name: Adjudication residual proof (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Disabled; coordinated proof workflow is authoritative.'
