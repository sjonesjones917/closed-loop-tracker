name: Apply semantic adjudication patch (disabled)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary competing fixup workflow disabled; adjudication residual proof is authoritative.'
