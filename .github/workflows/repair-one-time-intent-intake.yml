name: One-time intent intake repair
on:
  push:
    branches:
      - fix/one-time-intent-intake
permissions:
  contents: write
jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/one-time-intent-intake
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply and verify the repair
        run: |
          python3 .github/repair-one-time-intent-intake.py
          node --check workflow-schema.js
          node --check workflow-engine.js
          node --check prompt-engine.js
          node --check response-ingestion.js
          node --check app-core.js
          node --check verify-one-time-intent-intake.mjs
          node verify-one-time-intent-intake.mjs
          node verify-ingestion.mjs
          node verify-full-cycle.mjs
          node verify-prompt-semantics.mjs
      - name: Commit the verified repair
        run: |
          rm .github/repair-one-time-intent-intake.py
          rm .github/workflows/repair-one-time-intent-intake.yml
          git config user.name closed-loop-repair
          git config user.email actions@users.noreply.github.com
          git add -A
          git commit -m 'Fix one-time intent intake contract'
          git push origin HEAD:fix/one-time-intent-intake