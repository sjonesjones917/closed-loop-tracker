# Exact current-main proof

This branch changes no runtime source. Its only purpose is to trigger the repository's existing pull-request acceptance workflow against the exact `main` application tree at `f2a660e2c2e89a800018b462a2b44a954d1a503e`.

The proof marker must not be merged. Remove it after the exact test evidence is recorded.
