import fs from 'node:fs';
import vm from 'node:vm';
import {envelope,proposal,targetUpdate,refId,refTemp,evidence} from './test-fixtures.mjs';

globalThis.Event=globalThis.Event||class Event{constructor(type){this.type=type;}};
globalThis.dispatchEvent=globalThis.dispatchEvent||(()=>true);
for(const file of ['workbook.js','hash.js','workflow-schema.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js'])vm.runInThisContext(fs.readFileSync(file,'utf8'),{filename:file});
const core=globalThis.closedLoopCore,schema=globalThis.closedLoopWorkflowSchema,engine=globalThis.closedLoopWorkflowEngine,prompts=globalThis.closedLoopPromptEngine,ingestion=globalThis.closedLoopResponseIngestion,hash=globalThis.closedLoopHash;
if(!core||!schema||!engine||!prompts||!ingestion||!hash)throw new Error('Responsible-layer runtime failed to load.');
const assert=(v,m)=>{if(!v)throw new Error(m);};
const clone=v=>JSON.parse(JSON.stringify(v));
let project=core.createBlankState('JOB-FULL-CYCLE');
project.job.JOB_ID='JOB-FULL-CYCLE';
project.job.JOB_TITLE='Continuous 30-stage acceptance fixture';
project.job.EXACT_USER_OBJECTIVE_VERBATIM='Produce and verify one deterministic closed-loop acceptance artifact.';
project.job.EXACT_DELIVERABLE_REQUESTED='One verified acceptance artifact.';
project.job.SUPPLIED_MATERIALS_INVENTORY='Synthetic fixture material.';
project.job.CURRENT_INPUT_VERSION='INPUT-v001';
engine.ensureShape(project);engine.recordHumanInputVersion(project,['EXACT_USER_OBJECTIVE_VERBATIM','EXACT_DELIVERABLE_REQUESTED'],'FULL_CYCLE_OPERATOR');engine.recalculate(project);

const activePrompt=stage=>project.projectData.generatedPrompts.filter(p=>Number(p.stage)===stage&&!p.invalidatedBy).at(-1);
function savePrompt(stage,{operation,scope}={}){const p={...prompts.buildPromptRecord(stage,project,{...(operation?{operation}:{}),...(scope?{scope}:{})}),generatedAt:new Date().toISOString()};project.projectData.generatedPrompts.push(p);project.stages[stage].currentPromptId=p.instructionId;return p;}
function preferredEnum(name,values){const prefs={DETERMINATION:['SATISFIED','ACCEPTED','COMPLETE'],STATUS:['COMPLETE','READY','CURRENT','ACTIVE','RESOLVED'],STATE:['ACTIVE','CURRENT','COMPLETE'],RESULT:['SATISFIED','PASSED','SUCCESS','REJECTED'],INDEPENDENCE_STATUS:['INDEPENDENT'],MANDATORY_OPTIONAL_STATUS:['MANDATORY'],APPLICABILITY:['APPLICABLE'],INSPECTION_STATUS:['INSPECTED'],CURRENCY_STATUS:['CURRENT'],SUPERSESSION_STATUS:['NOT SUPERSEDED'],CONTROLLING_STATE:['CONTROLLING'],PHASE:['PRE_CORRECTION'],SEVERITY:['MINOR']};for(const [needle,candidates] of Object.entries(prefs))if(name.includes(needle))for(const c of candidates)if(values.includes(c))return c;return values[0];}
function typed(def,value){if(value===null||value===undefined)return value;switch(def?.valueType){case 'STRING':case 'REFERENCE':return String(value);case 'INTEGER':return Number.parseInt(value,10);case 'NUMBER':return Number(value);case 'BOOLEAN':return typeof value==='boolean'?value:['TRUE','YES','1','SATISFIED'].includes(String(value).toUpperCase());case 'STRING_ARRAY':case 'REFERENCE_ARRAY':return Array.isArray(value)?value.map(String):[String(value)];default:return value;}}
function valueFor(def,name){if(def?.enumValues?.length)return preferredEnum(name,def.enumValues);switch(def?.valueType){case 'INTEGER':return 1;case 'NUMBER':return 1;case 'BOOLEAN':return true;case 'STRING_ARRAY':case 'REFERENCE_ARRAY':return ['verified'];case 'OBJECT':return {};default:return `verified-${name.toLowerCase()}`;}}
function agentStageData(stage,overrides={}){const out={};for(const [name,def] of Object.entries(schema.STAGE_FIELDS[stage]||{}))if(def.producer===schema.PRODUCER.AGENT)out[name]=valueFor(def,name);for(const [name,value] of Object.entries(overrides)){const def=schema.STAGE_FIELDS[stage]?.[name];if(!def||def.producer!==schema.PRODUCER.AGENT)throw new Error(`Stage ${stage} override ${name} is not agent-owned.`);out[name]=typed(def,value);}return out;}
function agentFields(collection,overrides={}){const def=schema.RECORD_SCHEMAS[collection],out={};for(const name of schema.recordAgentFields(collection)){const fd=def.fieldDefinitions[name];if(def.required.includes(name))out[name]=valueFor(fd,name);}for(const [name,value] of Object.entries(overrides)){const fd=def.fieldDefinitions[name];if(!fd||fd.producer!==schema.PRODUCER.AGENT)throw new Error(`${collection}.${name} is not agent-owned.`);out[name]=typed(fd,value);}return out;}
function canonicalId(collection){return engine.recordId(engine.records(project,collection).at(-1),collection);}
function recordProposal(collection,key,overrides={},relationships={},evidenceRefs=['evidence-1']){return proposal(key,agentFields(collection,overrides),relationships,evidenceRefs);}
function reload(){project=clone(project);engine.ensureShape(project);engine.recalculate(project);}
function accept(stage,{operation,scope,stageData=null,records={},evidenceRecords=null}={}){
 const promptRecord=savePrompt(stage,{operation,scope});const body=envelope(schema,project,stage,promptRecord,{stageData:stageData??agentStageData(stage),records,evidenceRecords:evidenceRecords??[evidence('evidence-1',`Stage ${stage} controlled evidence`)]});const raw=JSON.stringify(body),acceptedBefore=project.projectData.acceptedChanges.length,canonicalBefore=Object.fromEntries(Object.keys(schema.RECORD_SCHEMAS).map(c=>[c,engine.records(project,c,{active:false}).length]));
 const prepared=ingestion.prepare(project,{stage,text:raw,promptRecord});assert(prepared.validation?.valid,`Stage ${stage} ${operation||promptRecord.operation} rejected: ${JSON.stringify(prepared.validation?.issues)}`);assert(prepared.proposal?.status==='PENDING_OPERATOR_REVIEW',`Stage ${stage} did not produce a pending proposal.`);assert(prepared.project.projectData.acceptedChanges.length===acceptedBefore,`Stage ${stage} mutated accepted state before acceptance.`);for(const [c,n] of Object.entries(canonicalBefore))assert(engine.records(prepared.project,c,{active:false}).length===n,`Stage ${stage} mutated ${c} before acceptance.`);
 const committed=ingestion.commit(prepared.project,prepared.proposal.proposalId,{operator:'FULL_CYCLE_OPERATOR',reviewNote:'Controlled full-cycle acceptance.'});project=committed.project;const receipt=project.projectData.outputReceipts.find(r=>r.receiptId===committed.receipt?.receiptId)||project.projectData.outputReceipts.at(-1);assert(project.projectData.rawResponses.some(r=>r.completeRawResponse===raw),`Stage ${stage} raw response was not preserved.`);assert(committed.manifest?.entries?.length>0||(Object.keys(body.stageData).length===0&&Object.keys(body.records).length===0),`Stage ${stage} extraction manifest is empty.`);assert(receipt?.acceptedCanonicalChangeId&&receipt.acceptedCanonicalChangeId!=='NONE',`Stage ${stage} receipt is not linked to accepted change.`);reload();return committed;
}
function complete(stage){engine.recalculate(project);assert(project.stages[stage].status==='COMPLETE',`Stage ${stage} is ${project.stages[stage].status}: ${project.stages[stage].gate.reasons.join('; ')}`);if(stage<30){const next=prompts.buildPromptRecord(stage+1,project);assert(next.prompt.includes(`JOB_ID: ${project.job.JOB_ID}`),'Next prompt lost project identity.');assert(next.prompt.includes('PRIOR STAGE DECISION AND ACCEPTED DATA'),'Next prompt omitted accepted prior-stage context.');}}
function runScope(slot){return {runId:slot.runId,contextId:slot.contextId};}
function fillContexts(stage,slots,prefix){for(let i=0;i<slots.length;i++)engine.registerFreshContext(project,{stage,externalContextIdentifier:`${prefix}-${String(i+1).padStart(2,'0')}`,operatorLabel:'FULL_CYCLE_OPERATOR'});}
function finishRuns(stage,slots,operation){for(let i=0;i<slots.length;i++){const slot=slots[i];accept(stage,{operation,scope:runScope(slot),records:{runs:[targetUpdate(slot.runId,agentFields('runs',{FRESH_CONTEXT_RECORD:slot.contextId,CONTAMINATION_CHECK:'NONE',TOOL_CONFIGURATION:'CONFIG-v1',EXECUTION_STATUS:'COMPLETE',COMPLETE_OUTPUT:`complete-output-${stage}-${i+1}`,OUTPUT_ARTIFACT_IDENTITIES:['NONE'],TOOL_FAILURES:'NONE',NOTES:'Controlled run completed.'}))]}});}}
function verifyRuns(stage,slots,operation,reqId,testId){for(let i=0;i<slots.length;i++){const slot=slots[i];accept(stage,{operation,scope:runScope(slot),records:{verification:[recordProposal('verification',`verification-${stage}-${i+1}`,{VERIFIER:'INDEPENDENT-VERIFIER',VERIFIER_CONTEXT_ID:`VERIFY-CONTEXT-${stage}-${i+1}`,INDEPENDENCE_STATUS:'INDEPENDENT',INPUTS:`Run ${slot.runId}`,PROCEDURE:'Apply the current deterministic test.',EXPECTED_RESULT:'SATISFIED',OBSERVED_RESULT:'SATISFIED',EXACT_EVIDENCE:`Exact evidence for ${slot.runId}`,DETERMINATION:'SATISFIED',UNDETERMINED_REASON:'NONE'},{REQ_ID:refId(reqId),RUN_ID:refId(slot.runId),TEST_ID:refId(testId)})]}});}}
function compareRuns(stage,operation,slot,reqId){accept(stage,{operation,scope:slot?runScope(slot):undefined,records:{comparisons:[recordProposal('comparisons',`comparison-${stage}`,{RUN_DETERMINATIONS:'All ten current runs SATISFIED.',INTERPRETATION_VARIANCE:'NONE',OUTPUT_VARIANCE:'NONE',AUTHORIZED_VARIANCE:'FALSE',INCONCLUSIVE_TESTS:'NONE',REPEATED_FAILURE_PATTERNS:'NONE',UNIQUE_FAILURES:'NONE',CORRECTNESS_AFFECTING_VARIANCE:'FALSE',DEFECT_IDS:'NONE',EVIDENCE:'All ten current runs compared.'},{REQ_ID:refId(reqId)})]}});}

// Stage 01: clarification -> typed answer -> regenerated prompt -> final proposal -> bound confirmation.
{
 const pr=savePrompt(1);const q=envelope(schema,project,1,pr,{responseType:'HUMAN_INPUT_REQUIRED',stageData:{},records:{},evidenceRecords:[],humanInputRequests:[{temporaryKey:'question-1',question:'Confirm the controlling jurisdiction label.',whyRequired:'Human authority is required for this label.',affectedStageFields:['EXACT_DELIVERABLE_REQUESTED'],affectedRecords:[],answerType:'TEXT',allowedValues:[],blocking:true}]});const prepared=ingestion.prepare(project,{stage:1,text:JSON.stringify(q),promptRecord:pr});assert(prepared.validation.valid,`Stage 1 clarification rejected: ${JSON.stringify(prepared.validation.issues)}`);project=ingestion.commit(prepared.project,prepared.proposal.proposalId,{operator:'FULL_CYCLE_OPERATOR'}).project;engine.recalculate(project);assert(project.stages[1].status==='BLOCKED','Stage 1 did not block on human clarification.');const request=project.projectData.humanInputRequests.at(-1),before=project.job.CURRENT_INPUT_VERSION;const answered=ingestion.answerHumanInput(project,{[request.requestId]:'United States'},{operator:'FULL_CYCLE_OPERATOR'});project=answered.project;assert(project.job.CURRENT_INPUT_VERSION!==before,'Clarification did not version User Job Input.');const regenerated=activePrompt(1);assert(regenerated&&regenerated.instructionId!==pr.instructionId,'Clarification did not regenerate Stage 1 prompt.');assert(regenerated.prompt.includes('United States'),'Answered clarification is absent from regenerated prompt.');const body=envelope(schema,project,1,regenerated,{stageData:agentStageData(1),records:{},evidenceRecords:[evidence('evidence-1','Stage 1 final evidence')]});const preparedFinal=ingestion.prepare(project,{stage:1,text:JSON.stringify(body),promptRecord:regenerated});assert(preparedFinal.validation.valid,`Stage 1 final response rejected: ${JSON.stringify(preparedFinal.validation.issues)}`);const committed=ingestion.commit(preparedFinal.project,preparedFinal.proposal.proposalId,{operator:'FULL_CYCLE_OPERATOR'});project=committed.project;engine.recordStageConfirmation(project,1,true,'The represented objective and deliverable match human intent.','FULL_CYCLE_OPERATOR',{acceptedChangeId:committed.change?.changeId,inputVersion:project.job.CURRENT_INPUT_VERSION,instructionId:regenerated.instructionId,contextSignature:regenerated.contextSignature});complete(1);
}

accept(2,{records:{sources:[recordProposal('sources','source-1',{TITLE:'Web Content Accessibility Guidelines (WCAG) 2.2',ISSUING_ORGANIZATION_OR_AUTHOR:'World Wide Web Consortium',SOURCE_TYPE:'OFFICIAL_STANDARD',PUBLICATION_ORIGIN:'W3C Recommendation',URL_REFERENCE:'https://www.w3.org/TR/WCAG22/',AUTHORITY_LEVEL:'PRIMARY TECHNICAL AUTHORITY',AUTHORITY_ROLE:'GOVERNING',RELEVANCE:'Applicable accessibility authority',APPLICABLE_PORTIONS:'Normative conformance requirements',INSPECTION_STATUS:'INSPECTED',CURRENCY_STATUS:'CURRENT',SUPERSESSION_STATUS:'NOT SUPERSEDED',CONTROLLING_STATE:'CONTROLLING WHERE APPLICABLE'})]}});complete(2);const sourceId=canonicalId('sources');
accept(3,{records:{research:[recordProposal('research','research-1',{PASS_NUMBER:'1',EXACT_PORTION_EXAMINED:'Normative conformance requirements',FINDING_CLASSIFICATION:'FACT',SOURCE_EVIDENCE:'Controlled Stage 3 research evidence'},{SOURCE_ID:refId(sourceId)})]}});complete(3);
accept(4,{records:{requirements:[recordProposal('requirements','requirement-1',{OBLIGATION:'The delivered artifact must preserve the declared deterministic acceptance identity.',REQUIREMENT_TYPE:'FUNCTIONAL',MANDATORY_OPTIONAL_STATUS:'MANDATORY',SOURCE_LOCATION:'WCAG 2.2 conformance context',SOURCE_AUTHORITY:'PRIMARY TECHNICAL AUTHORITY',USER_INPUT_RELATIONSHIP:'Current User Job Input',APPLICABILITY:'APPLICABLE',DEPENDENCIES:'NONE',PROHIBITIONS:'No silent mutation',DEFINED_TERMS:'Acceptance identity',OBSERVABLE_SATISFACTION_CONDITION:'Stored and released identities match.',INTENDED_VERIFICATION_METHOD:'Deterministic identity comparison',EXPECTED_EVIDENCE:'Canonical hashes and evidence record',FAILURE_CONDITION:'Any identity mismatch',SEVERITY:'MINOR',NOTES:'Synthetic full-cycle requirement'},{SOURCE_ID:refId(sourceId)})]}});complete(4);const reqId=canonicalId('requirements');
accept(5);complete(5);
accept(6,{records:{tests:[recordProposal('tests','test-1',{TEST_TYPE:'DETERMINISTIC',INPUTS:'Current artifact bytes',TOOLS:'SHA-256 comparison',PROCEDURE:'Compare canonical byte identity.',EXPECTED_RESULT:'Exact identity match.',FAILURE_CONDITION:'Any mismatch.',EVIDENCE_TO_PRESERVE:'Hashes and comparison record'},{REQ_ID:refId(reqId)})]}});complete(6);const testId=canonicalId('tests');
accept(7,{records:{failureTests:[recordProposal('failureTests','failure-1',{VIOLATION_MODE:'Single-byte mutation',FIXTURE:'Alter one byte.',EXPECTED_REJECTION:'REJECTED',ACTUAL_RESULT:'REJECTED',EVIDENCE:'Controlled invalid fixture was rejected.'},{REQ_ID:refId(reqId)})]}});complete(7);
accept(8,{records:{instructions:[recordProposal('instructions','instruction-1',{OBJECTIVE:'Produce the exact declared artifact.',AUTHORIZED_INPUTS:'Current canonical project inputs.',AUTHORITY_RULES:'Use only current authorized authority.',FAILURE_HANDLING:'Fail closed on uncertainty.',SCOPE:'This project only.',PROHIBITIONS:'No invented evidence.',DEFINED_TERMS:'Canonical means application-controlled.',ORDERED_PROCEDURE:'Read inputs; execute; verify; return structured response.',BRANCHES:'Block when required input is unavailable.',TOOL_REQUIREMENTS:'Use deterministic hashing where applicable.',OUTPUT_CONTRACT:'Return the required structured response.',FACTUAL_STATE_HANDLING:'Preserve TRUE/FALSE/UNKNOWN exactly.',REJECTION_BLOCKING_RULES:'Reject invalid or stale output.',COMPLETION_CONDITIONS:'All current requirements have trace coverage.',REQUIREMENT_TRACEABILITY:'REQ mapped by instructionTraces.',INSTRUCTION_TEXT:'Execute the ordered procedure and return the strict response envelope.'})],instructionTraces:[recordProposal('instructionTraces','trace-1',{INSTRUCTION_LOCATION:'Ordered procedure',IMPLEMENTED_BEHAVIOR:'Preserve exact artifact identity.'},{REQ_ID:refId(reqId),INSTRUCTION_ID:refTemp('instruction-1'),EVIDENCE_ID:refTemp('evidence-1')})]}});complete(8);const instructionId=canonicalId('instructions');
accept(9,{records:{preflightRecords:[recordProposal('preflightRecords','preflight-1',{CLAUSE:'All required instruction sections',MULTIPLE_INTERPRETATIONS:'NONE',UNDEFINED_OBJECTS:'NONE',UNSUPPLIED_DEPENDENCIES:'NONE',INTERNAL_CONFLICTS:'NONE',UNAVAILABLE_CAPABILITIES:'NONE',OBJECTIVELY_VERIFIABLE:'TRUE',RESPONSIBLE_OPERATION_ASSIGNED:'TRUE',ORDER_CLEAR:'TRUE',FAILURE_BEHAVIOR_DEFINED:'TRUE',TRACEABILITY:'COMPLETE',DETERMINATION:'SATISFIED',FINDINGS:'No material finding.',CORRECTIONS:'NONE',EVIDENCE:'Controlled independent preflight evidence'},{INSTRUCTION_ID:refId(instructionId)})]}});complete(9);

const candidate1='ARTIFACT-CANDIDATE-1',candidate1Bytes='candidate-v1';engine.registerArtifactBytes(project,{stage:10,artifactId:candidate1,filename:'candidate-v1.bin',mediaType:'application/octet-stream',byteSize:Buffer.byteLength(candidate1Bytes),sha256:hash.sha256Text(candidate1Bytes),lineage:{fixture:'full-cycle'}});const frozen1=engine.freezeCandidate(project,{stage:10,artifactIds:[candidate1],operatorLabel:'FULL_CYCLE_OPERATOR'});accept(10,{scope:{runId:'FREEZE-NOT-APPLICABLE',contextId:'FREEZE-NOT-APPLICABLE'},records:{},stageData:{}});complete(10);const initialIteration=frozen1.iteration.id,initialCandidate=frozen1.candidate.id;

const slots11=engine.reserveRunBatch(project,{stage:11,iterationId:initialIteration,candidateId:initialCandidate,count:10});fillContexts(11,slots11,'INITIAL-CONTEXT');finishRuns(11,slots11,'COMPLETE');complete(11);
verifyRuns(12,slots11,'COMPLETE',reqId,testId);complete(12);
compareRuns(13,'COMPLETE',null,reqId);complete(13);
accept(14,{records:{defects:[recordProposal('defects','defect-1',{OBSERVED_FAILURE:'Synthetic pre-correction identity mismatch.',EXPECTED_CONDITION:'Exact identity match.',EVIDENCE:'Controlled defect evidence.',SEVERITY:'MINOR',ROOT_CAUSE_CATEGORY:'INSTRUCTION',ROOT_CAUSE:'An instruction-level fixture defect.',CORRECTION:'Correct the fixture instruction.',CHANGED_ARTIFACTS:'candidate-v2.bin',VERIFICATION_RESULT:'PENDING',RELATIONSHIPS:'Current requirement and initial run.'},{REQ_ID:refId(reqId),RUN_ID:refId(slots11[0].runId)})],rootCauses:[recordProposal('rootCauses','rca-1',{CATEGORY:'INSTRUCTION',LAYER_TRACE:'Output -> execution -> instruction.',EARLIEST_DEFECTIVE_LAYER:'INSTRUCTION',ROOT_CAUSE:'Synthetic instruction fixture defect.',EVIDENCE:'Controlled RCA evidence.',DOWNSTREAM_INVALIDATION:'Stages 15 onward require corrected candidate.'},{DEFECT_ID:refTemp('defect-1')})]}});complete(14);const defectId=canonicalId('defects');
accept(15,{records:{regressions:[recordProposal('regressions','regression-1',{FAILURE_FIXTURE:'candidate-v1 identity mismatch fixture',REPRODUCTION_PROCEDURE:'Run the identity comparison.',DETECTION_METHOD:'SHA-256 equality check.',PRE_CORRECTION_RESULT:'FAILED',PRE_CORRECTION_EVIDENCE:'Observed controlled mismatch.',CORRECTION:'Use candidate-v2.',POST_CORRECTION_RESULT:'PENDING ACTUAL EXECUTION',POST_CORRECTION_EVIDENCE:'PENDING',PERMANENT_TEST_LOCATION:'TEST-IDENTITY',APPLICABILITY:'ACTIVE',RETIREMENT_AUTHORITY:'NONE'},{DEFECT_ID:refId(defectId),REQ_ID:refId(reqId)})],regressionExecutions:[recordProposal('regressionExecutions','regexec-pre',{PHASE:'PRE_CORRECTION',RESULT:'FAILED'},{REG_ID:refTemp('regression-1'),ITERATION_ID:refId(initialIteration),CANDIDATE_ID:refId(initialCandidate),EVIDENCE_ID:refTemp('evidence-1')})]}});complete(15);const regId=canonicalId('regressions');
accept(16,{records:{changes:[recordProposal('changes','change-1',{TRIGGERING_DEFECT_IDS:defectId,ROOT_CAUSE_ANALYSIS:'Synthetic instruction defect.',RESPONSIBLE_LAYER:'INSTRUCTION',OLD_ARTIFACT_VERSION:'candidate-v1',EXACT_MODIFICATION:'Correct the deterministic fixture input.',NEW_ARTIFACT_VERSION:'candidate-v2',DOWNSTREAM_INVALIDATION:'Repeat corrected iteration.',REQUIRED_RERUNS:'Ten-run corrected iteration and all verification.',INSTRUCTION_CHANGE_DETERMINATION:'CONTROLLED CHANGE',REQUIRED_REPEATED_PREFLIGHT:'REQUIRED',JUSTIFIED_UNCHANGED_ARTIFACTS:'All unrelated inputs unchanged.',EVIDENCE:'Controlled correction evidence.'})]}});complete(16);

const candidate2='ARTIFACT-CANDIDATE-2',candidate2Bytes='candidate-v2';engine.registerArtifactBytes(project,{stage:17,artifactId:candidate2,filename:'candidate-v2.bin',mediaType:'application/octet-stream',byteSize:Buffer.byteLength(candidate2Bytes),sha256:hash.sha256Text(candidate2Bytes),lineage:{supersedes:candidate1}});const frozen17=engine.freezeCandidate(project,{stage:17,artifactIds:[candidate2],operatorLabel:'FULL_CYCLE_OPERATOR',purpose:'CORRECTED'});const correctedIteration=frozen17.iteration.id,correctedCandidate=frozen17.candidate.id;const slots17=engine.reserveRunBatch(project,{stage:17,iterationId:correctedIteration,candidateId:correctedCandidate,count:10});fillContexts(17,slots17,'CORRECTED-CONTEXT');accept(17,{operation:'FREEZE',scope:runScope(slots17[0]),stageData:{},records:{}});finishRuns(17,slots17,'EXECUTE_RUN');verifyRuns(17,slots17,'VERIFY',reqId,testId);compareRuns(17,'COMPARE',slots17[0],reqId);accept(17,{operation:'REGRESSION',scope:runScope(slots17[0]),records:{regressionExecutions:[recordProposal('regressionExecutions','regexec-corrected',{PHASE:'POST_CORRECTION',RESULT:'SATISFIED'},{REG_ID:refId(regId),ITERATION_ID:refId(correctedIteration),CANDIDATE_ID:refId(correctedCandidate),EVIDENCE_ID:refTemp('evidence-1')})]}});assert(engine.records(project,'regressions').some(r=>engine.recordId(r,'regressions')===regId),'Permanent regression definition was lost from canonical history.');complete(17);

accept(18,{stageData:{},records:{}});complete(18);

const frozen19=engine.freezeCandidate(project,{stage:19,artifactIds:[candidate2],operatorLabel:'FULL_CYCLE_OPERATOR',purpose:'UNCHANGED_CONFIRMATION'});const confirmationIteration=frozen19.iteration.id;assert(frozen19.candidate.id===correctedCandidate,'Stage 19 changed the converged candidate identity.');const slots19=engine.reserveRunBatch(project,{stage:19,iterationId:confirmationIteration,candidateId:correctedCandidate,count:10});fillContexts(19,slots19,'CONFIRM-CONTEXT');accept(19,{operation:'CONFIRM_FREEZE',scope:runScope(slots19[0]),stageData:{},records:{}});finishRuns(19,slots19,'EXECUTE_RUN');verifyRuns(19,slots19,'VERIFY',reqId,testId);compareRuns(19,'COMPARE',slots19[0],reqId);accept(19,{operation:'REGRESSION_VERIFY',scope:runScope(slots19[0]),records:{regressionExecutions:[recordProposal('regressionExecutions','regexec-confirm',{PHASE:'POST_CORRECTION',RESULT:'SATISFIED'},{REG_ID:refId(regId),ITERATION_ID:refId(confirmationIteration),CANDIDATE_ID:refId(correctedCandidate),EVIDENCE_ID:refTemp('evidence-1')})]}});accept(19,{operation:'CONFIRM',scope:runScope(slots19[0]),records:{confirmationRecords:[recordProposal('confirmationRecords','confirmation-1',{ZERO_MATERIAL_CHANGES:'TRUE',VERSION_HASH_COMPARISON:'EXACT MATCH',TEN_NEW_CONTEXTS:'TRUE',COMPLETE_TEST_RESULTS:'SATISFIED',REGRESSION_RESULTS:'SATISFIED',COMPARISON_RESULTS:'SATISFIED',NEW_DEFECTS:'NONE',NEW_REQUIREMENTS:'NONE',NEW_FAILURE_CASES:'NONE',NEW_VARIANCE:'NONE',DETERMINATION:'SATISFIED',EVIDENCE:'Unchanged confirmation completed.'},{SOURCE_ITERATION_ID:refId(correctedIteration),CONFIRMATION_ITERATION_ID:refId(confirmationIteration)})]}});complete(19);

const baseline=engine.freezeBaseline(project,{artifactIds:[candidate2],operatorLabel:'FULL_CYCLE_OPERATOR',authorization:'AUTHORIZED'});accept(20,{stageData:{},records:{}});complete(20);

engine.registerFreshContext(project,{stage:21,externalContextIdentifier:'PRODUCTION-CONTEXT-01',operatorLabel:'FULL_CYCLE_OPERATOR'});const product=engine.reserveProductExecution(project,{operatorLabel:'FULL_CYCLE_OPERATOR'});accept(21,{records:{products:[targetUpdate(product.id,agentFields('products',{BASELINE_MATERIALS:[candidate2],EXECUTION_TIMESTAMPS:{started:'fixture',ended:'fixture'},TOOL_CONFIGURATION:'CONFIG-v1',DEVIATIONS:'NONE',FAILURES:'NONE',GENERATED_ARTIFACT_INVENTORY:['product-a.bin','product-b.bin']}))]}});const productA='ARTIFACT-PRODUCT-A',productB='ARTIFACT-PRODUCT-B',bytesA='product-a-bytes',bytesB='product-b-bytes';engine.registerArtifactBytes(project,{stage:21,artifactId:productA,filename:'product-a.bin',mediaType:'application/octet-stream',byteSize:Buffer.byteLength(bytesA),sha256:hash.sha256Text(bytesA),lineage:{productId:product.id}});engine.registerArtifactBytes(project,{stage:21,artifactId:productB,filename:'product-b.bin',mediaType:'application/octet-stream',byteSize:Buffer.byteLength(bytesB),sha256:hash.sha256Text(bytesB),lineage:{productId:product.id}});complete(21);

accept(22,{records:{deterministicResults:[recordProposal('deterministicResults','det-1',{TOOL_AND_VERSION:'SHA-256 fixture verifier v1',PROCEDURE:'Compare exact product identity.',EXPECTED_RESULT:'SATISFIED',ACTUAL_RESULT:'SATISFIED',DETERMINATION:'SATISFIED',EVIDENCE:'Deterministic product evidence.'},{PRODUCT_ID:refId(product.id),TEST_ID:refId(testId)})]}});complete(22);
accept(23,{records:{meaningResults:[recordProposal('meaningResults','meaning-1',{PRODUCT_LOCATION:'product-a.bin',EXTERNAL_SOURCE_EVIDENCE:'Current accepted source.',REQUIRED_MEANING:'Preserve acceptance identity.',OBSERVED_MEANING:'Acceptance identity preserved.',EVIDENCE_BASED_COMPARISON:'Equivalent required and observed meaning.',DETERMINATION:'SATISFIED',UNDETERMINED_REASON:'NONE'},{REQ_ID:refId(reqId),PRODUCT_ID:refId(product.id)})]}});complete(23);
accept(24,{records:{adversarialResults:[recordProposal('adversarialResults','attack-1',{ATTACK:'Single-byte identity mutation.',METHOD:'Mutate a controlled copy.',EXPECTED_BEHAVIOR:'Reject mismatch.',ACTUAL_RESULT:'Rejected mismatch.',DETERMINATION:'SATISFIED',SEVERITY:'MINOR',EVIDENCE:'Adversarial fixture rejected.'},{PRODUCT_ID:refId(product.id)})]}});complete(24);
accept(25,{records:{representationInspections:[recordProposal('representationInspections','inspect-a',{REQUIRED_BY_TRACE:'YES',TRANSFORMATION_CHAIN:'Direct stored bytes.',TRANSFORMATION_TOOLS_VERSIONS:'NONE',RENDERING_OPENING_EVIDENCE:'Opened successfully.',OBSERVATIONS:'No representation defect.',DETERMINATION:'SATISFIED',EVIDENCE:'Inspection A evidence.'},{ARTIFACT_ID:refId(productA)}),recordProposal('representationInspections','inspect-b',{REQUIRED_BY_TRACE:'YES',TRANSFORMATION_CHAIN:'Direct stored bytes.',TRANSFORMATION_TOOLS_VERSIONS:'NONE',RENDERING_OPENING_EVIDENCE:'Opened successfully.',OBSERVATIONS:'No representation defect.',DETERMINATION:'SATISFIED',EVIDENCE:'Inspection B evidence.'},{ARTIFACT_ID:refId(productB)})]}});complete(25);
accept(26,{records:{processAudits:[recordProposal('processAudits','process-audit',{APPROVED_INPUTS_VS_ACTUAL:'MATCH',APPROVED_INSTRUCTION_VS_ACTUAL:'MATCH',APPROVED_TOOLS_VS_ACTUAL:'MATCH',REQUIRED_TESTS_VS_EXECUTED:'MATCH',UNAUTHORIZED_MODIFICATION:'NONE',AUTHORIZED_CHANGES:'Stage 16 controlled correction only.',CHAIN_OF_CUSTODY:'COMPLETE',PROCESS_DEFECTS:'NONE',BLOCKERS:'NONE',PROCESS_DETERMINATION:'SATISFIED',PROCESS_EVIDENCE:'Complete process evidence.'})],productAudits:[recordProposal('productAudits','product-audit',{VALIDATOR_RESULTS:'SATISFIED',MEANING_VERIFICATION_RESULTS:'SATISFIED',PRODUCT_DEFECTS:'NONE',BLOCKERS:'NONE',PRODUCT_DETERMINATION:'SATISFIED',PRODUCT_EVIDENCE:'Complete product evidence.'})]}});complete(26);
accept(27,{records:{releaseGateReviews:[recordProposal('releaseGateReviews','release-review',{OBSERVED_BLOCKERS:'NONE',OBSERVED_VIOLATIONS:'NONE',OBSERVED_MISSING_EVIDENCE:'NONE',CONTROLLING_RULE_ANALYSIS:'All current release evidence is affirmatively satisfied.',EVIDENCE:'Controlled release review evidence.'},{PRODUCT_ID:refId(product.id),BASELINE_ID:refId(baseline.id)})]}});const release=engine.recordReleaseDetermination(project);assert(engine.recordValue(release,'DETERMINATION')==='ACCEPTED',`Release determination is ${engine.recordValue(release,'DETERMINATION')}.`);complete(27);

const audited=[{artifactId:productA,name:'product-a.bin',size:Buffer.byteLength(bytesA),sha256:hash.sha256Text(bytesA),storageReference:`indexeddb:${productA}`},{artifactId:productB,name:'product-b.bin',size:Buffer.byteLength(bytesB),sha256:hash.sha256Text(bytesB),storageReference:`indexeddb:${productB}`}],delivery=[clone(audited[1]),clone(audited[0])];const identities=engine.verifyArtifactIdentity(project,audited,delivery);assert(identities.length===2&&identities.every(r=>engine.recordValue(r,'AUTHORIZATION')==='AUTHORIZED'),'Order-independent release identity failed.');complete(28);
const chains=engine.constructEvidenceChains(project);assert(chains.length===1&&engine.recordValue(chains[0],'STATUS')==='COMPLETE',`Evidence chain incomplete: ${JSON.stringify(engine.recordValue(chains[0],'MISSING_LINKS'))}`);complete(29);
accept(30,{stageData:agentStageData(30),records:{}});complete(30);

assert(Object.values(project.stages).filter(s=>s.status==='COMPLETE').length===30,'Not all 30 stages completed.');const finalHash=hash.sha256Value(project);reload();assert(hash.sha256Value(project)===finalHash,'Reload changed canonical project semantics.');assert(project.job.CURRENT_STATE==='COMPLETE','Reloaded project is not COMPLETE.');
console.log(JSON.stringify({continuousLifecycle:true,stagesCompleted:30,clarificationCycle:true,confirmedDefect:true,controlledCorrection:true,correctedIteration:true,unchangedConfirmation:true,release:engine.recordValue(engine.records(project,'releaseRecords').at(-1),'DETERMINATION'),artifactIdentityCount:engine.records(project,'artifactIdentities').length,evidenceChainsComplete:engine.records(project,'evidenceChains').every(r=>engine.recordValue(r,'STATUS')==='COMPLETE')},null,2));
