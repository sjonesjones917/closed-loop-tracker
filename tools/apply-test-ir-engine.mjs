import fs from 'node:fs';
const path='workflow-engine.js';let s=fs.readFileSync(path,'utf8');
const once=(from,to,label)=>{const i=s.indexOf(from);if(i<0)throw new Error(`Missing ${label}`);if(s.indexOf(from,i+1)>=0)throw new Error(`Ambiguous ${label}`);s=s.slice(0,i)+to+s.slice(i+from.length);};
once(`const hash=globalThis.closedLoopHash;\nif(!core||!schema||!hash)throw new Error('workbook.js, hash.js, and workflow-schema.js must load before workflow-engine.js.');`,`const hash=globalThis.closedLoopHash;\nconst testRuntime=globalThis.closedLoopTestRuntime;\nif(!core||!schema||!hash||!testRuntime)throw new Error('workbook.js, hash.js, workflow-schema.js, and test-runtime.js must load before workflow-engine.js.');`,'runtime dependency');
once(`const APPLICATION_TEST_EXECUTORS=Object.freeze({});\nfunction applicationTestCapabilities(){return Object.freeze(Object.keys(APPLICATION_TEST_EXECUTORS));}`,`function applicationTestCapabilities(){return testRuntime.capabilities();}`,'native registry');
once(`  if(mode==='APPLICATION_DETERMINISTIC')return Object.prototype.hasOwnProperty.call(APPLICATION_TEST_EXECUTORS,requiredCapability);`,`  if(mode==='APPLICATION_DETERMINISTIC')return requiredCapability===testRuntime.CAPABILITY;`,'native capability check');
once(`      const unsupportedApplication=mandatoryTests.filter(test=>upper(recordValue(test,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&!Object.prototype.hasOwnProperty.call(APPLICATION_TEST_EXECUTORS,String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim()));`,`      const unsupportedApplication=mandatoryTests.filter(test=>upper(recordValue(test,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&!testRuntime.supports(test));`,'stage 6 support');
once(`      if(unsupportedApplication.length)reasons.push(\`${'${unsupportedApplication.length}'} mandatory test definition(s) claim APPLICATION_DETERMINISTIC without a registered application-native executor.\`);`,`      if(unsupportedApplication.length)reasons.push(\`${'${unsupportedApplication.length}'} mandatory test definition(s) claim APPLICATION_DETERMINISTIC without a valid supported Closed Loop Test IR specification.\`);`,'stage 6 support message');
const start=s.indexOf('function testExecutionPlan(project){'),end=s.indexOf('\nfunction operationalMetrics(project)',start);if(start<0||end<0)throw new Error('Missing testExecutionPlan block');
const plan=`function nativeTestInputPlan(project,test){\n  const bindings=recordValue(test,'EXECUTABLE_INPUT_BINDINGS');if(!bindings||typeof bindings!=='object'||Array.isArray(bindings))return {required:false,resolved:[],missing:[],deferred:false};\n  const productId=String(currentScope(project).productId||''),allArtifacts=recordsForCurrentScope(project,'artifacts'),resolved=[],missing=[];\n  for(const [binding,raw] of Object.entries(bindings)){const descriptor=typeof raw==='string'?{artifactId:raw}:raw&&typeof raw==='object'?raw:{},source=upper(descriptor.source||'CURRENT_PRODUCT'),candidates=allArtifacts.filter(a=>{if(source==='CURRENT_PRODUCT'&&productId&&String(a.scope?.productId||'')!==productId)return false;if(descriptor.artifactId&&recordId(a,'artifacts')!==String(descriptor.artifactId))return false;if(descriptor.filename&&String(recordValue(a,'FILENAME')||'')!==String(descriptor.filename))return false;if(descriptor.role&&upper(recordValue(a,'ROLE'))!==upper(descriptor.role))return false;return source==='CURRENT_PRODUCT'||source==='CANONICAL_ARTIFACT';});if(candidates.length!==1){missing.push({binding,reason:productId?('Binding '+binding+' resolves to '+candidates.length+' current artifacts; exactly one is required.'):('Binding '+binding+' awaits the finished product artifact.')});continue;}const a=candidates[0];resolved.push({binding,artifactId:recordId(a,'artifacts'),filename:String(recordValue(a,'FILENAME')||recordId(a,'artifacts')),sha256:String(recordValue(a,'SHA256')||''),byteSize:Number(recordValue(a,'BYTE_SIZE')),role:String(recordValue(a,'ROLE')||descriptor.role||'TEST_INPUT')});}\n  return {required:Object.keys(bindings).length>0,resolved,missing,deferred:!productId&&Object.keys(bindings).length>0};\n}\nfunction testExecutionPlan(project){\n  ensureShape(project);const evidenceById=new Map(records(project,'evidenceRecords').map(item=>[recordId(item,'evidenceRecords'),item])),artifactsById=new Map(records(project,'artifacts').map(item=>[recordId(item,'artifacts'),item]));\n  const executorMap={APPLICATION_DETERMINISTIC:'APPLICATION',EXTERNAL_AGENT_TOOL:'EXTERNAL_AGENT_TOOL',INDEPENDENT_AGENT_REVIEW:'INDEPENDENT_REVIEWER',HUMAN_INSPECTION:'HUMAN',EXTERNAL_SYSTEM:'EXTERNAL_SYSTEM',UNAVAILABLE:'UNAVAILABLE'},actionMap={APPLICATION_DETERMINISTIC:'NO_ACTION',EXTERNAL_AGENT_TOOL:'SEND_TO_TOOL_AGENT',INDEPENDENT_AGENT_REVIEW:'SEND_TO_INDEPENDENT_REVIEWER',HUMAN_INSPECTION:'HUMAN_INSPECTION',EXTERNAL_SYSTEM:'USE_EXTERNAL_SYSTEM',UNAVAILABLE:'BLOCKED'},routeMap={APPLICATION_DETERMINISTIC:'APPLICATION',EXTERNAL_AGENT_TOOL:'EXTERNAL_TOOL',INDEPENDENT_AGENT_REVIEW:'INDEPENDENT_AI',HUMAN_INSPECTION:'HUMAN',EXTERNAL_SYSTEM:'EXTERNAL_SYSTEM',UNAVAILABLE:'UNAVAILABLE'},userActionMap={APPLICATION_DETERMINISTIC:'RUN_IN_APP',EXTERNAL_AGENT_TOOL:'PREPARE_VERIFICATION_PACKAGE',INDEPENDENT_AGENT_REVIEW:'PREPARE_VERIFICATION_PACKAGE',HUMAN_INSPECTION:'COMPLETE_INSPECTION',EXTERNAL_SYSTEM:'RUN_EXTERNAL_SYSTEM',UNAVAILABLE:'BLOCKED'};\n  const items=recordsForCurrentScope(project,'tests').map(test=>{const mode=upper(recordValue(test,'EXECUTION_MODE'))||'UNSPECIFIED',requiredCapability=String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim(),artifactRequirements=String(recordValue(test,'ARTIFACT_REQUIREMENTS')||'').trim(),native=mode==='APPLICATION_DETERMINISTIC',nativeInputs=native?nativeTestInputPlan(project,test):{required:false,resolved:[],missing:[],deferred:false},artifactRequired=native?nativeInputs.required:Boolean(artifactRequirements&&!['NONE','NOT APPLICABLE','N/A'].includes(upper(artifactRequirements))),evidenceIds=safe(test.evidenceRefs).map(String),evidenceArtifactIds=[...new Set(evidenceIds.map(id=>evidenceById.get(id)).map(item=>String(recordValue(item,'ATTACHMENT_ID')||item?.relationships?.ATTACHMENT_ID||'').trim()).filter(Boolean))],artifactIds=native?nativeInputs.resolved.map(x=>x.artifactId):evidenceArtifactIds,missingArtifactIds=native?nativeInputs.missing.map(x=>x.binding):artifactIds.filter(id=>!artifactsById.has(id)),unverifiedArtifactIds=native?nativeInputs.resolved.filter(x=>{const a=artifactsById.get(x.artifactId);return !a||upper(recordValue(a,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED';}).map(x=>x.artifactId):artifactIds.filter(id=>{const artifact=artifactsById.get(id);return artifact&&upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED';}),artifactReady=native?(!nativeInputs.required||(!nativeInputs.missing.length&&!unverifiedArtifactIds.length)):(!artifactRequired||(artifactIds.length>0&&!missingArtifactIds.length&&!unverifiedArtifactIds.length)),applicationExecutorSupported=!native||testRuntime.supports(test),validMode=Object.prototype.hasOwnProperty.call(TEST_EXECUTION_ACTIONS,mode),capabilityReady=native?applicationExecutorSupported:capabilityAffirmativelyAvailable(project,requiredCapability,mode),executableNow=validMode&&capabilityReady&&artifactReady,blockingReason=!validMode?'Execution mode is not validly classified.':!requiredCapability?'Required capability is missing.':mode==='UNAVAILABLE'?'Required capability is explicitly unavailable.':native&&!applicationExecutorSupported?'The application cannot execute this test because its Test IR is absent, invalid, or unsupported.':!capabilityReady?('Required capability '+requiredCapability+' is not affirmatively available in current canonical state.'):!artifactReady?(nativeInputs.deferred?'The deterministic test is valid and will become executable when the exact finished-product bytes exist.':'Required exact artifact bytes are missing or no longer verified.'):null,requiredArtifactNames=native?nativeInputs.resolved.map(x=>x.filename):artifactIds.map(id=>String(recordValue(artifactsById.get(id),'FILENAME')||id)),operatorAction=executableNow?actionMap[mode]||'BLOCKED':'BLOCKED',requiredEvidenceDescription=String(recordValue(test,'EVIDENCE_TO_PRESERVE')||'Preserve evidence capable of establishing the tested proposition.').trim(),returnRequirements={structuredResponse:!native,requiredAttachmentKinds:native?[]:(artifactRequired?['CONTRACTED_VERIFICATION_ARTIFACT_OR_REPORT']:[]),requiredEvidenceDescription},send=native?nativeInputs.resolved:artifactIds.filter(id=>artifactsById.has(id)).map(id=>{const a=artifactsById.get(id);return {artifactId:id,filename:String(recordValue(a,'FILENAME')||id),sha256:String(recordValue(a,'SHA256')||'UNKNOWN'),role:String(recordValue(a,'ROLE')||artifactRequirements||'TEST_INPUT')};}),withhold=mode==='INDEPENDENT_AGENT_REVIEW'?[{artifactIdOrCategory:'prior verifier conclusions',reason:'Preserve reviewer independence.'},{artifactIdOrCategory:'proposed corrections',reason:'Prevent expectation bias.'}]:[],expectBack=native?[]:[{kind:'STRUCTURED_RESPONSE',filenameOrPattern:'final strict JSON response',required:true},{kind:'EVIDENCE',filenameOrPattern:requiredEvidenceDescription,required:true}],executionRoute=routeMap[mode]||'UNAVAILABLE',userAction=executableNow?(userActionMap[mode]||'BLOCKED'):'BLOCKED';return {testId:recordId(test,'tests'),requirementId:testRequirementId(test),testType:upper(recordValue(test,'TEST_TYPE'))||'UNKNOWN',executionMode:mode,requiredCapability,executorClass:executorMap[mode]||'UNAVAILABLE',executionRoute,userAction,executableNow,blockingReason,artifactRequirements,artifactRequired,evidenceIds,artifactIds,requiredArtifactIds:artifactIds,requiredArtifactNames,missingArtifactIds,unverifiedArtifactIds,artifactReady,applicationExecutorSupported,capabilityReady,operatorAction,operatorActionText:executableNow?(native?'Run this deterministic test inside the application.':(TEST_EXECUTION_ACTIONS[mode]||'')):(blockingReason||'Blocked.'),returnRequirements,handoff:{send,withhold,expectBack},nativeInputBindings:nativeInputs};});\n  const counts=Object.fromEntries(Object.keys(TEST_EXECUTION_ACTIONS).map(mode=>[mode,items.filter(item=>item.executionMode===mode).length])),incompleteTestIds=items.filter(item=>!TEST_EXECUTION_ACTIONS[item.executionMode]||!item.requiredCapability||!item.artifactRequirements).map(item=>item.testId),unavailableTestIds=items.filter(item=>item.executionMode==='UNAVAILABLE').map(item=>item.testId),unsupportedApplicationTestIds=items.filter(item=>item.executionMode==='APPLICATION_DETERMINISTIC'&&!item.applicationExecutorSupported).map(item=>item.testId),missingArtifactTestIds=items.filter(item=>item.artifactRequired&&!item.artifactReady&&!item.nativeInputBindings?.deferred).map(item=>item.testId);return {total:items.length,counts,incompleteTestIds,unavailableTestIds,unsupportedApplicationTestIds,missingArtifactTestIds,items};\n}\n`;
s=s.slice(0,start)+plan+s.slice(end);
const cmd=s.indexOf('function commandRecord(project,collection,fields,');if(cmd<0)throw new Error('Missing commandRecord');const insert=s.indexOf('\nfunction createHumanBlocker',cmd);if(insert<0)throw new Error('Missing commandRecord end');
const nativeFn=`\nfunction recordNativeDeterministicResult(project,{testId,runtimeResult}={}){\n  ensureShape(project);const id=String(testId||''),test=recordsForCurrentScope(project,'tests').find(t=>recordId(t,'tests')===id);if(!test)throw new Error('The deterministic test is not current.');if(!testRuntime.supports(test))throw new Error('The deterministic test is not supported by the registered application Test IR runtime.');const plan=testExecutionPlan(project).items.find(item=>item.testId===id);if(!plan||plan.userAction!=='RUN_IN_APP'||!plan.executableNow)throw new Error(plan?.blockingReason||'The deterministic test is not executable now.');const product=recordsForCurrentScope(project,'products').at(-1),productId=recordId(product,'products');if(!productId)throw new Error('A current finished product is required.');const specHash=hash.sha256Value(recordValue(test,'EXECUTABLE_SPEC'));if(String(runtimeResult?.testSpecSha256||'')!==specHash)throw new Error('Runtime result Test IR identity does not match the current canonical test.');const determination=upper(runtimeResult?.determination);if(!['SATISFIED','VIOLATED','UNDETERMINED','EXECUTION_FAILED'].includes(determination))throw new Error('Runtime result determination is invalid.');const artifactsById=new Map(recordsForCurrentScope(project,'artifacts').map(a=>[recordId(a,'artifacts'),a]));for(const input of plan.nativeInputBindings.resolved){const a=artifactsById.get(input.artifactId);if(!a||upper(recordValue(a,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||String(recordValue(a,'SHA256')||'')!==input.sha256||Number(recordValue(a,'BYTE_SIZE'))!==input.byteSize)throw new Error('A runtime input artifact is no longer the exact current verified byte identity.');}const manifest=plan.nativeInputBindings.resolved.map(x=>({artifactId:x.artifactId,filename:x.filename,sha256:x.sha256,byteSize:x.byteSize,binding:x.binding})),evidenceSha256=hash.sha256Value({testId:id,productId,specHash,manifest,runtimeVersion:runtimeResult.executorVersion,determination,observations:runtimeResult.observations}),existing=recordsForCurrentScope(project,'deterministicResults').find(r=>String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'')===id&&String(recordValue(r,'TEST_SPEC_SHA256')||'')===specHash&&String(recordValue(r,'RUNTIME_EXECUTION')?.evidenceSha256||'')===evidenceSha256);if(existing)return existing;for(const prior of recordsForCurrentScope(project,'deterministicResults').filter(r=>String(recordValue(r,'TEST_ID')||r.relationships?.TEST_ID||'')===id)){prior.active=false;prior.validity='SUPERSEDED';prior.invalidatedBy='NATIVE-RUNTIME-'+evidenceSha256;refreshRecordHashes(prior,'deterministicResults');}const runtimeExecution={executorVersion:String(runtimeResult.executorVersion||testRuntime.RUNTIME_VERSION),specVersion:String(runtimeResult.specVersion||testRuntime.SPEC_VERSION),evidenceSha256,determination,expected:clone(runtimeResult.expected),actual:clone(runtimeResult.actual),assertions:Number(runtimeResult.assertions||0),failedAssertions:Number(runtimeResult.failedAssertions||0),observations:clone(runtimeResult.observations||[]),artifactIdentities:manifest},productSha=manifest.length===1?manifest[0].sha256:hash.sha256Value(manifest),record=commandRecord(project,'deterministicResults',{PRODUCT_ID:productId,PRODUCT_SHA256:productSha,TEST_ID:id,DEFECT_ID:'',RUNTIME_EXECUTION:runtimeExecution,TEST_SPEC_SHA256:specHash,RUNTIME_DETERMINATION:determination},{stage:22,source:'APPLICATION_TEST_RUNTIME',scope:currentScope(project)});addHistory(project,'APPLICATION_DETERMINISTIC_TEST_EXECUTED',{stage:22,recordId:recordId(record,'deterministicResults'),testId:id,productId,determination,evidenceSha256});recalculate(project);return record;\n}\n`;
s=s.slice(0,insert)+nativeFn+s.slice(insert);
once(`  else if(collection==='deterministicResults'){if(!evidence.sufficient)reasons.push(...evidence.reasons);const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const actual=recordValue(record,'ACTUAL_RESULT'),expected=recordValue(record,'EXPECTED_RESULT')||recordValue(test,'EXPECTED_RESULT'),out=controlledOutcome(actual);if(out)determination=out;else if(!adjudicationEmpty(actual)&&!adjudicationEmpty(expected)&&upper(actual)===upper(expected))determination='SATISFIED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}`,`  else if(collection==='deterministicResults'){const native=upper(recordValue(test,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&recordValue(record,'RUNTIME_EXECUTION');if(native){const runtime=recordValue(record,'RUNTIME_EXECUTION'),claimedRuntime=upper(recordValue(record,'RUNTIME_DETERMINATION')),specHash=hash.sha256Value(recordValue(test,'EXECUTABLE_SPEC'));if(!testRuntime.supports(test))reasons.push('The canonical test is no longer supported by the application Test IR runtime.');if(String(recordValue(record,'TEST_SPEC_SHA256')||'')!==specHash)reasons.push('Native runtime result is bound to a stale Test IR identity.');const currentArtifacts=new Map(recordsForCurrentScope(project,'artifacts').map(a=>[recordId(a,'artifacts'),a]));for(const input of safe(runtime?.artifactIdentities)){const a=currentArtifacts.get(String(input.artifactId||''));if(!a||upper(recordValue(a,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||String(recordValue(a,'SHA256')||'')!==String(input.sha256||'')||Number(recordValue(a,'BYTE_SIZE'))!==Number(input.byteSize))reasons.push('Native runtime evidence references stale or unverified input bytes.');}if(!runtime?.evidenceSha256||!safe(runtime?.artifactIdentities).length)reasons.push('Native runtime evidence is missing its execution identity or exact input manifest.');determination=['SATISFIED','VIOLATED','UNDETERMINED','EXECUTION_FAILED'].includes(claimedRuntime)?claimedRuntime:'UNDETERMINED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}else{if(!evidence.sufficient)reasons.push(...evidence.reasons);const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const actual=recordValue(record,'ACTUAL_RESULT'),expected=recordValue(record,'EXPECTED_RESULT')||recordValue(test,'EXPECTED_RESULT'),out=controlledOutcome(actual);if(out)determination=out;else if(!adjudicationEmpty(actual)&&!adjudicationEmpty(expected)&&upper(actual)===upper(expected))determination='SATISFIED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}}`,'native result adjudication');
once(`    case 22:{\n      requireAccepted();const mandatoryIds=`,`    case 22:{\n      const mandatoryIds=`,'stage 22 acceptance');
once(`expected=recordsForCurrentScope(project,'tests').filter(test=>mandatoryIds.has(testRequirementId(test))&&upper(recordValue(test,'TEST_TYPE'))==='DETERMINISTIC'&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY'))),results=`,`expected=recordsForCurrentScope(project,'tests').filter(test=>mandatoryIds.has(testRequirementId(test))&&upper(recordValue(test,'TEST_TYPE'))==='DETERMINISTIC'&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY'))),externalExpected=expected.filter(test=>upper(recordValue(test,'EXECUTION_MODE'))!=='APPLICATION_DETERMINISTIC');if(externalExpected.length)requireAccepted();const results=`,'stage 22 external acceptance');
once(`if(results.some(record=>upper(recordValue(record,'DETERMINATION'))!=='SATISFIED'))reasons.push('A deterministic finished-product verification is violated or undetermined.');`,`if(results.some(record=>effectiveDetermination('deterministicResults',record,testForResult(project,record),project)!=='SATISFIED'))reasons.push('A deterministic finished-product verification is violated or undetermined.');`,'stage 22 effective result');
once(`  if(relevant.length&&relevant.every(i=>i.operatorAction==='NO_ACTION'))return 'No external action required. The application can perform the current deterministic verification route.';`,`  if(relevant.length&&relevant.every(i=>i.executionMode==='APPLICATION_DETERMINISTIC')){const runnable=relevant.filter(i=>i.userAction==='RUN_IN_APP');if(runnable.length)return 'No external action required. Run '+runnable.length+' application-native deterministic test'+(runnable.length===1?'':'s')+' against the exact current product bytes.';return 'No external action required. Application-native deterministic verification is waiting for its exact required product bytes.';}`,'next action native');
once(`applicationTestCapabilities,capabilityAffirmativelyAvailable,testExecutionPlan,executionHandoff,`,`applicationTestCapabilities,capabilityAffirmativelyAvailable,nativeTestInputPlan,testExecutionPlan,recordNativeDeterministicResult,executionHandoff,`,'engine exports');
fs.writeFileSync(path,s);console.log('engine patch applied');
