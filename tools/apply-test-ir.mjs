import fs from 'node:fs';

function read(path){return fs.readFileSync(path,'utf8');}
function write(path,text){fs.writeFileSync(path,text);}
function replaceOnce(text,from,to,label){const i=text.indexOf(from);if(i<0)throw new Error(`Missing patch target: ${label}`);if(text.indexOf(from,i+1)>=0)throw new Error(`Ambiguous patch target: ${label}`);return text.slice(0,i)+to+text.slice(i+from.length);}
function replaceAllRequired(text,from,to,min,label){const count=text.split(from).length-1;if(count<min)throw new Error(`Expected at least ${min} patch targets for ${label}; found ${count}`);return text.split(from).join(to);}

// workflow-schema.js — add subject-neutral executable Test IR fields without changing the workflow/stage model.
{
  let s=read('workflow-schema.js');
  s=replaceOnce(s,
`      "REQUIRED_CAPABILITY",\n      "ARTIFACT_REQUIREMENTS",\n      "INPUTS",`,
`      "REQUIRED_CAPABILITY",\n      "ARTIFACT_REQUIREMENTS",\n      "EXECUTABLE_SPEC_VERSION",\n      "EXECUTABLE_SPEC",\n      "EXECUTABLE_INPUT_BINDINGS",\n      "INPUTS",`,
'RECORD_OWNERSHIP.tests agent fields');
  s=replaceOnce(s,
`      "PRODUCT_SHA256",\n      "TEST_ID",\n      "DEFECT_ID"\n    ]\n  },\n  "meaningResults"`,
`      "PRODUCT_SHA256",\n      "TEST_ID",\n      "DEFECT_ID",\n      "RUNTIME_EXECUTION",\n      "TEST_SPEC_SHA256",\n      "RUNTIME_DETERMINATION"\n    ]\n  },\n  "meaningResults"`,
'RECORD_OWNERSHIP.deterministicResults application fields');
  s=replaceAllRequired(s,
`'TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','INPUTS','TOOLS','PROCEDURE','EXPECTED_RESULT','FAILURE_CONDITION','EVIDENCE_TO_PRESERVE','STATUS'`,
`'TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','EXECUTABLE_SPEC_VERSION','EXECUTABLE_SPEC','EXECUTABLE_INPUT_BINDINGS','INPUTS','TOOLS','PROCEDURE','EXPECTED_RESULT','FAILURE_CONDITION','EVIDENCE_TO_PRESERVE','STATUS'`,1,'tests field list');
  s=replaceAllRequired(s,
`'RESULT_ID','PRODUCT_ID','PRODUCT_SHA256','TEST_ID','DEFECT_ID','TOOL_AND_VERSION','PROCEDURE','EXPECTED_RESULT','ACTUAL_RESULT','DETERMINATION','EVIDENCE'`,
`'RESULT_ID','PRODUCT_ID','PRODUCT_SHA256','TEST_ID','DEFECT_ID','RUNTIME_EXECUTION','TEST_SPEC_SHA256','RUNTIME_DETERMINATION','TOOL_AND_VERSION','PROCEDURE','EXPECTED_RESULT','ACTUAL_RESULT','DETERMINATION','EVIDENCE'`,1,'deterministicResults field list');
  const overrideNeedle=`  TEST:Object.freeze({TEST_TYPE:Object.freeze({valueType:'STRING',enumValues:Object.freeze(['DETERMINISTIC','MEANING','ADVERSARIAL'])}),EXECUTION_MODE:Object.freeze({valueType:'STRING',enumValues:Object.freeze(['APPLICATION_DETERMINISTIC','EXTERNAL_AGENT_TOOL','INDEPENDENT_AGENT_REVIEW','HUMAN_INSPECTION','EXTERNAL_SYSTEM','UNAVAILABLE'])}),REQUIRED_CAPABILITY:Object.freeze({valueType:'STRING'}),ARTIFACT_REQUIREMENTS:Object.freeze({valueType:'STRING'})}),`;
  const overrideReplacement=`  TEST:Object.freeze({TEST_TYPE:Object.freeze({valueType:'STRING',enumValues:Object.freeze(['DETERMINISTIC','MEANING','ADVERSARIAL'])}),EXECUTION_MODE:Object.freeze({valueType:'STRING',enumValues:Object.freeze(['APPLICATION_DETERMINISTIC','EXTERNAL_AGENT_TOOL','INDEPENDENT_AGENT_REVIEW','HUMAN_INSPECTION','EXTERNAL_SYSTEM','UNAVAILABLE'])}),REQUIRED_CAPABILITY:Object.freeze({valueType:'STRING'}),ARTIFACT_REQUIREMENTS:Object.freeze({valueType:'STRING'}),EXECUTABLE_SPEC_VERSION:Object.freeze({valueType:'STRING',nullable:true}),EXECUTABLE_SPEC:Object.freeze({valueType:'OBJECT',nullable:true}),EXECUTABLE_INPUT_BINDINGS:Object.freeze({valueType:'OBJECT',nullable:true})}),`;
  if(s.includes(overrideNeedle))s=replaceOnce(s,overrideNeedle,overrideReplacement,'TEST type overrides');
  else {
    const marker=`const RECORD_FIELD_TYPE_OVERRIDES=Object.freeze({`;
    if(!s.includes(marker))throw new Error('Could not locate RECORD_FIELD_TYPE_OVERRIDES.');
    // Existing formatting differs on some revisions; patch individual TEST entries later if already expanded.
    if(!s.includes('EXECUTABLE_SPEC_VERSION'))throw new Error('TEST type override insertion target changed.');
  }
  const detOverrideMarker=`const RECORD_FIELD_TYPE_OVERRIDES=Object.freeze({`;
  // Add explicit native result types through the generic field-type map when no collection override exists.
  if(!s.includes("RUNTIME_DETERMINATION:Object.freeze({valueType:'STRING'")){
    const marker=`const EXPLICIT_RECORD_FIELD_TYPES=Object.freeze({`;
    const insertion=`const TEST_RUNTIME_FIELD_TYPES=Object.freeze({EXECUTABLE_SPEC_VERSION:'STRING',EXECUTABLE_SPEC:'OBJECT',EXECUTABLE_INPUT_BINDINGS:'OBJECT',RUNTIME_EXECUTION:'OBJECT',TEST_SPEC_SHA256:'STRING',RUNTIME_DETERMINATION:'STRING'});\n`;
    if(!s.includes(marker))throw new Error('Missing EXPLICIT_RECORD_FIELD_TYPES marker.');
    s=s.replace(marker,insertion+marker);
    const resolver=`const explicitRecordFieldType=(collection,name)=>`;
    if(s.includes(resolver)){
      const line=s.match(/const explicitRecordFieldType=.*?;\n/)?.[0];
      if(!line)throw new Error('Could not patch explicitRecordFieldType.');
      s=s.replace(line,line.replace('=>',`=>(TEST_RUNTIME_FIELD_TYPES[name]||` ).replace(/;\n$/,`);\n`));
    }
  }
  write('workflow-schema.js',s);
}

// workflow-engine.js — use the Test IR runtime as the only application-native per-test executor.
{
  let s=read('workflow-engine.js');
  s=replaceOnce(s,
`const hash=globalThis.closedLoopHash;\nif(!core||!schema||!hash)throw new Error('workbook.js, hash.js, and workflow-schema.js must load before workflow-engine.js.');`,
`const hash=globalThis.closedLoopHash;\nconst testRuntime=globalThis.closedLoopTestRuntime;\nif(!core||!schema||!hash||!testRuntime)throw new Error('workbook.js, hash.js, workflow-schema.js, and test-runtime.js must load before workflow-engine.js.');`,
'engine runtime dependency');
  s=replaceOnce(s,
`const APPLICATION_TEST_EXECUTORS=Object.freeze({});\nfunction applicationTestCapabilities(){return Object.freeze(Object.keys(APPLICATION_TEST_EXECUTORS));}`,
`function applicationTestCapabilities(){return testRuntime.capabilities();}`,
'native executor registry');
  s=replaceOnce(s,
`if(mode==='APPLICATION_DETERMINISTIC')return Object.prototype.hasOwnProperty.call(APPLICATION_TEST_EXECUTORS,requiredCapability);`,
`if(mode==='APPLICATION_DETERMINISTIC')return requiredCapability===testRuntime.CAPABILITY;`,
'capability availability');
  s=replaceOnce(s,
`const unsupportedApplication=mandatoryTests.filter(test=>upper(recordValue(test,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&!Object.prototype.hasOwnProperty.call(APPLICATION_TEST_EXECUTORS,String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim()));`,
`const unsupportedApplication=mandatoryTests.filter(test=>upper(recordValue(test,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&!testRuntime.supports(test));`,
'Stage 06 unsupported native executor');
  s=replaceOnce(s,
`if(unsupportedApplication.length)reasons.push(\`${unsupportedApplication.length} mandatory test definition(s) claim APPLICATION_DETERMINISTIC without a registered application-native executor.\`);`,
`if(unsupportedApplication.length)reasons.push(\`${unsupportedApplication.length} mandatory test definition(s) claim APPLICATION_DETERMINISTIC without a valid supported Closed Loop Test IR specification.\`);`,
'Stage 06 native executor message');

  const planStart=s.indexOf('function testExecutionPlan(project){');
  const planEnd=s.indexOf('\nfunction operationalMetrics(project)',planStart);
  if(planStart<0||planEnd<0)throw new Error('Could not locate testExecutionPlan.');
  const replacement=`function nativeTestInputPlan(project,test){\n  const bindings=recordValue(test,'EXECUTABLE_INPUT_BINDINGS');if(!bindings||typeof bindings!=='object'||Array.isArray(bindings))return {required:false,resolved:[],missing:[],deferred:false};\n  const productId=String(currentScope(project).productId||''),productArtifacts=recordsForCurrentScope(project,'artifacts').filter(a=>!productId||String(a.scope?.productId||'')===productId),resolved=[],missing=[];\n  for(const [binding,raw] of Object.entries(bindings)){const descriptor=typeof raw==='string'?{artifactId:raw}:raw&&typeof raw==='object'?raw:{},source=upper(descriptor.source||'CURRENT_PRODUCT'),candidates=productArtifacts.filter(a=>{if(descriptor.artifactId&&recordId(a,'artifacts')!==String(descriptor.artifactId))return false;if(descriptor.filename&&String(recordValue(a,'FILENAME')||'')!==String(descriptor.filename))return false;if(descriptor.role&&upper(recordValue(a,'ROLE'))!==upper(descriptor.role))return false;return source==='CURRENT_PRODUCT'||source==='CANONICAL_ARTIFACT';});if(candidates.length!==1){missing.push({binding,reason:productId?\`Binding \${binding} resolves to \${candidates.length} current product artifacts; exactly one is required.\`:\`Binding \${binding} awaits the finished product artifact.\`});continue;}const a=candidates[0];resolved.push({binding,artifactId:recordId(a,'artifacts'),filename:String(recordValue(a,'FILENAME')||recordId(a,'artifacts')),sha256:String(recordValue(a,'SHA256')||''),byteSize:Number(recordValue(a,'BYTE_SIZE')),role:String(recordValue(a,'ROLE')||descriptor.role||'TEST_INPUT')});}\n  return {required:Object.keys(bindings).length>0,resolved,missing,deferred:!productId&&Object.keys(bindings).length>0};\n}\nfunction testExecutionPlan(project){\n  ensureShape(project);const evidenceById=new Map(records(project,'evidenceRecords').map(item=>[recordId(item,'evidenceRecords'),item])),artifactsById=new Map(records(project,'artifacts').map(item=>[recordId(item,'artifacts'),item]));\n  const executorMap={APPLICATION_DETERMINISTIC:'APPLICATION',EXTERNAL_AGENT_TOOL:'EXTERNAL_AGENT_TOOL',INDEPENDENT_AGENT_REVIEW:'INDEPENDENT_REVIEWER',HUMAN_INSPECTION:'HUMAN',EXTERNAL_SYSTEM:'EXTERNAL_SYSTEM',UNAVAILABLE:'UNAVAILABLE'},actionMap={APPLICATION_DETERMINISTIC:'NO_ACTION',EXTERNAL_AGENT_TOOL:'SEND_TO_TOOL_AGENT',INDEPENDENT_AGENT_REVIEW:'SEND_TO_INDEPENDENT_REVIEWER',HUMAN_INSPECTION:'HUMAN_INSPECTION',EXTERNAL_SYSTEM:'USE_EXTERNAL_SYSTEM',UNAVAILABLE:'BLOCKED'},routeMap={APPLICATION_DETERMINISTIC:'APPLICATION',EXTERNAL_AGENT_TOOL:'EXTERNAL_TOOL',INDEPENDENT_AGENT_REVIEW:'INDEPENDENT_AI',HUMAN_INSPECTION:'HUMAN',EXTERNAL_SYSTEM:'EXTERNAL_SYSTEM',UNAVAILABLE:'UNAVAILABLE'},userActionMap={APPLICATION_DETERMINISTIC:'RUN_IN_APP',EXTERNAL_AGENT_TOOL:'PREPARE_VERIFICATION_PACKAGE',INDEPENDENT_AGENT_REVIEW:'PREPARE_VERIFICATION_PACKAGE',HUMAN_INSPECTION:'COMPLETE_INSPECTION',EXTERNAL_SYSTEM:'RUN_EXTERNAL_SYSTEM',UNAVAILABLE:'BLOCKED'};\n  const items=recordsForCurrentScope(project,'tests').map(test=>{const mode=upper(recordValue(test,'EXECUTION_MODE'))||'UNSPECIFIED',requiredCapability=String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim(),artifactRequirements=String(recordValue(test,'ARTIFACT_REQUIREMENTS')||'').trim(),native=mode==='APPLICATION_DETERMINISTIC',nativeInputs=native?nativeTestInputPlan(project,test):{required:false,resolved:[],missing:[],deferred:false},artifactRequired=native?nativeInputs.required:Boolean(artifactRequirements&&!['NONE','NOT APPLICABLE','N/A'].includes(upper(artifactRequirements))),evidenceIds=safe(test.evidenceRefs).map(String),evidenceArtifactIds=[...new Set(evidenceIds.map(id=>evidenceById.get(id)).map(item=>String(recordValue(item,'ATTACHMENT_ID')||item?.relationships?.ATTACHMENT_ID||'').trim()).filter(Boolean))],artifactIds=native?nativeInputs.resolved.map(x=>x.artifactId):evidenceArtifactIds,missingArtifactIds=native?nativeInputs.missing.map(x=>x.binding):artifactIds.filter(id=>!artifactsById.has(id)),unverifiedArtifactIds=native?nativeInputs.resolved.filter(x=>{const a=artifactsById.get(x.artifactId);return !a||upper(recordValue(a,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED';}).map(x=>x.artifactId):artifactIds.filter(id=>{const artifact=artifactsById.get(id);return artifact&&upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED';}),artifactReady=native?(!nativeInputs.required||(!nativeInputs.missing.length&&!unverifiedArtifactIds.length)):(!artifactRequired||(artifactIds.length>0&&!missingArtifactIds.length&&!unverifiedArtifactIds.length)),applicationExecutorSupported=!native||testRuntime.supports(test),validMode=Object.prototype.hasOwnProperty.call(TEST_EXECUTION_ACTIONS,mode),capabilityReady=native?applicationExecutorSupported:capabilityAffirmativelyAvailable(project,requiredCapability,mode),executableNow=validMode&&capabilityReady&&artifactReady,blockingReason=!validMode?'Execution mode is not validly classified.':!requiredCapability?'Required capability is missing.':mode==='UNAVAILABLE'?'Required capability is explicitly unavailable.':native&&!applicationExecutorSupported?'The application cannot execute this test because its Test IR is absent, invalid, or uses an unsupported application capability.':!capabilityReady?'Required capability '+requiredCapability+' is not affirmatively available in current canonical state.':!artifactReady?(nativeInputs.deferred?'The deterministic test is valid and will become executable when the exact finished-product bytes exist.':'Required exact artifact bytes are missing or no longer verified.'):null,requiredArtifactNames=native?nativeInputs.resolved.map(x=>x.filename):artifactIds.map(id=>String(recordValue(artifactsById.get(id),'FILENAME')||id)),operatorAction=executableNow?actionMap[mode]||'BLOCKED':'BLOCKED',requiredEvidenceDescription=String(recordValue(test,'EVIDENCE_TO_PRESERVE')||'Preserve evidence capable of establishing the tested proposition.').trim(),returnRequirements={structuredResponse:!native,requiredAttachmentKinds:native?[]:(artifactRequired?['CONTRACTED_VERIFICATION_ARTIFACT_OR_REPORT']:[]),requiredEvidenceDescription},send=(native?nativeInputs.resolved:artifactIds.filter(id=>artifactsById.has(id)).map(id=>{const a=artifactsById.get(id);return {artifactId:id,filename:String(recordValue(a,'FILENAME')||id),sha256:String(recordValue(a,'SHA256')||'UNKNOWN'),role:String(recordValue(a,'ROLE')||artifactRequirements||'TEST_INPUT')};})),withhold=mode==='INDEPENDENT_AGENT_REVIEW'?[{artifactIdOrCategory:'prior verifier conclusions',reason:'Preserve reviewer independence.'},{artifactIdOrCategory:'proposed corrections',reason:'Prevent expectation bias.'}]:[],expectBack=native?[]:[{kind:'STRUCTURED_RESPONSE',filenameOrPattern:'final strict JSON response',required:true},{kind:'EVIDENCE',filenameOrPattern:requiredEvidenceDescription,required:true}],executionRoute:routeMap[mode]||'UNAVAILABLE',userAction:executableNow?(userActionMap[mode]||'BLOCKED'):'BLOCKED';return {testId:recordId(test,'tests'),requirementId:testRequirementId(test),testType:upper(recordValue(test,'TEST_TYPE'))||'UNKNOWN',executionMode:mode,requiredCapability,executorClass:executorMap[mode]||'UNAVAILABLE',executionRoute,userAction,executableNow,blockingReason,artifactRequirements,artifactRequired,evidenceIds,artifactIds,requiredArtifactIds:artifactIds,requiredArtifactNames,missingArtifactIds,unverifiedArtifactIds,artifactReady,applicationExecutorSupported,capabilityReady,operatorAction,operatorActionText:executableNow?(native?'Run this deterministic test inside the application.':(TEST_EXECUTION_ACTIONS[mode]||'')):(blockingReason||'Blocked.'),returnRequirements,handoff:{send,withhold,expectBack},nativeInputBindings:nativeInputs};});\n  const counts=Object.fromEntries(Object.keys(TEST_EXECUTION_ACTIONS).map(mode=>[mode,items.filter(item=>item.executionMode===mode).length])),incompleteTestIds=items.filter(item=>!TEST_EXECUTION_ACTIONS[item.executionMode]||!item.requiredCapability||!item.artifactRequirements).map(item=>item.testId),unavailableTestIds=items.filter(item=>item.executionMode==='UNAVAILABLE').map(item=>item.testId),unsupportedApplicationTestIds=items.filter(item=>item.executionMode==='APPLICATION_DETERMINISTIC'&&!item.applicationExecutorSupported).map(item=>item.testId),missingArtifactTestIds=items.filter(item=>item.artifactRequired&&!item.artifactReady&&!item.nativeInputBindings?.deferred).map(item=>item.testId);return {total:items.length,counts,incompleteTestIds,unavailableTestIds,unsupportedApplicationTestIds,missingArtifactTestIds,items};\n}\nfunction recordNativeDeterministicResult(project,{testId,runtimeResult}={}){\n  ensureShape(project);const test=recordsForCurrentScope(project,'tests').find(t=>recordId(t,'tests')===String(testId||''));if(!test)throw new Error('The deterministic test is not current.');if(!testRuntime.supports(test))throw new Error('The deterministic test is not supported by the registered application Test IR runtime.');const plan=testExecutionPlan(project).items.find(item=>item.testId===String(testId));if(!plan||plan.userAction!=='RUN_IN_APP'||!plan.executableNow)throw new Error(plan?.blockingReason||'The deterministic test is not executable now.');const product=recordsForCurrentScope(project,'products').at(-1),productId=recordId(product,'products');if(!productId)throw new Error('A current finished product is required.');const expectedSpecHash=hash.sha256Value(recordValue(test,'EXECUTABLE_SPEC'));if(String(runtimeResult?.testSpecSha256||'')!==expectedSpecHash)throw new Error('Runtime result Test IR identity does not match the current canonical test.');if(!['SATISFIED','VIOLATED','UNDETERMINED','EXECUTION_FAILED'].includes(upper(runtimeResult?.determination)))throw new Error('Runtime result determination is invalid.');const artifactsById=new Map(recordsForCurrentScope(project,'artifacts').map(a=>[recordId(a,'artifacts'),a]));for(const input of plan.nativeInputBindings.resolved){const actual=artifactsById.get(input.artifactId);if(!actual||upper(recordValue(actual,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||String(recordValue(actual,'SHA256')||'')!==String(input.sha256)||Number(recordValue(actual,'BYTE_SIZE'))!==Number(input.byteSize))throw new Error('A runtime input artifact is no longer the exact current verified byte identity.');}const evidenceSha=hash.sha256Value({testId:String(testId),productId,specHash:expectedSpecHash,artifacts:plan.nativeInputBindings.resolved.map(x=>({artifactId:x.artifactId,sha256:x.sha256,byteSize:x.byteSize})),runtimeVersion:runtimeResult.executorVersion,determination:runtimeResult.determination,observations:runtimeResult.observations});const existing=recordsForCurrentScope(project,'deterministicResults').find(r=>String(recordValue(r,'TEST_ID'))===String(testId)&&String(recordValue(r,'TEST_SPEC_SHA256'))===expectedSpecHash&&String(recordValue(r,'RUNTIME_EXECUTION')?.evidenceSha256||'')===evidenceSha);if(existing)return existing;for(const prior of recordsForCurrentScope(project,'deterministicResults').filter(r=>String(recordValue(r,'TEST_ID'))===String(testId))){prior.active=false;prior.validity='SUPERSEDED';prior.invalidatedBy='NATIVE-RUNTIME-'+evidenceSha;refreshRecordHashes(prior,'deterministicResults');}const manifest=plan.nativeInputBindings.resolved.map(x=>({artifactId:x.artifactId,filename:x.filename,sha256:x.sha256,byteSize:x.byteSize})),productSha=manifest.length===1?manifest[0].sha256:hash.sha256Value(manifest),runtimeExecution={executorVersion:String(runtimeResult.executorVersion||testRuntime.RUNTIME_VERSION),specVersion:String(runtimeResult.specVersion||testRuntime.SPEC_VERSION),evidenceSha256:evidenceSha,determination:upper(runtimeResult.determination),expected:clone(runtimeResult.expected),actual:clone(runtimeResult.actual),assertions:Number(runtimeResult.assertions||0),failedAssertions:Number(runtimeResult.failedAssertions||0),observations:clone(runtimeResult.observations||[]),artifactIdentities:manifest};const record=commandRecord(project,'deterministicResults',{PRODUCT_ID:productId,PRODUCT_SHA256:productSha,TEST_ID:String(testId),DEFECT_ID:'',RUNTIME_EXECUTION:runtimeExecution,TEST_SPEC_SHA256:expectedSpecHash,RUNTIME_DETERMINATION:upper(runtimeResult.determination)},{stage:22,source:'APPLICATION_TEST_RUNTIME',scope:currentScope(project)});addHistory(project,'APPLICATION_DETERMINISTIC_TEST_EXECUTED',{stage:22,recordId:recordId(record,'deterministicResults'),testId:String(testId),productId,determination:runtimeExecution.determination,evidenceSha256:evidenceSha});recalculate(project);return record;\n}\n`;
  s=s.slice(0,planStart)+replacement+s.slice(planEnd);

  s=replaceOnce(s,
`  else if(collection==='deterministicResults'){if(!evidence.sufficient)reasons.push(...evidence.reasons);const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const actual=recordValue(record,'ACTUAL_RESULT'),expected=recordValue(record,'EXPECTED_RESULT')||recordValue(test,'EXPECTED_RESULT'),out=controlledOutcome(actual);if(out)determination=out;else if(!adjudicationEmpty(actual)&&!adjudicationEmpty(expected)&&upper(actual)===upper(expected))determination='SATISFIED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}`,
`  else if(collection==='deterministicResults'){const native=upper(recordValue(test,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&recordValue(record,'RUNTIME_EXECUTION');if(native){const runtime=recordValue(record,'RUNTIME_EXECUTION'),claimed=upper(recordValue(record,'RUNTIME_DETERMINATION')),expectedSpecHash=hash.sha256Value(recordValue(test,'EXECUTABLE_SPEC'));if(!testRuntime.supports(test))reasons.push('The canonical test is no longer supported by the application Test IR runtime.');if(String(recordValue(record,'TEST_SPEC_SHA256')||'')!==expectedSpecHash)reasons.push('Native runtime result is bound to a stale Test IR identity.');const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);determination=['SATISFIED','VIOLATED','UNDETERMINED','EXECUTION_FAILED'].includes(claimed)?claimed:'UNDETERMINED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}else{if(!evidence.sufficient)reasons.push(...evidence.reasons);const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);const actual=recordValue(record,'ACTUAL_RESULT'),expected=recordValue(record,'EXPECTED_RESULT')||recordValue(test,'EXPECTED_RESULT'),out=controlledOutcome(actual);if(out)determination=out;else if(!adjudicationEmpty(actual)&&!adjudicationEmpty(expected)&&upper(actual)===upper(expected))determination='SATISFIED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}}`,
'native deterministic adjudication');

  s=replaceOnce(s,
`  if(byteTest){requiredEvidenceClasses.push('APPLICATION_VERIFIED_BYTES');if(!verifiedArtifacts.length&&!matchingProductArtifact)reasons.push('Byte-identity evidence requires application-verified artifact bytes and SHA-256; prose or a claimed hash is insufficient.');}`,
`  if(upper(recordValue(test,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&recordValue(result,'RUNTIME_EXECUTION')){requiredEvidenceClasses.push('APPLICATION_RUNTIME_EXECUTION');const runtime=recordValue(result,'RUNTIME_EXECUTION'),inputs=safe(runtime?.artifactIdentities),currentArtifacts=new Map(recordsForCurrentScope(project,'artifacts').map(a=>[recordId(a,'artifacts'),a]));if(!runtime?.evidenceSha256||!inputs.length)reasons.push('Application-native deterministic evidence is missing its runtime execution identity or exact input artifact manifest.');for(const input of inputs){const a=currentArtifacts.get(String(input.artifactId||''));if(!a||upper(recordValue(a,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||String(recordValue(a,'SHA256')||'')!==String(input.sha256||'')||Number(recordValue(a,'BYTE_SIZE'))!==Number(input.byteSize))reasons.push('Application-native deterministic evidence references stale or unverified input bytes.');}}\n  if(byteTest&&!(upper(recordValue(test,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&recordValue(result,'RUNTIME_EXECUTION'))){requiredEvidenceClasses.push('APPLICATION_VERIFIED_BYTES');if(!verifiedArtifacts.length&&!matchingProductArtifact)reasons.push('Byte-identity evidence requires application-verified artifact bytes and SHA-256; prose or a claimed hash is insufficient.');}`,
'native evidence sufficiency');

  s=replaceOnce(s,
`    case 22:{\n      requireAccepted();const mandatoryIds=`,
`    case 22:{\n      const mandatoryIds=`,
'Stage 22 no unconditional external acceptance');
  s=replaceOnce(s,
`const expected=recordsForCurrentScope(project,'tests').filter(test=>mandatoryIds.has(testRequirementId(test))&&upper(recordValue(test,'TEST_TYPE'))==='DETERMINISTIC'&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY'))),results=`,
`const expected=recordsForCurrentScope(project,'tests').filter(test=>mandatoryIds.has(testRequirementId(test))&&upper(recordValue(test,'TEST_TYPE'))==='DETERMINISTIC'&&!['RETIRED','BLOCKED','NOT READY'].includes(upper(recordValue(test,'STATUS')||'READY'))),externalExpected=expected.filter(test=>upper(recordValue(test,'EXECUTION_MODE'))!=='APPLICATION_DETERMINISTIC');if(externalExpected.length)requireAccepted();const results=`,
'Stage 22 conditional acceptance');
  s=replaceOnce(s,
`if(results.some(record=>upper(recordValue(record,'DETERMINATION'))!=='SATISFIED'))reasons.push('A deterministic finished-product verification is violated or undetermined.');`,
`if(results.some(record=>effectiveDetermination('deterministicResults',record,testForResult(project,record),project)!=='SATISFIED'))reasons.push('A deterministic finished-product verification is violated or undetermined.');`,
'Stage 22 effective determinations');

  s=replaceOnce(s,
`  if(relevant.length&&relevant.every(i=>i.operatorAction==='NO_ACTION'))return 'No external action required. The application can perform the current deterministic verification route.';`,
`  if(relevant.length&&relevant.every(i=>i.executionMode==='APPLICATION_DETERMINISTIC')){const runnable=relevant.filter(i=>i.userAction==='RUN_IN_APP');if(runnable.length)return 'No external action required. Run '+runnable.length+' application-native deterministic test'+(runnable.length===1?'':'s')+' against the exact current product bytes.';return 'No external action required. Application-native deterministic verification is waiting for its exact required product bytes.';}`,
'next action native runtime');

  s=replaceOnce(s,
`  currentScope,recordsForScope,recordsForCurrentScope,scopeForIteration,recordsForIteration,verificationMatrix,evaluateIteration,DERIVATIONS,coverageMetrics,convergenceMetrics,releaseMetrics,applicationTestCapabilities,capabilityAffirmativelyAvailable,testExecutionPlan,executionHandoff,`,
`  currentScope,recordsForScope,recordsForCurrentScope,scopeForIteration,recordsForIteration,verificationMatrix,evaluateIteration,DERIVATIONS,coverageMetrics,convergenceMetrics,releaseMetrics,applicationTestCapabilities,capabilityAffirmativelyAvailable,nativeTestInputPlan,testExecutionPlan,recordNativeDeterministicResult,executionHandoff,`,
'engine exports');
  write('workflow-engine.js',s);
}

// response-ingestion.js — external agents cannot impersonate the application runtime, while external deterministic results keep their full observation contract.
{
  let s=read('response-ingestion.js');
  const needle=`        if(collection==='sources')for(const message of schema.sourceClassificationIssues(record.fields))issues.push(issue('INVALID_EXTERNAL_SOURCE',path,message));`;
  const replacement=`        if(collection==='tests'&&upper(record.fields.EXECUTION_MODE)==='APPLICATION_DETERMINISTIC'){const runtime=globalThis.closedLoopTestRuntime;if(!runtime||String(record.fields.REQUIRED_CAPABILITY||'')!==runtime.CAPABILITY)issues.push(issue('INVALID_APPLICATION_TEST_CAPABILITY',path,'APPLICATION_DETERMINISTIC tests must target the registered Closed Loop Test Runtime capability.'));const validation=runtime?.validateSpec(record.fields.EXECUTABLE_SPEC,record.fields.EXECUTABLE_INPUT_BINDINGS);if(!validation?.valid)issues.push(issue('INVALID_EXECUTABLE_TEST_IR',path,validation?.reasons?.join(' ')||'A valid executable Test IR specification is required.'));if(String(record.fields.EXECUTABLE_SPEC_VERSION||'')!==runtime?.SPEC_VERSION)issues.push(issue('INVALID_EXECUTABLE_TEST_IR_VERSION',path,\`Executable Test IR version must be \${runtime?.SPEC_VERSION||'the current application version'}.\`));}\n        if(collection==='deterministicResults'){const testId=String(record.relationships?.TEST_ID?.recordId||record.relationships?.TEST_ID?.tempKey||record.fields.TEST_ID||'');let test=null;if(record.relationships?.TEST_ID?.recordId)test=workflow.records(project,'tests',{active:true}).find(t=>workflow.recordId(t,'tests')===String(record.relationships.TEST_ID.recordId));if(test&&upper(workflow.recordValue(test,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC')issues.push(issue('WRONG_EXECUTOR',path,'Application-native deterministic results are created only by the application Test IR runtime; an external response cannot submit them.'));for(const required of ['TOOL_AND_VERSION','PROCEDURE','EXPECTED_RESULT','ACTUAL_RESULT','DETERMINATION','EVIDENCE']){const value=record.fields[required];if(value===undefined||value===null||(typeof value==='string'&&!value.trim()))issues.push(issue('MISSING_REQUIRED_FIELD',\`${path}/fields/\${pointerEscape(required)}\`,\`\${required} is required for an externally executed deterministic result.\`));}}\n        if(collection==='sources')for(const message of schema.sourceClassificationIssues(record.fields))issues.push(issue('INVALID_EXTERNAL_SOURCE',path,message));`;
  s=replaceOnce(s,needle,replacement,'ingestion native test validation');
  write('response-ingestion.js',s);
}

// index.html — load the runtime directly once; the worker is loaded only by the runtime.
{
  let s=read('index.html');
  s=s.replace("connect-src 'self'; object-src 'none'", "connect-src 'self'; worker-src 'self' blob:; object-src 'none'");
  const scriptNeedle=/<script defer src="workflow-schema\.js\?v=([^"]+)"><\/script>\s*<script defer src="workflow-engine\.js\?v=\1"><\/script>/;
  const m=s.match(scriptNeedle);if(!m)throw new Error('Could not locate workflow-schema/workflow-engine script pair.');
  s=s.replace(scriptNeedle,`<script defer src="workflow-schema.js?v=${m[1]}"></script>\n<script defer src="test-runtime.js?v=${m[1]}"></script>\n<script defer src="workflow-engine.js?v=${m[1]}"></script>`);
  write('index.html',s);
}

// app-core.js — expose a single compact native verification action, not runtime architecture.
{
  let s=read('app-core.js');
  s=replaceOnce(s,`let core,schema,engine,ingestion,projectStore,projects=[]`, `let core,schema,engine,ingestion,projectStore,testRuntime,projects=[]`,'app runtime variable');
  s=replaceOnce(s,`projectStore=globalThis.closedLoopProjectStore;if(!core||!schema||!engine||!ingestion||!projectStore)throw new Error('Closed-loop runtime modules did not load.');`, `projectStore=globalThis.closedLoopProjectStore;testRuntime=globalThis.closedLoopTestRuntime;if(!core||!schema||!engine||!ingestion||!projectStore||!testRuntime)throw new Error('Closed-loop runtime modules did not load.');`,'app load runtime');
  const guidanceStart=s.indexOf('function testExecutionGuidanceMarkup('),guidanceEnd=s.indexOf('\nfunction ',guidanceStart+10);
  if(guidanceStart<0||guidanceEnd<0)throw new Error('Could not locate testExecutionGuidanceMarkup.');
  const old=s.slice(guidanceStart,guidanceEnd);
  if(!old.includes('Advanced verification details'))throw new Error('Unexpected testExecutionGuidanceMarkup shape.');
  const patched=old.replace(/return `([^`]*?)<details class="record-card execution-routing-detail">/s,(match,prefix)=>{
    const insertion=`\${nativeRunnable.length?\`<div class="notice success native-test-action"><strong>Automatic verification ready.</strong> The application can run \${nativeRunnable.length} mechanical test\${nativeRunnable.length===1?'':'s'} against the exact current product bytes.<div class="button-row"><button type="button" class="primary" id="run-native-tests">Run \${nativeRunnable.length} automatic test\${nativeRunnable.length===1?'':'s'}</button></div></div>\`:''}`;
    return `return \`${prefix}${insertion}<details class="record-card execution-routing-detail">`;
  }).replace(/const plan=engine\.testExecutionPlan\(current\),/,'const plan=engine.testExecutionPlan(current),nativeRunnable=plan.items.filter(item=>n===22&&item.userAction===\'RUN_IN_APP\'&&item.executableNow&&!engine.recordsForCurrentScope(current,\'deterministicResults\').some(result=>String(engine.recordValue(result,\'TEST_ID\')||result.relationships?.TEST_ID||\'\')===item.testId&&engine.effectiveDetermination(\'deterministicResults\',result,engine.recordsForCurrentScope(current,\'tests\').find(test=>engine.recordId(test,\'tests\')===item.testId),current)===\'SATISFIED\')),');
  s=s.slice(0,guidanceStart)+patched+s.slice(guidanceEnd);
  const wireNeedle=`if($('#calculate-release-state'))$('#calculate-release-state').onclick=async()=>{`;
  const nativeHandler=`if($('#run-native-tests'))$('#run-native-tests').onclick=runNativeTests;`;
  if(!s.includes(nativeHandler))s=replaceOnce(s,wireNeedle,nativeHandler+wireNeedle,'wire native tests');
  const insertBefore=s.indexOf('\nfunction createUniqueJobId');
  if(insertBefore<0)throw new Error('Could not locate createUniqueJobId.');
  const fn=`\nasync function runNativeTests(){try{const plan=engine.testExecutionPlan(current),tests=engine.recordsForCurrentScope(current,'tests'),existing=engine.recordsForCurrentScope(current,'deterministicResults'),items=plan.items.filter(item=>item.userAction==='RUN_IN_APP'&&item.executableNow&&!existing.some(result=>String(engine.recordValue(result,'TEST_ID')||result.relationships?.TEST_ID||'')===item.testId&&engine.effectiveDetermination('deterministicResults',result,tests.find(test=>engine.recordId(test,'tests')===item.testId),current)==='SATISFIED'));if(!items.length){announce('automatic verification is already current');return;}const runtimeResults=[];for(const item of items){const test=tests.find(t=>engine.recordId(t,'tests')===item.testId);if(!test)throw new Error('Current deterministic test '+item.testId+' disappeared.');const runtimeArtifacts=[];for(const input of item.nativeInputBindings.resolved){const stored=await projectStore.getArtifact(input.artifactId);if(!stored?.blob)throw new Error('Stored bytes are unavailable for '+input.artifactId+'.');const bytes=await stored.blob.arrayBuffer(),sha256=globalThis.closedLoopHash.sha256Bytes(new Uint8Array(bytes));if(String(sha256)!==String(input.sha256)||Number(bytes.byteLength)!==Number(input.byteSize))throw new Error('Stored bytes no longer match canonical identity for '+input.artifactId+'.');runtimeArtifacts.push({artifactId:input.artifactId,filename:input.filename,sha256:input.sha256,byteSize:input.byteSize,role:input.role,binding:input.binding,bytes});}runtimeResults.push({testId:item.testId,result:await testRuntime.execute({test,artifacts:runtimeArtifacts})});}const next=clone(current);for(const row of runtimeResults)engine.recordNativeDeterministicResult(next,{testId:row.testId,runtimeResult:row.result});await persistReplacement(next);announce('automatic verification complete');render();requestAnimationFrame(()=>document.querySelector('.native-test-action, #validation-report, .next-action-panel')?.scrollIntoView({block:'center',behavior:'smooth'}));}catch(error){announce('automatic verification failed');alert('Automatic verification failed closed: '+(error.message||error));}}\n`;
  s=s.slice(0,insertBefore)+fn+s.slice(insertBefore);
  write('app-core.js',s);
}

// prompt-engine.js — publish the application-owned IR and require exact compilation when faithful.
{
  let s=read('prompt-engine.js');
  s=s.replace(/const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine\/\d+';/,`const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine/22';`);
  const capabilitiesNeedle=`APPLICATION-NATIVE TEST CAPABILITIES`;
  const idx=s.indexOf(capabilitiesNeedle);if(idx<0)throw new Error('Could not locate Stage 06 native capability prompt section.');
  const paragraph=`\n\nCLOSED LOOP TEST IR — APPLICATION-OWNED DETERMINISTIC LANGUAGE\nVersion: \${globalThis.closedLoopTestRuntime?.SPEC_VERSION||'closed-loop-test-spec/1'}\nCapability: \${globalThis.closedLoopTestRuntime?.CAPABILITY||'CLOSED_LOOP_TEST_RUNTIME_V1'}\nSupported subject-neutral operations: \${(globalThis.closedLoopTestRuntime?.OPS||[]).join(', ')}\nFor each test, first determine whether the proposition can be represented faithfully and completely with this registered language. If yes, you MUST use EXECUTION_MODE = APPLICATION_DETERMINISTIC, REQUIRED_CAPABILITY = \${globalThis.closedLoopTestRuntime?.CAPABILITY||'CLOSED_LOOP_TEST_RUNTIME_V1'}, EXECUTABLE_SPEC_VERSION = \${globalThis.closedLoopTestRuntime?.SPEC_VERSION||'closed-loop-test-spec/1'}, and provide EXECUTABLE_SPEC plus EXECUTABLE_INPUT_BINDINGS. Compose only supported operations. Never invent an operation and never emit arbitrary JavaScript, Python, shell commands, or executable code. If the proposition cannot be faithfully reduced to the registered deterministic language, choose the truthful residual executor route instead. Do not force a semantic, visual, physical, proprietary-system, or otherwise unobservable proposition into mechanical Test IR. The application validates the IR and owns the pass/fail result when it executes it.\n`;
  const lineEnd=s.indexOf('\n',idx);
  s=s.slice(0,lineEnd+1)+paragraph+s.slice(lineEnd+1);
  write('prompt-engine.js',s);
}

console.log('Test IR integration patch applied.');
