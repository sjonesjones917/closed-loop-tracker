import fs from 'node:fs';
import vm from 'node:vm';
globalThis.Event=globalThis.Event||class Event{constructor(type){this.type=type;}};
globalThis.dispatchEvent=globalThis.dispatchEvent||(()=>true);
for(const file of ['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js','prompt-engine.js','response-ingestion.js'])vm.runInThisContext(fs.readFileSync(file,'utf8'),{filename:file});
const core=globalThis.closedLoopCore,schema=globalThis.closedLoopWorkflowSchema,engine=globalThis.closedLoopWorkflowEngine,prompts=globalThis.closedLoopPromptEngine,ingestion=globalThis.closedLoopResponseIngestion,hash=globalThis.closedLoopHash;
const assert=(value,message)=>{if(!value)throw new Error(message);};
const p=core.createBlankState('JOB-STAGE01-CLOSURE');
Object.assign(p.job,{JOB_TITLE:'Intake closure',JOB_OWNER:'Operator',EXACT_USER_OBJECTIVE_VERBATIM:'Build the exact requested product.',SUPPLIED_MATERIALS_INVENTORY:'intent.txt',REQUIRED_OUTPUT_FORMAT:'Exact requested artifacts',PROHIBITED_ACTIONS:'Do not discard supplied intent.',EXPLICIT_USER_REQUIREMENTS:'Capture every supplied requirement exactly.',CURRENT_INPUT_VERSION:'INPUT-v001'});
engine.ensureShape(p);
engine.registerArtifactBytes(p,{stage:1,artifactId:'ARTIFACT-INTENT-001',filename:'intent.txt',mediaType:'text/plain',byteSize:42,sha256:'aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa',role:'HUMAN_INPUT'});
const manifest=engine.intakeCoverageManifest(p);
assert(manifest.units.some(x=>x.kind==='SUPPLIED_MATERIAL'&&x.artifactId==='ARTIFACT-INTENT-001'),'Stage 01 intake manifest does not bind supplied artifact identity.');
const prompt={...prompts.buildPromptRecord(1,p),generatedAt:new Date().toISOString()};
p.projectData.generatedPrompts.push(prompt);
for(const unit of manifest.units)assert(prompt.prompt.includes(unit.unitId),`Prompt 01 omitted ${unit.unitId}.`);
assert(prompt.prompt.includes('Classify every APPLICATION INTAKE MANIFEST unit exactly once'),'Prompt 01 does not command closed intake accounting.');
assert(!prompt.prompt.includes('EXECUTABLE_KIND = CUSTOM_PIPELINE'),'Prompt still contains obsolete CUSTOM_PIPELINE instruction.');
const capture={schema:'closed-loop-stage01-capture/1',inputVersion:manifest.inputVersion,manifestSha256:manifest.manifestSha256,units:manifest.units.map((unit,index)=>({sourceUnitId:unit.unitId,sourceRawValueSha256:unit.rawValueSha256,disposition:'retained as context',reason:'Preserved as controlling project authority.',extractedStatements:[{statementKey:`statement-${index+1}`,text:unit.rawValueText,statementClass:'CONTEXT'}]}))};
const incomplete=structuredClone(capture);incomplete.units.pop();
assert(!engine.evaluateIntakeAccounting(p,{capture:JSON.stringify(incomplete)}).complete,'Stage 01 accepted incomplete intake accounting.');
assert(engine.evaluateIntakeAccounting(p,{capture:JSON.stringify(capture)}).complete,'Complete Stage 01 intake accounting did not close.');
const evidence=[{temporaryKey:'evidence-1',kind:'INTAKE',description:'Stage 01 capture',authorityType:'AGENT_CLAIM',location:'response',content:'capture'}];
const envelope=inputCapture=>({schema:schema.RESPONSE_SCHEMA,jobId:p.job.JOB_ID,stage:1,operation:prompt.operation,promptIdentity:{instructionId:prompt.instructionId,bodySha256:prompt.bodySha256,contractSha256:prompt.contractSha256,contextSignature:prompt.contextSignature},scope:prompt.scope,responseType:'DATA_PROPOSAL',humanInputRequests:[],stageData:{EXACT_DELIVERABLE_REQUESTED:'Exact requested product',ASSUMPTIONS:'NONE',UNKNOWN_INFORMATION:'NONE',INPUT_SET_CONTENTS:JSON.stringify(inputCapture)},records:{},evidence,unresolved:[],warnings:[],attachments:[]});
let validation=ingestion.validateEnvelope(p,envelope(incomplete),{stage:1,promptRecord:prompt,rawSha256:hash.sha256Value(envelope(incomplete)),files:[]});
assert(validation.issues.some(x=>x.code==='INCOMPLETE_INTAKE_ACCOUNTING'),'Ingestion accepted incomplete Stage 01 accounting.');
validation=ingestion.validateEnvelope(p,envelope(capture),{stage:1,promptRecord:prompt,rawSha256:hash.sha256Value(envelope(capture)),files:[]});
assert(!validation.issues.some(x=>x.code==='INCOMPLETE_INTAKE_ACCOUNTING'),`Stage 01 complete intake closure rejected: ${JSON.stringify(validation.issues)}`);
console.log('verify-stage01-intake-closure: PASS');
