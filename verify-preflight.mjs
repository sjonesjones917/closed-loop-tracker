import fs from 'node:fs';
import vm from 'node:vm';
import {recordProposal,evidence} from './test-fixtures.mjs';
globalThis.Event=globalThis.Event||class Event{constructor(type){this.type=type;}};
globalThis.dispatchEvent=globalThis.dispatchEvent||(()=>true);
for(const f of ['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js','prompt-engine.js','response-ingestion.js'])vm.runInThisContext(fs.readFileSync(f,'utf8'),{filename:f});
const core=globalThis.closedLoopCore,schema=globalThis.closedLoopWorkflowSchema,engine=globalThis.closedLoopWorkflowEngine,prompts=globalThis.closedLoopPromptEngine,ingestion=globalThis.closedLoopResponseIngestion;
const assert=(v,m)=>{if(!v)throw new Error(m)};
function seed(jobId){
 const p=core.createBlankState(jobId);Object.assign(p.job,{JOB_TITLE:'Stage 09 independent preflight proof',EXACT_USER_OBJECTIVE_VERBATIM:'Produce the current required deliverable.',CURRENT_INPUT_VERSION:'INPUT-v001',CURRENT_SOURCE_SET_VERSION:'SOURCE-SET-v001',CURRENT_RESEARCH_VERSION:'RESEARCH-v001',CURRENT_REQUIREMENTS_VERSION:'REQUIREMENTS-v001',CURRENT_TEST_SUITE_VERSION:'TEST-SUITE-v001'});engine.ensureShape(p);
 for(let n=1;n<=7;n++){p.stages[n].status='COMPLETE';p.stages[n].gate={complete:true,blocked:false,reasons:[]};}
 const scope=engine.currentScope(p),requirementScope={...scope,instructionVersion:null};
 p.projectData.requirements.push({id:'REQ-1',stage:4,active:true,scope:{...requirementScope},fields:{REQ_ID:'REQ-1',MANDATORY_OPTIONAL_STATUS:'MANDATORY',STATUS:'ACTIVE',OBLIGATION:'Produce the required deliverable.',OBSERVABLE_SATISFACTION_CONDITION:'Required output exists.',FAILURE_CONDITION:'Required output is absent.'}});
 p.projectData.propositions.push({id:'PROP-1',stage:4,active:true,scope:{...requirementScope},fields:{PROPOSITION_ID:'PROP-1',REQUIREMENT_ID:'REQ-1',PROPOSITION_TEXT:'The required deliverable is produced.',STATUS:'CURRENT'},relationships:{REQUIREMENT_ID:'REQ-1'}});
 p.projectData.applicabilityRecords.push({id:'APP-1',stage:5,active:true,scope:{...requirementScope},fields:{APPLICABILITY_ID:'APP-1',SUBJECT_ID:'PROP-1',PROPOSED_APPLICABILITY:'APPLICABLE',SELECTED_APPLICABILITY:'APPLICABLE',TRUTH_VALUE:'TRUE',EPISTEMIC_BASIS:'EXTERNALLY_SUPPORTED',CURRENT_SCOPE_STATUS:'CURRENT',FRESHNESS_STATUS:'CURRENT',CONTRADICTION_STATUS:'CLEAR',REASONS:['Current mandatory fixture applies.']},relationships:{SUBJECT_ID:'PROP-1'}});
 p.projectData.proofExpressions.push({id:'PROOF-1',stage:6,active:true,scope:{...requirementScope},fields:{PROOF_EXPRESSION_ID:'PROOF-1',TARGET_PROPOSITION_ID:'PROP-1',PROPOSED_EXPRESSION:{type:'LEAF',observationId:'OBS-FUTURE'},NORMALIZED_EXPRESSION:{type:'LEAF',observationId:'OBS-FUTURE'},SEMANTIC_EQUIVALENCE_DISPOSITION:'EQUIVALENT',ACCEPTED_SEMANTIC_REVIEW_IDS:['REVIEW-1']},relationships:{TARGET_PROPOSITION_ID:'PROP-1'}});
 const pr={...prompts.buildPromptRecord(8,p,{operation:'COMPLETE'}),generatedAt:new Date().toISOString()};p.projectData.generatedPrompts.push(pr);
 const inst=recordProposal(schema,'instructions',{tempKey:'instruction',overrides:{OBJECTIVE:'Produce required content',AUTHORIZED_INPUTS:'Current canonical inputs',FAILURE_HANDLING:'Fail closed',AUTHORITY_RULES:'Use canonical authority',SCOPE:'Current job',PROHIBITIONS:'No invention',DEFINED_TERMS:'Defined',ORDERED_PROCEDURE:'Execute in order',TOOL_REQUIREMENTS:'Available tools only',OUTPUT_CONTRACT:'Structured output',FACTUAL_STATE_HANDLING:'Use explicit states',REJECTION_BLOCKING_RULES:'Block uncertainty',COMPLETION_CONDITIONS:'All gates pass',REQUIREMENT_TRACEABILITY:'Trace every requirement',INSTRUCTION_TEXT:'Controlled production instruction'}});
 const trace=recordProposal(schema,'instructionTraces',{tempKey:'trace',relationships:{REQ_ID:{recordId:'REQ-1'},INSTRUCTION_ID:{tempKey:'instruction'}},overrides:{INSTRUCTION_LOCATION:'Instruction section 1',IMPLEMENTED_BEHAVIOR:'Implements required content'}});
 const env={schema:schema.RESPONSE_SCHEMA,contractProfileId:schema.CONTRACT_PROFILE_ID,jobId:p.job.JOB_ID,stage:8,operation:'COMPLETE',promptIdentity:{instructionId:pr.instructionId,bodySha256:pr.bodySha256,contractSha256:pr.contractSha256,contextSignature:pr.contextSignature},scope:pr.scope,responseType:'DATA_PROPOSAL',humanInputRequests:[],stageData:{},records:{instructions:[inst],instructionTraces:[trace]},evidence:[evidence('stage-08-prerequisite')],unresolved:[],warnings:[],attachments:[]};
 const prepared=ingestion.prepare(p,{stage:8,text:JSON.stringify(env),promptRecord:pr});assert(prepared.validation.valid,`Stage 08 prerequisite invalid: ${JSON.stringify(prepared.validation.issues)}`);const committed=ingestion.commit(prepared.project,prepared.proposal.proposalId,{operator:'STAGE13_VERIFIER'}).project;for(let n=1;n<=7;n++){committed.stages[n].status='COMPLETE';committed.stages[n].gate={complete:true,blocked:false,reasons:[]};}const gate8=engine.gate(8,committed);assert(gate8.complete,`Stage 08 prerequisite did not complete: ${JSON.stringify(gate8)}`);committed.stages[8].status='COMPLETE';committed.stages[8].gate={complete:true,blocked:false,reasons:[]};return committed;
}
function submit(p,{record=true,determination='SATISFIED'}){
 engine.registerFreshContext(p,{stage:9,externalContextIdentifier:`PREFLIGHT-${p.job.JOB_ID}`,operatorLabel:'STAGE13_VERIFIER',purpose:'REVIEWER'});const contextId=engine.recordId(engine.records(p,'freshContexts').at(-1),'freshContexts');const instructionId=engine.recordId(engine.recordsForCurrentScope(p,'instructions').at(-1),'instructions');
 const pr={...prompts.buildPromptRecord(9,p,{operation:'COMPLETE',scope:{contextId}}),generatedAt:new Date().toISOString()};p.projectData.generatedPrompts.push(pr);
 const records=record?{preflightRecords:[recordProposal(schema,'preflightRecords',{tempKey:'preflight',relationships:{INSTRUCTION_ID:{recordId:instructionId}},overrides:{CLAUSE:'Full current production instruction',DETERMINATION:determination,FINDINGS:determination==='SATISFIED'?'No material ambiguity, conflict, unavailable capability, or unverifiable clause remains.':'Material ambiguity remains unresolved.',EVIDENCE:'Independent preflight review evidence'}})]}:{preflightRecords:[]};
 const envelope={schema:schema.RESPONSE_SCHEMA,contractProfileId:schema.CONTRACT_PROFILE_ID,jobId:p.job.JOB_ID,stage:9,operation:'COMPLETE',promptIdentity:{instructionId:pr.instructionId,bodySha256:pr.bodySha256,contractSha256:pr.contractSha256,contextSignature:pr.contextSignature},scope:pr.scope,responseType:'DATA_PROPOSAL',humanInputRequests:[],stageData:{},records,evidence:[evidence('stage-09-independent-preflight')],unresolved:[],warnings:[],attachments:[]};return {pr,prepared:ingestion.prepare(p,{stage:9,text:JSON.stringify(envelope),promptRecord:pr})};
}
function commitAndGate(prepared){const committed=ingestion.commit(prepared.project,prepared.proposal.proposalId,{operator:'STAGE13_VERIFIER'}).project;for(let n=1;n<=8;n++){committed.stages[n].status='COMPLETE';committed.stages[n].gate={complete:true,blocked:false,reasons:[]};}return {committed,gate:engine.gate(9,committed)};}
{const p=seed('JOB-STAGE13-MISSING-PREFLIGHT'),before=p.projectData.acceptedChanges.length,{prepared}=submit(p,{record:false});assert(prepared.validation.valid,`Missing-preflight fixture should reach Stage 09 gate: ${JSON.stringify(prepared.validation.issues)}`);assert(prepared.project.projectData.acceptedChanges.length===before,'Missing-preflight fixture mutated before acceptance.');const {gate}=commitAndGate(prepared);assert(!gate.complete,'Stage 09 accepted no preflight record.');}
{const p=seed('JOB-STAGE13-MATERIAL-FINDING'),before=p.projectData.acceptedChanges.length,{prepared}=submit(p,{record:true,determination:'VIOLATED'});assert(prepared.validation.valid,`Material-finding fixture should reach Stage 09 gate: ${JSON.stringify(prepared.validation.issues)}`);assert(prepared.project.projectData.acceptedChanges.length===before,'Material-finding fixture mutated before acceptance.');const {gate}=commitAndGate(prepared);assert(!gate.complete,'Stage 09 accepted unresolved material preflight finding.');}
{const p=seed('JOB-STAGE13-REPAIRED'),before=p.projectData.acceptedChanges.length,{pr,prepared}=submit(p,{record:true,determination:'SATISFIED'});assert(prepared.validation.valid,`Repaired preflight rejected: ${JSON.stringify(prepared.validation.issues)}`);assert(prepared.project.projectData.acceptedChanges.length===before,'Repaired preflight mutated before acceptance.');const {gate}=commitAndGate(prepared);assert(gate.complete&&gate.reasons.length===0,`Repaired preflight did not progress: ${JSON.stringify(gate)}`);for(const token of ['independent','preflight','without executing','material'])assert(pr.prompt.toLowerCase().includes(token),`Stage 09 prompt lacks semantic: ${token}`);}
console.log(JSON.stringify({controllerStage:'13',applicationStage:'09',independentPreflight:'PASS',intentionalInvalidFixturesRejected:['missing-preflight-record','unresolved-material-finding'],repairedPathProgressed:true,noMutationBeforeAcceptance:true,promptSemanticsChecked:true,isolatedDisposableProjects:true},null,2));
