import fs from 'node:fs';
import {createHash} from 'node:crypto';
const read=f=>fs.readFileSync(f,'utf8'),write=(f,s)=>fs.writeFileSync(f,s);
function one(text,search,replacement,label){const before=text;if(search instanceof RegExp){let count=0;text=text.replace(search,(...args)=>{count++;return typeof replacement==='function'?replacement(...args):replacement;});if(count!==1)throw new Error(`${label}: expected one regex match, got ${count}`);}else{const i=text.indexOf(search);if(i<0)throw new Error(`${label}: exact text not found`);if(text.indexOf(search,i+search.length)>=0)throw new Error(`${label}: exact text is not unique`);text=text.slice(0,i)+replacement+text.slice(i+search.length);}if(text===before)throw new Error(`${label}: replacement made no change`);return text;}
function patch(file,fn){const a=read(file),b=fn(a);if(a===b)throw new Error(`${file}: patch made no change`);write(file,b);}

patch('test-runtime.js',s=>s.replace("'JSON_SCHEMA','JSON_PATH_ASSERT','CSV_ASSERT','ARCHIVE_CONTENTS'","'JSON_SCHEMA','JSON_PATH_ASSERT','CSV_ASSERT','XML_ASSERT','ARCHIVE_CONTENTS'"));

patch('workflow-schema.js',s=>{
 s=one(s,"const RESPONSE_SCHEMA='closed-loop-stage-response/2';",`const RESPONSE_SCHEMA='closed-loop-stage-response/2';\nconst TEST_IR_VERSION='closed-loop-test-spec/1';\nconst TEST_IR_CAPABILITY='CLOSED_LOOP_TEST_IR';\nconst TEST_IR_OPERATIONS=Object.freeze(['LOAD_ARTIFACT','READ_BYTES','FILE_SIZE','FILENAME','MIME_TYPE','DECODE_UTF8','PARSE_JSON','PARSE_CSV','SELECT_JSON_PATH','COUNT','SUM','MIN','MAX','SORT','UNIQUE','HASH_SHA256','REGEX','LIST_ZIP_ENTRIES','VALIDATE_JSON_SCHEMA','ASSERT_TRUE','ASSERT_EQ','ASSERT_GT','ASSERT_GTE','ASSERT_LT','ASSERT_LTE','ASSERT_MATCH','ASSERT_CONTAINS','ASSERT_NOT_CONTAINS','ASSERT_SET_EQUAL','BYTE_COMPARE']);\nconst TEST_IR_KINDS=Object.freeze(['NONE','FILE_EXISTS','FILE_SIZE','FILE_HASH','FILENAME','MIME_TYPE','TEXT_EQUALS','TEXT_CONTAINS','TEXT_NOT_CONTAINS','REGEX_MATCH','JSON_PARSE','JSON_SCHEMA','JSON_PATH_ASSERT','CSV_ASSERT','XML_ASSERT','ARCHIVE_CONTENTS','NUMERIC_ASSERT','COUNT_ASSERT','SET_ASSERT','MANIFEST_ASSERT','BYTE_COMPARE','CUSTOM_PIPELINE']);\nconst TEST_IR_IMPLEMENTED_KINDS=Object.freeze(TEST_IR_KINDS.filter(kind=>!['NONE','XML_ASSERT','MANIFEST_ASSERT'].includes(kind)));`, 'schema Test IR constants');
 s=one(s,"19:Object.freeze(['EXECUTE_RUN','VERIFY','COMPARE','REGRESSION_VERIFY','CONFIRM'])","19:Object.freeze(['CONFIRM_FREEZE','EXECUTE_RUN','VERIFY','COMPARE','REGRESSION_VERIFY','CONFIRM'])",'Stage 19 CONFIRM_FREEZE');
 s=one(s,/"tests": \{[\s\S]*?\n  \},\n  "failureTests":/m,block=>block.replace('      "ARTIFACT_REQUIREMENTS",\n','      "ARTIFACT_REQUIREMENTS",\n      "EXECUTABLE_KIND",\n      "EXECUTABLE_SPEC_VERSION",\n      "EXECUTABLE_SPEC",\n      "EXECUTABLE_INPUT_BINDINGS",\n'),'tests ownership');
 s=one(s,/"deterministicResults": \{[\s\S]*?\n  \},\n  "meaningResults":/m,block=>block.replace('      "DEFECT_ID"\n    ]','      "DEFECT_ID",\n      "NATIVE_EXECUTION",\n      "NATIVE_RUNTIME_VERSION",\n      "NATIVE_TEST_SPEC_SHA256",\n      "NATIVE_INPUT_IDENTITIES",\n      "NATIVE_EXPECTED_RESULT",\n      "NATIVE_ACTUAL_RESULT",\n      "NATIVE_OBSERVATIONS",\n      "NATIVE_DETERMINATION",\n      "EVIDENCE_ID"\n    ]'),'deterministic ownership');
 s=one(s,/"evidenceRecords": \{[\s\S]*?\n  \},\n  "artifacts":/m,block=>block.replace('      "STATUS"\n    ]','      "STATUS",\n      "APPLICATION_EVIDENCE_KIND",\n      "APPLICATION_DESCRIPTION",\n      "APPLICATION_LOCATION",\n      "APPLICATION_CONTENT"\n    ]'),'evidence ownership');
 s=one(s,"    ARTIFACT_REQUIREMENTS:Object.freeze({valueType:'STRING',enumValues:Object.freeze([]),nullable:false,normalizerKey:null,closedProperties:null})\n  }),",`    ARTIFACT_REQUIREMENTS:Object.freeze({valueType:'STRING',enumValues:Object.freeze([]),nullable:false,normalizerKey:null,closedProperties:null}),\n    EXECUTABLE_KIND:Object.freeze({valueType:'STRING',enumValues:TEST_IR_KINDS,nullable:false,normalizerKey:null,closedProperties:null}),\n    EXECUTABLE_SPEC_VERSION:Object.freeze({valueType:'STRING',enumValues:Object.freeze([TEST_IR_VERSION,'NOT_APPLICABLE']),nullable:false,normalizerKey:null,closedProperties:null}),\n    EXECUTABLE_SPEC:Object.freeze({valueType:'OBJECT',enumValues:Object.freeze([]),nullable:false,normalizerKey:null,closedProperties:null}),\n    EXECUTABLE_INPUT_BINDINGS:Object.freeze({valueType:'OBJECT',enumValues:Object.freeze([]),nullable:false,normalizerKey:null,closedProperties:null})\n  }),\n  'DETERMINISTIC-RESULT':Object.freeze({NATIVE_EXECUTION:Object.freeze({valueType:'BOOLEAN',enumValues:Object.freeze([]),nullable:false,normalizerKey:null,closedProperties:null}),NATIVE_INPUT_IDENTITIES:Object.freeze({valueType:'OBJECT_ARRAY',enumValues:Object.freeze([]),nullable:false,normalizerKey:null,closedProperties:null}),NATIVE_OBSERVATIONS:Object.freeze({valueType:'STRING_ARRAY',enumValues:Object.freeze([]),nullable:false,normalizerKey:null,closedProperties:null}),NATIVE_DETERMINATION:Object.freeze({valueType:'STRING',enumValues:Object.freeze(['SATISFIED','VIOLATED','UNDETERMINED','EXECUTION_FAILED']),nullable:false,normalizerKey:null,closedProperties:null}),EVIDENCE_ID:Object.freeze({valueType:'REFERENCE',enumValues:Object.freeze([]),nullable:false,normalizerKey:null,closedProperties:null})}),\n  'EVIDENCE':Object.freeze({APPLICATION_CONTENT:Object.freeze({valueType:'OBJECT',enumValues:Object.freeze([]),nullable:false,normalizerKey:null,closedProperties:null})}),`,'record type overrides');
 s=one(s,"'TEST_ID','REQ_ID','TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','INPUTS','TOOLS','PROCEDURE','EXPECTED_RESULT','FAILURE_CONDITION','EVIDENCE_TO_PRESERVE','STATUS'", "'TEST_ID','REQ_ID','TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','EXECUTABLE_KIND','EXECUTABLE_SPEC_VERSION','EXECUTABLE_SPEC','EXECUTABLE_INPUT_BINDINGS','INPUTS','TOOLS','PROCEDURE','EXPECTED_RESULT','FAILURE_CONDITION','EVIDENCE_TO_PRESERVE','STATUS'",'tests record fields');
 s=one(s,"required:['TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','INPUTS','TOOLS','PROCEDURE','EXPECTED_RESULT','FAILURE_CONDITION','EVIDENCE_TO_PRESERVE','STATUS']", "required:['TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','EXECUTABLE_KIND','EXECUTABLE_SPEC_VERSION','EXECUTABLE_SPEC','EXECUTABLE_INPUT_BINDINGS','INPUTS','TOOLS','PROCEDURE','EXPECTED_RESULT','FAILURE_CONDITION','EVIDENCE_TO_PRESERVE','STATUS']",'tests required fields');
 s=one(s,"'RESULT_ID','PRODUCT_ID','PRODUCT_SHA256','TEST_ID','TOOL_AND_VERSION','PROCEDURE','EXPECTED_RESULT','ACTUAL_RESULT','DETERMINATION','EVIDENCE','DEFECT_ID'", "'RESULT_ID','PRODUCT_ID','PRODUCT_SHA256','TEST_ID','TOOL_AND_VERSION','PROCEDURE','EXPECTED_RESULT','ACTUAL_RESULT','DETERMINATION','EVIDENCE','DEFECT_ID','NATIVE_EXECUTION','NATIVE_RUNTIME_VERSION','NATIVE_TEST_SPEC_SHA256','NATIVE_INPUT_IDENTITIES','NATIVE_EXPECTED_RESULT','NATIVE_ACTUAL_RESULT','NATIVE_OBSERVATIONS','NATIVE_DETERMINATION','EVIDENCE_ID'",'deterministic result fields');
 s=one(s,"relationships:{PRODUCT_ID:'products',TEST_ID:'tests',DEFECT_ID:'defects'}", "relationships:{PRODUCT_ID:'products',TEST_ID:'tests',DEFECT_ID:'defects',EVIDENCE_ID:'evidenceRecords'}",'deterministic result evidence relationship');
 s=one(s,"'EVIDENCE_ID','KIND','DESCRIPTION','AUTHORITY_TYPE','SOURCE_ID','LOCATION','CONTENT','ATTACHMENT_ID','SHA256','STATUS'", "'EVIDENCE_ID','KIND','DESCRIPTION','AUTHORITY_TYPE','SOURCE_ID','LOCATION','CONTENT','ATTACHMENT_ID','SHA256','STATUS','APPLICATION_EVIDENCE_KIND','APPLICATION_DESCRIPTION','APPLICATION_LOCATION','APPLICATION_CONTENT'",'evidence fields');
 s=one(s,"19:Object.freeze({EXECUTE_RUN:Object.freeze", "19:Object.freeze({CONFIRM_FREEZE:Object.freeze({readCollections:['convergenceRecords','candidateFreezes','iterations'],agentWritableCollections:[],allowedStageData:[]}),EXECUTE_RUN:Object.freeze",'Stage19 operation contract');
 const validator=`\nfunction validateExecutableTestDefinition(fields={}){\n  const mode=String(fields.EXECUTION_MODE||'').toUpperCase(),kind=String(fields.EXECUTABLE_KIND||''),version=String(fields.EXECUTABLE_SPEC_VERSION||''),spec=fields.EXECUTABLE_SPEC,bindings=fields.EXECUTABLE_INPUT_BINDINGS,reasons=[];\n  if(!TEST_IR_KINDS.includes(kind))reasons.push('EXECUTABLE_KIND is not registered.');\n  if(!bindings||typeof bindings!=='object'||Array.isArray(bindings))reasons.push('EXECUTABLE_INPUT_BINDINGS must be an object.');\n  if(!spec||typeof spec!=='object'||Array.isArray(spec))reasons.push('EXECUTABLE_SPEC must be an object.');\n  if(mode==='APPLICATION_DETERMINISTIC'){if(String(fields.REQUIRED_CAPABILITY||'')!==TEST_IR_CAPABILITY)reasons.push('APPLICATION_DETERMINISTIC must use '+TEST_IR_CAPABILITY+'.');if(!TEST_IR_IMPLEMENTED_KINDS.includes(kind))reasons.push('Executable kind '+kind+' is not implemented by the current application runtime.');if(version!==TEST_IR_VERSION)reasons.push('Executable spec version must be '+TEST_IR_VERSION+'.');if(spec?.version!==TEST_IR_VERSION)reasons.push('EXECUTABLE_SPEC.version must be '+TEST_IR_VERSION+'.');if(kind==='CUSTOM_PIPELINE'){if(!Array.isArray(spec?.steps)||!spec.steps.length)reasons.push('CUSTOM_PIPELINE requires non-empty steps.');for(const step of Array.isArray(spec?.steps)?spec.steps:[]){if(!TEST_IR_OPERATIONS.includes(String(step?.op||'')))reasons.push('Unsupported Test IR operation '+String(step?.op||'UNKNOWN')+'.');}}}\n  else{if(kind!=='NONE')reasons.push('Non-application execution must use EXECUTABLE_KIND NONE.');if(version!=='NOT_APPLICABLE')reasons.push('Non-application execution must use EXECUTABLE_SPEC_VERSION NOT_APPLICABLE.');}\n  return {valid:reasons.length===0,supported:reasons.length===0,reasons,kind,version,spec,bindings};\n}\n`;
 s=one(s,"\nfunction authorizeMutation({fieldDefinition,actor,mutationType}){",validator+"\nfunction authorizeMutation({fieldDefinition,actor,mutationType}){",'schema executable validator');
 s=one(s,"PRODUCER,RESPONSE_SCHEMA,RESPONSE_TYPES,CONFLICT_POLICIES,", "PRODUCER,RESPONSE_SCHEMA,RESPONSE_TYPES,CONFLICT_POLICIES,TEST_IR_VERSION,TEST_IR_CAPABILITY,TEST_IR_OPERATIONS,TEST_IR_KINDS,TEST_IR_IMPLEMENTED_KINDS,",'schema exports constants');
 s=one(s,"operationContract,authorizeMutation,", "operationContract,authorizeMutation,validateExecutableTestDefinition,",'schema export validator');
 return s;
});

patch('workflow-engine.js',s=>{
 s=one(s,"  project.projectData.stageRecords=project.projectData.stageRecords&&typeof project.projectData.stageRecords==='object'?project.projectData.stageRecords:{};",`  project.projectData.stageRecords=project.projectData.stageRecords&&typeof project.projectData.stageRecords==='object'?project.projectData.stageRecords:{};\n  for(const test of project.projectData.tests){test.fields=test.fields&&typeof test.fields==='object'?test.fields:{};const current={...test.fields,...test};if(!Object.prototype.hasOwnProperty.call(current,'EXECUTABLE_KIND'))test.fields.EXECUTABLE_KIND=test.EXECUTABLE_KIND='NONE';if(!Object.prototype.hasOwnProperty.call(current,'EXECUTABLE_SPEC_VERSION'))test.fields.EXECUTABLE_SPEC_VERSION=test.EXECUTABLE_SPEC_VERSION='NOT_APPLICABLE';if(!Object.prototype.hasOwnProperty.call(current,'EXECUTABLE_SPEC'))test.fields.EXECUTABLE_SPEC=test.EXECUTABLE_SPEC={};if(!Object.prototype.hasOwnProperty.call(current,'EXECUTABLE_INPUT_BINDINGS'))test.fields.EXECUTABLE_INPUT_BINDINGS=test.EXECUTABLE_INPUT_BINDINGS={};}`,'test migration defaults');
 s=one(s,"const APPLICATION_TEST_EXECUTORS=Object.freeze({});\nfunction applicationTestCapabilities(){return Object.freeze(Object.keys(APPLICATION_TEST_EXECUTORS));}",`function executableTestDefinition(test){const fields=recordFields(test),validation=schema.validateExecutableTestDefinition(fields);return {...validation,capability:String(recordValue(test,'REQUIRED_CAPABILITY')||''),kind:String(recordValue(test,'EXECUTABLE_KIND')||''),spec:clone(recordValue(test,'EXECUTABLE_SPEC')||{}),bindings:clone(recordValue(test,'EXECUTABLE_INPUT_BINDINGS')||{})};}\nfunction applicationTestCapabilities(){return Object.freeze([schema.TEST_IR_CAPABILITY]);}`,'native executor registry replacement');
 s=one(s,"  if(mode==='APPLICATION_DETERMINISTIC')return Object.prototype.hasOwnProperty.call(APPLICATION_TEST_EXECUTORS,requiredCapability);", "  if(mode==='APPLICATION_DETERMINISTIC')return String(requiredCapability)===schema.TEST_IR_CAPABILITY;",'native capability availability');
 s=s.replace("Object.prototype.hasOwnProperty.call(APPLICATION_TEST_EXECUTORS,String(recordValue(test,'REQUIRED_CAPABILITY')||''))","executableTestDefinition(test).supported");
 s=s.replace("Object.prototype.hasOwnProperty.call(APPLICATION_TEST_EXECUTORS,requiredCapability)","executableTestDefinition(test).supported");
 const oldItems=/const items=recordsForCurrentScope\(project,'tests'\)\.map\(test=>\{const mode=[\s\S]*?return \{testId:recordId\(test,'tests'\),requirementId:testRequirementId\(test\),testType:[\s\S]*?handoff:\{send,withhold,expectBack\}\};\}\);/m;
 s=one(s,oldItems,`const items=recordsForCurrentScope(project,'tests').map(test=>{const mode=upper(recordValue(test,'EXECUTION_MODE'))||'UNSPECIFIED',requiredCapability=String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim(),artifactRequirements=String(recordValue(test,'ARTIFACT_REQUIREMENTS')||'').trim(),artifactRequired=Boolean(artifactRequirements&&!['NONE','NOT APPLICABLE','N/A'].includes(upper(artifactRequirements))),evidenceIds=safe(test.evidenceRefs).map(String),native=mode==='APPLICATION_DETERMINISTIC'?executableTestDefinition(test):null,bindingRules=native?.bindings&&typeof native.bindings==='object'?Object.values(native.bindings):[],bindingArtifactIds=bindingRules.map(rule=>{if(typeof rule==='string')return rule;if(rule?.artifactId)return String(rule.artifactId);const role=String(rule?.artifactRole||''),filename=String(rule?.filename||'');const matches=[...artifactsById.values()].filter(a=>(!role||upper(recordValue(a,'ROLE'))===upper(role))&&(!filename||String(recordValue(a,'FILENAME'))===filename));return matches.length===1?recordId(matches[0],'artifacts'):'';}).filter(Boolean),evidenceArtifactIds=evidenceIds.map(id=>evidenceById.get(id)).map(item=>String(recordValue(item,'ATTACHMENT_ID')||item?.relationships?.ATTACHMENT_ID||'').trim()).filter(Boolean),artifactIds=[...new Set([...bindingArtifactIds,...evidenceArtifactIds])],missingArtifactIds=artifactIds.filter(id=>!artifactsById.has(id)),unverifiedArtifactIds=artifactIds.filter(id=>{const artifact=artifactsById.get(id);return artifact&&upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED';}),nativeNeedsArtifact=mode==='APPLICATION_DETERMINISTIC'&&Object.keys(native?.bindings||{}).length>0,artifactReady=(!artifactRequired&&!nativeNeedsArtifact)||(artifactIds.length>0&&!missingArtifactIds.length&&!unverifiedArtifactIds.length),applicationExecutorSupported=mode!=='APPLICATION_DETERMINISTIC'||Boolean(native?.supported),validMode=Object.prototype.hasOwnProperty.call(TEST_EXECUTION_ACTIONS,mode),capabilityReady=mode==='APPLICATION_DETERMINISTIC'?applicationExecutorSupported:capabilityAffirmativelyAvailable(project,requiredCapability,mode),executableNow=validMode&&capabilityReady&&artifactReady,blockingReason=!validMode?'Execution mode is not validly classified.':!requiredCapability?'Required capability is missing.':mode==='UNAVAILABLE'?'Required capability is explicitly unavailable.':mode==='APPLICATION_DETERMINISTIC'&&!applicationExecutorSupported?'The application-native Test IR definition is unsupported: '+safe(native?.reasons).join(' '):!capabilityReady?'Required capability '+requiredCapability+' is not affirmatively available in current canonical state.':!artifactReady?'Required exact artifact bytes are missing, ambiguous, or no longer verified.':null,requiredArtifactNames=artifactIds.map(id=>String(recordValue(artifactsById.get(id),'FILENAME')||id)),operatorAction=executableNow?actionMap[mode]||'BLOCKED':'BLOCKED',executionRoute=mode==='APPLICATION_DETERMINISTIC'?'APPLICATION':mode==='INDEPENDENT_AGENT_REVIEW'?'INDEPENDENT_AI':mode==='HUMAN_INSPECTION'?'HUMAN':mode==='EXTERNAL_SYSTEM'?'EXTERNAL_SYSTEM':mode==='EXTERNAL_AGENT_TOOL'?'EXTERNAL_AGENT_TOOL':'BLOCKED',userAction=!executableNow?'BLOCKED':mode==='APPLICATION_DETERMINISTIC'?'RUN_IN_APP':mode==='INDEPENDENT_AGENT_REVIEW'?'SEND_VERIFICATION_PACKAGE':mode==='HUMAN_INSPECTION'?'COMPLETE_INSPECTION':mode==='EXTERNAL_SYSTEM'?'RUN_EXTERNAL_SYSTEM':'SEND_VERIFICATION_PACKAGE',requiredEvidenceDescription=String(recordValue(test,'EVIDENCE_TO_PRESERVE')||'Preserve evidence capable of establishing the tested proposition.').trim(),returnRequirements={structuredResponse:mode!=='APPLICATION_DETERMINISTIC',requiredAttachmentKinds:artifactRequired?['CONTRACTED_VERIFICATION_ARTIFACT_OR_REPORT']:[],requiredEvidenceDescription},send:artifactIds.filter(id=>artifactsById.has(id)).map(id=>{const a=artifactsById.get(id);return {artifactId:id,filename:String(recordValue(a,'FILENAME')||id),sha256:String(recordValue(a,'SHA256')||'UNKNOWN'),role:String(recordValue(a,'ROLE')||artifactRequirements||'TEST_INPUT')};}),withhold=mode==='INDEPENDENT_AGENT_REVIEW'?[{artifactIdOrCategory:'prior verifier conclusions',reason:'Preserve reviewer independence.'},{artifactIdOrCategory:'proposed corrections',reason:'Prevent expectation bias.'}]:[],expectBack=mode==='APPLICATION_DETERMINISTIC'?[]:[{kind:'STRUCTURED_RESPONSE',filenameOrPattern:'final strict JSON response',required:true},{kind:'EVIDENCE',filenameOrPattern:requiredEvidenceDescription,required:true}];return {testId:recordId(test,'tests'),requirementId:testRequirementId(test),testType:upper(recordValue(test,'TEST_TYPE'))||'UNKNOWN',executionMode:mode,requiredCapability,executorClass:executorMap[mode]||'UNAVAILABLE',executableNow,blockingReason,artifactRequirements,artifactRequired,evidenceIds,artifactIds,requiredArtifactIds:artifactIds,requiredArtifactNames,missingArtifactIds,unverifiedArtifactIds,artifactReady,applicationExecutorSupported,capabilityReady,operatorAction,executionRoute,userAction,operatorActionText:executableNow?(TEST_EXECUTION_ACTIONS[mode]||''):(blockingReason||'Blocked.'),returnRequirements,handoff:{send,withhold,expectBack},executableKind:native?.kind||'NONE',executableSpecVersion:native?.version||'NOT_APPLICABLE'};});`,'testExecutionPlan items');
 const nativeFns=`\nfunction workerExecute(workerUrl,payload,timeoutMs=10000){return new Promise((resolve,reject)=>{if(typeof Worker!=='function')return reject(new Error('Web Worker execution is unavailable.'));const worker=new Worker(workerUrl),requestId='TEST-IR-'+Math.random().toString(36).slice(2),timer=setTimeout(()=>{worker.terminate();reject(new Error('Application-native test exceeded the execution timeout.'));},timeoutMs);worker.onmessage=event=>{if(event.data?.requestId!==requestId)return;clearTimeout(timer);worker.terminate();event.data.ok?resolve(event.data.result):reject(new Error(event.data.error||'Test worker failed.'));};worker.onerror=event=>{clearTimeout(timer);worker.terminate();reject(new Error(event.message||'Test worker failed.'));};worker.postMessage({type:'EXECUTE_TEST_IR',requestId,payload});});}\nasync function executeApplicationTestBatch(project,{testIds=[],artifacts=[],workerUrl='test-worker.js',timeoutMs=10000}={}){ensureShape(project);const plan=testExecutionPlan(project),wanted=new Set(safe(testIds).map(String)),items=plan.items.filter(item=>item.executionMode==='APPLICATION_DETERMINISTIC'&&(!wanted.size||wanted.has(item.testId)));if(!items.length)throw new Error('No current application-native tests were selected.');for(const item of items)if(!item.executableNow)throw new Error(item.testId+': '+(item.blockingReason||'not executable'));const byArtifact=new Map(safe(artifacts).map(a=>[String(a.artifactId),a])),executions=[];for(const item of items){const test=recordsForCurrentScope(project,'tests').find(t=>recordId(t,'tests')===item.testId),native=executableTestDefinition(test),resolvedBindings={};for(const [name,rule] of Object.entries(native.bindings||{})){let id=typeof rule==='string'?rule:String(rule?.artifactId||'');if(!id){const role=String(rule?.artifactRole||''),filename=String(rule?.filename||''),matches=item.requiredArtifactIds.filter(x=>{const a=recordsForCurrentScope(project,'artifacts').find(y=>recordId(y,'artifacts')===x);return (!role||upper(recordValue(a,'ROLE'))===upper(role))&&(!filename||String(recordValue(a,'FILENAME'))===filename);});if(matches.length!==1)throw new Error(item.testId+': executable binding '+name+' is ambiguous or unavailable.');id=matches[0];}resolvedBindings[name]={artifactId:id};}const payloadArtifacts=item.requiredArtifactIds.map(id=>{const a=byArtifact.get(id);if(!a)throw new Error(item.testId+': exact bytes for '+id+' were not supplied to native execution.');return a;});const result=await workerExecute(workerUrl,{testId:item.testId,kind:native.kind,spec:native.spec,bindings:resolvedBindings,artifacts:payloadArtifacts},timeoutMs);executions.push({testId:item.testId,testRecordSha256:test.recordSha256||test.sha256||hash.recordSha256(test),result});}return {schema:'closed-loop-native-test-batch/1',projectRevision:Number(project.revision||0),scope:currentScope(project),scopeSha256:hash.sha256Value(currentScope(project)),createdAt:now(),executions};}\nfunction commitApplicationTestBatch(project,batch){ensureShape(project);if(batch?.schema!=='closed-loop-native-test-batch/1')throw new Error('Native test batch schema is invalid.');if(Number(batch.projectRevision)!==Number(project.revision||0))throw Object.assign(new Error('Native test batch is stale after project revision change.'),{code:'STALE_NATIVE_TEST_BATCH'});if(batch.scopeSha256!==hash.sha256Value(currentScope(project)))throw Object.assign(new Error('Native test batch scope is stale.'),{code:'STALE_NATIVE_TEST_BATCH'});const created=[];for(const execution of safe(batch.executions)){const test=recordsForCurrentScope(project,'tests').find(t=>recordId(t,'tests')===String(execution.testId));if(!test)throw new Error('Native test target no longer exists in current scope.');if(String(test.recordSha256||test.sha256||hash.recordSha256(test))!==String(execution.testRecordSha256))throw Object.assign(new Error('Native test definition changed after execution.'),{code:'STALE_NATIVE_TEST_BATCH'});const native=executableTestDefinition(test);if(!native.supported)throw new Error('Native test definition is no longer supported.');const result=execution.result||{},inputIdentities=safe(result.inputIdentities);for(const identity of inputIdentities){const artifact=recordsForCurrentScope(project,'artifacts').find(a=>recordId(a,'artifacts')===String(identity.artifactId));if(!artifact||upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||Number(recordValue(artifact,'BYTE_SIZE'))!==Number(identity.byteSize)||String(recordValue(artifact,'SHA256'))!==String(identity.sha256))throw Object.assign(new Error('Native test input bytes are stale or no longer verified.'),{code:'STALE_NATIVE_TEST_BATCH'});}const product=recordsForCurrentScope(project,'products').at(-1),productId=recordId(product,'products')||'UNKNOWN',executionIdentity=hash.sha256Value({testId:execution.testId,testRecordSha256:execution.testRecordSha256,inputIdentities,testSpecSha256:result.testSpecSha256,productId,status:result.status});const existing=recordsForCurrentScope(project,'deterministicResults').find(r=>r.nativeExecutionIdentity===executionIdentity);if(existing){created.push(existing);continue;}for(const prior of recordsForCurrentScope(project,'deterministicResults').filter(r=>truth(recordValue(r,'NATIVE_EXECUTION'))&&String(recordValue(r,'TEST_ID'))===String(execution.testId)&&String(recordValue(r,'PRODUCT_ID'))===String(productId))){prior.active=false;prior.validity='SUPERSEDED';prior.invalidatedBy='NATIVE-REEXECUTION-'+executionIdentity;refreshRecordHashes(prior,'deterministicResults');}const evidence=commandRecord(project,'evidenceRecords',{SOURCE_ID:null,ATTACHMENT_ID:null,SHA256:hash.sha256Value(result.evidence||{}),STATUS:'PRESERVED',APPLICATION_EVIDENCE_KIND:'DETERMINISTIC_TEST_RUNTIME',APPLICATION_DESCRIPTION:'Application-executed deterministic Test IR evidence.',APPLICATION_LOCATION:'BROWSER_WEB_WORKER',APPLICATION_CONTENT:{testId:execution.testId,runtimeVersion:result.executorVersion,testSpecSha256:result.testSpecSha256,inputIdentities,observations:safe(result.observations),evidence:result.evidence||null}},{stage:22,source:'APPLICATION_TEST_RUNTIME'});const determination=['SATISFIED','VIOLATED'].includes(upper(result.status))?upper(result.status):'UNDETERMINED',record=commandRecord(project,'deterministicResults',{PRODUCT_ID:productId,PRODUCT_SHA256:String(inputIdentities[0]?.sha256||'UNKNOWN'),TEST_ID:String(execution.testId),DEFECT_ID:null,NATIVE_EXECUTION:true,NATIVE_RUNTIME_VERSION:String(result.executorVersion||'UNKNOWN'),NATIVE_TEST_SPEC_SHA256:String(result.testSpecSha256||'UNKNOWN'),NATIVE_INPUT_IDENTITIES:clone(inputIdentities),NATIVE_EXPECTED_RESULT:JSON.stringify(result.expected??null),NATIVE_ACTUAL_RESULT:JSON.stringify(result.actual??null),NATIVE_OBSERVATIONS:safe(result.observations).map(String),NATIVE_DETERMINATION:determination,EVIDENCE_ID:evidence.id},{stage:22,source:'APPLICATION_TEST_RUNTIME'});record.nativeExecutionIdentity=executionIdentity;record.evidenceRefs=[evidence.id];record.relationships={PRODUCT_ID:productId,TEST_ID:String(execution.testId),EVIDENCE_ID:evidence.id};refreshRecordHashes(record,'deterministicResults');created.push(record);addHistory(project,'APPLICATION_TEST_IR_EXECUTED',{testId:execution.testId,resultId:record.id,evidenceId:evidence.id,determination,runtimeVersion:result.executorVersion,testSpecSha256:result.testSpecSha256});}recalculate(project);return created;}\n`;
 s=one(s,"function operationalMetrics(project){",nativeFns+"\nfunction operationalMetrics(project){",'native execution functions');
 s=one(s,"else if(collection==='deterministicResults'){if(!evidence.sufficient)", "else if(collection==='deterministicResults'){if(truth(recordValue(record,'NATIVE_EXECUTION'))){const native=upper(recordValue(record,'NATIVE_DETERMINATION'));if(record.source!=='APPLICATION_TEST_RUNTIME')reasons.push('Native deterministic result lacks application runtime authority.');if(!String(recordValue(record,'NATIVE_RUNTIME_VERSION')||'').startsWith('closed-loop-test-runtime/'))reasons.push('Native runtime identity is missing.');const epistemic=evaluateEvidenceSufficiency(project,{requirement:records(project,'requirements').find(r=>requirementId(r)===resultRequirementId(project,record))||null,test,result:record});if(!epistemic.sufficient)reasons.push(...epistemic.reasons);determination=['SATISFIED','VIOLATED'].includes(native)?native:'UNDETERMINED';if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}else{if(!evidence.sufficient)", 'native deterministic adjudication start');
 s=one(s,"if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}\n  else if(collection==='meaningResults')", "if(reasons.length&&determination==='SATISFIED')determination='UNDETERMINED';}}\n  else if(collection==='meaningResults')",'native deterministic adjudication close');
 s=one(s,"  if(mode==='UNAVAILABLE'){requiredEvidenceClasses.push('CAPABLE_EXECUTION');reasons.push('UNAVAILABLE execution cannot establish satisfaction.');}",`  if(truth(recordValue(result,'NATIVE_EXECUTION'))){requiredEvidenceClasses.push('APPLICATION_RUNTIME_EVIDENCE');const evidenceId=String(recordValue(result,'EVIDENCE_ID')||''),nativeEvidence=records(project,'evidenceRecords').find(e=>recordId(e,'evidenceRecords')===evidenceId&&e.source==='APPLICATION_TEST_RUNTIME'),inputs=safe(recordValue(result,'NATIVE_INPUT_IDENTITIES'));if(!nativeEvidence)reasons.push('Application-native result lacks canonical application runtime evidence.');if(!inputs.length)reasons.push('Application-native result lacks exact input identities.');for(const identity of inputs){const artifact=recordsForCurrentScope(project,'artifacts').find(a=>recordId(a,'artifacts')===String(identity.artifactId));if(!artifact||upper(recordValue(artifact,'AVAILABILITY'))!=='BYTES_PERSISTED_AND_VERIFIED'||Number(recordValue(artifact,'BYTE_SIZE'))!==Number(identity.byteSize)||String(recordValue(artifact,'SHA256'))!==String(identity.sha256))reasons.push('Application-native input identity is not current application-verified bytes: '+String(identity.artifactId||'UNKNOWN'));}}\n  if(mode==='UNAVAILABLE'){requiredEvidenceClasses.push('CAPABLE_EXECUTION');reasons.push('UNAVAILABLE execution cannot establish satisfaction.');}`,'native evidence sufficiency');
 s=one(s,"if(relevant.length&&relevant.every(i=>i.operatorAction==='NO_ACTION'))return 'No external action required. The application can perform the current deterministic verification route.';", "if(relevant.length&&relevant.every(i=>i.operatorAction==='NO_ACTION'))return stage===22?'Run verification in this application. Every currently required mechanical test is executable here against the exact verified product bytes.':'No external action required. The application can perform the current deterministic verification route.';",'next action native');
 s=one(s,"releaseMetrics,applicationTestCapabilities,capabilityAffirmativelyAvailable,testExecutionPlan,executionHandoff", "releaseMetrics,applicationTestCapabilities,capabilityAffirmativelyAvailable,testExecutionPlan,executeApplicationTestBatch,commitApplicationTestBatch,executionHandoff",'engine exports native execution');
 return s;
});

patch('response-ingestion.js',s=>{
 const needle=`        for(const required of definition.required){\n          const def=definition.fieldDefinitions[required];\n          if(def?.producer!==schema.PRODUCER.AGENT)continue;`;
 s=one(s,needle,`        if(collection==='tests'){const executable=schema.validateExecutableTestDefinition(record.fields||{});for(const reason of executable.reasons)issues.push(issue('INVALID_EXECUTABLE_TEST_SPEC',path+'/fields',reason));}\n${needle}`,'ingestion Test IR semantic validation');
 return s;
});

patch('prompt-engine.js',s=>{
 s=one(s,"const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine/21';","const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine/22';",'prompt version');
 s=one(s,"For every test define TEST_TYPE, EXECUTION_MODE, REQUIRED_CAPABILITY, ARTIFACT_REQUIREMENTS, inputs, tools, procedure, expected result, failure condition, and evidence to preserve.","For every test define TEST_TYPE, EXECUTION_MODE, REQUIRED_CAPABILITY, ARTIFACT_REQUIREMENTS, EXECUTABLE_KIND, EXECUTABLE_SPEC_VERSION, EXECUTABLE_SPEC, EXECUTABLE_INPUT_BINDINGS, inputs, tools, procedure, expected result, failure condition, and evidence to preserve. First determine whether the proposition can be expressed faithfully in the application-owned Closed Loop Test IR shown below. If it can, use APPLICATION_DETERMINISTIC, REQUIRED_CAPABILITY = CLOSED_LOOP_TEST_IR, and compile the requirement into that declarative IR. If it cannot, set EXECUTABLE_KIND = NONE, EXECUTABLE_SPEC_VERSION = NOT_APPLICABLE, EXECUTABLE_SPEC = {}, EXECUTABLE_INPUT_BINDINGS = {}, then route to the real independent/external/human executor. Never invent an IR operation and never use arbitrary JavaScript, Python, shell commands, eval, or executable code as Test IR.",'Stage6 Test IR authoring');
 s=one(s,"${workflow.applicationTestCapabilities().length?workflow.applicationTestCapabilities().join('\\\n'):'NONE'}\n\n`:''}",`${workflow.applicationTestCapabilities().length?workflow.applicationTestCapabilities().join('\\\n'):'NONE'}\n\n${stage===6?`CLOSED LOOP TEST IR\nVERSION: ${schema.TEST_IR_VERSION}\nSUPPORTED GENERIC OPERATIONS: ${schema.TEST_IR_OPERATIONS.join(', ')}\nSUPPORTED EXECUTABLE KINDS: ${schema.TEST_IR_IMPLEMENTED_KINDS.join(', ')}\nSECURITY BOUNDARY: ARBITRARY_JAVASCRIPT, ARBITRARY_PYTHON, SHELL_COMMAND, eval, Function constructors, and invented operations are prohibited. CUSTOM_PIPELINE composes only the registered generic operations. The runtime is subject/domain blind: bytes, files, text, structured data, selectors, calculations, comparisons, and assertions only.\n\n`:''}`: ''}`, 'prompt Test IR capability block');
 return s;
});

patch('project-store.js',s=>{
 const insert=`\nfunction crc32(bytes){let c=0xffffffff;for(const b of bytes){c^=b;for(let k=0;k<8;k++)c=(c>>>1)^((c&1)?0xedb88320:0);}return (c^0xffffffff)>>>0;}\nfunction u16(n){return [n&255,n>>>8&255];}function u32(n){return [n&255,n>>>8&255,n>>>16&255,n>>>24&255];}\nfunction zipStore(entries){const enc=new TextEncoder(),local=[],central=[];let offset=0;for(const entry of entries){const name=enc.encode(entry.name),data=entry.bytes instanceof Uint8Array?entry.bytes:new Uint8Array(entry.bytes),crc=crc32(data),header=new Uint8Array([...u32(0x04034b50),...u16(20),...u16(0),...u16(0),...u16(0),...u16(0),...u32(crc),...u32(data.length),...u32(data.length),...u16(name.length),...u16(0)]);local.push(header,name,data);const ch=new Uint8Array([...u32(0x02014b50),...u16(20),...u16(20),...u16(0),...u16(0),...u16(0),...u16(0),...u32(crc),...u32(data.length),...u32(data.length),...u16(name.length),...u16(0),...u16(0),...u16(0),...u16(0),...u32(0),...u32(offset)]);central.push(ch,name);offset+=header.length+name.length+data.length;}const centralSize=central.reduce((n,x)=>n+x.length,0),end=new Uint8Array([...u32(0x06054b50),...u16(0),...u16(0),...u16(entries.length),...u16(entries.length),...u32(centralSize),...u32(offset),...u16(0)]);return new Blob([...local,...central,end],{type:'application/zip'});}\nasync function createExecutionPackage({jobId,stage,testIds=[],productId=null,operation='COMPLETE'}={}){await ready;const projects=await readAll(),project=projects.find(p=>projectIdentity(p)===String(jobId));if(!project)throw new Error('Project is unavailable for verification package creation.');const engine=globalThis.closedLoopWorkflowEngine,hashRuntime=globalThis.closedLoopHash;if(!engine||!hashRuntime)throw new Error('Workflow/hash runtime is unavailable.');engine.ensureShape(project);const ids=new Set(testIds.map(String)),tests=engine.recordsForCurrentScope(project,'tests').filter(t=>!ids.size||ids.has(engine.recordId(t,'tests'))),handoff=engine.executionHandoff(project,{stage:Number(stage),operation,testIds:tests.map(t=>engine.recordId(t,'tests'))}),artifacts=[];for(const item of handoff.send){const canonical=engine.recordsForCurrentScope(project,'artifacts').find(a=>engine.recordId(a,'artifacts')===item.artifactId),stored=await getArtifact(item.artifactId);if(!canonical||!stored)throw new Error('Required verification artifact bytes are unavailable: '+item.artifactId);const bytes=new Uint8Array(await stored.blob.arrayBuffer()),sha=await hashRuntime.sha256Bytes(bytes);if(sha!==String(engine.recordValue(canonical,'SHA256'))||bytes.byteLength!==Number(engine.recordValue(canonical,'BYTE_SIZE')))throw new Error('Verification package blocked because stored bytes no longer match '+item.artifactId);artifacts.push({artifactId:item.artifactId,filename:String(engine.recordValue(canonical,'FILENAME')),sha256:sha,byteSize:bytes.byteLength,role:String(engine.recordValue(canonical,'ROLE')||''),bytes});}const prompt=project.projectData.generatedPrompts.filter(p=>Number(p.stage)===Number(stage)&&!p.invalidatedBy).at(-1),manifest={schema:'closed-loop-verification-package/1',jobId:String(jobId),stage:Number(stage),operation,productId:productId||project.job.CURRENT_PRODUCT_ID||null,testIds:tests.map(t=>engine.recordId(t,'tests')),createdAt:new Date().toISOString(),artifacts:artifacts.map(({artifactId,filename,sha256,byteSize,role})=>({artifactId,filename,sha256,byteSize,role})),withhold:handoff.withhold,expectBack:handoff.expectBack},entries=[{name:'manifest.json',bytes:new TextEncoder().encode(JSON.stringify(manifest,null,2)+'\\n')},{name:'test-spec.json',bytes:new TextEncoder().encode(JSON.stringify(tests.map(t=>({testId:engine.recordId(t,'tests'),fields:t.fields||t,relationships:t.relationships||{}})),null,2)+'\\n')},{name:'instruction.txt',bytes:new TextEncoder().encode(String(prompt?.prompt||'Use the current application-generated verification instruction.'))},...artifacts.map(a=>({name:'artifacts/'+a.filename.replace(/^\\/+/,''),bytes:a.bytes}))];const blob=zipStore(entries),packageSha256=await hashRuntime.sha256Bytes(await blob.arrayBuffer()),filename='VERIFY-'+String(jobId).replace(/[^A-Za-z0-9_-]/g,'_')+'-S'+String(stage).padStart(2,'0')+'.zip';return {filename,blob,manifest,packageSha256};}\n`;
 s=one(s,"async function storageHealth(){",insert+"\nasync function storageHealth(){",'execution package');
 s=one(s,"putArtifact,getArtifact,deleteArtifact,listArtifacts,verifyProjectArtifacts,", "putArtifact,getArtifact,deleteArtifact,listArtifacts,verifyProjectArtifacts,createExecutionPackage,",'project store export');
 return s;
});

patch('app-core.js',s=>{
 s=one(s,"const actionRows=plan.items.map(item=>({test:item.testId", "const nativeItems=n===22?plan.items.filter(item=>item.executionMode==='APPLICATION_DETERMINISTIC'):[],nativePending=nativeItems.filter(item=>!engine.recordsForCurrentScope(current,'deterministicResults').some(r=>String(engine.recordValue(r,'TEST_ID'))===item.testId&&engine.effectiveDetermination('deterministicResults',r,engine.recordsForCurrentScope(current,'tests').find(t=>engine.recordId(t,'tests')===item.testId),current)==='SATISFIED')),externalItems=plan.items.filter(item=>item.executionMode!=='APPLICATION_DETERMINISTIC'&&item.executionMode!=='UNAVAILABLE');\n  const actionRows=plan.items.map(item=>({test:item.testId",'native UI vars');
 s=one(s,"const downloadButtons=handoff.send.map(x=>`<button type=\"button\" data-download-artifact=\"${esc(x.artifactId)}\">Download ${esc(x.filename)}</button>`).join('');",`const downloadButtons=handoff.send.map(x=>\`<button type="button" data-download-artifact="\${esc(x.artifactId)}">Download \${esc(x.filename)}</button>\`).join(''),nativeControls=n===22&&nativeItems.length?\`<div class="notice"><strong>Finished product verification</strong><br>\${nativeItems.length} mechanical test\${nativeItems.length===1?'':'s'} can run inside this application against exact verified bytes. \${nativePending.length} still need execution.</div><div class="button-row">\${nativePending.length?\`<button class="primary" id="run-native-tests">Run \${nativePending.length} mechanical test\${nativePending.length===1?'':'s'}</button>\`:'<span class="status success">Automatic verification complete</span>'}</div>\`:'' ,packageControl=externalItems.length?\`<div class="button-row"><button type="button" id="download-verification-package">Download verification package</button></div>\`:'';`,'native UI controls');
 s=one(s,"${downloadButtons?`<div class=\"button-row\">${downloadButtons}</div>`:''}<details", "${nativeControls}${packageControl}${downloadButtons?`<div class=\"button-row\">${downloadButtons}</div>`:''}<details",'render native/package controls');
 const helpers=`\nfunction runtimeWorkerUrl(){const script=[...document.scripts].find(node=>String(node.src||'').includes('app-core.js')),token=script?new URL(script.src).searchParams.get('v'):'';return 'test-worker.js'+(token?'?v='+encodeURIComponent(token):'');}\nasync function exactArtifactPayloads(ids){const out=[];for(const id of [...new Set(ids.map(String))]){const canonical=engine.recordsForCurrentScope(current,'artifacts').find(a=>engine.recordId(a,'artifacts')===id),stored=await projectStore.getArtifact(id);if(!canonical||!stored)throw new Error('Required exact artifact bytes are unavailable: '+id);const bytes=await stored.blob.arrayBuffer(),sha=await globalThis.closedLoopHash.sha256Bytes(bytes);if(sha!==String(engine.recordValue(canonical,'SHA256'))||bytes.byteLength!==Number(engine.recordValue(canonical,'BYTE_SIZE')))throw new Error('Stored bytes no longer match canonical identity for '+id);out.push({artifactId:id,filename:String(engine.recordValue(canonical,'FILENAME')),mediaType:String(engine.recordValue(canonical,'TYPE')||stored.mediaType||'application/octet-stream'),sha256:sha,bytes});}return out;}\nasync function runNativeTests(){const plan=engine.testExecutionPlan(current),items=plan.items.filter(item=>item.executionMode==='APPLICATION_DETERMINISTIC'&&item.executableNow),ids=items.map(x=>x.testId);if(!ids.length)throw new Error('No application-native tests are ready to run.');const payloads=await exactArtifactPayloads(items.flatMap(x=>x.requiredArtifactIds)),batch=await engine.executeApplicationTestBatch(current,{testIds:ids,artifacts:payloads,workerUrl:runtimeWorkerUrl()});const expected=Number(current.revision||0),result=await projectStore.transact(current.job.JOB_ID,expected,next=>engine.commitApplicationTestBatch(next,batch));projects=projects.map(p=>p.job?.JOB_ID===result.project.job.JOB_ID?result.project:p);current=result.project;announce('automatic verification complete');render();requestAnimationFrame(()=>{const node=$('#execution-guidance');node?.focus();});}\nasync function downloadVerificationPackage(){const plan=engine.testExecutionPlan(current),testIds=plan.items.filter(item=>item.executionMode!=='APPLICATION_DETERMINISTIC'&&item.executionMode!=='UNAVAILABLE').map(item=>item.testId),pkg=await projectStore.createExecutionPackage({jobId:current.job.JOB_ID,stage:current.activeStage,testIds,productId:current.job.CURRENT_PRODUCT_ID,operation:selectedOperation(current.activeStage)}),url=URL.createObjectURL(pkg.blob),a=document.createElement('a');a.href=url;a.download=pkg.filename;a.click();setTimeout(()=>URL.revokeObjectURL(url),0);announce('verification package ready');}\n`;
 s=one(s,"function files(){",helpers+"\nfunction files(){",'native UI helpers');
 s=one(s,"document.querySelectorAll('[data-download-artifact]').forEach(b=>b.onclick=()=>downloadCanonicalArtifact", "if($('#run-native-tests'))$('#run-native-tests').onclick=()=>runNativeTests().catch(error=>{announce('automatic verification blocked');alert(error.message||error);$('#execution-guidance')?.focus();});if($('#download-verification-package'))$('#download-verification-package').onclick=()=>downloadVerificationPackage().catch(error=>{announce('verification package blocked');alert(error.message||error);});document.querySelectorAll('[data-download-artifact]').forEach(b=>b.onclick=()=>downloadCanonicalArtifact",'wire native controls');
 return s;
});

patch('verify-hash.mjs',s=>{
 s=one(s,"const runtimeFiles=['workbook.js','hash.js','workflow-schema.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js'];", "const directRuntimeFiles=['workbook.js','hash.js','workflow-schema.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js'];\nconst runtimeFiles=[...directRuntimeFiles,'test-runtime.js','test-worker.js'];",'hash runtime graph');
 s=one(s,"assert(scriptSources.length===runtimeFiles.length,`Expected ${runtimeFiles.length} direct deferred runtime scripts; found ${scriptSources.length}.`);\nscriptSources.forEach((source,index)=>{", "assert(scriptSources.length===directRuntimeFiles.length,`Expected ${directRuntimeFiles.length} direct deferred runtime scripts; found ${scriptSources.length}.`);\nscriptSources.forEach((source,index)=>{",'hash direct count');
 s=one(s,"assert(file===runtimeFiles[index],`Runtime script order mismatch at ${runtimeFiles[index]}.`);", "assert(file===directRuntimeFiles[index],`Runtime script order mismatch at ${directRuntimeFiles[index]}.`);",'hash direct order');
 return s;
});

patch('verify-definition-of-done.mjs',s=>{
 s=one(s,"assert(engine.applicationTestCapabilities().length===0,'APPLICATION_TEST_EXECUTORS is no longer empty without a proven native executor.');", "assert(engine.applicationTestCapabilities().includes(schema.TEST_IR_CAPABILITY),'Application-native Test IR capability is not registered.');assert(fs.existsSync('test-runtime.js')&&fs.existsSync('test-worker.js'),'Application-native Test IR runtime files are missing.');",'definition native capability');
 s=one(s,"assert(workflowSource.includes('node verify-semantic-invariant.mjs'),'Semantic false-acceptance invariant is not in CI.');", "assert(workflowSource.includes('node verify-semantic-invariant.mjs'),'Semantic false-acceptance invariant is not in CI.');\nassert(workflowSource.includes('verify-test-runtime.mjs'),'Generic Test IR runtime proof is not in CI.');",'definition runtime CI');
 return s;
});

patch('verify-prompt-semantics.mjs',s=>{
 s += `\n{const p=await promptEngine.buildPrompt(fixtureProject(),6,'COMPLETE');const text=p.prompt||p;if(!String(text).includes('closed-loop-test-spec/1'))throw new Error('Stage 06 does not publish the Test IR version.');for(const token of ['LOAD_ARTIFACT','PARSE_JSON','ASSERT_EQ','ARBITRARY_JAVASCRIPT','SHELL_COMMAND'])if(!String(text).includes(token))throw new Error('Stage 06 Test IR contract missing '+token);console.log(JSON.stringify({testIrPromptContract:true}));}\n`;
 return s;
});

patch('.github/workflows/pages.yml',s=>{
 s=one(s,"          node --check workflow-schema.js\n", "          node --check workflow-schema.js\n          node --check test-runtime.js\n          node --check test-worker.js\n          node --check verify-test-runtime.mjs\n",'CI syntax Test IR');
 s=one(s,"      - name: Verify canonical serialization and SHA-256\n        run: node verify-hash.mjs", "      - name: Verify generic deterministic Test IR runtime\n        run: node verify-test-runtime.mjs\n      - name: Verify canonical serialization and SHA-256\n        run: node verify-hash.mjs",'CI Test IR step');
 s=one(s,"node build-test-project.mjs && node verify-hash.mjs", "node build-test-project.mjs && node verify-test-runtime.mjs && node verify-hash.mjs",'deploy Test IR proof');
 s=one(s,"          node verify-ingestion.mjs > /tmp/verify-ingestion.out\n", "          node verify-test-runtime.mjs > /tmp/verify-test-runtime.out\n          node verify-ingestion.mjs > /tmp/verify-ingestion.out\n",'acceptance runtime output');
 s=one(s,"          const definition=JSON.parse(fs.readFileSync('/tmp/verify-definition-of-done.out','utf8'));", "          const definition=JSON.parse(fs.readFileSync('/tmp/verify-definition-of-done.out','utf8'));\n          const testRuntime=JSON.parse(fs.readFileSync('/tmp/verify-test-runtime.out','utf8').trim());",'acceptance parse runtime');
 s=one(s,"            responseSchema:'closed-loop-stage-response/2',", "            responseSchema:'closed-loop-stage-response/2',\n            testIrVersion:testRuntime.testIrVersion,\n            testRuntimeVersion:testRuntime.runtimeVersion,\n            applicationNativeTestIr:true,",'acceptance runtime fields');
 return s;
});

// Recompute the shared direct-script cache token from the complete runtime graph, including worker-only runtime files.
const graph=['workbook.js','hash.js','workflow-schema.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js','test-runtime.js','test-worker.js'];
const blobSha=file=>{const bytes=fs.readFileSync(file);return createHash('sha1').update(`blob ${bytes.length}\0`).update(bytes).digest('hex');};
const manifest=graph.map(file=>`${file}:${blobSha(file)}\n`).join(''),token=`runtime-${createHash('sha256').update(manifest).digest('hex').slice(0,16)}`;
patch('index.html',s=>s.replace(/(workbook|hash|workflow-schema|workflow-engine|prompt-engine|response-ingestion|project-store|app-core)\.js\?v=runtime-[a-f0-9]{16}/g,(m,name)=>`${name}.js?v=${token}`));
console.log(JSON.stringify({patched:true,runtimeBuildIdentity:token}));
