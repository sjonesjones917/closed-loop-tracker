t arr=j.runs[kind];
  return arr.map(r=>({runId:r.runId,prompt:buildPrompt(j,12,`RUN_ID: ${r.runId}\nRUN OUTPUT TO VERIFY:\n${r.output}`)}));
}
function saveVerification(j,kind,index,text){const r=j.runs[kind][index];r.verification=text;r.verified=!!text.trim()&&!/\bRESULT\s*:\s*(VIOLATED|UNDETERMINED)\b/i.test(text);return r}
function validateVerificationBatch(j,kind){const arr=j.runs[kind];return {ok:arr.length===10&&arr.every(r=>r.verified),count:arr.filter(r=>r.verified).length}}
function markBatchStage(j,n,kind){
  const s=j.stages[n-1],v=validateRunBatch(j,kind);s.result=`${kind.toUpperCase()} RUN BATCH: ${v.count}/10 complete`;s.validated=v.ok;s.status=v.ok?"COMPLETE":"FAILED";if(v.ok&&j.currentStage===n)j.currentStage=n+1;return v
}
function markVerificationStage(j,n,kind){
  const s=j.stages[n-1],v=validateVerificationBatch(j,kind);s.result=`${kind.toUpperCase()} VERIFICATION BATCH: ${v.count}/10 verified`;s.validated=v.ok;s.status=v.ok?"COMPLETE":"FAILED";if(v.ok&&j.currentStage===n)j.currentStage=n+1;return v
}
function exportState(j){return JSON.stringify(j,null,2)}
const API={STAGE_NAMES,STAGE_INSTRUCTIONS,MANDATORY_RULES,ENUMS,newJob,stage1Record,nextId,nextVersion,buildPrompt,exactOutputContract,saveStandardStage,createRunBatch,saveRunOutput,validateRunBatch,createVerificationPrompts,saveVerification,validateVerificationBatch,markBatchStage,markVerificationStage,exportState,hashString};
if(typeof module!=="undefined"&&module.exports)module.exports=API;
global.ClosedLoopCore=API;
})(typeof window!=="undefined"?window:globalThis);

</script><script>
const C=ClosedLoopCore, KEY="closed_loop_engine_v2"; let D=JSON.parse(localStorage.getItem(KEY)||'{"jobs":[]}'),jid=D.jid||null; const A=document.getElementById("app");
function save(){D.jid=jid;localStorage.setItem(KEY,JSON.stringify(D));hdr()}function j(){return D.jobs.find(x=>x.id===jid)}function hdr(){document.getElementById("sub").textContent=j()?j().id+" • Stage "+j().currentStage+"/30":"No project selected"}
function esc(v){return String(v??"").replace(/[&<>"]/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[m]))}async function cp(t){try{await navigator.clipboard.writeText(t)}catch{prompt("Copy:",t)}}
function nj(){let x=C.newJob();D.jobs.unshift(x);jid=x.id;save();intake()}function jobs(){A.innerHTML='<div class="row between"><h2>Projects</h2><button onclick="nj()">New Project</button></div><div class="c notice"><b>Included verification project</b><p class="muted">E2E Self Test is preloaded on a fresh install. Open it to inspect the completed 30-stage workflow and release state. Its generated artifact is committed beside this app as selftest-artifact.txt.</p></div>'+(D.jobs.length?D.jobs.map(x=>`<div class="c"><b>${esc(x.title||"Untitled job")}</b><br><small>${x.id}</small><p class="muted">${esc(x.objective||"No objective entered.")}</p><small>${x.stages?.filter(s=>s.status==="COMPLETE").length||0}/30 complete • Release: ${esc(x.releaseState||"—")}</small><br><br><button onclick="jid='${x.id}';save();flow()">Open</button> <button class="s" onclick="jid='${x.id}';save();intake()">Project input</button></div>`).join(""):'<div class="c notice"><b>Create a project.</b><p class="muted">Stage 1 is performed by the app from the job information. The first agent task is Stage 2.</p></div>');hdr()}
function f(label,key,area=false){return `<label>${label}</label>`+(area?`<textarea oninput="j()['${key}']=this.value;refreshStage1();save()">${esc(j()[key]||"")}</textarea>`:`<input value="${esc(j()[key]||"")}" oninput="j()['${key}']=this.value;refreshStage1();save()">`)}
function refreshStage1(){if(!j())return;j().stages[0].result=C.stage1Record(j())}
function intake(){if(!j())return jobs();A.innerHTML='<div class="row between"><h2>Project Input — Stage 1</h2><button class="s" onclick="finishIntake()">Save Stage 1</button></div><div class="c notice"><b>You enter only the project/job facts here.</b><p class="muted">The app generates JOB_ID and INPUT-v001. It does not send Stage 1 to an agent.</p></div><div class="c">'+f("Job name","title")+f("Exact user objective","objective",true)+f("Exact deliverable requested","deliverable",true)+f("All supplied files, messages, links, data, and constraints","supplied",true)+f("Required output format","format")+f("Deadline or temporal scope","deadline")+f("Known authoritative sources","authorities",true)+f("Available tools","tools",true)+f("Prohibited actions","prohibited",true)+f("Unknown information that may affect correctness","unknowns",true)+f("Explicit user requirements","explicitRequirements",true)+f("Assumptions","assumptions",true)+'</div><div class="c"><b>Generated Stage 1 record</b><pre>'+esc(C.stage1Record(j()))+'</pre></div>'}
function finishIntake(){refreshStage1();j().stages[0].validated=true;j().stages[0].status="COMPLETE";j().currentStage=Math.max(2,j().currentStage);save();flow()}
function flow(){if(!j())return jobs();let x=j(),c=x.stages.filter(s=>s.status==="COMPLETE").length,b=x.stages.filter(s=>s.status==="BLOCKED").length;A.innerHTML=`<div class="row between"><h2>${esc(x.title||"Workflow")}</h2><button class="s" onclick="intake()">Project input</button></div><div class="grid"><div class="metric"><b>${c}/30</b><small>Complete</small></div><div class="metric"><b>${b}</b><small>Blocked</small></div><div class="metric"><b>${x.iteration}</b><small>Iteration</small></div><div class="metric"><b>${x.releaseState||"—"}</b><small>Release</small></div></div><h3>Workflow</h3>`+C.STAGE_NAMES.map((name,i)=>{let s=x.stages[i];return `<div class="stage" onclick="stage(${i+1})"><div class="num">${String(i+1).padStart(2,"0")}</div><div><b>${esc(name)}</b><div class="muted" style="font-size:11px">${i===0?"Completed from job input":i+1===x.currentStage?"Next required work":"Workflow stage"}</div></div><span class="pill ${s.status}">${s.status.replaceAll("_"," ")}</span></div>`}).join("")}
function stage(n){if(n===1)return intake(); if([11,12,17,19].includes(n))return batchStage(n); let x=j(),