ing, limiting, or defining the requested result. Extract every statement changing what the final product must contain, omit, preserve, calculate, demonstrate, or accomplish. Separate mandatory requirements from recommendations/examples/explanations/optional practices. Identify user-created, format, medium, dependency, applicability, edge-condition, exception, and restrictive requirements. Record exact evidence. Perform a second targeted pass for omissions, exceptions, conflicts, and restrictions. Continue until a full additional pass produces no new material requirement category and every known controlling source has been examined.\n\nSTAGE 4 — COMPILE ATOMIC REQUIREMENTS\nCreate one independently testable requirement per record with: REQ_ID, REQUIREMENT, TYPE, SOURCE, SOURCE_LOCATION, AUTHORITY, APPLICABILITY, DEPENDENCIES, PROHIBITIONS, VERIFICATION_METHOD, EXPECTED_EVIDENCE, FAILURE_CONDITION, STATUS. Split compound requirements. Replace qualitative words with observable conditions.\n\nSTAGE 5 — RESOLVE THE REQUIREMENT SET\nCheck duplicates, conflicts, mutually unsatisfiable requirements, undefined terms, circular dependencies, missing prerequisites, unsupported requirements, indeterminate applicability, and requirements without a verification method. Resolve every resolvable defect. Mandatory unresolved authority/evidence problems are BLOCKED.\n\nSTAGE 6 — BUILD THE VERIFICATION SUITE\nCreate at least one verification procedure for every mandatory requirement using the strongest available method: formal proof/exact computation; deterministic programmatic test; schema/structural validation; exact source comparison; rule-based evaluation; independent semantic evaluation; authorized human judgment only when required. Separate tests by requirement. Calculate mandatory_test_coverage and do not authorize continuation below 1.00 unless blocked.\n\nSTAGE 7 — BUILD FAILURE/MUTATION TESTS\nFor every requirement construct at least one invalid case where applicable, define the required detect/reject/block response, execute the validator against the invalid case where possible, and repair any validator that accepts an invalid case. Preserve the failure fixture.\n\nSTAGE 8 — AUTHOR THE PRODUCTION INSTRUCTION\nWrite the complete production instruction FROM THE COMPILED REQUIREMENTS. It must explicitly contain OBJECTIVE, INPUTS, SOURCE AUTHORITY, SCOPE, DEFINED TERMS, REQUIRED PROCEDURE, DECISION RULES, TOOL RULES, OUTPUT CONTRACT, FAILURE HANDLING, COMPLETION CRITERIA. Describe actual operations and order; do not use vague placeholders such as “research all requirements” or “verify the result.” Require TRUE/FALSE/UNKNOWN handling.\n\nSTAGE 9 — PREFLIGHT THE PRODUCTION INSTRUCTION\nUse an evaluation process separate from execution of the target work. Examine every sentence for material ambiguity, undefined objects, missing information, conflicts, unavailable capabilities, objective verifiability, responsible operation, ordering, failure behavior, and requirement/control traceability. Correct every material defect. Repeat until no known material instruction defect remains. Version the corrected instruction.\n\nMANDATORY OPERATING RULES\n${RULES}\n\nREQUIRED RESPONSE FORMAT\nReturn exactly these top-level sections, in this order:\n=== STAGE 2 RESULT ===\n[completed source inventory; use SOURCE_ID: AUTO for each source]\n=== STAGE 3 RESULT ===\n[completed requirement research]\n=== STAGE 4 RESULT ===\n[atomic requirement records; use REQ_ID: AUTO]\n=== STAGE 5 RESULT ===\n[resolved requirement-set analysis and blockers]\n=== STAGE 6 RESULT ===\n[verification tests; use TEST_ID: AUTO; include mandatory_test_coverage]\n=== STAGE 7 RESULT ===\n[failure/mutation tests and actual validator results]\n=== STAGE 8 RESULT ===\n[complete production instruction]\n=== STAGE 9 RESULT ===\n[preflight findings + corrected complete instruction + instruction version]\n\nDo not generate the final requested deliverable. Production is forbidden until the workflow reaches Stage 21.`}
function splitBundle(t){let out={};for(let n=2;n<=9;n++){let re=new RegExp(`===\\s*STAGE\\s+${n}\\s+RESULT\\s*===([\\s\\S]*?)(?====\\s*STAGE\\s+${n+1}\\s+RESULT\\s*===|$)`,'i'),m=t.match(re);if(m)out[n]=m[1].trim()}return out}
function assignIds(x,n,t){let spec=n===2?['SOURCE_ID','SRC']:n===4?['REQ_ID','REQ']:n===6?['TEST_ID','TEST']:null;if(!spec)return t;let [field,pre]=spec;let re=new RegExp(`(${field}[ \\t]*:[ \\t]*)(AUTO)?(?=\\r?$)`,'gim');return t.replace(re,(all,prefix)=>prefix+seq(x,pre))}
function validatePrep(x,parts){let errs=[];for(let n=2;n<=9;n++)if(!parts[n])errs.push(`Missing Stage ${n} result.`);if(parts[2]&&!/TITLE|SOURCE/i.test(parts[2]))errs.push('Stage 2 does not contain a source inventory.');if(parts[3]&&!/SOURCE|REQUIREMENT|EVIDENCE/i.test(parts[3]))errs.push('Stage 3 does not contain requirement research evidence.');if(parts[4]&&!/REQUIREMENT\s*:/i.test(parts[4]))errs.push('Stage 4 has no atomic REQUIREMENT records.');if(parts[5]&&!/CONFLICT|DUPLICATE|RESOLV|BLOCK/i.test(parts[5]))errs.push('Stage 5 does not show requirement-set resolution.');let cov=parts[6]?.match(/mandatory[_ ]test[_ ]coverage\s*[:=]\s*(1(?:\.0+)?|100%)/i);if(!cov)errs.push('Stage 6 does not establish mandatory_test_coverage = 1.00/100%.');if(parts[7]&&!/INVALID|FAILURE|MUTATION|ADVERSARIAL/i.test(parts[7]))errs.push('Stage 7 does not contain failure/mutation tests.');for(let h of ['OBJECTIVE','INPUTS','SOURCE AUTHORITY','SCOPE','DEFINED TERMS','REQUIRED PROCEDURE','DECISION RULES','TOOL RULES','OUTPUT CONTRACT','FAILURE HANDLING','COMPLETION_CRITERIA'])if(p