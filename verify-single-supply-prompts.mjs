import fs from 'node:fs';
const p=fs.readFileSync('prompt-engine.js','utf8'),h=fs.readFileSync('index.html','utf8');const fail=m=>{throw new Error(m)};for(const x of ['PROJECT MEMORY / SINGLE-SUPPLY INVARIANT','Never ask the human to repeat, retype, restate, summarize, reconstruct, reattach, or resend it.','DO NOT ask the human to attach, reattach, resend, retype, restate, summarize, reconstruct','inspect them deeply enough to capture every human-supplied fact'])if(!p.includes(x))fail('missing prompt contract: '+x);if(/read only the minimum portions needed/.test(p))fail('Stage 01 still under-captures supplied material');if(/PATENT \/ REGULATED FILING/.test(p))fail('subject-specific prompt branch remains');if(!h.includes('.prompt{height:clamp(260px,45vh,520px);max-height:80vh;resize:vertical;'))fail('prompt box visual changed');if(!h.includes('.expandable-prompt{max-height:280px}.expandable-prompt.expanded{max-height:none}'))fail('prompt expansion visual changed');console.log('single-supply prompt contract: PASS');
