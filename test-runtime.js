(()=>{
'use strict';
const VERSION='closed-loop-test-runtime/1';
const SPEC_VERSION='closed-loop-test-spec/1';
const CAPABILITY='CLOSED_LOOP_TEST_IR';
const OPS=Object.freeze(['LOAD_ARTIFACT','READ_BYTES','DECODE_UTF8','PARSE_JSON','PARSE_CSV','PARSE_XML','SELECT_JSON_PATH','SELECT_XML','COUNT','SUM','MIN','MAX','SORT','UNIQUE','HASH_SHA256','REGEX','COMPARE','ASSERT_EQ','ASSERT_GT','ASSERT_GTE','ASSERT_LT','ASSERT_LTE','ASSERT_MATCH','ASSERT_CONTAINS','ASSERT_NOT_CONTAINS','ASSERT_SET_EQUAL','BYTE_COMPARE']);
const LIMITS=Object.freeze({maxInputBytes:16*1024*1024,maxDecompressedBytes:32*1024*1024,maxSteps:64,maxSelectorDepth:32,maxParsedDepth:64,maxCollectionItems:100000,maxRegexLength:2000,maxRegexInputBytes:4*1024*1024,maxWorkerMs:5000,maxArchiveExpansionBytes:32*1024*1024,maxCsvCells:250000});
const BUILD_QUERY=typeof document!=='undefined'&&document.currentScript?new URL(document.currentScript.src,document.baseURI).search:'';
const WORKER_URL='test-worker.js'+BUILD_QUERY;
const field=(test,key)=>test?.fields?.[key]??test?.[key];
const bytesOf=value=>value instanceof Uint8Array?value:value instanceof ArrayBuffer?new Uint8Array(value):ArrayBuffer.isView(value)?new Uint8Array(value.buffer,value.byteOffset,value.byteLength):null;
const stable=value=>JSON.stringify(value,(_,v)=>v&&typeof v==='object'&&!Array.isArray(v)?Object.fromEntries(Object.entries(v).sort(([a],[b])=>a.localeCompare(b))):v);
const allowedFlags=/^[gimsuy]*$/;
const isObject=value=>Boolean(value)&&typeof value==='object'&&!Array.isArray(value);
const CONTRACTS=Object.freeze({
  LOAD_ARTIFACT:{required:['binding'],allowed:['op','binding']},READ_BYTES:{required:[],allowed:['op']},DECODE_UTF8:{required:[],allowed:['op']},PARSE_JSON:{required:[],allowed:['op']},
  PARSE_CSV:{required:['delimiter','header','quoting','newline','encoding'],allowed:['op','delimiter','header','quoting','newline','encoding']},PARSE_XML:{required:[],allowed:['op']},
  SELECT_JSON_PATH:{required:['path'],allowed:['op','path']},SELECT_XML:{required:['path'],allowed:['op','path']},COUNT:{required:[],allowed:['op']},SUM:{required:[],allowed:['op']},MIN:{required:[],allowed:['op']},MAX:{required:[],allowed:['op']},SORT:{required:[],allowed:['op']},UNIQUE:{required:[],allowed:['op']},HASH_SHA256:{required:[],allowed:['op']},
  REGEX:{required:['pattern'],allowed:['op','pattern','flags']},COMPARE:{required:[],allowed:['op','value','binding','absoluteTolerance','relativeTolerance']},
  ASSERT_EQ:{required:['value'],allowed:['op','value','absoluteTolerance','relativeTolerance','message']},ASSERT_GT:{required:['value'],allowed:['op','value','message']},ASSERT_GTE:{required:['value'],allowed:['op','value','message']},ASSERT_LT:{required:['value'],allowed:['op','value','message']},ASSERT_LTE:{required:['value'],allowed:['op','value','message']},ASSERT_MATCH:{required:['pattern'],allowed:['op','pattern','flags','message']},ASSERT_CONTAINS:{required:['value'],allowed:['op','value','message']},ASSERT_NOT_CONTAINS:{required:['value'],allowed:['op','value','message']},ASSERT_SET_EQUAL:{required:['value'],allowed:['op','value','message']},BYTE_COMPARE:{required:['binding'],allowed:['op','binding']}
});
function depthOf(value,depth=0){if(depth>LIMITS.maxParsedDepth)return depth;if(!value||typeof value!=='object')return depth;const children=Array.isArray(value)?value:Object.values(value);let max=depth;for(const child of children){const d=depthOf(child,depth+1);if(d>max)max=d;if(max>LIMITS.maxParsedDepth)break;}return max;}
function enforceParsedLimits(value,label='parsed value'){if(depthOf(value)>LIMITS.maxParsedDepth)throw new Error(`${label} exceeds parsed-depth limit.`);if(Array.isArray(value)&&value.length>LIMITS.maxCollectionItems)throw new Error(`${label} exceeds collection-size limit.`);return value;}
async function sha256(bytes){const data=bytesOf(bytes);if(!data)throw new Error('HASH_SHA256 requires bytes.');const digest=await crypto.subtle.digest('SHA-256',data);return [...new Uint8Array(digest)].map(x=>x.toString(16).padStart(2,'0')).join('');}
function safeRegex(pattern,flags=''){const p=String(pattern),f=String(flags||'');if(p.length>LIMITS.maxRegexLength)throw new Error('Regex exceeds deterministic runtime pattern limit.');if(!allowedFlags.test(f)||new Set(f).size!==f.length)throw new Error('Regex flags are unsupported or duplicated.');if(/\(\?<=[^)]|\(\?<!/.test(p))throw new Error('Regex lookbehind is not supported.');if(/\\[1-9]/.test(p))throw new Error('Regex backreferences are not supported.');if(/(?:\*|\+|\{\d+(?:,\d*)?\})(?:\s*\)|\s*)?(?:\*|\+|\{)/.test(p))throw new Error('Nested or repeated quantifier pattern is not supported.');return new RegExp(p,f);}
function parseCsv(text,options){const delimiter=options.delimiter;if(typeof delimiter!=='string'||delimiter.length!==1)throw new Error('PARSE_CSV requires an explicit one-character delimiter.');if(typeof options.header!=='boolean')throw new Error('PARSE_CSV requires explicit header BOOLEAN.');if(options.quoting!=='DOUBLE_QUOTE')throw new Error('PARSE_CSV quoting must be DOUBLE_QUOTE.');if(!['LF','CRLF','AUTO'].includes(options.newline))throw new Error('PARSE_CSV newline must be LF, CRLF, or AUTO.');if(options.encoding!=='UTF-8')throw new Error('PARSE_CSV encoding must be UTF-8.');const rows=[];let row=[],cell='',quoted=false,cells=0;for(let i=0;i<text.length;i++){const ch=text[i];if(quoted){if(ch==='"'&&text[i+1]==='"'){cell+='"';i++;}else if(ch==='"')quoted=false;else cell+=ch;continue;}if(ch==='"'){if(cell.length)throw new Error('Malformed CSV: quote begins inside an unquoted field.');quoted=true;continue;}if(ch===delimiter){row.push(cell);cell='';cells++;}else if(ch==='\n'){row.push(cell);rows.push(row);row=[];cell='';cells++;}else if(ch==='\r'){if(text[i+1]==='\n')continue;row.push(cell);rows.push(row);row=[];cell='';cells++;}else cell+=ch;if(cells>LIMITS.maxCsvCells)throw new Error('CSV exceeds deterministic runtime cell limit.');}if(quoted)throw new Error('Malformed CSV: unterminated quoted field.');if(cell.length||row.length){row.push(cell);rows.push(row);}if(rows.length>LIMITS.maxCollectionItems)throw new Error('CSV exceeds deterministic runtime row limit.');if(!options.header)return rows;if(!rows.length)return [];const header=rows[0];if(new Set(header).size!==header.length)throw new Error('CSV header contains duplicate names.');return rows.slice(1).map(r=>Object.fromEntries(header.map((h,i)=>[h,r[i]??''])));}
function parseXmlRestricted(text){const src=String(text);if(/<!DOCTYPE|<!ENTITY/i.test(src))throw new Error('PARSE_XML forbids DTD and entity declarations.');const token=/<\/?[A-Za-z_][A-Za-z0-9_.:-]*(?:\s+[^<>]*?)?\/?>|[^<]+/g,root={name:'#document',children:[],text:''},stack=[root];let m;while((m=token.exec(src))){const t=m[0];if(t.startsWith('</')){const name=t.slice(2,-1).trim(),node=stack.pop();if(!node||node===root||node.name!==name)throw new Error('Malformed XML closing tag.');continue;}if(t.startsWith('<')){const selfClose=/\/>$/.test(t),name=(t.match(/^<([A-Za-z_][A-Za-z0-9_.:-]*)/)||[])[1];if(!name)throw new Error('Malformed XML tag.');const node={name,children:[],text:''};stack.at(-1).children.push(node);if(!selfClose)stack.push(node);if(stack.length>LIMITS.maxParsedDepth)throw new Error('XML exceeds parsed-depth limit.');continue;}stack.at(-1).text+=t;}if(stack.length!==1)throw new Error('Malformed XML: unclosed tag.');if(root.children.length!==1)throw new Error('PARSE_XML requires exactly one document element.');return enforceParsedLimits(root.children[0],'XML');}
function selectXml(value,path){const parts=String(path||'').split('/').filter(Boolean);if(!parts.length||parts.length>LIMITS.maxSelectorDepth)throw new Error('SELECT_XML requires a bounded absolute element path.');let nodes=[value];if(nodes[0]?.name===parts[0])parts.shift();for(const part of parts){if(!/^[A-Za-z_][A-Za-z0-9_.:-]*$/.test(part))throw new Error('Unsupported SELECT_XML selector token: '+part);nodes=nodes.flatMap(n=>(n?.children||[]).filter(c=>c.name===part));if(nodes.length>LIMITS.maxCollectionItems)throw new Error('SELECT_XML result exceeds collection-size limit.');}return nodes;}
function selectJsonPath(value,path){const text=String(path||'').trim();if(text==='$')return value;if(!text.startsWith('$.'))throw new Error('SELECT_JSON_PATH supports only deterministic root paths beginning with $.');const parts=[];for(const token of text.slice(2).split('.')){const m=token.match(/^([^\[\]]+)(?:\[(\d+)\])?$/);if(!m)throw new Error('Unsupported JSON path token: '+token);parts.push(m[1]);if(m[2]!==undefined)parts.push(Number(m[2]));}if(parts.length>LIMITS.maxSelectorDepth)throw new Error('SELECT_JSON_PATH exceeds selector-depth limit.');let out=value;for(const part of parts){if(out===null||out===undefined||!(part in Object(out)))throw new Error('JSON path does not exist: '+text);out=out[part];}return out;}
function validateBindings(bindings){const issues=[];if(!isObject(bindings))return {valid:false,issues:['EXECUTABLE_INPUT_BINDINGS must be an object.']};for(const [name,binding] of Object.entries(bindings)){if(!/^[A-Z][A-Z0-9_]{0,63}$/.test(name))issues.push(`Invalid Test IR binding name ${name}.`);if(typeof binding==='string')continue;if(!isObject(binding)){issues.push(`Binding ${name} must be an artifact ID string or a closed binding object.`);continue;}const allowed=new Set(['artifactId','source','artifactRole','filename']);for(const key of Object.keys(binding))if(!allowed.has(key))issues.push(`Binding ${name} contains unsupported key ${key}.`);if(binding.source&&!['CURRENT_PRODUCT','CURRENT_SCOPE'].includes(binding.source))issues.push(`Binding ${name} has unsupported source ${binding.source}.`);if(!binding.artifactId&&!binding.artifactRole&&!binding.filename)issues.push(`Binding ${name} does not identify an artifact.`);}return {valid:issues.length===0,issues};}
function validateSpec(spec,bindings){const issues=[];if(!isObject(spec))return {valid:false,issues:['Test IR must be an object.']};for(const key of Object.keys(spec))if(!['version','steps'].includes(key))issues.push(`Test IR contains unknown root property ${key}.`);if(spec.version!==SPEC_VERSION)issues.push('Unsupported Test IR version.');if(!Array.isArray(spec.steps)||!spec.steps.length)issues.push('Test IR requires at least one step.');if((spec.steps?.length||0)>LIMITS.maxSteps)issues.push('Test IR exceeds maximum step count.');for(const [index,step] of (spec.steps||[]).entries()){if(!isObject(step)){issues.push(`Step ${index} is not an object.`);continue;}if(!OPS.includes(step.op)){issues.push(`Step ${index} uses unsupported operation ${String(step.op)}.`);continue;}const contract=CONTRACTS[step.op];for(const key of Object.keys(step))if(!contract.allowed.includes(key))issues.push(`Step ${index} ${step.op} contains unknown property ${key}.`);for(const key of contract.required)if(!Object.prototype.hasOwnProperty.call(step,key))issues.push(`Step ${index} ${step.op} is missing required argument ${key}.`);if('binding' in step&&typeof step.binding!=='string')issues.push(`Step ${index} binding must be a string.`);if('path' in step&&typeof step.path!=='string')issues.push(`Step ${index} path must be a string.`);if('message' in step&&typeof step.message!=='string')issues.push(`Step ${index} message must be a string.`);if(['REGEX','ASSERT_MATCH'].includes(step.op)){try{safeRegex(step.pattern,step.flags);}catch(error){issues.push(`Step ${index}: ${error.message}`);}}if(step.op==='PARSE_CSV'){if(typeof step.delimiter!=='string'||step.delimiter.length!==1)issues.push(`Step ${index} PARSE_CSV delimiter must be one character.`);if(typeof step.header!=='boolean')issues.push(`Step ${index} PARSE_CSV header must be boolean.`);if(step.quoting!=='DOUBLE_QUOTE')issues.push(`Step ${index} PARSE_CSV quoting must be DOUBLE_QUOTE.`);if(!['LF','CRLF','AUTO'].includes(step.newline))issues.push(`Step ${index} PARSE_CSV newline is unsupported.`);if(step.encoding!=='UTF-8')issues.push(`Step ${index} PARSE_CSV encoding must be UTF-8.`);}for(const key of ['absoluteTolerance','relativeTolerance'])if(key in step&&(!Number.isFinite(Number(step[key]))||Number(step[key])<0))issues.push(`Step ${index} ${key} must be a nonnegative finite number.`);if(step.op==='ASSERT_SET_EQUAL'&&!Array.isArray(step.value))issues.push(`Step ${index} ASSERT_SET_EQUAL value must be an array.`);}if(bindings!==undefined){const b=validateBindings(bindings);issues.push(...b.issues);}return {valid:issues.length===0,issues};}
function supports(test){if(String(field(test,'EXECUTION_MODE')||'').toUpperCase()!=='APPLICATION_DETERMINISTIC')return false;if(String(field(test,'REQUIRED_CAPABILITY')||'').toUpperCase()!==CAPABILITY)return false;if(String(field(test,'EXECUTABLE_KIND')||'').toUpperCase()!=='TEST_IR')return false;if(field(test,'EXECUTABLE_SPEC_VERSION')!==SPEC_VERSION)return false;return validateSpec(field(test,'EXECUTABLE_SPEC'),field(test,'EXECUTABLE_INPUT_BINDINGS')).valid;}
function compareValues(actual,expected,step={}){const abs='absoluteTolerance' in step?Number(step.absoluteTolerance):null,rel='relativeTolerance' in step?Number(step.relativeTolerance):null;if((abs!==null||rel!==null)){const a=Number(actual),e=Number(expected);if(!Number.isFinite(a)||!Number.isFinite(e))throw new Error('Tolerance comparison requires finite numeric values.');const delta=Math.abs(a-e),bound=Math.max(abs??0,(rel??0)*Math.max(Math.abs(a),Math.abs(e)));return delta<=bound;}if(typeof actual==='number'&&typeof expected==='number'&&(!Number.isInteger(actual)||!Number.isInteger(expected)))throw new Error('Exact decimal-number equality requires explicit tolerance or exact decimal-string operands.');return stable(actual instanceof Uint8Array?[...actual]:actual)===stable(expected instanceof Uint8Array?[...expected]:expected);}
function assertion(ok,expected,actual,message){return {determination:ok?'SATISFIED':'VIOLATED',expected,actual,message:message||null};}
async function execute({spec,artifacts,canonicalBindings={}}){const check=validateSpec(spec);if(!check.valid)throw new Error(check.issues.join(' '));const source=isObject(artifacts)?artifacts:{},canonical=isObject(canonicalBindings)?canonicalBindings:{},entries=Object.values(source);let totalBytes=0;for(const item of entries){const b=bytesOf(item?.bytes??item);if(b)totalBytes+=b.byteLength;}if(totalBytes>LIMITS.maxInputBytes)throw new Error('Test input exceeds deterministic runtime total-byte limit.');let value=null,currentArtifact=null,lastRegex=null,lastAssertion=null;const observations=[];for(const [index,step] of spec.steps.entries()){switch(step.op){case 'LOAD_ARTIFACT':{const a=source[step.binding]??canonical[step.binding];if(a===undefined)throw new Error(`Binding ${step.binding} is unavailable.`);currentArtifact=a;value=a;observations.push({step:index,op:step.op,binding:step.binding,artifactId:a?.artifactId||null,filename:a?.filename||null,sha256:a?.sha256||null});break;}case 'READ_BYTES':{const b=bytesOf(currentArtifact?.bytes??value?.bytes??value);if(!b)throw new Error('READ_BYTES requires artifact bytes.');if(b.byteLength>LIMITS.maxInputBytes)throw new Error('READ_BYTES exceeds deterministic runtime byte limit.');value=b;observations.push({step:index,op:step.op,byteLength:b.byteLength});break;}case 'DECODE_UTF8':{const b=bytesOf(value);if(!b)throw new Error('DECODE_UTF8 requires bytes.');if(b.byteLength>LIMITS.maxInputBytes)throw new Error('Text input exceeds deterministic runtime byte limit.');value=new TextDecoder('utf-8',{fatal:true}).decode(b);break;}case 'PARSE_JSON':value=enforceParsedLimits(JSON.parse(String(value)),'JSON');break;case 'PARSE_CSV':value=enforceParsedLimits(parseCsv(String(value),step),'CSV');break;case 'PARSE_XML':value=parseXmlRestricted(String(value));break;case 'SELECT_JSON_PATH':value=selectJsonPath(value,step.path);break;case 'SELECT_XML':value=selectXml(value,step.path);break;case 'COUNT':if(value==null||typeof value.length!=='number')throw new Error('COUNT requires an array, string, or array-like value.');if(value.length>LIMITS.maxCollectionItems)throw new Error('COUNT input exceeds collection limit.');value=value.length;break;case 'SUM':case 'MIN':case 'MAX':{if(!Array.isArray(value)||value.length>LIMITS.maxCollectionItems)throw new Error(`${step.op} requires a bounded array.`);const nums=value.map(Number);if(nums.some(x=>!Number.isFinite(x)))throw new Error(`${step.op} requires finite numeric values.`);if(nums.some(x=>Number.isInteger(x)&&!Number.isSafeInteger(x)))throw new Error(`${step.op} rejects unsafe integers.`);value=step.op==='SUM'?nums.reduce((a,b)=>a+b,0):step.op==='MIN'?Math.min(...nums):Math.max(...nums);if(Number.isInteger(value)&&!Number.isSafeInteger(value))throw new Error(`${step.op} produced an unsafe integer.`);break;}case 'SORT':if(!Array.isArray(value)||value.length>LIMITS.maxCollectionItems)throw new Error('SORT requires a bounded array.');value=[...value].sort((a,b)=>stable(a).localeCompare(stable(b)));break;case 'UNIQUE':{if(!Array.isArray(value)||value.length>LIMITS.maxCollectionItems)throw new Error('UNIQUE requires a bounded array.');const seen=new Set();value=value.filter(v=>{const key=stable(v);if(seen.has(key))return false;seen.add(key);return true;});break;}case 'HASH_SHA256':value=await sha256(value);break;case 'REGEX':{const input=String(value);if(new TextEncoder().encode(input).byteLength>LIMITS.maxRegexInputBytes)throw new Error('REGEX input exceeds deterministic runtime limit.');lastRegex=safeRegex(step.pattern,step.flags);value=lastRegex.test(input);break;}case 'COMPARE':{const other='binding' in step?(source[step.binding]?.value??canonical[step.binding]?.value??source[step.binding]??canonical[step.binding]):step.value;value=compareValues(value,other,step);break;}case 'BYTE_COMPARE':{const left=bytesOf(value),right=bytesOf(source[step.binding]?.bytes??source[step.binding]??canonical[step.binding]?.bytes??canonical[step.binding]);if(!left||!right)throw new Error('BYTE_COMPARE requires byte-backed current value and target binding.');let equal=left.byteLength===right.byteLength;if(equal)for(let i=0;i<left.byteLength;i++)if(left[i]!==right[i]){equal=false;break;}value=equal;break;}case 'ASSERT_EQ':lastAssertion=assertion(compareValues(value,step.value,step),step.value,value,step.message);break;case 'ASSERT_GT':lastAssertion=assertion(Number(value)>Number(step.value),`> ${step.value}`,value,step.message);break;case 'ASSERT_GTE':lastAssertion=assertion(Number(value)>=Number(step.value),`>= ${step.value}`,value,step.message);break;case 'ASSERT_LT':lastAssertion=assertion(Number(value)<Number(step.value),`< ${step.value}`,value,step.message);break;case 'ASSERT_LTE':lastAssertion=assertion(Number(value)<=Number(step.value),`<= ${step.value}`,value,step.message);break;case 'ASSERT_MATCH':{const regex=lastRegex||safeRegex(step.pattern,step.flags);lastAssertion=assertion(regex.test(String(value)),String(regex),value,step.message);break;}case 'ASSERT_CONTAINS':{const ok=Array.isArray(value)?value.some(v=>stable(v)===stable(step.value)):String(value).includes(String(step.value));lastAssertion=assertion(ok,`contains ${stable(step.value)}`,value,step.message);break;}case 'ASSERT_NOT_CONTAINS':{const ok=Array.isArray(value)?!value.some(v=>stable(v)===stable(step.value)):!String(value).includes(String(step.value));lastAssertion=assertion(ok,`does not contain ${stable(step.value)}`,value,step.message);break;}case 'ASSERT_SET_EQUAL':{if(!Array.isArray(value))throw new Error('ASSERT_SET_EQUAL requires array input.');const a=[...new Set(value.map(stable))].sort(),b=[...new Set(step.value.map(stable))].sort();lastAssertion=assertion(stable(a)===stable(b),step.value,value,step.message);break;}default:throw new Error(`Unsupported Test IR operation ${step.op}.`);}if(lastAssertion){observations.push({step:index,op:step.op,...lastAssertion});if(lastAssertion.determination==='VIOLATED')return {status:'COMPLETE',determination:'VIOLATED',expected:lastAssertion.expected,actual:lastAssertion.actual,observations,executorVersion:VERSION,runtimeVersion:VERSION,specVersion:SPEC_VERSION};lastAssertion=null;}}const finalAssertion=[...observations].reverse().find(x=>x.determination);return {status:'COMPLETE',determination:finalAssertion?.determination||'UNDETERMINED',expected:finalAssertion?.expected??null,actual:finalAssertion?.actual??value,observations,executorVersion:VERSION,runtimeVersion:VERSION,specVersion:SPEC_VERSION};}
async function executeTest(test,artifacts,canonicalBindings={}){if(!supports(test))throw new Error('Test is not a valid supported application-deterministic Test IR test.');const spec=field(test,'EXECUTABLE_SPEC'),bindings=field(test,'EXECUTABLE_INPUT_BINDINGS'),validation=validateSpec(spec,bindings);if(!validation.valid)throw new Error(validation.issues.join(' '));if(typeof Worker==='undefined')return execute({spec,artifacts,canonicalBindings});return new Promise(resolve=>{const requestId='TEST-RUN-'+(globalThis.crypto?.randomUUID?.()||Math.random().toString(36).slice(2)),worker=new Worker(WORKER_URL),timer=setTimeout(()=>{worker.terminate();resolve({status:'EXECUTION_FAILED',determination:'UNDETERMINED',error:'Deterministic worker exceeded execution time limit.',runtimeVersion:VERSION,specVersion:SPEC_VERSION});},LIMITS.maxWorkerMs);const finish=result=>{clearTimeout(timer);worker.terminate();resolve(result);};worker.onmessage=event=>{if(event.data?.requestId===requestId)finish(event.data.result);};worker.onerror=event=>finish({status:'EXECUTION_FAILED',determination:'UNDETERMINED',error:event.message||'Deterministic worker failed.',runtimeVersion:VERSION,specVersion:SPEC_VERSION});worker.postMessage({requestId,spec,artifacts,canonicalBindings});});}
const operationContracts=()=>JSON.parse(JSON.stringify(CONTRACTS));
globalThis.closedLoopTestRuntime=Object.freeze({VERSION,SPEC_VERSION,CAPABILITY,OPS,LIMITS,WORKER_URL,validateSpec,validateBindings,supports,execute,executeTest,capabilities:()=>Object.freeze([CAPABILITY]),operationContracts});
})();