import fs from 'node:fs';
import {createHash} from 'node:crypto';
const readLines=p=>fs.readFileSync(p,'utf8').split('\n');
const writeLines=(p,l)=>fs.writeFileSync(p,l.join('\n'));
function index(lines,needle,path){const m=[];for(let i=0;i<lines.length;i++)if(lines[i].includes(needle))m.push(i);if(m.length!==1)throw new Error(`${path}: expected one ${needle}, got ${m.length}`);return m[0];}
function replaceLine(path,needle,replacement){const lines=readLines(path),i=index(lines,needle,path);lines.splice(i,1,...(Array.isArray(replacement)?replacement:[replacement]));writeLines(path,lines);}
function insertBefore(path,needle,newLines){const lines=readLines(path),i=index(lines,needle,path);lines.splice(i,0,...newLines);writeLines(path,lines);}
function insertAfter(path,needle,newLines){const lines=readLines(path),i=index(lines,needle,path);lines.splice(i+1,0,...newLines);writeLines(path,lines);}
function replaceText(path,from,to){const s=fs.readFileSync(path,'utf8'),a=s.indexOf(from),b=s.lastIndexOf(from);if(a<0||a!==b)throw new Error(`${path}: exact text anchor count != 1`);fs.writeFileSync(path,s.slice(0,a)+to+s.slice(a+from.length));}

insertBefore('workflow-engine.js','const TEST_EXECUTION_ACTIONS=Object.freeze({',["const APPLICATION_TEST_EXECUTORS=Object.freeze({});","function applicationTestCapabilities(){return Object.freeze(Object.keys(APPLICATION_TEST_EXECUTORS));}"]);
replaceLine('workflow-engine.js',"APPLICATION_DETERMINISTIC:'No operator execution is required now.","  APPLICATION_DETERMINISTIC:'No operator execution is required only when the exact REQUIRED_CAPABILITY names a registered application-native test executor. Otherwise request a corrected test definition; the browser must not pretend it can run the test.',");
replaceLine('workflow-engine.js',"return {testId:recordId(test,'tests'),requirementId:testRequirementId(test),testType:upper(recordValue(test,'TEST_TYPE'))",[
"    const requiredCapability=String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim(),applicationExecutorSupported=mode!=='APPLICATION_DETERMINISTIC'||Object.prototype.hasOwnProperty.call(APPLICATION_TEST_EXECUTORS,requiredCapability);",
"    const operatorAction=mode==='APPLICATION_DETERMINISTIC'&&!applicationExecutorSupported?`Request a corrected test definition. No application-native executor is registered for ${requiredCapability||'the declared capability'}; use the real capable execution mode or UNAVAILABLE.`:(TEST_EXECUTION_ACTIONS[mode]||'Execution responsibility is not validly classified.');",
"    return {testId:recordId(test,'tests'),requirementId:testRequirementId(test),testType:upper(recordValue(test,'TEST_TYPE'))||'UNKNOWN',executionMode:mode,requiredCapability,artifactRequirements:String(recordValue(test,'ARTIFACT_REQUIREMENTS')||'').trim(),applicationExecutorSupported,operatorAction};"
]);
insertAfter('workflow-engine.js',"const unavailableTestIds=items.filter(item=>item.executionMode==='UNAVAILABLE')",["  const unsupportedApplicationTestIds=items.filter(item=>item.executionMode==='APPLICATION_DETERMINISTIC'&&!item.applicationExecutorSupported).map(item=>item.testId);"]);
replaceLine('workflow-engine.js','return {total:items.length,counts,incompleteTestIds,unavailableTestIds,items};',"  return {total:items.length,counts,incompleteTestIds,unavailableTestIds,unsupportedApplicationTestIds,items};");
insertAfter('workflow-engine.js','if(unavailable.length)reasons.push(`',["      const unsupportedApplication=mandatoryTests.filter(test=>upper(recordValue(test,'EXECUTION_MODE'))==='APPLICATION_DETERMINISTIC'&&!Object.prototype.hasOwnProperty.call(APPLICATION_TEST_EXECUTORS,String(recordValue(test,'REQUIRED_CAPABILITY')||'').trim()));","      if(unsupportedApplication.length)reasons.push(`${unsupportedApplication.length} mandatory test definition(s) claim APPLICATION_DETERMINISTIC without a registered application-native executor.`);"]);
replaceLine('workflow-engine.js','currentScope,recordsForScope,recordsForCurrentScope,scopeForIteration,recordsForIteration,verificationMatrix,evaluateIteration,DERIVATIONS,coverageMetrics,convergenceMetrics,releaseMetrics,testExecutionPlan,operationalMetrics,gate,deriveStageData,recalculate,invalidateDownstream,applicationInitialFields,',"  currentScope,recordsForScope,recordsForCurrentScope,scopeForIteration,recordsForIteration,verificationMatrix,evaluateIteration,DERIVATIONS,coverageMetrics,convergenceMetrics,releaseMetrics,applicationTestCapabilities,testExecutionPlan,operationalMetrics,gate,deriveStageData,recalculate,invalidateDownstream,applicationInitialFields,");

replaceLine('prompt-engine.js',"6:'Define this job’s verification suite before any production instruction is authored.","6:'Define this job’s verification suite before any production instruction is authored. A TEST record is a verification specification: it does not by itself prove that an executable test file exists or that any test has run. Every active mandatory requirement must have at least one valid proposed test. Use the strongest suitable method, including exact computation, programmatic checks, schema or structure checks, source comparison, rule-based verification, independent review, human inspection, or specialized external systems where appropriate. For every test define TEST_TYPE, EXECUTION_MODE, REQUIRED_CAPABILITY, ARTIFACT_REQUIREMENTS, inputs, tools, procedure, expected result, failure condition, and evidence to preserve. EXECUTION_MODE must be exactly one of APPLICATION_DETERMINISTIC, EXTERNAL_AGENT_TOOL, INDEPENDENT_AGENT_REVIEW, HUMAN_INSPECTION, EXTERNAL_SYSTEM, or UNAVAILABLE. APPLICATION_DETERMINISTIC is reserved for an exact REQUIRED_CAPABILITY listed in APPLICATION-NATIVE TEST CAPABILITIES in this controlling prompt. If that list is NONE, do not select APPLICATION_DETERMINISTIC; use the real capable executor or UNAVAILABLE. This static browser is not a universal compiler, script runner, CAD/CAE tool, lab, or machine controller. Create executable test artifacts only when an executable artifact is actually the appropriate verification mechanism. If you actually return a test fixture, script, model, report, or other file, declare it as a required attachment, create an evidence entry whose attachmentRef points to that attachment temporaryKey, and include that evidence temporaryKey in the corresponding TEST record evidenceRefs. Return the exact file through the external environment so the operator can attach its bytes; a filename, claimed hash, repository path, or code block is not possession of a file. This yields the canonical TEST -> EVIDENCE_ID -> ATTACHMENT_ID trace after validated ingestion. Use ARTIFACT_REQUIREMENTS = NONE when no separate executable/input artifact is required. Any non-NONE ARTIFACT_REQUIREMENTS means actual byte-backed artifact evidence is mandatory before the response can validate. If the required capability is unavailable and no valid equivalent path exists, use EXECUTION_MODE = UNAVAILABLE and do not pretend the test can run. The application assigns TEST_ID, controlled test-suite identity, and exact mandatory requirement-to-test coverage; do not assert those application-owned values.',");
insertAfter('prompt-engine.js','${contextFor(stage,state,operation,scope)}',['','APPLICATION-NATIVE TEST CAPABILITIES',"${workflow.applicationTestCapabilities().length?workflow.applicationTestCapabilities().join('\\n'):'NONE'}"]);
replaceLine('prompt-engine.js',"12:'Verify each current execution independently.","12:'Verify each current execution independently. Execute only the currently missing REQ_ID × RUN_ID × TEST_ID triples listed by the application in VERIFICATION BATCH PLAN, in listed order. Respect each test’s EXECUTION_MODE, REQUIRED_CAPABILITY, ARTIFACT_REQUIREMENTS, and actual artifact accessibility. Never substitute a different executor merely because it is convenient. APPLICATION_DETERMINISTIC is valid only for a capability explicitly listed in APPLICATION-NATIVE TEST CAPABILITIES; if none is registered, the test definition is invalid and must be corrected rather than externally fabricated. An external verifier must not fabricate application-native execution. EXTERNAL_AGENT_TOOL requires the actual declared tool/access, INDEPENDENT_AGENT_REVIEW requires an independent reviewer, HUMAN_INSPECTION requires the specified human observation, and EXTERNAL_SYSTEM requires the specified external system/lab/machine evidence. If the required executor, capability, or exact artifact is unavailable, do not claim the test ran: preserve the triple as UNDETERMINED with the exact reason/evidence when the contract permits, or return the appropriate blocking/failure disposition. Preserve verifier and verifier-context identity, independence status, inputs, procedure, expected result, observed result, exact evidence, SATISFIED/VIOLATED/UNDETERMINED, defect reference where applicable, and reason for UNDETERMINED. If the complete listed batch would exceed the declared response byte limit, return the longest leading prefix that fits; never skip ahead, repeat an already-current triple, substitute wrong-version or wrong-iteration records, or self-validate. After accepted ingestion the application regenerates this stage from the newly missing set. The application reconciles the complete matrix mathematically and the stage cannot complete from a partial batch.',");

insertAfter('test-fixtures.mjs','if(Object.hasOwn(overrides,name))return overrides[name];',["  if(String(name).toUpperCase()==='EXECUTION_MODE')return 'EXTERNAL_AGENT_TOOL';"]);
insertBefore('app-core.js',"const intro=n===6?'Stage 6 defines how each requirement will be verified.",["  if(plan.unsupportedApplicationTestIds.length)actionRows.unshift({mode:'Invalid application executor claim',tests:plan.unsupportedApplicationTestIds.length,nextAction:'Request a corrected Stage 6 test definition. No registered application-native executor exists for the declared capability; do not run or accept a substitute execution.'});"]);

replaceLine('verify-prompt-semantics.mjs','TEST_DEFINITION_ARTIFACT_BOUNDARY_MISSING',["    if(!record.prompt.includes('A TEST record is a verification specification')||!record.prompt.includes('a filename, claimed hash, repository path, or code block is not possession of a file'))issues.push('TEST_DEFINITION_ARTIFACT_BOUNDARY_MISSING');","    if(!record.prompt.includes('APPLICATION-NATIVE TEST CAPABILITIES\\nNONE')||!record.prompt.includes('do not select APPLICATION_DETERMINISTIC'))issues.push('UNREGISTERED_APPLICATION_EXECUTOR_NOT_BLOCKED');","    if(!record.prompt.includes('TEST -> EVIDENCE_ID -> ATTACHMENT_ID')||!record.prompt.includes('attachmentRef')||!record.prompt.includes('evidenceRefs'))issues.push('TEST_ARTIFACT_CANONICAL_LINK_MISSING');"]);
insertBefore('verify-prompt-semantics.mjs','let checked=0;',['{'," const fixture=fs.readFileSync('test-fixtures.mjs','utf8');"," if(!fixture.includes(\"EXECUTION_MODE')return 'EXTERNAL_AGENT_TOOL'\"))throw new Error('Synthetic fixtures still default to a nonexistent application-native executor.');"," if(engine.applicationTestCapabilities().length!==0)throw new Error('A native test capability was registered without a proven application executor test in this patch.');"," const ui=fs.readFileSync('app-core.js','utf8');"," if(!ui.includes('Invalid application executor claim')||!ui.includes('No registered application-native executor exists'))throw new Error('Operator UI does not fail unsupported application-native test claims closed.');",'}','']);
insertBefore('verify-complete.mjs','// Invalid canonical relationship is rejected before mutation.',['// APPLICATION_DETERMINISTIC cannot satisfy Stage 06 unless an actual application-native executor is registered.','{',"  assert(engine.applicationTestCapabilities().length===0,'Unexpected application-native executor registration.');","  const p=project('JOB-NATIVE-EXECUTOR-TRUTH');","  Object.assign(p.job,{CURRENT_SOURCE_SET_VERSION:'SOURCE-SET-v001',CURRENT_REQUIREMENTS_VERSION:'REQUIREMENTS-v001',CURRENT_TEST_SUITE_VERSION:'TEST-SUITE-v001'});","  const scope={inputVersion:'INPUT-v001',sourceSetVersion:'SOURCE-SET-v001',requirementsVersion:'REQUIREMENTS-v001'};","  p.projectData.requirements.push({id:'REQ-NATIVE-1',stage:4,active:true,scope,fields:{REQ_ID:'REQ-NATIVE-1',MANDATORY_OPTIONAL_STATUS:'MANDATORY',STATUS:'ACTIVE'}});","  p.projectData.tests.push({id:'TEST-NATIVE-1',stage:6,active:true,scope:{...scope,testSuiteVersion:'TEST-SUITE-v001'},fields:{TEST_ID:'TEST-NATIVE-1',REQ_ID:'REQ-NATIVE-1',TEST_TYPE:'DETERMINISTIC',EXECUTION_MODE:'APPLICATION_DETERMINISTIC',REQUIRED_CAPABILITY:'NONEXISTENT_NATIVE_EXECUTOR',ARTIFACT_REQUIREMENTS:'NONE',INPUTS:'canonical input',TOOLS:'application',PROCEDURE:'native check',EXPECTED_RESULT:'satisfied',FAILURE_CONDITION:'not satisfied',EVIDENCE_TO_PRESERVE:'derived evidence',STATUS:'READY'},relationships:{REQ_ID:'REQ-NATIVE-1'}});",'  const plan=engine.testExecutionPlan(p);',"  assert(plan.unsupportedApplicationTestIds.includes('TEST-NATIVE-1'),'Execution plan did not identify unsupported application-native test execution.');",'  const g=engine.gate(6,p);',"  assert(g.reasons.some(reason=>reason.includes('without a registered application-native executor')),'Stage 06 accepted an APPLICATION_DETERMINISTIC test with no actual executor.');",'}','']);

replaceText('README.md','The static browser is authoritative only for deterministic operations it actually implements. Tool-dependent checks run in the capable external environment; independent reviews use an independent context; irreducible inspections remain human/reviewer work; specialized systems/labs provide their own execution evidence. A test definition, executable/input artifact, and execution result are distinct records of reality.','The static browser is authoritative only for deterministic operations it actually implements. APPLICATION_DETERMINISTIC is accepted only when the controlling prompt lists an exact registered application-native test capability; the current registry is intentionally empty until a per-test executor is implemented and proven, so the mode cannot be used as a fictional shortcut. Tool-dependent checks run in the capable external environment; independent reviews use an independent context; irreducible inspections remain human/reviewer work; specialized systems/labs provide their own execution evidence. A test definition, executable/input artifact, and execution result are distinct records of reality. When an executable/input test artifact is returned, the existing response evidence path binds TEST -> EVIDENCE_ID -> ATTACHMENT_ID after the application verifies the actual attachment bytes.');

{const runtimeFiles=['workbook.js','hash.js','workflow-schema.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js'];const gitBlobSha=file=>{const bytes=fs.readFileSync(file);return createHash('sha1').update(`blob ${bytes.length}\0`).update(bytes).digest('hex');};const manifest=runtimeFiles.map(file=>`${file}:${gitBlobSha(file)}\n`).join('');const token=`runtime-${createHash('sha256').update(manifest).digest('hex').slice(0,16)}`;const path='index.html',old='runtime-823183a72b1e5462',source=fs.readFileSync(path,'utf8'),count=source.split(old).length-1;if(count!==8)throw new Error(`index expected 8 old runtime tokens, got ${count}`);fs.writeFileSync(path,source.split(old).join(token));}
