import fs from 'node:fs';
const patch=(file,from,to)=>{let s=fs.readFileSync(file,'utf8');if(!s.includes(from))throw new Error(`missing patch target ${file}`);fs.writeFileSync(file,s.replace(from,to));};
patch('app-core.js',"if(error?.code==='MISSING_REQUIRED_PROMPT_SCOPE')return `CONTROLLING INSTRUCTION UNAVAILABLE\\n\\n${error.message}\\n\\nReserve/select the exact execution lane shown for this stage, then save or copy the instruction.`;throw error;","if(error?.code==='MISSING_REQUIRED_PROMPT_SCOPE')return `CONTROLLING INSTRUCTION UNAVAILABLE\\n\\n${error.message}\\n\\nReserve/select the exact execution lane shown for this stage, then save or copy the instruction.`;if(error?.code==='STAGE04_PREREQUISITES_INCOMPLETE'||error?.code==='STAGE04_OBLIGATION_UNIVERSE_EMPTY')return `CONTROLLING INSTRUCTION UNAVAILABLE\\n\\n${error.message}\\n\\nComplete and accept the required upstream stage work. The application will then generate Stage 04 from captured canonical state; do not reattach or retype the original user intent.`;throw error;");
patch('project-store.js',"project?.schema!=='closed-loop-project/2'","project?.schema!=='closed-loop-project/3'");
console.log('store and UI patch applied');
