import fs from 'node:fs';
const r=p=>fs.readFileSync(p,'utf8'),w=(p,s)=>fs.writeFileSync(p,s),rep=(s,a,b,n)=>{if(!s.includes(a))throw new Error('missing '+n);return s.replace(a,b)};

let s=r('response-ingestion.js');
const marker="function answerHumanInput(project,answers,{operator='HUMAN_OPERATOR'}={}){";
const abandon=`function abandon(project,proposalId,{operator='HUMAN_OPERATOR',reason='Operator abandoned the pending attempt.'}={}){\n  const next=clone(project);workflow.ensureShape(next);const proposal=findProposal(next,proposalId);if(!proposal)throw new Error('Response proposal does not exist.');if(proposal.status!=='PENDING_OPERATOR_REVIEW')throw new Error(\`Response proposal is \${proposal.status}, not pending review.\`);\n  const at=now(),note=String(reason||'Operator abandoned the pending attempt.');proposal.status='ABANDONED_BY_OPERATOR';proposal.reviewedAt=at;proposal.reviewedBy=operator;proposal.reviewNote=note;const validation=findValidation(next,proposal.validationId);if(validation)validation.status='ABANDONED_BY_OPERATOR';const raw=findRaw(next,proposal.rawResponseId);if(raw)raw.status='ABANDONED_BY_OPERATOR';const receipt=findReceipt(next,proposal.receiptId);if(receipt){receipt.completionState='ABANDONED_BY_OPERATOR';receipt.nextRequiredVerificationStage=\`REGENERATE STAGE \${String(proposal.stage).padStart(2,'0')} RESPONSE\`;}\n  const d=disposition(next,'ABANDONED_RESPONSE',{stage:proposal.stage,rawResponseId:proposal.rawResponseId,promptId:proposal.promptId,validationId:proposal.validationId,proposalId,receiptId:proposal.receiptId,details:{operator,reason:note,canonicalAcceptedWorkChanged:false,newPromptRequired:true}});workflow.addHistory(next,'RESPONSE_ATTEMPT_ABANDONED',{stage:proposal.stage,rawResponseId:proposal.rawResponseId,proposalId,dispositionId:d.dispositionId,operator,reason:note,canonicalAcceptedWorkChanged:false,newPromptRequired:true});\n  const controlling=safe(next.projectData.generatedPrompts).find(x=>(x.instructionId||x.promptId)===proposal.promptId&&!x.invalidatedBy);if(controlling)controlling.invalidatedBy=d.dispositionId;const promptEngine=globalThis.closedLoopPromptEngine;if(promptEngine){const preview=clone(next);preview.revision=Number(next.revision||0)+1;const generated=promptEngine.buildPromptRecord(proposal.stage,preview,{operation:proposal.envelope.operation,scope:clone(proposal.envelope.scope||{})});workflow.registerGeneratedPrompt(next,{...generated,generatedAt:now()});}\n  workflow.recalculate(next);return {project:next,disposition:d,receipt};\n}\n\n`;
s=rep(s,marker,abandon+marker,'abandon insertion');s=rep(s,'commit,reject,answerHumanInput','commit,reject,abandon,answerHumanInput','abandon export');w('response-ingestion.js',s);

s=r('workflow-engine.js');const resMarker="function resolveHumanBlocker(project,{blockerId,resolutionEvidence,operatorLabel='HUMAN_OPERATOR'}={}){";
const custody=`function reconcileArtifactCustodyVerification(project,{report,stage=project.activeStage}={}){\n  ensureShape(project);if(!report||typeof report!=='object')throw new Error('An application artifact-verification report is required.');const open=records(project,'blockers',{active:false}).filter(record=>record.source==='APPLICATION'&&record.applicationBlockerKind==='ARTIFACT_CUSTODY'&&isActiveRecord(record)&&['OPEN','BLOCKED','IN PROGRESS','UNKNOWN'].includes(upper(recordValue(record,'STATUS')||'OPEN')));\n  if(report.verified){for(const blocker of open){const updates={STATUS:'RESOLVED',RESOLUTION_EVIDENCE:\`Application re-read \${report.artifactCount} stored Blob(s); all required canonical byte identities reconciled at \${report.at||now()}.\`,CLOSURE:'CLOSED',REEVALUATION:'COMPLETED',REQUIRED_REVALIDATION:'RECALCULATED'};blocker.fields=blocker.fields&&typeof blocker.fields==='object'?blocker.fields:{};for(const [key,value] of Object.entries(updates)){blocker.fields[key]=value;blocker[key]=value;}blocker.updatedAt=now();blocker.resolvedBy='APPLICATION';refreshRecordHashes(blocker,'blockers');addHistory(project,'APPLICATION_ARTIFACT_CUSTODY_RESTORED',{recordId:recordId(blocker,'blockers'),verificationAt:report.at||now()});}if(open.length)recalculate(project);return {verified:true,resolvedBlockerIds:open.map(record=>recordId(record,'blockers'))};}\n  const failures=safe(report.artifacts).filter(item=>!item.verified),artifactIds=failures.map(item=>item.artifactId).filter(Boolean),reason=\`Stored artifact custody verification failed: \${failures.map(item=>\`\${item.artifactId||'UNKNOWN'} (\${item.issue||'byte/hash/metadata mismatch'})\`).join(', ')||'required canonical bytes are missing or mismatched'}.\`;let blocker=open[0];if(!blocker){blocker=commandRecord(project,'blockers',{MISSING_ITEM_TYPE:'ARTIFACT_CUSTODY',MISSING_FACT_INPUT_AUTHORITY_EVIDENCE_CAPABILITY_DECISION_RULE:reason,AFFECTED_REQUIREMENTS:'UNKNOWN',AFFECTED_TESTS:'UNKNOWN',AFFECTED_ARTIFACTS:artifactIds.length?artifactIds:'UNKNOWN',WHY_WORK_CANNOT_CONTINUE:reason,ATTEMPTED_RESOLUTIONS:'APPLICATION BYTE RE-READ AND SHA-256 VERIFICATION',DOWNSTREAM_WORK_STOPPED:'RELEASE AND DEPENDENT WORK',OWNER:'APPLICATION',STATUS:'OPEN',RESOLUTION_EVIDENCE:'NONE',CLOSURE:'OPEN',REEVALUATION:'REQUIRED',REQUIRED_REVALIDATION:'REQUIRED'},{stage,source:'APPLICATION'});blocker.applicationBlockerKind='ARTIFACT_CUSTODY';refreshRecordHashes(blocker,'blockers');addHistory(project,'APPLICATION_ARTIFACT_CUSTODY_BLOCKED',{recordId:recordId(blocker,'blockers'),artifactIds,verificationAt:report.at||now()});}\n  project.release.authorization='NOT AUTHORIZED';project.release.authorizedArtifactIds=[];for(const collection of ['artifactIdentities','evidenceChains','releaseRecords'])for(const record of records(project,collection,{active:false}))if(isActiveRecord(record)){record.active=false;record.validity='INVALIDATED';record.invalidatedBy=recordId(blocker,'blockers');refreshRecordHashes(record,collection);}recalculate(project);return {verified:false,blockerId:recordId(blocker,'blockers')};\n}\n`;
s=rep(s,resMarker,custody+resMarker,'custody insertion');s=rep(s,'registerGeneratedPrompt,createHumanBlocker,resolveHumanBlocker','registerGeneratedPrompt,createHumanBlocker,reconcileArtifactCustodyVerification,resolveHumanBlocker','custody export');w('workflow-engine.js',s);

s=r('project-store.js');
const old=/async function verifyProjectArtifacts\(jobId\)\{[\s\S]*?\n\}/;if(!old.test(s))throw new Error('verifyProjectArtifacts missing');
const verify=`async function verifyProjectArtifacts(jobId){\n  const id=String(jobId||'').trim();if(!id)throw storageError('JOB_ID is required for artifact verification.','ARTIFACT_VERIFY_JOB_ID_REQUIRED');const projects=await readAll(),project=projects.find(item=>projectIdentity(item)===id);if(!project)throw storageError(\`Project \${id} is not stored.\`,'ARTIFACT_VERIFY_PROJECT_MISSING');const rows=await listArtifacts(id),rowById=new Map(rows.map(row=>[String(row.artifactId),row])),engine=globalThis.closedLoopWorkflowEngine,expected=engine.records(project,'artifacts').filter(record=>engine.isActiveRecord(record)&&String(engine.recordValue(record,'AVAILABILITY')||'').toUpperCase()==='BYTES_PERSISTED_AND_VERIFIED'),expectedById=new Map(expected.map(record=>[engine.recordId(record,'artifacts'),record])),results=[];let byteSize=0;\n  for(const record of expected){const artifactId=engine.recordId(record,'artifacts'),row=rowById.get(artifactId),canonicalFilename=String(engine.recordValue(record,'FILENAME')||''),canonicalSize=Number(engine.recordValue(record,'BYTE_SIZE')),canonicalSha256=String(engine.recordValue(record,'SHA256')||'');if(!row){results.push({artifactId,filename:canonicalFilename,storedByteSize:null,actualByteSize:null,storedSha256:null,actualSha256:null,verified:false,issue:'MISSING_STORED_BLOB'});continue;}const bytes=new Uint8Array(await row.blob.arrayBuffer()),actualSize=bytes.byteLength,actualSha256=await hash.sha256Bytes(bytes);byteSize+=actualSize;const verified=String(row.jobId)===id&&String(row.filename)===canonicalFilename&&Number(row.byteSize)===canonicalSize&&String(row.sha256)===canonicalSha256&&actualSize===canonicalSize&&actualSha256===canonicalSha256;results.push({artifactId,filename:canonicalFilename,storedByteSize:Number(row.byteSize),actualByteSize:actualSize,storedSha256:String(row.sha256||''),actualSha256,verified,issue:verified?null:'CANONICAL_BLOB_IDENTITY_MISMATCH'});}\n  for(const row of rows)if(!expectedById.has(String(row.artifactId))){const bytes=new Uint8Array(await row.blob.arrayBuffer()),actualSize=bytes.byteLength,actualSha256=await hash.sha256Bytes(bytes);byteSize+=actualSize;const verified=actualSize===Number(row.byteSize)&&actualSha256===String(row.sha256);results.push({artifactId:String(row.artifactId),filename:String(row.filename||row.artifactId),storedByteSize:Number(row.byteSize),actualByteSize:actualSize,storedSha256:String(row.sha256||''),actualSha256,verified,issue:verified?null:'UNREFERENCED_STORED_BLOB_MISMATCH',canonicalExpectation:false});}\n  const mismatches=results.filter(item=>!item.verified),report={jobId:id,artifactCount:rows.length,expectedCanonicalArtifactCount:expected.length,byteSize,verifiedCount:results.length-mismatches.length,mismatchCount:mismatches.length,verified:mismatches.length===0,artifacts:results,at:now()};await metaPut('lastArtifactVerification:'+id,{jobId:id,artifactCount:report.artifactCount,expectedCanonicalArtifactCount:report.expectedCanonicalArtifactCount,byteSize:report.byteSize,mismatchCount:report.mismatchCount,verified:report.verified,at:report.at});return report;\n}`;
s=s.replace(old,verify);s=rep(s,"await metaPut('lastVerifiedExport',{jobId,packageSha256,artifactCount:artifactEntries.length,at:now()});return new Blob([compressed],{type:'application/gzip'});","const exportRecord={jobId,packageSha256,artifactCount:artifactEntries.length,at:now()};await metaPut('lastVerifiedExport',exportRecord);await metaPut('lastVerifiedExport:'+jobId,exportRecord);return new Blob([compressed],{type:'application/gzip'});",'backup meta');w('project-store.js',s);

s=r('app-core.js');s=s.replace('const operationSelection={},runSelection={},dismissedProposalIds=new Set();','const operationSelection={},runSelection={};');
s=s.replace("function proposalMarkup(n){const p=safe(current.projectData.responseProposals).filter(x=>x.status==='PENDING_OPERATOR_REVIEW'&&operatorLaneMatches(x,n)).at(-1);if(!p)return '';if(dismissedProposalIds.has(p.proposalId))return `<div class=\"notice\" id=\"dismissed-proposal-notice\"><strong>Proposal hidden for this session.</strong><br>Canonical state changed: NO. The validated proposal and preserved raw response remain unchanged.<div class=\"button-row\"><button id=\"show-discarded-proposal\" type=\"button\">Review preserved proposal</button></div></div>`;const rows=","function proposalMarkup(n){const p=safe(current.projectData.responseProposals).filter(x=>x.status==='PENDING_OPERATOR_REVIEW'&&operatorLaneMatches(x,n)).at(-1);if(!p)return '';const rows=");
s=s.replace('<button id="discard-proposal-view" type="button">Discard from current view</button>','<button id="abandon-proposal" type="button">Discard pending attempt</button>').replace('Discard from current view changes only this browser view. It does not reject, accept, or alter canonical project state.','Discard pending attempt records an auditable abandonment, accepts no proposed canonical work, supersedes the old prompt, and prepares a fresh authoritative prompt.');
const reject="async function rejectPendingProposal(requestCorrection=false){const p=pendingProposal();if(!p)return;const correction=$('#correction-reason')?.value.trim()||'';if(requestCorrection&&!correction){alert('Describe the correction or refinement that is required.');$('#correction-reason')?.focus();return;}const result=ingestion.reject(current,p.proposalId,{operator:$('#operator-label')?.value.trim()||'HUMAN_OPERATOR',reason:requestCorrection?correction:'Rejected after proposal review.',requestCorrection});await persistReplacement(result.project);announce(requestCorrection?'correction requested':'response rejected');render();}";
s=rep(s,reject,reject+"\nasync function abandonPendingProposal(){const p=pendingProposal();if(!p)return;try{const result=ingestion.abandon(current,p.proposalId,{operator:$('#operator-label')?.value.trim()||'HUMAN_OPERATOR'});await persistReplacement(result.project);announce('pending attempt discarded; canonical accepted work changed: no; new prompt required: yes');render();queueMicrotask(()=>$('#generated-prompt')?.focus());}catch(error){alert(`Pending attempt was not discarded: ${error.message||error}`);}}",'abandon app');
const rf=/async function refreshProjectStorage\(\{verify=false\}=\{\}\)\{[\s\S]*?\n\}/;if(!rf.test(s))throw new Error('refresh storage missing');
const refresh=`async function refreshProjectStorage({verify=false}={}){const jobId=String(current?.job?.JOB_ID||'');try{const [health,artifacts,verification,lastBackupRecord]=await Promise.all([projectStore.storageHealth(),projectStore.listArtifacts(jobId),verify?projectStore.verifyProjectArtifacts(jobId):Promise.resolve(null),projectStore.metaGet('lastVerifiedExport:'+jobId)]);projectStorageSnapshot={jobId,artifactCount:artifacts.length,byteSize:artifacts.reduce((sum,row)=>sum+Number(row.byteSize||0),0),persistent:Boolean(health.persistent),usage:health.usage,quota:health.quota,lastBackup:lastBackupRecord?.at||null,verification};if(verification){const openCustodyBlocker=engine.records(current,'blockers',{active:false}).some(record=>record.source==='APPLICATION'&&record.applicationBlockerKind==='ARTIFACT_CUSTODY'&&engine.isActiveRecord(record)&&['OPEN','BLOCKED','IN PROGRESS','UNKNOWN'].includes(String(engine.recordValue(record,'STATUS')||'OPEN').toUpperCase()));if(!verification.verified||openCustodyBlocker){const next=clone(current);engine.reconcileArtifactCustodyVerification(next,{report:verification,stage:Number(current.activeStage||1)});await persistReplacement(next);}announce(verification.verified?'stored files verified':'stored file integrity mismatch; release blocked and delivery authorization revoked');}render();return projectStorageSnapshot;}catch(error){announce('storage verification failed');alert(\`Project storage check failed: \${error.message||error}\`);return null;}}`;
s=s.replace(rf,refresh);
const ar=/async function archiveCurrentProject\(\)\{[\s\S]*?\n\}/;if(!ar.test(s))throw new Error('archive missing');s=s.replace(ar,`async function archiveCurrentProject(){if(current?.isRetainedTestProject){alert('The built-in retained reference project cannot be archived.');return;}const jobId=current.job.JOB_ID,replacement=projects.find(project=>project.job?.JOB_ID!==jobId&&!projectArchived(project));if(!replacement){alert('Create or restore another active project before archiving this one.');return;}const next=clone(current);lifecycleUpdate(next,{archivedAt:new Date().toISOString(),archivedBy:'HUMAN_OPERATOR'});try{await persistReplacement(next);current=replacement;try{await projectStore.metaPut('selectedProject',current.job.JOB_ID);}catch(error){console.error('Selected-project metadata update failed after successful archive',error);}announce(\`project \${jobId} archived\`);render();}catch(error){alert(\`Project was not archived: \${error.message||error}\`);}}`);
s=s.replace("if($('#parse-output'))$('#parse-output').onclick=prepareStageResponse;if($('#clear-response-draft'))$('#clear-response-draft').onclick=clearResponseBox;if($('#replace-agent-attempt'))$('#replace-agent-attempt').onclick=replaceAgentAttempt;if($('#discard-proposal-view'))$('#discard-proposal-view').onclick=()=>{const proposal=pendingProposal();if(proposal){dismissedProposalIds.add(proposal.proposalId);announce('proposal hidden; canonical state changed: no');render();}};if($('#show-discarded-proposal'))$('#show-discarded-proposal').onclick=()=>{const proposal=pendingProposal();if(proposal)dismissedProposalIds.delete(proposal.proposalId);render();};","if($('#parse-output'))$('#parse-output').onclick=prepareStageResponse;if($('#clear-response-draft'))$('#clear-response-draft').onclick=clearResponseBox;if($('#replace-agent-attempt'))$('#replace-agent-attempt').onclick=replaceAgentAttempt;if($('#abandon-proposal'))$('#abandon-proposal').onclick=abandonPendingProposal;");
s=s.replace("current=projects.find(p=>p.job?.JOB_ID===savedSelectedProjectId)||projects[0];","current=projects.find(p=>p.job?.JOB_ID===savedSelectedProjectId&&!projectArchived(p))||projects.find(p=>!projectArchived(p))||projects[0];");
const mode="function interactionModeMarkup(n){const pending=pendingProposal(),latestValidation=safe(current.projectData.responseValidations).filter(x=>Number(x.stage)===n).at(-1),accepted=acceptedLaneChanges(n).at(-1),requests=safe(current.projectData.humanInputRequests).filter(x=>Number(x.stage)===n&&String(x.status||'OPEN').toUpperCase()==='OPEN');if(accepted)return";
s=rep(s,mode,"function interactionModeMarkup(n){const pending=pendingProposal(),latestValidation=safe(current.projectData.responseValidations).filter(x=>Number(x.stage)===n).at(-1),latestDisposition=safe(current.projectData.operationDispositions).filter(x=>Number(x.stage)===n).at(-1),accepted=acceptedLaneChanges(n).at(-1),requests=safe(current.projectData.humanInputRequests).filter(x=>Number(x.stage)===n&&String(x.status||'OPEN').toUpperCase()==='OPEN');if(latestDisposition?.status==='ABANDONED_RESPONSE')return `<div class=\"notice\"><strong>The pending attempt was discarded.</strong><br>Canonical accepted work changed: NO · Audit state changed: YES · New prompt required: YES. Use only the newly generated authoritative prompt; the abandoned response cannot be accepted later.</div>`;if(accepted)return",'abandon mode');w('app-core.js',s);

s=r('verify-browser.mjs');s=s.replace("await evalValue(cdp,`(async()=>{const id=${JSON.stringify(lifecycleJobId)};await closedLoopProjectStore.putArtifact({artifactId:'LIFECYCLE-VERIFY-CURRENT',jobId:id,blob:new Blob(['verified-lifecycle-bytes'],{type:'text/plain'}),filename:'verified-lifecycle-bytes.txt',mediaType:'text/plain'});return true})()`);await click(cdp,'#verify-project-files');","await evalValue(cdp,`(async()=>{const id=${JSON.stringify(lifecycleJobId)};await closedLoopProjectStore.putArtifact({artifactId:'LIFECYCLE-VERIFY-CURRENT',jobId:id,blob:new Blob(['verified-lifecycle-bytes'],{type:'text/plain'}),filename:'verified-lifecycle-bytes.txt',mediaType:'text/plain'});return true})()`);await evalValue(cdp,`[...document.querySelectorAll('#project-management details')].find(d=>d.querySelector('summary')?.innerText.includes('Backup, import & storage')).open=true`);await click(cdp,'#verify-project-files');");w('verify-browser.mjs',s);

s=r('verify-complete.mjs');const cm='console.log(JSON.stringify({';const guards=`const lifecycleApp=fs.readFileSync('app-core.js','utf8'),lifecycleStore=fs.readFileSync('project-store.js','utf8'),lifecycleIngestion=fs.readFileSync('response-ingestion.js','utf8'),lifecycleEngine=fs.readFileSync('workflow-engine.js','utf8');\nassert(lifecycleIngestion.includes('function abandon(project,proposalId')&&lifecycleIngestion.includes("'ABANDONED_BY_OPERATOR'")&&lifecycleIngestion.includes("'ABANDONED_RESPONSE'"),'Pending proposal abandonment is not durable/auditable.');\nassert(!lifecycleIngestion.includes('abandonedResponses')&&!lifecycleApp.includes('dismissedProposalIds')&&lifecycleApp.includes('Discard pending attempt'),'Proposal abandonment introduced a parallel collection or remains session-only.');\nassert(lifecycleStore.includes('MISSING_STORED_BLOB')&&lifecycleStore.includes('CANONICAL_BLOB_IDENTITY_MISMATCH'),'Stored-file verification does not reconcile canonical artifact identity to actual Blob custody.');\nassert(lifecycleEngine.includes('reconcileArtifactCustodyVerification')&&lifecycleEngine.includes("source:'APPLICATION'"),'Artifact custody mismatch is not application-owned.');\nassert(lifecycleStore.includes("lastVerifiedExport:'+jobId"),'Backup status is not tracked per project.');\nassert(lifecycleApp.includes('savedSelectedProjectId&&!projectArchived(p)'),'Archived project can still be restored as the normal selected project on reload.');\n\n`;s=rep(s,cm,guards+cm,'guards');w('verify-complete.mjs',s);
