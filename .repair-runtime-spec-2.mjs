import fs from 'node:fs';
let app=fs.readFileSync('app-core.js','utf8');
let residual=0;
app=app.replace(/(CURRENT_(?:ITERATION|SOURCE_SET_VERSION|RESEARCH_VERSION|REQUIREMENTS_VERSION|TEST_SUITE_VERSION|INSTRUCTION_VERSION|CANDIDATE_ID|BASELINE_ID|PRODUCT_ID|PRODUCT_VERSION|DELIVERY_CANDIDATE_SET_ID|REVIEW_VERSION|RECONCILED_REVIEW_VERSION|RELEASE_ID|HASH_REVIEW_ID|EVIDENCE_CHAIN_VERSION|DELIVERY_ID):[^\n]*?)\|\|(?:'NONE'|'NOT APPLICABLE'|'')/g,(m,prefix)=>{residual++;return `${prefix}||null`;});
if(!residual)throw new Error('Expected at least one residual canonical-pointer sentinel to repair.');
fs.writeFileSync('app-core.js',app);

let prompt=fs.readFileSync('prompt-engine.js','utf8');
const oldScope="function scopeFor(stage,state,overrides={}){const job=state?.job||{},own=(key,fallback)=>Object.prototype.hasOwnProperty.call(overrides,key)?overrides[key]:fallback,iterationId=own('iterationId',job.CURRENT_ITERATION||null),candidate=safe(state?.projectData?.candidateFreezes).filter(x=>x?.active!==false&&!x?.invalidatedBy).filter(x=>!iterationId||String(recordValue(x,'ITERATION_ID')||x?.scope?.iterationId||'')===String(iterationId)).at(-1);return {projectRevision:Number(state?.revision||0),inputVersion:job.CURRENT_INPUT_VERSION||null,sourceSetVersion:job.CURRENT_SOURCE_SET_VERSION||null,requirementsVersion:job.CURRENT_REQUIREMENTS_VERSION||null,testSuiteVersion:job.CURRENT_TEST_SUITE_VERSION||null,instructionVersion:job.CURRENT_INSTRUCTION_VERSION||null,iterationId,candidateId:own('candidateId',candidate?recordId(candidate,'candidateFreezes'):null),runId:own('runId',null),contextId:own('contextId',null),baselineId:own('baselineId',!placeholder(job.CURRENT_BASELINE_ID)?job.CURRENT_BASELINE_ID:null),productId:own('productId',!placeholder(job.CURRENT_PRODUCT_ID)?job.CURRENT_PRODUCT_ID:null)};}";
const newScope="function scopeFor(stage,state,overrides={}){const job=state?.job||{},own=(key,fallback)=>Object.prototype.hasOwnProperty.call(overrides,key)?overrides[key]:fallback,cleanPointer=value=>placeholder(value)?null:value,iterationId=own('iterationId',cleanPointer(job.CURRENT_ITERATION)),candidate=safe(state?.projectData?.candidateFreezes).filter(x=>x?.active!==false&&!x?.invalidatedBy).filter(x=>!iterationId||String(recordValue(x,'ITERATION_ID')||x?.scope?.iterationId||'')===String(iterationId)).at(-1);return {projectRevision:Number(state?.revision||0),inputVersion:cleanPointer(job.CURRENT_INPUT_VERSION),sourceSetVersion:cleanPointer(job.CURRENT_SOURCE_SET_VERSION),researchVersion:cleanPointer(job.CURRENT_RESEARCH_VERSION),requirementsVersion:cleanPointer(job.CURRENT_REQUIREMENTS_VERSION),testSuiteVersion:cleanPointer(job.CURRENT_TEST_SUITE_VERSION),instructionVersion:cleanPointer(job.CURRENT_INSTRUCTION_VERSION),iterationId,candidateId:own('candidateId',candidate?recordId(candidate,'candidateFreezes'):cleanPointer(job.CURRENT_CANDIDATE_ID)),runId:own('runId',null),contextId:own('contextId',null),sourceConvergedIterationId:own('sourceConvergedIterationId',null),confirmationIterationId:own('confirmationIterationId',null),baselineId:own('baselineId',cleanPointer(job.CURRENT_BASELINE_ID)),productId:own('productId',cleanPointer(job.CURRENT_PRODUCT_ID)),productVersion:own('productVersion',cleanPointer(job.CURRENT_PRODUCT_VERSION)),deliveryCandidateSetId:own('deliveryCandidateSetId',cleanPointer(job.CURRENT_DELIVERY_CANDIDATE_SET_ID)),reviewVersion:own('reviewVersion',cleanPointer(job.CURRENT_REVIEW_VERSION)),reconciledReviewVersion:own('reconciledReviewVersion',cleanPointer(job.CURRENT_RECONCILED_REVIEW_VERSION)),releaseId:own('releaseId',cleanPointer(job.CURRENT_RELEASE_ID)),hashReviewId:own('hashReviewId',cleanPointer(job.CURRENT_HASH_REVIEW_ID)),evidenceChainVersion:own('evidenceChainVersion',cleanPointer(job.CURRENT_EVIDENCE_CHAIN_VERSION))};}";
if(!prompt.includes(oldScope))throw new Error('Old prompt scope function not found exactly.');
prompt=prompt.replace(oldScope,newScope);
fs.writeFileSync('prompt-engine.js',prompt);

let ingestion=fs.readFileSync('response-ingestion.js','utf8');
const oldKeys="const RESPONSE_SCOPE_KEYS=Object.freeze(['projectRevision','inputVersion','sourceSetVersion','requirementsVersion','testSuiteVersion','instructionVersion','iterationId','candidateId','runId','contextId','baselineId','productId']);";
const newKeys="const RESPONSE_SCOPE_KEYS=Object.freeze(['projectRevision','inputVersion','sourceSetVersion','researchVersion','requirementsVersion','testSuiteVersion','instructionVersion','iterationId','candidateId','runId','contextId','sourceConvergedIterationId','confirmationIterationId','baselineId','productId','productVersion','deliveryCandidateSetId','reviewVersion','reconciledReviewVersion','releaseId','hashReviewId','evidenceChainVersion']);";
if(!ingestion.includes(oldKeys))throw new Error('Old response scope universe not found exactly.');
ingestion=ingestion.replace(oldKeys,newKeys);
const oldTarget="const PROMPT_TARGET_SCOPE_KEYS=Object.freeze(['iterationId','candidateId','runId','contextId','baselineId','productId']);";
const newTarget="const PROMPT_TARGET_SCOPE_KEYS=Object.freeze(['inputVersion','sourceSetVersion','researchVersion','requirementsVersion','testSuiteVersion','instructionVersion','iterationId','candidateId','runId','contextId','sourceConvergedIterationId','confirmationIterationId','baselineId','productId','productVersion','deliveryCandidateSetId','reviewVersion','reconciledReviewVersion','releaseId','hashReviewId','evidenceChainVersion']);";
if(!ingestion.includes(oldTarget))throw new Error('Old prompt target scope key set not found exactly.');
ingestion=ingestion.replace(oldTarget,newTarget);
fs.writeFileSync('response-ingestion.js',ingestion);
