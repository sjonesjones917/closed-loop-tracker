import fs from 'node:fs';
const read=path=>fs.readFileSync(path,'utf8');
const write=(path,text)=>fs.writeFileSync(path,text);
const replace=(text,from,to,label)=>{if(!text.includes(from))throw new Error(`Missing ${label}`);return text.replace(from,to);};

let s=read('response-ingestion.js');
const answerMarker="function answerHumanInput(project,answers,{operator='HUMAN_OPERATOR'}={}){";
const abandon=`function abandon(project,proposalId,{operator='HUMAN_OPERATOR',reason='Operator abandoned the pending attempt.'}={}){\n  const next=clone(project);workflow.ensureShape(next);const proposal=findProposal(next,proposalId);if(!proposal)throw new Error('Response proposal does not exist.');if(proposal.status!=='PENDING_OPERATOR_REVIEW')throw new Error(\`Response proposal is \${proposal.status}, not pending review.\`);\n  const at=now(),note=String(reason||'Operator abandoned the pending attempt.');proposal.status='ABANDONED_BY_OPERATOR';proposal.reviewedAt=at;proposal.reviewedBy=operator;proposal.reviewNote=note;const validation=findValidation(next,proposal.validationId);if(validation)validation.status='ABANDONED_BY_OPERATOR';const raw=findRaw(next,proposal.rawResponseId);if(raw)raw.status='ABANDONED_BY_OPERATOR';const receipt=findReceipt(next,proposal.receiptId);if(receipt){receipt.completionState='ABANDONED_BY_OPERATOR';receipt.nextRequiredVerificationStage=\`REGENERATE STAGE \${String(proposal.stage).padStart(2,'0')} RESPONSE\`;}\n  const d=disposition(next,'ABANDONED_RESPONSE',{stage:proposal.stage,rawResponseId:proposal.rawResponseId,promptId:proposal.promptId,validationId:proposal.validationId,proposalId,receiptId:proposal.receiptId,details:{operator,reason:note,canonicalAcceptedWorkChanged:false,newPromptRequired:true}});workflow.addHistory(next,'RESPONSE_ATTEMPT_ABANDONED',{stage:proposal.stage,rawResponseId:proposal.rawResponseId,proposalId,dispositionId:d.dispositionId,operator,reason:note,canonicalAcceptedWorkChanged:false,newPromptRequired:true});\n  const controlling=safe(next.projectData.generatedPrompts).find(x=>(x.instructionId||x.promptId)===proposal.promptId&&!x.invalidatedBy);if(controlling)controlling.invalidatedBy=d.dispositionId;const promptEngine=globalThis.closedLoopPromptEngine;if(promptEngine){const preview=clone(next);preview.revision=Number(next.revision||0)+1;const generated=promptEngine.buildPromptRecord(proposal.stage,preview,{operation:proposal.envelope.operation,scope:clone(proposal.envelope.scope||{})});workflow.registerGeneratedPrompt(next,{...generated,generatedAt:now()});}\n  workflow.recalculate(next);return {project:next,disposition:d,receipt};\n}\n\n`;
s=replace(s,answerMarker,abandon+answerMarker,'response abandonment insertion');
s=replace(s,'commit,reject,answerHumanInput','commit,reject,abandon,answerHumanInput','response abandonment export');
write('response-ingestion.js',s);

s=read('workflow-engine.js');
const resolveMarker="function resolveHumanBlocker(project,{blockerId,resolutionEvidence,operatorLabel='HUMAN_OPERATOR'}={}){";
const custody=`function reconcileArtifactCustodyVerification(project,{report,stage=project.activeStage}={}){\n  ensureShape(project);if(!report||typeof report!=='object')throw new Error('An application artifact-verification report is required.');const open=records(project,'blockers',{active:false}).filter(record=>record.source==='APPLICATION'&&record.applicationBlockerKind==='ARTIFACT_CUSTODY'&&isActiveRecord(record)&&['OPEN','BLOCKED','IN PROGRESS','UNKNOWN'].includes(upper(recordValue(record,'STATUS')||'OPEN')));\n  if(report.verified){for(const blocker of open){const updates={STATUS:'RESOLVED',RESOLUTION_EVIDENCE:\`Application re-read \${report.artifactCount} stored Blob(s); all required canonical byte identities reconciled at \${report.at||now()}.\`,CLOSURE:'CLOSED',REEVALUATION:'COMPLETED',REQUIRED_REVALIDATION:'RECALCULATED'};blocker.fields=blocker.fields&&typeof blocker.fields==='object'?blocker.fields:{};for(const [key,value] of Object.entries(updates)){blocker.fields[key]=value;blocker[key]=value;}blocker.updatedAt=now();blocker.resolvedBy='APPLICATION';refreshRecordHashes(blocker,'blockers');addHistory(project,'APPLICATION_ARTIFACT_CUSTODY_RESTORED',{recordId:recordId(blocker,'blockers'),verificationAt:report.at||now()});}if(open.length)recalculate(project);return {verified:true,resolvedBlockerIds:open.map(record=>recordId(record,'blockers'))};}\n  const failures=safe(report.artifacts).filter(item=>!item.verified),artifactIds=failures.map(item=>item.artifactId).filter(Boolean),reason=\`Stored artifact custody verification failed: \${failures.map(item=>\`\${item.artifactId||'UNKNOWN'} (\${item.issue||'byte/hash/metadata mismatch'})\`).join(', ')||'required canonical bytes are missing or mismatched'}.\`;let blocker=open[0];if(!blocker){blocker=commandRecord(project,'blockers',{MISSING_ITEM_TYPE:'ARTIFACT_CUSTODY',MISSING_FACT_INPUT_AUTHORITY_EVIDENCE_CAPABILITY_DECISION_RULE:reason,AFFECTED_REQUIREMENTS:'UNKNOWN',AFFECTED_TESTS:'UNKNOWN',AFFECTED_ARTIFACTS:artifactIds.length?artifactIds:'UNKNOWN',WHY_WORK_CANNOT_CONTINUE:reason,ATTEMPTED_RESOLUTIONS:'APPLICATION BYTE RE-READ AND SHA-256 VERIFICATION',DOWNSTREAM_WORK_STOPPED:'RELEASE AND DEPENDENT WORK',OWNER:'APPLICATION',STATUS:'OPEN',RESOLUTION_EVIDENCE:'NONE',CLOSURE:'OPEN',REEVALUATION:'REQUIRED',REQUIRED_REVALIDATION:'REQUIRED'},{stage,source:'APPLICATION'});blocker.applicationBlockerKind='ARTIFACT_CUSTODY';refreshRecordHashes(blocker,'blockers');addHistory(project,'APPLICATION_ARTIFACT_CUSTODY_BLOCKED',{recordId:recordId(blocker,'blockers'),artifactIds,verificationAt:report.at||now()});}\n  project.release.authorization='NOT AUTHORIZED';project.release.authorizedArtifactIds=[];for(const collection of ['artifactIdentities','evidenceChains','releaseRecords'])for(const record of records(project,collection,{active:false}))if(isActiveRecord(record)){record.active=false;record.validity='INVALIDATED';record.invalidatedBy=recordId(blocker,'blockers');refreshRecordHashes(record,collection);}recalculate(project);return {verified:false,blockerId:recordId(blocker,'blockers')};\n}\n`;
s=replace(s,resolveMarker,custody+resolveMarker,'artifact custody adjudicator');
s=replace(s,'registerGeneratedPrompt,createHumanBlocker,resolveHumanBlocker','registerGeneratedPrompt,createHumanBlocker,reconcileArtifactCustodyVerification,resolveHumanBlocker','artifact custody export');
write('workflow-engine.js',s);

s=read('project-store.js');
const listMarker="async function listArtifacts(jobId){const tx=await openTransaction(ARTIFACTS,'readonly'),rows=await request(tx.objectStore(ARTIFACTS).getAll());await complete(tx);return rows.filter(r=>r.jobId===String(jobId));}\n";
const verify=`async function verifyProjectArtifacts(jobId){\n  const id=String(jobId||'').trim();if(!id)throw storageError('JOB_ID is required for artifact verification.','ARTIFACT_VERIFY_JOB_ID_REQUIRED');const projects=await readAll(),project=projects.find(item=>projectIdentity(item)===id);if(!project)throw storageError(\`Project \${id} is not stored.\`,'ARTIFACT_VERIFY_PROJECT_MISSING');const rows=await listArtifacts(id),rowById=new Map(rows.map(row=>[String(row.artifactId),row])),engine=globalThis.closedLoopWorkflowEngine,expected=engine.records(project,'artifacts').filter(record=>engine.isActiveRecord(record)&&String(engine.recordValue(record,'AVAILABILITY')||'').toUpperCase()==='BYTES_PERSISTED_AND_VERIFIED'),expectedById=new Map(expected.map(record=>[engine.recordId(record,'artifacts'),record])),results=[];let byteSize=0;\n  for(const record of expected){const artifactId=engine.recordId(record,'artifacts'),row=rowById.get(artifactId),canonicalFilename=String(engine.recordValue(record,'FILENAME')||''),canonicalSize=Number(engine.recordValue(record,'BYTE_SIZE')),canonicalSha256=String(engine.recordValue(record,'SHA256')||'');if(!row){results.push({artifactId,filename:canonicalFilename,storedByteSize:null,actualByteSize:null,storedSha256:null,actualSha256:null,verified:false,issue:'MISSING_STORED_BLOB'});continue;}const bytes=new Uint8Array(await row.blob.arrayBuffer()),actualSize=bytes.byteLength,actualSha256=await hash.sha256Bytes(bytes);byteSize+=actualSize;const verified=String(row.jobId)===id&&String(row.filename)===canonicalFilename&&Number(row.byteSize)===canonicalSize&&String(row.sha256)===canonicalSha256&&actualSize===canonicalSize&&actualSha256===canonicalSha256;results.push({artifactId,filename:canonicalFilename,storedByteSize:Number(row.byteSize),actualByteSize:actualSize,storedSha256:String(row.sha256||''),actualSha256,verified,issue:verified?null:'CANONICAL_BLOB_IDENTITY_MISMATCH'});}\n  for(const row of rows)if(!expectedById.has(String(row.artifactId))){const bytes=new Uint8Array(await row.blob.arrayBuffer()),actualSize=bytes.byteLength,actualSha256=await hash.sha256Bytes(bytes);byteSize+=actualSize;const verified=actualSize===Number(row.byteSize)&&actualSha256===String(row.sha256);results.push({artifactId:String(row.artifactId),filename:String(row.filename||row.artifactId),storedByteSize:Number(row.byteSize),actualByteSize:actualSize,storedSha256:String(row.sha256||''),actualSha256,verified,issue:verified?null:'UNREFERENCED_STORED_BLOB_MISMATCH',canonicalExpectation:false});}\n  const mismatches=results.filter(item=>!item.verified),report={jobId:id,artifactCount:rows.length,expectedCanonicalArtifactCount:expected.length,byteSize,verifiedCount:results.length-mismatches.length,mismatchCount:mismatches.length,verified:mismatches.length===0,artifacts:results,at:now()};await metaPut('lastArtifactVerification:'+id,{jobId:id,artifactCount:report.artifactCount,expectedCanonicalArtifactCount:report.expectedCanonicalArtifactCount,byteSize:report.byteSize,mismatchCount:report.mismatchCount,verified:report.verified,at:report.at});return report;\n}\n`;
s=replace(s,listMarker,listMarker+verify,'project artifact verifier');
s=replace(s,"await metaPut('lastVerifiedExport',{jobId,packageSha256,artifactCount:artifactEntries.length,at:now()});return new Blob([compressed],{type:'application/gzip'});","const exportRecord={jobId,packageSha256,artifactCount:artifactEntries.length,at:now()};await metaPut('lastVerifiedExport',exportRecord);await metaPut('lastVerifiedExport:'+jobId,exportRecord);return new Blob([compressed],{type:'application/gzip'});",'per-project backup metadata');
s=replace(s,"const lastCommitted=await request(meta.get('lastCommittedRevision'));if(String(lastCommitted?.value?.jobId||'')===jobId)meta.delete('lastCommittedRevision');","const lastCommitted=await request(meta.get('lastCommittedRevision'));if(String(lastCommitted?.value?.jobId||'')===jobId)meta.delete('lastCommittedRevision');for(const key of ['lastVerifiedExport:'+jobId,'lastArtifactVerification:'+jobId])meta.delete(key);",'project lifecycle meta cleanup');
s=replace(s,'putArtifact,getArtifact,deleteArtifact,listArtifacts,exportPackage','putArtifact,getArtifact,deleteArtifact,listArtifacts,verifyProjectArtifacts,exportPackage','project verifier export');
write('project-store.js',s);

s=read('app-core.js');
s=replace(s,"let core,schema,engine,ingestion,projectStore,projects=[],current,projectUi={},projectStorage={artifactCount:0,byteSize:0,integrity:'NOT CHECKED',lastVerifiedAt:null,mismatches:[]};","let core,schema,engine,ingestion,projectStore,projects=[],current,projectUi={},projectStorage={artifactCount:0,byteSize:0,integrity:'NOT CHECKED',lastVerifiedAt:null,lastBackup:null,mismatches:[]};",'project storage state');
const refresh=/async function refreshProjectStorage\(\{verify=false\}=\{\}\)\{[\s\S]*?\n\}/;
if(!refresh.test(s))throw new Error('Missing refreshProjectStorage');
const refreshImpl=`async function refreshProjectStorage({verify=false}={}){\n  const jobId=String(current?.job?.JOB_ID||'');if(!jobId)return projectStorage;const [artifacts,lastBackupRecord]=await Promise.all([projectStore.listArtifacts(jobId),projectStore.metaGet('lastVerifiedExport:'+jobId)]);let verification=null;\n  if(verify){verification=await projectStore.verifyProjectArtifacts(jobId);const next=clone(current),reconciliation=engine.reconcileArtifactCustodyVerification(next,{report:verification,stage:Number(current.activeStage||1)});if(!verification.verified||safe(reconciliation.resolvedBlockerIds).length)await persistReplacement(next);const prior=projectUiEntry(jobId);projectUi[jobId]={...prior,lastIntegrityVerification:verification.at,lastIntegrityStatus:verification.verified?'VERIFIED':'FAILED'};await saveProjectUi();}\n  const prior=projectUiEntry(jobId);projectStorage={artifactCount:artifacts.length,byteSize:artifacts.reduce((sum,row)=>sum+Number(row.byteSize||0),0),integrity:verification?(verification.verified?'VERIFIED':'FAILED'):'NOT CHECKED',lastVerifiedAt:prior.lastIntegrityVerification||null,lastBackup:lastBackupRecord?.at||null,mismatches:verification?safe(verification.artifacts).filter(item=>!item.verified).map(item=>item.artifactId):[]};return projectStorage;\n}`;
s=s.replace(refresh,refreshImpl);
s=replace(s,"function pendingProposal(){const dismissed=new Set(safe(projectUiEntry(current?.job?.JOB_ID).dismissedProposalIds));return safe(current.projectData.responseProposals).filter(x=>x.status==='PENDING_OPERATOR_REVIEW'&&!dismissed.has(x.proposalId)&&operatorLaneMatches(x,current.activeStage)).at(-1);}","function pendingProposal(){return safe(current.projectData.responseProposals).filter(x=>x.status==='PENDING_OPERATOR_REVIEW'&&operatorLaneMatches(x,current.activeStage)).at(-1);}",'pending proposal authority');
const discard=/async function discardCurrentAttempt\(\)\{[\s\S]*?\n\}/;
if(!discard.test(s))throw new Error('Missing discardCurrentAttempt');
const discardImpl=`async function discardCurrentAttempt(){const p=pendingProposal(),node=$('#stage-output');if(node)node.value='';if(!p){announce('unsaved attempt cleared; canonical state changed: NO');render();return;}try{const result=ingestion.abandon(current,p.proposalId,{operator:$('#operator-label')?.value.trim()||'HUMAN_OPERATOR'});await persistReplacement(result.project);announce('pending attempt discarded; canonical accepted work changed: NO; audit state changed: YES; new prompt required: YES');render();queueMicrotask(()=>$('#generated-prompt')?.focus());}catch(error){announce('pending attempt was not discarded');alert(\`Pending attempt was not discarded: \${error.message||error}\`);}}`;
s=s.replace(discard,discardImpl);
s=replace(s,"const archived=projects.filter(project=>projectIsArchived(project)),lastEvent=safe(current.projectData.history).at(-1),health=globalThis.closedLoopStorageHealth||{},lastBackup=health.lastVerifiedExport?.jobId===jobId?health.lastVerifiedExport:null;","const archived=projects.filter(project=>projectIsArchived(project)),lastEvent=safe(current.projectData.history).at(-1),health=globalThis.closedLoopStorageHealth||{},lastBackup=projectStorage.lastBackup?{at:projectStorage.lastBackup}:null;",'per-project backup display');
s=replace(s,'Display-name, archive, and dismissed-attempt preferences are browser-local management metadata','Display-name and archive preferences are browser-local management metadata','project management explanation');
s=replace(s,"setTimeout(()=>URL.revokeObjectURL(a.href),0);globalThis.closedLoopStorageHealth=await projectStore.storageHealth();announce(kind==='backup'?'complete project backup exported':'complete project package exported');render();","setTimeout(()=>URL.revokeObjectURL(a.href),0);globalThis.closedLoopStorageHealth=await projectStore.storageHealth();await refreshProjectStorage();announce(kind==='backup'?'complete project backup exported':'complete project package exported');render();",'backup state refresh');
const modePrefix="function interactionModeMarkup(n){const pending=pendingProposal(),latestValidation=safe(current.projectData.responseValidations).filter(x=>Number(x.stage)===n).at(-1),accepted=acceptedLaneChanges(n).at(-1),requests=safe(current.projectData.humanInputRequests).filter(x=>Number(x.stage)===n&&String(x.status||'OPEN').toUpperCase()==='OPEN');if(accepted)return";
const modeNew="function interactionModeMarkup(n){const pending=pendingProposal(),latestValidation=safe(current.projectData.responseValidations).filter(x=>Number(x.stage)===n).at(-1),accepted=acceptedLaneChanges(n).at(-1),requests=safe(current.projectData.humanInputRequests).filter(x=>Number(x.stage)===n&&String(x.status||'OPEN').toUpperCase()==='OPEN'),abandoned=safe(current.projectData.responseDispositions).filter(x=>Number(x.stage)===n&&x.type==='ABANDONED_RESPONSE').at(-1);if(accepted)return";
s=replace(s,modePrefix,modeNew,'interaction mode abandonment data');
s=replace(s,"if(latestValidation&&!latestValidation.valid)return `<div class=\"notice danger\"><strong>Return a corrected final JSON.</strong><br>Canonical state changed: NO. The rejected/stale response does not control current state. Save and use the replacement current instruction.</div>`;if(requests.length||n===1&&!current.stages[1].responseDraft)return","if(latestValidation&&!latestValidation.valid)return `<div class=\"notice danger\"><strong>Return a corrected final JSON.</strong><br>Canonical state changed: NO. The rejected/stale response does not control current state. Save and use the replacement current instruction.</div>`;if(abandoned)return `<div class=\"notice\"><strong>The pending attempt was discarded.</strong><br>Canonical accepted work changed: NO · Audit state changed: YES · New prompt required: YES. Use only the newly generated authoritative prompt; the abandoned response cannot be accepted later.</div>`;if(requests.length||n===1&&!current.stages[1].responseDraft)return",'interaction mode abandonment state');
write('app-core.js',s);

s=read('verify-project-lifecycle.mjs');
s=s.replace("const app=fs.readFileSync('app-core.js','utf8'),store=fs.readFileSync('project-store.js','utf8'),html=fs.readFileSync('index.html','utf8');","const app=fs.readFileSync('app-core.js','utf8'),store=fs.readFileSync('project-store.js','utf8'),ingestion=fs.readFileSync('response-ingestion.js','utf8'),engine=fs.readFileSync('workflow-engine.js','utf8'),pages=fs.readFileSync('.github/workflows/pages.yml','utf8'),html=fs.readFileSync('index.html','utf8');");
s=s.replace("assert(app.includes('dismissedProposalIds')&&app.includes('canonical state changed: NO'),'Discarded attempts must remain non-canonical UI lifecycle state.');","assert(!app.includes('dismissedProposalIds')&&ingestion.includes('function abandon(project,proposalId')&&ingestion.includes(\"'ABANDONED_RESPONSE'\")&&app.includes('canonical accepted work changed: NO'),'Discarded pending attempts must be auditable without changing accepted canonical work.');\nassert(store.includes('verifyProjectArtifacts')&&store.includes('MISSING_STORED_BLOB')&&store.includes('CANONICAL_BLOB_IDENTITY_MISMATCH'),'Stored-file verification must reconcile canonical artifact identities to actual Blob custody.');\nassert(engine.includes('reconcileArtifactCustodyVerification')&&engine.includes(\"source:'APPLICATION'\")&&engine.includes(\"project.release.authorization='NOT AUTHORIZED'\"),'Artifact custody failure must be application-owned and revoke release authorization.');\nassert(app.includes(\"integrity:verification?(verification.verified?'VERIFIED':'FAILED'):'NOT CHECKED'\"),'Historical verification must never display current VERIFIED state without a current byte re-read.');\nassert(store.includes(\"lastVerifiedExport:'+jobId\")&&app.includes('projectStorage.lastBackup'),'Backup status must remain project-specific.');\nfor(const token of ['currentScopeSelectorCoverage','exactReqRunTestCoverage','applicableCurrentRegressionSuccess','mandatoryEvidenceChainCoverage','releaseArtifactIdentityCoverage','favorableAgentVerdictsOverridingContradictoryObservations','externallySupportedUnestablishedIndependenceTreatedAsProven'])assert(pages.includes(token),`Acceptance reduction lost required invariant ${token}.`);");
s=s.replace("console.log(JSON.stringify({projectLifecycleControls:true,compactHeader:true,dangerHiddenByDefault:true,transactionalDeleteRetained:true,lifecycleMetadataDeleteAtomic:true,unsafeOverrides:0}));","console.log(JSON.stringify({projectLifecycleControls:true,compactHeader:true,dangerHiddenByDefault:true,transactionalDeleteRetained:true,lifecycleMetadataDeleteAtomic:true,durableAttemptAbandonment:true,canonicalBlobReverification:true,applicationCustodyBlocking:true,perProjectBackupState:true,zeroLossAcceptanceReduction:true,unsafeOverrides:0}));");
write('verify-project-lifecycle.mjs',s);
