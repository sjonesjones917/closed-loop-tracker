# Accumulated-data defect burn-down

## Continuation from deployed main 5966caba (2026-09-12)

The runtime recovery repair in PR #1217 is already merged at `5966caba47a43d1b55b88d7db8ddaeabee1392ca`. Existing CI run `34656413112` passed its four jobs. The older sections below are the retained investigation history, not new claims of unfinished patches. The newly attached CLRT-SPEC(20260911-234845).txt has the same substantive controlling text (introductory label and whitespace differ); the repository normative source remains unchanged.

PR #1218 continues the same production/CI line. RED commit `72be8aaabd00faed6ed2d5ce26db0d5ca1a56d33`, CI run `34659881720`, job `103459980018`: the real export encoder allocated an entire 196,607-byte artifact buffer. A separate local production hash regression read 2,097,153 bytes at once. These are measured allocations, not a claim to have reproduced an iPhone process termination in Chromium.

| ID | Reproduction | Earliest owner and repair | Affected stages / paths | Regression / acceptance |
|---|---|---|---|---|
| DP-33 | Whole-Blob hashing and its callers allocate the complete selected/stored file; the hash regression measured a 2,097,153-byte read. | `hash.js:sha256Bytes`: incremental reads of at most 65,536 bytes, using the existing SHA-256 implementation; callers pass immutable Blobs. Hash loops yield between bounded batches. | File intake, stored read-back, response staging and returned-file re-verification at all external stages; context download; custody checks; artifact download; Stage 28 audited/delivery identity. Native Test IR input buffers and response parsing still have their declared full-input contracts. | `verify-hash.mjs`: independent node:crypto across block/file boundaries. Existing ingestion/lifecycle tests. `verify-mobile-stage-action.mjs`: real IndexedDB custody, verification, staging, download. |
| DP-34 | Complete export reads each artifact whole and retains all base64 strings. Execution packages also read whole context files into strings. RED CI fails at the export allocation. | `project-store.js:packageJsonChunks/exportPackage/createExecutionPackage/compressJson`: lazy file members, bounded base64 and UTF-8 decoding/escaping, sequential canonical hashing and compression with backpressure. Normal values still use the existing canonical serializer. No package/schema/digest change. | Complete export/backup at all 30 stages; every applicable execution-package handoff and prompt context member. Read-side project hashing yields without changing synchronous transaction integrity checks. | `verify-project-lifecycle.mjs`: both actual package encoders, independent crypto oracle, all base64 remainder sizes, UTF-8/escape boundaries, exact tails, damaged-file rejection. Existing local/deployed browser suite retains 48 MB/600-response export and adds noncompressible file export. |
| DP-35 | Files view creates all 600 download controls; proposal review resolves every one of 600 changes while its details remain closed. | `app-core.js:files/details/renderDetail/wireDetails/proposalMarkup`: 20-file pages, exact download binding after page changes, and lazy row projection at disclosure. | Global Files view; response proposal review at all external operations, including Stage 01 extraction summary. | Existing lifecycle verifier executes production view functions, checks all 600 IDs including the final page, and counts deferred lookup work. Existing mobile verifier clicks first/last file pages and downloads the exact final artifact. |
| DP-36 | Stage 15 and Stage 29 views calculate 600 regression histories and 600 evidence explanations before disclosure (local RED observed). | The same `details` projection mechanism; `regressionLifecycleMarkup/evidenceExplanationMarkup` defer calculation to the visible page. | Stage 15 permanent regression history; Stage 29 proposition support; shared record disclosure used throughout all 30 views. | Existing lifecycle regression counts production calculation calls. All-stage Chromium pressure proof remains required. |
| DP-37 | Restore materializes the entire compressed file, an expanded byte array, complete base64-decoded strings, and copied artifact entries retaining base64. | `project-store.js:importPackage/base64ToBlob`: decompress directly to the existing JSON parser, decode base64 in bounded chunks, omit redundant base64 from verified Blob metadata, yield while checking package hash. | Complete backup restore/import at every stage; preserved responses, prompts, canonical state, artifact custody. | Existing lifecycle decoder checks exact bytes, whitespace, remainder/padding and invalid encodings. Existing mobile browser test restores the real exported package with whole-file/base64 read guards and checks all 22 stored artifacts. |

The complete restored JSON object/text and canonical project transactions are still finite-memory operations. This repair removes the reproduced redundant file buffers; it does not claim unlimited project size, zero device computation, or physical-iPhone acceptance. Browser-local static operation and no backend synchronization remain the controlling architecture (Sections 5 and 11). A worker alone would still execute on the phone.

The specification explicitly separates proposal commit authorization (17.12) from stage completion conditions (37), and requires ACCEPTED/REJECTED/BLOCKED precedence (1). A received/accepted proposal cannot establish an independent review or missing evidence merely by being saved. The operator must receive the one calculated next action; the prior shared-gate and response-state regressions remain mandatory. No new manual completion step or status authority is added here.

Local checks: new hash, both package formats, staged file handling, deferred views and ingestion pass. The existing pre-browser CI blocks passed through migration and Stage 01 accounting; the Stage 04 block reached its sequential Chromium subcheck, which is unavailable in this workspace. Browser installation timed out. The unmodified GitHub Pages workflow remains the required full-CI/local-Chromium/deployment/exact-byte/deployed-Chromium authority. Exact final run identities are recorded on PR #1218 after execution.

Final acceptance remains open for actual physical-iPhone Safari, approved visual-baseline authority, the separately recorded proof-expression conformance gap, and a real externally executed complete project. The prior revision-89 project mentioned below is not attached in this conversation; no exact replay of that dataset is claimed here.


## Current investigation: actual project revision 89

Baseline: `4518783fc4ef8b27a65792f81ae76ec0d296697f`. There is no unfinished runtime patch on this baseline. The supplied complete project package has SHA-256 `20f41dc44694d8a0e1c643f4557ba53cea4fc8746f223c85c622afdc3c7c194a`; the package, embedded project and all eight stored files pass their recorded digests. Recalculation through the production modules reproduces Stage 06 BLOCKED with 90/91 coverage, 191 missing-artifact findings and four invalid timing contracts. The latest accepted response contains its evidence inline and declares zero attachments. User project contents must not be committed to this repository.

### Shared mechanism and stage inventory

| Mechanism | Production owners / consumers | Direct stages | Downstream and related scope |
|---|---|---|---|
| Optional attachment references | `response-ingestion.js:planProposal`; `workflow-engine.js:testExecutionPlan/evaluateEvidenceContract/evaluateEvidenceSufficiency`; canonical storage validation | Evidence intake in every external operation; test design 06; mutation validation 07; verification 12, 17, 19, 22–24; regression 15, 17, 19 | Result adjudication, evidence chains 29, release/terminal 27–30. Preserve missing real-reference rejection; sentinel text is not an artifact identity. |
| Test scheduling and handoff selection | `testDueState`, `verificationMatrix`, `finalProductTestSelection`, `executionHandoff`; `app-core.js:stagePlanItems/testExecutionGuidanceMarkup` | Design 06; failure tests 07; run verification 12/17/19; regression 15/17/19; final verification 22–24 | Comparison 13, convergence 18, representation 25, release 27 and delivery/evidence/registry 28–30 consume results or scheduled obligations. Deferred does not mean executed or satisfied. |
| Requirement applicability and coverage | `mandatoryRequirements`, `deriveMandatoryTestCoverage`, `coverageMetrics`, `evaluateApplicability`, proof obligations | Authored 04, resolved/reviewed 05, covered 06/07/08 | Matrices 12/13/17/18/19, final verification 22–25, audits/release/evidence/terminal 26–30. A selected semantic proposal is not automatically an independently accepted applicability determination; UNKNOWN must not silently remove scope. |
| Test timing validation | `response-ingestion.js:validateEnvelope`, `workflow-engine.js:testDueState`, `prompt-engine.js` contract and recovery context | 06 COMPLETE and RECONCILE_VERIFICATION_SUITE (the only current operations allowed to write tests) | All consumers of the accepted suite. Reject contradictory timing at intake; preserve an actionable path for already accepted legacy records. |
| Acceptance, completion and correction | Ingestion `prepareCaptured/commit/createReplacementPrompt`; engine `recalculate/operationalNextAction`; prompt `recoveryFeedback/contextFor`; app `interactionModeMarkup` | Shared across all 30 views / all external operations | The specification separates proposal storage (17.12) from the Stage 37 completion predicate. This internal distinction must not produce conflicting guidance or extra user validation steps. Keep human decisions and application control steps with their owning executor; corrections must contain current findings and must not resurrect older rejected output. |
| Current response attempt / selected-file view | `app-core.js:pendingReturnedResponse/returnedFileSlotsMarkup/interactionModeMarkup`, saved prompt/reservation scope | All 30 stage views and every external operation | Latest relevant attempt, operation/scope isolation, reload, duplicate selection, correction export, acceptance; historical failed attempts remain auditable but must not demand files for the current accepted attempt. |
| Backward version dependencies in prompts | `prompt-engine.js:scopeFor`; reservation, ingestion and UI scope consumers | All external operations at Stages 01–20 before their later source/requirement/suite/instruction/iteration/baseline/product owners | Returning upstream after later work must retain relevant current inputs without acquiring a dependency on downstream work that will be rebuilt. |
| Accumulated data and state replay | `hash.js`, `project-store.js`, prompt context materialization and export, app detail/preview rendering | All 30 stages, all external operations, project export/import | Existing bounded text/record views, scrolling buttons, serialized exports, exact bytes and 48 MB browser pressure checks remain required. No backend or visual redesign is part of this repair. |

| ID | Reproduction / remaining investigation | Regression owner | Status |
|---|---|---|---|
| DP-24 | Every accepted test in revision 89 resolves its inline evidence's `ATTACHMENT_ID: UNKNOWN` as a missing real artifact. Related evidence consumers normalize this sentinel inconsistently. | Existing complete/ingestion suites, browser acceptance | LOCAL GREEN: seven absent-reference sentinels normalized; real-reference negatives retained |
| DP-25 | Stage 06 displays TEST-000001 as needing execution inputs even though its earliest/required stage is 12. Generic per-run handoff/guidance also selects future product tests. | Existing due-stage/final-timing/complete verifiers | LOCAL GREEN: due-stage handoffs shared by engine and UI; Stage 06 does not request future execution inputs |
| DP-26 | Four accepted tests set FINAL_PRODUCT_REQUIRED for delivery-only phases. Field-wise intake validation accepts the combination; completion validation rejects it. | Existing ingestion and final-timing verifiers | LOCAL GREEN: contradictory timing rejected before proposal; same findings exported in correction |
| DP-27 | Coverage counts a Stage-04-only requirement whose semantic proposal selects NOT_APPLICABLE. Its independent semantic review is absent; subtracting it unconditionally would bypass authority. Coverage also counts non-ready tests while the dedicated derivation excludes them. | Existing complete/semantic/due-stage verifiers | LOCAL GREEN: effective reviewed applicability only; self/stale/unreviewed negatives; ready-test denominator consistent |
| DP-28 | After acceptance the actual stage remains BLOCKED but NEXT_REQUIRED_ACTION is SELECT_RESPONSE_JSON_FILE and newPromptRequired is false. The saved accepted response ends validation-failure feedback, leaving completion failures outside the correction handoff. | Existing prompt/ingestion/operator/browser verifiers | LOCAL GREEN: existing Stage 06 reconciliation selected; current gate and record-level timing/coverage findings transported |
| DP-29 | The old failed response's design-file slot remains visible after a newer zero-attachment response is accepted. An older acceptance can also mask a newer pending/failed response. | Existing file-first operator and Chromium verifiers, all-stage views | LOCAL GREEN: latest current attempt controls pending files, receipt and validation report; 30-stage regression |
| DP-30 | Public gate evaluation and the gate used by recalculation can return different reasons for the same project. The controlling-completion layer adds applicability/proof requirements outside the base recalculation closure. | Existing full-cycle/semantic/store verifiers | LOCAL GREEN: saved status uses the authoritative gate; missing Stage 05 review selects the existing review operation |
| DP-31 | Reopening an earlier-stage review after later work exists binds its prompt to downstream suite/instruction/iteration/product versions. Rebuilding that downstream work can stale its own upstream review. | `prompt-engine.js:scopeFor`; all-stage prompt and full-cycle verifiers | RED through real Stage 05 prompt after accepted Stage 06; LOCAL GREEN: stage-owned version boundaries checked in every external operation; Stage 05 review followed by Stage 06 rebuild and all 30 stages complete |
| DP-32 | The current next-action button displayed while inspecting an older stage exports using activeStage instead of its owning current stage (Stage 05 SEMANTIC_REVIEW was sent to Stage 04). | Existing `app-core.js:wire` next-instruction handler and `verify-file-first-operator.mjs` | RED then LOCAL GREEN: existing button targets the canonical current stage; review, reconciliation, run, verification and product lanes covered |

Local validation on the repair branch: `verify-ingestion.mjs`, `verify-complete.mjs` (including the all-30-stage full cycle), and `verify-file-first-operator.mjs` pass. The failing baseline is committed at `ba63d0d25be60ff14af15cacb7f82d162b2e8865`; additional correction/coverage regressions were also observed failing before their production repairs. Restoring the derived-data assignment exposed wall-clock fallbacks in Stage 16 DATE and Stage 27 DATE_AND_TIME. Those now remain empty until an owning record supplies the event time; save/reload no longer manufactures a different timestamp. Existing full-cycle persistence assertions cover this failure.

Actual-package replay: all 191 tests now have zero phantom UNKNOWN artifact references; all 25 raw responses, seven accepted changes and eight stored files are retained. The missing Stage 05 review is correctly blocking and its instruction (58,087 bytes) plus bounded context file (3,866,164 bytes) are generated with automatic author/reviewer references. The four invalid delivery timing records remain visible as correction findings; no semantic result is fabricated.

Pending: exact final CI/local Chromium/deployed-byte/deployed Chromium proof. Actual physical-iPhone Safari proof and approved visual-baseline authority remain open; no automated desktop result substitutes for them.


The earlier notes saying the complete project was unavailable are historical. This package now enables exact state replay. It does not supply physical-iPhone acceptance or approved visual-baseline authority; those remain bound to the eventual final deployment.

Scope: all 30 workflow stages, all 50 external-agent operations, and shared data views, export, persistence, validation/recovery, and handoff. Stage 03 was the first reported expansion failure. Stage 04 is a reported handoff failure, not the scope boundary.

Governing source: `specification/closed-loop-reliability-controlling-implementation-specification.txt`, SHA-256 `6ffd7b3ef6c141754d4381c43c33767c3d8f265833f06a7fccb7518bba818bd9`. The attached 20260910-211200 specification has identical substantive text, with only an introductory “The one” and whitespace differences. Starting main: `feb1303c837b7d4399930f779b63c8bb0c34a0e4`. Existing CI: `.github/workflows/pages.yml`.

Each row follows reproduction → failing regression → responsible-layer repair → passing regression → affected integration → full CI → browser → merge. A local pass does not close final acceptance.

| ID | Reproduced behavior / evidence | Earliest responsible production layer and repair | Permanent regression | Remaining proof |
|---|---|---|---|---|
| DP-01 | Stage 04 pressure fixture produced a 1,258,708-byte instruction on starting main. CI run 34518586684 failed the existing Stage 04 pressure assertion. | `prompt-engine.js`: centralized PROMPT_INLINE_LIMITS/1; operation-authorized oversized data moves into digest-bound context.json. Large duplicate context snapshots are compacted in saved prompt metadata. No transferred content is truncated. | `verify-user-prompt-invariants.mjs`; `verify-all-stage-prompts.mjs` now exercises every one of 50 external operations, UTF-8 bytes, untrusted delimiter-shaped content, tail preservation, and canonical immutability. | Exact-final CI, deployed handoff and actual-iPhone replay. |
| DP-02 | Every external operation lacked manifest-bound transport for accumulated corrections. RED-only commit 0b615f33ae392ac87acaf1a3f55fb27bdec8edd2; CI run 34531507325 failed. | Same shared prompt owner; `project-store.js` persists context as verified artifact Blobs on project writes, including automatic replacement prompts; `app-core.js` exports exact saved files and serializes concurrent export requests. | All-operation regression plus `verify-browser.mjs` Stage 03 real manifest/instruction/context buttons, reload, exact bytes, and complete backup. | CI browser, deployed browser and physical device. |
| DP-03 | `details()` serialized every raw response, prompt, proposal and nested record even while its panel was closed. | `app-core.js`: deferred record disclosure, 20-record pages, 4,096-character text pages and complete-text download; provenance is deferred/paged; validation details are deferred. References are released when closed or replaced. | `verify-mobile-stage-action.mjs`: at least 48 MB retained history; collapsed and opened DOM bounds; all 30 stage views. | Final browser run and physical memory-pressure acceptance. |
| DP-04 | Expanded instructions used height:auto / max-height:none. Prior PRs restricted the problem to Stage 04. | Shared `.expandable-prompt.expanded` remains internally scrollable and viewport-bounded at every stage. Large preserved instruction previews are bounded separately from authoritative export bytes. | Existing mobile verifier now expands, scrolls and collapses across all 30 stage views. Existing collapsed-dimension assertions are retained; only the defect-causing expanded rule changed. | Final browser run and approved visual-baseline comparison. |
| DP-05 | Hash regression observed one 1,921,475-character UTF-8 allocation from accumulated state before repair. | `hash.js`: the existing canonical serializer and SHA-256 implementation process bounded chunks. Canonicalization identity and all digest values remain unchanged. `project-store.js` no longer deep-clones the complete project merely to omit its root digest. | `verify-hash.mjs`: independent node:crypto oracle across padding boundaries, chunk boundaries, UTF-8 and large data; all existing canonicalization rejection cases. | Full integration and final browser pressure proof. |
| DP-06 | Complete export created full JSON text and UTF-8 copies before compression; single-project export loaded every stored project. | `project-store.js`: keyed project read, incremental canonical JSON into CompressionStream; existing package schema, integrity checks, historical records and artifact bytes retained. | Existing lifecycle, file-first and browser package round trips; Stage 03 accumulated-context backup/hash test and the real complete-export button with 48 MB/600 retained responses, tail preservation and package-digest verification. | Final browser run. Largest individual artifact/base64 and full JSON import are still material memory limits; no unlimited-size claim is made. |

| DP-07 | Production export-coordinator regression lost instruction/context when manifest was requested first in the same turn; only one of three requested files was delivered. | `app-core.js`: queued requests preserve order, serialize persistence, guard project/stage navigation and recover after interruption. | `verify-project-lifecycle.mjs` executes the production coordinator against a delayed persistence boundary: RED then GREEN locally. `verify-browser.mjs` presses the real Stage 03 manifest/instruction controls in the same event turn. | Exact-final CI and deployed browser. |


| DP-08 | Changing projects during delayed complete export named project A/B bytes as project C. RED commit 99ba610ebcc240f78f7ca1f4770324432925aa08; production-handler regression recorded both wrong filenames. Concurrent backup/export also allowed overlapping large package assembly. | `app-core.js`: capture requested JOB_ID before awaiting; queue complete export and backup preparation, preserve every request, and recover after a failed export. | `verify-project-lifecycle.mjs` runs the actual handler across project navigation, overlapping requests and injected export failure. RED then GREEN locally. Existing mobile browser proof invokes the real complete-export control with 48 MB history. | Exact-final CI, deployed browser and physical-device acceptance. |

## Acceptance and persistence regression reported after deployment

The screenshot following deployment `2be9c817619885dd048ac0da9e05d74013f21c96` shows a canonical commit exception, not an agent-response validation failure. The app itself generated an invalid reference. This path must accept a valid response and persist the still-pending future proof; the human or external agent must not repair application-owned records.

| ID | Reproduced behavior / evidence | Earliest responsible production layer and repair | Permanent regression | Remaining proof |
|---|---|---|---|---|
| DP-09 | Existing full-cycle Stage 04 acceptance followed by the real store validator reproduces `PROOF_EXPRESSION_ID: Expected non-empty REFERENCE string.` RED commit `1e75c365a8dc9ccafc6b6cf849a9e78337e7b14b`. | `workflow-engine.js:obligations` emits null while the future expression is absent; `workflow-schema.js` declares this specific application-owned reference nullable. Gate blockers remain active. Storage validation remains strict. | `verify-full-cycle.mjs` now checks production storage integrity after every data-response acceptance. `verify-ingestion.mjs` proves pending proof persists and empty/dangling references remain rejected. `verify-browser-extra.mjs` uses file selection, validation failure, regenerated instruction/manifest/context export, corrected file, acceptance, and reload. | Exact-revision CI and deployed browser, followed by actual project/device acceptance. |
| DP-10 | The strengthened full cycle next fails at Stage 11: first-iteration PREVIOUS_ITERATION_ID and CHANGESET_ID contain invalid empty references; unchanged confirmation also lacks a changeset. | The existing iteration producers now emit null for absent links, with those two reference fields explicitly nullable. Actual non-null relationships remain validated. | Same production-validator assertion across the complete existing lifecycle, including changed and unchanged iterations. | Exact-revision CI and deployed browser. |
| DP-11 | The strengthened full cycle rejects the application-recorded candidate-selection array because humanDecisions.VALUE was declared STRING; later human authorizations also have structured values. | The existing schema declares VALUE as canonical JSON, preserving typed human values. The existing ingestion validator uses the canonical serializer to reject unsupported values without constructing a complete JSON copy. Human ownership and command authority remain unchanged. | `verify-ingestion.mjs` tests strings, booleans, numbers, arrays, objects and explicit null, and rejects undefined, functions, non-finite numbers, cycles and undefined object members. Complete-cycle persistence covers actual component selections and authorizations. | Exact-revision CI and deployed browser. |

The full-cycle and ingestion regressions pass locally after these production repairs. The browser regression separately checks automatic agent correction for invalid agent data; an application-generated integrity defect is not sent to the agent as a repair task.

## Restore response recovery and scrolling controls

PR #1211 merged as `939ab58f7f08a8004230cf160747db6b5ee3f53a`. Main CI run `34544905507` passed full software CI, Chromium, exact deployed-byte verification and deployed Chromium. This closes the automated deployed checks for DP-09–11; actual project, physical iPhone and visual authority remain open.

| ID | Reproduced behavior / evidence | Earliest responsible production layer and repair | Permanent regression | Remaining proof |
|---|---|---|---|---|
| DP-12 | Selecting response.json again after staging/review advanced the project revision was blocked before validation with the screenshot's “Save/export the current authoritative instruction” popup. RED `b2ed6d0f4d4b6e7cca12d438d9be99c8e8801098`, CI `34546844838`. | `app-core.js`: response selection finds the saved attempt without requiring the current UI revision. Fresh instruction generation retains its revision check. Exact pending response reselection preserves its existing proposal and revision; normal validation still enforces identity, scope, reservations and engine version. | `verify-file-first-operator.mjs` executes the production handler; `verify-browser-extra.mjs` retries invalid input, exports the regenerated correction, reselects the pending corrected response after reload, accepts and reloads. | Exact final full CI, deployed browser and physical replay. |
| DP-13 | Operational response errors opened blocking native dialogs and could automatically download another full response recovery copy. | The existing inline validation-report area now reports response staging, returned-file, acceptance and correction failures. Only that area is updated, including when a proposal is pending; response controls and unsaved operator input are retained. Persisted bytes and accepted work remain preserved; automatic recovery-copy download is removed. Agent validation failures continue through the existing automatic correction-instruction path. | Same production-handler regression plus real browser storage-fault injection: inline report, no dialog/download, unchanged project, subsequent successful retry. | Exact final full CI and deployed browser. |
| DP-14 | The bounded expanded instruction hid its original up arrow when the page scrolled above its first line; bounded record text had no matching internal-scroll arrows. Production navigation RED reproduced the missing up arrow. | `app-core.js`: the existing arrow controls account for both page position and internal scrolling, and target the bounded text being read. Existing button identity, appearance, location, proposal navigation and collapse behavior are preserved. | `verify-browser.mjs` adds page-scroll-at-first-line coverage; `verify-mobile-stage-action.mjs` clicks the original arrows through accumulated record text and all 30 expanded-stage views. | Exact final full CI, deployed browser and physical Safari/approved visual acceptance. |

| DP-15 | CI `34547307639` passed the all-30-stage scrolling and accumulated-export browser checks, then failed when a second invalid response was selected before fresh instruction export. Production ingestion reproduced `Invalid operation-reservation transition REJECTED -> REJECTED`. | `response-ingestion.js:prepareCaptured` rejects only a reservation currently in RESPONSE_STAGED. Closed attempt states remain unchanged; the normal stale-reservation and input findings are persisted and included in the regenerated correction instruction. Validation and reservation authority are not weakened. | `verify-reservation-contract.mjs` reproduces successive invalid responses and tests ACCEPTED, REJECTED, CANCELLED, SUPERSEDED and EXPIRED_BY_SCOPE terminal states; correction generation and no canonical acceptance are asserted. The existing Stage 04 browser retry regression covers the complete file path. | Exact final full CI and deployed browser. |

## Evidence and acceptance boundaries

### Remaining application popups after the response-only restoration

PR #1212 merged as `306223b1df14dbfede59c3b9bb5d0bd32e4c9fd5`; run `34550626090` passed all four jobs, including exact deployed bytes and live Chromium. The subsequent Stage 05 screenshot establishes that response-only popup coverage was insufficient.

| ID | Reproduced behavior / evidence | Earliest responsible production layer and repair | Permanent regression | Remaining proof |
|---|---|---|---|---|
| DP-16 | Stage 05 instruction export opens the exact reported author-context popup. The message entered in `17484f9370698d478a532be91adb6e98c0dffa35` on 2026-09-04. Fifty-one native alert calls remained across exports, storage, lifecycle and stage controls. RED `da4b65de12ad0fb4c15df2bbcc76ec03da836abd` executes the actual save/export handlers and fails on the screenshot message. | `app-core.js`: remaining action failures update existing inline notices, preserve forms, and restore the previous notice when a subsequent action announces progress. Missing author/reviewer identity points to the existing context input with plain action guidance. No new controls, native-dialog override, schema changes, or workflow authority bypass. | `verify-file-first-operator.mjs` through existing ingestion CI: Stage 05 actual export, existing context disclosure/focus, preserved input/revision, failures across all 30 stage handoffs, and no native popup calls. `verify-browser-extra.mjs`: Stage 05 after real Stage 04 acceptance, inline export failure, registration through the existing control, exact manifest/instruction export recovery, and project/stage validation failures. Existing response-correction, scroll, pressure and full-cycle regressions remain in place. | Exact final CI, deployed Chromium, actual project and physical-iPhone/approved visual acceptance. |

| DP-17 | After the Stage 05 missing-context export failure, focus moved to an unexplained input while the error notice remained above the viewport. RED `f050cb9fb14cbca2524644ceb30fc528d52bf789` fails the actual export-handler regression because the existing field help never explains an agent conversation. | `app-core.js:reportActionFailure/savePromptRecord/announce`: temporarily reuse the existing context-field help as the existing warning-banner style only when missing author/reviewer context blocks export. Explain the external chat name/link, author/reviewer purpose, application-owned internal IDs, and why an external chat needs human identification. Restore the original help after recovery. No new form, control, stylesheet, context authority, or workflow behavior. | Existing `verify-file-first-operator.mjs` covers author/reviewer explanations, unchanged project/form, conditional help restoration and all-stage inline failures. Existing `verify-browser-extra.mjs` covers real Stage 04 acceptance → Stage 05 export failure at 393 px, same help element/child count/banner shape, visible unobscured text, registration, disappearance, and exact recovered manifest/instruction export. | Exact final CI/deployed Chromium; physical Safari and approved visual authority remain separate acceptance requirements. |

- CI run 34533237893 passed all software/full-cycle gates, the sequential stage walkthrough, and the 48 MB/600-record all-30-stage browser pressure fixture (collapsed DOM: 466 characters/8 nodes; opened page: 3,881 characters/93 nodes). Its remaining browser failure was the old requirement to eagerly construct more than eight provenance cards while their parent was closed. The updated regression requires zero closed cards and the complete first page after disclosure.
- Local existing CI source, schema, architecture, migration, accounting, Test IR, ingestion negatives, full-cycle, lifecycle and semantic checks have run. The local sequential browser subcheck is blocked by absent Chromium; the binary download endpoint is unavailable in this workspace. GitHub's existing CI provides its normal Chromium environment. No browser gate is skipped in CI.
- This change does not create a backend. The governing architecture is browser-local, so rendering and processing still occur on the device. Removing duplicated work and bounding active presentation/working buffers reduces pressure; it does not turn the phone into a remote display.
- The user's actual crashing project package is not attached in this conversation. Production-path pressure fixtures are evidence about reproduced mechanisms, not a replay of that exact user dataset.
- The existing deployed Chromium and acceptance reruns also execute `verify-mobile-stage-action.mjs`, including all-30-stage accumulated views and complete export; this fills a deployed pressure-proof gap without adding a workflow or framework.
- Physical-iPhone Safari evidence and approved visual-baseline authority remain OPEN. Both must bind to the final deployed commit and deployment manifest. Responsive Chromium is supplementary evidence only.
- A real externally executed project and physical acceptance cannot be manufactured from automated fixtures or previous-revision receipts.

Coverage mapping: `verification/requirement-test-coverage.json`. This records confident requirement-to-test links and evidence gaps without promoting test presence to proof.

## Automatic context preparation at the existing export boundary

DP-17 explained an unnecessary manual task. The current repair supersedes that manual-registration path; the existing inline error area remains, with no added banner or popup. External conversation names are optional facts, not application identity allocation or proof of provider independence (spec 14.1–14.3, 25.2, 26.1, 26.5–26.6, freshContexts ownership).

| ID | Reproduced behavior / evidence | Earliest responsible production layer and repair | Permanent regression | Remaining proof |
|---|---|---|---|---|
| DP-18 | Stage 05 authoritative export cannot bind an author unless the human first names/registers a chat. RED `beb958d258a658668333af20a8de60dc415614cc` fails the production reservation path. The same human bookkeeping affected reviewer operations and Stage 21 product preparation. | `workflow-schema.js` assigns context bookkeeping to the application. `workflow-engine.js:preparePromptContext` and `prompt-engine.js:reserveAndBuildPromptRecord` prepare context/product records in the existing export transaction. Preview uses copy-on-write and cannot mutate canonical state. `app-core.js` removes manual chat-registration forms and their export blockers; existing handoff controls remain. Unobserved external identifiers stay UNKNOWN; supplied legacy facts remain preserved. | Existing reservation, production-handler, full-cycle and Chromium tests exercise first export, atomic revision, repeat export, author/reviewer role separation, all applicable reviewer operations, product reservation, no invented human-registration event, and no manual context control. | Exact final full CI and deployed browser; actual user project and physical/visual acceptance remain open. |
| DP-19 | After automatic contexts, the full cycle stops at Stage 23 because two UNKNOWN external identifiers are compared as the same real chat. | `workflow-engine.js:evaluateContextIndependence` compares supplied external identifiers only. Canonical identity separation remains required; run context count derives from canonical context records. Known duplicate external IDs, reused canonical IDs, prohibited inputs and contamination checks remain enforced. Application-observable identity separation does not establish hidden provider independence. | `verify-complete.mjs`: unnamed distinct contexts pass, self-review and known duplicate names fail; prior-review leakage still fails. `verify-full-cycle.mjs`: all 30 stages through release, artifact identity, evidence chains and reload, with no manual external chat naming. | Exact final full CI and deployed browser. |
| DP-20 | First run instruction lacks required run/context scope until the operator invokes a separate reservation action. Existing full-cycle regression fails at the first Stage 11 run. | Shared `preparePromptContext` invokes the existing ten-slot allocator for Stage 11/17/19 first export; subsequent exports reuse the reserved batch. Existing handoff action replaces the clerical reservation prompt. Partial/contradictory batches remain blocked. Preview copies only writable bookkeeping collections, preserving accumulated raw data by reference. | `verify-full-cycle.mjs`: first export prepares the initial/corrected/unchanged batches, exactly ten distinct slots per iteration, no external registration, preview cannot mutate canonical state, complete accepted lifecycle. Existing reservation and run-batch negative tests continue. | Exact final full CI and deployed browser. |

The existing all-30-stage pressure, scrolling, complete manifest/instruction/backup export, rejected-response correction, schema, migration and lifecycle regressions remain in the same CI workflow. No backend, styling system, popup, or testing framework is introduced. This is a bounded repair of reproduced defects, not a claim that unexecuted physical acceptance or every specification requirement is proven.


### Stage 06 returned-file revision conflict and manifest retry

| ID | Reproduction | Production repair | Permanent evidence | Remaining proof |
|---|---|---|---|---|
| DP-21 | User screenshot: returned-file validation expected project revision 84, storage held 85. Existing production-handler regression fails with that exact conflict; the stale selected project also blocks subsequent correction export. RED commit c1deec21b5468a2c66ce3bb27ca6852aa53a6a3b. | `app-core.js:persistReplacement` preserves CAS, discards the failed candidate, and reloads the selected stored project. Returned-file validation and prompt saving recompute once from that state. Accepted/pending newer responses are not revalidated. | `verify-file-first-operator.mjs`: retained raw/file identity, newer-state markers, bounded retry and correction manifest; `verify-browser-extra.mjs`: actual IndexedDB independent revision advance then actual returned-file validation button. | Exact final CI/deployment, actual user response.json replay and physical iPhone Safari. |
| DP-22 | After raw capture/attachment mapping advanced revision, the app stopped finding its open saved instruction. Manifest export attempted to reserve an already-controlled operation slot. Production lookup regression failed before the repair. | `app-core.js:currentPromptRecord` also resolves the same still-open reservation with unchanged authority/run scope. Terminal attempts and changed scope remain ineligible. Re-export does not advance revision. | Operator regression plus real browser manifest button before returned-file validation, exact original instruction identity and unchanged stored revision. | Exact final CI/deployed browser and physical iPhone. |
| DP-23 | Attached Stage 06 design explicitly blocked on missing application stage scheduling context. Attached instruction and 3,105,556-byte context match the manifest, but supply no numeric stage schedule. Existing timing test now fails on that evidence gap. | `prompt-engine.js` supplies canonical stage titles and verification-phase routing in the controlling Stage 06 instruction. Existing pre-repair Stage 06 attempts receive the missing schedule when exported, with the prior reservation superseded in the same save. No human scheduling form or popup. | `verify-final-product-timing.mjs` checks the generated scheduling context alongside existing deferred/due tests; operator regression covers upgrading an already-saved Stage 06 attempt. | Exact final CI and a new actual external-agent response. |

The user supplied instruction SHA-256 `c98ab3b06148d15a272958e250fd1d58b3bc91adc0844269e91679caaa93cfbc` (56,143 bytes), context SHA-256 `b4afe4b87ecd0cc31f460cbffb6e2edfa9eb61c8525b169fb18cd09c5315c550` (3,105,556 bytes), and design SHA-256 `5690284acfdeb84460c3fa7b11244a9dbe39b2f43de6ecb78b5f0d817f3276cf` (1,688,050 bytes). The design describes revision 79; the supplied instruction/manifest bind revision 83. The authoritative response.json and complete project were not attached, so this is not an exact replay of their entire project. User project content is not committed to this repository.

## Stage 06 proof repair and shared review defects — PR #1219

PR #1218 is merged as `efa160f5f1fffc82e71ae608bf3e0446e5954dfb`. All four jobs in run `34662163406` passed, including deployed-byte verification and deployed Chromium. This closes the machine-verification work for DP-33–37; final actual-project, physical-iPhone and visual authority remain open.

The next scope audit follows proof data from author intake through review, evaluation, derived records, storage, instruction export and downstream gates. It affects Stage 06 directly and Stages 07–30 through their proof prerequisites. The shared reviewer-context classification also affects Stage 05. Observation-backed proof affects Stages 12/17/19/22–25/29 and downstream release/delivery. No backend is introduced: the controlling specification requires the static/browser-local architecture. Bounded processing and presentation remain the implemented mobile mechanism.

| Defect | Production reproduction | Earliest owner / mechanical repair | Permanent regression and scope |
|---|---|---|---|
| DP-38 | Author response passed Stage 06 before PROOF_REVIEW; author raw ID became semantic review evidence. RED `f38cd4ddfc5f51e1b9d6afd0c96dcd92e97cb49d`, CI `34663595944`, job `103470936922`. | `response-ingestion.js` no longer derives proof authority from author claims. `prompt-engine.js` binds a distinct automatic reviewer to the exact authored test/expression/variance/dependency set. `workflow-engine.js` checks current semantic hashes, accepted review provenance and registered distinct contexts; review-only acceptance preserves the authored suite version. | Existing full cycle: author cannot pass; actual PROOF_REVIEW passes; changed targets, self/foreign contexts, raw-author identity and missing review fail; rebuild/reload retain the correct result. Existing Chromium extra verifier performs the real response-file/accept/export/review/reload flow. Stages 06–30. |
| DP-39 | Unsupported artifact/dependency leaves reached evaluation; proposed arbitrary objects were copied as normalized expressions. | One bounded closed validator/normalizer in `workflow-engine.js` serves intake, prompt contract, current evaluation and stage gating. Exact operators/leaf types, declared evidence/extraction/scope, typed same-response references, current canonical references, cycles and resource bounds are checked. Test observations are restricted to the exact test leaf. | Existing raw-first full-cycle negatives reject malformed/empty/coerced/multi-target/wrong-family/missing/stale expressions before any suite write and preserve raw text. Existing semantic invariant covers closed three-valued truth tables, cycle/depth rejection and unknown evidence. Stages 06–30. |
| DP-40 | Removing manual reviewer registration from the existing Stage 05 full-cycle fixture produced `Stage 05 semantic review independence was not application-established to the observable basis.` | `response-ingestion.js` recognizes the application's existing reviewer-context source as well as human-registered reviewer contexts. Stage 06 reuses the same automatic preparation and retry binding. | Full cycle now uses automatic Stage 05 preparation; browser continues through Stage 05 review acceptance without a chat-name field. Stages 05/06. |
| DP-41 | Real Stage 10 candidate freeze changed the proof-obligation identity although its requirement, suite and expression were unchanged. The new regression failed at that production command. | `workflow-engine.js:deriveCurrentObligations` preserves identity for the current authored definition and updates scope hash/satisfaction/blockers in place. Replacing the definition still supersedes old records; historical accepted evidence remains retained. | Full cycle checks identity across candidate freeze and completion. Prerequisite expression evaluation rejects circular references. Applies to calculation at all later stages and the data accumulated in views/exports. Rejection-only and reservation-only probes did not reproduce growth and are not counted as separate defects. |
| DP-42 | A SELF_ASSERTED observation with an accepted-relation flag could produce TRUE, even without source evidence. | The shared observation owner requires an allowed proof basis, current freshness, source evidence and raw/native provenance before truth extraction. | Existing semantic invariant: self-asserted, missing-source, expired and contradictory evidence cannot satisfy proof. Valid evidence truth tables remain positive. All observation-backed proof stages. |
| DP-43 | Export reused an already-open Stage 06 instruction that omitted the new closed proof contract. Production export-handler regression failed on unchanged instruction identity. | The existing Stage 06 instruction-upgrade path also checks the proof-contract version and supersedes only the old open attempt before rebuilding. | Existing file-first operator verifier: legacy export refreshes automatically, preserves response evidence and uses the current proof contract. Stage 06 author/review/correction exports. |

Review target hashes are computed once per synchronous derivation and discarded afterward; cached proof authority cannot survive a project mutation. The existing paging, 65,536-byte file bounds and all-30-stage accumulated-data browser checks remain required. The current complete lifecycle is a controlled fixture, not an independently executed real external project.

Specification §29.13 explicitly requires independent proof sufficiency, not author-declared EQUIVALENT. Sections 17.12, 37 and 39.5–39.6 explicitly distinguish accepting a response from completion and require the accepted-response receipt. The application owns the current next action and selects PROOF_REVIEW or correction automatically; no manual stage-completion or context-registration step is added.

Status: production RED evidence recorded; local proof/ingestion/integration checks pass as recorded in this PR. Full final CI, Chromium, merge, deployed bytes and deployed Chromium must be recorded before closure. Actual physical-iPhone Safari and approved visual-baseline authority must bind to the final deployed revision. The revision-89 project mentioned in prior issue history is not attached in this conversation, so no new replay of it is claimed.

DP-38 follow-up: the existing independence evaluator is also applied at review acceptance and current proof-authority evaluation. A RED production-state regression showed known shared external conversation IDs could still pass when canonical IDs differed. Known reuse and affirmative contamination now block, while two UNKNOWN external names remain distinct unknown facts. The full-cycle negatives permanently cover both cases.

DP-38 next-action regression: an already-reviewed suite with no ready test incorrectly selected another review. The production next-action regression was RED; the application now selects PROOF_REVIEW only when independent review is the sole remaining gate failure and author provenance is bound. Structural/coverage failures and unbound legacy authors select the existing reconciliation operation.

DP-39 intake boundary cases: the pre-repair production module throws on a null proof record and accepts duplicate expressions for one proposition. Existing raw-first regressions now reject both through normal validation without a partial suite write; current gate evaluation also blocks multiple active expressions for a proposition.
