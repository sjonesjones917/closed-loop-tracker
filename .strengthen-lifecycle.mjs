import fs from 'node:fs';
const r=p=>fs.readFileSync(p,'utf8'),w=(p,s)=>fs.writeFileSync(p,s),rep=(s,a,b,n)=>{if(!s.includes(a))throw new Error('Missing target: '+n);return s.replace(a,b)};

let s=r('response-ingestion.js');
const marker="function answerHumanInput(project,answers,{operator='HUMAN_OPERATOR'}={}){";
const abandon=`function abandon(project,proposalId,{operator='HUMAN_OPERATOR',reason='Operator abandoned the pending attempt.'}={}){\n  const next=clone(project);workflow.ensureShape(next);const proposal=findProposal(next,proposalId);if(!proposal)throw new Error('Response proposal does not exist.');if(proposal.status!=='PENDING_OPERATOR_REVIEW')throw new Error(\`Response proposal is \${proposal.status}, not pending review.\`);\n  const at=now(),note=String(reason||'Operator abandoned the pending attempt.');proposal.status='ABANDONED_BY_OPERATOR';proposal.reviewedAt=at;proposal.reviewedBy=operator;proposal.reviewNote=note;const validation=findValidation(next,proposal.validationId);if(validation)validation.status='ABANDONED_BY_OPERATOR';const raw=findRaw(next,proposal.rawResponseId);if(raw)raw.status='ABANDONED_BY_OPERATOR';const receipt=findReceipt(next,proposal.receiptId);if(receipt){receipt.completionState='ABANDONED_BY_OPERATOR';receipt.nextRequiredVerificationStage=\`REGENERATE STAGE \${String(proposal.stage).padStart(2,'0')} RESPONSE\`;}\n  const d=disposition(next,'ABANDONED_RESPONSE',{stage:proposal.stage,rawResponseId:proposal.rawResponseId,promptId:proposal.promptId,validationId:proposal.validationId,proposalId,receiptId:proposal.receiptId,details:{operator,reason:note,canonicalAcceptedWorkChanged:false,newPromptRequired:true}});workflow.addHistory(next,'RESPONSE_ATTEMPT_ABANDONED',{stage:proposal.stage,rawResponseId:proposal.rawResponseId,proposalId,dispositionId:d.dispositionId,operator,reason:note,canonicalAcceptedWorkChanged:false,newPromptRequired:true});\n  const controlling=safe(next.projectData.generatedPrompts).find(x=>(x.instructionId||x.promptId)===proposal.promptId&&!x.invalidatedBy);if(controlling)controlling.invalidatedBy=d.dispositionId;const promptEngine=globalThis.closedLoopPromptEngine;if(promptEngine){const preview=clone(next);preview.revision=Number(next.revision||0)+1;const generated=promptEngine.buildPromptRecord(proposal.stage,preview,{operation:proposal.envelope.operation,scope:clone(proposal.envelope.scope||{})});workflow.registerGeneratedPrompt(next,{...generated,generatedAt:now()});}\n  workflow.recalculate(next);return {project:next,disposition:d,receipt};\n}\n\n`;
s=rep(s,marker,abandon+marker,'abandon insertion');s=rep(s,'commit,reject,answerHumanInput','commit,reject,abandon,answerHumanInput','abandon export');w('response-ingestion.js',s);

s=r('workflow-engine.js');const resolveMarker="function resolveHumanBlocker(project,{blockerId,resolutionEvidence,operatorLabel='HUMAN_OPERATOR'}={}){";
const custody=`function reconcileArtifactCustodyVerification(project,{report,stage=project.activeStage}={}){\n  ensureShape(project);if(!report||typeof report!=='object')throw new Error('An application artifact-verification report is required.');const open=records(project,'blockers',{active:false}).filter(record=>record.source==='APPLICATION'&&record.applicationBlockerKind==='ARTIFACT_CUSTODY'&&isActiveRecord(record)&&['OPEN','BLOCKED','IN PROGRESS','UNKNOWN'].includes(upper(recordValue(record,'STATUS')||'OPEN')));\n  if(report.verified){for(const blocker of open){const updates={STATUS:'RESOLVED',RESOLUTION_EVIDENCE:\`Application re-read \${report.storedArtifactCount} stored Blob(s); every current canonical artifact requiring byte custody reconciled by ARTIFACT_ID, filename, byte size, and SHA-256 at \${report.at||now()}.\`,CLOSURE:'CLOSED',REEVALUATION:'COMPLETED',REQUIRED_REVALIDATION:'RECALCULATED'};blocker.fields=blocker.fields&&typeof blocker.fields==='object'?blocker.fields:{};for(const [key,value] of Object.entries(updates)){blocker.fields[key]=value;blocker[key]=value;}blocker.updatedAt=now();blocker.resolvedBy='APPLICATION';refreshRecordHashes(blocker,'blockers');addHistory(project,'APPLICATION_ARTIFACT_CUSTODY_RESTORED',{recordId:recordId(blocker,'blockers'),verificationAt:report.at||now()});}if(open.length)recalculate(project);return {verified:true,resolvedBlockerIds:open.map(record=>recordId(record,'blockers'))};}\n  const failures=safe(report.artifacts).filter(item=>!item.verified),artifactIds=failures.map(item=>item.artifactId).filter(Boolean),reason=\`Stored artifact custody verification failed: \${failures.map(item=>\`\${item.artifactId||'UNKNOWN'} (\${item.issue||'byte/hash/metadata mismatch'})\`).join(', ')||'required canonical bytes are missing or mismatched'}.\`;let blocker=open[0];if(!blocker){blocker=commandRecord(project,'blockers',{MISSING_ITEM_TYPE:'ARTIFACT_CUSTODY',MISSING_FACT_INPUT_AUTHORITY_EVIDENCE_CAPABILITY_DECISION_RULE:reason,AFFECTED_REQUIREMENTS:'UNKNOWN',AFFECTED_TESTS:'UNKNOWN',AFFECTED_ARTIFACTS:artifactIds.length?artifactIds:'UNKNOWN',WHY_WORK_CANNOT_CONTINUE:reason,ATTEMPTED_RESOLUTIONS:'APPLICATION BYTE RE-READ AND CANONICAL SHA-256 RECONCILIATION',DOWNSTREAM_WORK_STOPPED:'RELEASE AND DEPENDENT WORK',OWNER:'APPLICATION',STATUS:'OPEN',RESOLUTION_EVIDENCE:'NONE',CLOSURE:'OPEN',REEVALUATION:'REQUIRED',REQUIRED_REVALIDATION:'REQUIRED'},{stage,source:'APPLICATION'});blocker.applicationBlockerKind='ARTIFACT_CUSTODY';refreshRecordHashes(blocker,'blockers');addHistory(project,'APPLICATION_ARTIFACT_CUSTODY_BLOCKED',{recordId:recordId(blocker,'blockers'),artifactIds,verificationAt:report.at||now()});}\n  project.release.authorization='NOT AUTHORIZED';project.release.authorizedArtifactIds=[];for(const collection of ['artifactIdentities','evidenceChains','releaseRecords'])for(const record of records(project,collection,{active:false}))if(isActiveRecord(record)){record.active=false;record.validity='INVALIDATED';record.invalidatedBy=recordId(blocker,'blockers');refreshRecordHashes(record,collection);}recalculate(project);return {verified:false,blockerId:recordId(blocker,'blockers')};\n}\n`;
s=rep(s,resolveMarker,custody+resolveMarker,'custody insertion');s=rep(s,'registerGeneratedPrompt,createHumanBlocker,resolveHumanBlocker','registerGeneratedPrompt,createHumanBlocker,reconcileArtifactCustodyVerification,resolveHumanBlocker','custody export');w('workflow-engine.js',s);

s=r('project-store.js');const storageMarker='async function storageHealth(){';
const verify=`async function verifyProjectArtifacts(jobId){\n  const id=String(jobId||'').trim();if(!id)throw storageError('JOB_ID is required for artifact verification.','ARTIFACT_VERIFY_JOB_ID_REQUIRED');const projects=await readAll(),project=projects.find(item=>projectIdentity(item)===id);if(!project)throw storageError(\`Project \${id} is not stored.\`,'ARTIFACT_VERIFY_PROJECT_MISSING');const rows=await listArtifacts(id),rowById=new Map(rows.map(row=>[String(row.artifactId),row])),engine=globalThis.closedLoopWorkflowEngine,expected=engine.records(project,'artifacts').filter(record=>engine.isActiveRecord(record)&&String(engine.recordValue(record,'AVAILABILITY')||'').toUpperCase()==='BYTES_PERSISTED_AND_VERIFIED'),expectedById=new Map(expected.map(record=>[engine.recordId(record,'artifacts'),record])),results=[];let byteSize=0;\n  for(const record of expected){const artifactId=engine.recordId(record,'artifacts'),row=rowById.get(artifactId),canonicalFilename=String(engine.recordValue(record,'FILENAME')||''),canonicalSize=Number(engine.recordValue(record,'BYTE_SIZE')),canonicalSha256=String(engine.recordValue(record,'SHA256')||'');if(!row){results.push({artifactId,filename:canonicalFilename,verified:false,issue:'MISSING_STORED_BLOB'});continue;}const bytes=new Uint8Array(await row.blob.arrayBuffer()),actualSize=bytes.byteLength,actualSha256=await hash.sha256Bytes(bytes);byteSize+=actualSize;const ok=String(row.jobId)===id&&String(row.filename)===canonicalFilename&&Number(row.byteSize)===canonicalSize&&String(row.sha256)===canonicalSha256&&actualSize===canonicalSize&&actualSha256===canonicalSha256;results.push({artifactId,filename:canonicalFilename,storedByteSize:Number(row.byteSize),actualByteSize:actualSize,storedSha256:String(row.sha256||''),actualSha256,verified:ok,issue:ok?null:'CANONICAL_BLOB_IDENTITY_MISMATCH'});}\n  for(const row of rows)if(!expectedById.has(String(row.artifactId))){const bytes=new Uint8Array(await row.blob.arrayBuffer()),actualSize=bytes.byteLength,actualSha256=await hash.sha256Bytes(bytes);byteSize+=actualSize;const ok=actualSize===Number(row.byteSize)&&actualSha256===String(row.sha256);results.push({artifactId:String(row.artifactId),filename:String(row.filename||row.artifactId),storedByteSize:Number(row.byteSize),actualByteSize:actualSize,storedSha256:String(row.sha256||''),actualSha256,verified:ok,issue:ok?null:'UNREFERENCED_STORED_BLOB_MISMATCH',canonicalExpectation:false});}\n  const mismatches=results.filter(item=>!item.verified);return {jobId:id,storedArtifactCount:rows.length,expectedCanonicalArtifactCount:expected.length,byteSize,verifiedCount:results.length-mismatches.length,mismatchCount:mismatches.length,verified:mismatches.length===0,artifacts:results,at:now()};\n}\n`;
s=rep(s,storageMarker,verify+storageMarker,'project verify insertion');s=rep(s,'removeProject,putArtifact,getArtifact,deleteArtifact,listArtifacts,exportPackage','removeProject,putArtifact,getArtifact,deleteArtifact,listArtifacts,verifyProjectArtifacts,exportPackage','verify export');w('project-store.js',s);

s=r('app-core.js');
const oldRefresh=/async function refreshProjectStorage\(\{verify=false\}=\{\}\)\{[\s\S]*?\n\}/;if(!oldRefresh.test(s))throw new Error('refreshProjectStorage missing');
const newRefresh=`async function refreshProjectStorage({verify=false}={}){\n  const jobId=String(current?.job?.JOB_ID||'');if(!jobId)return projectStorage;const artifacts=await projectStore.listArtifacts(jobId),report=verify?await projectStore.verifyProjectArtifacts(jobId):null,prior=projectUiEntry(jobId),lastVerifiedAt=verify?report.at:(prior.lastIntegrityVerification||null),mismatches=report?safe(report.artifacts).filter(item=>!item.verified).map(item=>item.artifactId):safe(prior.lastIntegrityMismatches);projectStorage={artifactCount:artifacts.length,byteSize:artifacts.reduce((sum,row)=>sum+Number(row.byteSize||0),0),integrity:verify?(report.verified?'VERIFIED':'FAILED'):(prior.lastIntegrityStatus||'NOT CHECKED'),lastVerifiedAt,mismatches};\n  if(verify){projectUi[jobId]={...prior,lastIntegrityVerification:lastVerifiedAt,lastIntegrityStatus:projectStorage.integrity,lastIntegrityMismatches:mismatches};await saveProjectUi();const openCustody=engine.records(current,'blockers',{active:false}).some(record=>record.source==='APPLICATION'&&record.applicationBlockerKind==='ARTIFACT_CUSTODY'&&engine.isActiveRecord(record)&&['OPEN','BLOCKED','IN PROGRESS','UNKNOWN'].includes(String(engine.recordValue(record,'STATUS')||'OPEN').toUpperCase()));if(!report.verified||openCustody){const next=clone(current);engine.reconcileArtifactCustodyVerification(next,{report,stage:Number(current.activeStage||1)});await persistReplacement(next);}}return projectStorage;\n}`;
s=s.replace(oldRefresh,newRefresh);
s=s.replace("function pendingProposal(){const dismissed=new Set(safe(projectUiEntry(current?.job?.JOB_ID).dismissedProposalIds));return safe(current.projectData.responseProposals).filter(x=>x.status==='PENDING_OPERATOR_REVIEW'&&!dismissed.has(x.proposalId)&&operatorLaneMatches(x,current.activeStage)).at(-1);}","function pendingProposal(){return safe(current.projectData.responseProposals).filter(x=>x.status==='PENDING_OPERATOR_REVIEW'&&operatorLaneMatches(x,current.activeStage)).at(-1);}");
s=s.replace("async function discardCurrentAttempt(){const p=pendingProposal(),jobId=current.job.JOB_ID;if(p){const ui=projectUiEntry(jobId),ids=new Set(safe(ui.dismissedProposalIds));ids.add(p.proposalId);projectUi[jobId]={...ui,dismissedProposalIds:[...ids]};await saveProjectUi();}const node=$('#stage-output');if(node)node.value='';announce('attempt discarded; canonical state changed: NO');render();}","async function discardCurrentAttempt(){const p=pendingProposal(),node=$('#stage-output');if(node)node.value='';if(!p){announce('unsaved attempt cleared; canonical accepted work changed: NO');return render();}try{const result=ingestion.abandon(current,p.proposalId,{operator:$('#operator-label')?.value.trim()||'HUMAN_OPERATOR'});await persistReplacement(result.project);announce('attempt discarded; canonical accepted work changed: no; audit state changed: yes; new prompt required: yes');render();queueMicrotask(()=>$('#generated-prompt')?.focus());}catch(error){alert(`Pending attempt was not discarded: ${error.message||error}`);}}");
s=s.replace('Display-name, archive, and dismissed-attempt preferences are browser-local management metadata and do not rewrite controlled job identity or accepted evidence.','Display-name and archive preferences are browser-local management metadata and do not rewrite controlled job identity or accepted evidence. Discarded response attempts remain preserved in the response audit trail and cannot later be accepted.');
s=s.replace('Clearing or discarding here does not accept, reject, or alter canonical evidence. Preserved raw audit records remain preserved. Canonical state changed: NO.','Clearing unsaved text changes no persisted state. Discarding a validated pending attempt accepts no proposed canonical work, preserves its raw audit trail, supersedes that attempt, and requires a new controlling prompt.');
const oldMode="function interactionModeMarkup(n){const pending=pendingProposal(),latestValidation=safe(current.projectData.responseValidations).filter(x=>Number(x.stage)===n).at(-1),accepted=acceptedLaneChanges(n).at(-1),requests=safe(current.projectData.humanInputRequests).filter(x=>Number(x.stage)===n&&String(x.status||'OPEN').toUpperCase()==='OPEN');if(accepted)return";
const newMode="function interactionModeMarkup(n){const pending=pendingProposal(),latestValidation=safe(current.projectData.responseValidations).filter(x=>Number(x.stage)===n).at(-1),latestDisposition=safe(current.projectData.operationDispositions).filter(x=>Number(x.stage)===n).at(-1),accepted=acceptedLaneChanges(n).at(-1),requests=safe(current.projectData.humanInputRequests).filter(x=>Number(x.stage)===n&&String(x.status||'OPEN').toUpperCase()==='OPEN');if(latestDisposition?.status==='ABANDONED_RESPONSE')return `<div class=\"notice\"><strong>The pending attempt was discarded.</strong><br>Canonical accepted work changed: NO · Audit state changed: YES · New prompt required: YES. Use only the newly generated authoritative prompt; the abandoned response cannot be accepted later.</div>`;if(accepted)return";
s=rep(s,oldMode,newMode,'interaction mode');
const oldDownload="async function downloadProjectPackage(kind='export'){const blob=await projectStore.exportPackage(current.job.JOB_ID),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`${current.job.JOB_ID||'project'}${kind==='backup'?'.backup':''}.closed-loop.json.gz`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),0);globalThis.closedLoopStorageHealth=await projectStore.storageHealth();announce(kind==='backup'?'complete project backup exported':'complete project package exported');render();}";
const newDownload="async function downloadProjectPackage(kind='export'){const jobId=current.job.JOB_ID,blob=await projectStore.exportPackage(jobId),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`${jobId||'project'}${kind==='backup'?'.backup':''}.closed-loop.json.gz`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),0);if(kind==='backup'){projectUi[jobId]={...projectUiEntry(jobId),lastCompleteBackup:{at:new Date().toISOString(),verifiedPackage:true}};await saveProjectUi();}globalThis.closedLoopStorageHealth=await projectStore.storageHealth();announce(kind==='backup'?'complete project backup exported':'complete project package exported');render();}";
s=rep(s,oldDownload,newDownload,'backup status');s=s.replace("const archived=projects.filter(project=>projectIsArchived(project)),lastEvent=safe(current.projectData.history).at(-1),health=globalThis.closedLoopStorageHealth||{},lastBackup=health.lastVerifiedExport?.jobId===jobId?health.lastVerifiedExport:null;","const archived=projects.filter(project=>projectIsArchived(project)),lastEvent=safe(current.projectData.history).at(-1),health=globalThis.closedLoopStorageHealth||{},lastBackup=projectUiEntry(jobId).lastCompleteBackup||(health.lastVerifiedExport?.jobId===jobId?health.lastVerifiedExport:null);");w('app-core.js',s);

s=r('verify-project-lifecycle.mjs');s=s.replace("const app=fs.readFileSync('app-core.js','utf8'),store=fs.readFileSync('project-store.js','utf8'),html=fs.readFileSync('index.html','utf8');","const app=fs.readFileSync('app-core.js','utf8'),store=fs.readFileSync('project-store.js','utf8'),ingestion=fs.readFileSync('response-ingestion.js','utf8'),engine=fs.readFileSync('workflow-engine.js','utf8'),html=fs.readFileSync('index.html','utf8');");s=s.replace("assert(app.includes('dismissedProposalIds')&&app.includes('canonical state changed: NO'),'Discarded attempts must remain non-canonical UI lifecycle state.');","assert(!app.includes('dismissedProposalIds')&&ingestion.includes('function abandon(project,proposalId')&&ingestion.includes(\"'ABANDONED_RESPONSE'\")&&app.includes('Audit state changed: YES'),'Discarded attempts must be durably abandoned without accepting proposed canonical work.');\nassert(store.includes('verifyProjectArtifacts')&&store.includes('MISSING_STORED_BLOB')&&store.includes('CANONICAL_BLOB_IDENTITY_MISMATCH'),'Stored-file verification must reconcile canonical artifact expectations to actual Blob bytes.');\nassert(engine.includes('reconcileArtifactCustodyVerification')&&engine.includes(\"applicationBlockerKind='ARTIFACT_CUSTODY'\"),'Artifact custody failures must be application-owned and release-blocking.');\nassert(app.includes('lastCompleteBackup'),'Backup status must be project-specific in the management UI.');");w('verify-project-lifecycle.mjs',s);
