import fs from 'node:fs';
import crypto from 'node:crypto';

const read=p=>fs.readFileSync(p,'utf8');
const write=(p,s)=>fs.writeFileSync(p,s);
const must=(s,needle,label)=>{if(!s.includes(needle))throw new Error(`Missing ${label||needle}`);};
const once=(s,from,to,label)=>{must(s,from,label||from);const n=s.replace(from,to);if(n===s)throw new Error(`No change for ${label||from}`);return n;};
const between=(s,start,end,replacement,label)=>{const a=s.indexOf(start),b=s.indexOf(end,a+start.length);if(a<0||b<0)throw new Error(`Could not locate ${label}`);return s.slice(0,a)+replacement+s.slice(b);};
const sha=s=>crypto.createHash('sha256').update(s).digest('hex');

// Preserve the exact visual baseline. This implementation pass is architectural/behavioral only.
let html=read('index.html');
const styleBlock=html.match(/<style>[\s\S]*?<\/style>/)?.[0];
if(!styleBlock)throw new Error('index.html style block missing');
const styleSha=sha(styleBlock);

// 1) Controlling identities and visible Stage 16 wording.
let workbook=read('workbook.js');
workbook=once(workbook,"const PROJECT_SCHEMA='closed-loop-project/2';","const PROJECT_SCHEMA='closed-loop-project/3';",'project schema /3');
workbook=once(workbook,"'REVISE THE RESPONSIBLE LAYER'","'CORRECT THE ROOT CAUSE'",'Stage 16 visible name');
write('workbook.js',workbook);

// 2) /3 response/schema ownership and exact Test IR contract.
let schema=read('workflow-schema.js');
schema=once(schema,"const RESPONSE_SCHEMA='closed-loop-stage-response/2';","const RESPONSE_SCHEMA='closed-loop-stage-response/3';",'response schema /3');
schema=once(schema,"const STAGE_COUNT=core.STAGE_COUNT;","const STAGE_COUNT=core.STAGE_COUNT;\nconst PACKAGE_SCHEMA='closed-loop-verification-package/1';",'verification package schema');
schema=once(schema,"const HUMAN_JOB_FIELDS=Object.freeze([\n  'JOB_TITLE','JOB_OWNER','EXACT_USER_OBJECTIVE_VERBATIM'","const HUMAN_JOB_FIELDS=Object.freeze([\n  'EXACT_USER_OBJECTIVE_VERBATIM'",'human job partition');
schema=once(schema,"const APPLICATION_JOB_FIELDS=Object.freeze([","const HUMAN_DECISION_JOB_FIELDS=Object.freeze(['JOB_TITLE','JOB_OWNER']);\nconst APPLICATION_JOB_FIELDS=Object.freeze([",'human decision job partition');
schema=once(schema,"  if(HUMAN_JOB_FIELDS.includes(name))return field(name,PRODUCER.HUMAN,{requiredAtStage:name==='EXACT_USER_OBJECTIVE_VERBATIM'?1:null,provenanceRequired:false,valueType:name==='DESIRED_SOURCE_COUNT'?'INTEGER':'STRING',nullable:name!=='EXACT_USER_OBJECTIVE_VERBATIM'});","  if(HUMAN_DECISION_JOB_FIELDS.includes(name))return field(name,PRODUCER.HUMAN_DECISION,{provenanceRequired:false,nullable:true});\n  if(HUMAN_JOB_FIELDS.includes(name))return field(name,PRODUCER.HUMAN,{requiredAtStage:name==='EXACT_USER_OBJECTIVE_VERBATIM'?1:null,provenanceRequired:false,valueType:name==='DESIRED_SOURCE_COUNT'?'INTEGER':'STRING',nullable:name!=='EXACT_USER_OBJECTIVE_VERBATIM'});",'job field producer');
schema=once(schema,"[...new Set([...HUMAN_JOB_FIELDS,...APPLICATION_JOB_FIELDS,...AGENT_JOB_FIELDS])]","[...new Set([...HUMAN_JOB_FIELDS,...HUMAN_DECISION_JOB_FIELDS,...APPLICATION_JOB_FIELDS,...AGENT_JOB_FIELDS])]",'job field union');

// Test ownership: agent designs IR, application owns version/hash.
schema=once(schema,'      "EXECUTABLE_KIND",\n      "EXECUTABLE_SPEC_VERSION",\n      "EXECUTABLE_SPEC",','      "EXECUTABLE_KIND",\n      "EXECUTABLE_SPEC",','test ownership remove spec version from agent');
schema=once(schema,'      "TEST_ID",\n      "REQ_ID",\n      "STATUS"\n    ]\n  },\n  "failureTests"','      "TEST_ID",\n      "REQ_ID",\n      "STATUS",\n      "EXECUTABLE_SPEC_VERSION",\n      "EXECUTABLE_SPEC_SHA256"\n    ]\n  },\n  "failureTests"','test ownership application version/hash');
schema=once(schema,"'TEST_ID','REQ_ID','TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','EXECUTABLE_KIND','EXECUTABLE_SPEC_VERSION','EXECUTABLE_SPEC','EXECUTABLE_INPUT_BINDINGS'","'TEST_ID','REQ_ID','TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','EXECUTABLE_KIND','EXECUTABLE_SPEC_VERSION','EXECUTABLE_SPEC_SHA256','EXECUTABLE_SPEC','EXECUTABLE_INPUT_BINDINGS'",'test fields add hash');
schema=once(schema,"    EXECUTABLE_SPEC_VERSION:Object.freeze({valueType:VALUE_TYPES.STRING,enumValues:[],nullable:true,normalizerKey:null,closedProperties:null}),","    EXECUTABLE_SPEC_VERSION:Object.freeze({valueType:VALUE_TYPES.STRING,enumValues:[],nullable:true,normalizerKey:null,closedProperties:null}),\n    EXECUTABLE_SPEC_SHA256:Object.freeze({valueType:VALUE_TYPES.STRING,enumValues:[],nullable:true,normalizerKey:null,closedProperties:null}),",'test hash type');

const exactOps="Object.freeze(['LOAD_ARTIFACT','READ_BYTES','DECODE_UTF8','PARSE_JSON','PARSE_CSV','PARSE_XML','SELECT_JSON_PATH','SELECT_XML','COUNT','SUM','MIN','MAX','SORT','UNIQUE','HASH_SHA256','REGEX','COMPARE','ASSERT_EQ','ASSERT_GT','ASSERT_GTE','ASSERT_LT','ASSERT_LTE','ASSERT_MATCH','ASSERT_CONTAINS','ASSERT_NOT_CONTAINS','ASSERT_SET_EQUAL','BYTE_COMPARE'])";
schema=schema.replace("executableKinds:Object.freeze(['NONE','CUSTOM_PIPELINE'])","executableKinds:Object.freeze(['NONE','TEST_IR'])");
schema=schema.replace(/operations:Object\.freeze\(\[[^\n]+\]\)/,`operations:${exactOps}`);
schema=schema.replace(/limits:Object\.freeze\(\{maxSteps:64,maxTextBytes:16777216,maxCollectionItems:100000,maxRegexLength:2000,maxCsvCells:250000\}\)/,"limits:Object.freeze({maxInputBytes:16777216,maxDecompressedBytes:33554432,maxSteps:64,maxSelectorDepth:32,maxParsedDepth:64,maxCollectionItems:100000,maxRegexLength:2000,maxRegexInputLength:1048576,maxWorkerDurationMs:10000,maxArchiveExpansionBytes:33554432,maxCsvCells:250000})");
schema=schema.replaceAll("'CUSTOM_PIPELINE'","'TEST_IR'").replaceAll('CUSTOM_PIPELINE','TEST_IR');
schema=schema.replace("issues.push('EXECUTABLE_KIND must be TEST_IR.')","issues.push('EXECUTABLE_KIND must be TEST_IR.')");
schema=once(schema,"version:'closed-loop-workflow-schema/2'","version:'closed-loop-workflow-schema/3'",'schema implementation version');
schema=once(schema,"  PROJECT_SCHEMA,WORKFLOW_ID,STAGE_COUNT,VALUE_TYPES","  PROJECT_SCHEMA,RESPONSE_SCHEMA,PACKAGE_SCHEMA,WORKFLOW_ID,STAGE_COUNT,VALUE_TYPES",'schema exports identities');
schema=once(schema,"  JOB_FIELDS,HUMAN_JOB_FIELDS,APPLICATION_JOB_FIELDS,AGENT_JOB_FIELDS,HUMAN_INTAKE_FIELDS,","  JOB_FIELDS,HUMAN_JOB_FIELDS,HUMAN_DECISION_JOB_FIELDS,APPLICATION_JOB_FIELDS,AGENT_JOB_FIELDS,HUMAN_INTAKE_FIELDS,",'human-decision export');
write('workflow-schema.js',schema);

// 3) Replace the deterministic runtime with a closed declarative, worker-safe implementation.
const runtime=`(()=>{\n'use strict';\nconst VERSION='closed-loop-test-runtime/1';\nconst SPEC_VERSION='closed-loop-test-spec/1';\nconst CAPABILITY='CLOSED_LOOP_TEST_IR';\nconst OPS=Object.freeze(['LOAD_ARTIFACT','READ_BYTES','DECODE_UTF8','PARSE_JSON','PARSE_CSV','PARSE_XML','SELECT_JSON_PATH','SELECT_XML','COUNT','SUM','MIN','MAX','SORT','UNIQUE','HASH_SHA256','REGEX','COMPARE','ASSERT_EQ','ASSERT_GT','ASSERT_GTE','ASSERT_LT','ASSERT_LTE','ASSERT_MATCH','ASSERT_CONTAINS','ASSERT_NOT_CONTAINS','ASSERT_SET_EQUAL','BYTE_COMPARE']);\nconst LIMITS=Object.freeze({maxInputBytes:16777216,maxDecompressedBytes:33554432,maxSteps:64,maxSelectorDepth:32,maxParsedDepth:64,maxCollectionItems:100000,maxRegexLength:2000,maxRegexInputLength:1048576,maxWorkerDurationMs:10000,maxArchiveExpansionBytes:33554432,maxCsvCells:250000});\nconst FORBIDDEN_STEP_KEYS=Object.freeze(['code','javascript','python','shell','command','eval','function','script','import','url','network']);\nconst STEP_KEYS=Object.freeze({LOAD_ARTIFACT:['op','binding'],READ_BYTES:['op'],DECODE_UTF8:['op'],PARSE_JSON:['op'],PARSE_CSV:['op','delimiter','header','quote','newline','encoding'],PARSE_XML:['op'],SELECT_JSON_PATH:['op','path'],SELECT_XML:['op','path'],COUNT:['op'],SUM:['op'],MIN:['op'],MAX:['op'],SORT:['op'],UNIQUE:['op'],HASH_SHA256:['op'],REGEX:['op','pattern','flags'],COMPARE:['op','value','binding'],ASSERT_EQ:['op','value','message'],ASSERT_GT:['op','value','message','absoluteTolerance','relativeTolerance'],ASSERT_GTE:['op','value','message','absoluteTolerance','relativeTolerance'],ASSERT_LT:['op','value','message','absoluteTolerance','relativeTolerance'],ASSERT_LTE:['op','value','message','absoluteTolerance','relativeTolerance'],ASSERT_MATCH:['op','pattern','flags','message'],ASSERT_CONTAINS:['op','value','message'],ASSERT_NOT_CONTAINS:['op','value','message'],ASSERT_SET_EQUAL:['op','value','message'],BYTE_COMPARE:['op','binding']});\nconst bytesOf=v=>v instanceof Uint8Array?v:v instanceof ArrayBuffer?new Uint8Array(v):ArrayBuffer.isView(v)?new Uint8Array(v.buffer,v.byteOffset,v.byteLength):null;\nconst field=(test,key)=>test?.fields?.[key]??test?.[key];\nconst stable=value=>JSON.stringify(value,(_,v)=>v&&typeof v==='object'&&!Array.isArray(v)?Object.fromEntries(Object.entries(v).sort(([a],[b])=>a.localeCompare(b))):v);\nconst depthOf=(v,d=0)=>{if(d>LIMITS.maxParsedDepth)throw new Error('Parsed structure exceeds deterministic depth limit.');if(v&&typeof v==='object')for(const x of Object.values(v))depthOf(x,d+1);return d;};\nasync function sha256(bytes){const data=bytesOf(bytes);if(!data)throw new Error('HASH_SHA256 requires bytes.');const digest=await crypto.subtle.digest('SHA-256',data);return [...new Uint8Array(digest)].map(x=>x.toString(16).padStart(2,'0')).join('');}\nfunction parseCsv(text,step){if(step.encoding!=='UTF-8')throw new Error('PARSE_CSV encoding must be UTF-8.');if(typeof step.delimiter!=='string'||step.delimiter.length!==1)throw new Error('PARSE_CSV delimiter must be one character.');if(typeof step.header!=='boolean')throw new Error('PARSE_CSV header must be BOOLEAN.');if(typeof step.quote!=='string'||step.quote.length!==1)throw new Error('PARSE_CSV quote must be one character.');if(!['LF','CRLF','AUTO'].includes(step.newline))throw new Error('PARSE_CSV newline must be LF, CRLF, or AUTO.');const d=step.delimiter,q=step.quote,rows=[];let row=[],cell='',quoted=false,cells=0;for(let i=0;i<text.length;i++){const ch=text[i];if(quoted){if(ch===q&&text[i+1]===q){cell+=q;i++;}else if(ch===q)quoted=false;else cell+=ch;continue;}if(ch===q){if(cell.length)throw new Error('Malformed CSV quote.');quoted=true;continue;}if(ch===d){row.push(cell);cell='';cells++;continue;}const nl=ch==='\\n'||ch==='\\r';if(nl){if(step.newline==='LF'&&ch!=='\\n')throw new Error('CSV newline contract violated.');if(step.newline==='CRLF'&&(ch!=='\\r'||text[i+1]!=='\\n'))throw new Error('CSV newline contract violated.');if(ch==='\\r'&&text[i+1]==='\\n')i++;row.push(cell);rows.push(row);row=[];cell='';cells++;continue;}cell+=ch;if(cells>LIMITS.maxCsvCells)throw new Error('CSV exceeds cell limit.');}if(quoted)throw new Error('Malformed CSV: unterminated quoted field.');if(cell.length||row.length){row.push(cell);rows.push(row);}if(rows.length>LIMITS.maxCollectionItems)throw new Error('CSV exceeds row limit.');if(!step.header)return rows;const header=rows.shift()||[];if(new Set(header).size!==header.length)throw new Error('CSV header contains duplicate names.');return rows.map(r=>Object.fromEntries(header.map((h,i)=>[h,r[i]??''])));}\nfunction parseXml(text){const root={name:'#document',attrs:{},children:[],text:''},stack=[root],token=/<([^>]+)>|([^<]+)/g;let m,count=0;while((m=token.exec(text))){if(m[2]){stack.at(-1).text+=m[2];continue;}const raw=m[1].trim();if(raw.startsWith('?')||raw.startsWith('!'))continue;if(raw.startsWith('/')){const name=raw.slice(1).trim();if(stack.length===1||stack.at(-1).name!==name)throw new Error('Malformed XML closing element.');stack.pop();continue;}const self=raw.endsWith('/'),body=self?raw.slice(0,-1).trim():raw,mm=body.match(/^([A-Za-z_][\\w:.-]*)([\\s\\S]*)$/);if(!mm)throw new Error('Unsupported XML element syntax.');const node={name:mm[1],attrs:{},children:[],text:''};const attrs=mm[2].trim();if(attrs){const re=/([A-Za-z_][\\w:.-]*)\\s*=\\s*(["'])(.*?)\\2/g;let a,used='';while((a=re.exec(attrs))){node.attrs[a[1]]=a[3];used+=a[0];}if(used.replace(/\\s/g,'')!==attrs.replace(/\\s/g,''))throw new Error('Unsupported XML attribute syntax.');}stack.at(-1).children.push(node);count++;if(count>LIMITS.maxCollectionItems)throw new Error('XML exceeds node limit.');if(!self){stack.push(node);if(stack.length>LIMITS.maxParsedDepth)throw new Error('XML exceeds depth limit.');}}if(stack.length!==1)throw new Error('Malformed XML: unclosed element.');if(root.children.length!==1)throw new Error('XML must contain exactly one root element.');return root.children[0];}\nfunction selectorParts(path,prefix){const text=String(path||'').trim();if(!text.startsWith(prefix))throw new Error('Unsupported selector root.');const tail=text.slice(prefix.length);const parts=tail?tail.split('/').filter(Boolean):[];if(parts.length>LIMITS.maxSelectorDepth)throw new Error('Selector exceeds depth limit.');return parts;}\nfunction selectJsonPath(value,path){const text=String(path||'').trim();if(text==='$')return value;if(!text.startsWith('$.'))throw new Error('SELECT_JSON_PATH supports only registered $.a.b[0] paths.');const parts=[];for(const token of text.slice(2).split('.')){const m=token.match(/^([^\\[\\]]+)(?:\\[(\\d+)\\])?$/);if(!m)throw new Error('Unsupported JSON selector token.');parts.push(m[1]);if(m[2]!==undefined)parts.push(Number(m[2]));}if(parts.length>LIMITS.maxSelectorDepth)throw new Error('Selector exceeds depth limit.');let out=value;for(const p of parts){if(out===null||out===undefined||!(p in Object(out)))throw new Error('JSON selector does not exist.');out=out[p];}return out;}\nfunction selectXml(root,path){const parts=selectorParts(path,'/');let nodes=[root];if(parts[0]===root.name)parts.shift();for(const part of parts){const m=part.match(/^([A-Za-z_][\\w:.-]*)(?:\\[(\\d+)\\])?$/);if(!m)throw new Error('Unsupported XML selector token.');const next=[];for(const n of nodes){const xs=n.children.filter(x=>x.name===m[1]);if(m[2]!==undefined){const x=xs[Number(m[2])];if(x)next.push(x);}else next.push(...xs);}nodes=next;}return nodes.length===1?nodes[0]:nodes;}\nfunction safeRegex(pattern,flags,input){if(pattern.length>LIMITS.maxRegexLength)throw new Error('Regex exceeds pattern limit.');if(input.length>LIMITS.maxRegexInputLength)throw new Error('Regex input exceeds limit.');if(/\\\\[1-9]|\\(\\?<[=!]|\\(\\?R|\\(\\?0/.test(pattern))throw new Error('Regex construct is outside the registered safe subset.');if(/[^dgimsuvy]/.test(flags))throw new Error('Unsupported regex flags.');return new RegExp(pattern,flags);}\nconst comparable=v=>v instanceof Uint8Array?[...v]:v;\nfunction decimalParts(v){const s=String(v);if(!/^-?\\d+(?:\\.\\d+)?$/.test(s))return null;const neg=s.startsWith('-'),t=neg?s.slice(1):s,[a,b='']=t.split('.');return {neg,digits:BigInt((a+b)||'0'),scale:b.length};}\nfunction exactDecimalCompare(a,b){const A=decimalParts(a),B=decimalParts(b);if(!A||!B)return null;const scale=Math.max(A.scale,B.scale),x=A.digits*10n**BigInt(scale-A.scale)*(A.neg?-1n:1n),y=B.digits*10n**BigInt(scale-B.scale)*(B.neg?-1n:1n);return x<y?-1:x>y?1:0;}\nfunction numericCompare(a,b,step){const exact=exactDecimalCompare(a,b);if(exact!==null)return exact;const abs=step.absoluteTolerance,rel=step.relativeTolerance;if(abs===undefined&&rel===undefined)throw new Error('Approximate numeric comparison requires explicit tolerance.');const x=Number(a),y=Number(b);if(!Number.isFinite(x)||!Number.isFinite(y))throw new Error('Numeric comparison requires finite values.');const tolerance=Math.max(Number(abs||0),Math.abs(y)*Number(rel||0));if(Math.abs(x-y)<=tolerance)return 0;return x<y?-1:1;}\nfunction assertResult(ok,expected,actual,message){return {determination:ok?'SATISFIED':'VIOLATED',expected,actual,message:message||null};}\nfunction validateSpec(spec){const issues=[];if(!spec||typeof spec!=='object'||Array.isArray(spec))issues.push('Test IR must be an object.');if(spec?.version!==SPEC_VERSION)issues.push('Unsupported Test IR version.');if(!Array.isArray(spec?.steps)||!spec.steps.length)issues.push('Test IR requires steps.');if((spec?.steps?.length||0)>LIMITS.maxSteps)issues.push('Test IR exceeds step limit.');for(const [i,step] of (spec?.steps||[]).entries()){if(!step||typeof step!=='object'||Array.isArray(step)){issues.push('Step '+i+' is not an object.');continue;}if(!OPS.includes(step.op)){issues.push('Step '+i+' uses unsupported operation '+String(step.op)+'.');continue;}for(const k of Object.keys(step))if(!STEP_KEYS[step.op].includes(k))issues.push('Step '+i+' contains unknown property '+k+'.');for(const k of FORBIDDEN_STEP_KEYS)if(Object.prototype.hasOwnProperty.call(step,k))issues.push('Step '+i+' contains forbidden executable field '+k+'.');if(step.op==='PARSE_CSV'){if(typeof step.delimiter!=='string'||step.delimiter.length!==1)issues.push('PARSE_CSV delimiter is required.');if(typeof step.header!=='boolean')issues.push('PARSE_CSV header is required.');if(typeof step.quote!=='string'||step.quote.length!==1)issues.push('PARSE_CSV quote is required.');if(!['LF','CRLF','AUTO'].includes(step.newline))issues.push('PARSE_CSV newline is required.');if(step.encoding!=='UTF-8')issues.push('PARSE_CSV encoding must be UTF-8.');}if(['REGEX','ASSERT_MATCH'].includes(step.op)&&String(step.pattern??'').length>LIMITS.maxRegexLength)issues.push('Regex exceeds limit.');}return {valid:issues.length===0,issues};}\nfunction validateBindings(bindings){const issues=[];if(!bindings||typeof bindings!=='object'||Array.isArray(bindings))return {valid:false,issues:['EXECUTABLE_INPUT_BINDINGS must be an object.']};for(const [name,b] of Object.entries(bindings)){if(!/^[A-Z][A-Z0-9_]{0,63}$/.test(name))issues.push('Invalid binding name '+name+'.');if(typeof b==='string'){if(!b.trim())issues.push('Empty binding '+name+'.');continue;}if(!b||typeof b!=='object'||Array.isArray(b)){issues.push('Invalid binding '+name+'.');continue;}const allowed=new Set(['artifactId','source','artifactRole','filename']);for(const k of Object.keys(b))if(!allowed.has(k))issues.push('Binding '+name+' contains unsupported key '+k+'.');if(b.source&&!['CURRENT_PRODUCT','CURRENT_SCOPE'].includes(b.source))issues.push('Binding '+name+' has unsupported source.');if(!b.artifactId&&!b.artifactRole&&!b.filename)issues.push('Binding '+name+' does not identify an artifact.');}return {valid:!issues.length,issues};}\nfunction supports(test){return String(field(test,'EXECUTION_MODE')||'').toUpperCase()==='APPLICATION_DETERMINISTIC'&&String(field(test,'REQUIRED_CAPABILITY')||'').toUpperCase()===CAPABILITY&&String(field(test,'EXECUTABLE_KIND')||'').toUpperCase()==='TEST_IR'&&field(test,'EXECUTABLE_SPEC_VERSION')===SPEC_VERSION&&validateSpec(field(test,'EXECUTABLE_SPEC')).valid&&validateBindings(field(test,'EXECUTABLE_INPUT_BINDINGS')).valid;}\nasync function execute({spec,artifacts}){const check=validateSpec(spec);if(!check.valid)throw new Error(check.issues.join(' '));const source=artifacts&&typeof artifacts==='object'?artifacts:{};let value=null,currentArtifact=null,lastRegex=null;const observations=[];let totalBytes=0;for(const a of Object.values(source)){const b=bytesOf(a?.bytes);if(b)totalBytes+=b.byteLength;}if(totalBytes>LIMITS.maxInputBytes)throw new Error('Total input bytes exceed deterministic runtime limit.');for(const [i,step] of spec.steps.entries()){let assertion=null;switch(step.op){case 'LOAD_ARTIFACT':{const a=source[step.binding];if(!a)throw new Error('Artifact binding unavailable: '+step.binding);currentArtifact=a;value=a;observations.push({step:i,op:step.op,binding:step.binding,artifactId:a.artifactId||null,sha256:a.sha256||null});break;}case 'READ_BYTES':{const b=bytesOf(currentArtifact?.bytes??value?.bytes??value);if(!b)throw new Error('READ_BYTES requires bytes.');value=b;break;}case 'DECODE_UTF8':{const b=bytesOf(value);if(!b)throw new Error('DECODE_UTF8 requires bytes.');value=new TextDecoder('utf-8',{fatal:true}).decode(b);break;}case 'PARSE_JSON':value=JSON.parse(String(value));depthOf(value);break;case 'PARSE_CSV':value=parseCsv(String(value),step);depthOf(value);break;case 'PARSE_XML':value=parseXml(String(value));break;case 'SELECT_JSON_PATH':value=selectJsonPath(value,step.path);break;case 'SELECT_XML':value=selectXml(value,step.path);break;case 'COUNT':{if(value==null||typeof value.length!=='number')throw new Error('COUNT requires array-like input.');value=value.length;break;}case 'SUM':case 'MIN':case 'MAX':{if(!Array.isArray(value)||value.length>LIMITS.maxCollectionItems)throw new Error(step.op+' requires bounded array.');const vals=value.map(v=>{if(!Number.isSafeInteger(v))throw new Error(step.op+' version 1 requires exact safe integers.');return v;});value=step.op==='SUM'?vals.reduce((a,b)=>a+b,0):step.op==='MIN'?Math.min(...vals):Math.max(...vals);if(!Number.isSafeInteger(value))throw new Error(step.op+' exceeded exact integer range.');break;}case 'SORT':if(!Array.isArray(value))throw new Error('SORT requires array.');value=[...value].sort((a,b)=>stable(a).localeCompare(stable(b)));break;case 'UNIQUE':if(!Array.isArray(value))throw new Error('UNIQUE requires array.');{const seen=new Set();value=value.filter(v=>{const k=stable(v);if(seen.has(k))return false;seen.add(k);return true;});}break;case 'HASH_SHA256':value=await sha256(value);break;case 'REGEX':{const input=String(value),r=safeRegex(String(step.pattern||''),String(step.flags||''),input);lastRegex=r;value=r.test(input);break;}case 'COMPARE':{const other=Object.prototype.hasOwnProperty.call(step,'value')?step.value:source[step.binding]?.value;value=stable(comparable(value))===stable(comparable(other));break;}case 'BYTE_COMPARE':{const a=bytesOf(value),b=bytesOf(source[step.binding]?.bytes);if(!a||!b)throw new Error('BYTE_COMPARE requires bytes.');value=a.byteLength===b.byteLength&&a.every((x,j)=>x===b[j]);break;}case 'ASSERT_EQ':{if((typeof value==='number'&&!Number.isInteger(value))||(typeof step.value==='number'&&!Number.isInteger(step.value)))throw new Error('Precision-sensitive exact decimal equality must use exact decimal strings.');assertion=assertResult(stable(comparable(value))===stable(comparable(step.value)),step.value,comparable(value),step.message);break;}case 'ASSERT_GT':case 'ASSERT_GTE':case 'ASSERT_LT':case 'ASSERT_LTE':{const c=numericCompare(value,step.value,step),ok=step.op==='ASSERT_GT'?c>0:step.op==='ASSERT_GTE'?c>=0:step.op==='ASSERT_LT'?c<0:c<=0;assertion=assertResult(ok,step.op+' '+step.value,value,step.message);break;}case 'ASSERT_MATCH':{const input=String(value),r=lastRegex||safeRegex(String(step.pattern||''),String(step.flags||''),input);assertion=assertResult(r.test(input),String(r),value,step.message);break;}case 'ASSERT_CONTAINS':{const ok=Array.isArray(value)?value.some(v=>stable(v)===stable(step.value)):String(value).includes(String(step.value));assertion=assertResult(ok,'contains '+stable(step.value),value,step.message);break;}case 'ASSERT_NOT_CONTAINS':{const ok=Array.isArray(value)?!value.some(v=>stable(v)===stable(step.value)):!String(value).includes(String(step.value));assertion=assertResult(ok,'does not contain '+stable(step.value),value,step.message);break;}case 'ASSERT_SET_EQUAL':{if(!Array.isArray(value)||!Array.isArray(step.value))throw new Error('ASSERT_SET_EQUAL requires arrays.');const a=[...new Set(value.map(stable))].sort(),b=[...new Set(step.value.map(stable))].sort();assertion=assertResult(stable(a)===stable(b),step.value,value,step.message);break;}}if(assertion){observations.push({step:i,op:step.op,...assertion});if(assertion.determination==='VIOLATED')return {status:'COMPLETE',determination:'VIOLATED',expected:assertion.expected,actual:assertion.actual,observations,runtimeVersion:VERSION,specVersion:SPEC_VERSION};}}const last=[...observations].reverse().find(x=>x.determination);return {status:'COMPLETE',determination:last?.determination||'UNDETERMINED',expected:last?.expected??null,actual:last?.actual??value,observations,executorVersion:VERSION,runtimeVersion:VERSION,specVersion:SPEC_VERSION};}\nfunction executeTest(test,artifacts,canonicalBindings={},options={}){const spec=field(test,'EXECUTABLE_SPEC');if(typeof Worker==='undefined'||options.direct===true)return execute({spec,artifacts});const workerUrl=options.workerUrl||('test-worker.js'+(options.buildIdentity?'?v='+encodeURIComponent(options.buildIdentity):''));return new Promise((resolve,reject)=>{const worker=new Worker(workerUrl);const requestId=crypto.randomUUID?crypto.randomUUID():String(Date.now())+Math.random();const timer=setTimeout(()=>{worker.terminate();const e=new Error('Deterministic worker execution exceeded duration limit.');e.code='EXECUTION_TIMEOUT';reject(e);},LIMITS.maxWorkerDurationMs);worker.onmessage=e=>{if(e.data?.requestId!==requestId)return;clearTimeout(timer);worker.terminate();resolve(e.data.result);};worker.onerror=e=>{clearTimeout(timer);worker.terminate();reject(new Error(e.message||'Worker execution failed.'));};worker.postMessage({requestId,spec,artifacts,canonicalBindings});});}\nglobalThis.closedLoopTestRuntime=Object.freeze({VERSION,SPEC_VERSION,CAPABILITY,OPS,LIMITS,validateSpec,validateBindings,supports,execute,executeTest,capabilities:()=>Object.freeze([CAPABILITY])});\n})();\n`;
write('test-runtime.js',runtime);

const worker=`'use strict';\ntry{self.fetch=undefined;}catch{}\ntry{self.XMLHttpRequest=undefined;}catch{}\ntry{self.WebSocket=undefined;}catch{}\nconst token=new URL(self.location.href).searchParams.get('v');\nimportScripts('test-runtime.js'+(token?'?v='+encodeURIComponent(token):''));\nself.onmessage=async event=>{const {requestId,spec,artifacts}=event.data||{};try{const result=await self.closedLoopTestRuntime.execute({spec,artifacts});self.postMessage({requestId,result});}catch(error){self.postMessage({requestId,result:{status:'EXECUTION_FAILED',determination:'UNDETERMINED',error:String(error?.message||error),runtimeVersion:self.closedLoopTestRuntime?.VERSION||'UNKNOWN'}});}};\n`;
write('test-worker.js',worker);

// 4) Engine: add application-owned manifests and exact native routing action.
let engine=read('workflow-engine.js');
engine=once(engine,"  'history','newJobResets','reviews','recoveredProjects','responseDispositions','executionFailures'","  'history','newJobResets','reviews','recoveredProjects','responseDispositions','executionFailures','intakeManifests','obligationManifests','promptContextManifests','blindAliasMaps','nativeExecutionEvents','importedOriginalPayloads'",'durable metadata collections');
engine=once(engine,"actionMap={APPLICATION_DETERMINISTIC:'NO_ACTION'","actionMap={APPLICATION_DETERMINISTIC:'RUN_IN_APP'",'native operator action');
const manifestFns=`\nfunction splitInputUnits(value){if(value===undefined||value===null)return [];if(typeof value!=='string')return [JSON.stringify(value)];return value.split(/\\r?\\n+/).map(x=>x.trim()).filter(Boolean);}\nfunction intakeCoverageManifest(project){ensureShape(project);const inputVersion=String(project.job.CURRENT_INPUT_VERSION||'UNKNOWN'),units=[];for(const [name,def] of Object.entries(schema.JOB_FIELDS||{})){if(!['HUMAN','HUMAN_DECISION'].includes(def.producer))continue;for(const [index,raw] of splitInputUnits(project.job?.[name]).entries()){const rawValue=String(raw),rawValueSha256=hash.sha256Text(rawValue),unitId='INPUT-UNIT-'+hash.sha256Value([inputVersion,name,index,rawValueSha256]).slice(0,20).toUpperCase();units.push({unitId,inputVersion,sourcePath:'/job/'+name,sourceIndex:index,rawValue,rawValueSha256});}}const manifestId='INTAKE-'+hash.sha256Value({inputVersion,units:units.map(x=>[x.unitId,x.rawValueSha256])}).slice(0,24).toUpperCase();return {schema:'closed-loop-intake-manifest/1',manifestId,inputVersion,units,unitCount:units.length,coverageRequired:units.length};}\nfunction obligationManifest(project){ensureShape(project);const currentIntake=intakeCoverageManifest(project),items=[];const add=(kind,sourceId,location,text,provenance={})=>{for(const [index,raw] of splitInputUnits(text).entries()){const value=String(raw),rawValueSha256=hash.sha256Text(value),obligationId='OBL-'+hash.sha256Value([kind,sourceId,location,index,rawValueSha256]).slice(0,20).toUpperCase();items.push({obligationId,kind,sourceId,location,index,text:value,rawValueSha256,provenance});}};for(const u of currentIntake.units)add('HUMAN_INPUT',u.unitId,u.sourcePath,u.rawValue,{inputVersion:u.inputVersion,intakeUnitId:u.unitId});for(const name of ['EXACT_DELIVERABLE_REQUESTED','INPUT_SET_CONTENTS'])add('STAGE01_JOB_DEFINITION','STAGE-01','/job/'+name,project.job?.[name],{inputVersion:currentIntake.inputVersion});for(const r of recordsForCurrentScope(project,'candidateRequirements'))add('EXTERNAL_CANDIDATE_OBLIGATION',recordId(r,'candidateRequirements'),String(recordValue(r,'SOURCE_LOCATION')||''),recordValue(r,'CANDIDATE_OBLIGATION'),{sourceId:String(recordValue(r,'SOURCE_ID')||r.relationships?.SOURCE_ID||'')});for(const r of recordsForCurrentScope(project,'research'))for(const f of ['MANDATORY_STATEMENTS','RECOMMENDATIONS','OPTIONAL_PRACTICES','PROHIBITIONS','EXCEPTIONS','DEPENDENCIES','APPLICABILITY_FACTS','RESTRICTIONS','INVALIDATING_MATERIAL'])add('SOURCE_RESEARCH',recordId(r,'research'),String(recordValue(r,'EXACT_PORTION_EXAMINED')||''),recordValue(r,f),{researchField:f,sourceId:String(recordValue(r,'SOURCE_ID')||r.relationships?.SOURCE_ID||'')});const scope=currentScope(project),manifestId='OBLIGATION-'+hash.sha256Value({scope,items:items.map(x=>[x.obligationId,x.rawValueSha256])}).slice(0,24).toUpperCase();return {schema:'closed-loop-obligation-manifest/1',manifestId,scope,items,itemCount:items.length,coverageRequired:items.length};}\nfunction accountingClosure(manifest,accounting,stage){const expected=new Set((stage===1?manifest.units:manifest.items).map(x=>stage===1?x.unitId:x.obligationId)),seen=new Set(),issues=[];const rows=Array.isArray(accounting?.entries)?accounting.entries:[];for(const row of rows){const id=String(row?.inputId||'');if(!expected.has(id))issues.push('Unknown accounting identity '+id+'.');if(seen.has(id))issues.push('Duplicate accounting identity '+id+'.');seen.add(id);}for(const id of expected)if(!seen.has(id))issues.push('Missing accounting identity '+id+'.');return {complete:issues.length===0,covered:seen.size,total:expected.size,coverage:expected.size?seen.size/expected.size:1,issues};}\n`;
engine=once(engine,"function operationalMetrics(project){",manifestFns+"\nfunction operationalMetrics(project){",'manifest functions');
engine=once(engine,"executionHandoff,evaluateContextIndependence","executionHandoff,intakeCoverageManifest,obligationManifest,accountingClosure,evaluateContextIndependence",'manifest exports');
engine=once(engine,"addHistory(project,'APPLICATION_TEST_EXECUTED',{stage:22,recordId:recordId(result,'deterministicResults'),testId:tid,productId:pid,determination,evidenceId:recordId(evidence,'evidenceRecords'),testSpecSha256:specSha256,inputArtifactIds:artifactIdentities.map(x=>x.artifactId)});","project.projectData.nativeExecutionEvents.push({executionEventId:allocateInfrastructureId(project,'NATIVE-EXEC','nativeExecutionEvents'),stage:22,testId:tid,productId:pid,testSpecSha256:specSha256,runtimeVersion:evidencePayload.runtimeVersion,inputArtifactIds:artifactIdentities.map(x=>x.artifactId),inputArtifactSha256Values:artifactIdentities.map(x=>x.sha256),determination,evidenceId:recordId(evidence,'evidenceRecords'),projectRevision:Number(project.revision||0),eventSequence:Number(project.projectData.eventSequence||0)+1,deviceTimestamp:now()});addHistory(project,'APPLICATION_TEST_EXECUTED',{stage:22,recordId:recordId(result,'deterministicResults'),testId:tid,productId:pid,determination,evidenceId:recordId(evidence,'evidenceRecords'),testSpecSha256:specSha256,inputArtifactIds:artifactIdentities.map(x=>x.artifactId)});",'native execution event');
write('workflow-engine.js',engine);

// 5) Prompt authority: remove every project-subject runtime branch and make capture-once an explicit invariant in every prompt.
let prompt=read('prompt-engine.js');
prompt=prompt.replace("const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine/26';","const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine/27';");
const domainStart='${stage===1?`STAGE 01 DOMAIN INTAKE ADAPTATION';
const clarificationStart='${stage===1?`STAGE 01 CLARIFICATION EXPERIENCE';
prompt=between(prompt,domainStart,clarificationStart,"${`SUBJECT-NEUTRAL PROJECT ADAPTATION\nInfer only the subject-specific facts, terminology, authorities, artifacts, tools, interfaces, and human-only decisions that are actually implied by this current project's human input, supplied materials, and canonical context. Do not select behavior from a hard-coded subject catalogue. Do not ask the human for a fact already supplied or reliably obtainable from authorized materials, tools, or research. Do not perform work belonging to a later stage.\n`}\n\n",'hard-coded domain prompt block');
prompt=prompt.replace(/For PATENT \/ REGULATED FILING jobs,[\s\S]*?Do not turn researchable legal strategy into a human question\.\n/,'');
const memory=`PROJECT MEMORY — CAPTURE ONCE, REUSE EVERYWHERE\nEvery project-relevant fact, requirement, constraint, decision, prohibition, material reference, artifact identity, and human answer already present in current canonical context is available project memory. Use it. NEVER ask the human to retype, resend, reattach, restate, or summarize information merely because this is a later stage, a new prompt, or a different external-agent conversation. If required information is present anywhere in the authorized current context, consume it directly. Ask the human only for a genuinely human-only fact or decision that is not already represented and cannot be established from authorized materials, tools, or research. If a required earlier-stage capture is missing or incomplete, return BLOCKED with MISSING_APPLICATION_CONTEXT or INADEQUATE_PRIOR_OUTPUT and identify the responsible earlier stage; do not convert an application capture defect into a repeat request to the human. A later human correction creates a new input version; it does not erase the preserved prior history.\n\n`;
prompt=once(prompt,'JOB CONTROL\n',memory+'JOB CONTROL\n','global capture-once rule');
const bounded='AUTHORIZED BOUNDED CONTEXT FOR THIS STAGE\n';
const accountingPrompt=`${stage===1?`APPLICATION-OWNED INTAKE COVERAGE MANIFEST\n${show(workflow.intakeCoverageManifest(state))}\nClassify EVERY listed unit exactly once in response.accounting.entries. Omission is a validation failure. Allowed dispositions: INCORPORATED, RETAINED_CONTEXT, UNRESOLVED_HUMAN_ONLY, LATER_RESOLVABLE, INAPPLICABLE. Do not invent IDs.\n\n`:''}${stage===4?`APPLICATION-OWNED OBLIGATION MANIFEST\n${show(workflow.obligationManifest(state))}\nAccount for EVERY listed obligation exactly once in response.accounting.entries. Omission is a validation failure. Allowed dispositions: REQUIREMENT, RETAINED_CONTEXT, INAPPLICABLE, BLOCKED. REQUIREMENT entries must identify one or more requirementTempKeys proposed in this same response. Do not rediscover the input universe and do not ask the human to resend it.\n\n`:''}`;
prompt=once(prompt,bounded,accountingPrompt+bounded,'accounting manifests in prompts');
prompt=prompt.replaceAll('EXECUTABLE_KIND = CUSTOM_PIPELINE','EXECUTABLE_KIND = TEST_IR').replaceAll('CUSTOM_PIPELINE','TEST_IR');
prompt=prompt.replace('Supported operations: ${schema.TEST_IR.operations.join(\', \')}','Supported operations: ${schema.TEST_IR.operations.join(\', \')}\nOperation contracts are closed: unknown operation properties are invalid. PARSE_CSV requires delimiter, header, quote, newline, and encoding=UTF-8. SELECT_JSON_PATH and SELECT_XML use only the registered restricted selector subsets. Runtime limits: ${show(schema.TEST_IR.limits)}');
write('prompt-engine.js',prompt);

// 6) Ingestion: explicit /3 accounting closure before any canonical mutation.
let ingestion=read('response-ingestion.js');
ingestion=once(ingestion,"'warnings','attachments']","'warnings','attachments','accounting']",'accounting top-level key');
ingestion=once(ingestion,"const WARNING_KEYS=Object.freeze(['code','message','path']);","const WARNING_KEYS=Object.freeze(['code','message','path']);\nconst ACCOUNTING_KEYS=Object.freeze(['manifestId','entries']);\nconst ACCOUNTING_ENTRY_KEYS=Object.freeze(['inputId','disposition','requirementTempKeys','reason']);\nconst INTAKE_DISPOSITIONS=Object.freeze(['INCORPORATED','RETAINED_CONTEXT','UNRESOLVED_HUMAN_ONLY','LATER_RESOLVABLE','INAPPLICABLE']);\nconst OBLIGATION_DISPOSITIONS=Object.freeze(['REQUIREMENT','RETAINED_CONTEXT','INAPPLICABLE','BLOCKED']);",'accounting constants');
const validationInsert=`\n  if([1,4].includes(stageNumber)&&envelope.responseType==='DATA_PROPOSAL'){\n    const manifest=stageNumber===1?workflow.intakeCoverageManifest(project):workflow.obligationManifest(project),accounting=envelope.accounting,path='/accounting';\n    if(!object(accounting)){issues.push(issue('MISSING_ACCOUNTING',path,'Stage '+stageNumber+' requires complete application-manifest accounting.'));}\n    else{unknownKeys(accounting,ACCOUNTING_KEYS,path,issues);if(String(accounting.manifestId||'')!==String(manifest.manifestId))issues.push(issue('WRONG_ACCOUNTING_MANIFEST',path+'/manifestId','Accounting manifest identity does not match the current application manifest.'));if(!Array.isArray(accounting.entries))issues.push(issue('INVALID_ACCOUNTING_ENTRIES',path+'/entries','accounting.entries must be an array.'));else{const allowed=stageNumber===1?INTAKE_DISPOSITIONS:OBLIGATION_DISPOSITIONS;accounting.entries.forEach((row,index)=>{const p=path+'/entries/'+index;if(!object(row)){issues.push(issue('INVALID_ACCOUNTING_ENTRY',p,'Accounting entry must be an object.'));return;}unknownKeys(row,ACCOUNTING_ENTRY_KEYS,p,issues);if(!String(row.inputId||'').trim())issues.push(issue('MISSING_ACCOUNTING_ID',p+'/inputId','inputId is required.'));if(!allowed.includes(row.disposition))issues.push(issue('INVALID_ACCOUNTING_DISPOSITION',p+'/disposition','Invalid accounting disposition.'));if(stageNumber===4&&row.disposition==='REQUIREMENT'&&(!Array.isArray(row.requirementTempKeys)||!row.requirementTempKeys.length))issues.push(issue('MISSING_REQUIREMENT_MAPPING',p+'/requirementTempKeys','REQUIREMENT disposition requires one or more requirementTempKeys.'));});const closure=workflow.accountingClosure(manifest,accounting,stageNumber);for(const message of closure.issues)issues.push(issue('INCOMPLETE_ACCOUNTING',path,message));}}\n  }\n`;
ingestion=once(ingestion,"  if(envelope.responseType==='HUMAN_INPUT_REQUIRED'){",validationInsert+"\n  if(envelope.responseType==='HUMAN_INPUT_REQUIRED'){",'accounting validation');
ingestion=once(ingestion,"attachments:clone(envelope.attachments||[])};","attachments:clone(envelope.attachments||[]),accounting:clone(envelope.accounting||null)};",'proposal preserves accounting');
write('response-ingestion.js',ingestion);

// 7) /2 -> /3 migration and original import preservation without creating competing active state.
let store=read('project-store.js');
store=once(store,"for(const source of legacy){const project=core.migrateState(clone(source));","for(const source of legacy){const original=clone(source),project=core.migrateState(clone(source));engine.ensureShape(project);project.projectData.importedOriginalPayloads.push({schema:String(original.schema||original.workflow||'UNKNOWN'),sha256:hash.sha256Value(original),payload:original,operational:false,importedAt:now()});",'legacy original preservation');
// The replacement above inserted ensureShape earlier; remove the now duplicated immediate ensureShape once.
store=store.replace("engine.ensureShape(project);project.projectData.importedOriginalPayloads.push({schema:String(original.schema||original.workflow||'UNKNOWN'),sha256:hash.sha256Value(original),payload:original,operational:false,importedAt:now()});engine.ensureShape(project);engine.recalculate(project);","engine.ensureShape(project);project.projectData.importedOriginalPayloads.push({schema:String(original.schema||original.workflow||'UNKNOWN'),sha256:hash.sha256Value(original),payload:original,operational:false,importedAt:now()});engine.recalculate(project);");
write('project-store.js',store);

// 8) Script graph/CSP. No style/layout bytes may change.
html=html.replace("connect-src 'self'; object-src","connect-src 'self'; worker-src 'self'; object-src");
const scriptNeedle='<script defer src="workflow-schema.js?v=';
const ix=html.indexOf(scriptNeedle);if(ix<0)throw new Error('workflow-schema script missing');
const match=html.slice(ix).match(/^<script defer src="workflow-schema\.js\?v=([^"]+)"><\/script>/);if(!match)throw new Error('workflow-schema token missing');const token=match[1];
const schemaTag=match[0];html=html.replace(schemaTag,schemaTag+`\n<script defer src="test-runtime.js?v=${token}"></script>`);
if(sha(html.match(/<style>[\s\S]*?<\/style>/)?.[0]||'')!==styleSha)throw new Error('Visual baseline changed: index style bytes differ.');
write('index.html',html);

// 9) CI proof order and machine report identities.
let ci=read('.github/workflows/pages.yml');
ci=ci.replace("const expected=['workbook.js','hash.js','workflow-schema.js','workflow-engine.js'","const expected=['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js'");
ci=ci.replaceAll("projectSchema:'closed-loop-project/2'","projectSchema:'closed-loop-project/3'").replaceAll("responseSchema:'closed-loop-stage-response/2'","responseSchema:'closed-loop-stage-response/3'");
ci=ci.replace("nativeTestSpecVersion:'closed-loop-test-spec/1',","nativeTestSpecVersion:'closed-loop-test-spec/1',\n            testIrSchema:'closed-loop-test-spec/1',\n            verificationPackageSchema:'closed-loop-verification-package/1',");
ci=ci.replace("      - name: Verify generic deterministic Test IR runtime\n        run: node verify-test-runtime.mjs","      - name: Verify controlling /3 specification invariants\n        run: node verify-controlling-spec.mjs\n      - name: Verify generic deterministic Test IR runtime\n        run: node verify-test-runtime.mjs");
ci=ci.replaceAll("node build-test-project.mjs && node verify-test-runtime.mjs", "node build-test-project.mjs && node verify-controlling-spec.mjs && node verify-test-runtime.mjs");
write('.github/workflows/pages.yml',ci);

// 10) Permanent targeted conformance/regression verifier. It deliberately checks invalid and repaired accounting closure.
const verifier=`import fs from 'node:fs';import vm from 'node:vm';import crypto from 'node:crypto';\nconst assert=(x,m)=>{if(!x)throw new Error(m)};const txt=p=>fs.readFileSync(p,'utf8');\nconst workbook=txt('workbook.js'),schema=txt('workflow-schema.js'),prompt=txt('prompt-engine.js'),runtime=txt('test-runtime.js'),worker=txt('test-worker.js'),html=txt('index.html'),engine=txt('workflow-engine.js'),ingestion=txt('response-ingestion.js'),ci=txt('.github/workflows/pages.yml');\nassert(workbook.includes(\"PROJECT_SCHEMA='closed-loop-project/3'\"),'project schema is not /3');assert(schema.includes(\"RESPONSE_SCHEMA='closed-loop-stage-response/3'\"),'response schema is not /3');assert(workbook.includes(\"'CORRECT THE ROOT CAUSE'\"),'Stage 16 visible label wrong');assert(!prompt.includes('PATENT / REGULATED FILING')&&!prompt.includes('SOFTWARE / MULTI-FILE SYSTEM')&&!prompt.includes('BUILDING / ARCHITECTURE / AEC')&&!prompt.includes('PHYSICAL / MECHANICAL / CAD'),'runtime prompt contains prohibited domain catalogue');assert(prompt.includes('PROJECT MEMORY — CAPTURE ONCE, REUSE EVERYWHERE'),'capture-once invariant absent');assert(prompt.includes('APPLICATION-OWNED INTAKE COVERAGE MANIFEST')&&prompt.includes('APPLICATION-OWNED OBLIGATION MANIFEST'),'accounting manifests absent from prompt');assert(prompt.includes('do not ask the human to resend it')||prompt.includes('do not ask the human to resend'),'Stage 4 reuse rule absent');assert(![schema,prompt,runtime].some(x=>x.includes('CUSTOM_PIPELINE')),'CUSTOM_PIPELINE remains');assert(schema.includes(\"['NONE','TEST_IR']\"),'TEST_IR kind enum wrong');for(const op of ['PARSE_XML','SELECT_XML','BYTE_COMPARE','ASSERT_SET_EQUAL'])assert(runtime.includes(\"'\"+op+\"'\"),'missing Test IR op '+op);for(const prohibited of ['ASSERT_EXISTS','ASSERT_TYPE','ASSERT_NE'])assert(!runtime.includes(\"'\"+prohibited+\"'\"),'unrequested second-language primitive remains '+prohibited);assert(runtime.includes('maxWorkerDurationMs')&&runtime.includes('maxRegexInputLength')&&runtime.includes('maxSelectorDepth')&&runtime.includes('maxParsedDepth'),'runtime limits incomplete');assert(worker.includes('self.fetch=undefined')&&worker.includes('self.XMLHttpRequest=undefined'),'worker network surface not closed');assert(html.includes(\"worker-src 'self'\"),'CSP does not permit same-origin worker');const src=[...html.matchAll(/<script\\s+defer\\s+src=\"([^\"]+)\"/g)].map(m=>m[1].split('?')[0]);assert(JSON.stringify(src)===JSON.stringify(['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js']),'runtime dependency order wrong');assert(html.includes('.prompt{height:clamp(260px,45vh,520px);')&&html.includes('.expandable-prompt{max-height:280px}'),'prompt box visual baseline changed');assert(engine.includes('intakeCoverageManifest')&&engine.includes('obligationManifest')&&engine.includes('accountingClosure'),'application accounting missing');assert(ingestion.includes('INCOMPLETE_ACCOUNTING')&&ingestion.includes('WRONG_ACCOUNTING_MANIFEST'),'ingestion does not fail closed on accounting');assert(ci.includes(\"projectSchema:'closed-loop-project/3'\")&&ci.includes(\"responseSchema:'closed-loop-stage-response/3'\"),'CI report still claims /2');console.log(JSON.stringify({controllingSpecV3:true,visualBaselinePreserved:true,stage01Accounting:true,stage04Accounting:true,captureOncePromptInvariant:true,subjectNeutralPrompts:true,testIrSchema:'closed-loop-test-spec/1'}));\n`;
write('verify-controlling-spec.mjs',verifier);

// Cache identity must change because runtime files changed, but visual CSS remains byte-identical.
html=read('index.html');const bundleFiles=['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js'];const build='runtime-'+sha(bundleFiles.map(p=>read(p)).join('\n')).slice(0,16);html=html.replace(/(?:runtime-[a-f0-9]+)(?=<\/script>)/g,build).replace(/\?v=runtime-[a-f0-9]+/g,'?v='+build);if(sha(html.match(/<style>[\s\S]*?<\/style>/)?.[0]||'')!==styleSha)throw new Error('Visual baseline changed during cache token refresh.');write('index.html',html);

console.log(JSON.stringify({materialized:true,projectSchema:'closed-loop-project/3',responseSchema:'closed-loop-stage-response/3',styleSha256:styleSha,buildIdentity:build}));
