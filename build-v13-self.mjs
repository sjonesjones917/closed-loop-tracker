import fs from 'node:fs';

const BUILD_REVISION='2026-08-22-stage2-dedicated-external-research-wrapper-v5';

const replaceBetween=(text,start,end,replacement)=>{
  const a=text.indexOf(start),b=text.indexOf(end,a);
  if(a<0||b<0)throw new Error(`Patch anchor missing: ${start} / ${end}`);
  return text.slice(0,a)+replacement+text.slice(b);
};

function build(sidecarPath,output){
  let s=fs.readFileSync('app-v11.html','utf8');

  s=replaceBetween(s,'function prior(p,n){','function batchDigest(s){',`function prior(p,n){if(n===1)return'NONE';if(n===2)return\`===== STAGE 1 — DEFINE JOB =====\\nUse the job definition represented by the exact objective, exact requested deliverable, constraints/prohibitions, required output form, applicable domains, and external research questions. Do not carry implementation files, generated artifacts, project JSON, tests, source code, candidate files, or other workflow work products forward as research authority.\`;return p.stages.slice(0,n-1).map((s,i)=>{const batch=batchDigest(s);return \`===== STAGE \${i+1} — \${STAGES[i][0]} =====\\n\${s.response||'NO STAGE SUMMARY'}\${batch?\`\\n\\nBATCH RECORDS\\n\${batch}\`:''}\\n\\nEVIDENCE / NOTES\\n\${s.notes||'NONE'}\`}).join('\\n\\n')}\n`);

  s=replaceBetween(s,'function prompt(p,n){','function runPrompt(p,n,i,role){',`function researchPrompt(p,n){gate(p,n);const stageName=STAGES[n-1][0],stageInstruction=STAGES[n-1][1],inventory=n===3?String(p.stages[1]?.response||'').trim():'';if(n===3&&!inventory)throw Error('Stage 3 is blocked until Stage 2 contains a completed external source inventory.');const handoff=n===2?'RAW STAGE 1 RESPONSE AND SUPPLIED IMPLEMENTATION FILE LIST OMITTED BY DESIGN. Use only the user objective, requested deliverable, explicit constraints, output form, temporal scope, and research questions to define the research scope.':'APPROVED STAGE 2 EXTERNAL SOURCE INVENTORY:\\n'+inventory;return\`CLOSED-LOOP AGENT RELIABILITY WORKFLOW\\n\\nJOB_ID: \${p.jobId}\\nPROJECT_ID: \${p.projectId}\\nPROJECT: \${p.name}\\n\\nSTAGE \${n} OF 31 — \${stageName}\\n\\nSTAGE \${n} RESEARCH AUTHORITY BOUNDARY — EXTERNAL SOURCES ONLY\\n\\nUSER JOB DEFINITION — SCOPE, NOT EXTERNAL RESEARCH AUTHORITY\\nEXACT USER OBJECTIVE:\\n\${p.objective}\\n\\nEXACT DELIVERABLE REQUESTED:\\n\${p.deliverable}\\n\\nEXPLICIT CONSTRAINTS / PROHIBITED ACTIONS:\\n\${p.constraints||'NONE SUPPLIED'}\\n\\nREQUIRED OUTPUT FORM:\\n\${p.format}\\n\\nTEMPORAL SCOPE:\\n\${p.deadline}\\n\\nRESEARCH INPUT BOUNDARY\\n\${handoff}\\n\\nEXECUTE THIS STAGE NOW\\n\${stageInstruction}\\n\\nRESEARCH RULES\\n- Search the internet and other external authoritative information sources now.\\n- Find and actually examine independent official websites, government authorities, statutes, regulations, case law, standards, specifications, official platform and API documentation, manufacturer documentation, books, libraries, academic literature, peer-reviewed literature, professional references, public databases, patent databases, policies, and other external authorities appropriate to this job.\\n- Prefer primary sources, current official documentation, and controlling authority.\\n- User input defines the requested result and research scope; it does not automatically establish external truth.\\n- Do not inspect, inventory, cite, or treat supplied implementation files as research sources.\\n- Do not inspect, inventory, cite, or treat application HTML, JavaScript, source code, tests, generated prompts, generated project JSON, candidate files, prior workflow outputs, project records, implementation behavior, or any other work product of this job as research authority.\\n- The requested product may not exist yet. Establish externally governed requirements first; later stages may inspect produced artifacts only as verification subjects.\\n- Do not invent missing facts. A mandatory unavailable authority or fact is BLOCKED.\\n- Do not perform a later stage.\\n- Preserve exact external provenance and identifiers.\\n\\nRETURN ONLY THE COMPLETED STAGE \${n} RESULT.\`}\nfunction prompt(p,n){gate(p,n);if(n===2||n===3)return researchPrompt(p,n);return\`CLOSED-LOOP AGENT RELIABILITY WORKFLOW\\n\\nJOB_ID: \${p.jobId}\\nPROJECT_ID: \${p.projectId}\\nPROJECT: \${p.name}\\n\\nEXACT USER OBJECTIVE:\\n\${p.objective}\\n\\nEXACT DELIVERABLE REQUESTED:\\n\${p.deliverable}\\n\\nSUPPLIED FILES / MESSAGES / LINKS / DATA:\\n\${p.inputs||'NONE SUPPLIED'}\\n\\nCONSTRAINTS / PROHIBITED ACTIONS:\\n\${p.constraints||'NONE SUPPLIED'}\\n\\nREQUIRED OUTPUT FORMAT:\\n\${p.format}\\n\\nDEADLINE / TEMPORAL SCOPE:\\n\${p.deadline}\\n\\nSTAGE \${n} OF 31 — \${STAGES[n-1][0]}\\n\\nEXECUTE THIS STAGE NOW\\n\${STAGES[n-1][1]}\\n\\nRULES\\n- Execute the operation; do not merely describe how.\\n- Perform all AI-capable work available to you.\\n- Use actual supplied files/tools/sources when required.\\n- Do not invent missing facts. A mandatory unavailable fact is BLOCKED.\\n- Do not perform a later stage.\\n- Preserve exact evidence and identifiers.\\n\\nPRIOR COMPLETED HANDOFF\\n\${prior(p,n)}\\n\\nRETURN ONLY THE COMPLETED STAGE RESULT.\`}\n`);

  s=replaceBetween(s,'function runPrompt(p,n,i,role){','function requireText(t,label,min=100){',`function runPrompt(p,n,i,role,targetOverride=''){gate(p,n);const rid=\`RUN-\${String(i+1).padStart(3,'0')}\`,base=prompt(p,n);let target='';if(role==='verifier'){if(n===12)target=p.stages[10].producers[i]||'';else if(n===19)target=p.stages[17].producers[i]||'';else if(n===20)target=targetOverride||p.stages[19].producers[i]||'';if(!String(target).trim())throw Error(\`Verifier prompt for \${rid} is blocked until that run's actual producer output exists.\`)}return \`\${base}\\n\\n\${role==='producer'?'INDEPENDENT EXECUTION':'INDEPENDENT VERIFICATION'} RECORD \${rid}\\n\${role==='producer'?\`Execute \${rid} in a fresh context. Do not read or reuse any other run output. Return this run's complete actual output and evidence.\`:\`Verify \${rid} independently against every mandatory requirement and approved test. Do not trust the producing run's conclusion.\\n\\nTARGET RUN OUTPUT TO VERIFY:\\n\${target}\`}\`}\n`);
  const oldRequire="function requireText(t,label,min=100){t=String(t||'').trim();if(t.length<min)throw Error(`${label} is required and must contain a substantive completed result (minimum ${min} characters).`);return t}";
  const newRequire="function requireText(t,label,min=100){const raw=String(t??''),checked=raw.trim();if(checked.length<min)throw Error(`${label} is required and must contain a substantive completed result (minimum ${min} characters).`);return raw}";
  if(!s.includes(oldRequire))throw new Error('requireText exact-byte preservation patch anchor missing');
  s=s.replace(oldRequire,newRequire);
  const oldHandler=`document.querySelectorAll('[data-rprompt]').forEach(b=>b.onclick=()=>{const [role,i]=b.dataset.rprompt.split(':');copyText(runPrompt(p,n,Number(i),role))});`;
  const newHandler=`document.querySelectorAll('[data-rprompt]').forEach(b=>b.onclick=()=>{const [role,i]=b.dataset.rprompt.split(':'),idx=Number(i),target=role==='verifier'&&n===20?(document.querySelector(\`[data-producer="\${idx}"]\`)?.value||''):'';try{copyText(runPrompt(p,n,idx,role,target))}catch(e){$('stageMsg').textContent=e.message;setStatus(e.message,true)}});`;
  if(!s.includes(oldHandler))throw new Error('Run-prompt click handler patch anchor missing');
  s=s.replace(oldHandler,newHandler);

  s=s.replaceAll('Closed-Loop Reliability v11','Closed-Loop Reliability v13')
    .replaceAll('Closed-Loop Agent Reliability v11','Closed-Loop Agent Reliability v13')
    .replaceAll('schemaVersion:11','schemaVersion:13')
    .replaceAll('schemaVersion!==11','schemaVersion!==13')
    .replaceAll('closedLoopReliability.projects.v11','closedLoopReliability.projects.v13')
    .replaceAll('closedLoopReliability.selected.v11','closedLoopReliability.selected.v13')
    .replaceAll('__CLR_V11__','__CLR_V13__')
    .replaceAll('valid v11 project export','valid v13 project export')
    .replaceAll('Load verified E2E export','Reload verified self-project export')
    .replaceAll('31 sequential operations · phone-first · every completion requires an actual pasted result · no seeded completed project','31 sequential operations · Stage 2 external-authority boundary · every completion requires an actual pasted result · no seeded completed project');

  const oldStage1="['DEFINE JOB','Read the original request and supplied inputs. Create the authoritative JOB RECORD and INPUT-v001 now. Preserve the exact objective and requested deliverable; inventory inputs, constraints, format, deadline, sources, tools, prohibited actions, explicit requirements, assumptions, and unknowns. Do not research or draft the final deliverable.'],";
  const newStage1="['DEFINE JOB','Define the real job now from USER INPUT only. Read the complete user request. Preserve the exact user objective and exact requested deliverable. Extract every explicit user requirement and prohibition, required output form, applicable domain or domains, known user-supplied facts, assumptions, unknowns, blockers, and the external research questions that must be answered later. Stage 1 is not research. Do not inspect generated implementation artifacts, candidate application files, project JSON, generated tests, or prior workflow work products to decide what the requested result should contain. Do not invent missing facts. Return the completed job definition.'],";
  const oldStage2="['INVENTORY SOURCES','Inspect the actual supplied sources and build the complete source inventory now. Record SOURCE_ID, description, type, origin, version/date, authority, SOURCE_ROLE, relevant portions, conflicts, and blockers. Use current authoritative research only when the job requires it.'],";
  const newStage2="['INVENTORY SOURCES','EXTERNAL RESEARCH ONLY. Build the EXTERNAL SOURCE inventory for this job now. Do not inventory, inspect, cite, or treat any supplied project file, application file, HTML, JavaScript, generated file, test, workflow output, project JSON, candidate artifact, or prior stage work product as a research source. Search outside this project now. Use the internet, official websites, government sources, statutes, regulations, case law, standards bodies, technical specifications, official platform and API documentation, manufacturer documentation, books, textbooks, libraries, academic papers, peer-reviewed literature, professional publications, public databases, patents/patent databases, policies, and other independent external authorities appropriate to the job domain. Actually open and examine the external sources; do not merely name sources you already know. Start only from the Stage 1 job definition and its external research questions. Determine what independent external authority controls correctness, what official standards/specifications apply, what laws/regulations apply, what technical documentation governs the required behavior, what established engineering/professional references apply, and what current external facts affect the job. Prefer primary, official, current, controlling sources. For every external source actually examined record SOURCE_ID, exact title, SOURCE_TYPE, author or issuing organization, publisher, URL or external location, publication date, effective date when applicable, access date, version/revision, authority level, SOURCE_ROLE, relevance to the job, exact relevant portion or section, primary/secondary status, conflicts, and blockers/additional research required. Record EXTERNAL_SEARCH_PERFORMED: true only after real external searching and examination occurred. Stage 2 is BLOCKED if a required research area has no appropriate external source and no explicit blocker. Return ONLY the external source inventory. Do not inspect app-v11.html, build-v13-self.mjs, self-browser-e2e.mjs, self-e2e-agent.mjs, app-v13-candidate1.html, app-v13.html, SELF_VERIFIED_PROJECT.json, or any other internal file as a source. Do not perform Stage 3 requirement extraction.'],";
  const oldStage3="['RESEARCH REQUIREMENTS','Read the controlling sources and extract every obligation, restriction, exception, dependency, applicability condition, format condition, numerical condition, and explicit user requirement now. Cite exact evidence and repeat until no new material category is found.'],";
  const newStage3="['RESEARCH REQUIREMENTS','Research the requirements governing this job from the Stage 2 EXTERNAL SOURCE inventory and additional external authoritative sources discovered as necessary. Search externally as needed. Do not derive requirements from application files, generated code, tests, project JSON, candidate implementations, generated prompts, prior workflow outputs, or other work products. The product may not exist yet. For every finding identify the external source and exact relevant portion, state what it requires or establishes, explain why it applies, distinguish mandatory requirements from recommendations, distinguish direct requirements from derived engineering implications, identify exceptions, conflicts, and applicability conditions, and preserve enough provenance for independent verification. Continue until every external research question from Stage 1 is answered or explicitly BLOCKED. Return research findings only; do not compile the Stage 4 requirement registry yet.'],";
  if(!s.includes(oldStage1)||!s.includes(oldStage2)||!s.includes(oldStage3))throw new Error('Stage 1-3 prompt patch anchors missing');
  s=s.replace(oldStage1,newStage1).replace(oldStage2,newStage2).replace(oldStage3,newStage3);

  const oldStage1Validation="if(n===1){has(U,/JOB_ID/,'Stage 1 must contain JOB_ID.');has(U,/INPUT-V?001/,'Stage 1 must create INPUT-v001.');if(!U.includes(p.objective.slice(0,Math.min(24,p.objective.length)).toUpperCase()))throw Error('Stage 1 must preserve content from the real project objective.');if(!U.includes(p.deliverable.slice(0,Math.min(24,p.deliverable.length)).toUpperCase()))throw Error('Stage 1 must preserve content from the real requested deliverable.')}";
  const newStage1Validation="if(n===1){has(U,/JOB_ID/,'Stage 1 must contain JOB_ID.');has(U,/EXPLICIT USER REQUIREMENTS/,'Stage 1 must record explicit user requirements.');has(U,/EXPLICIT PROHIBITIONS/,'Stage 1 must record explicit prohibitions.');has(U,/EXTERNAL RESEARCH QUESTIONS/,'Stage 1 must record external research questions without researching them yet.');has(U,/ASSUMPTIONS/,'Stage 1 must record assumptions.');has(U,/UNKNOWNS/,'Stage 1 must record unknowns.');has(U,/BLOCKERS/,'Stage 1 must record blockers.');if(!U.includes(p.objective.slice(0,Math.min(24,p.objective.length)).toUpperCase()))throw Error('Stage 1 must preserve content from the real project objective.');if(!U.includes(p.deliverable.slice(0,Math.min(24,p.deliverable.length)).toUpperCase()))throw Error('Stage 1 must preserve content from the real requested deliverable.')}";
  if(!s.includes(oldStage1Validation))throw new Error('Stage 1 validator patch anchor missing');
  s=s.replace(oldStage1Validation,newStage1Validation);

  const oldStage2Validation="if(n===2){has(U,/SOURCE_ID/,'Stage 2 must contain SOURCE_ID.');has(U,/SOURCE_ROLE/,'Stage 2 must contain SOURCE_ROLE.')}";
  const newStage2Validation="if(n===2){has(U,/SOURCE_ID/,'Stage 2 must contain SOURCE_ID.');has(U,/SOURCE_ROLE/,'Stage 2 must contain SOURCE_ROLE.');has(U,/EXTERNAL_SEARCH_PERFORMED\\s*[:=]\\s*TRUE/,'Stage 2 must affirm EXTERNAL_SEARCH_PERFORMED: true.');if(/SOURCE_TYPE\\s*[:=]\\s*(USER_INPUT|FILE|HTML|JAVASCRIPT|PROJECT_JSON|APPLICATION_FILE|GENERATED_FILE|WORK_PRODUCT)/i.test(t))throw Error('Stage 2 research sources must be independent external sources, not user input or project work products.');if(!/(https?:\\/\\/|DOI\\s*[:=]|ISBN\\s*[:=]|RFC\\s*[-:]?\\s*\\d+)/i.test(t))throw Error('Stage 2 must record externally identifiable source provenance such as a URL, DOI, ISBN, or RFC identifier.');}";
  if(!s.includes(oldStage2Validation))throw new Error('Stage 2 validator patch anchor missing');
  s=s.replace(oldStage2Validation,newStage2Validation);

  const keyAnchor="const KEY='closedLoopReliability.projects.v13',SEL='closedLoopReliability.selected.v13';";
  if(!s.includes(keyAnchor))throw new Error('v13 key anchor missing');
  s=s.replace(keyAnchor,`const SELF_PROJECT_PATH=${JSON.stringify(sidecarPath)};${keyAnchor}`);

  s=replaceBetween(s,"$('loadVerifiedBtn').onclick=async()=>{","$('exportBtn').onclick=",`async function loadSelfProject(manual=false){try{const r=await fetch(SELF_PROJECT_PATH,{cache:'no-store'});if(!r.ok){if(manual)throw Error(\`Verified self-project export unavailable (\${r.status}).\`);return false}importProjectObject(await r.json());if(manual)show('workflow');return true}catch(e){if(manual)setStatus(e.message,true);return false}};$('loadVerifiedBtn').onclick=()=>loadSelfProject(true);`);

  s=replaceBetween(s,'if(n===22){const m=t.match(/FINAL_ARTIFACT','if(n===29){',`if(n===22){const m=t.match(/FINAL_ARTIFACT\\s*:\\s*([\\s\\S]*)$/i);p.artifact=(m?m[1]:'').replace(/^\\n/,'');if(!p.artifact)throw Error('Stage 22 FINAL_ARTIFACT is empty.');const f=t.match(/ARTIFACT_NAME\\s*:\\s*([A-Za-z0-9_-]+(?:\\.[A-Za-z0-9_-]+)*)/i);if(f)p.artifactName=f[1]}`);

  const initAnchor='load();render();window.__CLR_V13__=';
  if(!s.includes(initAnchor))throw new Error('v13 initialization anchor missing');
  s=s.replace(initAnchor,'load();render();loadSelfProject(false);window.__CLR_V13__=');

  const researchStart=s.indexOf('function researchPrompt(p,n)');
  const researchEnd=s.indexOf('function prompt(p,n)',researchStart);
  const researchSlice=researchStart>=0&&researchEnd>researchStart?s.slice(researchStart,researchEnd):'';
  const dedicatedResearchPrompt=researchStart>=0&&s.includes('if(n===2||n===3)return researchPrompt(p,n)');
  const researchPromptOmitsSuppliedInputs=!researchSlice.includes('p.inputs')&&!researchSlice.includes('prior(p,n)');
  const legacyStage2InstructionAbsent=!s.includes('Inspect the actual supplied sources and build the complete source inventory now.');
  if(!dedicatedResearchPrompt)throw new Error('Dedicated Stage 2/3 research prompt was not generated.');
  if(!researchPromptOmitsSuppliedInputs)throw new Error('Stage 2/3 research prompt still receives supplied implementation inputs or raw prior handoff.');
  if(!legacyStage2InstructionAbsent)throw new Error('Legacy circular Stage 2 instruction remains in generated application.');

  fs.writeFileSync(output,s);
  return {output,bytes:Buffer.byteLength(s),buildRevision:BUILD_REVISION,sidecarPath,autoLoadsSidecar:s.includes('loadSelfProject(false)'),containsNoEmbeddedCompletedProject:!s.includes('HARDCODED_COMPLETED_PROJECT'),preservesArtifactName:s.includes('ARTIFACT_NAME'),nonCircularStage2:s.includes('EXTERNAL RESEARCH ONLY.'),stage2RejectsWorkProducts:s.includes('Stage 2 research sources must be independent external sources'),dedicatedResearchPrompt,researchPromptOmitsSuppliedInputs,legacyStage2InstructionAbsent};
}

const candidate=build('SELF_VERIFIED_PROJEC.json','app-v13-candidate1.html');
const corrected=build('SELF_VERIFIED_PROJECT.json','app-v13.html');
console.log(JSON.stringify({status:'BUILT',buildRevision:BUILD_REVISION,candidate,corrected},null,2));