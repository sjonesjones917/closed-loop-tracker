import fs from 'node:fs';
const ops=[["workflow-engine.js","function validateIntakeAccounting(project,content){const manifest=currentIntakeCoverageManifest(project),entries=parseAccountingContent(content),issues=[];if(!entries)return {valid:false,issues:['INTAKE_ACCOUNTING evidence content must be strict JSON containing an entries array.'],manifest};const allowed=new Set(['INCORPORATED','RETAINED_CONTEXT','UNRESOLVED_HUMAN_ONLY','LATER_RESOLVABLE','INAPPLICABLE']);const seen=new Set();for(const item of entries){if(!item||typeof item!=='object'){issues.push('Intake accounting entry must be an object.');continue;}if(!manifest.entries.some(x=>x.inputUnitId===item.inputUnitId)){issues.push('Unknown intake inputUnitId '+String(item.inputUnitId)+'.');continue;}if(seen.has(item.inputUnitId))issues.push('Duplicate intake accounting for '+item.inputUnitId+'.');seen.add(item.inputUnitId);if(!allowed.has(String(item.disposition||'').toUpperCase()))issues.push('Invalid intake disposition for '+item.inputUnitId+'.');if(String(item.disposition||'').toUpperCase()==='INAPPLICABLE'&&!String(item.reason||'').trim())issues.push('Inapplicable intake unit requires a reason: '+item.inputUnitId+'.');}for(const unit of manifest.entries)if(!seen.has(unit.inputUnitId))issues.push('Missing intake accounting for '+unit.inputUnitId+'.');return {valid:issues.length===0,issues,manifest,coverage:manifest.entries.length?seen.size/manifest.entries.length:1};}\n","function validateIntakeAccounting(project,content){const manifest=currentIntakeCoverageManifest(project),entries=parseAccountingContent(content),issues=[];if(!entries)return {valid:false,issues:['INTAKE_ACCOUNTING evidence content must be strict JSON containing an entries array.'],manifest};const allowed=new Set(['INCORPORATED','RETAINED_CONTEXT','UNRESOLVED_HUMAN_ONLY','LATER_RESOLVABLE','INAPPLICABLE']);const seen=new Set();for(const item of entries){if(!item||typeof item!=='object'){issues.push('Intake accounting entry must be an object.');continue;}if(!manifest.entries.some(x=>x.inputUnitId===item.inputUnitId)){issues.push('Unknown intake inputUnitId '+String(item.inputUnitId)+'.');continue;}if(seen.has(item.inputUnitId))issues.push('Duplicate intake accounting for '+item.inputUnitId+'.');seen.add(item.inputUnitId);const disposition=String(item.disposition||'').toUpperCase();if(!allowed.has(disposition))issues.push('Invalid intake disposition for '+item.inputUnitId+'.');if(disposition!=='INCORPORATED'&&!String(item.reason||'').trim())issues.push(disposition+' intake disposition requires a reason: '+item.inputUnitId+'.');}for(const unit of manifest.entries)if(!seen.has(unit.inputUnitId))issues.push('Missing intake accounting for '+unit.inputUnitId+'.');return {valid:issues.length===0,issues,manifest,coverage:manifest.entries.length?seen.size/manifest.entries.length:1};}\nfunction validateIntakeExhaustion(content){const issues=[];let parsed=null;try{parsed=JSON.parse(String(content||''));}catch{return {valid:false,issues:['INTAKE_EXHAUSTION evidence content must be strict JSON.']};}if(!parsed||typeof parsed!=='object'||Array.isArray(parsed))return {valid:false,issues:['INTAKE_EXHAUSTION evidence content must be one object.']};const allowedKeys=new Set(['status','blockingNow','askNowNonblocking','deferredHumanOnly','laterResolvable']);for(const key of Object.keys(parsed))if(!allowedKeys.has(key))issues.push('Unknown INTAKE_EXHAUSTION key '+key+'.');if(String(parsed.status||'').toUpperCase()!=='EXHAUSTED')issues.push('INTAKE_EXHAUSTION status must be EXHAUSTED.');for(const key of ['blockingNow','askNowNonblocking','deferredHumanOnly','laterResolvable'])if(!Array.isArray(parsed[key]))issues.push('INTAKE_EXHAUSTION '+key+' must be an array.');if(Array.isArray(parsed.blockingNow)&&parsed.blockingNow.length)issues.push('Stage 01 DATA_PROPOSAL cannot leave BLOCKING_NOW human-only questions unanswered.');if(Array.isArray(parsed.askNowNonblocking)&&parsed.askNowNonblocking.length)issues.push('Stage 01 DATA_PROPOSAL cannot leave ASK_NOW_NONBLOCKING questions unasked; ask them conversationally first, then move explicit unknown/deferred answers to deferredHumanOnly.');for(const item of Array.isArray(parsed.deferredHumanOnly)?parsed.deferredHumanOnly:[]){if(!item||typeof item!=='object'||!String(item.question||'').trim()||!String(item.humanDisposition||'').trim())issues.push('Each deferredHumanOnly entry must preserve the asked question and the human disposition.');}for(const item of Array.isArray(parsed.laterResolvable)?parsed.laterResolvable:[]){if(!item||typeof item!=='object'||!String(item.issue||'').trim()||!String(item.resolutionSource||'').trim())issues.push('Each laterResolvable entry must identify the issue and the non-human resolution source.');}return {valid:issues.length===0,issues,parsed};}\nfunction acceptedAccountingEvidence(project,stage,kind){const latest=acceptedChanges(project,stage).at(-1);if(!latest)return null;const proposal=safe(project.projectData.responseProposals).find(x=>x.proposalId===latest.proposalId&&x.status==='ACCEPTED');return safe(proposal?.envelope?.evidence).find(e=>upper(e?.kind)===upper(kind))||null;}\nfunction intakeClosureStatus(project){const accounting=acceptedAccountingEvidence(project,1,'INTAKE_ACCOUNTING'),exhaustion=acceptedAccountingEvidence(project,1,'INTAKE_EXHAUSTION'),accountingCheck=accounting?validateIntakeAccounting(project,accounting.content):{valid:false,issues:['Accepted Stage 01 response has no current INTAKE_ACCOUNTING evidence.']},exhaustionCheck=exhaustion?validateIntakeExhaustion(exhaustion.content):{valid:false,issues:['Accepted Stage 01 response has no INTAKE_EXHAUSTION evidence.']};return {complete:accountingCheck.valid&&exhaustionCheck.valid,accounting:accountingCheck,exhaustion:exhaustionCheck,reasons:[...accountingCheck.issues,...exhaustionCheck.issues]};}\nfunction stage3ExhaustionStatus(project){const reasons=[],data=project?.stages?.[3]?.agentData||project?.stages?.[3]?.acceptedData||{},noSource=upper(project?.stages?.[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE',sources=recordsForCurrentScope(project,'sources'),research=recordsForCurrentScope(project,'research');if(!noSource){const researched=new Set(research.map(record=>String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||''))),missing=sources.map(record=>recordId(record,'sources')).filter(id=>!researched.has(id));if(missing.length)reasons.push('Current Stage 03 research is missing source coverage for '+missing.join(', ')+'.');}if(!truth(data.ALL_KNOWN_CONTROLLING_SOURCES_EXAMINED))reasons.push('Stage 03 has not affirmatively established that all known controlling sources were examined.');if(!truth(data.SECOND_CONFLICT_AND_EXCEPTION_PASS_COMPLETED))reasons.push('Stage 03 has not completed the required second conflict and exception pass.');if(numeric(data.LATEST_PASS_NUMBER)<2)reasons.push('Stage 03 latest pass number must be at least 2 before research can be treated as exhausted.');if(!falsey(data.NEW_MATERIAL_CATEGORY_FOUND_IN_LATEST_PASS))reasons.push('Stage 03 latest pass must explicitly establish that no new material requirement category was found.');const gaps=upper(data.RESEARCH_GAPS_AND_BLOCKERS);if(gaps&&!['NONE','NOT APPLICABLE','N/A'].includes(gaps))reasons.push('Stage 03 still reports research gaps or blockers: '+String(data.RESEARCH_GAPS_AND_BLOCKERS)+'.');return {complete:reasons.length===0,reasons,sourceCount:sources.length,researchCount:research.length};}\n"],["workflow-engine.js","      const intakeManifest=currentIntakeCoverageManifest(project);if(!intakeManifest.entries.length||intakeManifest.coverage!==1)reasons.push('Stage 01 controlled-input accounting manifest is incomplete.');\n","      const intakeManifest=currentIntakeCoverageManifest(project);if(!intakeManifest.entries.length)reasons.push('Stage 01 controlled-input accounting manifest is empty or unavailable.');\n      if(hasAccepted){const closure=intakeClosureStatus(project);if(!closure.complete)reasons.push(...closure.reasons.map(reason=>'Stage 01 intake is not exhausted: '+reason));}\n"],["workflow-engine.js","      requireAccepted();const sourceIds=all('sources').map(record=>recordId(record,'sources')),noSource=upper(project.stages[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE';\n      if(!sourceIds.length){if(!noSource)reasons.push('Stage 03 cannot proceed without a current Stage 02 source set or valid no-source determination.');break;}\n      requireCount('research',1);const researched=new Set(collection('research').map(record=>String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||''))),missing=sourceIds.filter(id=>!researched.has(id));if(missing.length)reasons.push(`Research is missing for source(s): ${missing.join(', ')}.`);break;\n","      requireAccepted();const noSource=upper(project.stages[2]?.agentData?.SOURCE_APPLICABILITY_DETERMINATION)==='NO_APPLICABLE_EXTERNAL_SOURCE',sourceIds=recordsForCurrentScope(project,'sources').map(record=>recordId(record,'sources'));\n      if(!sourceIds.length&&!noSource)reasons.push('Stage 03 cannot proceed without a current Stage 02 source set or valid no-source determination.');\n      if(hasAccepted){const exhaustion=stage3ExhaustionStatus(project);if(!exhaustion.complete)reasons.push(...exhaustion.reasons);}\n      break;\n"],["workflow-engine.js","","      const stage1=gate(1,project),stage3=gate(3,project);if(!stage1.complete)reasons.push('Stage 04 cannot compile requirements because Stage 01 human-authority intake is not exhausted.');if(!stage3.complete)reasons.push('Stage 04 cannot compile requirements because Stage 03 source research is not exhausted.');\n"],["workflow-engine.js"," if(stage===4)return actionObject('CONTINUE_AGENT_CONVERSATION','Compile the complete requirement specification','Use the current Stage 04 instruction. It contains the application-enumerated obligation universe assembled from prior supplied and accepted project information. Do not attach, resend, retype, or summarize the original intent material.',{primaryButton:'Copy current instruction'});\n"," if(stage===4){const intake=gate(1,project),research=gate(3,project);if(!intake.complete||!research.complete)return actionObject('BLOCKED','Stage 04 is waiting for complete upstream capture','Stage 04 cannot be generated from partial upstream work. Finish Stage 01 human-authority intake and Stage 03 source-research exhaustion for the current scope. Do not reattach, retype, or resummarize the original intent; correct the responsible upstream stage instead.',{blockingReason:[...intake.reasons,...research.reasons].join('; '),newPromptRequired:true});return actionObject('CONTINUE_AGENT_CONVERSATION','Compile the complete requirement specification','Use the current Stage 04 instruction. It contains the application-enumerated obligation universe assembled from the exhausted Stage 01 intake and exhausted Stage 03 research. Do not attach, resend, retype, or summarize the original intent material.',{primaryButton:'Copy current instruction'});}\n"],["workflow-engine.js","globalThis.closedLoopWorkflowEngine=Object.freeze({actionObject,operationalNextActionText,buildIntakeCoverageManifest,currentIntakeCoverageManifest,buildObligationManifest,currentObligationManifest,refreshAccountingManifests,validateIntakeAccounting,validateObligationAccounting,testRuntimeCapabilities:()=>({capabilities:[...testRuntime.capabilities()],operations:[...testRuntime.OPS],limits:{...testRuntime.LIMITS},contracts:testRuntime.CONTRACTS}),recordMigratedAcceptedChange,\n","globalThis.closedLoopWorkflowEngine=Object.freeze({actionObject,operationalNextActionText,buildIntakeCoverageManifest,currentIntakeCoverageManifest,buildObligationManifest,currentObligationManifest,refreshAccountingManifests,validateIntakeAccounting,validateIntakeExhaustion,intakeClosureStatus,stage3ExhaustionStatus,validateObligationAccounting,testRuntimeCapabilities:()=>({capabilities:[...testRuntime.capabilities()],operations:[...testRuntime.OPS],limits:{...testRuntime.LIMITS},contracts:testRuntime.CONTRACTS}),recordMigratedAcceptedChange,\n"]];
for(const [file,oldText,newText] of ops){let s=fs.readFileSync(file,'utf8');if(!s.includes(oldText))throw new Error('Patch target missing in '+file+': '+oldText.slice(0,120));s=s.replace(oldText,newText);fs.writeFileSync(file,s);}
console.log('patch group applied',1,ops.length);
