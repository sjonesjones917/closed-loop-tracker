import fs from 'node:fs';
let s=fs.readFileSync('verify.mjs','utf8');s=s.replace("'PROMPT IDENTITY — ECHO EXACTLY'","'AUTHORITATIVE FILE-FIRST RESPONSE BINDING'");fs.writeFileSync('verify.mjs',s);
s=fs.readFileSync('verify-prompt-semantics.mjs','utf8');const a=`const identityMarker='\\n\\nPROMPT IDENTITY — ECHO EXACTLY\\n';\nconst markerIndex=promptIdentityRecord.prompt.indexOf(identityMarker);\nassert(markerIndex>0,'Generated prompt is missing its identity block.');\nconst exactBody=promptIdentityRecord.prompt.slice(0,markerIndex);\nconst embeddedBodySha256=(promptIdentityRecord.prompt.match(/BODY_SHA256:\\s*([0-9a-f]{64})/i)||[])[1];\nassert(embeddedBodySha256===promptIdentityRecord.bodySha256,'Embedded BODY_SHA256 differs from the prompt record bodySha256.');\nassert(globalThis.closedLoopHash.sha256Text(exactBody)===promptIdentityRecord.bodySha256,'bodySha256 does not hash the exact displayed and copied instruction body.');\nassert(globalThis.closedLoopHash.sha256Text(promptIdentityRecord.prompt)===promptIdentityRecord.fullTextSha256,'fullTextSha256 does not hash the exact complete prompt.');`;
const b=`assert(promptIdentityRecord.prompt.includes('AUTHORITATIVE FILE-FIRST RESPONSE BINDING'),'Generated prompt is missing its file-first identity instructions.');\nassert(!promptIdentityRecord.prompt.includes('BODY_SHA256:'),'Authoritative prompt contains a self-referential body hash.');\nassert(globalThis.closedLoopHash.sha256Text(promptIdentityRecord.prompt)===promptIdentityRecord.bodySha256,'bodySha256 does not hash the exact final instruction.txt bytes.');\nassert(promptIdentityRecord.fullTextSha256===promptIdentityRecord.bodySha256,'Prompt full-text and authoritative body identities diverge.');\nassert(promptIdentityRecord.prompt.endsWith('\\n')&&!promptIdentityRecord.prompt.endsWith('\\n\\n'),'Authoritative prompt must have exactly one final newline.');\nassert(!promptIdentityRecord.prompt.includes('\\r'),'Authoritative prompt must use LF line endings only.');`;
if(!s.includes(a))throw new Error('old identity verifier block missing');s=s.replace(a,b);fs.writeFileSync('verify-prompt-semantics.mjs',s);console.log('prompt verifier corrections applied');
