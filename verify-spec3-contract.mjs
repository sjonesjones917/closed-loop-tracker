import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';globalThis.Event=globalThis.Event||class Event{constructor(type){this.type=type}};globalThis.dispatchEvent=globalThis.dispatchEvent||(()=>true);for(const f of ['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js','prompt-engine.js'])vm.runInThisContext(fs.readFileSync(f,'utf8'),{filename:f});const core=closedLoopCore,schema=closedLoopWorkflowSchema,rt=closedLoopTestRuntime,engine=closedLoopWorkflowEngine,prompt=closedLoopPromptEngine;assert.equal(core.PROJECT_SCHEMA,'closed-loop-project/3');assert.equal(schema.RESPONSE_SCHEMA,'closed-loop-stage-response/3');assert.equal(core.WORKFLOW_ID,'mobile-closed-loop/30');assert.equal(core.STAGE_COUNT,30);assert.equal(core.STAGES[15].title,'CORRECT THE ROOT CAUSE');assert.equal(schema.JOB_FIELDS.JOB_TITLE.producer,'HUMAN_DECISION');assert.equal(schema.JOB_FIELDS.JOB_OWNER.producer,'HUMAN_DECISION');const applicationStageFields={4:['CONDITIONAL_REQUIREMENTS','OPTIONAL_REQUIREMENTS','BLOCKED_REQUIREMENTS'],6:['BLOCKED_MANDATORY_REQUIREMENTS'],12:['RUNS'],13:['REQUIREMENTS_SATISFIED_BY_ALL_TEN'],14:['CONFIRMED_ROOT_CAUSES','UNDETERMINED_ROOT_CAUSES','BLOCKED_ANALYSES'],15:['UNCONVERTED_CONFIRMED_DEFECTS'],17:['NEW_FROZEN_VERSIONS','OLD_CONVERSATIONS_CONTINUED','RUN_NAMESPACE','IDENTICAL_PACKAGE_CONFIRMED_FOR_ALL_RUNS','PRIOR_OUTPUTS_WITHHELD','EXECUTE_COMPLETED','VERIFY_COMPLETED','COMPARE_COMPLETED','ROOT_CAUSE_COMPLETED','REGRESSION_TESTS_ADDED','CORRECTIONS_COMPLETED'],19:['COMPLETE_TEST_SUITE_RUN','CROSS_RUN_COMPARISON_COMPLETED','NEW_CRITICAL_DEFECTS','NEW_MAJOR_DEFECTS','NEW_REQUIREMENTS_DISCOVERED','INJECTED_DEFECTS_NOT_DETECTED','NEW_CORRECTNESS_AFFECTING_VARIANCE','CONFIRMATION_ACCEPTANCE_CRITERIA_SATISFIED','REQUIRED_RETURN_STAGE'],22:['APPLICABLE_MANDATORY_DETERMINISTIC_TESTS','PRODUCT_REJECTED_BY_MANDATORY_FAILURE'],23:['EVALUATOR_INDEPENDENT_FROM_GENERATOR','UNSUPPORTED_BARE_CONCLUSIONS'],24:['REVIEWER_INDEPENDENT','ATTACKS_EXECUTED','UNDETERMINED_ATTACKS','REGRESSIONS_FOUND','RETURN_TO_ROOT_CAUSE_REQUIRED'],27:['CONTROLLING_DECISION_RULE','CONTROLLING_REASON'],30:['DEFECT_RECORDS_MISSING_REQUIRED_FIELDS']};for(const [stage,names] of Object.entries(applicationStageFields))for(const name of names)assert.equal(schema.STAGE_FIELDS[stage][name].producer,'APPLICATION',`Stage ${stage} ${name} must be application-owned.`);assert(rt.OPS.includes('PARSE_XML')&&rt.OPS.includes('SELECT_XML'));const html=fs.readFileSync('index.html','utf8');assert(html.includes('.expandable-prompt{height:280px;max-height:280px}.expandable-prompt.expanded{height:auto;max-height:none}'));assert(html.indexOf('workflow-schema.js')<html.indexOf('test-runtime.js')&&html.indexOf('test-runtime.js')<html.indexOf('workflow-engine.js'));assert(html.includes("worker-src 'self'"));const legacy=core.createBlankState('J');legacy.schema='closed-loop-project/2';legacy.projectData.extensionX={x:1};const migrated=core.migrateState(legacy);assert.equal(migrated.schema,'closed-loop-project/3');assert.deepEqual(migrated.projectData.extensionX,{x:1});const p=core.createBlankState('CAPTURE');Object.assign(p.job,{EXACT_USER_OBJECTIVE_VERBATIM:'Build the requested thing',EXPLICIT_USER_REQUIREMENTS:'Never ask me for the same project data twice',SUPPLIED_MATERIALS_INVENTORY:'intent.pdf',EXACT_DELIVERABLE_REQUESTED:'finished product',INPUT_SET_CONTENTS:'captured project requirements'});engine.ensureShape(p);engine.recalculate(p);const intake=engine.stage01IntakeManifest(p),ob=engine.stage04ObligationManifest(p);assert(intake.entries.length>=3);assert(ob.entries.some(x=>JSON.stringify(x.value).includes('Never ask me')));const pr=prompt.buildPromptRecord(4,p).prompt;for(const t of ['PROJECT DATA EXECUTION RULE — MANDATORY','APPLICATION-OWNED STAGE 04 OBLIGATION MANIFEST','Never ask the human','Never ask me for the same project data twice'])assert(pr.includes(t),t);console.log(JSON.stringify({projectSchema:core.PROJECT_SCHEMA,responseSchema:schema.RESPONSE_SCHEMA,stageCount:core.STAGE_COUNT,intake: intake.entries.length,obligations:ob.entries.length,visualBaselineRestored:true,testRuntimeLoaded:true}));