import fs from 'node:fs';const base=process.env.PAGE_URL;if(!base)throw Error('PAGE_URL is required');const paths=['index.html','app.js','app-core.js','workbook.js','workflow-model.js','workflow-engine.js','project-store.js','response-ingestion.js','prompt-engine.js','TEST_PROJECT.json'];for(const path of paths){const r=await fetch(new URL(`${path}?verify=${Date.now()}`,base),{cache:'no-store'});if(!r.ok)throw Error(`${path} ${r.status}`);const live=await r.text(),local=fs.readFileSync(path,'utf8');if(live!==local)throw Error(`${path} deployed bytes differ from verified source`)}const project=JSON.parse(fs.readFileSync('TEST_PROJECT.json','utf8'));if(project.jobId!=='JOB-20260823144121'||project.currentStage!==2||project.stageRecords['1'].status!=='COMPLETE')throw Error('Retained project source wrong');console.log(JSON.stringify({pageUrl:base,sourceIdentity:paths,retainedJob:project.jobId,currentStage:2},null,2));
