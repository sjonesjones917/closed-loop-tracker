import fs from 'node:fs';
import crypto from 'node:crypto';
const files={};
const read=p=>files[p]??=fs.readFileSync(p,'utf8');
const write=(p,s)=>{files[p]=s;fs.writeFileSync(p,s)};
const must=(s,needle,label)=>{if(!s.includes(needle))throw new Error('Missing anchor '+label+': '+needle);};
const rep=(s,a,b,label)=>{must(s,a,label);return s.replace(a,b)};

let w=read('workbook.js');
w=rep(w,"const PROJECT_SCHEMA='closed-loop-project/2';","const PROJECT_SCHEMA='closed-loop-project/3';\nconst LEGACY_PROJECT_SCHEMA='closed-loop-project/2';",'project schema');
w=rep(w,"'REVISE THE RESPONSIBLE LAYER'","'CORRECT THE ROOT CAUSE'",'stage16 title');
w=rep(w,"if(p.schema!=='human-project/30')throw new Error(`Unsupported project schema: ${p.schema||'MISSING'}`);","if(!['human-project/30',LEGACY_PROJECT_SCHEMA].includes(p.schema))throw new Error(`Unsupported project schema: ${p.schema||'MISSING'}`);",'migration source allowlist');
w=w.replaceAll("schema:'human-project/30'","schema:String(original.schema||'human-project/30')");
write('workbook.js',w);

let s=read('workflow-schema.js');
s=rep(s,"const RESPONSE_SCHEMA='closed-loop-stage-response/2';","const RESPONSE_SCHEMA='closed-loop-stage-response/3';",'response schema');
s=rep(s,"const HUMAN_JOB_FIELDS=Object.freeze([\n  'JOB_TITLE','JOB_OWNER','EXACT_USER_OBJECTIVE_VERBATIM'","const HUMAN_JOB_FIELDS=Object.freeze([\n  'EXACT_USER_OBJECTIVE_VERBATIM'",'job human fields');
s=rep(s,"const APPLICATION_JOB_FIELDS=Object.freeze([","const HUMAN_DECISION_JOB_FIELDS=Object.freeze(['JOB_TITLE','JOB_OWNER']);\nconst APPLICATION_JOB_FIELDS=Object.freeze([",'human decision job fields');
s=rep(s,"if(APPLICATION_JOB_FIELDS.includes(name))return field(name,PRODUCER.APPLICATION,{derivation:`Application derives ${name} from canonical project state.`});\n  if(HUMAN_JOB_FIELDS.includes(name))","if(APPLICATION_JOB_FIELDS.includes(name))return field(name,PRODUCER.APPLICATION,{derivation:`Application derives ${name} from canonical project state.`});\n  if(HUMAN_DECISION_JOB_FIELDS.includes(name))return field(name,PRODUCER.HUMAN_DECISION,{provenanceRequired:false,nullable:true});\n  if(HUMAN_JOB_FIELDS.includes(name))",'job decision producer');
s=rep(s,"[...new Set([...HUMAN_JOB_FIELDS,...APPLICATION_JOB_FIELDS,...AGENT_JOB_FIELDS])]","[...new Set([...HUMAN_JOB_FIELDS,...HUMAN_DECISION_JOB_FIELDS,...APPLICATION_JOB_FIELDS,...AGENT_JOB_FIELDS])]",'job field union');
s=rep(s,"executableKinds:Object.freeze(['NONE','CUSTOM_PIPELINE'])","executableKinds:Object.freeze(['NONE','TEST_IR'])",'test ir kind');
const oldOps="operations:Object.freeze(['LOAD_ARTIFACT','READ_BYTES','DECODE_UTF8','PARSE_JSON','PARSE_CSV','SELECT_JSON_PATH','COUNT','SUM','MIN','MAX','SORT','UNIQUE','HASH_SHA256','REGEX','COMPARE','BYTE_COMPARE','ASSERT_EXISTS','ASSERT_TYPE','ASSERT_EQ','ASSERT_NE','ASSERT_GT','ASSERT_GTE','ASSERT_LT','ASSERT_LTE','ASSERT_MATCH','ASSERT_CONTAINS','ASSERT_NOT_CONTAINS','ASSERT_SET_EQUAL'])";
const newOps="operations:Object.freeze(['LOAD_ARTIFACT','READ_BYTES','DECODE_UTF8','PARSE_JSON','PARSE_CSV','PARSE_XML','SELECT_JSON_PATH','SELECT_XML','COUNT','SUM','MIN','MAX','SORT','UNIQUE','HASH_SHA256','REGEX','COMPARE','ASSERT_EQ','ASSERT_GT','ASSERT_GTE','ASSERT_LT','ASSERT_LTE','ASSERT_MATCH','ASSERT_CONTAINS','ASSERT_NOT_CONTAINS','ASSERT_SET_EQUAL','BYTE_COMPARE'])";
s=rep(s,oldOps,newOps,'test ir operations');
s=rep(s,"if(String(get('EXECUTABLE_KIND')||'').toUpperCase()!=='CUSTOM_PIPELINE')issues.push('EXECUTABLE_KIND must be CUSTOM_PIPELINE.');","if(String(get('EXECUTABLE_KIND')||'').toUpperCase()!=='TEST_IR')issues.push('EXECUTABLE_KIND must be TEST_IR.');",'test ir validator kind');
s=rep(s,'      "EXECUTABLE_KIND",\n      "EXECUTABLE_SPEC_VERSION",\n      "EXECUTABLE_SPEC",','      "EXECUTABLE_KIND",\n      "EXECUTABLE_SPEC",','test agent version removal');
s=rep(s,'      "TEST_ID",\n      "REQ_ID",\n      "STATUS"','      "TEST_ID",\n      "REQ_ID",\n      "EXECUTABLE_SPEC_VERSION",\n      "EXECUTABLE_SPEC_SHA256",\n      "STATUS"','test application version/hash');
s=rep(s,"'TEST_ID','REQ_ID','TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','EXECUTABLE_KIND','EXECUTABLE_SPEC_VERSION','EXECUTABLE_SPEC','EXECUTABLE_INPUT_BINDINGS','INPUTS'","'TEST_ID','REQ_ID','TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','EXECUTABLE_KIND','EXECUTABLE_SPEC_VERSION','EXECUTABLE_SPEC_SHA256','EXECUTABLE_SPEC','EXECUTABLE_INPUT_BINDINGS','INPUTS'",'test fields hash');
s=rep(s,"    EXECUTABLE_SPEC_VERSION:Object.freeze({valueType:VALUE_TYPES.STRING,enumValues:[],nullable:true,normalizerKey:null,closedProperties:null}),","    EXECUTABLE_SPEC_VERSION:Object.freeze({valueType:VALUE_TYPES.STRING,enumValues:[],nullable:true,normalizerKey:null,closedProperties:null}),\n    EXECUTABLE_SPEC_SHA256:Object.freeze({valueType:VALUE_TYPES.STRING,enumValues:[],nullable:true,normalizerKey:null,closedProperties:null}),",'test hash type metadata');
write('workflow-schema.js',s);

let tr=read('test-runtime.js');
tr=rep(tr,"  'LOAD_ARTIFACT','READ_BYTES','DECODE_UTF8','PARSE_JSON','PARSE_CSV','SELECT_JSON_PATH',\n  'COUNT','SUM','MIN','MAX','SORT','UNIQUE','HASH_SHA256','REGEX','COMPARE','BYTE_COMPARE',\n  'ASSERT_EXISTS','ASSERT_TYPE','ASSERT_EQ','ASSERT_NE','ASSERT_GT','ASSERT_GTE','ASSERT_LT','ASSERT_LTE',\n  'ASSERT_MATCH','ASSERT_CONTAINS','ASSERT_NOT_CONTAINS','ASSERT_SET_EQUAL'","  'LOAD_ARTIFACT','READ_BYTES','DECODE_UTF8','PARSE_JSON','PARSE_CSV','PARSE_XML','SELECT_JSON_PATH','SELECT_XML',\n  'COUNT','SUM','MIN','MAX','SORT','UNIQUE','HASH_SHA256','REGEX','COMPARE',\n  'ASSERT_EQ','ASSERT_GT','ASSERT_GTE','ASSERT_LT','ASSERT_LTE','ASSERT_MATCH','ASSERT_CONTAINS','ASSERT_NOT_CONTAINS','ASSERT_SET_EQUAL','BYTE_COMPARE'",'runtime ops');
tr=rep(tr,"const LIMITS=Object.freeze({maxSteps:64,maxTextBytes:16*1024*1024,maxCollectionItems:100000,maxRegexLength:2000,maxCsvCells:250000});","const LIMITS=Object.freeze({maxInputBytes:16*1024*1024,maxDecompressedBytes:32*1024*1024,maxSteps:64,maxSelectorDepth:32,maxParsedDepth:64,maxCollectionItems:100000,maxRegexLength:2000,maxRegexInputBytes:4*1024*1024,maxCsvCells:250000,maxWorkerDurationMs:10000,maxArchiveExpansionBytes:32*1024*1024,maxTextBytes:16*1024*1024});",'runtime limits');
tr=rep(tr,"function parseCsv(text){\n  const rows=[];let row=[],cell='',quoted=false,cells=0;","function parseCsv(text,config){\n  const delimiter=String(config?.delimiter??''),quote=String(config?.quote??''),newline=String(config?.newline??''),header=config?.header,encoding=String(config?.encoding??'');\n  if(delimiter.length!==1||quote.length!==1||!['LF','CRLF','AUTO'].includes(newline)||typeof header!=='boolean'||encoding!=='UTF-8')throw new Error('PARSE_CSV requires explicit delimiter, quote, newline, header, and UTF-8 encoding.');\n  const rows=[];let row=[],cell='',quoted=false,cells=0;",'csv explicit config');
tr=tr.replaceAll("if(ch==='\"'&&text[i+1]==='\"'){cell+='\"';i++;}else if(ch==='\"')quoted=false;","if(ch===quote&&text[i+1]===quote){cell+=quote;i++;}else if(ch===quote)quoted=false;");
tr=tr.replaceAll("if(ch==='\"'){if(cell.length)throw new Error('Malformed CSV: quote begins inside an unquoted field.');quoted=true;continue;}","if(ch===quote){if(cell.length)throw new Error('Malformed CSV: quote begins inside an unquoted field.');quoted=true;continue;}");
tr=tr.replaceAll("if(ch===','){row.push(cell);cell='';cells++;}","if(ch===delimiter){row.push(cell);cell='';cells++;}");
tr=rep(tr,"  return rows;\n}\nfunction selectJsonPath","  if(header&&rows.length){const keys=rows.shift();return rows.map(row=>Object.fromEntries(keys.map((key,i)=>[key,row[i]??''])));}\n  return rows;\n}\nfunction parseXml(text){\n  const src=String(text||'').trim();if(!src.startsWith('<')||!/^[\\s\\S]*<\\/?[A-Za-z_][A-Za-z0-9_.:-]*/.test(src))throw new Error('PARSE_XML requires well-formed XML text.');\n  const tokens=src.match(/<[^>]+>|[^<]+/g)||[],root={name:'#document',children:[]},stack=[root];let depth=0;\n  for(const token of tokens){if(token.startsWith('<?')||token.startsWith('<!--'))continue;if(token.startsWith('</')){const name=token.slice(2,-1).trim();const node=stack.pop();if(!node||node.name!==name)throw new Error('Malformed XML closing tag.');depth--;continue;}if(token.startsWith('<')){const selfClose=/\\/\\s*>$/.test(token),m=token.match(/^<\\s*([A-Za-z_][A-Za-z0-9_.:-]*)(?:\\s[^>]*)?\\/?>$/);if(!m)throw new Error('Unsupported XML syntax.');const node={name:m[1],children:[],text:''};stack[stack.length-1].children.push(node);if(!selfClose){stack.push(node);depth++;if(depth>LIMITS.maxParsedDepth)throw new Error('XML exceeds parsed-depth limit.');}}else if(stack.length>1)stack[stack.length-1].text+=token;}\n  if(stack.length!==1)throw new Error('Malformed XML: unclosed element.');return root.children.length===1?root.children[0]:root;\n}\nfunction selectXml(value,path){const text=String(path||'').trim();if(!/^\\/?[A-Za-z_][A-Za-z0-9_.:-]*(?:\\/[A-Za-z_][A-Za-z0-9_.:-]*)*$/.test(text))throw new Error('SELECT_XML supports only slash-separated element names.');const parts=text.replace(/^\\//,'').split('/');if(parts.length>LIMITS.maxSelectorDepth)throw new Error('XML selector exceeds selector-depth limit.');let nodes=[value];for(const [i,name] of parts.entries()){if(i===0&&nodes.length===1&&nodes[0]?.name===name)continue;nodes=nodes.flatMap(n=>(n?.children||[]).filter(c=>c.name===name));}return nodes;}\nfunction selectJsonPath",'xml parser');
tr=rep(tr,"if(String(field(test,'EXECUTABLE_KIND')||'').toUpperCase()!=='CUSTOM_PIPELINE')return false;","if(String(field(test,'EXECUTABLE_KIND')||'').toUpperCase()!=='TEST_IR')return false;",'runtime supports kind');
tr=rep(tr,"      case 'PARSE_CSV':value=parseCsv(String(value));break;\n      case 'SELECT_JSON_PATH':value=selectJsonPath(value,step.path);break;","      case 'PARSE_CSV':value=parseCsv(String(value),step);break;\n      case 'PARSE_XML':value=parseXml(String(value));break;\n      case 'SELECT_JSON_PATH':value=selectJsonPath(value,step.path);break;\n      case 'SELECT_XML':value=selectXml(value,step.path);break;",'runtime xml execution');
tr=tr.replace(/\n      case 'ASSERT_EXISTS':[\s\S]*?case 'ASSERT_EQ':/,"\n      case 'ASSERT_EQ':");
tr=tr.replace(/\n      case 'ASSERT_NE':[\s\S]*?case 'ASSERT_GT':/,"\n      case 'ASSERT_GT':");
tr=rep(tr,"      case 'REGEX':{const r=new RegExp(String(step.pattern||''),String(step.flags||''));lastRegex=r;value=r.test(String(value));break;}","      case 'REGEX':{const input=String(value);if(new TextEncoder().encode(input).byteLength>LIMITS.maxRegexInputBytes)throw new Error('REGEX input exceeds deterministic runtime limit.');const r=new RegExp(String(step.pattern||''),String(step.flags||''));lastRegex=r;value=r.test(input);break;}",'regex input limit');
write('test-runtime.js',tr);

let e=read('workflow-engine.js');
e=rep(e,"if(!core||!schema||!hash)throw new Error('workbook.js, hash.js, and workflow-schema.js must load before workflow-engine.js.');","const testRuntime=globalThis.closedLoopTestRuntime;\nif(!core||!schema||!hash||!testRuntime)throw new Error('workbook.js, hash.js, workflow-schema.js, and test-runtime.js must load before workflow-engine.js.');",'runtime dependency');
e=rep(e,"  project.projectData.userEntered=project.projectData.userEntered&&typeof project.projectData.userEntered==='object'?project.projectData.userEntered:{};","  project.projectData.userEntered=project.projectData.userEntered&&typeof project.projectData.userEntered==='object'?project.projectData.userEntered:{};\n  project.projectData.intakeCoverageManifests=safe(project.projectData.intakeCoverageManifests);\n  project.projectData.obligationManifests=safe(project.projectData.obligationManifests);\n  project.projectData.blindAliasMaps=safe(project.projectData.blindAliasMaps);\n  project.projectData.nativeExecutionEvents=safe(project.projectData.nativeExecutionEvents);",'manifest stores');
e=rep(e,"function applicationTestCapabilities(){return Object.freeze([schema.TEST_IR.capability]);}\nfunction applicationTestSupported(test){return schema.validateTestIRTest(test).valid;}","function applicationTestCapabilities(){return Object.freeze([...testRuntime.capabilities()]);}\nfunction applicationTestSupported(test){return testRuntime.supports(test);}\nfunction intakeCoverageManifest(project){\n  ensureShape(project);const inputVersion=String(project.job.CURRENT_INPUT_VERSION||'UNVERSIONED'),definitions=schema.JOB_FIELDS||{},units=[];\n  for(const [name,definition] of Object.entries(definitions)){if(!['HUMAN','HUMAN_DECISION'].includes(definition.producer))continue;const raw=project.job?.[name];if(raw===undefined||raw===null||String(raw).trim()==='')continue;const parts=typeof raw==='string'?String(raw).split(/\\r?\\n/).map(x=>x.trim()).filter(Boolean):[JSON.stringify(raw)];for(const [index,value] of parts.entries()){const rawValueHash=hash.sha256Value(value),unitId='INPUT-UNIT-'+hash.sha256Value({inputVersion,name,index,rawValueHash}).slice(0,20).toUpperCase();units.push({unitId,field:name,index,sourceLocation:'User Job Input/'+name,rawValueHash,value});}}\n  const manifestHash=hash.sha256Value({inputVersion,units:units.map(({unitId,field,index,rawValueHash})=>({unitId,field,index,rawValueHash}))}),existing=project.projectData.intakeCoverageManifests.find(x=>x.inputVersion===inputVersion&&x.manifestHash===manifestHash);if(existing)return existing;const manifest={manifestId:'INTAKE-MANIFEST-'+manifestHash.slice(0,20).toUpperCase(),inputVersion,manifestHash,units,createdAt:now(),applicationOwned:true};project.projectData.intakeCoverageManifests.push(manifest);return manifest;\n}\nfunction obligationManifest(project){\n  ensureShape(project);const intake=intakeCoverageManifest(project),scope=currentScope(project),items=[];const add=(origin,provenance,text,sourceId=null)=>{const value=String(text??'').trim();if(!value||['NONE','NOT APPLICABLE','UNKNOWN'].includes(upper(value)))return;for(const [index,line] of value.split(/\\r?\\n/).map(x=>x.trim()).filter(Boolean).entries()){const obligationId='OBLIGATION-'+hash.sha256Value({origin,provenance,index,line,scope}).slice(0,20).toUpperCase();items.push({obligationId,origin,provenance,sourceId,text:line});}};\n  for(const unit of intake.units)add('HUMAN',unit.unitId,unit.value);for(const key of ['EXACT_DELIVERABLE_REQUESTED','ASSUMPTIONS','UNKNOWN_INFORMATION','INPUT_SET_CONTENTS'])add('STAGE_01','Stage01/'+key,project.stages?.[1]?.agentData?.[key]??project.job?.[key]);for(const record of recordsForCurrentScope(project,'candidateRequirements'))add('EXTERNAL_SOURCE','candidateRequirements/'+recordId(record,'candidateRequirements'),recordValue(record,'CANDIDATE_OBLIGATION'),String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||''));for(const record of recordsForCurrentScope(project,'research'))for(const key of ['MANDATORY_STATEMENTS','PROHIBITIONS','EXCEPTIONS','DEPENDENCIES','RESTRICTIONS','INVALIDATING_MATERIAL'])add('SOURCE_RESEARCH','research/'+recordId(record,'research')+'/'+key,recordValue(record,key),String(recordValue(record,'SOURCE_ID')||record.relationships?.SOURCE_ID||''));const manifestHash=hash.sha256Value({scope,items:items.map(x=>({obligationId:x.obligationId,origin:x.origin,provenance:x.provenance,sourceId:x.sourceId,text:x.text}))}),existing=project.projectData.obligationManifests.find(x=>x.manifestHash===manifestHash);if(existing)return existing;const manifest={manifestId:'OBLIGATION-MANIFEST-'+manifestHash.slice(0,20).toUpperCase(),scope,manifestHash,items,createdAt:now(),applicationOwned:true};project.projectData.obligationManifests.push(manifest);return manifest;\n}",'runtime authority and manifests');
const oaStart=e.indexOf('function operationalNextAction(project,currentStage){');const oaEnd=e.indexOf('\n\nfunction applicationTestCapabilities',oaStart);if(oaStart<0||oaEnd<0)throw new Error('operationalNextAction anchors missing');
const structured=`function operationalNextAction(project,currentStage){\n  const stage=Number(currentStage||1),requests=unresolvedHumanRequests(project,stage),base={actionType:'PASTE_FINAL_JSON',heading:'Use the current instruction',explanation:'Continue the current controlled stage and return the final structured response only when the external work or conversation is complete.',primaryButton:'Continue current stage',secondaryAction:null,filesToSend:[],filesToWithhold:[],expectedReturnFiles:[],blockingReason:null,canonicalStateChanged:false,newPromptRequired:false};\n  if(requests.length)return {...base,actionType:'CONTINUE_AGENT_CONVERSATION',heading:'Answer the current human-only question',explanation:'Provide only the genuinely human-owned answer. Saving it creates a new User Job Input version, invalidates the old prompt/proposal, and requires a replacement prompt.',primaryButton:'Continue current stage',newPromptRequired:true};\n  if(stage===16){const correction=stage16CorrectionPlan(project);return {...base,actionType:correction.actionType==='HUMAN_AUTHORITY'?'CONTINUE_AGENT_CONVERSATION':correction.actionType==='CONTROLLED_CORRECTION'?'AI_REVIEW':'COMPLETE',heading:correction.heading,explanation:correction.explanation};}\n  const plan=testExecutionPlan(project),relevant=[12,22,23,24].includes(stage)?plan.items:[],blocked=relevant.find(i=>!i.executableNow);if(blocked)return {...base,actionType:'BLOCKED',heading:'Verification is blocked',explanation:blocked.blockingReason||'Required execution evidence is not currently obtainable.',primaryButton:null,blockingReason:blocked.blockingReason||'Required capability or bytes are unavailable.',filesToSend:blocked.handoff?.send||[],filesToWithhold:blocked.handoff?.withhold||[],expectedReturnFiles:blocked.handoff?.expectBack||[]};\n  const item=relevant.find(i=>i.operatorAction!=='NO_ACTION');if(item){const common={...base,filesToSend:item.handoff?.send||[],filesToWithhold:item.handoff?.withhold||[],expectedReturnFiles:item.handoff?.expectBack||[]};if(item.operatorAction==='SEND_TO_INDEPENDENT_REVIEWER')return {...common,actionType:'AI_REVIEW',heading:'Open a fresh independent reviewer context',explanation:'Use only the current application-selected review package and withhold prohibited prior conclusions.',primaryButton:'Prepare review package'};if(item.operatorAction==='SEND_TO_TOOL_AGENT')return {...common,actionType:'EXTERNAL_AGENT_TOOL',heading:'Use the required tool-capable agent',explanation:'Run the current instruction only in an environment with '+(item.requiredCapability||'the declared capability')+'.',primaryButton:'Prepare execution package'};if(item.operatorAction==='HUMAN_INSPECTION')return {...common,actionType:'HUMAN_INSPECTION',heading:'Human inspection is required',explanation:'Perform the current inspection and preserve the requested human-owned observation evidence.',primaryButton:'Review inspection instructions'};if(item.operatorAction==='USE_EXTERNAL_SYSTEM')return {...common,actionType:'EXTERNAL_SYSTEM',heading:'Use the required external system',explanation:'Use '+(item.requiredCapability||'the declared external system')+' and return attributable system evidence.',primaryButton:'Prepare external-system package'};}\n  if(relevant.length&&relevant.every(i=>i.operatorAction==='NO_ACTION'))return {...base,actionType:'RUN_APP_TESTS',heading:'Run verification in the application',explanation:'No external execution is required for the current supported deterministic tests.',primaryButton:'Run tests'};\n  if(stage===4)return {...base,actionType:'AI_REVIEW',heading:'Compile the complete requirement specification',explanation:'The current prompt contains the application-enumerated obligation universe. Do not ask the user to resend original project intent; report an earlier-stage capture defect if canonical context is incomplete.',primaryButton:'Copy current instruction'};\n  return base;\n}`;
e=e.slice(0,oaStart)+structured+e.slice(oaEnd);
e=rep(e,'testExecutionPlan,executionHandoff,evaluateContextIndependence','testExecutionPlan,executionHandoff,intakeCoverageManifest,obligationManifest,evaluateContextIndependence','manifest exports');
write('workflow-engine.js',e);

let p=read('prompt-engine.js');
p=p.replace(/const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine\/\d+';/,"const PROMPT_ENGINE_VERSION='closed-loop-prompt-engine/27';");
const hbStart=p.indexOf('function humanInputBlock(job){');const hbEnd=p.indexOf('\nconst procedures=',hbStart);if(hbStart<0||hbEnd<0)throw new Error('humanInputBlock anchors missing');
const hb=`function humanInputBlock(job,stage){\n const definitions=schema.JOB_FIELDS||{},all=Object.entries(definitions).filter(([,definition])=>['HUMAN','HUMAN_DECISION'].includes(definition?.producer)).map(([name])=>name),byStage={1:all,2:['EXACT_USER_OBJECTIVE_VERBATIM','KNOWN_AUTHORITATIVE_SOURCES','DESIRED_SOURCE_COUNT','AVAILABLE_TOOLS','PROHIBITED_ACTIONS'],4:all,8:['EXACT_USER_OBJECTIVE_VERBATIM','REQUIRED_OUTPUT_FORMAT','DEADLINE_OR_TEMPORAL_SCOPE','AVAILABLE_TOOLS','PROHIBITED_ACTIONS','EXPLICIT_USER_REQUIREMENTS'],10:['AVAILABLE_TOOLS','PROHIBITED_ACTIONS'],11:['AVAILABLE_TOOLS','PROHIBITED_ACTIONS'],21:['REQUIRED_OUTPUT_FORMAT','AVAILABLE_TOOLS','PROHIBITED_ACTIONS','EXPLICIT_USER_REQUIREMENTS']},names=byStage[stage]||[];\n return names.length?names.map(name=>\`${'${name}'}:\\n${'${show(job?.[name])}'}\`).join('\\n\\n'):'NONE';\n}`;
p=p.slice(0,hbStart)+hb+p.slice(hbEnd);
p=p.replace('${humanInputBlock(j)}','${humanInputBlock(j,stage)}');
let start=p.indexOf('${stage===1?`STAGE 01 DOMAIN INTAKE ADAPTATION');let end=p.indexOf('${stage===1?`STAGE 01 CLARIFICATION EXPERIENCE',start);if(start<0||end<0)throw new Error('domain adaptation block anchors missing');p=p.slice(0,start)+`PROJECT-SUBJECT NEUTRAL ADAPTATION\nDerive subject-specific human-authority questions only from the exact user request, accessible supplied materials, and current canonical context. The application runtime does not know or branch on the project subject. Do not use a hard-coded subject catalogue, keyword mode, or domain template. Ask only genuinely human-owned facts or decisions; obtain researchable facts from authorized materials or research. Preserve every relevant user statement canonically and never ask the user to repeat information already available.\n\n`+p.slice(end);
start=p.indexOf('${stage===1?`STAGE 01 CLARIFICATION EXPERIENCE');end=p.indexOf('${stage===2?`STAGE 02 SOURCE DISCOVERY GUIDANCE',start);if(start<0||end<0)throw new Error('stage1 clarification block anchors missing');p=p.slice(0,start)+`STAGE 01 CLARIFICATION EXPERIENCE\nClassify every unresolved issue as BLOCKING_NOW, ASK_NOW_NONBLOCKING, or LATER_RESOLVABLE. Ask every BLOCKING_NOW and ASK_NOW_NONBLOCKING human-only question conversationally before final JSON. Unknown or deferred is acceptable for ASK_NOW_NONBLOCKING. Never ask for facts already present in supplied material or obtainable by authorized research. When supplied bytes are unavailable, retain the material reference without inferring contents and do not ask the human to re-enter them merely because bytes are unavailable.\n\n`+p.slice(end);
const contextAnchor='AUTHORIZED BOUNDED CONTEXT FOR THIS STAGE\n${contextFor(stage,state,operation,scope)}';
p=rep(p,contextAnchor,"${stage===1?`APPLICATION-ENUMERATED INTAKE COVERAGE MANIFEST — CLASSIFY EVERY ID\n${show(workflow.intakeCoverageManifest(state))}\n\n`:''}${stage===4?`APPLICATION-ENUMERATED OBLIGATION UNIVERSE — ACCOUNT FOR EVERY ID\n${show(workflow.obligationManifest(state))}\n\n`:''}AUTHORIZED BOUNDED CONTEXT FOR THIS STAGE\n${contextFor(stage,state,operation,scope)}",'manifest prompt context');
p=rep(p,"EXECUTABLE_KIND = CUSTOM_PIPELINE","EXECUTABLE_KIND = TEST_IR",'prompt test ir kind');
write('prompt-engine.js',p);

let a=read('app-core.js');
a=rep(a,"if(![core.SCHEMA,'human-project/30'].includes(raw?.schema))","if(![core.SCHEMA,'closed-loop-project/2','human-project/30'].includes(raw?.schema))",'import seed migration');
a=rep(a,"if(raw.schema&&![core.SCHEMA,'human-project/30'].includes(raw.schema))","if(raw.schema&&![core.SCHEMA,'closed-loop-project/2','human-project/30'].includes(raw.schema))",'import validation migration');
a=rep(a,"const label=k=>String(k||'')","const actionText=value=>value&&typeof value==='object'?[value.heading,value.explanation].filter(Boolean).join(' — '):String(value||'');\nconst label=k=>String(k||'')",'action text helper');
a=a.replaceAll("esc(current.job.NEXT_REQUIRED_ACTION||'No next action recorded.')","esc(actionText(current.job.NEXT_REQUIRED_ACTION)||'No next action recorded.')");
a=a.replaceAll("esc(current.job.NEXT_REQUIRED_ACTION)","esc(actionText(current.job.NEXT_REQUIRED_ACTION))");
a=rep(a,"async function executeTestWorker(spec,artifacts,timeoutMs=10000)","async function executeTestWorker(spec,artifacts,timeoutMs=globalThis.closedLoopTestRuntime?.LIMITS?.maxWorkerDurationMs||10000)",'worker timeout authority');
write('app-core.js',a);

let h=read('index.html');
h=h.replace('.expandable-prompt{height:280px;max-height:280px}', '.expandable-prompt{max-height:280px}');
if(!h.includes("worker-src 'self'"))h=h.replace("connect-src 'self'; object-src", "connect-src 'self'; worker-src 'self'; object-src");
const engineScript=h.match(/<script defer src="workflow-engine\.js\?v=([^"]+)"><\/script>/);if(!engineScript)throw new Error('workflow-engine script anchor missing');if(!h.includes('test-runtime.js?v='))h=h.replace(engineScript[0],`<script defer src="test-runtime.js?v=${engineScript[1]}"></script>\n${engineScript[0]}`);
const runtimeFiles=['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js'];const token='runtime-'+crypto.createHash('sha256').update(runtimeFiles.map(f=>fs.readFileSync(f)).join('')).digest('hex').slice(0,16);h=h.replace(/runtime-[a-f0-9]+/g,token);write('index.html',h);

for(const name of fs.readdirSync('.').filter(x=>/^(verify.*\.mjs|test-fixtures\.mjs|README\.md)$/.test(x))){let t=read(name);t=t.replaceAll('closed-loop-stage-response/2','closed-loop-stage-response/3').replaceAll('CUSTOM_PIPELINE','TEST_IR');if(name!=='verify-spec-conformance.mjs')t=t.replaceAll("'closed-loop-project/2'","'closed-loop-project/3'").replaceAll('"closed-loop-project/2"','"closed-loop-project/3"');write(name,t);}

fs.writeFileSync('verify-spec-conformance.mjs',`import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';\nglobalThis.Event=globalThis.Event||class Event{constructor(type){this.type=type;}};globalThis.dispatchEvent=globalThis.dispatchEvent||(()=>true);for(const file of ['workbook.js','hash.js','workflow-schema.js','test-runtime.js','workflow-engine.js','prompt-engine.js'])vm.runInThisContext(fs.readFileSync(file,'utf8'),{filename:file});const core=globalThis.closedLoopCore,schema=globalThis.closedLoopWorkflowSchema,engine=globalThis.closedLoopWorkflowEngine,prompts=globalThis.closedLoopPromptEngine;assert.equal(core.PROJECT_SCHEMA,'closed-loop-project/3');assert.equal(schema.RESPONSE_SCHEMA,'closed-loop-stage-response/3');assert.equal(core.STAGE_COUNT,30);assert.equal(core.STAGES[15].title,'CORRECT THE ROOT CAUSE');assert.equal(schema.JOB_FIELDS.JOB_TITLE.producer,'HUMAN_DECISION');assert.equal(schema.JOB_FIELDS.JOB_OWNER.producer,'HUMAN_DECISION');assert.deepEqual(schema.TEST_IR.executableKinds,['NONE','TEST_IR']);for(const op of ['PARSE_XML','SELECT_XML'])assert(schema.TEST_IR.operations.includes(op));for(const op of ['ASSERT_EXISTS','ASSERT_TYPE','ASSERT_NE'])assert(!schema.TEST_IR.operations.includes(op));const source=fs.readFileSync('prompt-engine.js','utf8');for(const token of ['PATENT / REGULATED FILING','SOFTWARE / MULTI-FILE SYSTEM','BUILDING / ARCHITECTURE / AEC','PHYSICAL / MECHANICAL / CAD / CAM / CNC / ADDITIVE'])assert(!source.includes(token),'hard-coded subject branch remains: '+token);const p=core.createBlankState('JOB-ZERO-LOSS');Object.assign(p.job,{CURRENT_INPUT_VERSION:'INPUT-v001',CURRENT_SOURCE_SET_VERSION:'SOURCE-SET-v001',EXACT_USER_OBJECTIVE_VERBATIM:'Build the requested product.',EXPLICIT_USER_REQUIREMENTS:'Never ask me to supply the same project requirement twice.\\nPreserve every project requirement.',SUPPLIED_MATERIALS_INVENTORY:'intent.pdf',EXACT_DELIVERABLE_REQUESTED:'Requested product',ASSUMPTIONS:'NONE',UNKNOWN_INFORMATION:'NONE',INPUT_SET_CONTENTS:'Captured intent content: do not require repeat attachment.'});engine.ensureShape(p);const intake=engine.intakeCoverageManifest(p);assert(intake.units.length>=3);const obligation=engine.obligationManifest(p);assert(obligation.items.some(x=>x.text.includes('Never ask me')));const stage4=prompts.buildPromptRecord(4,p,{operation:'COMPLETE'});assert(stage4.prompt.includes('APPLICATION-ENUMERATED OBLIGATION UNIVERSE'));assert(stage4.prompt.includes('Never ask me to supply the same project requirement twice.'));assert(!stage4.prompt.includes('attach or provide the original before final JSON'));assert.equal(engine.executionHandoff(p,{stage:4,operation:'COMPLETE'}).send.length,0);const migrated=core.migrateState({...JSON.parse(JSON.stringify(p)),schema:'closed-loop-project/2'});assert.equal(migrated.schema,'closed-loop-project/3');assert(migrated.projectData.migrationArchives.some(x=>x.schema==='closed-loop-project/2'));const action=engine.operationalNextAction(p,4);for(const key of ['actionType','heading','explanation','primaryButton','secondaryAction','filesToSend','filesToWithhold','expectedReturnFiles','blockingReason','canonicalStateChanged','newPromptRequired'])assert(Object.prototype.hasOwnProperty.call(action,key),key);const html=fs.readFileSync('index.html','utf8');assert(html.includes("worker-src 'self'"));assert(html.indexOf('test-runtime.js')<html.indexOf('workflow-engine.js'));assert(html.includes('.expandable-prompt{max-height:280px}'));assert(!html.includes('.expandable-prompt{height:280px;max-height:280px}'));console.log(JSON.stringify({schema3:true,subjectNeutralPrompts:true,captureOnce:true,stage4ObligationUniverse:true,structuredAction:true,testIrContract:true,visualPromptPreviewRestored:true}));\n`);
