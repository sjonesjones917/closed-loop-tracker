import fs from 'node:fs';
const path='.apply-native-current.mjs';
let text=fs.readFileSync(path,'utf8');
const old="{const runtimeFiles=['workbook.js','hash.js','workflow-schema.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js'];const gitBlobSha=file=>{const bytes=fs.readFileSync(file);return createHash('sha1').update(`blob ${bytes.length}\\0`).update(bytes).digest('hex');};const manifest=runtimeFiles.map(file=>`${file}:${gitBlobSha(file)}\\n`).join('');const token=`runtime-${createHash('sha256').update(manifest).digest('hex').slice(0,16)}`;const path='index.html',old='runtime-823183a72b1e5462',source=fs.readFileSync(path,'utf8'),count=source.split(old).length-1;if(count!==8)throw new Error(`index expected 8 old runtime tokens, got ${count}`);fs.writeFileSync(path,source.split(old).join(token));}";
const replacement="{const runtimeFiles=['workbook.js','hash.js','workflow-schema.js','workflow-engine.js','prompt-engine.js','response-ingestion.js','project-store.js','app-core.js'];const gitBlobSha=file=>{const bytes=fs.readFileSync(file);return createHash('sha1').update(`blob ${bytes.length}\\0`).update(bytes).digest('hex');};const manifest=runtimeFiles.map(file=>`${file}:${gitBlobSha(file)}\\n`).join('');const token=`runtime-${createHash('sha256').update(manifest).digest('hex').slice(0,16)}`;const path='index.html',source=fs.readFileSync(path,'utf8'),matches=source.match(/runtime-[0-9a-f]{16}/g)||[],unique=[...new Set(matches)];if(matches.length!==8||unique.length!==1)throw new Error(`index expected one shared runtime token across 8 scripts; found ${matches.length} matches / ${unique.length} unique`);fs.writeFileSync(path,source.replaceAll(unique[0],token));}";
if(!text.includes(old)||text.indexOf(old)!==text.lastIndexOf(old))throw new Error('Expected one old cache-token transformer block.');
text=text.replace(old,replacement);
fs.writeFileSync(path,text);
