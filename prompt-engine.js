(()=>{
'use strict';
const core=globalThis.closedLoopCore;
const schema=globalThis.closedLoopWorkflowSchema;
const hash=globalThis.closedLoopHash;
if(!core||!schema||!hash)throw new Error('workbook.js, hash.js, and workflow-schema.js must load before prompt-engine.js.');
const show=v=>{if(v===undefined||v===null||v==='')return 'UNKNOWN';if(Array.isArray(v)&&!v.length)return 'NONE';if(typeof v==='object')return JSON.stringify(v,null,2);return String(v)};
const procedures={
1:'Initialize only this current job from this current job’s exact user input. Preserve the verbatim objective, exact deliverable, supplied-material inventory and provenance, inspection state, format requirements, temporal and geographic scope, user-supplied authority, tools, prohibitions, explicit user requirements, assumptions, unresolved unknowns, and controlled INPUT-vN identity. Determine whether the requested deliverable is realistically executable in the available environment. The application itself does not connect to or modify an external repository. If implementation requires repository, runtime, account, network, tool, or other execution access that is not actually available in the authorized context, do not pretend implementation can occur here: define the most complete implementation-ready specification, architecture, patch plan, acceptance criteria, or other reliable deliverable that can actually be produced. Self-contained deliverables such as research, analysis, specifications, papers, applications, and similar artifacts may be produced directly when the available environment is sufficient. Assign and preserve this job’s unique JOB_ID before downstream artifacts. Do not create, prescribe, or instruct reuse of a master prompt, master job, reusable job definition, reusable target specification, or template for other jobs. Do not infer requirements for unrelated jobs from this job. Do not begin substantive external-source research or downstream production work.',
2:'Build the external governing source inventory for this current job only. Treat any DESIRED OR SUGGESTED SOURCE COUNT in User Job Input as a search target, never as authority to invent or pad sources. Prefer the most authoritative, primary, official, controlling, current sources appropriate to the information type. Verify source identity, publisher or issuing authority, currency, supersession state, and applicability from the source itself when possible; use reputable secondary sources for corroboration or context when appropriate, not as substitutes for available controlling primary authority. If a target count is supplied, seek at least that many legitimate useful sources when the domain supports them, continue beyond it when needed for governing coverage, and return fewer rather than adding weak or irrelevant sources. If no legitimate external authority applies after evidence-supported inspection, return an explicit no-applicable-source determination rather than inventing a source. Stage 01 User Job Input and Supplied Material remain authorized project inputs, but they are not automatically independent external governing sources and must not receive SOURCE_ID merely because they were supplied. Identify genuinely independent external authorities relevant to the not-yet-existing target product. Establish actual identity, issuing organization or author, source type, publication origin, URL/reference, version, date, retrieval date where applicable, authority level and role, relevance, applicable portions, inspection state, currency, supersession, controlling status, and actual SHA-256 only when controlled bytes exist. Establish an explicit authority hierarchy and separate source-conflict records. Never use the target product, this operating application, its repository, source code, UI, stored project state, screenshots, prior versions, previous generated targets, project outputs, generated prompts/instructions, or another implementation of the same target as governing source authority. Do not research requirements yet. Create SOURCE-SET-vN only from legitimate external governing sources actually established in this stage; if none are yet established, leave the source set uncreated and record UNKNOWN/BLOCKED as applicable.',
3:'Research only the legitimate Stage 02 external governing source set, source-by-source and pass-by-pass. Examine exact portions and separately capture facts, mandatory obligations, recommendations, optional practices, examples, prohibitions, exceptions, dependencies, applicability facts, restrictions, invalidating material, conflicts, superseded guidance, source evidence, candidate requirement IDs, and unresolved questions. Do not research the target product, this operating application, repository source code, prior implementations, project-generated artifacts, or current UI behavior as requirement authority. Repeat discovery passes until saturation is actually supported by the evidence.',
4:'Compile atomic requirements for this current job from authorized User Job Input plus legitimately applicable Stage 03 external-source research, preserving provenance for each obligation. Each REQ_ID must express one independently testable obligation where possible and record type, mandatory/optional status, governing source or user-input relationship, exact source location when applicable, authority, applicability, dependencies, prohibitions, defined terms, observable satisfaction condition, intended verification method, expected evidence, failure condition, severity, status, and notes. Do not derive requirements from the target product or an existing implementation merely because that implementation contains a behavior.',
5:'Resolve the current job’s requirement set. Detect duplicate or conflicting requirements, impossible combinations, undefined terms, circular dependencies, missing prerequisites, unsupported requirements, uncertain applicability, and requirements lacking a verification method. Preserve the defect, governing evidence, resolution or unresolved state, changed requirement, resulting controlled version, affected downstream work, and blockers. Never silently guess away a conflict or manufacture authority.',
6:'Build this job’s verification suite before any production instruction is authored. Every active mandatory REQ_ID must have at least one valid TEST_ID. Use the strongest suitable method, including exact computation, programmatic checks, schema or structure checks, source comparison, rule-based verification, and independent human meaning/content review where deterministic checks cannot establish satisfaction. Define inputs, tools, procedure, expected result, failure condition, and evidence to preserve. Calculate mandatory requirement-to-test coverage exactly and block progression below 100 percent.',
7:'Build this job’s MUTATION-SUITE-vN to prove validators reject realistic invalid states. Include applicable missing inputs, invalid values, wrong versions, duplicate identifiers, contradictions, omitted required sections, prohibited material, corrupt references, malformed files, conflicting authority, unavailable tools, unsupported claims, injected source instructions, known-invalid artifacts, and other relevant mutations. Preserve fixture, expected rejection, actual result, validator defect where applicable, and evidence.',
8:'Author this job’s production instruction only from the resolved current requirement set and verification architecture. Define objective, authorized inputs, authority rules, failure handling, scope, prohibitions, defined terms, ordered procedure, branches, tool requirements, output contract, TRUE/FALSE/UNKNOWN handling, rejection/blocking rules, completion conditions, and requirement traceability. Do not copy or imitate an existing target implementation as authority. Do not turn this job’s production instruction into a reusable instruction for unrelated jobs unless the current user input explicitly requires such a deliverable.',
9:'Preflight this job’s production instruction in an independent context where required. Review each material clause for multiple interpretations, undefined objects, unsupplied dependencies, internal conflicts, unavailable capabilities, objective verifiability, responsible operation, ordering, defined failure behavior, and traceability. Preserve findings, defects, corrections, repeated review, final approved instruction version, and evidence. Do not execute target production during preflight.',
10:'Freeze the exact candidate for this job and iteration. The human selects authorized canonical components and the application assigns CANDIDATE_ID and ITERATION_ID, freezes the exact component manifest, and calculates hashes from verified bytes. Preserve role distribution, immutable locations, tool configuration, settings, permissions, and limitations. The agent must not invent or assign candidate identity, versions, hashes, or lifecycle state. The same candidate package must be used unchanged across the ten-run batch. A material change terminates the candidate and requires a new identity.',
11:'Run exactly ten independent executions for this job using the identical frozen candidate in ten fresh contexts. For each application-reserved RUN_ID preserve context identity, timestamps, frozen candidate identity, contamination check, tool configuration, execution status, complete output, output artifact identities and hashes where applicable, tool failures, and notes. Runs may not see another run’s output, reviewer comments, prior failure explanations, or proposed corrections. Never select a preferred run to discard the others.',
12:'Verify each of this job’s ten executions independently. Create exactly one current applicable verification record for every required REQ_ID × RUN_ID × TEST_ID triple. Preserve verifier and verifier-context identity, independence status, inputs, procedure, expected result, observed result, exact evidence, SATISFIED/VIOLATED/UNDETERMINED, DEFECT_ID where applicable, and reason for UNDETERMINED. Reject missing, duplicate, wrong-version, wrong-iteration, or self-validated triples. Reconcile counts mathematically and do not let the generator act as its sole validator.',
13:'Compare all ten executions for this job requirement-by-requirement. Preserve every run determination, all-ten satisfaction, any violation, any undetermined result, interpretation variance, output variance, authorized versus unauthorized variance, inconclusive tests, repeated and unique failure patterns, correctness-affecting variance, linked defects, and evidence. Never discard a run because another run appears preferable.',
14:'Root-cause every material defect in this job by tracing backward through product/output, execution, instruction, requirement, research, source, user input, tool, and audit as applicable. Classify the root cause using the controlled categories, identify the earliest defective layer, preserve evidence, and identify downstream invalidation. Do not patch only the final symptom when an earlier layer is responsible.',
15:'Convert every confirmed failure in this job into permanent regression data. Link REG_ID to DEFECT_ID and REQ_ID; preserve failure fixture and identity/hash when available, reproduction procedure, detection method, an actual pre-correction execution that demonstrates failure, correction, later post-correction execution evidence, permanent test location, applicability, active/retired state, and retirement authority where applicable.',
16:'Revise only the responsible earliest defective layer for this job. Create a controlled CHANGESET_ID preserving triggering defects, RCA, responsible layer, old artifact version, exact modification, new version, downstream invalidation, required reruns, instruction-change determination, repeated preflight requirement, and justified unchanged artifacts. Never overwrite a controlled version in place.',
17:'After a material upstream correction, create a complete new ten-execution iteration for this job with a new application-assigned ITERATION_ID and CANDIDATE_ID. Freeze the corrected versions, use ten new independent contexts, withhold prior outputs, use one identical candidate package across all ten runs, and repeat execution, verification, comparison, RCA, regression execution, corrections, and lineage through the declared operation subcontracts.',
18:'Determine convergence for this job from evidence, never from run count alone. Calculate mandatory requirement coverage, mandatory verification coverage, applicable regression success, critical defect count, major defect count, mandatory unresolved unknown count, correctness-affecting contradiction count, correctness-affecting ambiguity count, and unexplained correctness-affecting variance count. Convergence exists only when required coverage and regression success are 100 percent and every blocking count is zero simultaneously.',
19:'After convergence, run this job’s unchanged confirmation iteration using the exact converged component versions and hashes with zero material changes. Use ten new independent contexts, rerun the complete verification matrix and applicable regression suite, compare all runs, and detect new defects, missed requirements, missed failure cases, or new correctness-affecting variance. Any failure routes back to the responsible earlier stage.',
20:'Freeze this job’s production baseline only after successful unchanged confirmation. The human authorizes the baseline and the application assigns BASELINE_ID, freezes the exact approved versions and artifact bytes, calculates hashes, and records immutable baseline identity. Any material change removes baseline validity until required workflow is repeated. The agent must not assign baseline identity, hashes, authorization, or lifecycle state.',
21:'Produce the job’s finished deliverable only when the actual authorized execution environment can do so reliably from the approved baseline. The application itself does not connect to or modify an external repository. If the requested implementation depends on repository, runtime, account, network, deployment, or other execution access that is absent from the authorized environment, do not claim implementation or fabricate product bytes; produce the complete implementation-ready specification/patch plan or return the appropriate BLOCKED/EXECUTION_FAILED disposition according to the controlling deliverable and capability state. For a self-contained deliverable that can actually be generated here, use a fresh production context and only approved baseline materials. The application assigns PRODUCT_ID and execution identity and is authoritative for actual file bytes, filenames, media types, sizes, hashes, storage references, and lineage after operator-supplied bytes are persisted and verified. No uncontrolled post-generation editing is permitted.',
22:'Run deterministic verification against the actual finished product bytes wherever deterministic testing is possible. Verify applicable arithmetic, counts, schemas, filenames, inventory, hashes, required sections, ordering, identifiers, duplicates, references, links, dates, enumerations, required/prohibited text, package contents, structure, dimensions, and other objective properties. Preserve product identity/hash, tool/version, procedure, expected result, actual result, determination, evidence, and defect.',
23:'Perform independent meaning/content verification on this job’s actual finished product where deterministic checks cannot establish substantive satisfaction. For each applicable requirement preserve requirement identity, product location, external-source evidence where applicable, required meaning, observed meaning, evidence-based comparison, SATISFIED/VIOLATED/UNDETERMINED, defect, and reason a determination could not be made. Use human-facing terminology such as Meaning Verification or Content Verification.',
24:'Perform adversarial verification on this job’s finished product. Test applicable missing material, prohibited material, contradictions, impossible logic, unsupported facts, external-source misrepresentation, wrong versions, broken references, hidden assumptions, partial completion, nonsensical meaning, terminology inconsistency, unhandled exceptions, stale facts, malformed files, hidden content, export corruption, superficial keyword compliance, and every active applicable historical regression pattern. Preserve attack, method, expected behavior, actual result, defect, severity, and evidence.',
25:'Inspect this job’s exact final delivered representation and package. Preserve artifact inventory, filename, version, byte size, SHA-256, requirement trace, transformation chain and tool versions, before/after hashes, actual rendering/opening evidence, clipping, missing/blank content, broken layout, graphics, font substitution, overlap, hidden content, ordering, export corruption, package inventory, missing/unexpected/duplicate/corrupt files, defects, and coverage counts. Objective byte and file facts are application-derived; irreducible inspection judgments are reviewer or human authority.',
26:'Reconcile this job’s process evidence and product evidence as two separate propositions. Process audit must compare approved versus actual inputs, instruction, tools, required tests, unauthorized modification, authorized changes, chain of custody, defects, blockers, determination, and evidence. Product audit must reconcile mandatory requirement count, affirmative satisfaction count, mandatory test count, validator results, meaning/content verification results, critical/major defects, mandatory unknowns, blockers, determination, and evidence. Neither proposition proves the other.',
27:'Review the complete current release evidence for this job. The application, not the agent or human, calculates and records exactly one release determination: ACCEPTED, REJECTED, or BLOCKED from canonical current evidence. Investigate or explain missing evidence when useful, but do not set or override the deterministic release state.',
28:'Only after Stage 27 is ACCEPTED, verify exact artifact identity immediately before release. The human selects audited and delivery bytes; the application joins them by authorized artifact identity and filename, hashes the exact bytes, and compares byte size and SHA-256 independently of file-selection order. Any missing, extra, duplicate, renamed-without-authorization, resized, rehashed, stale, or otherwise mismatched file stops release.',
29:'Preserve this job’s complete evidence chain for every mandatory requirement: EXTERNAL SOURCE / USER AUTHORITY AS APPLICABLE -> REQUIREMENT -> INSTRUCTION TRACE -> EXECUTION OR PRODUCT ELEMENT -> EVERY APPLICABLE TEST -> EVERY REQUIRED TEST RESULT -> CANONICAL EVIDENCE -> CURRENT RELEASE DECISION and, when released, EVERY APPLICABLE ARTIFACT HASH IDENTITY. The application constructs these relationships from canonical IDs; investigate missing links only. Missing links remain missing and produce FALSE, UNKNOWN, UNDETERMINED, or BLOCKED as appropriate; never fabricate a chain link.',
30:'Preserve this job’s failures permanently in append-only defect and regression history. Every defect record must retain stable identity, date, JOB_ID, ITERATION_ID, RUN_ID and/or PRODUCT_ID, REQ_ID, observed failure, expected condition, evidence, severity, root-cause category and cause, correction, changed artifacts, REG_ID, verification result, status, relationships, and append-only corrections. Every confirmed defect requires permanent regression information, and the latest applicable regression execution must have succeeded before baseline approval. Never rewrite history.'
};
const contextCollections=Object.freeze(Object.fromEntries(Array.from({length:schema.STAGE_COUNT},(_,i)=>{const stage=i+1;return [stage,[...(schema.STAGE_CONTRACTS[stage]?.readCollections||[])]];})));
const recordId=(record,collection)=>String(record?.id||record?.recordId||record?.[schema.RECORD_SCHEMAS[collection]?.idField]||record?.fields?.[schema.RECORD_SCHEMAS[collection]?.idField]||'UNKNOWN');
const activeRecords=(state,collection)=>Array.isArray(state?.projectData?.[collection])?state.projectData[collection].filter(x=>x?.active!==false&&!x?.invalidatedBy):[];
function boundedCollection(state,collection){const active=activeRecords(state,collection);if(!active.length)return 'NONE';return show({totalActive:active.length,records:active.map(record=>({id:recordId(record,collection),stage:record.stage??'UNKNOWN',scope:record.scope||{},fields:record.fields||record,relationships:record.relationships||{},evidenceRefs:record.evidenceRefs||record.evidenceIds||[],contentSha256:record.contentSha256||record.sha256||'UNKNOWN'})),omitted:0,selectionRule:'All active records selected by the controlling schema readCollections contract. No records are silently omitted; large artifact bytes remain referenced by canonical artifact identity.'});}
function correctionRequests(state,stage){return (state?.projectData?.rejectedResponses||[]).filter(x=>Number(x.stage)===Number(stage)&&x.requestCorrection===true&&String(x.status||'').toUpperCase()==='CORRECTION_REQUESTED').map(x=>({rejectedResponseId:x.rejectedResponseId,rawResponseId:x.rawResponseId,proposalId:x.proposalId,reason:x.reason,requestedAt:x.rejectedAt}));}
function contextFor(stage,state,operation){const parts=[];if(stage>1){const prior=state?.stages?.[stage-1]?{agentData:state.stages[stage-1].agentData||state.stages[stage-1].acceptedData||{},humanData:state.stages[stage-1].humanData||{},derivedData:state.stages[stage-1].derivedData||{}}:'NONE';parts.push(`PRIOR STAGE DECISION AND ACCEPTED DATA\n${show(prior)}`);}const open=(state?.projectData?.blockers||[]).filter(x=>!x.invalidatedBy&&!['CLOSED','RESOLVED','RETIRED'].includes(String(x?.fields?.STATUS||x?.STATUS||x?.status||'OPEN').toUpperCase()));if(open.length)parts.push(`APPLICABLE OPEN BLOCKERS\n${show(open)}`);const questions=(state?.projectData?.humanInputRequests||[]).filter(x=>Number(x.stage)===stage&&String(x.status||'OPEN').toUpperCase()==='OPEN');if(questions.length)parts.push(`UNRESOLVED HUMAN INPUT REQUESTS\n${show(questions)}`);const answered=(state?.projectData?.humanInputAnswers||[]).map(x=>({stage:x.stage,questionId:x.requestId,question:x.question,answer:x.answer,answerType:x.answerType||'UNKNOWN',inputVersion:x.inputVersion||state?.job?.CURRENT_INPUT_VERSION||'UNKNOWN',operatorLabel:x.operatorLabel||x.operator||'UNSPECIFIED',affectedStageFields:x.affectedStageFields||[],affectedRecords:x.affectedRecords||[]}));if(answered.length)parts.push(`ANSWERED HUMAN CLARIFICATIONS\n${show(answered)}`);const corrections=correctionRequests(state,stage);if(corrections.length)parts.push(`REQUESTED RESPONSE CORRECTIONS\n${show(corrections)}`);const contract=schema.STAGE_CONTRACTS[stage],op=schema.operationContract(stage,operation||contract.operations[0]);for(const collection of op?.readCollections||contract.readCollections||[])parts.push(`${collection.replace(/([a-z])([A-Z])/g,'$1 $2').replace(/_/g,' ').toUpperCase()}\n${boundedCollection(state,collection)}`);return parts.join('\n\n')||'No additional stage-specific canonical records are established.';}
function scopeFor(stage,state,overrides={}){const j=state?.job||{};return {projectRevision:Number(state?.revision||0),inputVersion:j.CURRENT_INPUT_VERSION||null,sourceSetVersion:j.CURRENT_SOURCE_SET_VERSION||null,requirementsVersion:j.CURRENT_REQUIREMENTS_VERSION||null,testSuiteVersion:j.CURRENT_TEST_SUITE_VERSION||null,instructionVersion:j.CURRENT_INSTRUCTION_VERSION||null,iterationId:j.CURRENT_ITERATION||null,candidateId:state?.projectData?.candidateFreezes?.filter(x=>x?.active!==false&&!x?.invalidatedBy).at(-1)?.id||null,runId:overrides.runId||null,contextId:overrides.contextId||null,baselineId:j.CURRENT_BASELINE_ID&&!['NONE','NOT APPLICABLE'].includes(String(j.CURRENT_BASELINE_ID))?j.CURRENT_BASELINE_ID:null,productId:j.CURRENT_PRODUCT_ID&&!['NONE','NOT APPLICABLE'].includes(String(j.CURRENT_PRODUCT_ID))?j.CURRENT_PRODUCT_ID:null};}
function responseContractDescriptor(stage,operation){const contract=schema.STAGE_CONTRACTS[stage],op=schema.operationContract(stage,operation);return {schema:schema.RESPONSE_SCHEMA,stage,operation,responseTypes:schema.RESPONSE_TYPES,scopeRequirements:op?.scopeRequirements||contract.scopeRequirements,agentStageFields:contract.allowedStageData,agentWritableCollections:op?.agentWritableCollections||contract.agentWritableCollections,resourceLimits:contract.resourceLimits};}
function responseContract(stage,operation,instructionId,bodySha256,contractSha256,contextSignature,scope){const contract=schema.STAGE_CONTRACTS[stage],op=schema.operationContract(stage,operation),writable=op?.agentWritableCollections||contract.agentWritableCollections,recordShape=Object.fromEntries(writable.map(collection=>[collection,[{tempKey:'response-local-key',targetId:null,fields:Object.fromEntries(schema.recordAgentFields(collection).map(name=>[name,'<value>'])),relationships:{},evidenceRefs:['evidence-1']}]]));return JSON.stringify({schema:schema.RESPONSE_SCHEMA,jobId:'<exact current JOB_ID>',stage,operation,promptIdentity:{instructionId,bodySha256,contractSha256,contextSignature},scope,responseType:'DATA_PROPOSAL',humanInputRequests:[],stageData:Object.fromEntries(contract.allowedStageData.map(name=>[name,'<value>'])),records:recordShape,evidence:[{temporaryKey:'evidence-1',kind:'WORKFLOW_EVIDENCE',description:'Exact evidence supporting proposed values',location:'<source/output location>',content:'<exact evidence or faithful excerpt>'}],unresolved:[],warnings:[],attachments:[]},null,2);}
function body(stage,state,operation){const d=core.STAGES[stage-1],j=state?.job||{},contract=schema.STAGE_CONTRACTS[stage],op=schema.operationContract(stage,operation||contract.operations[0]),fields=contract.allowedStageData.length?contract.allowedStageData.map(x=>`- ${x}`).join('\n'):'- No agent-owned stageData fields; use permitted records/evidence only.',writable=op?.agentWritableCollections||contract.agentWritableCollections,collections=writable.length?writable.map(c=>`- ${c}: ${schema.recordAgentFields(c).join(', ')||'no agent-owned fields'}`).join('\n'):'- NONE';return `COPY BLOCK — STAGE ${String(stage).padStart(2,'0')} — ${d.title}

ROLE
You are the ${d.role}. Perform only Stage ${String(stage).padStart(2,'0')} for this single current project.

PROJECT-SCOPE AND CAPABILITY BOUNDARY
This instruction belongs only to JOB_ID ${j.JOB_ID||'UNKNOWN'}. The project target is treated as not yet existing until Stage 21. The operating application, its repository, source code, UI, stored state, screenshots, prior target versions, generated project artifacts, and other implementations of the same target are never Stage 02 governing sources or Stage 03 requirement authority. This application itself does not connect to or modify external repositories, accounts, deployment targets, or runtimes. An external agent/tool may implement work only when that capability and the necessary authorized access actually exist outside this application. Never claim execution, repository modification, deployment, file creation, browsing, or tool use that did not actually occur.

JOB CONTROL
JOB_ID: ${j.JOB_ID||'UNKNOWN'}
JOB_TITLE: ${j.JOB_TITLE||'UNKNOWN'}
CURRENT_ITERATION: ${j.CURRENT_ITERATION||'NOT APPLICABLE'}
CURRENT_STAGE: STAGE ${String(stage).padStart(2,'0')}
CURRENT_STATE: ${j.CURRENT_STATE||'UNKNOWN'}
NEXT_REQUIRED_ACTION: ${j.NEXT_REQUIRED_ACTION||'UNKNOWN'}
INPUT_VERSION: ${j.CURRENT_INPUT_VERSION||'UNKNOWN'}
SOURCE_SET_VERSION: ${j.CURRENT_SOURCE_SET_VERSION||'NOT APPLICABLE'}
REQUIREMENTS_VERSION: ${j.CURRENT_REQUIREMENTS_VERSION||'NOT APPLICABLE'}
TEST_SUITE_VERSION: ${j.CURRENT_TEST_SUITE_VERSION||'NOT APPLICABLE'}
INSTRUCTION_VERSION: ${j.CURRENT_INSTRUCTION_VERSION||'NOT APPLICABLE'}
BASELINE_ID: ${j.CURRENT_BASELINE_ID||'NOT APPLICABLE'}
PRODUCT_ID: ${j.CURRENT_PRODUCT_ID||'NOT APPLICABLE'}

AUTHORIZED USER JOB INPUT
VERBATIM REQUEST / OBJECTIVE:
${show(j.EXACT_USER_OBJECTIVE_VERBATIM)}

REQUESTED DELIVERABLE:
${show(j.EXACT_DELIVERABLE_REQUESTED)}

SUPPLIED MATERIALS:
${show(j.SUPPLIED_MATERIALS_INVENTORY)}

REQUIRED OUTPUT FORMAT:
${show(j.REQUIRED_OUTPUT_FORMAT)}

TEMPORAL / DEADLINE SCOPE:
${show(j.DEADLINE_OR_TEMPORAL_SCOPE)}

DESIRED / SUGGESTED SOURCE COUNT:
${show(j.DESIRED_SOURCE_COUNT)}

USER-SUPPLIED KNOWN AUTHORITY (classification preserved; do not automatically relabel as external authority):
${show(j.KNOWN_AUTHORITATIVE_SOURCES)}

AVAILABLE / REQUIRED TOOLS:
${show(j.AVAILABLE_TOOLS)}

PROHIBITED ACTIONS:
${show(j.PROHIBITED_ACTIONS)}

EXPLICIT USER REQUIREMENTS:
${show(j.EXPLICIT_USER_REQUIREMENTS)}

AUTHORIZED CONTEXT FOR THIS STAGE AND OPERATION
${contextFor(stage,state,operation)}

SUFFICIENCY AND RECOVERY CHECK — DO THIS BEFORE THE STAGE TASK
Determine whether the combination of User Job Input, current canonical application context, accepted prior-stage work, supplied artifacts, available tools/capabilities, and current-stage evidence is sufficient to perform this stage reliably. What counts as sufficient is stage- and job-dependent: reason about it; do not use a fixed checklist as a substitute for judgment.
- If a required fact, preference, tradeoff, authorization, or decision can only come from the human, return HUMAN_INPUT_REQUIRED with the smallest precise set of questions needed.
- If required canonical application context, prior-stage evidence, authority, an artifact, or a relationship is absent or internally inconsistent, return BLOCKED with the exact missing/defective item and why it prevents reliable work. Do not ask the human to transcribe information the application or an agent should supply.
- If the prior agent result is inadequate, incomplete, contradictory, unsupported, or too shallow for this stage, identify the exact defect and return BLOCKED when it prevents reliable progress; if this prompt contains REQUESTED RESPONSE CORRECTIONS, produce a complete replacement response that addresses every requested correction rather than merely appending commentary.
- If execution or a required tool/capability is unavailable, return EXECUTION_FAILED when an attempted execution failed, otherwise BLOCKED with MISSING_CAPABILITY. Never describe unexecuted work as executed.
- If the requested implementation is too large or requires external-repository/runtime access that is not actually available, do not simulate completion. Produce an implementation-ready specification rather than pretending implementation occurred, using the most complete reliable specification or implementation-ready plan permitted by the current stage and requested deliverable; return BLOCKED if the controlling deliverable cannot legitimately be satisfied that way.
- If sufficient information exists, perform the complete stage task now. Do not ask optional questions that do not materially improve correctness.

STAGE-SPECIFIC TASK
${procedures[stage]}

PERMITTED AGENT-OWNED STAGE DATA
${fields}

PERMITTED RECORD COLLECTIONS AND AGENT-OWNED FIELDS
${collections}

COMPLETION CONDITIONS
${(d.completionGate||[]).map(x=>`- ${x}`).join('\n')}

MANDATORY RESPONSE RULES
- Return exactly one JSON object and no Markdown fence, preamble, or trailing prose.
- Use schema ${schema.RESPONSE_SCHEMA} and echo the exact operation, scope, instructionId, bodySha256, contractSha256, and contextSignature supplied below.
- Use only DATA_PROPOSAL, HUMAN_INPUT_REQUIRED, BLOCKED, or EXECUTION_FAILED as responseType.
- Never assign canonical application IDs, versions, timestamps, counts, hashes, statuses, coverage values, release determinations, current stage/state, actual byte metadata, or other application-owned values. Use temporaryKey and response-local references where relationships are needed.
- Never set a HUMAN or HUMAN_DECISION-owned field. When unavailable human information is required, return HUMAN_INPUT_REQUIRED and structured humanInputRequests. Do not convert a missing human decision into an assumption.
- Include evidence for every agent-produced canonical value that requires provenance.
- Do not include collections or fields outside the current stage/operation contract.
- Preserve TRUE/FALSE/UNKNOWN/NONE/NOT APPLICABLE and SATISFIED/VIOLATED/UNDETERMINED meanings exactly.
- Stage 02 may contain only genuinely independent external governing sources; target-product and repository artifacts are prohibited.
- Stage 03 may research only accepted Stage 02 external governing sources.
- If required authority, evidence, capability, artifact, application context, or decision rule is unavailable, fail closed with the correct structured response type rather than inventing data.
- A rejected response or rejected data is not canonical and must not be reused as accepted project truth.
- Never silently omit requested or controlling context. If the supplied context itself is inconsistent, identify that condition rather than guessing.

RESPONSE ENVELOPE
The application will provide the controlling prompt identity immediately after this hashed instruction body. Echo that identity exactly in promptIdentity.

END HASHED INSTRUCTION BODY`;}
function buildPromptRecord(stageOrDefinition,state,options={}){const stage=Number(stageOrDefinition?.number||stageOrDefinition);if(!Number.isInteger(stage)||stage<1||stage>schema.STAGE_COUNT)throw new Error(`Stage must be 1 through ${schema.STAGE_COUNT}.`);const d=core.STAGES[stage-1],existing=(state?.projectData?.generatedPrompts||[]).filter(x=>Number(x.stage)===stage),activeExisting=existing.filter(x=>!x.invalidatedBy),operation=options.operation||schema.STAGE_CONTRACTS[stage].operations[0];if(!schema.STAGE_CONTRACTS[stage].operations.includes(operation))throw new Error(`Operation ${operation} is not valid for Stage ${stage}.`);const opContract=schema.operationContract(stage,operation),scope=scopeFor(stage,state,options.scope||{}),corrections=correctionRequests(state,stage),contextManifest={stage,operation,scope,readCollections:Object.fromEntries((opContract?.readCollections||schema.STAGE_CONTRACTS[stage].readCollections||[]).map(collection=>[collection,activeRecords(state,collection).map(record=>({id:recordId(record,collection),scope:record.scope||{},contentSha256:record.contentSha256||record.sha256||hash.sha256Value(record.fields||record)}))])),answeredHumanClarifications:(state?.projectData?.humanInputAnswers||[]).map(x=>({stage:x.stage,requestId:x.requestId,answerId:x.answerId,inputVersion:x.inputVersion||state?.job?.CURRENT_INPUT_VERSION||null,answerSha256:hash.sha256Value(x.answer)})),correctionRequests:corrections.map(x=>({rejectedResponseId:x.rejectedResponseId,reasonSha256:hash.sha256Value(x.reason)}))};const contextSignature=hash.sha256Value(contextManifest),bodyText=body(stage,state,operation);if(new TextEncoder().encode(bodyText).byteLength>750000){const error=new Error('The complete controlling prompt context exceeds 750000 bytes. No context was silently omitted. Reduce the authorized project scope or split the job before generating this instruction.');error.code='PROMPT_CONTEXT_LIMIT';throw error;}const bodySha256=hash.sha256Text(bodyText),descriptor=responseContractDescriptor(stage,operation),contractSha256=hash.sha256Value(descriptor),same=activeExisting.find(x=>x.contextSignature===contextSignature&&x.bodySha256===bodySha256&&x.contractSha256===contractSha256&&x.operation===operation),instructionId=same?.instructionId||same?.promptId||`INSTRUCTION-${String(state?.job?.JOB_ID||'UNKNOWN').replace(/[^A-Za-z0-9-]/g,'')}-S${String(stage).padStart(2,'0')}-${String(existing.length+1).padStart(3,'0')}`,identityBlock=`\n\nPROMPT IDENTITY — ECHO EXACTLY\nINSTRUCTION_ID: ${instructionId}\nBODY_SHA256: ${bodySha256}\nCONTRACT_SHA256: ${contractSha256}\nCONTEXT_SIGNATURE: ${contextSignature}\nOPERATION: ${operation}\nPROJECT_REVISION: ${scope.projectRevision}\n\nSTRICT RESPONSE CONTRACT\n${responseContract(stage,operation,instructionId,bodySha256,contractSha256,contextSignature,scope)}\n\nEND COPY BLOCK — STAGE ${String(stage).padStart(2,'0')}`,prompt=bodyText+identityBlock;return {instructionId,promptId:instructionId,stage,operation,role:d.role,bodySha256,sha256:bodySha256,contractSha256,contextSignature,contextManifest,scope,scopeSha256:hash.sha256Value(scope),prompt,fullTextSha256:hash.sha256Text(prompt)};}
function build(stageOrDefinition,state,options){return buildPromptRecord(stageOrDefinition,state,options).prompt;}
core.buildStagePrompt=build;
globalThis.closedLoopPromptEngine=Object.freeze({version:'closed-loop-prompt-engine/5',build,buildPromptRecord,procedures,contextCollections,contextFor,scopeFor,responseContractDescriptor,responseContract});
})();