import fs from 'node:fs';
const edit=(path,from,to,label)=>{const s=fs.readFileSync(path,'utf8'),i=s.indexOf(from);if(i<0)throw new Error('Missing '+label);if(s.indexOf(from,i+from.length)>=0)throw new Error('Non-unique '+label);fs.writeFileSync(path,s.slice(0,i)+to+s.slice(i+from.length));console.log('patched '+path+' '+label);};
edit('workflow-schema.js',"required:['TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','EXECUTABLE_KIND','INPUTS','TOOLS','PROCEDURE','EXPECTED_RESULT','FAILURE_CONDITION','EVIDENCE_TO_PRESERVE','STATUS']","required:['TEST_TYPE','EXECUTION_MODE','REQUIRED_CAPABILITY','ARTIFACT_REQUIREMENTS','INPUTS','TOOLS','PROCEDURE','EXPECTED_RESULT','FAILURE_CONDITION','EVIDENCE_TO_PRESERVE','STATUS']",'keep Test IR optional for non-native tests');
edit('verify-complete.mjs',"assert(engine.applicationTestCapabilities().length===0,'Unexpected application-native executor registration.');","assert(JSON.stringify(engine.applicationTestCapabilities())===JSON.stringify(['CLOSED_LOOP_TEST_IR']),'The only registered application-native executor must be the subject-neutral Closed Loop Test IR.');",'native executor expectation');
